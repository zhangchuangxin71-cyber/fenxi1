# videoaudiotext Flow B API
# Install runtime:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Install + test:   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
# System:           ffmpeg (apt/brew)

# --- Runtime (matches pyproject.toml [project.dependencies]) ---
jieba>=0.42.1
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
oss2>=2.18.0
fonttools>=4.0

# --- Optional (install manually when needed) ---
# ASR subtitle force-align: pip install openai-whisper  (+ SUBTITLE_ASR_ALIGN=1)
