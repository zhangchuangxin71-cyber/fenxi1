# 大模型与知识库项目实现详解

> 分析范围：当前工作区中与文档解析、知识库、RAG 检索、大模型问答、报告生成和公众号文章生成直接相关的项目。  
> 分析方式：基于源码、配置、数据库脚本、接口模型和测试进行静态分析；未连接真实 PostgreSQL、对象存储、Ark、MinerU 或联网搜索服务执行端到端请求。  
> 分析日期：2026-08-25。

---

## 1. 先说结论：这些文件夹共同实现了什么

这批代码不是几个完全独立的演示，而是一套逐步演进的“文档知识库 + 大模型内容生产平台”。主流程是：

```text
本地文件/OSS 文档
        │
        ▼
文档摄取 repo-doc-ingestion ──调用──> MinerU
        │                           PDF/OCR/版面/表格解析
        ▼
PostgreSQL
documents + document_bindings + doc_nodes + doc_pages
        │
        ▼
rag-retrieval-service
查询分类 → 文档路由 → 树节点导航 → 页级取证 → 预算合并
        │
        ├──────────────┬─────────────────────┐
        ▼              ▼                     ▼
rag-knowledge-chat  rag-report-agent   wechat-article-agent
知识问答            问答/大纲/报告       调研/审核/文章/配图/HTML
```

其中存在三组版本关系：

1. `pageindex-rag-master-*` 是早期的一体化原型，包含文档解析、树索引、PostgreSQL 存储、BM25 文档初筛和问答 Agent。
2. `repo-doc-rag-agent-aggregation-dev-*/.../src` 是目前更完整的服务化实现，应该作为学习和理解现状的主要依据。
3. 根目录的 `rag-report-agent-master-*` 是较旧的报告 Agent 快照；聚合仓库 `src/rag-report-agent` 增加了远程检索客户端、错误处理、配置和测试，是更接近当前主线的版本。

这套知识库的核心特点是：**它不是以向量数据库为中心的传统 embedding RAG，而是以“文档目录树 + 页内容 + LLM 路由”为中心的 PageIndex 风格 RAG。** BM25/关键词主要用于文档级预筛，最终证据仍以原始页为单位返回。代码里虽然保留了 `semantic/hybrid/keyword` 等请求字段，但当前核心实现并没有看到独立向量库或 chunk embedding 主链路。

---

## 2. 相关目录与职责

为避免重复很长的目录名，本文使用以下简称：

```text
OLD_PAGEINDEX = pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b
OLD_REPORT    = rag-report-agent-master-d60e2231b31cb4052e4656017e0a62cba8269174
AGG           = repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/
                repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e
```

| 目录 | 实际职责 | 是否主线 |
|---|---|---|
| `OLD_PAGEINDEX` | 一体化 PageIndex 原型：PDF/Markdown 解析、树索引、存储和问答 | 原理学习非常重要，但不是当前服务化主线 |
| `AGG/src/MinerU` | 上游文档解析引擎，负责 PDF、图片、Office 等转 Markdown/JSON | 主线依赖 |
| `AGG/src/repo-doc-ingestion` | 文档下载、解析、结构化、入库、任务管理、回调和原始数据修复 | 主线 |
| `AGG/src/rag-retrieval-service` | 查询理解、多文档路由、树导航、页级检索、Token 预算与证据合并 | 主线 |
| `AGG/src/rag-knowledge-chat` | OpenAI 兼容的流式知识问答服务 | 主线 |
| `AGG/src/rag-report-agent` | 问答、大纲、报告撰写和报告编辑 Agent | 主线 |
| `AGG/src/wechat-article-agent` | 有状态、可人工审核的公众号文章生产 Agent | 主线 |
| `OLD_REPORT` | 报告 Agent 的旧独立快照 | 仅用于版本对照 |

`multimodal-retrieval-master-*` 也涉及检索和 embedding，但它主要服务于图像/视频多模态检索，不在本次“文档知识库主链路”的重点范围内。

---

## 3. 整体架构与服务关系

### 3.1 容器和端口

聚合仓库顶层 Compose 将系统拆成 7 个容器、6 类业务能力：

| 容器/服务 | 默认端口 | 作用 |
|---|---:|---|
| MinerU API | 8135 | 文档版面、OCR、表格、公式和阅读顺序解析 |
| repo-doc-ingestion | 8100 | 异步摄取文档并写 PostgreSQL |
| rag-report-agent | 8115 | 报告 Agent SSE 接口 |
| rag-retrieval-service | 8120 | 内部统一检索 API |
| rag-knowledge-chat | 8130 | OpenAI 兼容知识问答 SSE |
| wechat-article-adapter | 8140 | 公众号 Agent 的公开接口和请求治理 |
| wechat-article-runtime | 8141（容器内） | 私有 LangGraph Agent Server |

PostgreSQL 是外部依赖，Compose 本身没有创建数据库容器。服务间主要通过 HTTP 连接，数据面通过 PostgreSQL 共享。

### 3.2 服务依赖

```text
repo-doc-ingestion ──> MinerU
repo-doc-ingestion ──> PostgreSQL

rag-retrieval-service ──> PostgreSQL

rag-knowledge-chat ──> rag-retrieval-service ──> PostgreSQL
rag-report-agent   ──> rag-retrieval-service ──> PostgreSQL

wechat adapter ──> wechat runtime ──> rag-retrieval-service
       │                   ├─────────> PostgreSQL checkpoint/artifact
       │                   ├─────────> LLM
       │                   ├─────────> 可选 Web Search
       │                   └─────────> Seedream 图片生成
       └──负责限流、串行化、恢复、取消和 SSE 规范化
```

### 3.3 为什么拆成这些服务

- 摄取和检索解耦：解析 Office/PDF 很重，不能阻塞在线问答。
- 检索和内容生成解耦：知识问答、报告、公众号文章共享同一份证据服务。
- 无状态问答与有状态工作流分开：普通聊天可由客户端携带历史；复杂文章生产需要检查点、版本和人工审核。
- 原始文档内容与用户绑定分开：相同内容可复用解析结果，避免重复解析和存储。

---

## 4. 最底层原理：旧版 PageIndex 如何建立树索引

旧版代码是理解整套系统设计的最佳入口。主要文件：

- `OLD_PAGEINDEX/app/pageindex/client.py`
- `OLD_PAGEINDEX/app/pageindex/page_index.py`
- `OLD_PAGEINDEX/app/pageindex/page_index_md.py`
- `OLD_PAGEINDEX/app/pageindex/postgres_store.py`
- `OLD_PAGEINDEX/app/pageindex/retrieve.py`
- `OLD_PAGEINDEX/app/assistant_components.py`
- `OLD_PAGEINDEX/app/qa/agent_mode.py`
- `OLD_PAGEINDEX/app/qa/langgraph_adapter.py`

### 4.1 PDF 建索引过程

`PageIndexClient.index()` 为文档生成 UUID，识别 PDF 或 Markdown，然后调用对应解析器。

PDF 的逻辑不只是固定长度切块，而是：

1. 读取 PDF 每页文本。
2. 检测 PDF 中是否存在目录页。
3. 提取和规范化目录内容。
4. 推断目录页码和 PDF 实际页码的偏移。
5. 如果没有可靠目录，则按 Token 规模对页面分组并让模型生成层级目录。
6. 验证目录项、标题和页码是否一致，失败时重试和修复。
7. 对过大的节点递归拆分。
8. 将目录项转换成包含父子关系、标题、起止页、摘要和文本的树。
9. 生成整篇文档的描述，供后续文档路由使用。

源码中的关键函数包括：

```text
find_toc_pages
extract_toc_content
detect_page_index
process_no_toc
verify_toc
process_large_node_recursively
tree_parser
page_index
```

这意味着系统先建立“文档地图”，查询时先判断应进入哪个章节，再取具体页面。与把全文切成 500 Token 后直接做向量 Top-K 相比，它更适合结构清晰、篇幅很长的报告、制度和论文。

### 4.2 Markdown 建树过程

`page_index_md.py` 的基本思路是：

1. 读取 Markdown 标题级别。
2. 根据 `#`、`##`、`###` 等层级构建父子节点。
3. 将文本行映射为逻辑页面，记录每个节点的 `start_index`、`end_index`。
4. 统计节点 Token 数。
5. 对树进行瘦身或拆分，避免单节点过大。
6. 可选生成节点摘要和文档描述。

得到的树节点通常包含：

```json
{
  "node_id": "...",
  "title": "章节标题",
  "level": 2,
  "start_index": 12,
  "end_index": 18,
  "summary": "章节摘要",
  "text": "章节正文",
  "nodes": []
}
```

### 4.3 为什么节点和页面要同时存

- 节点树负责导航：回答“去哪里找”。
- 页面负责取证：回答“原文具体说了什么”。
- 节点摘要适合低成本路由，但不能替代原始证据。
- 最终引用页内容能减少“摘要二次失真”。

旧版保存 PDF 时会从节点 JSON 中移除冗余正文，因为正文已经存在 `doc_pages`；读取时再按需加载重字段。这是降低 JSONB 体积和内存占用的设计。

### 4.4 旧版存储模型

旧版 PostgreSQL 主要有三张表：

```text
documents
  doc_id UUID PK
  user_id, session_id
  name, type, description
  status, is_temporary
  page_count, node_count ...
  raw JSONB

doc_nodes
  doc_id FK
  node_id, parent_id, sibling_order, level
  title, summary, text
  start_index, end_index, child_count
  UNIQUE(doc_id, node_id)

doc_pages
  doc_id FK
  page_number, content
  UNIQUE(doc_id, page_number)
```

写入时将嵌套树展平到 `doc_nodes`，读取时根据 `parent_id` 和顺序重建树。文档、节点和页面在事务中写入；更新文档时会替换节点和页面。

### 4.5 旧版问答如何检索

旧版有两种思路。

第一种是固定 Pipeline：

```text
rewrite_query
  → select_documents
  → collect_evidence
  → generate_answer
  → update_memory
```

第二种是工具调用 Agent：

```text
list_documents
get_document
get_document_structure
get_page_content
```

Agent 先看文档列表和描述，再看结构，最后按页码读取原文。`get_document_structure` 会去掉大段正文，只把标题、页码和摘要给模型，节省上下文。

### 4.6 BM25 在这里到底做了什么

`assistant_components.py` 使用 `rank_bm25`，但语料是：

```text
文档名称 + 文档描述 + 文件名
```

它不是对所有页面做 BM25。中文分词策略也比较轻量：中文单字和相邻双字，英文和数字按连续块。因此 BM25 的作用只是从很多文档中快速找候选，之后仍由 LLM 判断文档和页面。

如果 BM25 依赖不可用，代码会退化成 Token 重叠计分；再失败则选择前几个文档，保证链路仍可运行。

---

## 5. 生产版文档摄取服务如何实现

主目录：`AGG/src/repo-doc-ingestion`。

### 5.1 对外 API

主要接口包括：

```text
POST   /documents/ingest
POST   /documents/temp-ingest
POST   /internal/documents/raw/repair
GET    /documents/{doc_id}/status
GET    /tasks/{task_id}
DELETE /documents/{doc_id}
DELETE /sessions/{session_id}/temp-documents
GET    /health...
```

普通摄取请求支持单文件，也支持 `documents[]` 批量输入。关键字段有：

- `user_id`、`kb_id`：用户和知识库范围。
- `oss_key`、`file_name`、`file_type`：对象存储文件信息。
- `idempotency_key`：调用方重试时避免创建重复任务。
- `is_temp`、`session_id`：临时会话文档。
- `callback`：异步完成通知。
- `config`：OCR、语言、表格/图片、树深度、节点 Token、摘要等解析参数。

任务状态依次为：

```text
queued → parsing → extracting → structuring → storing → completed
                                                     └→ failed
```

### 5.2 任务队列和并发

`IngestionService` 在进程内维护普通优先级和高优先级队列：

- 普通 worker 处理正式知识库文档。
- 临时文档进入高优先队列，由专门 worker 尽快处理。
- `max high burst` 防止高优先任务持续占满所有执行机会。
- 解析阶段带超时、心跳和进度上报。
- 任务和事件写入数据库，进程重启后仍能查询历史状态。

这是一个应用内异步队列，不等同于 Kafka/Celery。学习时要注意：多实例部署是否会重复捞任务、队列恢复策略如何实现，需要结合部署约束和任务表状态一起验证。

### 5.3 一次摄取的完整调用链

```text
1. API 校验请求
2. 解析或计算目标 doc_id / 内容指纹
3. 检查幂等键和重复文档
4. 创建 ingestion_tasks 记录
5. 推入普通或高优先队列
6. worker 创建任务临时目录
7. 从 OSS 下载源文件
8. 选择 MinerU 或本地解析器
9. 将解析结果规范化为 PageIndex 文档树、节点和页
10. 事务写入 documents/doc_nodes/doc_pages/document_bindings
11. 更新任务状态和事件
12. 回调调用方；失败回调可重试或进入 DLQ
13. finally 清理临时目录
```

承担核心编排的是 `DocumentAssistantRunner`；把各种解析结果转换成统一 PageIndex 数据结构的是 `DocumentAssistantAdapter`。

### 5.4 支持的文档类型

生产版不再只支持 PDF/Markdown，还包含：

- PDF
- DOC/DOCX
- Markdown/TXT
- HTML
- XLS/XLSX
- PPT/PPTX

Office 文档可借助 LibreOffice 转换；PDF 可使用 PyMuPDF/PyPDF2，并可调用 Ark 视觉模型补充表格/OCR。MinerU 可作为更强的统一解析后端。

### 5.5 MinerU 在系统中的位置

MinerU 负责“把复杂文件变成可理解的结构化内容”，不是负责检索或回答。它可处理版面、OCR、表格、公式和阅读顺序，并输出 Markdown/JSON。

摄取服务的 MinerU Adapter 支持：

- 同步或异步 API。
- 轮询异步任务。
- 并发限制。
- 超时、退避和重试。
- 失败时根据配置退回本地解析器。
- 对超长 Markdown/TXT/HTML 叶子节点做弱标题拆分。

系统会保留精简后的原始解析数据：

```text
raw.raw_mineru.md_content
raw.raw_mineru.content_list
raw.raw_mineru.middle_json
```

模型原始响应、裁剪图和 Base64 大对象不会全部塞进主文档 JSON，以免数据库快速膨胀。

### 5.6 生产版数据模型的关键改造

生产版把“内容”和“归属”拆开了：

```text
documents                         document_bindings
┌─────────────────────┐          ┌──────────────────────────┐
│ doc_id UUID PK      │ 1      N │ doc_id                   │
│ name/description    │──────────│ user_id                  │
│ status/raw/counts   │          │ kb_id                    │
└─────────────────────┘          │ session_id               │
          │                      │ is_temporary             │
          │ 1                    │ PK(doc_id,user_id,kb_id) │
          │                      └──────────────────────────┘
          ├──── N doc_nodes
          └──── N doc_pages
```

含义是：

- `documents` 表示一份共享的内容实体。
- `document_bindings` 表示哪个用户、哪个知识库或临时会话可以使用它。
- 相同内容可以共享解析产物，而不用每个用户重新 OCR 和建树。
- 删除文档时先删除绑定；只有没有任何绑定后，才删除共享内容。

任务另存为：

```text
ingestion_tasks
ingestion_task_events
```

`documents.raw` 也经过瘦身：页正文和节点正文以关系表为准，JSONB 主要留元数据和必要的 MinerU 原始结构。

### 5.7 内容 ID、幂等与重复处理

生产代码会在提交任务前解析目标文档 ID/内容指纹。由内容派生 ID 的好处是：

- 重试同一个文件容易命中同一内容。
- 临时文档重复上传时可直接复用。
- 共享内容和用户绑定自然分离。

正式文档重复时可能返回冲突，而缺少 `raw_mineru` 的旧文档可以进入修复流程。

### 5.8 原始 MinerU 数据修复为什么复杂

修复接口不是简单覆盖 JSON。实现包含：

1. 在 `documents.raw` 中原子声明修复权。
2. 记录 `repair_id`、状态、尝试次数、租约和冷却时间。
3. 防止两个 worker 同时重复修复。
4. 重新下载并解析源文件。
5. 验证内容仍映射到同一个 `doc_id`。
6. 只补 `raw.raw_mineru`，不改现有节点、页面和绑定。
7. 记录可重试性和最后错误，并限制最大尝试次数。

这个设计保护了已经可检索的知识库结构，避免一次“补原始数据”意外改变在线问答结果。

---

## 6. 检索服务如何实现：这套系统最核心的部分

主目录：`AGG/src/rag-retrieval-service`。

### 6.1 API 和输入

主要接口：

```text
POST /rag/v1/retrieve
POST /rag/v1/documents/meta
POST /rag/v1/documents/route
POST /rag/v1/documents/raw
GET  /rag/v1/documents/raw/status
GET  /healthz
GET  /readyz
```

检索请求包含：

- `user_id`、`kb_id`、`session_id`。
- 正式 `doc_ids` 和临时 `temp_doc_ids`。
- 单个查询或查询数组。
- `top_k` 和最大返回 Token。
- `search_mode`：`semantic`、`hybrid`、`keyword`；部分值当前是兼容/预留语义。
- 是否确保文档覆盖、是否返回文档元数据和 debug。

### 6.2 LangGraph 检索图

核心图在 `app/graph/builder.py`，可概括为：

```text
START
  │
  ▼
校验请求并加载文档范围
  │
  ▼
查询分类
  │
  ├─只有一篇文档：跳过多文档路由
  │
  └─多篇文档：关键词预筛
                 │
                 ├─候选过多且无法可靠缩小：返回需要澄清
                 └─LLM 多文档路由
  │
  ▼
核心访问 scope/direct/focused
  │
  ▼
Token 预算检查
  │
  ├─预算允许：补查 possible 文档
  └─预算不足：跳过
  │
  ▼
Broad Retrieval 补充覆盖
  │
  ▼
去重、覆盖保护、排序、截断
  │
  ▼
END
```

### 6.3 四类查询访问模式

分类器把查询映射到四种实际访问方式：

| 类型 | 典型问题 | 实现方式 |
|---|---|---|
| `scope_direct` | “库里有哪些文件”“这份文件叫什么” | 直接返回文档元数据 |
| `routed_direct` | “第 5 页是什么”“给我目录”“第三章摘要” | 规则/LLM 规划后直接取页、目录、节点摘要 |
| `routed_focused` | “赔偿条件是什么”“作者如何定义风险” | 树节点导航并检查具体页，寻找精确证据 |
| `routed_broad` | “总结全文”“比较三份材料” | 规则化覆盖各章节并在文档间轮询取样 |

这种分类很重要：不同问题不应该统一走一个向量 Top-K。问目录时直接读目录；问全文总结时要覆盖全文；问具体事实时才做精确导航。

### 6.4 查询分类的两套策略

默认稳健策略会拆成多个小判断：

- 查询改写和上下文指代解析。
- 是否是 scope 查询。
- 是否是 direct 查询。
- 是否需要 focused 检索。
- 多问题和多文档分组。

每个模型结果都使用严格 JSON Schema，失败时有规则回退。

快速策略则用一次 LLM 分类完成大部分判断，降低延迟，但错一个字段会影响更多后续路径。因此代码同时保留规则策略和 fallback。

### 6.5 多文档路由

当候选文档很多时，流程不是把所有文档结构全部发给模型：

1. 先用文件名、描述和关键词做廉价预筛。
2. 候选仍过多时，用 IDF/锚点词做第二轮缩小。
3. 对候选按预计 Token 用 LPT 思路分组，尽量平衡每个 LLM 批次。
4. LLM 只输出 `accept` 或 `possible`；未返回的候选视为 `reject`。
5. 如果大量文件名字和描述都非常相似，系统宁可要求用户澄清，也不盲选错误文件。

### 6.6 Focused Search：树导航找精确证据

Focused Search 的核心不是相似度检索，而是受约束的树扫描：

```text
文档描述
  → 顶层目录节点
  → LLM 选择相关节点
  → 展开子节点
  → 确定节点页码范围
  → Page Inspector 阅读原始页
  → accept / possible / reject
```

节点只作为导航工具，最终公开返回的 chunk 仍来自页面。这样既保留结构推理，又让引用落到原始内容。

如果某份文档没有可用树结构，代码会回退到页面扫描，避免整篇文档完全不可检索。

### 6.7 Direct Retrieval

明确提到页码、章节、目录、摘要的问题，会先用规则规划器识别：

- 页码范围。
- 章节标题。
- 目录请求。
- 文档级摘要。

复杂表达可再用 LLM 解析。此路径减少无意义的树遍历，也避免“用户要第 5 页，系统却返回语义相似的第 18 页”。

### 6.8 Broad Retrieval

Broad Retrieval 不依赖 LLM，主要是确定性规则：

1. 尽量排除封面、目录、序言等低信息页面。
2. 从每个主要章节选择代表页。
3. 没有可靠结构时按全篇均匀间隔采样。
4. 用问题关键词对剩余页面做排序。
5. 多文档和多查询组之间轮询取页，避免第一篇文档吃光预算。

这适用于全文总结和跨文档比较。它不保证读取全文，但努力保证“覆盖面”而非只取局部相似页。

### 6.9 Token 预算与合并

检索服务使用 Tiktoken 估算 Token。`top_k` 是软限制，不是所有路径的硬上限：

- scope/direct 返回的原子结果可能超过 `top_k`。
- focused 的 `accept` 证据优先。
- `possible` 用来填补信息不足。
- broad 最后补覆盖。
- 合并时做规范化去重。
- 尽可能保留各文档和查询组的最低覆盖。
- 最后按连续内容边界截断到 `max_return_tokens`。

结果中会带覆盖、warning 和 `truncated_by` 等信息，供上层决定是否需要提示用户。

### 6.10 超时与降级

`RetrievalEngine` 给整张图设置总超时，并预留结果收尾时间。若中途已经找到证据，即使后续节点超时，也会尝试返回部分结果；只有完全没有证据时才返回 503/504。

可靠性组件包括：

- 全局公平 LLM 调度器。
- 每请求并发上限。
- RPM/Token Bucket 限流。
- admission queue。
- circuit breaker。
- 各节点超时。
- JSON 解析和规则 fallback。
- 部分结果保留。

Debug Trace 只有在进程配置允许且请求也显式要求时才收集；当前实现不是依赖 LangSmith 才能运行。

### 6.11 必须注意的权限边界

这是本次源码分析发现的最重要风险点之一。

`app/db/repositories.py` 中构造文档范围、页面和元数据查询的函数虽然接收 `user_id`、`kb_id`、`session_id`，但当前 SQL 实际主要只按：

```sql
d.status = 'ready'
AND d.doc_id IN (...调用方显式提供的 doc_ids...)
```

筛选。`document_bindings` 主要通过 lateral join 用于补元数据，没有在这些查询中形成严格的用户/知识库隔离条件。

因此当前边界是：**检索服务信任上游传入的 doc_ids 已经通过授权。** 如果外部调用者能猜到其他用户的 UUID，并可直接调用检索接口，就可能形成越权读取风险。

部署时至少需要做到：

- 检索服务不直接暴露公网。
- API Gateway 或上游业务服务先校验用户对每个 doc_id 的绑定关系。
- 更稳妥的修复是在检索 SQL 本身加入 `EXISTS document_bindings` 条件，形成纵深防御。
- 临时文档还应同时验证 `session_id` 和 `is_temporary`。

这不是 PageIndex 算法问题，而是服务化后“身份域”和“内容域”拆分带来的安全责任边界。

---

## 7. 知识问答服务如何实现

主目录：`AGG/src/rag-knowledge-chat`。

### 7.1 对外形态

接口为：

```text
POST /v1/chat/completions
```

它模仿 OpenAI Chat Completions，但只支持流式返回。主要限制：

- `model` 必须是服务约定的别名 `rag-knowledge-chat`。
- `stream=true`。
- 消息角色只接受 user/assistant。
- 最后一条必须是用户消息。
- 总字符数有限制。
- 临时文档必须提供 session。
- `incremental_doc_ids` 必须是当前文档集合的子集。
- `include_usage` 等部分 OpenAI 参数不支持。

### 7.2 一次问答最多两次主要 Ark 调用

普通路径下，Orchestrator 的核心是“先判断是否检索，再生成答案”：

```text
用户历史消息
   │
   ├─从检索服务获取增量/唯一文档的真实文件名
   │
   ▼
第一次 Ark：严格 JSON 路由
   │  输出 needs_retrieval、queries[]、rewrite_query、reason_code
   │
   ├─不需要检索 → 直接通用回答
   │
   ├─指代不清 → 生成澄清问题
   │
   └─需要检索
          │
          ▼
      调用 retrieval-service
          │
          ▼
      证据编号为 [1] [2] ...
          │
          ▼
第二次 Ark：基于证据流式生成答案
          │
          ▼
解析答案中的引用，只返回实际使用的 references
```

### 7.3 为什么先获取真实文件名

用户经常说“总结刚上传的那篇”或“这份和上一份有什么区别”。如果只把 UUID 给 LLM，它无法理解指代。

因此服务会先查询文档元数据，用：

- 增量上传列表。
- 当前只有一篇的文档。
- 真实文件名。
- 完整对话历史。

来解析“这篇、上一份、刚上传”的含义。

### 7.4 多问题拆解

第一次模型调用返回 `queries[]`，而不是只有一个字符串。它会：

- 保留用户所有子问题。
- 将跨文档比较拆成独立事实查询。
- 为每个查询给出检索改写和答案关注点。
- 标记已解析或仍有歧义。

这样可以分别检索 A 文档和 B 文档，再让回答模型做比较，避免用一个混合查询导致只命中其中一篇。

### 7.5 动态检索参数

服务根据问题数量、文档数和问题类型动态决定：

- `top_k`。
- `max_return_tokens`。
- 是否扩大覆盖。

传给检索服务的模式固定使用兼容的 `hybrid`，真正的路由逻辑仍由检索服务内部分类完成。

### 7.6 防止“假装用了知识库”

回答会区分四种依据状态：

| basis | 含义 |
|---|---|
| `knowledge_base` | 有知识库证据，并要求引用 |
| `general_no_retrieval` | 判断无需检索，使用通用能力 |
| `general_no_result` | 查了但没有结果 |
| `general_retrieval_error` | 检索失败，退化为通用回答或错误提示 |

前端可以据此显示明确提示，避免“没有检索到任何材料，却说根据知识库”。

### 7.7 引用和提示词安全

检索证据按 `[N]` 编号，提示词要求只能据证据回答并引用。最终服务解析模型正文中的引用，只把真正使用的来源放进 `references`。

文件名和文档正文都被当作不可信数据，不允许其中的指令覆盖系统提示。这是 RAG 中防 Prompt Injection 的必要边界。

### 7.8 SSE 协议

标准文本放在：

```text
choices[0].delta.content
```

思考内容可放 `reasoning_content`；RAG 扩展字段在顶层传输：

- status
- references
- chunks
- debug
- error

最后发送 `[DONE]`。一旦 HTTP 流已经开始，中途错误无法再改 HTTP 状态码，因此错误也必须编码成 SSE 事件，HTTP 仍可能是 200。

### 7.9 状态特点

该服务本身基本无会话持久化：完整历史由客户端每次传入。`session_id` 主要用来限定临时文档，而不是服务端聊天记忆。这使水平扩容简单，但客户端必须正确保存消息历史。

---

## 8. 报告 Agent 如何实现

主目录：`AGG/src/rag-report-agent`。根目录 `OLD_REPORT` 是较旧快照。

### 8.1 接口和状态输入

主要接口：

```text
POST /agent/v1/chat/completions
```

请求除了 query/history/doc IDs，还会带：

- `current_outline`
- `confirmed_outline`
- `current_report`
- retrieval config
- generation config
- 图片生成相关凭据或配置

它没有像公众号 Agent 那样使用数据库 checkpointer。报告、大纲和历史需要调用方在后续请求中带回来，所以更接近“客户端持有工作状态”。

### 8.2 LangGraph 主流程

```text
START
  │
  ├─明显闲聊且无文档 → intent
  └─有文档/知识问题 → query_rewrite → doc_router → retrieve → intent
                                                          │
                                                          ▼
                                                    task_dispatch
                       ┌─────────────┬────────────┬────────────┐
                       ▼             ▼            ▼            ▼
                      QA          生成大纲      撰写报告      编辑报告
```

意图类型包括：

```text
general_qa
knowledge_qa
report_outline
outline_modify
report_write
report_edit
```

路由先用规则识别明显情况，再用快速模型分类。它支持多意图，因此一次请求可能先回答问题，再继续执行报告任务。

### 8.3 查询改写

查询最多拆成 4 个结构化改写，每个包含：

```text
id
question
retrieval_query
answer_focus
requires_retrieval
profile = precise | overview | report | compare
```

`profile` 会影响检索覆盖和 Token 预算：精确问答取少而准的证据，报告/比较则扩大覆盖。

### 8.4 文档路由

文档路由先用文件名字符 n-gram、当前文档、最新文档和“报告相关”提示做候选打分，再让 LLM 根据元数据选择。正式和临时文档分别维护，避免作用域混淆。

### 8.5 远程检索和二次相关性判断

当前聚合仓库版本通过 HTTP 调用 `rag-retrieval-service`。旧版直接查 PostgreSQL 的 retriever 仍有保留，但不是新主线。

每个 query group 可单独调用检索：

1. 根据 profile 选择 top_k 和文档覆盖。
2. 根据查询、文档数和 broad 标记选择 4K/8K/16K/32K 的远程返回预算。
3. 过滤位置与问题明显不匹配的 chunk。
4. 使用额外 LLM 做相关性判定。
5. 合并和去重。
6. 如果无证据，记录 `rag_fallback`，生成时明确降级。

### 8.6 生成节点

- QA：使用检索 chunk，要求引用；没有证据时明确声明或走通用回答。
- 大纲：流式生成文本，同时解析为结构化 outline，发送增量和完成事件。
- 报告：要求已有/已确认大纲，按大纲生成长文本，可规划图片并插入。
- 大纲修改：基于当前大纲和用户修改意见重写。
- 报告编辑：基于当前报告做局部或整体修改。

报告生成允许更大的输出 Token，通常至少 8192。引用只保留模型正文中真正使用的 chunk ID。

### 8.7 SSE 事件

它不是纯 OpenAI delta，而是面向 Agent UI 的事件流：

```text
stream_start
step
intent
text_delta / thinking_delta
outline_delta / outline_complete
report_start / report_text_delta / report_end
references
error
stream_end
```

前端可以用 `step` 展示“正在选择文档、检索、生成大纲”等过程，用专门事件分别更新聊天区、大纲编辑器和报告编辑器。

---

## 9. 公众号文章 Agent 如何实现

主目录：`AGG/src/wechat-article-agent`。这是整套系统中状态管理和人工协作最完整的部分。

### 9.1 为什么分为公开 Adapter 和私有 Runtime

```text
客户端
  │
  ▼
Adapter :8140
协议校验、限流、同会话串行、response_id、取消、SSE 规范化
  │
  ▼
LangGraph Runtime :8141
图执行、checkpoint、interrupt/resume、模型和工具调用
```

拆分后的 Adapter 是稳定的公共边界，Runtime 可以专注 LangGraph 语义。Runtime 不应直接对公网暴露。

### 9.2 对外接口

```text
POST /v1/responses
POST /v1/responses/{response_id}/cancel
GET  /health/live
GET  /health/ready
```

请求接近 OpenAI Responses API，包含 `input` 消息、上下文和 `stream=true`。HITL 恢复请求还需要：

- `previous_response_id`
- `interrupt_id`
- `approve | revise | regenerate`
- 可选澄清选择或冲突处理选择

这样能检查客户端提交的审核操作是否对应当前等待中的 interrupt，防止旧页面误操作新任务。

### 9.3 完整工作流

简化后的图如下：

```text
START
  ▼
intent_router
  ├─非公众号任务 → non_wechat
  ├─信息不足 → clarification interrupt
  └─公众号任务
       ▼
start_planning
  ├─读取 session memory
  └─orchestrator 选择从哪个阶段进入
       ▼
entry_dispatch
  ├─docs_research
  ├─task_spec
  ├─outline
  └─article

docs_research:
  加载文档元数据
    → 材料充分性判断
    → 可选 Web 概览
    → 规划调研方向
    → 用户选择调研方向 interrupt
    → 文档材料 worker ┐
    → 网络材料 worker ┘ 并行
    → 材料标准化
    → 压缩到预算
    → 冲突检查
    → 如有冲突，用户裁决 interrupt
    → 材料定稿

生成阶段:
  task spec 生成 → 用户审核 interrupt
  outline 生成   → 用户审核 interrupt
  article 生成   → 用户审核 interrupt
  image 生成
  HTML 排版
  END
```

### 9.4 Orchestrator 与增量重做

用户修改需求时，不必每次从头调研。Orchestrator 会：

1. 找到最近一个已批准的祖先 revision。
2. 比较新输入与已有产物。
3. 让 LLM 判断应该从调研、任务书、大纲还是文章阶段重新进入。
4. 判断失败时采用更保守的 `docs_research`。
5. 只继承进入点之前已经批准的字段。

例如只要求“标题更有冲击力”，可以从文章或大纲阶段重做；如果换了主题或新增关键材料，则回到调研阶段。

### 9.5 人工审核 HITL

工作流中有多类 interrupt：

- 公众号意图澄清。
- 调研方向选择。
- 材料冲突裁决。
- 任务书审核。
- 大纲审核。
- 文章审核。

LangGraph 节点返回表单描述并暂停。客户端再次请求时，Adapter 校验 `response_id + interrupt_id`，Runtime 使用 `Command` 从 checkpoint 恢复，并按 approve/revise/regenerate/cancel 跳转。

如果用户不点击按钮而直接输入一句话，Adapter 还会用 LLM 将自由文本分类为：

```text
replay / approve / revise / regenerate / supersede
```

低置信度时不擅自执行，而是重新展示审核卡片。

### 9.6 文档原始数据和自动修复

公众号长文需要的不只是页级 QA chunk，还可能需要完整文档材料。因此它会调用检索服务的：

- 文档 metadata。
- raw document。
- raw repair status。

如果 raw 接口返回 202，说明旧文档缺少 MinerU 原始结果并已触发修复。Agent 会轮询修复状态，完成后重新获取，而不是马上把“正在修复”当作空文档。

### 9.7 材料库、压缩和冲突

文档材料和可选网络材料可并行采集，之后统一成 material library。每项材料带：

- 来源。
- 证据文本。
- 主题或标签。
- 可引用位置。
- 可信度/权威性相关信息。

Compacter 根据总 Token 预算压缩材料，尽量保留覆盖和证据。Conflict Checker 检测数字、事实或观点冲突；存在冲突时，不让模型静默挑一个，而是要求用户选择：

- 以知识库文档为准。
- 以更权威来源为准。
- 使用用户自定义决定。

### 9.8 结构化生成

任务书、大纲、文章等阶段使用 Pydantic Schema 约束模型输出，并有解析修复与重试。这样下游不需要从自由文本中猜标题层级、目标读者或章节结构。

文章通过后还可以：

1. 生成图片计划。
2. 调用 Seedream 生成图片。
3. 根据标题路径和段落位置插图。
4. 将 Markdown 渲染成安全 HTML。

排版相关技术包括 Jinja2、MarkdownIt、Bleach 和 TinyCSS2。旧式渲染器会清洗危险 HTML，只允许安全的 HTTP 图片，并按标题路径和段落序号放置 `<figure>`。

### 9.9 两套持久化：Checkpoint 与业务 Artifact

它同时使用两种持久化，不能混为一谈。

#### LangGraph Checkpoint

使用 `AsyncPostgresSaver` 保存图的执行状态，包括当前节点、interrupt 和恢复所需数据。配置有 TTL，默认可见配置约为 4320 分钟，即 72 小时，并定期清理。没有退回 pickle 的不安全序列化方案。

#### article_artifacts

这是业务可查询、可版本化的文章产物表，主要字段有：

```text
artifact_id, session_id, run_id
current_response_id, runtime_run_id
revision, parent_artifact_id
status, current_stage, approved_through_stage
doc_ids, temp_doc_ids, relevant_doc_ids
research_direction, material_library
task_spec, outline, article_markdown
images, final_html
last_error, created_at, updated_at, expires_at
```

每次重要修改创建新的 revision，通过 `parent_artifact_id` 形成版本链。新版本只继承进入点之前已经批准的产物，防止把未审核内容误当成稳定输入。

### 9.10 并发、取消和恢复

- PostgreSQL advisory transaction lock 对同一 session 串行化。
- 条件更新同时检查 response 所有权和当前状态，避免旧请求覆盖新请求。
- 状态包括 running、waiting、cancelling、completed、failed、cancelled、superseded。
- 新输入可以 supersede 旧 revision。
- 取消正在运行的任务时会请求 Runtime cancel。
- 如果任务停在 interrupt，取消会以 cancel 决策恢复图，让它正常走取消分支。
- SSE 客户端断开不会自动取消远端运行，避免手机锁屏或网络抖动毁掉长任务。
- TTL 清理使用 `FOR UPDATE SKIP LOCKED`，允许维护 worker 并发清理而不互相阻塞。

---

## 10. 四条最重要的端到端时序

### 10.1 上传正式知识库文档

```text
业务端 → ingestion: 提交 OSS key、user、kb、幂等键
ingestion → DB: 检查内容/绑定/重复任务
ingestion → queue: 创建异步任务
worker → OSS: 下载
worker → MinerU: 解析 PDF/Office
MinerU → worker: Markdown + content_list + middle_json
worker → PageIndex adapter: 建目录树、页和摘要
worker → DB: 写共享 documents/nodes/pages
worker → DB: 写 user/kb binding
worker → callback: completed 或 failed
```

### 10.2 普通知识库问答

```text
客户端 → knowledge-chat: 历史、问题、doc_ids
knowledge-chat → retrieval meta: 获取真实文件名
knowledge-chat → Ark #1: 是否检索、拆问题、解指代
knowledge-chat → retrieval: queries[]
retrieval → DB: 元数据、树节点、页面
retrieval → LLM/规则: 分类、路由、导航、页判断
retrieval → knowledge-chat: 编号证据
knowledge-chat → Ark #2: 基于证据流式回答
knowledge-chat → 客户端: content + references + [DONE]
```

### 10.3 生成报告

```text
客户端 → report-agent: 问题/任务 + 当前大纲/报告
report-agent → rewrite/router: 拆查询和选文档
report-agent → retrieval: 按 profile 分组检索
report-agent → relevance judge: 二次筛证据
report-agent → intent/task_dispatch
report-agent → LLM: QA/大纲/报告/编辑
report-agent → 客户端: 专用 SSE 事件
客户端保存新大纲/报告，并在下一请求带回
```

### 10.4 公众号文章生产

```text
客户端 → adapter: 新请求
adapter → artifact DB: admit、锁 session、创建 revision
adapter → runtime: 启动或恢复 LangGraph
runtime → retrieval/raw/web: 调研材料
runtime → checkpoint: 暂停等待调研方向
adapter → 客户端: interrupt 卡片
客户端 → adapter: previous_response_id + interrupt_id + 决策
runtime → checkpoint: 恢复
runtime → LLM: task spec → outline → article
每阶段均可暂停审核、修改或重生成
runtime → Seedream/layout: 图片和 HTML
runtime → artifact DB: 保存最终产物和版本
adapter → 客户端: 完成事件
```

---

## 11. 大模型分别被用在什么地方

这套系统不是只在最后一步调用模型，而是把模型用于“理解、路由、检查、生成”四类任务。

| 场景 | 模型任务 | 是否可规则降级 |
|---|---|---|
| PDF 无目录/目录修复 | 生成或校验结构 | 部分可，质量会下降 |
| 节点/文档摘要 | 建索引元数据 | 可关闭 |
| 查询改写 | 消解指代、拆子问题 | 有启发式 fallback |
| 文档路由 | accept/possible/reject | 有关键词预筛和 fallback |
| 树导航 | 选择相关章节 | 无树时可扫页 |
| 页面检查 | 判断页面是否是证据 | 可保留候选/降级 |
| 知识回答 | 根据编号证据回答 | 核心生成步骤 |
| 报告意图 | QA/大纲/撰写/编辑 | 规则优先，模型补充 |
| 证据二次判断 | 去掉位置/主题不匹配 chunk | 可配置降级 |
| 文章调研规划 | 选调研方向和材料策略 | 失败时保守回退 |
| 材料冲突检测 | 发现矛盾 | 需要用户最终裁决 |
| 结构化内容生成 | 任务书、大纲、文章 | Schema + 修复/重试 |
| 自由文本审核判断 | approve/revise 等 | 低置信度回放卡片 |

模型接入主要围绕 Ark/OpenAI 兼容协议，旧原型还出现 OpenAI Agents SDK、LiteLLM 和 LangGraph Adapter。生产服务普遍用严格 JSON Schema/Pydantic 模型约束中间输出，而不是相信自由文本格式。

---

## 12. 技术栈总表

### 12.1 后端与接口

- Python 3.x
- FastAPI
- Uvicorn
- Pydantic / pydantic-settings
- SSE-Starlette 或手写 OpenAI 风格 SSE
- HTTPX/异步 HTTP 客户端

### 12.2 Agent 与大模型

- LangGraph：检索图、报告 Agent、公众号工作流
- LangGraph Agent Server：公众号私有 Runtime
- langgraph-checkpoint-postgres：可恢复状态
- OpenAI 兼容 API / 火山方舟 Ark
- OpenAI Agents SDK：旧版工具调用 Agent
- LiteLLM：旧版多模型适配痕迹
- 结构化输出：JSON Schema + Pydantic

### 12.3 文档解析

- MinerU
- PyMuPDF
- PyPDF2
- LibreOffice
- Markdown 解析
- OCR/视觉模型
- Office、HTML、表格和演示文档适配器

### 12.4 检索和文本处理

- PageIndex 式目录树
- BM25（文档级预筛）
- 关键词/IDF/字符 n-gram
- Tiktoken Token 预算
- 页级证据与引用
- 无独立向量数据库的主链路

### 12.5 数据和并发

- PostgreSQL
- JSONB
- Psycopg / psycopg pool
- 事务、外键、唯一约束
- PostgreSQL advisory lock
- `FOR UPDATE SKIP LOCKED`
- 内容实体与授权 binding 分离

### 12.6 文章排版与测试

- Jinja2
- MarkdownIt
- Bleach
- TinyCSS2
- Playwright
- Pytest
- Ruff
- Mypy
- Docker Compose

---

## 13. 关键设计优点

### 13.1 结构感比普通切块强

目录树能回答章节、页码、全文总结等传统向量 RAG 容易处理不好的问题。

### 13.2 最终证据回到原始页

摘要用于导航，页面用于回答和引用，降低多次摘要造成的事实漂移。

### 13.3 一个检索服务复用给多个 Agent

知识聊天、报告和公众号文章共享证据层，避免各自实现一套不一致的数据库查询。

### 13.4 规则和 LLM 组合

能用规则确定的页码、目录、全文采样不强行调用模型；复杂语义再交给 LLM。这样兼顾成本、延迟和稳定性。

### 13.5 长流程有业务版本和图检查点

公众号 Agent 同时保留运行状态和业务产物版本，适合数分钟、跨多次人工操作的任务。

---

## 14. 当前实现需要特别关注的问题

### 14.1 检索授权不能只信 doc_ids

当前 SQL 没有形成完整 user/kb/session 强过滤。应在数据库查询中加入 binding 验证，并让内部服务只在受控网络开放。

### 14.2 这不是向量语义检索

接口名里出现 `semantic/hybrid` 不代表已经构建 embedding/vector index。若学习目标是向量数据库、ANN、reranker，本项目主链路并不能完整覆盖，需要另学 pgvector/Milvus/FAISS 和 embedding 模型。

### 14.3 LLM 调用次数可能较多

稳健分类、多文档路由、树导航、页面判断和最终生成会叠加调用。系统已有限流、并发、超时和 fallback，但上线前仍要按真实文档规模压测成本和 P95/P99。

### 14.4 进程内队列和调度器有部署约束

摄取队列、检索 LLM scheduler 等部分状态是进程局部的。增加 worker/副本前要确认重复消费、公平性和恢复语义。检索图通常应维持单 worker 进程并由内部协程管理并发，或重构为外部共享调度。

### 14.5 无状态与有状态服务的职责不同

- Knowledge Chat：历史由客户端带入。
- Report Agent：大纲和报告由客户端带入。
- WeChat Agent：数据库 checkpoint + artifact。

前端不能用同一套“只传最后一句话”的方式接三个服务。

### 14.6 原始文档是 Prompt Injection 输入

知识库文件可能包含“忽略系统指令”等恶意文本。Knowledge Chat 已在提示词中建立不可信证据边界，但所有新 Agent 和工具调用都必须保持这一原则，尤其不能让文档内容直接决定工具参数或访问范围。

---

## 15. 推荐学习顺序

### 第一阶段：理解 PageIndex 数据结构

按顺序阅读：

1. `OLD_PAGEINDEX/app/pageindex/page_index_md.py`
2. `OLD_PAGEINDEX/app/pageindex/page_index.py`
3. `OLD_PAGEINDEX/app/pageindex/postgres_store.py`
4. `OLD_PAGEINDEX/app/pageindex/retrieve.py`

目标：自己能画出 node tree、page 和 document 的关系，并解释为什么树只导航、页才是证据。

### 第二阶段：理解一次生产摄取

阅读：

1. `AGG/src/repo-doc-ingestion/app/ingestion/container.py`
2. `AGG/src/repo-doc-ingestion/app/ingestion/agent_adapter.py`
3. `AGG/src/repo-doc-ingestion/app/parser/mineru_adapter.py`
4. `AGG/src/repo-doc-ingestion/app/storage/postgres_store.py`

目标：跟踪一个 OSS 文件从 API 到任务、下载、MinerU、PageIndex payload、事务入库和 callback。

### 第三阶段：吃透检索图

阅读：

1. `AGG/src/rag-retrieval-service/app/graph/builder.py`
2. `app/classification/`
3. `app/document_routing/`
4. `app/focused_search/`
5. `app/broad_retrieval/service.py`
6. `app/merge/service.py`
7. `app/db/repositories.py`
8. `app/service.py`

目标：为 scope/direct/focused/broad 各构造两个问题，手工推演会走哪条边。

### 第四阶段：学习简单 RAG 应用层

阅读 Knowledge Chat：

1. `app/chat/orchestrator.py`
2. `app/chat/prompts.py`
3. `app/chat/evidence.py`
4. `app/integrations/retrieval.py`
5. `app/streaming/openai_sse.py`

目标：理解“路由模型 → 检索 → 回答模型 → 引用过滤”的最小生产闭环。

### 第五阶段：学习多意图内容 Agent

阅读 Report Agent：

1. `app/agent/graph.py`
2. `app/agent/router.py`
3. `app/agent/state.py`
4. `app/agent/nodes/`
5. `app/rag/remote_retriever.py`
6. `app/api/sse.py`

目标：理解一次请求如何按意图选择 QA、大纲、写作或编辑，以及前端如何消费不同事件。

### 第六阶段：学习可恢复的人机协作 Agent

阅读 WeChat Agent：

1. 图构建和 `graph/state.py`
2. interrupt/review 节点
3. Adapter admission 和 Runtime client
4. checkpoint 初始化
5. artifact repository/model
6. material workers 和 conflict checker
7. 图片与 HTML layout

目标：掌握 interrupt/resume、revision、approved ancestor、advisory lock、取消与 supersede。

---

## 16. 建议亲手完成的练习

1. 用一个 20 页带目录 PDF，打印最终 node tree，并核对页码偏移。
2. 构造无目录 PDF，观察模型生成目录及验证失败后的回退。
3. 对同一文件建立两个用户 binding，删除其中一个，确认共享文档仍存在。
4. 为检索服务准备四类问题，记录实际图路径和模型调用次数。
5. 模拟 20 个名字相似的文档，观察 keyword prefilter 的澄清分支。
6. 设置极小 Token 预算，观察 merge 如何保留文档覆盖并截断。
7. 修改 SQL，让所有页面查询强制校验 `document_bindings`，并补越权测试。
8. 在 Knowledge Chat 中测试“这篇、上一份、刚上传”指代。
9. 构造两篇数字冲突的材料，观察公众号 Agent 的冲突 interrupt。
10. 在文章审核等待期间发送新需求，跟踪 revision、supersede 和 checkpoint。

---

## 17. 可以用来检查自己是否真正理解的问题

- 为什么不直接把所有节点正文发给模型？
- 为什么最终 chunk 必须回到页面，而不是只返回节点摘要？
- `top_k` 为什么在 direct/scope 路径不是绝对硬限制？
- 全文总结为什么需要 broad coverage，而不是相似度最高的几页？
- 内容去重和访问授权为什么要拆成 `documents` 与 `document_bindings`？
- 客户端断开 SSE 为什么不一定应该取消后台长任务？
- LangGraph checkpoint 与 `article_artifacts` 为什么不能只保留一个？
- 报告 Agent 为什么比公众号 Agent 更无状态？
- 模型结构化输出失败后，哪些步骤可以规则回退，哪些必须让用户重试？
- 如果未来加入向量检索，应该插在文档预筛、页候选生成还是最终 rerank 的哪一层？

---

## 18. 总结

这批项目已经形成一条相当完整的大模型知识库工程链路：

```text
复杂文档解析
→ PageIndex 树和页级存储
→ 内容复用与用户绑定
→ 查询分类和多文档路由
→ 结构导航与页级取证
→ Token 预算和证据合并
→ 知识问答/报告/公众号文章
→ SSE 流式输出
→ 长任务检查点、人工审核和版本化产物
```

最值得学习的不是某一个 Prompt，而是三个工程思想：

1. **检索方式服从问题类型**：页码、目录、精确事实、全文总结使用不同路径。
2. **模型不独自承担可靠性**：规则预筛、Schema、超时、降级、预算、引用和数据库约束共同工作。
3. **生成流程的状态要显式建模**：短问答由客户端带历史，复杂内容生产则需要 checkpoint、artifact、revision 和 HITL。

如果以源码学习为目标，先从旧版 PageIndex 建树和取页入手，再看生产摄取与检索图，最后进入报告和公众号 Agent，会比直接从最复杂的 LangGraph 工作流开始更容易建立完整认知。
