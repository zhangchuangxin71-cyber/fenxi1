# Media Editor 文案自动成片项目面试题

> 对应项目：`media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6`  
> 配套总览：[项目功能技术栈与学习路线](./项目功能技术栈与学习路线.md)  
> 定位：把口播文案、外部 TTS 音频、图片/视频素材和字幕合成为可发布视频。

---

## 一、项目整体理解

### 1. 请用 3 分钟介绍这个项目

回答要点：

- 输入口播文案，使用 LLM 或规则按视觉画面单元分句。
- 为具象分句生成 Chinese-CLIP 风格素材检索词。
- 接收外部 TTS 生成的逐句 WAV URL，不在本项目内做真实 TTS。
- 下载、调速、补停顿并拼接人声，生成统一时间轴和 SRT。
- 调用方为各句绑定图片或视频素材。
- 使用 FFmpeg 完成裁切、循环、画幅适配、转场、BGM 混音和成片。
- 字幕支持 SRT/ASS、硬字幕/软字幕、中文断行、CPS、安全区、字体和渐进显示。
- FastAPI 将流程拆为 split、audio、visual preview、compose 等异步阶段。

### 2. 完整成片流程是什么？

```text
口播文案
→ LLM/规则视觉分句
→ 生成素材检索词
→ 外部系统逐句 TTS
→ audio/process 下载 WAV、调速、补 gap、拼接
→ 生成 master.wav 和 subtitle.srt
→ 调用方绑定图片/视频素材
→ visual preview 检查画面和字幕
→ compose 裁切/循环/缩放素材
→ 拼接画面并与人声/BGM 混音
→ 根据 SRT + style 生成 ASS
→ 硬字幕烧录或软字幕封装
→ MP4/WAV/ASS 上传 OSS
```

### 3. 为什么把流程拆成多个 API 阶段？

- 分句后用户可能要调整文案和素材检索词。
- TTS 是外部系统，需要中间交付和确认。
- 画面绑定需要人工或其他检索服务参与。
- 字幕预览可在昂贵正式合成前发现问题。
- 每阶段可独立重试、缓存和展示进度。

### 4. 这个项目不包含哪些能力？

- 不包含真正的 TTS 模型或厂商 TTS SDK，只接收音频 URL。
- 不包含完整 CLIP/Milvus 素材库，只生成检索词并接收素材绑定。
- 不等同于长视频自动分镜服务。
- 不提供专业 NLE 的任意多轨时间线编辑界面。

---

## 二、API、工作区和任务状态

### 5. 主要阶段接口有哪些？

```text
POST /api/v1/split/run
POST /api/v1/audio/process/run
GET  /api/v1/subtitle/fonts
POST /api/v1/visual/preview/run
POST /api/v1/compose/run
GET  /api/v1/tasks/{id}
POST /api/v1/tasks/{id}/cancel
```

另有健康、清理和协调状态等维护接口。

### 6. 为什么长阶段采用异步 Job？

- 下载素材、音频处理和 FFmpeg 合成都可能耗时数分钟。
- HTTP 请求只负责提交，客户端轮询状态。
- Job 记录进度、阶段、结果和错误。
- 取消和失败清理可以在后台统一处理。

### 7. Workspace 中保存什么？

- workspace metadata。
- 各阶段 job JSON。
- 分句、音频 revision、素材绑定和渲染配置。
- 下载的源素材和中间 clip。
- master audio、SRT、ASS、预览和最终 deliverables。
- 日志、digest 和取消标志。

它同时承担单机持久化和阶段间数据交换。

### 8. 服务重启后任务如何处理？

- Job 的内存 active registry 会丢失。
- Workspace JSON 和文件仍然存在。
- 启动 reconcile 扫描工作区，将没有实际 worker 的 running 状态修正为失败/中断。
- 已完成产物可继续使用，但正在执行的 FFmpeg 不能真正续跑。

### 9. 如何避免同一 workspace 重复启动 compose？

- 进程内 `_active` 表做快速检查。
- 使用文件系统 `flock` 对 workspace stage 做原子 claim。
- 同时检查磁盘上的 active job。
- 多层检查应对同进程并发和单机多 Uvicorn worker。

### 10. 文件锁方案可以跨多台机器吗？

不一定。它适合共享同一可靠文件锁语义的单机多进程。跨机器和普通对象存储不保证 `flock`，生产集群应改用 PostgreSQL advisory lock、Redis 分布式锁或数据库原子状态更新。

### 11. Job 取消如何保证幂等和竞态安全？

- 已取消再次取消应返回稳定状态。
- 已完成任务通常不能被改成 cancelled。
- Worker 在阶段边界和耗时步骤前后检查取消标志。
- 取消和完成竞争时使用预期状态或锁保护。
- 清理中间文件前确认没有仍在使用它的任务。

### 12. 为什么要使用 digest？

- 文案、音频配置、素材绑定和渲染样式都可以计算稳定摘要。
- 输入没变化时复用中间产物。
- 输入变化时只失效真正依赖它的下游阶段。
- 防止把旧音频或旧素材误用于新文案。

---

## 三、LLM 文案分镜

### 13. LLM 分句的目标是什么？

不是普通语法断句，而是将文案拆成“一个片段对应一次素材检索和一个主镜头”的视觉单元。

### 14. `[V:关键词]` 和 `[NV]` 分别表示什么？

- `[V:...]`：有明确可拍画面，附带 2～3 个视觉检索词。
- `[NV]`：纯情绪、抽象哲理、感叹或互动，没有独立画面。
- 后处理会把 `[NV]` 合并到邻近可视化单元，避免生成无素材短片。

### 15. 为什么 Prompt 强调“不得改写原文”？

- TTS、字幕和素材必须对应同一段文字。
- LLM 润色会导致前后阶段文本不一致。
- 字幕无法与外部 TTS 一一匹配。
- 因此模型只允许插入标记，不应增删正文。

### 16. 如何验证 LLM 输出的文本保真度？

- 去掉所有 `[V]`/`[NV]` 标记后与原文比较。
- 先检查精确拼接或去空白后相等。
- 再用去句读后的 SequenceMatcher 比较语义核心。
- 同时约束长度比例，防止模型大量增删内容。
- 低于阈值则拒绝模型结果并回退规则分句。

### 17. 为什么不能简单按逗号“一逗一镜”？

- 逗号可能只是换气或同一动作的结果补充。
- 主谓宾不能被拆断。
- “一边……一边……”等固定对举通常属于同一镜头。
- 不同具象主体的枚举即使都用逗号，也应该拆开。

### 18. 后处理如何判断两个短段是否合并？

考虑：

- 是否存在句末硬边界。
- 当前段是否以续写词开头。
- 是否出现新的场景或时间转折。
- 两段具体名词是否重合。
- 是否是不同主体枚举。
- 合并后长度是否超过动态上限。

### 19. 为什么要保存 Prompt 版本哈希？

- Prompt 变化会改变分句结果。
- 缓存键仅使用原文会错误复用旧算法结果。
- `text + prompt_version_hash + mode/config` 才能准确隔离缓存版本。

### 20. LLM 失败时如何降级？

- `TEXT_SPLIT_MODE=auto` 时可回退规则算法。
- 规则使用标点、句长、语义连续和 jieba 词性等信息。
- 降级结果仍需满足文本保真和片段长度约束。
- 前端应能看到实际使用了 LLM 还是规则。

### 21. 素材检索词应该包含什么？

- 可见的人物、场所、物品、动作和环境。
- 避免“美好、震撼、感动”等抽象评价。
- 可以补充不矛盾的场景词。
- 不能凭空添加原文没有的主体、品牌或地点。

---

## 四、音频和统一时间轴

### 22. 为什么项目接收逐句 WAV，而不是在内部直接 TTS？

- TTS 厂商和音色选择属于上游业务。
- 项目专注时间轴和成片。
- 通过 URL 契约可替换任何 TTS 服务。
- 代价是必须严格校验句子索引和文本一致性。

### 23. `speed` 如何影响音频和句间停顿？

- 逐句音频使用 FFmpeg 调整速度。
- 句间默认留白可按 `0.35 / speed` 缩放。
- 最后一句通常不追加普通 gap。
- 画面 clip 时长必须使用调速后的语音时长和 gap。

### 24. master audio、speech duration 和 clip duration 有何区别？

- speech duration：该句真正口播时间。
- gap：句间停顿。
- clip duration：该句画面占用时长，通常是 speech + gap，并可能受转场影响。
- master audio：所有处理后语音和停顿拼接成的完整音轨。

### 25. 为什么 SRT 是 compose 阶段的重要时间源？

- 它记录最终口播在全局时间轴上的开始和结束。
- compose 可以从 SRT 推导每句时长和 gap，避免调用方重复计算。
- 字幕、画面和音频使用同一来源，减少漂移。
- 若客户端传入 duration 与 SRT 不一致，应以明确的主时间轴为准。

### 26. SRT cue 与分句数量不一致怎么办？

- 可按一对一匹配。
- 无法一对一时，根据最大的时间间隙聚类。
- 也可使用文本相似度辅助分组。
- 匹配仍不可靠时应报错，不能静默错位。

### 27. 为什么音频要有 revision 和 config digest？

- 改语速、音源或句子会产生不同 master audio 和字幕。
- revision 让用户预览和确认特定版本。
- digest 防止确认请求误指向配置已经变化的结果。
- 音频版本变化应使下游素材/compose 缓存失效。

### 28. BGM 如何与人声混音？

- BGM 可循环或裁切到成片时长。
- 设置独立 voice volume 和 bgm volume。
- 口播期间可 ducking 降低 BGM。
- 开头和结尾可淡入淡出。
- 混音后仍需要检查峰值、削波和整体响度。

### 29. Crossfade 会怎样影响总时长？

- 两段重叠 xfade 会让总输出比简单时长求和更短。
- 画面、音频和字幕时间轴必须共同考虑 transition duration。
- 不能先独立算字幕时间，再随意增加转场，否则会逐段累积漂移。

---

## 五、图片和视频素材处理

### 30. 图片如何转换为指定时长的视频？

- 以图片作为循环输入。
- 使用 scale/crop/pad 适配目标画布。
- 指定输出帧率、时长和像素格式。
- 编码为与其他片段一致的中间视频参数。

### 31. 视频素材比口播短怎么办？

- 从 `start_sec` 开始裁切。
- 可使用 `stream_loop` 循环源视频。
- 最终严格截断到 clip duration。
- 需要避免循环边界产生音频或时间戳异常，素材通常先导出无声片段。

### 32. 视频素材比口播长怎么办？

- 从配置的 start offset 截取需要的区间。
- 不应解码和保留整段无用内容。
- 还需确保 start offset 没有超过源时长。

### 33. `scale`、`crop` 和 `pad` 如何选择？

- `scale + crop` 填满画布，但会裁掉边缘。
- `scale + pad` 保留完整画面，但会产生黑边或背景区域。
- 产品可按 contain/cover 策略选择。
- 输出宽高和编码像素格式通常必须是偶数。

### 34. 为什么要断言每个 clip 的画布尺寸？

- concat 要求编码参数、分辨率和时间基一致。
- 任意一段尺寸错误都可能导致 FFmpeg concat 失败或画面跳变。
- 在片段阶段尽早探测和报错，比最终合成失败更容易定位。

### 35. 为什么素材片段通常先去掉原音轨？

- 成片的主音轨来自 master voice 和 BGM。
- 素材自带音频会造成多个音轨、混响或时间长度异常。
- 统一生成 silent clip 后再一次性合成主音轨更可控。

### 36. 如何控制并行 FFmpeg 数量？

- Job 线程池控制同时运行的工作流数量。
- Clip 线程池控制单工作流内并行素材数。
- 两层并发乘积才是最坏 FFmpeg 数量。
- 还要考虑 FFmpeg 编码自身线程数，避免 CPU 过度订阅。

---

## 六、字幕系统

### 37. SRT 和 ASS 分别适合什么？

- SRT 简单、兼容性好，主要表达文本和时间。
- ASS 支持字体、字号、描边、位置、逐字/渐进效果和行级覆盖。
- 项目用 SRT 承担主时间轴，再根据 style 生成 ASS。

### 38. 硬字幕和软字幕有什么区别？

- 硬字幕通过 libass 烧进视频像素，所有播放器一致，但无法关闭。
- 软字幕作为独立字幕轨封装，编码快、可关闭，但播放器样式支持不一致。
- 硬字幕需要字体文件和正确 FontName。

### 39. 为什么字体文件名不等于 ASS FontName？

- libass 根据字体内部 name table 查找 family。
- 文件名可能是中文别名或包含字重信息。
- 项目使用 FontTools 扫描字体元数据并维护别名映射。
- Docker 没有完整 fontconfig 时，必须使用 libass 能匹配的 family 名。

### 40. 中文字幕为什么不能简单按字符数断行？

- 不同汉字、数字、英文的像素宽度不同。
- 引号和括号内容不应被拆成孤立半边。
- 词组不能随意拆开。
- 画布方向、字体、字号和内容安全区都会影响最大宽度。

### 41. 项目如何保护引号和括号？

- 扫描引号和括号形成 protected spans。
- 候选断点不能落在保护区内部。
- 如果断行造成单独的左/右引号或括号，尝试重新合并。
- 这是中文排版质量的重要细节。

### 42. CPS 是什么，为什么要限制？

```text
CPS = 有效字符数 / 展示秒数
```

- CPS 太高时观众来不及阅读。
- 项目优先尝试合并相邻 cue。
- 不能跨素材段错误合并。
- 无法合并时在不覆盖下一 cue 的前提下延长结束时间。
- 仍无法满足则生成质量警告。

### 43. 什么是渐进式字幕？

- 一句内容不是一次性全部出现，而是随着口播逐步展示行或词组。
- 需要将 speech duration 分配给多个 progressive cue。
- 每条仍要满足最短显示时间、CPS 和宽度约束。
- 快口播时需要减少渐进层数，不能生成大量极短 cue。

### 44. 什么是字幕安全区？

- 字幕不能超出画布，也不能落到素材实际可见内容之外。
- 竖屏中的横屏素材可能存在上下留白；横屏中的竖屏素材可能有左右柱状区域。
- 项目根据每段媒体的 content rect 计算可用区域、字体、宽度和 margin override。

### 45. 为什么预览和正式烧录要共用排版逻辑？

- 如果预览用浏览器 CSS、正式成片用 libass，字体度量和断行会不同。
- 用户确认的预览必须尽量接近最终成片。
- 最好复用同一 ASS 生成、字体解析、安全区和 cue 优化代码。

### 46. Whisper 在项目中起什么作用？

- 可选对最终音频做 ASR 时间对齐。
- 用于修正 TTS 文件和估算时间之间的误差。
- 未安装或未启用时保持原时间轴，不影响基础流程。
- ASR 结果不能盲目覆盖原文，需要文本匹配和置信度保护。

---

## 七、最终合成、交付和清理

### 47. 最终合成需要完成哪些工作？

- 构造每句画面 clip。
- 按时间轴拼接或添加 xfade。
- 生成/选择 master voice。
- 可选混合 BGM。
- 生成最终 ASS。
- 硬字幕烧录或软字幕封装。
- 校验时长、分辨率和文件存在。
- 调整 MP4 为 faststart。
- 上传视频和字幕到 OSS。

### 48. 为什么需要 MP4 `+faststart`？

- 普通 MP4 的 moov 可能位于文件尾。
- 浏览器需要下载很大部分后才能播放。
- faststart 将 moov 移到前面，提高边下边播和首帧速度。
- 可以无损 `-c copy` 重排容器。

### 49. OSS 上传成功后什么时候可以删除本地工作区？

- 确认视频、字幕等必要 deliverable 均上传成功。
- Job 结果已经原子保存。
- 没有重试或预览仍依赖本地文件。
- 日志和错误信息已经归档。
- 删除过程中要考虑取消和查询的竞态。

### 50. 工作区清理为什么要区分 empty、abandoned 和 composed？

- 空工作区可以较快清理。
- 未完成工作区需要给用户恢复时间。
- 已合成工作区可能为重新下载或问题排查保留更久。
- 新建 grace period 防止正在初始化的目录被清理。
- 维护接口默认 dry-run，降低误删风险。

### 51. 外部素材 URL 有哪些安全风险？

- SSRF、重定向绕过和云元数据访问。
- 超大文件和慢速下载攻击。
- 非媒体内容伪装扩展名。
- 签名 URL 泄露。
- 应校验协议、主机/IP、大小、超时、实际媒体格式和日志脱敏。

---

## 八、性能、评测与系统改进

### 52. 一次 compose 为什么容易把 CPU 打满？

- 多个素材并行执行 FFmpeg。
- 每个 FFmpeg 编码器还会自行创建线程。
- 同时可能执行音频处理、字幕烧录和上传。
- 全局 job 并发、clip 并发和编码线程数需要统一预算。

### 53. 如何定位音画不同步？

按主时间轴逐层检查：

1. 单句调速后的真实音频时长。
2. gap 和 crossfade。
3. master audio 总时长。
4. SRT 每句 start/end。
5. 每段 clip 实际时长。
6. concat 后视频总时长。
7. 最终 mux 是否使用 `-shortest` 截断。

### 54. 如何评估自动成片质量？

- 分句文本保真率。
- 视觉单元边界人工评分。
- 素材与文案相关性。
- 音画同步误差。
- 字幕 CPS、越界和错误断行率。
- 最终分辨率、黑帧、花屏和播放成功率。
- 任务成功率、P95 时延和单片成本。

### 55. 如何实现更可靠的缓存？

缓存键至少包含：

- 输入内容哈希。
- LLM Prompt 版本。
- 模型版本。
- 音频 URL/内容哈希、速度和 gap 配置。
- 素材 URL、start offset、目标时长和画布。
- 字幕样式、字体文件版本和渲染配置。
- FFmpeg/编码预设版本。

### 56. 如何扩展到多机部署？

- Workspace 元数据迁移到 PostgreSQL。
- 中间和最终产物迁移到 OSS/共享对象存储。
- 使用可靠任务队列。
- 文件锁改为数据库/Redis 分布式协调。
- API 与 FFmpeg worker 分离。
- 每个 Job 使用显式租约、心跳、幂等状态机和可重试步骤。

### 57. 如何与视频切片和多模态检索项目打通？

```text
video-clip 产出镜头片段和时间范围
→ multimodal-retrieval 建立 CLIP/BGE 索引
→ media-editor 分句并产生 visual queries
→ 检索服务返回候选 segment URL + start/end
→ 用户/规则确认素材绑定
→ media-editor 自动成片
```

需要统一媒体 ID、租户、OSS key、授权、分辨率、时长和相关性分数字段。

---

## 九、容易暴露“只看过 README”的追问

- LLM 分句为什么不是普通标点断句？
- 去掉 `[V]/[NV]` 后为什么必须验证原文？
- Prompt 版本为什么要进入缓存键？
- 项目内部是否真的提供 TTS？
- speech duration 和 clip duration 有什么区别？
- 为什么 compose 应以 SRT 为时间源？
- xfade 为什么会缩短总时长？
- 图片和短视频分别如何补到目标时长？
- 为什么素材原音通常被移除？
- 字体文件名为什么不能直接写进 ASS FontName？
- CPS 超限时为什么不能总是延长 cue？
- 安全区为什么要随每段素材变化？
- 服务重启后为什么能看到工作区，却不能续跑 FFmpeg？
- 单机文件锁为什么不等于分布式锁？

---

## 十、现场画图题

### 1. Flow B 阶段流程

```text
split → 外部 TTS → audio/process → media bind
                                      ↓
                             visual preview
                                      ↓
                                  compose
                                      ↓
                           MP4 + WAV + ASS → OSS
```

### 2. 时间轴关系

```text
sentence speech ──────┐
                      ├→ clip duration → video segment
gap/crossfade ────────┘

all speech + gaps → master audio
same timeline      → SRT/ASS
```

### 3. 字幕处理

```text
SRT cues
→ 文本/时间优化
→ 引号括号保护
→ 像素宽度与安全区
→ CPS 护栏
→ 渐进 cue
→ ASS
→ libass 烧录
```

---

## 十一、自评重点

重点题：第 2、7、9、12、15、16、17、23、24、25、29、33、36、39、42、44、47、53、56、57 题。

| 分数 | 表现 |
|---:|---|
| 0 | 只知道调用 FFmpeg 合成视频 |
| 1 | 能描述分句、音频、素材和字幕流程 |
| 2 | 能解释统一时间轴、缓存失效、字幕排版和并发 |
| 3 | 能排查音画同步并设计多机可靠执行架构 |
