# Video Clip 视频智能切片项目面试题

> 对应项目：`video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457`  
> 配套总览：[项目功能技术栈与学习路线](./项目功能技术栈与学习路线.md)  
> 定位：长视频导入、预览、分镜、精确切片和 OSS 发布服务，不是完整时间线编辑器。

---

## 一、项目介绍与架构

### 1. 请用 3 分钟介绍这个项目

回答要点：

- FastAPI 提供视频导入、预览、分镜、切片、发布和清理接口。
- 支持 OSS Key、OSS URL 和普通公网 HTTP/HTTPS URL。
- FFprobe 探测视频元数据，FFmpeg 负责代理视频、切片、缩略图和流式输出。
- 自动分镜支持 PySceneDetect 的传统视觉检测，也支持豆包 Ark 多模态语义分镜。
- 后台任务由同进程 InlineScheduler 和线程执行。
- 任务元数据保存在 MemoryStore，本地文件和 OSS 保存媒体产物。
- 当前只能安全运行单 API 实例，进程重启后内存任务账本丢失。

### 2. 一条视频从导入到发布经过哪些步骤？

```text
客户端提交 OSS key/URL
→ 校验来源并创建 video_id
→ 后台下载或转存源视频
→ FFprobe 获取时长、分辨率、FPS、音轨
→ 可选生成胶片条与波形
→ 手动或自动生成 segments
→ 用户确认并启动 cut
→ 多路 FFmpeg 精确切片
→ 校验每段时长和帧数
→ 生成缩略图
→ 上传 staging/final OSS
→ 返回 CDN 或签名 URL
```

### 3. 为什么把 Route、Service、Worker、Core 和 Storage 分层？

- Route 负责 HTTP 契约和参数校验。
- Service 负责编排视频和任务状态。
- Worker 负责后台长任务的状态转换。
- Core 封装 FFmpeg、分镜和媒体算法。
- Storage 封装内存账本、本地文件和 OSS。
- 分层后可以单测算法，也可以未来替换队列和持久化实现。

### 4. 为什么说它不是完整的视频编辑器？

- 它主要处理一条长视频的分段和片段发布。
- 没有多轨时间线、字幕编排、TTS、人声/BGM 混音等完整成片能力。
- `media-editor` 才负责文案、音频、素材和字幕合成。

---

## 二、视频导入与安全

### 5. 支持哪些视频导入方式？

- OSS object key。
- OSS URL。
- 普通 HTTP/HTTPS URL。
- 服务将源片保存到工作目录或按流程转存 OSS，再由后台任务探测媒体。

### 6. 为什么视频导入要异步执行？

- 远程下载、FFprobe 和派生产物生成耗时不稳定。
- 大文件不适合让 HTTP 请求一直阻塞。
- API 可以先返回 `video_id`，客户端再查询状态。
- 后台任务可以独立控制并发、失败状态和清理。

### 7. 普通 URL 导入如何防 SSRF？

回答要点：

- 只允许 HTTP/HTTPS。
- 禁止 URL 中携带用户名和密码。
- 可配置来源域名白名单。
- DNS 解析后拒绝内网、环回、链路本地、保留和组播地址。
- 禁止自动重定向，每次重定向都重新校验目标。
- 连接建立后检查实际 peer IP，降低 DNS rebinding 风险。
- 限制重定向次数、下载体积和超时。

### 8. 只在请求开始时校验 URL 为什么不够？

- 重定向可能跳转到 `127.0.0.1` 或云元数据地址。
- DNS 在校验后可能返回另一个地址。
- 因此每次重定向和建立连接后的实际 peer 都需要再次检查。

### 9. 如何防止恶意大文件撑满磁盘？

- 先检查 `Content-Length`。
- 下载过程中累计实际字节，超过上限立即终止。
- 流式写入，不一次性读入内存。
- 失败时删除不完整文件。
- 定期执行按 TTL 和磁盘水位的清理。

### 10. 下载重试应该重试哪些错误？

- 网络超时、连接中断和部分 5xx 可重试。
- 非法 URL、SSRF 拒绝和明确超限不应重试。
- 使用指数退避并限制尝试次数。
- 每次重试前清理残缺目标文件。

---

## 三、任务状态和并发

### 11. 视频任务有哪些核心状态？

```text
pending
detecting
preview
processing
done
failed
```

需要区分“已检测出分镜等待用户确认”和“正在实际切片”。

### 12. InlineScheduler 是如何工作的？

- API 接收任务后创建 asyncio task。
- 使用 `asyncio.Semaphore` 限制同时运行的后台任务数。
- 阻塞型工作通过 `asyncio.to_thread` 进入线程。
- 活跃任务超过一定倍数时直接拒绝，形成简单背压。
- 关闭阶段拒绝新任务。

### 13. 为什么不能无限地把任务放进内存队列？

- 请求量高时内存会持续增长。
- 排队时间不可控，客户端仍以为任务已被可靠接收。
- 进程崩溃会丢失全部未执行任务。
- 因此需要有界队列、背压，生产规模更大时应使用持久化队列。

### 14. 为什么当前部署只能运行一个 API 实例？

- `MemoryStore` 在进程内保存 video、task 和 segment。
- 每个实例拥有不同账本。
- 请求经过负载均衡后可能查不到另一个实例创建的 ID。
- 多实例还可能同时操作相同文件。

### 15. 服务重启后会发生什么？

- `video_id`、`job_id`、segment 状态等内存数据丢失。
- 本地或 OSS 媒体文件可能还存在，形成孤儿产物。
- 客户端查询旧 ID 会失败。
- 启动清理需要回收孤儿文件，但不能恢复完整任务语义。

### 16. 如何改造成可水平扩容？

- 用 PostgreSQL 保存视频、任务和片段状态。
- 用 Redis Streams、RabbitMQ、Kafka 或 Celery 处理持久任务。
- 使用分布式锁或原子状态 claim 防止重复执行。
- 媒体源和产物放 OSS，而不是依赖实例本地盘。
- Worker 和 API 分开部署并支持幂等重试。

### 17. Worker 为什么需要幂等 claim？

- 客户端可能重复提交 cut。
- 任务恢复或队列重投可能重复执行。
- 原子地从预期状态切到 processing，只有一个 worker 成功。
- 发布前还要检查已有产物是否仍有效。

---

## 四、分镜算法

### 18. 项目支持哪三种分镜模式？

- 手动分镜：用户传入开始和结束时间。
- ContentDetector：基于相邻帧内容差异检测硬切。
- AdaptiveDetector：根据局部变化自适应阈值，降低闪光等干扰。
- `semantic`：先生成压缩代理视频，再调用 Ark 多模态模型理解语义镜头。

### 19. ContentDetector 和 AdaptiveDetector 如何选择？

- 剪辑硬切明显、光照稳定时 ContentDetector 简单直接。
- 闪光、运动和镜头变化复杂时 AdaptiveDetector 更抗局部干扰。
- 阈值越低通常越敏感，但两个检测器的阈值语义不同，需要做参数映射。

### 20. 为什么传统分镜之外还需要语义分镜？

- 画面连续不代表语义没有变化。
- 运镜、访谈或长镜头中可能发生主题切换。
- 传统像素变化容易把闪光和快速运动误判为切镜。
- 多模态模型可以根据人物、场景和事件语义判断边界。

### 21. 为什么语义分镜先生成代理视频？

- 原视频可能分辨率高、码率高、文件过大。
- 上传大视频会增加网络成本并触发模型接口负载限制。
- 代理视频降低分辨率、帧率和码率，但尽量保留语义变化。
- 如果仍超目标体积，会再次进行更激进压缩。

### 22. Ark 调用如何处理 429、413 和网络异常？

- 429 作为限流错误，退避后重试。
- 413 表示 payload 过大，可尝试更紧的代理压缩。
- 408/5xx/网络超时通常可重试。
- 明确格式或鉴权错误不应无限重试。
- 错误分类需要决定 retryable、rate_limited 和 payload_issue。

### 23. 为什么自动分镜后还要合并短片段？

- 检测器可能因闪烁、快速运动产生密集边界。
- 极短片段不利于预览、语义理解和素材复用。
- 不同检测器使用不同合并间隔，硬切检测应更保守，避免抹掉合法快切。

### 24. 如何验证自动分镜结果是否合法？

- 开始时间非负，结束时间大于开始时间。
- 片段不能超出源视频时长。
- 片段按时间排序。
- 重叠和间隙符合产品语义。
- 时间点换算到帧后仍至少包含一帧。

---

## 五、FFmpeg 精确切片

### 25. 为什么使用半开区间 `[start, end)`？

- 当前片段包含 start 帧，不包含 end 帧。
- end 正好可以作为下一段 start。
- 相邻片段不会重复边界帧，也不会凭空丢一帧。
- 帧数可直接表示为 `end_frame - start_frame`。

### 26. 为什么优先使用帧号，而不是浮点秒？

- 浮点时间未必正好落在帧边界。
- `round(start * fps)` 可以得到明确帧编号。
- 预期帧数更容易校验。
- 避免相邻切片因时间舍入发生重复帧或缺帧。

### 27. FFmpeg 中把 `-ss` 放在输入前后有什么区别？

- 输入前 seek 快，但通常先跳到邻近关键帧，精度可能较低。
- 输入后 seek 需要解码到目标位置，较慢但更精确。
- 项目先尝试精确解码，失败时用“粗 seek + 细 seek”两段方式回退。

### 28. 为什么同时设置 `-frames:v` 和 `-t`？

- `-frames:v` 控制严格视频帧数。
- `-t` 控制整体输出时长和音频处理范围。
- 两者共同约束，减少视频与音频时长不一致。

### 29. 为什么有音频时使用 `apad`？

- 有些源音轨比画面略短。
- 如果直接使用 `-shortest`，输出可能提前结束并少掉尾部视频帧。
- `apad` 将音频补到目标时长，让视频帧数成为主约束。

### 30. 切片完成后为什么同时校验时长和帧数？

- 容器时长会受 AAC padding 和 metadata 影响。
- 单看时长可能误判实际画面帧数。
- 帧数接近期望时，可以容忍少量容器时长偏差。
- 帧数和时长都偏差较大时才判定失败。

### 31. 切片失败有哪些回退路径？

```text
精确解码 + 音频
→ 两段 seek + 音频
→ 精确解码 + 纯视频
```

每次失败删除残缺文件；最终仍失败才记录 segment 错误。

### 32. 如何处理源视频尾部元数据不准确？

- 探测实际视频流时长。
- 根据时长估算最大可用帧。
- 将末段的 frame range clamp 到源范围。
- 至少保留一帧，避免请求超出尾部导致少切或空文件。

### 33. VFR 视频会带来什么问题？

- 可变帧率下 `时间 × 平均 FPS` 不一定对应精确帧。
- 按 CFR 思路计算的边界可能漂移。
- 更严格方案需要使用 PTS/time_base 或先转成 CFR 代理。
- 面试中应主动说明当前按平均 FPS 的边界假设。

### 34. 单任务内部并行切片如何控制并发？

- 使用 ThreadPoolExecutor 并行启动多个 FFmpeg 子进程。
- worker 数取配置、CPU 数和 segment 数的最小值。
- 需要同时考虑“后台任务并发 × 单任务 FFmpeg 并发”，防止嵌套并发打满机器。

---

## 六、预览、波形与 OSS

### 35. 视频预览为什么需要支持 HTTP Range？

- 浏览器播放 MP4 会请求指定字节范围。
- 拖动进度条不应该从头下载整部视频。
- 服务需要解析单 Range，返回 206、`Content-Range` 和正确长度。

### 36. 胶片条是如何生成的？

- 根据视频时长估算抽帧数量。
- 长视频降低采样密度，并设置硬上限，避免一次抽数百帧。
- FFmpeg 抽帧后横向拼接为 JPEG。
- 结果带版本化缓存；采样规则变化时递增缓存版本。
- 使用并发限制，避免多个胶片任务拖垮机器。

### 37. 音频波形如何生成？

- FFmpeg 将音轨解码为低采样率 PCM。
- 按展示 bins 分组。
- 每组计算归一化峰值。
- 前端用峰值数组画时间轴波形，不需要传完整音频。

### 38. 为什么支持 FFmpeg 输出直接流式上传 OSS？

- 大量切片全部先落本地会消耗两倍磁盘空间和 I/O。
- fragmented MP4 可以从 stdout 持续产生数据。
- OSS multipart upload 边读边传，降低本地空间压力。
- 失败时必须 abort multipart，避免残留未完成上传。

### 39. `+faststart` 和 fragmented MP4 分别解决什么？

- `+faststart` 把 moov 元数据移到文件头，适合完整 MP4 的 Web 快速播放。
- fragmented MP4 使用 moof/mdat 分片，适合尚未完整生成时流式传输。
- 普通落盘产物和管道上传需要采用不同 movflags。

### 40. CDN URL、签名 URL 和本地回退如何选择？

- 公共发布产物优先返回配置的 CDN URL。
- 私有 OSS 对象使用时效签名 URL。
- OSS 访问失败时可根据场景回退到本地 API 预览。
- 日志不能打印完整签名查询参数，避免泄露凭证。

---

## 七、运维、质量与改进

### 41. 健康检查应该检查哪些内容？

- FastAPI 进程。
- FFmpeg 和 FFprobe 可执行性。
- OSS 配置与连通性。
- InlineScheduler 是否启动和当前负载。
- MemoryStore 容量。
- 本地磁盘空间。

### 42. 清理策略为什么既需要 TTL 又需要磁盘水位？

- TTL 处理正常过期产物。
- 磁盘水位应对突然的大量视频和异常残留。
- 当剩余空间过低时，可以忽略普通保留期，优先删除最旧的 done/preview 产物。
- 运行中任务不能被普通清理误删。

### 43. 如何观察一条任务为什么慢？

应拆分指标：

- 下载时长和吞吐。
- FFprobe 时长。
- 分镜算法耗时。
- Ark 上传、排队和响应耗时。
- 每段 FFmpeg 耗时与失败重试次数。
- OSS multipart 吞吐。
- 队列等待时间与实际执行时间。

### 44. 如何评估分镜质量？

- 边界 Precision、Recall、F1。
- 允许一定秒数或帧数容差的边界命中。
- 过切率和漏切率。
- 平均片段长度与极短片段比例。
- 人工对语义完整性的评分。
- 传统检测和语义检测的成本、延迟对比。

### 45. 如何评估切片正确性？

- 输出帧数与期望帧数偏差。
- 时长误差。
- 相邻片段重复帧/缺帧率。
- 音画同步误差。
- 有无黑帧、空片和损坏文件。
- 不同编码、无音轨、VFR 和尾部异常样本覆盖。

### 46. 这个项目最优先应该改进什么？

高优先级通常是：

1. 用持久化数据库替换 MemoryStore。
2. 用可靠任务队列替换纯进程内调度。
3. 增加 API 鉴权和租户隔离。
4. 增加 VFR/PTS 边界处理。
5. 建立分镜和切片质量评测集。
6. 完善分布式清理和媒体生命周期管理。

---

## 八、容易暴露“只看过 README”的追问

- 切片区间为什么是 `[start, end)`？
- 为什么 `-shortest` 可能导致少视频帧？
- 为什么先校验帧数，再容忍少量容器时长误差？
- 精确 seek 和输入前 seek 有什么区别？
- HTTP 重定向为什么必须重新做 SSRF 校验？
- 为什么建立连接后还要检查 peer IP？
- fragmented MP4 为什么适合管道上传？
- 胶片条为什么需要抽帧硬上限和缓存版本？
- 为什么当前服务不能简单扩成两个副本？
- 后台任务并发和单任务 FFmpeg 并发为什么要相乘计算？

---

## 九、现场画图题

### 1. 导入与切片时序

```text
Client → API → Scheduler → Download/OSS → FFprobe
                                  ↓
Client ← Job status ← MemoryStore ← Worker
                                  ↓
                         Detector → FFmpeg → OSS
```

### 2. 精确切片回退

```text
帧边界计算
→ 精确解码
→ 校验时长/帧数
   ├─成功：完成
   └─失败：两段 seek
            └─仍失败：无音频重试
```

---

## 十、自评重点

重点题：第 2、7、12、14、18、21、25、27、29、30、34、38、44、46 题。

| 分数 | 表现 |
|---:|---|
| 0 | 只知道项目用了 FFmpeg |
| 1 | 能描述接口和基本流程 |
| 2 | 能解释帧边界、并发、SSRF 和任务状态 |
| 3 | 能指出单实例限制，并给出可靠扩容和评测方案 |
