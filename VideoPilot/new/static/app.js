const $ = (selector) => {
  const node = document.querySelector(selector);
  return node || (selector === "#chatMessages" ? ensureChatMessages() : null);
};
const api = window.ClipTalkApi.request;
const apiJson = window.ClipTalkApi.requestJson;
const apiBlob = window.ClipTalkApi.requestBlob;
function ensureChatMessages() {
  let root = document.querySelector("#chatMessages");
  if (root) return root;
  const panel = $(".chat-panel") || document.body;
  root = document.createElement("section");
  root.id = "chatMessages";
  root.className = "chat-messages";
  root.setAttribute("aria-live", "polite");
  panel.prepend(root);
  return root;
}
function syncThinkingOrbs(root = document) {
  if (!root) return;
  window.ThinkingOrbsBridge?.sync(root);
}
function syncGenerativeLoaders(root = document) {
  if (!root) return;
  try { window.GenerativeLoadersBridge?.sync(root); }
  catch (error) { console.warn("Generative loader sync skipped", error); }
}
function renderGenerativeLoader(element, options = {}) {
  if (!element) return false;
  try { return window.GenerativeLoadersBridge?.render(element, options) !== false; }
  catch (error) { console.warn("Generative loader render skipped", error); return false; }
}
function updateGenerativeLoader(element, options = {}) {
  if (!element) return false;
  try { return window.GenerativeLoadersBridge?.update(element, options) !== false; }
  catch (error) { console.warn("Generative loader update skipped", error); return false; }
}
function clearGenerativeLoader(element, options = {}) {
  if (!element) return;
  try { window.GenerativeLoadersBridge?.clear(element, options); }
  catch (error) { console.warn("Generative loader cleanup skipped", error); }
}
function syncBorderBeams(root = document) {
  if (!root) return;
  try { window.BorderBeamBridge?.sync(root); }
  catch (error) { console.warn("Border beam sync skipped", error); }
}
function updateBorderBeam(element, options = {}) {
  if (!element) return false;
  try { return window.BorderBeamBridge?.update(element, options) !== false; }
  catch (error) { console.warn("Border beam update skipped", error); return false; }
}
function clearBorderBeam(element) {
  if (!element) return;
  try { window.BorderBeamBridge?.clear(element); }
  catch (error) { console.warn("Border beam cleanup skipped", error); }
}
function setGenerativeInlineStatus(target, message = "", tone = "neutral", variant = "glyph") {
  const node = typeof target === "string" ? $(target) : target;
  if (!node) return;
  node.classList.add("generative-inline-status");
  let motion = node.querySelector(".generative-inline-motion");
  let label = node.querySelector(".generative-status-label");
  if (!motion || !label) {
    clearGenerativeLoader(motion);
    node.textContent = "";
    motion = document.createElement("span");
    motion.className = "generative-inline-motion hidden";
    motion.dataset.generativeLoader = "inline";
    motion.dataset.loaderSize = "15";
    label = document.createElement("b");
    label.className = "generative-status-label";
    node.append(motion, label);
  }
  label.textContent = message;
  node.dataset.tone = tone;
  node.classList.toggle("hidden", !message);
  const loading = tone === "loading" && Boolean(message);
  motion.dataset.loaderVariant = variant;
  motion.dataset.loaderLabel = message || "正在处理";
  motion.dataset.loaderActive = String(loading);
  motion.classList.toggle("hidden", !loading);
  if (loading) renderGenerativeLoader(motion, { kind: "inline", variant, size: 15, label: message });
  else clearGenerativeLoader(motion);
}
let toastTimer = null;
function clearToast() {
  clearTimeout(toastTimer);
  toastTimer = null;
  const region = $("#toastRegion");
  if (region) {
    region.innerHTML = "";
    delete region.dataset.jobId;
  }
}

function showToast(message, tone = "error") {
  const region = $("#toastRegion");
  if (!region) return;
  clearToast();
  if (currentJob?.id) region.dataset.jobId = String(currentJob.id);
  region.innerHTML = `<div class="toast ${tone}"><span>${escapeHtml(message)}</span><button type="button" aria-label="关闭提示">×</button></div>`;
  const toast = region.querySelector(".toast");
  toast.querySelector("button")?.addEventListener("click", clearToast);
  toastTimer = setTimeout(clearToast, 5200);
}
window.showToast = showToast;

function resizeChatComposerInput() {
  const input = $("#chatInput");
  const form = $("#chatForm");
  if (!input || !form) return;
  const minHeight = 42;
  const maxHeight = 84;
  input.style.setProperty("height", "auto", "important");
  const naturalHeight = Math.max(minHeight, input.scrollHeight);
  const inputHeight = Math.min(maxHeight, naturalHeight);
  input.style.setProperty("height", `${inputHeight}px`, "important");
  input.style.setProperty("overflow-y", naturalHeight > maxHeight ? "auto" : "hidden", "important");
  form.style.setProperty("--ct-composer-input-height", `${inputHeight}px`);
  form.classList.toggle("composer-has-overflow", naturalHeight > maxHeight);
}

function updateComposerBeam() {
  const shell = $(".chat-input-shell");
  const input = $("#chatInput");
  if (!shell || !input) return;
  const sending = shell.classList.contains("is-sending");
  const focused = document.activeElement === input;
  const hasText = Boolean(input.value.trim());
  const disabled = input.disabled;
  const sendButton = $("#sendButton");
  shell.classList.toggle("has-text", hasText);
  shell.classList.toggle("is-focused", focused);
  shell.classList.toggle("is-disabled", disabled);
  if (sendButton) {
    sendButton.disabled = disabled || sending || actionBusy || !hasText;
    const label = !currentJob ? (videoInput?.files?.length ? "生成剪辑计划" : "记录剪辑要求") : isAgentInstructionDraft(currentJob) ? "生成剪辑计划" : "发送";
    sendButton.title = label;
    sendButton.setAttribute("aria-label", label);
  }
  const draftStatus = $("#newTaskDraftStatus");
  if (!currentJob && draftStatus && !actionBusy && !sending) {
    draftStatus.textContent = videoInput?.files?.length
      ? hasText ? "素材与要求已准备，可以生成计划" : "已添加视频，等待剪辑要求"
      : hasText || pendingNewTaskInstruction ? "已记录要求，等待添加素材" : "等待添加素材";
  }
  // The composer intentionally uses the full rotating bloom from the Border
  // Beam homepage. Output-version buttons keep the compact edge tracer so the
  // two interactions communicate different states.
  updateBorderBeam(shell, sending
    ? { size: "md", color: "ocean", strength: .96, duration: 1.35, brightness: 1.38, saturation: 1.15, hueRange: 30, active: true }
    : focused
      ? { size: "md", color: "ocean", strength: .86, duration: 1.9, brightness: 1.32, saturation: 1.1, hueRange: 28, active: true }
      : hasText
        ? { size: "md", color: "ocean", strength: .72, duration: 2.7, brightness: 1.24, saturation: 1.04, hueRange: 24, active: true }
        : { size: "md", color: "ocean", strength: disabled ? .32 : .56, duration: disabled ? 5.2 : 3.8, brightness: disabled ? 1.12 : 1.18, saturation: disabled ? .94 : 1, hueRange: disabled ? 20 : 22, active: true });
  resizeChatComposerInput();
}

function setComposerSending(sending) {
  const shell = $(".chat-input-shell");
  if (!shell) return;
  shell.classList.toggle("is-sending", Boolean(sending));
  updateComposerBeam();
}

function outputBeamKey(jobId, node) {
  return `${String(jobId || "unknown")}:${String(node?.dataset.autoVersion || node?.dataset.autoOutput || "")}`;
}

function setOutputVersionBeamSelection(selectedNode = null) {
  document.querySelectorAll(".auto-version-button").forEach((node) => {
    const selected = node === selectedNode
      || (!selectedNode && currentOutput?.filename && node.dataset.autoOutput === currentOutput.filename);
    node.classList.toggle("beam-selected", Boolean(selected));
    // On compact buttons the package mask can fall back to a solid fill in
    // Chromium. Remove any stale mount and use the border-only CSS effect.
    clearBorderBeam(node);
  });
}

function clearOutputVersionSelectionState() {
  document.querySelectorAll(".auto-version-button").forEach((node) => {
    const arrivalTimer = borderBeamArrivalTimers.get(node);
    if (arrivalTimer) window.clearTimeout(arrivalTimer);
    borderBeamArrivalTimers.delete(node);
    node.classList.remove("active", "beam-selected", "beam-arriving");
    clearBorderBeam(node);
  });
  syncCurrentOutputEditAction();
}

function syncOutputVersionBeams(job = currentJob, root = document) {
  if (!job || !root) return;
  const nodes = [...root.querySelectorAll?.(".auto-version-button") || []];
  const jobId = String(job.id || "");
  const firstObservation = !borderBeamSeenOutputJobs.has(jobId);
  const newlyObserved = [];
  nodes.forEach((node) => {
    const key = outputBeamKey(jobId, node);
    const selected = currentOutput?.filename && node.dataset.autoOutput === currentOutput.filename;
    if (!borderBeamSeenOutputs.has(key)) {
      borderBeamSeenOutputs.add(key);
      if (!firstObservation) newlyObserved.push(node);
    }
    // Selection is derived from the media currently shown in the player.
    // Toggling instead of only adding prevents a source-preview switch from
    // leaving a stale output card highlighted after the conversation reruns.
    node.classList.toggle("active", Boolean(selected));
  });
  // A batch can expose several versions at the same moment. Highlight only
  // the newest one; animating every card at once looks like a rendering bug
  // and destroys the comparison labels.
  const arrivingNode = newlyObserved[newlyObserved.length - 1];
  if (arrivingNode) {
    arrivingNode.classList.add("beam-arriving");
    const previousTimer = borderBeamArrivalTimers.get(arrivingNode);
    if (previousTimer) window.clearTimeout(previousTimer);
    const timer = window.setTimeout(() => {
      if (!arrivingNode.isConnected) return;
      arrivingNode.classList.remove("beam-arriving");
      borderBeamArrivalTimers.delete(arrivingNode);
      setOutputVersionBeamSelection(arrivingNode.classList.contains("active") ? arrivingNode : null);
    }, 3200);
    borderBeamArrivalTimers.set(arrivingNode, timer);
  }
  borderBeamSeenOutputJobs.add(jobId);
  setOutputVersionBeamSelection(nodes.find((node) => node.classList.contains("active")) || null);
}
const uploadForm = $("#uploadForm");
const videoInput = $("#videoInput");
const chatForm = $("#chatForm");
const chatInput = $("#chatInput");
const mainVideo = $("#mainVideo");
const timelinePanel = $("#timelinePanel");
const timelineViewport = $("#timelineViewport");
const timelineTrackContent = $("#timelineTrackContent");
const waveformCanvas = $("#waveformCanvas");
const viewerShell = $("#viewerShell");
const mediaFrame = $("#mediaFrame");
const localPreviewPanel = $("#localPreviewPanel");
let currentJob = null;
window.ClipTalkCurrentJobId = () => String(currentJob?.id || "");
window.ClipTalkCurrentJobSnapshot = () => currentJob;
window.ClipTalkRefreshCurrentJob = () => { if (currentJob) pollJob(); };
window.ClipTalkSwitchWorkspaceJob = async (job) => {
  const targetId = String(job?.id || "");
  if (!targetId) return;
  const sourceId = String(currentJob?.id || "");
  let latest = job;
  try {
    const response = await api(`/api/jobs/${encodeURIComponent(targetId)}`);
    if (response?.job?.id) latest = response.job;
  } catch {
    // The plan result still provides a usable handoff snapshot if the fresh
    // read fails. Never block navigation solely because a refresh raced it.
  }
  const visibleId = String(currentJob?.id || "");
  if (visibleId && visibleId !== sourceId && visibleId !== targetId) return;
  if (!switchWorkspaceJob(latest)) return;
  void window.ClipTalkAgentWorkspace?.resumeForJob?.(currentJob);
  if (jobNeedsPolling(currentJob)) pollJob();
};
let currentOutput = null;
function currentOutputIsReviewSample(output = currentOutput, version = null) {
  const locatedVersion = version || (output?.filename && currentJob
    ? orderedJobOutputs(currentJob).find(({ item }) => String(item?.filename || "") === String(output.filename))?.version
    : null);
  const kind = String(output?.outputKind || output?.kind || locatedVersion?.variantKind || "").toLowerCase();
  const versionId = String(locatedVersion?.id || "").toLowerCase();
  return Boolean(
    output?.previewOnly
    || locatedVersion?.previewOnly
    || versionId === "agent-review-previews"
    || kind.includes("review_preview")
    || kind.includes("agent_review")
  );
}

function syncOutputPreviewMode() {
  const outputActive = Boolean(viewerMediaKind === "output" && currentOutput);
  const active = Boolean(outputActive && currentOutputIsReviewSample(currentOutput));
  document.body.classList.toggle("ct-output-preview-mode", outputActive);
  document.body.classList.toggle("ct-agent-preview-mode", active);
  document.body.dataset.viewerMode = viewerMediaKind || "source";
  if (active) document.body.dataset.outputPreviewMode = "agent-sample";
  else delete document.body.dataset.outputPreviewMode;
  syncLegacyOutputPanelsForV4(outputActive);
  syncUnifiedWorkspaceView();
  try { window.ClipTalkWorkspaceController?.syncMaterialsSummary?.(); }
  catch { /* summary sync is best-effort */ }
  return active;
}

function syncLegacyOutputPanelsForV4(forceOutputMode = null) {
  const v4 = document.body.classList.contains("ct-workbench-v4");
  const outputMode = forceOutputMode === null
    ? Boolean(viewerMediaKind === "output" && currentOutput)
    : Boolean(forceOutputMode);
  const suppress = Boolean(v4 && outputMode);
  ["clipSection", "railOutput", "reviewWorkbench", "railBody", "ctReviewEmpty"].forEach((id) => {
    const node = $(`#${id}`);
    if (!node) return;
    const wasSuppressed = node.classList.contains("ct-v4-preview-suppressed");
    node.classList.toggle("ct-v4-preview-suppressed", suppress);
    if (suppress) {
      node.classList.add("hidden");
      node.style.display = "none";
    } else if (wasSuppressed) {
      node.classList.remove("ct-v4-preview-suppressed");
      node.style.removeProperty("display");
    }
  });
  return suppress;
}

const selectedOutputByJob = new Map();
let initialOutputJobId = null;
const observedFormalOutputs = new Map();
function observeFormalDelivery(job) {
  const entries = orderedJobOutputs(job).filter(({ item, version }) => !currentOutputIsReviewSample(item, version));
  const known = observedFormalOutputs.get(job.id);
  observedFormalOutputs.set(job.id, new Set(entries.map(({ item }) => item.filename)));
  const delivered = known && entries.filter(({ item }) => !known.has(item.filename)).at(-1);
  if (!delivered) return;
  const filename = delivered.item.filename;
  window.setTimeout(() => {
    if (currentJob?.id !== job.id) return;
    showToast(`成片已生成：${delivered.item.displayTitle || delivered.item.title || filename}`, "success");
    const action = document.createElement("button");
    action.type = "button";
    action.textContent = "下载或长期保留";
    action.onclick = () => {
      if (currentJob?.id !== job.id) return void showToast("任务已切换，请在原任务查看成片");
      window.ClipTalkWorkspaceController?.openDeliveryResult?.(filename);
    };
    document.querySelector(".toast-region .toast, #toastRegion .toast")?.append(action);
  }, 0);
}
function selectedJobOutput(job = currentJob) {
  const entries = orderedJobOutputs(job);
  const filename = selectedOutputByJob.get(String(job?.id || ""));
  return entries.find(({ item }) => item.filename === filename)
    || entries.filter(({ item, version }) => !currentOutputIsReviewSample(item, version)).at(-1)
    || entries.at(-1) || null;
}
window.ClipTalkSelectedJobOutput = selectedJobOutput;
window.ClipTalkCurrentOutputSnapshot = () => ({
  mediaKind: viewerMediaKind,
  output: currentOutput,
  isReviewSample: currentOutputIsReviewSample(currentOutput),
});
let currentCandidate = null;
let currentContentPreviewSearchId = "";
let currentEventGroup = null;
let currentEventSegment = null;
let pollTimer = null;
let elapsedTicker = null;
let pollFailureDelay = 2500;
let sourcePreviewPollTimer = null;
let localPreviewUrl = null;
let actionBusy = false;
let draftBootstrapBusy = false;
let pendingCreateIntentMode = "auto";
let pendingCreateWorkflowKind = "";
let pendingWorkflowSwitch = "";
let pendingWorkflowOptions = { scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3 };
let pendingCapabilityCategory = "auto";
let pendingCapabilityOption = "";
let quickWorkflowValidation = "";
let legacyWorkflowPickerOpen = false;
let capabilityDrawerReturnFocus = null;
let candidateRailOpen = false;
function setCandidateRailOpen(open) {
  candidateRailOpen = Boolean(open);
  document.body.dataset.ctCandidateRailOpen = String(candidateRailOpen);
  if (candidateRailOpen) {
    window.ClipTalkWorkspaceController?.openRail?.("materials");
  } else {
    const body = $("#railBody");
    const host = $("#chatStageHost");
    if (body && host && body.parentElement !== host) host.append(body);
  }
}
let contentReviewDraftTimer = null;
let contentReviewDraftGeneration = 0;
let contentBasketSaveTimer = null;
let contentBasketSaveGeneration = 0;
let duplicateContentCompositionNotice = null;
let speakerConfirmationBusy = false;
let currentPersonJobId = "";
let focusedCurrentPersonRef = "";
let selectedCurrentPersons = new Set();
let selectedPersonCorrectionPersons = new Set();
let selectedCurrentPersonRanges = new Set();
let selectedCurrentPersonRangeOwner = "";
let currentPersonMatchMode = "any";
let currentPersonActivity = "appearance";
const currentPersonRangePageSize = 40;
const currentPersonRangeLimits = new Map();
const expandedContentSearchIds = new Set();
const contentSearchDetailCache = new Map();
const contentSearchFilterState = new Map();
const borderBeamSeenOutputJobs = new Set();
const borderBeamSeenOutputs = new Set();
const borderBeamArrivalTimers = new WeakMap();
let fragmentDownloadBusy = false;
let candidatePreviewEnd = null;
let candidatePreviewToken = 0;
let contentPreviewSequence = null;
let contentEvidenceRequestToken = 0;
let sourcePreviewRetryToken = 0;
let playbackRequestToken = 0;
let mainVideoAutoplayToken = 0;
let browserFallbackAttempts = new Set();
let viewerMediaKind = "source";
let waveformJobId = null;
let waveformLoadingJobId = null;
let waveformData = null;
let waveformRequestToken = 0;
let waveformRetryAt = 0;
let timelineAssetsJobId = null;
let timelineAssetsLoadingJobId = null;
let timelineAssets = null;
let timelineAssetsRetryAt = 0;
window.ClipTalkTimelineAssetsSnapshot = () => ({
  jobId: timelineAssetsJobId,
  assets: timelineAssets,
});
let timelineTranscript = [];
let transcriptLoadingJobId = null;
let timelineTranscriptJobId = null;
let transcriptRetryAt = 0;
let timelineViewStart = 0;
let timelineViewEnd = 0;
// Source review uses source-video time; every generated version uses its own
// playback time. Moving between the two is an explicit user action so a click
// on an output timeline can never silently replace the video being reviewed.
let timelineCoordinateSpace = "source";
let timelineSelectedOutputSegmentIndex = null;
let timelineReviewFollow = false;
let timelineSnapEnabled = true;
let timelineFrame = null;
let boundaryDrag = null;
let timelineRangeDrag = null;
let timelinePanDrag = null;
let timelinePanFrame = null;
let timelineOverviewDrag = null;
let timelinePointerInside = false;
let timelineSpaceHeld = false;
let timelineSpaceDidPan = false;
let timelineSuppressClickUntil = 0;
let pendingTimelineSelection = null;
let pendingTimelineOriginal = null;
let contentBoundaryTimelineEdit = null;
let timelineManualSelectMode = false;
let timelineVisualMode = "waveform";
let timelineCutsVisible = true;
let timelineSpeakerFilter = "all";
let timelineMediaRenderKey = "";
let waveformRenderKey = "";
let timelineResizeTimer = null;
let timelineResizeFrame = null;
let timelineLabelLayoutWidth = 0;
let timelineLabelLayoutHeight = 0;
let timelineFrameSelectionTime = null;
let mediaFitFrame = null;
let mediaLayoutObserver = null;
let outputAssemblyMode = "single_reel";
let pendingSegmentSelections = new Map();
let timelineChatSelections = [];
let eventGroupSelectionOrder = [];
let ignoredChatContextKeys = new Set();
let activeProposalSourceRange = null;
let autoAdvanceCandidates = true;
let candidateReviewSort = "score";
let locallyExcludedCandidates = new Set();
let currentJobRevision = "";
let autoCompositionDockJobId = "";
let autoCompositionLogExpanded = false;
let visionSettingsState = null;
let selectedVisionProvider = "";
let visionDiscoveredModels = [];
let visionVerifiedAt = null;
let visionSettingsBusy = false;
let visionSettingsDirty = false;
let llmSettingsState = null;
let selectedLlmProvider = "";
let llmDiscoveredModels = [];
let llmVerifiedAt = null;
let llmSettingsBusy = false;
let llmSettingsDirty = false;
let selectedLlmMode = "reuse_vision";
const studio = $(".studio");
const timelineLayerStorageKey = "vlm-highlight-timeline-layers-v1";
const currentJobStorageKey = "vlm-highlight-current-job-v1";
const portraitVideoWidthStorageKey = "cliptalk-portrait-video-width-v2";
const evidencePanelWidthStorageKey = "cliptalk-evidence-panel-width-v1";
const secondaryEditorLayoutStorageKey = "cliptalk-secondary-editor-layout-v2";
let reviewLayoutMediaIdentity = "";
let sourceInspectionContext = null;

function beginSourceInspection() {
  if (viewerMediaKind !== "output" || !currentOutput) return;
  sourceInspectionContext = {
    jobId: currentJob?.id, filename: currentOutput.filename,
    time: Number(mainVideo.currentTime) || 0,
    layout: $("#reviewView")?.dataset.reviewLayout || "landscape",
  };
  let button = $("#returnInspectedVersion");
  if (!button) {
    button = document.createElement("button");
    button.id = "returnInspectedVersion";
    button.className = "download-button";
    button.type = "button";
    $(".asset-actions")?.prepend(button);
    button.addEventListener("click", () => {
      const context = sourceInspectionContext;
      if (context?.jobId === currentJob?.id) selectOutput(context.filename, false, context.time);
    });
  }
  button.textContent = `返回 ${currentOutput.title || "当前版本"}`;
  button.classList.remove("hidden");
}
let directorStage = "conversation";
let timelineExpanded = false;
let reviewLowerPanelMode = "review";
let personReviewMode = "correct";
let voiceReviewMode = "correct";
let secondaryInspectorMode = "clip";
let secondaryEditorMaterialFilter = null;
let secondaryEditorMaterialSearch = "";
let secondaryEditorUpperRatio = 58;
let secondaryEditorResizeDrag = null;
let secondaryEditorInspectorWidth = 420;
let secondaryEditorInspectorResizeDrag = null;
let secondaryEditorTimelineLayoutFrame = null;
let unifiedWorkspaceView = null;
let timelinePrecisionDrawerOrigin = null;
let timelinePrecisionDrawerTrigger = null;
let openingHomeTaskId = null;
let restoringHistory = false;
// Invalidates in-flight restore/open/poll requests when the user starts or
// opens a different task. A late response must never replace the active task.
let workspaceGeneration = 0;
let currentCreationSessionId = "";
let pendingNewTaskInstruction = "";
let pendingNewTaskInstructionCommitted = false;
let activeChatController = null;
// A user-initiated return to the home dashboard must win over an in-flight
// task restore request so resetWorkspace() cannot be undone by a late response.
let homeNavigationRequested = false;
window.alert = (message) => showToast(message);
const workspaceStateMachine = window.ClipTalkWorkspaceState.create((next) => {
  document.body.dataset.workspaceState = next;
  if (next !== "completed" || !document.body.dataset.precisionEditing) {
    document.body.dataset.precisionEditing = "false";
  }
});

function syncTaskCreationLayout() {
  const uploadView = $("#uploadView");
  const creating = Boolean(studio
    && !currentJob
    && !studio.classList.contains("home-mode"));
  studio?.classList.remove("task-creation-mode");
  studio?.classList.toggle("new-task-workbench", creating);
  const assistant = studio?.querySelector(":scope > .chat-panel");
  if (assistant) {
    assistant.inert = false;
    assistant.removeAttribute("aria-hidden");
    assistant.classList.toggle("creation-assistant", creating);
  }
  syncAssistantPanelResizerAvailability();
  uploadView?.classList.toggle("new-task-upload", creating);
  return creating;
}

const ASSISTANT_WIDTH_KEYS = {
  creation: "cliptalk-new-task-assistant-width:v1",
  workspace: "cliptalk-workspace-assistant-width:v1",
};

function assistantPanelWidthMode() {
  return studio?.classList.contains("new-task-workbench") ? "creation" : "workspace";
}

function assistantPanelWidthBounds(mode = assistantPanelWidthMode()) {
  const creating = mode === "creation";
  const min = creating ? 380 : window.innerWidth >= 1440 ? 360 : 320;
  const reviewRail = studio?.querySelector(":scope > #reviewRail");
  const railVisible = !creating && reviewRail && getComputedStyle(reviewRail).display !== "none";
  const railWidth = railVisible ? reviewRail.getBoundingClientRect().width : 0;
  const rightDividerWidth = railVisible ? 8 : 0;
  const centerMinimum = 480;
  const available = Math.floor(window.innerWidth - 84 - 8 - rightDividerWidth - railWidth - centerMinimum);
  return { min, max: Math.max(min, Math.min(620, (window.innerWidth - 84) / 2, available)) };
}

function applyAssistantPanelWidth(width, { persist = false, mode = assistantPanelWidthMode() } = {}) {
  if (!studio) return 0;
  const bounds = assistantPanelWidthBounds(mode);
  const next = Math.round(Math.min(bounds.max, Math.max(bounds.min, Number(width) || bounds.min)));
  const property = mode === "creation" ? "--ct-new-task-left" : "--ct-user-left-pane";
  studio.style.setProperty(property, `${next}px`);
  const resizer = studio.querySelector(":scope > .panel-resizer-left");
  if (resizer) {
    resizer.setAttribute("aria-valuemin", String(bounds.min));
    resizer.setAttribute("aria-valuemax", String(bounds.max));
    resizer.setAttribute("aria-valuenow", String(next));
  }
  if (persist) {
    try { localStorage.setItem(ASSISTANT_WIDTH_KEYS[mode], String(next)); }
    catch { /* local preferences are optional */ }
  }
  return next;
}

function resetAssistantPanelWidth() {
  const mode = assistantPanelWidthMode();
  const property = mode === "creation" ? "--ct-new-task-left" : "--ct-user-left-pane";
  studio?.style.removeProperty(property);
  try { localStorage.removeItem(ASSISTANT_WIDTH_KEYS[mode]); }
  catch { /* local preferences are optional */ }
  const resizer = studio?.querySelector(":scope > .panel-resizer-left");
  const fallback = mode === "creation" ? 430 : 432;
  const width = Math.round(studio?.querySelector(":scope > #assistantPanel")?.getBoundingClientRect().width || fallback);
  if (resizer) resizer.setAttribute("aria-valuenow", String(width));
  window.dispatchEvent(new Event("resize"));
}

function syncAssistantPanelResizerAvailability() {
  const resizer = studio?.querySelector(":scope > .panel-resizer-left");
  if (!resizer || !studio) return false;
  const resizable = window.innerWidth >= 1280
    && !studio.classList.contains("home-mode")
    && !studio.classList.contains("assistant-collapsed");
  resizer.classList.toggle("hidden", !resizable);
  resizer.setAttribute("aria-hidden", String(!resizable));
  resizer.tabIndex = resizable ? 0 : -1;
  if (resizable) {
    const mode = assistantPanelWidthMode();
    let preferred = mode === "creation" ? 430 : 432;
    try {
      const stored = Number(localStorage.getItem(ASSISTANT_WIDTH_KEYS[mode]));
      if (Number.isFinite(stored) && stored > 0) preferred = stored;
    } catch { /* local preferences are optional */ }
    if (!document.body.classList.contains("assistant-panel-resizing")) applyAssistantPanelWidth(preferred, { mode });
  }
  return resizable;
}

function setupAssistantPanelResizer() {
  const resizer = studio?.querySelector(":scope > .panel-resizer-left");
  const assistant = studio?.querySelector(":scope > #assistantPanel");
  if (!resizer || !assistant || resizer.dataset.resizeReady === "true") return;
  resizer.dataset.resizeReady = "true";

  let drag = null;
  const finish = (event) => {
    if (!drag) return;
    if (event?.pointerId != null && event.pointerId !== drag.pointerId) return;
    const width = Math.round(assistant.getBoundingClientRect().width);
    drag = null;
    document.body.classList.remove("assistant-panel-resizing");
    resizer.classList.remove("is-dragging");
    applyAssistantPanelWidth(width, { persist: true, mode: dragMode });
    window.dispatchEvent(new Event("resize"));
  };
  let dragMode = assistantPanelWidthMode();
  resizer.addEventListener("pointerdown", (event) => {
    if (event.button !== 0 || !syncAssistantPanelResizerAvailability()) return;
    dragMode = assistantPanelWidthMode();
    drag = {
      pointerId: event.pointerId,
      startX: event.clientX,
      startWidth: assistant.getBoundingClientRect().width,
    };
    resizer.setPointerCapture?.(event.pointerId);
    document.body.classList.add("assistant-panel-resizing");
    resizer.classList.add("is-dragging");
    event.preventDefault();
  });
  resizer.addEventListener("pointermove", (event) => {
    if (!drag || event.pointerId !== drag.pointerId) return;
    applyAssistantPanelWidth(drag.startWidth + event.clientX - drag.startX, { mode: dragMode });
  });
  resizer.addEventListener("pointerup", finish);
  resizer.addEventListener("pointercancel", finish);
  resizer.addEventListener("dblclick", resetAssistantPanelWidth);
  resizer.addEventListener("keydown", (event) => {
    if (!["ArrowLeft", "ArrowRight", "Home", "End"].includes(event.key)) return;
    const mode = assistantPanelWidthMode();
    const bounds = assistantPanelWidthBounds(mode);
    const current = assistant.getBoundingClientRect().width;
    const next = event.key === "Home" ? bounds.min
      : event.key === "End" ? bounds.max
        : current + (event.key === "ArrowRight" ? 16 : -16);
    applyAssistantPanelWidth(next, { persist: true, mode });
    window.dispatchEvent(new Event("resize"));
    event.preventDefault();
  });
  window.addEventListener("resize", () => {
    syncAssistantPanelResizerAvailability();
  });
  syncAssistantPanelResizerAvailability();
}

function assistantPanelStorageKey(job = currentJob) {
  // v2 resets the old stage-based collapsed preference after the split-glass
  // workspace redesign. Users can still collapse the assistant per task.
  return `cliptalk-assistant-expanded:v2:${String(job?.id || "new-task")}`;
}

function storedAssistantPanelPreference(job = currentJob) {
  try {
    const value = localStorage.getItem(assistantPanelStorageKey(job));
    return value == null ? null : value === "true";
  } catch { return null; }
}

function setAssistantPanelExpanded(expanded, { persist = false } = {}) {
  const open = Boolean(expanded);
  const wasCollapsed = Boolean(studio?.classList.contains("assistant-collapsed"));
  studio?.classList.toggle("assistant-collapsed", !open);
  syncAssistantPanelResizerAvailability();
  const toggle = $("#assistantPanelToggle");
  if (toggle) {
    toggle.setAttribute("aria-expanded", String(open));
    toggle.title = open ? "收起 AI 助手" : "展开 AI 助手";
    const icon = toggle.querySelector("span");
    if (icon) icon.textContent = open ? "‹" : "›";
  }
  if (persist) {
    try { localStorage.setItem(assistantPanelStorageKey(), String(open)); }
    catch { /* local preferences are optional */ }
  }
  if (wasCollapsed !== !open) {
    window.requestAnimationFrame(() => {
      // The editor, player and canvas timeline all depend on their container
      // width. Notify every owner after the grid has released/taken the
      // assistant column so none of them keep the previous measurements.
      window.dispatchEvent(new Event("resize"));
      scheduleMediaFrameFit(true);
      scheduleTimelineResizeRender(true);
    });
  }
  return open;
}

function portraitDesktopWorkspaceActive() {
  return window.innerWidth >= 1280
    && document.body.dataset.shellMode === "workspace"
    && $("#reviewView")?.dataset.reviewLayout === "portrait";
}

function syncAssistantPanelForStage() {
  const stored = storedAssistantPanelPreference();
  return setAssistantPanelExpanded(stored ?? true);
}

function syncStageNavigationPlacement() {
  const nav = $("#directorStageNav");
  const mount = $("#workspaceFlowMount");
  const panelHeader = document.querySelector(".chat-panel .panel-header");
  if (!nav || !panelHeader) return;
  if (currentJob && mount && nav.parentElement !== mount) mount.append(nav);
  if (!currentJob && nav.parentElement !== panelHeader) panelHeader.append(nav);
}

function syncUnifiedWorkspaceView() {
  const workflow = currentJob ? workflowKindForJob(currentJob) : (pendingCreateWorkflowKind || "highlight");
  const phase = ({ conversation: "brief", analysis: "analysis", events: "events", compose: "compose" })[directorStage] || "brief";
  const subjectPanelOpen = document.body.classList.contains("person-workspace-open")
    || document.body.classList.contains("voice-workspace-open");
  let selectionPurpose = "none";
  let subphase = "";
  let selectedIds = [];
  let focusedId = "";
  if (workflow === "person_edit" && subjectPanelOpen) {
    subphase = personReviewMode === "correct" ? "correct_person_clusters" : "select_person_targets";
    selectionPurpose = personReviewMode === "correct" ? "person_correction" : "include";
    selectedIds = personReviewMode === "correct" ? [...selectedCurrentPersonRanges] : [...selectedCurrentPersons];
    focusedId = focusedCurrentPersonRef;
  } else if (workflow === "speaker_edit" && subjectPanelOpen) {
    subphase = voiceReviewMode === "correct" ? "correct_voice_clusters" : "select_voice_targets";
    selectionPurpose = voiceReviewMode === "correct" ? "voice_correction" : "include";
    selectedIds = voiceReviewMode === "correct" ? [...selectedVoiceCorrectionVoices] : [...selectedCurrentVoices];
    focusedId = focusedCurrentVoiceRef;
  } else if (phase === "events") {
    selectionPurpose = "include";
  }
  unifiedWorkspaceView = window.ClipTalkWorkspaceState.deriveView({
    workflow, phase, subphase,
    panel: timelineExpanded ? "timeline" : phase === "compose" ? "versions" : reviewLowerPanelMode,
    mediaKind: viewerMediaKind === "output" ? "output" : "source",
    focusedId, selectedIds, selectionPurpose,
    assistantExpanded: !studio?.classList.contains("assistant-collapsed"),
  });
  if (studio) {
    studio.dataset.workflow = unifiedWorkspaceView.workflow;
    studio.dataset.workflowPhase = unifiedWorkspaceView.phase;
    studio.dataset.workflowSubphase = unifiedWorkspaceView.subphase;
    studio.dataset.selectionPurpose = unifiedWorkspaceView.selectionPurpose;
    studio.dataset.mediaKind = unifiedWorkspaceView.mediaKind;
  }
  return unifiedWorkspaceView;
}

$("#assistantPanelToggle")?.addEventListener("click", () => {
  const opening = Boolean(studio?.classList.contains("assistant-collapsed"));
  setAssistantPanelExpanded(opening, { persist: true });
  if (opening && portraitDesktopWorkspaceActive()) {
    window.ClipTalkWorkspaceController?.setRailExpanded?.(false, { persist: true, coordinate: false });
  }
  syncUnifiedWorkspaceView();
});

window.ClipTalkWorkspacePanels = {
  ...(window.ClipTalkWorkspacePanels || {}),
  setAssistantExpanded: setAssistantPanelExpanded,
  storedAssistantPreference: storedAssistantPanelPreference,
  portraitDesktopActive: portraitDesktopWorkspaceActive,
};

function syncWorkspaceState(overrides = {}) {
  if (overrides.home === false
    && !["settings", "library"].includes(String(document.body.dataset.shellView || ""))) {
    // Collapse the persistent history sidebar before any media/evidence panel
    // measures its available width. Waiting for the class observer's next
    // microtask made initial panel sizes depend on the previous page layout.
    window.ClipTalkAppShell?.showView("workspace", { route: false });
  }
  const next = workspaceStateMachine.update({
    job: currentJob,
    hasUpload: Boolean(videoInput?.files?.length),
    home: Boolean(studio?.classList.contains("home-mode")),
    ...overrides,
  });
  syncTaskCreationLayout();
  syncPortraitWorkspaceControls();
  requestAnimationFrame(() => window.ClipTalkWorkspaceController?.syncPortraitPanels?.(next));
  return next;
}

let subtitleReviewDraft = null;
let subtitleReviewResolver = null;
let subtitleReviewActiveCueId = null;
let subtitleReviewBusy = false;
let subtitleReviewReleaseFocus = null;

function subtitleCueId() {
  return `cue_local_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 9)}`;
}

function markSubtitleDraftChanged() {
  if (!subtitleReviewDraft) return;
  subtitleReviewDraft.status = "draft";
  subtitleReviewDraft.confirmedAt = null;
  const state = $("#subtitleSaveState");
  if (state) state.textContent = "有未确认修改";
}

function subtitlePositionLabel(style = {}) {
  const horizontal = ({ left: "左", center: "居中", right: "右" })[style.horizontal] || "居中";
  const vertical = ({ top: "顶部", middle: "中央", bottom: "底部" })[style.vertical] || "底部";
  return `${vertical}${horizontal}`;
}

function activeSubtitleCue() {
  return subtitleReviewDraft?.cues?.find((cue) => cue.id === subtitleReviewActiveCueId) || subtitleReviewDraft?.cues?.[0] || null;
}

function subtitleSuggestionRiskLabel(value) {
  return ({ low: "低风险", medium: "需核对", high: "高风险" })[value] || "需核对";
}

function subtitlePreviewMediaRect(stage, video) {
  const stageWidth = Math.max(1, stage?.clientWidth || 1);
  const stageHeight = Math.max(1, stage?.clientHeight || 1);
  const sourceWidth = Math.max(1, Number(video?.videoWidth || currentJob?.videoInfo?.width || 16));
  const sourceHeight = Math.max(1, Number(video?.videoHeight || currentJob?.videoInfo?.height || 9));
  const scale = Math.min(stageWidth / sourceWidth, stageHeight / sourceHeight);
  const width = sourceWidth * scale;
  const height = sourceHeight * scale;
  return { left: (stageWidth - width) / 2, top: (stageHeight - height) / 2, width, height };
}

function updateSubtitlePreview() {
  const cue = activeSubtitleCue();
  const text = $("#subtitlePreviewText");
  const stage = $("#subtitlePreviewStage");
  if (!text || !stage || !subtitleReviewDraft) return;
  if (cue && subtitleReviewActiveCueId !== cue.id) subtitleReviewActiveCueId = cue.id;
  const style = subtitleReviewDraft.cueStyleOverrides?.[cue?.id] || subtitleReviewDraft.globalStyle || {};
  const video = $("#subtitleReviewVideo");
  const mediaRect = subtitlePreviewMediaRect(stage, video);
  const horizontalBase = ({ left: 5, center: 50, right: 95 })[style.horizontal] ?? 50;
  const verticalBase = ({ top: 5, middle: 50, bottom: 95 })[style.vertical] ?? 95;
  const left = Math.max(5, Math.min(95, horizontalBase + Number(style.offsetXRatio || 0) * 100));
  const top = Math.max(5, Math.min(95, verticalBase + Number(style.offsetYRatio || 0) * 100));
  const speakerPrefix = cue?.showSpeakerLabel && cue?.speakerLabel ? `${cue.speakerLabel}：` : "";
  text.textContent = `${speakerPrefix}${cue?.text || "（空字幕不会烧录）"}`;
  text.style.left = `${mediaRect.left + mediaRect.width * left / 100}px`;
  text.style.top = `${mediaRect.top + mediaRect.height * top / 100}px`;
  text.style.maxWidth = `${mediaRect.width * .90}px`;
  text.style.fontSize = `${Math.max(8, Math.min(mediaRect.width, mediaRect.height) * Number(style.fontSizeRatio || .04))}px`;
  text.style.transform = `translate(${style.horizontal === "left" ? "0" : style.horizontal === "right" ? "-100%" : "-50%"},${style.vertical === "top" ? "0" : style.vertical === "bottom" ? "-100%" : "-50%"})`;
  const speakerColor = String(cue?.speakerColor || "").replace(/^0x/i, "#");
  text.style.color = speakerColor || (style.preset === "bold" ? "#ffe66d" : "#fff");
  text.style.fontWeight = ["bold", "social"].includes(style.preset) ? "750" : "500";
  text.style.background = style.preset === "social" ? "rgba(0,0,0,.55)" : "transparent";
  const safeArea = stage.querySelector(".subtitle-safe-area");
  if (safeArea) {
    safeArea.style.inset = "auto";
    safeArea.style.left = `${mediaRect.left + mediaRect.width * .05}px`;
    safeArea.style.top = `${mediaRect.top + mediaRect.height * .05}px`;
    safeArea.style.width = `${mediaRect.width * .90}px`;
    safeArea.style.height = `${mediaRect.height * .90}px`;
  }
  const summary = $("#subtitleStyleSummary");
  if (summary) summary.textContent = `字号 ${(Number(style.fontSizeRatio || .04) * 100).toFixed(1)}%（按画面短边） · ${subtitlePositionLabel(style)}${subtitleReviewDraft.cueStyleOverrides?.[cue?.id] ? " · 本条单独设置" : " · 全部字幕"}`;
  if (cue && video && Number.isFinite(Number(cue.sourceStart))) {
    const seek = () => { try { video.currentTime = Math.max(0, Number(cue.sourceStart)); } catch {} };
    if (video.readyState >= 1) seek(); else video.addEventListener("loadedmetadata", seek, { once: true });
  }
}

function renderSubtitleCueList() {
  const list = $("#subtitleCueList");
  if (!list || !subtitleReviewDraft) return;
  const cues = subtitleReviewDraft.cues || [];
  if (subtitleReviewActiveCueId && !cues.some((cue) => cue.id === subtitleReviewActiveCueId)) subtitleReviewActiveCueId = cues[0]?.id || null;
  const count = $("#subtitleCueCount");
  if (count) count.textContent = `${cues.length} 条字幕${new Set(cues.map((cue) => cue.outputIndex)).size > 1 ? ` · ${new Set(cues.map((cue) => cue.outputIndex)).size} 条成片` : ""}`;
  const context = subtitleReviewDraft.correctionContext || {};
  const contextPanel = $("#subtitleCorrectionContext");
  if (contextPanel) {
    const terms = (context.terms || []).slice(0, 12);
    contextPanel.classList.toggle("hidden", !context.generatedAt);
    contextPanel.innerHTML = context.generatedAt ? `<div><strong>本视频上下文</strong><span>${escapeHtml(context.summary || "已结合完整逐字稿建立校对上下文")}</span></div>${terms.length ? `<div class="subtitle-context-terms">${terms.map((item) => `<span title="${escapeHtml(item.evidence || "上下文证据")}">${escapeHtml(item.term)}</span>`).join("")}</div>` : ""}<small>词表只作为纠错旁证，不会直接覆盖识别文字。</small>` : "";
  }
  const lowRiskPending = cues.filter((cue) => cue.suggestionStatus === "pending" && cue.suggestionRisk === "low").length;
  const acceptSafe = $("#subtitleAcceptSafeButton");
  if (acceptSafe) {
    acceptSafe.classList.toggle("hidden", lowRiskPending < 1);
    acceptSafe.textContent = `接受低风险建议${lowRiskPending ? `（${lowRiskPending}）` : ""}`;
  }
  list.innerHTML = cues.length ? cues.map((cue, index) => `
    <article class="subtitle-cue ${cue.id === subtitleReviewActiveCueId ? "is-active" : ""}" data-subtitle-cue="${escapeHtml(cue.id)}">
      <div class="subtitle-cue-head"><span class="subtitle-cue-index">${String(index + 1).padStart(2, "0")}${Number(cue.outputIndex || 0) ? ` · 成片 ${Number(cue.outputIndex) + 1}` : ""}${cue.speakerLabel ? ` · <b class="subtitle-speaker" style="--subtitle-speaker-color:${escapeHtml(cue.speakerColor || "#fff")}">${escapeHtml(cue.speakerLabel)}</b>` : ""}</span><label class="subtitle-cue-time"><b>成片</b><input data-cue-start type="number" min="0" step="0.01" aria-label="字幕成片入点（秒）" value="${Number(cue.start || 0).toFixed(2)}"><span>→</span><input data-cue-end type="number" min="0" step="0.01" aria-label="字幕成片出点（秒）" value="${Number(cue.end || 0).toFixed(2)}"><span>秒</span></label></div>
      <textarea data-cue-text maxlength="500" aria-label="字幕文字">${escapeHtml(cue.text || "")}</textarea>
      ${cue.suggestionStatus === "pending" ? `<div class="subtitle-suggestion" data-risk="${escapeHtml(cue.suggestionRisk || "medium")}"><div class="subtitle-suggestion-head"><strong>AI 校对建议（未应用）</strong><span>${subtitleSuggestionRiskLabel(cue.suggestionRisk)}${cue.suggestionConfidence != null ? ` · ${Math.round(Number(cue.suggestionConfidence) * 100)}%` : ""}</span></div><p>${escapeHtml(cue.suggestedText || "")}</p><small>${escapeHtml(cue.suggestionReason || "根据完整逐字稿和相邻上下文")}</small>${cue.suggestionEvidence?.length ? `<ul>${cue.suggestionEvidence.map((value) => `<li>${escapeHtml(value)}</li>`).join("")}</ul>` : ""}<div class="subtitle-suggestion-actions"><button type="button" data-cue-accept>接受修改</button><button type="button" data-cue-ignore>保留原文</button></div></div>` : ""}
      <div class="subtitle-cue-actions"><button type="button" data-cue-play>试听定位</button><button type="button" data-cue-split>拆分</button><button type="button" data-cue-merge>与下一条合并</button><button type="button" data-cue-delete>删除</button></div>
    </article>`).join("") : `<p class="subtitle-ai-notice">当前没有字幕条目。可返回并选择不添加字幕。</p>`;
  list.querySelectorAll("[data-subtitle-cue]").forEach((card) => {
    const cue = cues.find((item) => item.id === card.dataset.subtitleCue);
    if (!cue) return;
    card.addEventListener("click", () => {
      subtitleReviewActiveCueId = cue.id;
      list.querySelectorAll(".subtitle-cue").forEach((node) => node.classList.toggle("is-active", node === card));
      updateSubtitlePreview();
    });
    card.querySelector("[data-cue-text]")?.addEventListener("input", (event) => {
      cue.text = event.target.value;
      if (cue.suggestionStatus === "pending") cue.suggestionStatus = "ignored";
      markSubtitleDraftChanged(); updateSubtitlePreview();
    });
    [["[data-cue-start]", "start"], ["[data-cue-end]", "end"]].forEach(([selector, key]) => card.querySelector(selector)?.addEventListener("change", (event) => {
      cue[key] = Number(event.target.value); markSubtitleDraftChanged();
    }));
    card.querySelector("[data-cue-play]")?.addEventListener("click", () => {
      showSource({ autoplay: false }); seekSourceTime(Number(cue.sourceStart ?? 0));
      const video = $("#subtitleReviewVideo"); if (video) { video.currentTime = Math.max(0, Number(cue.sourceStart ?? 0)); video.play().catch(() => {}); }
    });
    card.querySelector("[data-cue-accept]")?.addEventListener("click", () => {
      cue.text = cue.suggestedText || cue.text; cue.suggestionStatus = "accepted"; markSubtitleDraftChanged(); renderSubtitleCueList(); updateSubtitlePreview();
    });
    card.querySelector("[data-cue-ignore]")?.addEventListener("click", () => {
      cue.suggestionStatus = "ignored"; markSubtitleDraftChanged(); renderSubtitleCueList();
    });
    card.querySelector("[data-cue-delete]")?.addEventListener("click", () => {
      subtitleReviewDraft.cues = cues.filter((item) => item.id !== cue.id); markSubtitleDraftChanged(); renderSubtitleCueList(); updateSubtitlePreview();
    });
    card.querySelector("[data-cue-split]")?.addEventListener("click", () => {
      const duration = Number(cue.end) - Number(cue.start); if (duration < .2) return void showToast("这条字幕太短，无法继续拆分");
      const middle = Number(cue.start) + duration / 2;
      const textValue = String(cue.text || "");
      let textBreak = Math.floor(textValue.length / 2);
      for (let offset = 0; offset < textValue.length / 2; offset += 1) { const candidate = [textBreak + offset, textBreak - offset].find((value) => /[，。！？、；\s]/.test(textValue[value] || "")); if (candidate !== undefined) { textBreak = candidate + 1; break; } }
      const second = { ...cue, id: subtitleCueId(), start: Number(middle.toFixed(3)), text: textValue.slice(textBreak).trim(), originalText: textValue.slice(textBreak).trim(), suggestionStatus: "none", suggestedText: null };
      cue.end = Number(middle.toFixed(3)); cue.text = textValue.slice(0, textBreak).trim(); cue.suggestionStatus = "none";
      subtitleReviewDraft.cues.splice(cues.indexOf(cue) + 1, 0, second); markSubtitleDraftChanged(); renderSubtitleCueList();
    });
    card.querySelector("[data-cue-merge]")?.addEventListener("click", () => {
      const index = cues.indexOf(cue); const next = cues[index + 1];
      if (!next || Number(next.outputIndex || 0) !== Number(cue.outputIndex || 0)) return void showToast("没有可合并的下一条字幕");
      cue.end = Math.max(Number(cue.end), Number(next.end)); cue.text = [cue.text, next.text].filter(Boolean).join(" "); cue.suggestionStatus = "none";
      subtitleReviewDraft.cues.splice(index + 1, 1); markSubtitleDraftChanged(); renderSubtitleCueList(); updateSubtitlePreview();
    });
  });
  updateSubtitlePreview();
}

function setSubtitleReviewBusy(busy, label = "") {
  subtitleReviewBusy = Boolean(busy);
  $("#subtitleReview")?.querySelectorAll("button,input,textarea").forEach((node) => { node.disabled = subtitleReviewBusy; });
  const state = $("#subtitleSaveState"); if (state && label) state.textContent = label;
}

async function saveSubtitleReviewDraft(confirmed = false) {
  if (!currentJob || !subtitleReviewDraft || subtitleReviewBusy) return null;
  const actionToken = captureJobAction();
  const draftId = String(subtitleReviewDraft.id || "");
  setSubtitleReviewBusy(true, confirmed ? "正在确认字幕…" : "正在保存…");
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/subtitle-drafts/${encodeURIComponent(draftId)}`, {
      method: "PUT", headers: { "Content-Type": "application/json" }, body: { revision: subtitleReviewDraft.revision, cues: subtitleReviewDraft.cues, globalStyle: subtitleReviewDraft.globalStyle, cueStyleOverrides: subtitleReviewDraft.cueStyleOverrides || {}, confirmed, sourceSubtitleAcknowledged: Boolean($("#subtitleSourceAck")?.checked) },
    });
    if (!jobActionStillCurrent(actionToken)) return null;
    subtitleReviewDraft = payload.draft;
    const sourceAck = $("#subtitleSourceAck"); if (sourceAck) sourceAck.checked = Boolean(subtitleReviewDraft.sourceSubtitleAcknowledged);
    const confirmButton = $("#subtitleConfirmButton"); if (confirmButton) confirmButton.disabled = !sourceAck?.checked;
    const state = $("#subtitleSaveState"); if (state) state.textContent = confirmed ? "字幕已确认" : "草稿已保存，仍需确认";
    renderSubtitleCueList();
    return subtitleReviewDraft;
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); return null; }
  finally { if (jobActionStillCurrent(actionToken)) setSubtitleReviewBusy(false); }
}

function closeSubtitleReview(result = null) {
  const review = $("#subtitleReview");
  review?.classList.add("hidden");
  review?.setAttribute("aria-hidden", "true");
  if (review) review.dataset.overlayState = "closed";
  $("#subtitleReviewVideo")?.pause();
  subtitleReviewReleaseFocus?.();
  subtitleReviewReleaseFocus = null;
  const resolve = subtitleReviewResolver; subtitleReviewResolver = null;
  subtitleReviewDraft = null;
  subtitleReviewActiveCueId = null;
  setSubtitleReviewBusy(false);
  if (resolve) resolve(result);
}

function setSubtitleTranscriptionFeedback(transcription = null) {
  const state = transcription && typeof transcription === "object" ? transcription : null;
  const detail = String(state?.detail || "");
  const progress = Number(state?.progress);
  const percent = Number.isFinite(progress) && progress > 0 ? ` · ${Math.round(progress * 100)}%` : "";
  const label = detail ? `${detail}${percent}` : "";
  const editorButton = $("#secondaryEditorSubtitleReview");
  if (editorButton) {
    editorButton.dataset.idleLabel ||= editorButton.textContent || "建立并校对字幕";
    editorButton.textContent = label ? `识别对白${percent || "…"}` : editorButton.dataset.idleLabel;
  }
  const editorStatus = $("#secondaryEditorSubtitleStatus");
  if (editorStatus) {
    editorStatus.dataset.idleLabel ||= editorStatus.textContent || "字幕必须确认后才能烧录";
    editorStatus.textContent = label || editorStatus.dataset.idleLabel;
  }
  document.querySelectorAll("[data-content-subtitle-status]").forEach((node) => {
    node.dataset.idleLabel ||= node.textContent || "";
    node.textContent = label || node.dataset.idleLabel;
  });
}

async function reviewSubtitlesBeforeRender(outputs, subtitleStyle = "clean") {
  if (!currentJob || subtitleReviewBusy) return null;
  const actionToken = captureJobAction();
  const secondaryWasOpen = secondaryEditorOpen();
  setSubtitleReviewBusy(true, "正在建立字幕草稿…");
  let transcriptionAnnounced = false;
  let startTranscription = true;
  try {
    let payload = null;
    while (jobActionStillCurrent(actionToken)) {
      payload = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/subtitle-drafts`, {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: { outputs, subtitleStyle, startTranscription },
      });
      if (payload?.status !== "transcribing") break;
      if (secondaryWasOpen && !secondaryEditorOpen()) return null;
      const transcription = payload.transcription || {};
      setSubtitleTranscriptionFeedback(transcription);
      setSubtitleReviewBusy(true, transcription.detail || "正在识别所选视频的对白…");
      if (!transcriptionAnnounced) {
        showToast("当前任务尚无对白转写，正在自动识别；完成后会直接打开字幕校对。", "success");
        transcriptionAnnounced = true;
      }
      startTranscription = false;
      await new Promise((resolve) => window.setTimeout(resolve, Math.max(600, Number(payload.retryAfterMs || 1200))));
    }
    if (!jobActionStillCurrent(actionToken)) return null;
    if (!payload?.draft) throw new Error("字幕草稿建立失败，请重试");
    subtitleReviewDraft = payload.draft;
    subtitleReviewActiveCueId = subtitleReviewDraft.cues?.[0]?.id || null;
    window.ClipTalkPrepareWorkspaceOverlay?.("subtitle");
    const review = $("#subtitleReview");
    review?.classList.remove("hidden");
    review?.setAttribute("aria-hidden", "false");
    if (review) review.dataset.overlayState = "open";
    subtitleReviewReleaseFocus?.(false);
    subtitleReviewReleaseFocus = activateModalFocus(review?.querySelector(".subtitle-review-panel"), {
      initialFocus: review?.querySelector(".subtitle-review-close"),
      additionalActive: [review].filter(Boolean),
    });
    const sourceAck = $("#subtitleSourceAck"); if (sourceAck) sourceAck.checked = Boolean(subtitleReviewDraft.sourceSubtitleAcknowledged);
    const confirmButton = $("#subtitleConfirmButton"); if (confirmButton) confirmButton.disabled = !sourceAck?.checked;
    const video = $("#subtitleReviewVideo");
    const reviewSource = secondaryEditorOpen()
      ? (currentJob.previewUrl || currentJob.sourceUrl || $("#secondaryEditorVideo")?.dataset.sourceUrl || "")
      : (mainVideo?.currentSrc || "");
    if (video && reviewSource) { video.src = reviewSource; video.load(); }
    const state = $("#subtitleSaveState"); if (state) state.textContent = "识别草稿待人工确认";
    renderSubtitleCueList();
    setSubtitleReviewBusy(false);
    return await new Promise((resolve) => { subtitleReviewResolver = resolve; });
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); return null; }
  finally {
    setSubtitleTranscriptionFeedback(null);
    if (jobActionStillCurrent(actionToken)) setSubtitleReviewBusy(false);
  }
}

async function applySubtitleStyleCommand(command) {
  if (!currentJob || !subtitleReviewDraft || subtitleReviewBusy) return;
  const actionToken = captureJobAction();
  const draftId = String(subtitleReviewDraft.id || "");
  const status = $("#subtitleCommandStatus"); if (status) status.textContent = "正在理解命令…";
  setSubtitleReviewBusy(true);
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/subtitle-drafts/${encodeURIComponent(draftId)}/style-command`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: { text: command, cueId: subtitleReviewActiveCueId },
    });
    if (!jobActionStillCurrent(actionToken) || !subtitleReviewDraft || String(subtitleReviewDraft.id || "") !== draftId) return;
    const proposal = payload.proposal;
    if (proposal.scope === "cue" && proposal.cueId) subtitleReviewDraft.cueStyleOverrides = { ...(subtitleReviewDraft.cueStyleOverrides || {}), [proposal.cueId]: proposal.style };
    else subtitleReviewDraft.globalStyle = proposal.style;
    markSubtitleDraftChanged(); updateSubtitlePreview();
    if (status) status.textContent = `${proposal.summary} · ${proposal.scope === "cue" ? "仅当前字幕" : "已应用到全部字幕"}`;
  } catch (error) { if (jobActionStillCurrent(actionToken) && status) status.textContent = error.message; }
  finally { if (jobActionStillCurrent(actionToken)) setSubtitleReviewBusy(false); }
}

$("#subtitleReview")?.querySelectorAll("[data-subtitle-close]").forEach((button) => button.addEventListener("click", () => closeSubtitleReview(null)));
$("#subtitleReviewVideo")?.addEventListener("loadedmetadata", updateSubtitlePreview);
window.addEventListener("resize", () => {
  if (!$("#subtitleReview")?.classList.contains("hidden")) updateSubtitlePreview();
});
$("#subtitleSaveButton")?.addEventListener("click", () => saveSubtitleReviewDraft(false));
$("#subtitleConfirmButton")?.addEventListener("click", async () => { const draft = await saveSubtitleReviewDraft(true); if (draft) closeSubtitleReview(draft); });
$("#subtitleSourceAck")?.addEventListener("change", (event) => {
  const checked = Boolean(event.currentTarget?.checked);
  const button = $("#subtitleConfirmButton"); if (button) button.disabled = !checked;
  const state = $("#subtitleSaveState"); if (state && !checked) state.textContent = "请先确认原视频字幕状态";
});
$("#subtitleSuggestButton")?.addEventListener("click", async () => {
  if (!subtitleReviewDraft || subtitleReviewBusy) return;
  if (!await saveSubtitleReviewDraft(false)) return;
  const actionToken = captureJobAction();
  const draftId = String(subtitleReviewDraft?.id || "");
  if (!draftId) return;
  setSubtitleReviewBusy(true, "AI 正在理解完整逐字稿并逐句校对…");
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/subtitle-drafts/${encodeURIComponent(draftId)}/suggestions`, { method: "POST", headers: { "Content-Type": "application/json" }, body: {} });
    if (!jobActionStillCurrent(actionToken) || !subtitleReviewDraft || String(subtitleReviewDraft.id || "") !== draftId) return;
    subtitleReviewDraft = payload.draft; renderSubtitleCueList();
    const risks = payload.riskCounts || {};
    $("#subtitleSaveState").textContent = payload.suggestionCount ? `发现 ${payload.suggestionCount} 条建议 · ${Number(risks.low || 0)} 条低风险` : "未发现有充分证据的文字问题";
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) setSubtitleReviewBusy(false); }
});
$("#subtitleAcceptSafeButton")?.addEventListener("click", () => {
  if (!subtitleReviewDraft || subtitleReviewBusy) return;
  let accepted = 0;
  for (const cue of subtitleReviewDraft.cues || []) {
    if (cue.suggestionStatus !== "pending" || cue.suggestionRisk !== "low" || !cue.suggestedText) continue;
    cue.text = cue.suggestedText;
    cue.suggestionStatus = "accepted";
    accepted += 1;
  }
  if (!accepted) return;
  markSubtitleDraftChanged(); renderSubtitleCueList(); updateSubtitlePreview();
  $("#subtitleSaveState").textContent = `已应用 ${accepted} 条低风险建议，仍需确认字幕`;
});
$("#subtitleCommandForm")?.addEventListener("submit", (event) => { event.preventDefault(); const input = $("#subtitleCommandInput"); const value = input?.value.trim(); if (value) applySubtitleStyleCommand(value); });
$("#subtitleReview")?.querySelectorAll("[data-subtitle-command]").forEach((button) => button.addEventListener("click", () => applySubtitleStyleCommand(button.dataset.subtitleCommand)));

function captureJobAction(job = currentJob) {
  return { generation: workspaceGeneration, jobId: String(job?.id || "") };
}

function jobActionStillCurrent(token) {
  return Boolean(token)
    && token.generation === workspaceGeneration
    && !homeNavigationRequested
    && (!token.jobId || String(currentJob?.id || "") === token.jobId);
}

function commitJobAction(job, token, { allowJobSwitch = false } = {}) {
  if (!job?.id || !jobActionStillCurrent(token)) return false;
  const switchesJob = Boolean(token.jobId) && String(job.id) !== token.jobId;
  if (switchesJob && !allowJobSwitch) return false;
  // A derived task is the one intentional case where a response from the
  // current job is allowed to replace it with a new job id. Clear the old
  // identity only after the generation/id guard above has proved that the
  // response still belongs to the visible workspace. This lets renderJob run
  // its normal per-job reset path while stale responses remain rejected.
  if (switchesJob) {
    currentJob = null;
    currentJobRevision = "";
  }
  renderJob(job);
  return String(currentJob?.id || "") === String(job.id);
}

function invalidateWorkspaceRequests() {
  workspaceGeneration += 1;
  contentEvidenceRequestToken += 1;
  contentPreviewSequence = null;
  activeChatController?.abort();
  activeChatController = null;
  clearTimeout(pollTimer);
  pollTimer = null;
  actionBusy = false;
  draftBootstrapBusy = false;
  clearToast();
  resetCandidateDrawerContext();
  closeSubtitleReview(null);
  dismissActionConfirmation();
}

function createClientSessionId(prefix = "session") {
  return globalThis.crypto?.randomUUID?.() || `${prefix}_${Date.now()}_${Math.random().toString(16).slice(2)}`;
}

function beginTaskCreationSession() {
  currentCreationSessionId = createClientSessionId("create");
  window.ClipTalkAppShell?.syncCurrentJob?.(null);
  return currentCreationSessionId;
}

function activeCreationToken() {
  if (!currentCreationSessionId) beginTaskCreationSession();
  return { generation: workspaceGeneration, sessionId: currentCreationSessionId };
}

function creationTokenStillCurrent(token) {
  return Boolean(token)
    && token.generation === workspaceGeneration
    && String(token.sessionId || "") === String(currentCreationSessionId || "")
    && !currentJob;
}

function switchWorkspaceJob(job, { invalidate = true } = {}) {
  if (!job?.id) return false;
  if (invalidate) invalidateWorkspaceRequests();
  else {
    clearToast();
    resetCandidateDrawerContext();
    closeSubtitleReview(null);
    dismissActionConfirmation();
  }
  if (workflowKindForJob(job) !== "speaker_edit") closeVoiceProfiles();
  if (workflowKindForJob(job) !== "person_edit") closePersonProfiles();
  pendingWorkflowSwitch = "";
  pendingCapabilityCategory = "auto";
  pendingCapabilityOption = "";
  legacyWorkflowPickerOpen = false;
  renderQuickWorkflowPicker(null);
  currentCreationSessionId = "";
  pendingWorkflowOptions = { scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3 };
  quickWorkflowValidation = "";
  homeNavigationRequested = false;
  // Hard boundary between task conversations. Clear the old DOM before the
  // new task is rendered so late Agent/SSE updates cannot visually append to
  // the previous task's chat history.
  $("#chatMessages")?.replaceChildren();
  const coverTrack = $("#timelineCoverTrack");
  if (coverTrack) coverTrack.classList.add("hidden");
  $("#timelineCoverVariants")?.replaceChildren();
  $("#timelineCoverEditor")?.replaceChildren();
  currentJob = null;
  currentJobRevision = "";
  restoringHistory = true;
  try {
    renderJob(job);
  } finally {
    restoringHistory = false;
  }
  const workflowKind = workflowKindForJob(currentJob || job);
  if (workflowKind) document.body.dataset.ctWorkflowKind = workflowKind;
  else delete document.body.dataset.ctWorkflowKind;
  return String(currentJob?.id || "") === String(job.id);
}

function routeJobId() {
  try {
    const hash = String(window.location.hash || "").replace(/^#/, "");
    if (!hash) return null;
    return new URLSearchParams(hash).get("job") || null;
  } catch {
    return null;
  }
}

function setRouteJobId(jobId = null) {
  try {
    const url = new URL(window.location.href);
    const params = new URLSearchParams(String(url.hash || "").replace(/^#/, ""));
    if (jobId) {
      params.set("job", String(jobId));
    } else {
      params.delete("job");
    }
    url.hash = params.toString();
    window.history.replaceState(window.history.state, "", url.href);
  } catch { /* URL/history may be unavailable in embedded previews */ }
}

function rememberCurrentJob(job) {
  if (!job?.id) return;
  try { localStorage.setItem(currentJobStorageKey, String(job.id)); } catch { /* storage may be disabled */ }
  try { sessionStorage.setItem(currentJobStorageKey, String(job.id)); } catch { /* storage may be disabled */ }
  // The URL is tab-local and therefore wins over a shared localStorage value
  // when more than one task is open in the browser.
  setRouteJobId(job.id);
}

function forgetCurrentJob() {
  try { localStorage.removeItem(currentJobStorageKey); } catch { /* storage may be disabled */ }
  try { sessionStorage.removeItem(currentJobStorageKey); } catch { /* storage may be disabled */ }
  setRouteJobId(null);
}

function storedCurrentJobId() {
  try {
    return sessionStorage.getItem(currentJobStorageKey)
      || localStorage.getItem(currentJobStorageKey)
      || null;
  } catch {
    try { return sessionStorage.getItem(currentJobStorageKey) || null; } catch { return null; }
  }
}

function storedPortraitVideoWidth(jobId = currentJob?.id) {
  if (!jobId) return null;
  try {
    const saved = JSON.parse(localStorage.getItem(portraitVideoWidthStorageKey) || "{}");
    const width = Number(saved?.[String(jobId)]);
    return Number.isFinite(width) && width > 0 ? width : null;
  } catch {
    return null;
  }
}

function rememberPortraitVideoWidth(width, jobId = currentJob?.id) {
  if (!jobId || !Number.isFinite(Number(width))) return;
  let preferences = {};
  try {
    const saved = JSON.parse(localStorage.getItem(portraitVideoWidthStorageKey) || "{}");
    if (saved && typeof saved === "object" && !Array.isArray(saved)) preferences = saved;
  } catch { /* replace malformed legacy data below */ }
  preferences[String(jobId)] = Math.round(Number(width));
  try { localStorage.setItem(portraitVideoWidthStorageKey, JSON.stringify(preferences)); }
  catch { /* storage may be disabled */ }
}

function forgetPortraitVideoWidth(jobId = currentJob?.id) {
  if (!jobId) return;
  try {
    const saved = JSON.parse(localStorage.getItem(portraitVideoWidthStorageKey) || "{}");
    if (!saved || typeof saved !== "object" || Array.isArray(saved)) return;
    delete saved[String(jobId)];
    localStorage.setItem(portraitVideoWidthStorageKey, JSON.stringify(saved));
  } catch { /* storage may be disabled */ }
}

function portraitVideoWidthLimits() {
  const reviewView = $("#reviewView");
  const available = Math.max(0, reviewView?.clientWidth || 0);
  const availableHeight = Math.max(0, (reviewView?.clientHeight || 0) - 54);
  const mediaAspect = Math.max(.35, Math.min(.95, activeMediaAspect()));
  const heightFittedWidth = Math.round(Math.max(0, availableHeight - 70) * mediaAspect + 16);
  const minimum = Math.min(320, Math.max(280, Math.round(available * .28)));
  const leftMinimum = Math.min(460, Math.max(360, Math.round(available * .36)));
  const maximum = Math.max(minimum, Math.min(440, available - leftMinimum - 22));
  const widthFallback = Math.round(Math.min(420, available * .38));
  const fallback = Math.max(minimum, Math.min(maximum, heightFittedWidth || widthFallback));
  return { minimum, maximum, fallback };
}

function setPortraitVideoWidth(value, { persist = false } = {}) {
  const reviewView = $("#reviewView");
  const handle = $("#portraitVideoResizer");
  if (!reviewView || !handle) return 0;
  const { minimum, maximum, fallback } = portraitVideoWidthLimits();
  const requested = Number(value);
  const width = Math.round(Math.max(minimum, Math.min(maximum, Number.isFinite(requested) ? requested : fallback)));
  reviewView.style.setProperty("--portrait-video-width", `${width}px`);
  reviewView.dataset.portraitVideoWidth = String(width);
  handle.setAttribute("aria-valuemin", String(minimum));
  handle.setAttribute("aria-valuemax", String(maximum));
  handle.setAttribute("aria-valuenow", String(width));
  if (persist) rememberPortraitVideoWidth(width);
  scheduleMediaFrameFit(true);
  return width;
}

function restorePortraitVideoWidth() {
  const saved = storedPortraitVideoWidth();
  return setPortraitVideoWidth(saved ?? portraitVideoWidthLimits().fallback);
}

function syncPortraitWorkspaceControls() {
  const reviewView = $("#reviewView");
  const button = $("#portraitPrecisionToggle");
  if (!button || !reviewView) return;
  const portrait = reviewView.dataset.reviewLayout === "portrait";
  const workspaceState = document.body.dataset.workspaceState || "";
  const available = Boolean(portrait && currentJob && timelinePanel
    && ["reviewing", "composing", "completed"].includes(workspaceState));
  button.classList.toggle("hidden", !available);
  button.setAttribute("aria-pressed", String(Boolean(available && timelineExpanded)));
  button.setAttribute("aria-expanded", String(Boolean(available && timelineExpanded)));
  button.textContent = timelineExpanded ? "返回预览" : "查看时间线";
}

function syncPortraitHeaderActions() {
  const reviewView = $("#reviewView");
  const actions = reviewView?.querySelector(".review-header .asset-actions");
  const more = $("#reviewMoreActions");
  const moreMenu = more?.querySelector(":scope > div");
  const download = $("#downloadButton");
  if (!actions || !more || !moreMenu || !download) return;
  if (download.parentElement !== actions) {
    actions.insertBefore(download, more);
  }
}

function setReviewLayout(layout, { source = "auto" } = {}) {
  const normalized = layout === "portrait" ? "portrait" : "landscape";
  const reviewView = $("#reviewView");
  if (reviewView) {
    reviewView.dataset.reviewLayout = normalized;
    reviewView.dataset.reviewLayoutSource = source;
  }
  if (normalized === "portrait") restorePortraitVideoWidth();
  else {
    reviewView?.style.removeProperty("--portrait-shell-width");
    reviewView?.style.removeProperty("--portrait-shell-height");
  }
  syncPortraitWorkspaceControls();
  syncPortraitHeaderActions();
  syncReviewWorkbench();
  scheduleTimelineResizeRender(true);
  scheduleMediaFrameFit(true);
  requestAnimationFrame(() => window.ClipTalkWorkspaceController?.syncPortraitPanels?.());
  return normalized;
}

function currentReviewMediaIdentity() {
  const jobId = String(currentJob?.id || "pending");
  if (viewerMediaKind === "output" && currentOutput) {
    const outputId = String(currentOutput.filename || currentOutput.videoUrl || currentOutput.previewUrl || "output");
    return `${jobId}:output:${outputId}`;
  }
  return `${jobId}:source`;
}

function recommendedReviewLayout(aspect) {
  const normalized = Number(aspect);
  return Number.isFinite(normalized) && normalized > 0 && normalized < 1 ? "portrait" : "landscape";
}

function syncReviewLayoutForMedia(aspect) {
  if (sourceInspectionContext && sourceInspectionContext.jobId === currentJob?.id && viewerMediaKind !== "output") {
    setReviewLayout(sourceInspectionContext.layout, { source: "version-review" });
    return;
  }
  if (sourceInspectionContext?.jobId !== currentJob?.id) {
    sourceInspectionContext = null;
    $("#returnInspectedVersion")?.classList.add("hidden");
  }
  const mediaIdentity = currentReviewMediaIdentity();
  const nextLayout = recommendedReviewLayout(aspect);
  const reviewView = $("#reviewView");
  const mediaChanged = Boolean(reviewLayoutMediaIdentity && reviewLayoutMediaIdentity !== mediaIdentity);
  const layoutChanged = Boolean(reviewView?.dataset.reviewLayout && reviewView.dataset.reviewLayout !== nextLayout);
  reviewLayoutMediaIdentity = mediaIdentity;
  // A timeline opened for the previous media/layout must not silently turn a
  // newly selected portrait video into the dense precision workspace.
  if ((layoutChanged || mediaChanged && nextLayout === "portrait") && timelineExpanded) setReviewLowerPanelMode("collapsed");
  setReviewLayout(nextLayout, { source: "auto" });
}

async function updateCurrentProjectSettings(patch = {}) {
  if (!currentJob?.id) throw new Error("请先创建或打开一个剪辑任务");
  const settings = {};
  if (Object.hasOwn(patch, "outputAspect")) {
    if (!["source", "16:9", "9:16"].includes(patch.outputAspect)) throw new Error("成片画幅设置无效");
    settings.outputAspect = patch.outputAspect;
  }
  if (Object.hasOwn(patch, "outputFit")) {
    if (!["blur", "crop"].includes(patch.outputFit)) throw new Error("画幅适配方式无效");
    if (!Object.hasOwn(currentJob.projectSettings || {}, "outputFit")) throw new Error("画幅设置功能已更新，请刷新页面后重试；如仍无法保存，请联系管理员重启服务");
    settings.outputFit = patch.outputFit;
  }
  const actionToken = captureJobAction();
  const { job } = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/project-settings`, {
    method: "PATCH",
    headers: { "Content-Type": "application/json" },
    body: settings,
  });
  if (!commitJobAction(job, actionToken)) throw new Error("当前任务已经切换，设置未应用到新任务");
  window.ClipTalkWorkspaceController?.syncProjectControls?.();
  return job;
}

async function planCurrentOutputAspect(aspect, filename = currentOutput?.filename, fit = "blur") {
  const normalized = String(aspect || "");
  if (!["16:9", "9:16"].includes(normalized)) return false;
  if (!currentJob || !orderedJobOutputs(currentJob).length) {
    showToast(`已保存 ${normalized} 成片画幅，将在生成成片时应用`, "success");
    return false;
  }
  const entry = locateJobOutput(filename);
  if (!entry) return void showToast("请先选择要转换画幅的版本");
  const identity = `V${entry.version?.number || 1} · ${entry.output.filename}`;
  const actionToken = captureJobAction();
  const method = fit === "crop" ? "居中裁切填满画布，允许裁去画面边缘" : "完整保留原画面，使用虚化背景补齐画布";
  if (!await requestActionConfirmation({ title: `生成 ${normalized} 版本`, summary: `基于 ${identity}`, details: [method, "新版本单独保留，原版本保持不变"], confirmLabel: "生成画幅方案" })) return false;
  if (!jobActionStillCurrent(actionToken) || !locateJobOutput(entry.output.filename)) return void showToast("任务或版本已变化，请重新选择后生成");
  const submitted = await window.ClipTalkAgentWorkspace?.submitGoal?.(
    `基于当前任务的指定成片 ${identity} 生成 ${normalized} 画幅审核版本，${method}，不覆盖原成片。`,
    { visibleGoal: `基于 ${identity} 生成 ${normalized} 画幅审核版本` },
  );
  return Boolean(submitted);
}

window.ClipTalkUpdateProjectSettings = updateCurrentProjectSettings;
window.ClipTalkPlanOutputAspect = planCurrentOutputAspect;

function setDirectorWorkspaceEmpty(visible) {
  if (visible) {
    const dock = document.querySelector("#autoCompositionDock");
    dock?.classList.add("hidden");
    if (dock) dock.innerHTML = "";
    autoCompositionDockJobId = "";
    autoCompositionLogExpanded = false;
  }
  const empty = $(".director-workspace-empty");
  if (!empty) return;
  empty.classList.toggle("hidden", !visible);
  // Keep this explicit as a guard against legacy theme rules or stale layout
  // classes making the onboarding placeholder reappear over a real task.
  empty.style.display = visible ? "" : "none";
}

function setupDirectorWorkspace() {
  const host = $("#chatStageHost");
  const rail = $(".review-rail");
  if (!host || !rail || !studio || host.dataset.ready === "true") return;
  // Keep every task-stage surface in one persistent host.  In particular the
  // progress console must not remain in the rail while the chat renderer
  // rebuilds #chatMessages; otherwise it is removed and re-inserted on every
  // poll, which looks like a flicker.
  ["jobStatus", "railBody", "clipSection", "railOutput"].forEach((id) => {
    const node = document.getElementById(id);
    if (node) host.append(node);
  });
  host.dataset.ready = "true";
  setupAssistantPanelResizer();
  studio.classList.add("director-merged");
  rail.classList.add("merged-review-rail");
  $(".panel-resizer-right")?.classList.add("hidden");
  const dockMode = $("#reviewActionMode");
  const dockToggle = $("#reviewActionToggle");
  $("#reviewActionPrimary")?.addEventListener("click", runReviewDockPrimaryAction);
  $("#reviewActionSettings")?.addEventListener("click", toggleReviewDockSettings);
  dockMode?.addEventListener("change", applyReviewDockMode);
  dockToggle?.addEventListener("change", applyReviewDockToggle);
  host.addEventListener("change", () => window.requestAnimationFrame(syncReviewActionDock));
  setDirectorStage("conversation");
}

function reviewWorkbenchPresentation(job = currentJob) {
  const workflow = workflowKindForJob(job);
  const panelOpen = document.body.classList.contains("voice-workspace-open")
    ? "voice" : document.body.classList.contains("person-workspace-open") ? "person" : "";
  if (panelOpen === "voice") return timelineExpanded
    ? { kicker: "发言时间", title: "说话人与发言时间", hint: "在下方轨道直接勾选声音；点击发言区间试听源片" }
    : voiceReviewMode === "correct"
      ? { kicker: "声音校正", title: "第 1 步 · 校正声音分组", hint: `${currentVoices.length ? `${currentVoices.length} 个声音 · ` : ""}试听、合并或移动错误发言；不会改变最终剪辑选择` }
      : { kicker: "剪辑目标", title: "第 2 步 · 选择剪辑目标", hint: "勾选要保留或排除的声音，再查看对应发言片段" };
  if (panelOpen === "person") return timelineExpanded
    ? { kicker: "出镜时间", title: "人物与出镜时间", hint: "在下方轨道直接勾选人物；点击出镜区间预览源画面" }
    : personReviewMode === "correct"
      ? { kicker: "人物校正", title: "第 1 步 · 校正人物分组", hint: `${currentPersonCatalog(job).length ? `${currentPersonCatalog(job).length} 个人物 · ` : ""}合并重复人物或修正错误出镜段` }
      : { kicker: "剪辑目标", title: "第 2 步 · 选择剪辑目标", hint: "播放人物片段并勾选，确认后再提取出镜内容" };
  if (timelineExpanded && directorStage !== "compose") {
    if (workflow === "speaker_edit") return { kicker: "发言时间", title: "核对发言来源时间", hint: "精细时间轴已展开；返回审核列表可调整全部发言" };
    if (workflow === "person_edit") return { kicker: "出镜时间", title: "核对人物来源时间", hint: "精细时间轴已展开；返回审核列表可调整全部出镜片段" };
    if (workflow === "content_search") return { kicker: "片段边界", title: "核对片段与边界", hint: "精细时间轴已展开；返回审核列表可调整全部匹配内容" };
    return { kicker: "镜头时间", title: "核对事件与镜头时间", hint: "精细时间轴已展开；返回审核列表可调整全部推荐事件" };
  }
  if (directorStage === "compose") return { kicker: "版本与交付", title: "版本与交付", hint: "生成的审核样片和成片版本集中在这里，可播放或下载" };
  if (workflow === "speaker_edit") return { kicker: "发言片段", title: "确认说话人发言片段", hint: "预览不会改变边界，确认后才会生成" };
  if (workflow === "person_edit") return { kicker: "出镜片段", title: "第 2 步 · 核对出镜片段", hint: "勾选最终保留范围；下一步只负责合成，不会再改变片段" };
  if (workflow === "content_search") return { kicker: "匹配片段", title: "确认找到的内容", hint: "点击正文预览；需要修改时使用“调整边界”" };
  return { kicker: "高光事件", title: "审核推荐事件", hint: "选择事件并预览镜头，确认后再生成成片" };
}

function reviewPanelSwitchPresentation(job = currentJob) {
  const workflow = workflowKindForJob(job);
  let label = "审核内容";
  let count = 0;
  if (directorStage === "compose") {
    label = "成片版本";
    count = jobOutputCount(job);
  } else if (workflow === "speaker_edit") {
    label = "发言片段";
    count = Number(job?.contentSearch?.candidates?.length || job?.candidates?.length || 0);
  } else if (workflow === "person_edit") {
    label = "出镜片段";
    count = Number(job?.contentSearch?.candidates?.length || job?.candidates?.length || 0);
  } else if (workflow === "content_search") {
    label = "内容片段";
    count = Number(job?.contentSearch?.candidates?.length || 0);
  } else {
    label = "高光事件";
    count = Number(job?.eventGroups?.length || job?.candidates?.length || 0);
  }
  return { label, count };
}

function reviewSubjectPanelPresentation(job = currentJob) {
  const workflow = workflowKindForJob(job);
  if (workflow === "person_edit") {
    const hasClips = Number(job?.contentSearch?.candidates?.length || 0) > 0;
    return { visible: true, kind: "person", label: hasClips ? "重新选人物" : "画面人物", count: currentPersonCatalog(job).length, controls: "personProfilePanel" };
  }
  if (workflow === "speaker_edit") {
    return { visible: true, kind: "voice", label: "说话人", count: currentVoices.length, controls: "voiceProfilePanel" };
  }
  return { visible: false, kind: "", label: "", count: 0, controls: "" };
}

function reviewPanelDeckAvailable(available) {
  return Boolean(available && $("#reviewView")?.dataset.reviewLayout !== "portrait");
}

function syncReviewPanelSwitch(available, deckEnabled) {
  const switcher = $("#reviewPanelSwitch");
  const reviewView = $("#reviewView");
  if (!switcher || !reviewView) return;
  switcher.classList.toggle("hidden", !deckEnabled);
  reviewView.classList.toggle("lower-panel-deck", deckEnabled);
  document.body.classList.toggle("review-lower-panel-deck", deckEnabled);
  reviewView.dataset.lowerPanelMode = deckEnabled ? reviewLowerPanelMode : "review";
  const presentation = reviewPanelSwitchPresentation(currentJob);
  const subjectPresentation = reviewSubjectPanelPresentation(currentJob);
  const label = $("#reviewPanelReviewLabel");
  const count = $("#reviewPanelReviewCount");
  if (label) label.textContent = presentation.label;
  if (count) {
    count.textContent = String(presentation.count);
    count.classList.toggle("hidden", presentation.count <= 0);
  }
  const subjectButton = $("#reviewPanelSubjectTab");
  const subjectLabel = $("#reviewPanelSubjectLabel");
  const subjectCount = $("#reviewPanelSubjectCount");
  const subjectOpen = subjectPresentation.kind === "person"
    ? document.body.classList.contains("person-workspace-open")
    : subjectPresentation.kind === "voice" && document.body.classList.contains("voice-workspace-open");
  const subjectActive = deckEnabled && subjectOpen && reviewLowerPanelMode === "review";
  if (subjectButton) {
    subjectButton.classList.toggle("hidden", !deckEnabled || !subjectPresentation.visible);
    subjectButton.classList.toggle("active", subjectActive);
    subjectButton.dataset.subjectKind = subjectPresentation.kind;
    subjectButton.setAttribute("aria-controls", subjectPresentation.controls || "reviewWorkbench");
    subjectButton.setAttribute("aria-expanded", String(subjectActive));
    subjectButton.setAttribute("aria-pressed", String(subjectActive));
  }
  if (subjectLabel) subjectLabel.textContent = subjectPresentation.label;
  if (subjectCount) {
    subjectCount.textContent = String(subjectPresentation.count);
    subjectCount.classList.toggle("hidden", subjectPresentation.count <= 0);
  }
  const candidateButton = $("#openCandidateDrawer");
  const candidateCount = $("#reviewPanelCandidateCount");
  const candidateTotal = Number(currentJob?.candidates?.length || 0);
  // Keep reviewed source candidates available after composition so users can
  // revisit the evidence and build another version from the same task.
  const candidateVisible = deckEnabled && workflowKindForJob(currentJob) === "highlight"
    && candidateTotal > 0;
  const candidateActive = candidateVisible && candidateRailOpen;
  if (candidateButton) {
    candidateButton.classList.toggle("hidden", !candidateVisible);
    candidateButton.classList.toggle("active", candidateActive);
    candidateButton.disabled = !candidateVisible;
    candidateButton.setAttribute("aria-expanded", String(candidateActive));
    candidateButton.setAttribute("aria-pressed", String(candidateActive));
  }
  if (candidateCount) {
    candidateCount.textContent = String(candidateTotal);
    candidateCount.classList.toggle("hidden", candidateTotal <= 0);
  }
  const timelineAvailable = available && Boolean(currentJob);
  document.querySelectorAll("[data-review-panel-mode]").forEach((button) => {
    const mode = button.dataset.reviewPanelMode;
    const active = deckEnabled && reviewLowerPanelMode === mode && !(mode === "review" && subjectOpen);
    button.classList.toggle("active", active);
    button.setAttribute("aria-expanded", String(active));
    button.setAttribute("aria-pressed", String(active));
    if (mode === "timeline") button.disabled = !timelineAvailable;
  });
  const hint = $("#reviewPanelSwitchHint");
  if (hint) hint.textContent = candidateActive
    ? "正在查看全部镜头候选"
    : reviewLowerPanelMode === "review"
    ? `正在查看${subjectActive ? subjectPresentation.label : presentation.label}`
    : reviewLowerPanelMode === "timeline" ? "正在精细核对源片时间" : "视频优先 · 选择面板后展开";
}

function syncReviewWorkbench() {
  const workbench = $("#reviewWorkbench");
  const host = $("#chatStageHost");
  if (!workbench || !host) return;
  const panelOpen = document.body.classList.contains("voice-workspace-open")
    || document.body.classList.contains("person-workspace-open");
  const available = Boolean(currentJob) && (panelOpen || directorStage === "events" || directorStage === "compose");
  const reviewView = $("#reviewView");
  const deckEnabled = reviewPanelDeckAvailable(available);
  const visible = available && (!deckEnabled || reviewLowerPanelMode === "review") && !timelineExpanded;
  const presentation = reviewWorkbenchPresentation(currentJob);
  workbench.classList.toggle("hidden", !visible);
  reviewView?.classList.toggle("workbench-visible", visible);
  reviewView?.classList.toggle("workbench-output-mode", visible && directorStage === "compose" && !panelOpen);
  const workbenchKind = available ? workflowKindForJob(currentJob) : "";
  const contextual = visible && !timelineExpanded && (deckEnabled || panelOpen);
  if (reviewView) reviewView.dataset.workbenchKind = workbenchKind;
  reviewView?.classList.toggle("workbench-contextual", contextual);
  workbench.dataset.reviewKind = workbenchKind;
  host.classList.toggle("subject-panel-hidden", panelOpen);
  const kicker = $("#reviewWorkbenchKicker");
  const title = $("#reviewWorkbenchTitle");
  const hint = $("#reviewWorkbenchHint");
  if (kicker) kicker.textContent = presentation.kicker;
  if (title) title.textContent = presentation.title;
  if (hint) hint.textContent = presentation.hint;
  syncReviewPanelSwitch(available, deckEnabled);
  syncReviewActionDock();
  syncPrecisionTimelineToolbar();
  window.requestAnimationFrame(() => scheduleMediaFrameFit(true));
}

function setReviewDockOptions(select, options = [], value = "") {
  if (!select) return;
  const signature = JSON.stringify(options);
  if (select.dataset.options !== signature) {
    select.innerHTML = options.map((option) => `<option value="${escapeHtml(option.value)}">${escapeHtml(option.label)}</option>`).join("");
    select.dataset.options = signature;
  }
  if ([...select.options].some((option) => option.value === String(value))) select.value = String(value);
}

function reviewDockContentRoot() {
  return $("#chatStageHost")?.querySelector(".content-search-review:not(.historical)") || null;
}

function contentReviewRootsForSearch(job = currentJob, searchId = "") {
  const targetSearchId = String(
    searchId || job?.contentSearchSession?.activeSearchId || job?.contentSearch?.id || "",
  );
  if (!targetSearchId) return [];
  return [...document.querySelectorAll(".content-search-review[data-content-search-id]")]
    .filter((root) => String(root.dataset.contentSearchId || "") === targetSearchId);
}

function activeContentReviewRoot(job = currentJob, { searchId = "", preferredRoot = null } = {}) {
  const roots = contentReviewRootsForSearch(job, searchId);
  if (preferredRoot?.isConnected && roots.includes(preferredRoot)) return preferredRoot;
  return roots.find((root) => root.closest("#chatStageHost") && !root.classList.contains("historical"))
    || roots.find((root) => root.closest("#reviewWorkbench") && !root.classList.contains("historical"))
    || roots.find((root) => !root.hidden && !root.classList.contains("hidden") && !root.closest(".hidden"))
    || roots.find((root) => !root.classList.contains("historical"))
    || roots[0]
    || null;
}

function selectedHighlightReviewGroups(job = currentJob, host = $("#chatStageHost")) {
  if (!job || !host) return [];
  const railInputs = [...host.querySelectorAll(".rail-event-check")];
  const groupInputs = railInputs.length ? railInputs : [...host.querySelectorAll(".event-group-check")];
  return groupInputs.filter((input) => input.checked).map((input) => {
    const group = (job.eventGroups || []).find((item) => String(item.id) === String(input.value));
    if (!group) return null;
    let segments = [...(group.segments || [])];
    if (railInputs.length) {
      const segmentInputs = [...(input.closest(".event-group-row")?.querySelectorAll(".rail-segment-check") || [])];
      if (segmentInputs.length) {
        const selectedIds = new Set(segmentInputs.filter((item) => item.checked).map((item) => String(item.value)));
        segments = segments.filter((segment) => selectedIds.has(String(segment.id)));
      }
    }
    return { ...group, segments };
  }).filter((group) => group?.segments?.length);
}

function syncReviewActionDock() {
  const dock = $("#reviewActionDock");
  if (!dock) return;
  const kicker = $("#reviewActionKicker");
  const title = $("#reviewActionTitle");
  const meta = $("#reviewActionMeta");
  const modeWrap = $("#reviewActionModeWrap");
  const modeLabel = $("#reviewActionModeLabel");
  const mode = $("#reviewActionMode");
  const toggleWrap = $("#reviewActionToggleWrap");
  const toggle = $("#reviewActionToggle");
  const toggleLabel = $("#reviewActionToggleLabel");
  const settings = $("#reviewActionSettings");
  const primary = $("#reviewActionPrimary");
  const subjectPanelOpen = document.body.classList.contains("voice-workspace-open")
    || document.body.classList.contains("person-workspace-open");
  if (!currentJob || (directorStage === "compose" && !subjectPanelOpen)
    || ($("#reviewWorkbench")?.classList.contains("hidden") && !timelineExpanded)) {
    dock.classList.add("hidden");
    dock.dataset.kind = "";
    return;
  }
  modeWrap?.classList.add("hidden");
  toggleWrap?.classList.add("hidden");
  settings?.classList.add("hidden");
  dock.classList.remove("hidden");
  let kind = "";
  let selectedCount = 0;
  let duration = 0;
  let disabled = false;

  if (document.body.classList.contains("voice-workspace-open")) {
    kind = "voice";
    const selected = currentVoices.filter((voice) => selectedCurrentVoices.has(String(voice.speakerRef)));
    selectedCount = selected.length;
    duration = selected.reduce((sum, voice) => sum + Number(voice.speechSeconds || 0), 0);
    if (kicker) kicker.textContent = "声音选择";
    if (title) title.textContent = selectedCount ? `已选 ${selectedCount} 个声音` : "选择要保留的声音";
    if (meta) meta.textContent = selectedCount ? `发言合计约 ${duration.toFixed(1)} 秒 · 生成前仍可审核片段` : "试听代表片段并勾选，预览不会改变选择";
    modeWrap?.classList.remove("hidden");
    if (modeLabel) modeLabel.textContent = "剪辑方式";
    setReviewDockOptions(mode, [
      { value: "include", label: "只保留所选声音" },
      { value: "exclude", label: "排除所选声音" },
      { value: "qa_pair", label: "回答及前置问题" },
    ], $("#currentVoiceMode")?.value || "include");
    if (settings && currentVoiceTimeline.length) {
      settings.textContent = "校正归属";
      settings.classList.remove("hidden");
    }
    if (primary) primary.textContent = "查看所选发言";
    disabled = !selectedCount || (mode?.value === "qa_pair" && selectedCount !== 1) || actionBusy;
  } else if (document.body.classList.contains("person-workspace-open")) {
    kind = "person";
    const selected = currentPersonCatalog(currentJob).filter((person) => selectedCurrentPersons.has(String(person.id)));
    selectedCount = selected.length;
    duration = selected.reduce((sum, person) => sum + personAppearanceSeconds(person), 0);
    if (kicker) kicker.textContent = "人物选择";
    if (title) title.textContent = selectedCount ? `已选 ${selectedCount} 个人物` : "选择画面人物";
    if (meta) meta.textContent = selectedCount ? `原始出镜合计约 ${duration.toFixed(1)} 秒 · 实际结果会合并重叠范围` : "先播放出镜片段，再勾选需要保留的人物";
    if (selectedCount > 1) {
      modeWrap?.classList.remove("hidden");
      if (modeLabel) modeLabel.textContent = "多人条件";
      setReviewDockOptions(mode, [
        { value: "any", label: "任一人物出现" },
        { value: "all", label: "所有人物同框" },
      ], currentPersonMatchMode);
    }
    toggleWrap?.classList.remove("hidden");
    if (toggle) toggle.checked = currentPersonActivity === "speaking";
    if (toggleLabel) toggleLabel.textContent = "只保留正在说话";
    if (primary) primary.textContent = currentPersonActivity === "speaking" ? "整理人物发言片段" : "整理人物出镜片段";
    disabled = !selectedCount || actionBusy;
  } else {
    const contentRoot = reviewDockContentRoot();
    if (contentRoot && workflowKindForJob(currentJob) !== "highlight") {
      kind = "content";
      const ids = new Set([...contentRoot.querySelectorAll("[data-content-match]:checked")].map((input) => String(input.value)));
      const selected = (currentJob.contentSearch?.candidates || []).filter((item) => ids.has(String(item.id)));
      const timing = contentBasketTimingSummary(selected);
      selectedCount = selected.length;
      duration = timing.uniqueDuration;
      const output = contentRoot.querySelector("[data-content-output-mode]");
      const personContent = workflowKindForJob(currentJob) === "person_edit";
      if (kicker) kicker.textContent = personContent ? "第 2 步 · 出镜片段" : "内容选择";
      if (title) title.textContent = selectedCount
        ? `已核对 ${selectedCount} 个${personContent ? "出镜" : ""}片段`
        : personContent ? "核对需要保留的出镜片段" : "选择需要生成的片段";
      if (meta) meta.textContent = selectedCount ? `成片约 ${duration.toFixed(1)} 秒${timing.overlapCount ? ` · 已合并 ${timing.overlapCount} 处重叠` : ""}` : "预览或调整边界后勾选片段";
      if (selectedCount > 1 && output) {
        modeWrap?.classList.remove("hidden");
        if (modeLabel) modeLabel.textContent = "输出";
        setReviewDockOptions(mode, [
          { value: "single_reel", label: "合成一条视频" },
          { value: "separate_events", label: "每段分别导出" },
        ], output.value || "single_reel");
      }
      if (settings && contentRoot.querySelector(".content-generation-settings:not(.hidden)")) {
        settings.textContent = "生成设置";
        settings.classList.remove("hidden");
      }
      if (primary) primary.textContent = personContent ? "核对完成，合成视频" : "生成所选片段";
      disabled = !selectedCount || Boolean(contentRoot.querySelector("[data-confirm-content]")?.disabled) || actionBusy;
    } else {
      const host = $("#chatStageHost");
      const railGroupInputs = [...(host?.querySelectorAll(".rail-event-check") || [])];
      const groups = railGroupInputs.length
        ? railGroupInputs.filter((input) => input.checked)
        : [...(host?.querySelectorAll(".event-group-check:checked") || [])];
      const candidates = [...(host?.querySelectorAll(".candidate-list input:checked") || [])];
      kind = groups.length || host?.querySelector(".event-group-list") ? "highlight-groups" : "highlight-candidates";
      if (kind === "highlight-groups") {
        const selected = selectedHighlightReviewGroups(currentJob, host);
        selectedCount = selected.length;
        const segmentCount = selected.reduce((sum, group) => sum + group.segments.length, 0);
        duration = selected.reduce((sum, group) => sum + group.segments.reduce((inner, segment) => inner + Number(segment.effectiveDuration || segment.duration || (Number(segment.end) - Number(segment.start)) || 0), 0), 0);
        if (!railGroupInputs.length) {
          modeWrap?.classList.remove("hidden");
          if (modeLabel) modeLabel.textContent = "输出";
          setReviewDockOptions(mode, [
            { value: "single_reel", label: "合成一条视频" },
            { value: "separate_events", label: "事件分别导出" },
          ], mode?.dataset.highlightValue || "single_reel");
        }
        if (title) title.textContent = selectedCount ? `已选 ${selectedCount} 个事件 · ${segmentCount} 个镜头` : "选择高光事件";
        if (meta) meta.textContent = selectedCount ? `预计 ${duration.toFixed(1)} 秒 · 下一步确认输出方式` : "预览并勾选需要进入成片的内容";
        if (primary) primary.textContent = railGroupInputs.length ? "继续生成成片" : "生成所选高光";
      } else {
        const selected = candidates.map((input) => currentJob.candidates?.find((item) => Number(item.index) === Number(input.value))).filter(Boolean);
        selectedCount = selected.length;
        duration = selected.reduce((sum, item) => sum + Number(item.duration || 0), 0);
        if (title) title.textContent = selectedCount ? `已选 ${selectedCount} 个高光镜头` : "选择高光镜头";
      }
      if (kicker) kicker.textContent = "高光选择";
      if (kind !== "highlight-groups" || !railGroupInputs.length) {
        if (meta) meta.textContent = selectedCount ? `预计 ${duration.toFixed(1)} 秒 · 生成前不会覆盖已有版本` : "预览并勾选需要进入成片的内容";
        if (primary) primary.textContent = "生成所选高光";
      }
      disabled = !selectedCount || actionBusy;
    }
  }
  dock.dataset.kind = kind;
  if (primary) primary.disabled = disabled;
  if (timelineExpanded && kind) compactPrecisionSelectionTitle(precisionTimelineKind());
}

function precisionTimelineKind(job = currentJob) {
  if (!job) return "";
  if (document.body.classList.contains("voice-workspace-open")) return "voice";
  if (document.body.classList.contains("person-workspace-open")) return "person";
  if (directorStage === "compose" && jobOutputCount(job) > 0) return "output";
  return workflowKindForJob(job) === "content_search" ? "content" : "highlight";
}

function restoreTimelinePrecisionDrawerContent() {
  const origin = timelinePrecisionDrawerOrigin;
  timelinePrecisionDrawerOrigin = null;
  if (!origin?.node || !origin.parent?.isConnected) return;
  if (origin.next?.parentElement === origin.parent) origin.parent.insertBefore(origin.node, origin.next);
  else origin.parent.append(origin.node);
}

function closeTimelinePrecisionDrawer({ restoreFocus = false } = {}) {
  const drawer = $("#timelinePrecisionDrawer");
  if (!drawer) return;
  restoreTimelinePrecisionDrawerContent();
  drawer.classList.add("hidden");
  drawer.setAttribute("aria-hidden", "true");
  drawer.dataset.overlayState = "closed";
  drawer.inert = true;
  drawer.dataset.kind = "";
  const body = $("#timelinePrecisionDrawerBody");
  if (body) body.innerHTML = "";
  document.body.classList.remove("timeline-precision-drawer-open");
  timelinePrecisionDrawerTrigger?.setAttribute?.("aria-expanded", "false");
  if (restoreFocus && timelinePrecisionDrawerTrigger?.isConnected) timelinePrecisionDrawerTrigger.focus();
  timelinePrecisionDrawerTrigger = null;
}

function closeTimelinePrecisionDrawerFromUser() {
  closeTimelinePrecisionDrawer({ restoreFocus: true });
}

function openTimelinePrecisionDrawer(title, kind, trigger, node = null) {
  const drawer = $("#timelinePrecisionDrawer");
  const body = $("#timelinePrecisionDrawerBody");
  if (!drawer || !body) return false;
  if (window.ClipTalkPrepareWorkspaceOverlay?.("timeline") === false) return false;
  closeTimelinePrecisionDrawer();
  timelinePrecisionDrawerTrigger = trigger || document.activeElement;
  timelinePrecisionDrawerTrigger?.setAttribute?.("aria-expanded", "true");
  drawer.dataset.kind = kind;
  $("#timelinePrecisionDrawerTitle").textContent = title;
  if (node) {
    timelinePrecisionDrawerOrigin = { node, parent: node.parentElement, next: node.nextElementSibling };
    body.append(node);
    if (node.tagName === "DETAILS") node.open = true;
  }
  drawer.classList.remove("hidden");
  drawer.setAttribute("aria-hidden", "false");
  drawer.dataset.overlayState = "open";
  drawer.inert = false;
  document.body.classList.add("timeline-precision-drawer-open");
  $("#timelinePrecisionDrawerClose")?.focus();
  return true;
}

function openContentPrecisionSettings(trigger) {
  const root = reviewDockContentRoot();
  const settings = root?.querySelector(".content-generation-settings:not(.hidden)");
  if (!root || !settings || !openTimelinePrecisionDrawer("生成设置", "content", trigger)) return;
  const body = $("#timelinePrecisionDrawerBody");
  const output = settings.querySelector("[data-content-output-mode]");
  const order = settings.querySelector("[data-content-order-mode]");
  const subtitle = settings.querySelector("[data-content-subtitle]");
  const subtitleStatus = settings.querySelector("[data-content-subtitle-status]")?.textContent || "";
  const outputVisible = !output?.closest("[data-content-output-wrap]")?.classList.contains("hidden");
  const orderVisible = !order?.closest("[data-content-order-wrap]")?.classList.contains("hidden");
  const subtitleVisible = subtitle && !subtitle.closest(".content-subtitle-toggle")?.classList.contains("hidden");
  body.innerHTML = `<div class="precision-content-settings">
    ${outputVisible ? `<label><span>输出方式</span><select data-precision-content-output>${[...output.options].map((item) => `<option value="${escapeHtml(item.value)}" ${item.value === output.value ? "selected" : ""}>${escapeHtml(item.textContent)}</option>`).join("")}</select></label>` : ""}
    ${orderVisible ? `<label><span>合成顺序</span><select data-precision-content-order>${[...order.options].map((item) => `<option value="${escapeHtml(item.value)}" ${item.value === order.value ? "selected" : ""}>${escapeHtml(item.textContent)}</option>`).join("")}</select></label>` : ""}
    ${subtitleVisible ? `<label class="precision-content-subtitle"><input type="checkbox" data-precision-content-subtitle ${subtitle.checked ? "checked" : ""}><span><b>添加 AI 字幕</b><small>${escapeHtml(subtitleStatus)}</small></span></label>` : ""}
  </div>`;
  body.querySelector("[data-precision-content-output]")?.addEventListener("change", (event) => {
    output.value = event.currentTarget.value;
    output.dispatchEvent(new Event("change", { bubbles: true }));
  });
  body.querySelector("[data-precision-content-order]")?.addEventListener("change", (event) => {
    order.value = event.currentTarget.value;
    order.dispatchEvent(new Event("change", { bubbles: true }));
  });
  body.querySelector("[data-precision-content-subtitle]")?.addEventListener("change", (event) => {
    subtitle.checked = event.currentTarget.checked;
    subtitle.dispatchEvent(new Event("change", { bubbles: true }));
  });
}

function syncPrecisionOutputContext() {
  const context = $("#timelinePrecisionContext");
  const select = $("#videoViewSelect");
  const meta = $("#timelinePrecisionOutputMeta");
  if (!context || !select || !meta) return;
  const outputMode = timelineExpanded && precisionTimelineKind() === "output";
  if (!outputMode) {
    const assetActions = document.querySelector(".review-header .asset-actions");
    const finalize = $("#finalizePreviewButton");
    if (assetActions && select.parentElement !== assetActions) assetActions.insertBefore(select, finalize || null);
    select.classList.remove("precision-version-select");
    meta.classList.add("hidden");
    meta.textContent = "";
    return;
  }
  if (select.parentElement !== context) context.insertBefore(select, meta);
  select.classList.add("precision-version-select");
  select.classList.remove("hidden");
  const located = currentOutput?.filename ? locateJobOutput(currentOutput.filename) : null;
  if (!located) {
    meta.textContent = "选择版本查看播放时间线";
    meta.dataset.tone = "neutral";
  } else {
    const score = Number(located.version?.reviewReport?.overallScore ?? located.output?.reviewReport?.overallScore ?? located.output?.qualityReport?.score);
    const quality = outputVersionQualityLabel(located.version, located.output);
    meta.textContent = `${quality}${Number.isFinite(score) ? ` · ${Math.round(score)} 分` : ""} · ${Number(located.output.duration || 0).toFixed(1)} 秒`;
    meta.dataset.tone = /需人工|未通过|复核/.test(quality) ? "review" : "passed";
  }
  meta.classList.remove("hidden");
}

function compactPrecisionSelectionTitle(kind) {
  const title = $("#reviewActionTitle");
  if (!title) return;
  if (kind === "person") {
    title.textContent = selectedCurrentPersons.size ? `已选 ${selectedCurrentPersons.size} 人` : "未选择";
    return;
  }
  if (kind === "voice") {
    title.textContent = selectedCurrentVoices.size ? `已选 ${selectedCurrentVoices.size} 个声音` : "未选择";
    return;
  }
  if (kind === "content") {
    const count = reviewDockContentRoot()?.querySelectorAll("[data-content-match]:checked").length || 0;
    title.textContent = count ? `已选 ${count} 段` : "未选择";
    return;
  }
  if (kind === "highlight") {
    title.textContent = title.textContent
      .replace(/^选择高光(?:事件|镜头)$/, "未选择")
      .replace(/^已选 (\d+) 个事件 · (\d+) 个镜头$/, "$1 事件 · $2 镜头")
      .replace(/^已选 (\d+) 个高光镜头$/, "$1 个镜头");
  }
}

function syncPrecisionTimelineToolbar() {
  const context = $("#timelinePrecisionContext");
  const header = $("#timelinePanel > header");
  const workbench = $("#reviewWorkbench");
  const dock = $("#reviewActionDock");
  if (!context || !header || !workbench || !dock) return;
  const precision = Boolean(timelineExpanded && currentJob && !studio?.classList.contains("home-mode"));
  const kind = precision ? precisionTimelineKind(currentJob) : "";
  document.body.classList.toggle("single-timeline-workspace", precision);
  $("#timelinePanel")?.classList.toggle("precision-workspace", precision);
  $("#timelinePanel")?.setAttribute("data-precision-kind", kind);
  context.classList.toggle("hidden", !precision);
  if (!precision) {
    closeTimelinePrecisionDrawer();
    if (dock.parentElement !== workbench) workbench.append(dock);
    [$("#closeVoiceProfiles"), $("#closePersonProfiles")].forEach((button) => {
      const workbenchHeader = workbench.querySelector(".review-workbench-header");
      if (button && workbenchHeader && button.parentElement !== workbenchHeader) workbenchHeader.append(button);
    });
    syncPrecisionOutputContext();
    return;
  }
  if (timelinePrecisionDrawerOrigin && timelinePrecisionDrawerOrigin.node === $("#currentVoiceTimelineSection") && kind !== "voice") {
    closeTimelinePrecisionDrawer();
  }
  if (kind === "output") {
    if (dock.parentElement !== workbench) workbench.append(dock);
  } else if (!dock.classList.contains("hidden") && dock.parentElement !== context) {
    context.append(dock);
  }
  if (kind !== "output") compactPrecisionSelectionTitle(kind);
  const closeButton = kind === "voice" ? $("#closeVoiceProfiles") : kind === "person" ? $("#closePersonProfiles") : null;
  [$("#closeVoiceProfiles"), $("#closePersonProfiles")].forEach((button) => {
    if (!button || button === closeButton) return;
    const workbenchHeader = workbench.querySelector(".review-workbench-header");
    if (workbenchHeader && button.parentElement !== workbenchHeader) workbenchHeader.append(button);
  });
  if (closeButton && closeButton.parentElement !== header) header.append(closeButton);
  syncPrecisionOutputContext();
  const toggle = $("#timelineExpandToggle");
  if (toggle) toggle.textContent = kind === "output" ? "返回版本列表" : "返回审核列表";
}

function applyReviewDockMode() {
  const dock = $("#reviewActionDock");
  const mode = $("#reviewActionMode");
  if (!dock || !mode) return;
  if (dock.dataset.kind === "content") {
    const actual = reviewDockContentRoot()?.querySelector("[data-content-output-mode]");
    if (actual) { actual.value = mode.value; actual.dispatchEvent(new Event("change", { bubbles: true })); }
  } else if (dock.dataset.kind === "voice") {
    const actual = $("#currentVoiceMode");
    if (actual) { actual.value = mode.value; actual.dispatchEvent(new Event("change", { bubbles: true })); }
  } else if (dock.dataset.kind === "person") {
    currentPersonMatchMode = mode.value === "all" ? "all" : "any";
    $("#currentPersonMatchModes")?.querySelectorAll("input").forEach((input) => { input.checked = input.value === currentPersonMatchMode; });
    syncCurrentPersonControls();
  } else if (dock.dataset.kind === "highlight-groups") {
    mode.dataset.highlightValue = mode.value;
  }
  syncReviewActionDock();
}

function applyReviewDockToggle() {
  if ($("#reviewActionDock")?.dataset.kind !== "person") return;
  const actual = $("#currentPersonSpeakingOnly");
  if (actual) {
    actual.checked = Boolean($("#reviewActionToggle")?.checked);
    actual.dispatchEvent(new Event("change", { bubbles: true }));
  }
}

function toggleReviewDockSettings() {
  const dock = $("#reviewActionDock");
  if (!dock) return;
  if (dock.dataset.kind === "content") {
    if (timelineExpanded) return openContentPrecisionSettings($("#reviewActionSettings"));
    const actions = reviewDockContentRoot()?.querySelector(".content-search-actions");
    if (!actions) return;
    actions.classList.toggle("dock-settings-open");
    const details = actions.querySelector(".content-generation-settings");
    if (details) details.open = actions.classList.contains("dock-settings-open");
    if (actions.classList.contains("dock-settings-open")) actions.scrollIntoView({ behavior: "smooth", block: "nearest" });
  } else if (dock.dataset.kind === "voice") {
    const details = $("#currentVoiceTimelineSection");
    if (!details) return;
    if (timelineExpanded) {
      openTimelinePrecisionDrawer("校正发言归属", "voice", $("#reviewActionSettings"), details);
      return;
    }
    details.open = true;
    details.scrollIntoView({ behavior: "smooth", block: "nearest" });
  }
}

function runReviewDockPrimaryAction() {
  const dock = $("#reviewActionDock");
  if (!dock || $("#reviewActionPrimary")?.disabled) return;
  if (dock.dataset.kind === "content") return reviewDockContentRoot()?.querySelector("[data-confirm-content]")?.click();
  if (dock.dataset.kind === "voice") return $("#searchSelectedVoices")?.click();
  if (dock.dataset.kind === "person") return $("#searchSelectedPersons")?.click();
  if (dock.dataset.kind === "highlight-groups") {
    const composeButton = $("#chatStageHost")?.querySelector(".open-compose-stage");
    if (composeButton) return composeButton.click();
    const separate = $("#reviewActionMode")?.value === "separate_events";
    return $("#chatStageHost")?.querySelector(separate ? ".export-event-groups" : ".confirm-event-groups")?.click();
  }
  return $("#chatStageHost")?.querySelector(".confirm-selected")?.click();
}

function placeAnalysisConsole() {
  const status = $("#jobStatus");
  const host = $("#chatStageHost");
  const messages = $("#chatMessages");
  if (!status || !messages) return;
  // Keep the legacy console mounted so shared update functions can still write
  // progress facts, but use #inlineAnalysisProgress as the visible pipeline
  // surface. The legacy node remains visible only when it contains the model
  // decision buttons that the user must be able to click.
  status.classList.add("chat-analysis-console");
  host?.classList.remove("hidden", "director-stage-hidden");
  host?.style.removeProperty("display");
  if (status.parentElement !== messages) messages.append(status);
  const showDecisionPanel = currentJob?.status === "awaiting_model_decision";
  status.classList.toggle("hidden", !showDecisionPanel);
  status.classList.toggle("pipeline-active", showDecisionPanel);
  status.hidden = !showDecisionPanel;
  if (showDecisionPanel) status.style.removeProperty("display");
  else status.style.display = "none";
  status.setAttribute("aria-hidden", showDecisionPanel ? "false" : "true");
}

// Keep one source of truth for pipeline states. The inline conversation
// progress card and the polling loop must agree about which states are still
// active; otherwise a running job can lose its progress card between polls.
const ACTIVE_JOB_STATUSES = new Set([
  "briefing",
  "queued",
  "running",
  "processing",
  "analyzing",
  "cancelling",
  "awaiting_model_decision",
]);

function isActiveJobStatus(status) {
  return ACTIVE_JOB_STATUSES.has(String(status || ""));
}

function isPipelineRunningStatus(status) {
  const value = String(status || "");
  return isActiveJobStatus(value) && !["briefing", "queued", "cancelling", "awaiting_model_decision"].includes(value);
}

function executionForJob(job = currentJob) {
  return window.ClipTalkWorkspaceState?.execution?.(job) || {
    status: "waiting_user", operation: "none", active: false, outcome: "none",
    progress: {}, result: {}, capabilities: {},
  };
}

function interactionCapabilitiesForJob(job = currentJob) {
  const executionActive = Boolean(executionForJob(job).active);
  const agentOperation = agentInteractionStateForJob(job);
  const active = executionActive || agentOperation.active;
  const contentTask = String(job?.taskMode || "") === "content_extract";
  const waitingForContentReview = String(job?.status || "") === "awaiting_content_confirmation";
  const reason = agentOperation.active
    ? `Agent 正在执行${agentOperation.title ? `“${agentOperation.title}”` : "当前步骤"}，完成后即可操作`
    : executionActive ? "当前任务正在处理，完成后即可操作" : "";
  return {
    active,
    reason,
    canRestoreContentSearch: contentTask && !active,
    canMutateContentBasket: contentTask && !active,
    canEditContentBoundary: contentTask && !active && waitingForContentReview,
    canCorrectPerson: !active,
    canCorrectVoice: !active,
  };
}

function agentInteractionStateForJob(job = currentJob) {
  const agent = job?.agent && typeof job.agent === "object" ? job.agent : {};
  const handedOff = String(job?.stage || "") === "agent_handed_off"
    || String(job?.execution?.phase || "") === "agent_handed_off";
  const planActive = ["approved", "running"].includes(String(agent.status || ""));
  const workspaceActive = ["", "ready", "running"].includes(String(agent.workspaceStatus || ""));
  const stepStatus = String(agent.currentStepStatus || "");
  const actionRequired = stepStatus === "action_required";
  const tool = String(agent.currentStepTool || "");
  const operationByTool = {
    analyze_highlights: "highlight_analysis",
    search_content: "content_search",
    discover_people: "person_discovery",
    discover_speakers: "speaker_discovery",
    prepare_subtitle_review: "subtitle_review",
    render_review_preview: "render",
  };
  return {
    active: Boolean(!handedOff && planActive && workspaceActive && !actionRequired),
    actionRequired,
    tool,
    operation: operationByTool[tool] || "agent_operation",
    status: stepStatus,
    title: String(agent.currentStepTitle || ""),
  };
}

function workflowOperationActive(job, { operations = [], agentTools = [] } = {}) {
  const execution = executionForJob(job);
  if (execution.active && (!operations.length || operations.includes(String(execution.operation || "")))) return true;
  const agent = agentInteractionStateForJob(job);
  return Boolean(agent.active && (!agentTools.length || agentTools.includes(agent.tool)));
}

function voiceDiscoveryPresentationForJob(job, discovery = {}) {
  const normalized = discovery && typeof discovery === "object" ? { ...discovery } : {};
  const status = String(normalized.status || "not_started");
  if (["loading", "running", "ready", "failed"].includes(status)) return normalized;
  if (!workflowOperationActive(job, {
    operations: ["speaker_discovery"],
    agentTools: ["discover_speakers"],
  })) return normalized;
  const agent = agentInteractionStateForJob(job);
  const execution = executionForJob(job);
  return {
    ...normalized,
    status: "running",
    detail: normalized.detail || execution.detail || agent.title || "正在区分视频中的匿名说话人",
    currentAction: normalized.currentAction || agent.title || execution.detail || "正在补全说话人证据",
  };
}

function unavailableInteractionMessage(job = currentJob, fallback = "当前操作暂不可用") {
  return interactionCapabilitiesForJob(job).reason || fallback;
}

function executionCancelLabel(job = currentJob) {
  const execution = executionForJob(job);
  if (execution.status === "cancelling") return "正在停止…";
  if (execution.operation === "auto_composition" || execution.operation === "quality_review") return "停止自动成片";
  if (execution.operation === "render") return "停止生成";
  if (execution.operation === "person_tracking") return "停止人物扫描";
  if (execution.operation === "speaker_discovery") return "停止说话人识别";
  if (execution.operation === "target_voice_search") return "停止目标声音检索";
  if (execution.operation === "content_search") return "停止检索";
  return execution.active ? "停止分析" : "取消任务";
}

function jobNeedsPolling(job = currentJob) {
  return Boolean(executionForJob(job).active);
}

function jobPollDelay(job = currentJob) {
  const execution = executionForJob(job);
  // Cancellation is a short-lived acknowledgement state. Poll it quickly so
  // the UI reflects the terminal state as soon as the worker or watchdog
  // finishes, instead of adding another normal 1.5–2.5 second delay.
  if (execution.status === "cancelling") return 450;
  const stage = String(execution.phase || job?.stage || "");
  if (stage === "speech_recognition" || stage === "speech_analysis" || stage === "audio_analysis") return 1800;
  if (stage === "coarse_vlm" || stage === "refine_vlm" || stage === "event_director") return 1500;
  if (["auto_composition", "quality_review"].includes(execution.operation)) return 1600;
  return 1400;
}

function renderJobError(message = "", source = "job") {
  const errorNode = $("#jobError");
  if (!errorNode) return;
  const text = String(message || "").trim();
  errorNode.textContent = text;
  errorNode.classList.toggle("hidden", !text);
  if (text) errorNode.dataset.errorSource = source;
  else delete errorNode.dataset.errorSource;
}

function clearRecoveredPollError() {
  const errorNode = $("#jobError");
  if (errorNode?.dataset.errorSource !== "poll") return;
  renderJobError(currentJob?.error || "", "job");
}

function friendlyProgressDetail(value) {
  const detail = String(value || "");
  const replacements = [
    [/^正在准备人物轨迹采样帧/, "人物识别 1/2 · 正在准备解码分析帧"],
    [/^正在抽取人物轨迹采样帧/, "人物识别 1/2 · 正在解码分析帧"],
    [/^正在初始化本地人体、ReID 与人脸锚点模型/, "人物识别 2/2 · 正在初始化人物检测与身份关联模型"],
    [/^正在建立人物轨迹/, "人物识别 2/2 · 正在检测人物并关联轨迹"],
    [/^正在用时空轨迹和 ReID 合并同一人物/, "人物识别 2/2 · 正在合并同一人物的连续轨迹"],
    [/^人物轨迹索引已完成/, "人物识别完成"],
  ];
  const match = replacements.find(([pattern]) => pattern.test(detail));
  return match ? detail.replace(match[0], match[1]) : detail;
}

function analysisConsoleVisible(job) {
  const execution = executionForJob(job);
  // Automatic composition owns a dedicated version/review progress card.
  // Rendering the generic pipeline card as well duplicates state and can
  // compress its inner grid when the outer job is already in review.
  return job?.status !== "awaiting_model_decision"
    && Boolean(execution.active)
    && !["auto_composition", "quality_review"].includes(execution.operation);
}

function setDirectorStage(stage = "conversation") {
  const previousStage = directorStage;
  directorStage = ["conversation", "analysis", "events", "compose"].includes(stage) ? stage : "conversation";
  if (previousStage !== directorStage && directorStage === "compose" && currentJob) {
    reviewLowerPanelMode = jobOutputCount(currentJob) > 1 ? "review" : "collapsed";
    timelineExpanded = false;
    document.body.dataset.timelineExpanded = "false";
    document.body.dataset.precisionEditing = "false";
  } else if (previousStage === "compose" && directorStage === "events") {
    reviewLowerPanelMode = "review";
  }
  updateDirectorFlow(currentJob);
  const messages = $("#chatMessages");
  const composer = $("#chatForm");
  const body = $("#railBody");
  const output = $("#railOutput");
  const clips = $("#clipSection");
  setDirectorWorkspaceEmpty(!currentJob);
  if (!body || !output || !clips) return;
  // Keep the conversation mounted while analysis, rendering, and completed
  // results are visible. Progress and the final handoff belong in the dialog;
  // switching to the compose stage must not make the chat disappear.
  // Conversation, review controls and composition controls share one scroll
  // surface. Stages only decide which controls are expanded in that stream.
  messages?.classList.remove("director-stage-hidden");
  composer?.classList.remove("director-stage-hidden");
  // The final three-column shell keeps the current contextual status visible
  // in the right rail during planning and analysis. Only the version stage
  // replaces it with generated outputs.
  body.classList.toggle("director-stage-hidden", directorStage === "compose");
  output.classList.toggle("director-stage-hidden", directorStage !== "compose");
  clips.classList.toggle("director-stage-hidden", directorStage !== "compose");
  renderDirectorContext(currentJob);
  syncStageNavigationPlacement();
  syncAssistantPanelForStage();
  requestAnimationFrame(() => window.ClipTalkWorkspaceController?.syncPortraitPanels?.());
  syncReviewWorkbench();
  syncUnifiedWorkspaceView();
}

function directorFlowStage(job = currentJob) {
  if (!job) return "brief";
  const execution = executionForJob(job);
  // A failed content render keeps its confirmed candidates intact.
  if (contentGenerationFailed(job)) return "events";
  const canonicalPhase = String(job.presentation?.phase || job.workflow?.phase || "");
  if (canonicalPhase) {
    const phases = {
      prepare: "brief", analysis: "analysis", search: "analysis",
      review: "events", render: "compose", complete: "compose",
    };
    if (phases[canonicalPhase]) return phases[canonicalPhase];
  }
  if (["briefing", "brief_confirmation"].includes(job.status)) return "brief";
  const compositionStage = ["render", "auto_composition", "quality_review"].includes(execution.operation)
    || ["edit_planning", "edit_planning_complete", "rendering", "render", "auto_composition"].includes(String(job.stage || ""));
  const autoCompositionStatus = String(job.autoComposition?.status || "");
  const hasOutputs = jobOutputCount(job) > 0;
  // Event review deliberately remains available after automatic rendering, so
  // the backend can keep the job in awaiting_confirmation.  The visible flow
  // must follow the actual render/output state instead of treating every such
  // job as if analysis were still in progress.
  if (job.status === "completed" || compositionStage || ["queued", "running", "completed"].includes(autoCompositionStatus)) return "compose";
  if (execution.active) return compositionStage ? "compose" : "analysis";
  if (job.status === "awaiting_confirmation") return hasOutputs ? "compose" : "events";
  if (job.status === "awaiting_content_confirmation") return "events";
  if (["cancelled", "failed"].includes(job.status)) return "analysis";
  return "brief";
}

function compositionIsComplete(job = currentJob) {
  if (!job) return false;
  const execution = executionForJob(job);
  return !execution.active && execution.outcome === "output_ready";
}

function updateDirectorFlow(job = currentJob) {
  const internalStage = directorFlowStage(job);
  const current = internalStage;
  const order = ["brief", "analysis", "events", "compose"];
  const position = order.indexOf(current);
  const finished = current === "compose" && compositionIsComplete(job);
  document.querySelectorAll("[data-director-flow]").forEach((step) => {
    const index = order.indexOf(step.dataset.directorFlow);
    const isCurrent = step.dataset.directorFlow === current;
    step.classList.toggle("current", isCurrent);
    step.classList.toggle("complete", index >= 0 && (index < position || (finished && index === position)));
    step.classList.toggle("upcoming", index > position);
    if (isCurrent) step.setAttribute("aria-current", "step");
    else step.removeAttribute("aria-current");
  });
}

function workflowKindForJob(job = currentJob) {
  const explicit = String(job?.request?.workflowKind || job?.workflowKind || "").trim();
  if (["highlight", "content_search", "person_edit", "speaker_edit"].includes(explicit)) return explicit;
  if (job?.request?.entryWorkflow === "voice_discovery") return "speaker_edit";
  if (job?.request?.entryWorkflow === "person_discovery") return "person_edit";
  return String(job?.taskMode || "highlight") === "content_extract" ? "content_search" : "highlight";
}

function isAgentInstructionDraft(job = currentJob) {
  if (!job || job.instructionSubmitted) return false;
  if (job.agentDraft) return true;
  if (String(job.status || "") === "awaiting_agent_instruction") return true;
  return String(job.stage || "") === "agent_workspace_ready"
    && String(job.request?.entryWorkflow || "") === "agent";
}

function taskModePresentation(job = currentJob) {
  const workflowKey = workflowKindForJob(job);
  const copy = window.ClipTalkCopy?.workflow(workflowKey);
  const agentEntry = String(job?.request?.entryWorkflow || "") === "agent";
  return {
    key: workflowKey === "highlight" ? "highlight" : "content_extract",
    label: agentEntry ? "智能剪辑 Agent" : (copy?.label || "视频剪辑"),
    phaseLabels: copy?.phases || {},
    nav: copy?.navigation || [],
    workflowKey,
    agentEntry,
  };
}

function workflowAssistantLabel(_job = currentJob) {
  return "视频剪辑助手";
}

function workflowVocabulary(job = currentJob) {
  const copy = window.ClipTalkCopy?.workflow(workflowKindForJob(job));
  return {
    output: copy?.output || "成片",
    outputPlural: copy?.outputPlural || "成片版本",
    candidate: copy?.candidate || "片段",
    candidatePlural: copy?.candidatePlural || "片段",
    timeline: copy?.timeline || "时间线",
  };
}

function contentGenerationFailed(job = currentJob) {
  if (!job || String(job.taskMode || "") !== "content_extract" || String(job.status || "") !== "failed") return false;
  return /生成|渲染|合成|编码|edl/i.test(`${job.detail || ""} ${job.error || ""} ${job.currentAction || ""}`);
}

function updateTaskModeNavigation(job = currentJob) {
  const presentation = taskModePresentation(job);
  const nav = $("#directorStageNav");
  if (!nav) return;
  nav.setAttribute("aria-label", `${presentation.label}流程`);
  nav.querySelectorAll("[data-director-flow]").forEach((step, index) => {
    const [label, detail] = presentation.nav[index] || presentation.nav.at(-1);
    const strong = step.querySelector("strong");
    const small = step.querySelector("small");
    if (strong) strong.textContent = label;
    if (small) small.textContent = detail;
    step.setAttribute("aria-label", `${label}：${detail}`);
    step.title = `${label} · ${detail}`;
  });
}

function renderDirectorContext(job = currentJob) {
  const summary = $("#directorContextSummary");
  if (!summary) return;
  if (!job || directorStage === "conversation") {
    summary.classList.add("hidden");
    summary.innerHTML = "";
    return;
  }
  if (String(job.taskMode || "") === "content_extract") {
    const search = job.contentSearch || {};
    const query = search.intent?.query || search.instruction || job.request?.contentInstruction || "按描述查找内容";
    const scope = search.intent?.searchScope || search.retrievalStats?.searchScope || {};
    const scopeText = Number(scope.end) > Number(scope.start)
      ? `${formatTime(scope.start)} → ${formatTime(scope.end)}`
      : "整个源视频";
    const resultMode = String(search.resultMode || search.queryPlan?.result?.mode || "top_k") === "exhaustive" ? "全部匹配" : "最相关片段";
    summary.innerHTML = `<div><small>当前查找条件</small><strong>${escapeHtml(query)}</strong></div><span>范围：${escapeHtml(scopeText)}</span><span>结果：${escapeHtml(resultMode)}</span><span>选择片段后可预览并调整范围</span>`;
    summary.classList.remove("hidden");
    return;
  }
  const brief = job.brief || {};
  const focus = (brief.focus || []).filter(Boolean).join("、") || "综合判断";
  const target = Number(brief.targetDurationSeconds || job.totalTargetSeconds || 0);
  const subtitle = brief.subtitlePreference === "burn" ? "添加字幕" : "不加字幕";
  summary.innerHTML = `<div><small>当前剪辑需求</small><strong>${escapeHtml(brief.objective || "事件高光合集")}</strong></div><span>重点：${escapeHtml(focus)}</span><span>${target ? `单条目标 ${target.toFixed(1)} 秒` : "单条时长由 AI 推荐"}</span><span>${escapeHtml(subtitle)}</span>`;
  summary.classList.remove("hidden");
}

function renderDirectorTaskSummary(job = currentJob) {
  const summary = $("#directorTaskSummary");
  if (!summary) return;
  if (!job) {
    summary.innerHTML = `<strong>等待创建任务</strong><span>上传视频后，AI 会先确认你的剪辑目标。</span>`;
    updateTaskModeNavigation(null);
    return;
  }
  const presentation = taskModePresentation(job);
  const vocabulary = workflowVocabulary(job);
  updateTaskModeNavigation(job);
  const brief = job.brief || {};
  const objective = brief.objective || job.request?.objective || "事件高光合集";
  const duration = Number(job.videoInfo?.duration || 0);
  const target = Number(brief.targetDurationSeconds || job.totalTargetSeconds || 0);
  const outputCount = jobOutputCount(job);
  const compositionComplete = compositionIsComplete(job);
  const queued = job.status === "queued";
  const contentMode = presentation.key === "content_extract";
  const execution = executionForJob(job);
  const autoCompositionRunning = execution.active && ["auto_composition", "quality_review"].includes(execution.operation);
  const unifiedPresentation = window.ClipTalkWorkspaceState?.derivePresentation?.(job);
  const stagedCapability = selectedDraftCapability(job);
  const status = isAgentInstructionDraft(job) ? (stagedCapability
    ? `已选择${stagedCapability.option.label} · 补充要求后生成方案`
    : "待输入剪辑要求")
    : unifiedPresentation?.label || (queued ? "任务已提交，正在等待后台开始处理"
    : compositionComplete
      ? `${outputCount} 个${vocabulary.outputPlural}已完成${!contentMode && job.status === "awaiting_confirmation" ? " · 事件和镜头均可单独下载" : ""}`
      : autoCompositionRunning ? (job.autoComposition?.detail || "正在生成高光成片")
        : job.status === "awaiting_content_confirmation" ? `${vocabulary.candidatePlural}已就绪 · 待确认`
          : job.status === "awaiting_confirmation" ? "事件已就绪 · 待审核"
            : job.status === "completed" ? `${vocabulary.output}已完成` : execution.detail || job.detail || "任务进行中");
  const flowStage = directorFlowStage(job);
  const workflowChoices = [
    ["highlight", "智能高光"], ["content_search", "内容检索"],
    ["person_edit", "人物聚焦"], ["speaker_edit", "发言剪辑"],
  ].filter(([key]) => key !== presentation.workflowKey);
  const voicePanelOpen = document.body.classList.contains("voice-workspace-open");
  const personPanelOpen = document.body.classList.contains("person-workspace-open");
  const workflowPanelButton = presentation.workflowKey === "speaker_edit"
    ? `<button type="button" class="open-speaker-workspace" data-open-speaker-workspace aria-controls="voiceProfilePanel" aria-expanded="${voicePanelOpen}">说话人面板</button>`
    : presentation.workflowKey === "person_edit"
      ? `<button type="button" class="open-person-workspace" data-open-person-workspace aria-controls="personProfilePanel" aria-expanded="${personPanelOpen}">人物面板</button>`
      : "";
  const personStageLabel = presentation.workflowKey === "person_edit"
    ? contentGenerationFailed(job) ? "核对出镜 · 上次合成未完成"
      : flowStage === "compose" ? "合成视频"
        : flowStage === "events" ? "核对出镜"
          : flowStage === "analysis" ? "选择人物" : "准备画面"
    : "";
  const handedOff = String(job.stage || "") === "agent_handed_off";
  const agentStageLabel = handedOff ? "已转到新任务" : presentation.agentEntry ? ({
    awaiting_confirmation: "确认执行计划", approved: "执行计划", running: "执行计划",
    action_required: "等待你处理", preview_ready: "计划完成", no_result: "未找到可用内容", failed: "计划失败",
    cancelled: "计划已停止",
  })[String(job.agent?.status || "")] || "生成执行计划" : "";
  const stageLabel = queued ? "等待处理"
    : unifiedPresentation?.railTitle || agentStageLabel || personStageLabel || presentation.phaseLabels[flowStage] || "处理中";
  const workflowSwitchMenu = isAgentInstructionDraft(job) ? ""
    : `<details class="workflow-switch-menu"><summary>基于此视频新建任务</summary><div>${workflowChoices.map(([key, label]) => `<button type="button" data-workflow-switch="${key}">${label}</button>`).join("")}</div></details>`;
  summary.innerHTML = `<div class="director-task-heading"><span class="task-mode-badge ${presentation.key} ${presentation.workflowKey}">${presentation.label}</span><small>当前阶段 · ${stageLabel}</small>${workflowPanelButton}${workflowSwitchMenu}</div><strong title="${escapeHtml(job.filename || objective)}">${escapeHtml(job.filename || objective)}</strong><p>${escapeHtml(status)}</p><div class="director-task-facts"><span>${duration ? `源视频 ${formatClock(duration)}` : "源视频待准备"}</span>${target && presentation.key === "highlight" ? `<span>目标 ${target.toFixed(1)} 秒</span>` : ""}</div>`;
}

function renderReviewStatus(job = currentJob) {
  const node = $("#reviewStatus");
  if (!node) return;
  const modeBadge = $("#reviewTaskMode");
  if (modeBadge) {
    const presentation = taskModePresentation(job);
    modeBadge.textContent = presentation.label;
    modeBadge.classList.toggle("content_extract", presentation.key === "content_extract");
    modeBadge.classList.toggle("highlight", presentation.key === "highlight");
    ["content_search", "person_edit", "speaker_edit"].forEach((key) => modeBadge.classList.toggle(key, presentation.workflowKey === key));
    modeBadge.classList.toggle("hidden", !job);
  }
  const stageLabels = {
    agent_plan_available: "等待 Agent 计划",
    agent_workspace_ready: "等待剪辑目标",
    agent_plan_generating: "生成执行计划",
    starting: "读取素材",
    probing: "读取素材",
    audio_analysis: "理解声音",
    speech_recognition: "理解对白",
    speech_analysis: "分析语音",
    content_transcription: "识别对白",
    content_sampling: "准备画面检索",
    content_indexing: "准备所需索引",
    content_recognition: "建立所需索引",
    content_index_ready: "索引已就绪",
    content_search: "搜索内容",
    content_person_description: "匹配人物描述",
    content_active_speaker: "识别说话人物",
    content_refinement: "精修边界",
    content_search_ready: "等待确认",
    sampling: "粗看全片",
    coarse_vlm: "粗看全片",
    refine_vlm: "精修镜头",
    event_grouping: "事件编排",
    event_director: "事件编排",
    rendering: "生成成片",
    auto_composition: "生成成片",
  };
  const execution = executionForJob(job);
  const workflowKey = workflowKindForJob(job);
  const vocabulary = workflowVocabulary(job);
  const active = execution.active;
  const outputCount = jobOutputCount(job);
  const compositionComplete = compositionIsComplete(job);
  const autoCompositionRunning = active && ["auto_composition", "quality_review"].includes(execution.operation);
  const renderRunning = active && (
    execution.operation === "render" || ["rendering", "render"].includes(String(job?.stage || ""))
  );
  const agentPlanRunning = String(job?.status || "") === "awaiting_agent_plan"
    && ["approved", "running"].includes(String(job?.agent?.status || ""));
  const workflowStageLabel = workflowKey === "person_edit"
    ? ({ content_sampling: "准备人物画面", content_indexing: "发现画面人物", content_recognition: "扫描人物轨迹", content_search: "整理出镜片段", content_refinement: "精修人物边界" }[String(job?.stage || "")])
    : workflowKey === "speaker_edit"
      ? ({ audio_analysis: "准备声音", speech_recognition: "识别语音", voice_discovery: "区分说话人", voice_discovery_ready: "等待选择发言" }[String(job?.stage || "")])
      : "";
  const unifiedPresentation = window.ClipTalkWorkspaceState?.derivePresentation?.(job);
  const label = unifiedPresentation?.label || (!job ? "等待分析"
    : compositionComplete ? `${outputCount} 个${vocabulary.outputPlural}已完成`
      : autoCompositionRunning ? "AI 生成 · 高光成片"
      : renderRunning ? `正在合成${vocabulary.output}`
      : active ? `AI 分析 · ${workflowStageLabel || stageLabels[String(job.stage || "")] || "处理中"}`
      : job.status === "awaiting_agent_plan" ? displayStatusForJob(job).text
      : job.status === "awaiting_content_confirmation" ? `等待确认${vocabulary.candidatePlural}`
        : job.status === "awaiting_confirmation" ? "等待审核"
        : job.status === "completed" ? "已完成"
          : job.status === "failed" ? (contentGenerationFailed(job) ? `${vocabulary.output}生成失败` : "分析失败") : "已停止");
  node.textContent = label;
  node.classList.toggle("active", unifiedPresentation ? unifiedPresentation.running : (active || autoCompositionRunning || agentPlanRunning) && !compositionComplete);
  if (unifiedPresentation) node.dataset.state = unifiedPresentation.key;
  node.classList.toggle("complete", compositionComplete);
}

try {
  const savedTimelineLayers = JSON.parse(localStorage.getItem(timelineLayerStorageKey) || "null");
  if (savedTimelineLayers) {
    timelineCutsVisible = savedTimelineLayers.cuts !== false;
  }
} catch { /* storage may be disabled */ }

function evidencePanelWidthLimits() {
  const stage = $("#reviewStage");
  const stageWidth = Math.round(stage?.getBoundingClientRect().width || 0);
  const compact = window.innerWidth <= 1180;
  const min = compact ? 220 : 240;
  const minViewer = compact ? 280 : 300;
  const max = Math.max(min, Math.min(560, stageWidth - minViewer - 10));
  const fallback = Math.max(min, Math.min(compact ? 290 : 350, stageWidth * (compact ? .31 : .29)));
  return { stageWidth, min, max, fallback };
}

function storedEvidencePanelWidth() {
  try {
    const saved = JSON.parse(localStorage.getItem(evidencePanelWidthStorageKey) || "null");
    return Number(saved?.width) || 0;
  } catch { return 0; }
}

function setEvidencePanelWidth(value, { persist = false } = {}) {
  const stage = $("#reviewStage");
  const handle = $("#reviewEvidenceResizer");
  if (!stage || !handle || window.innerWidth <= 900) return 0;
  const { stageWidth, min, max, fallback } = evidencePanelWidthLimits();
  if (stageWidth <= 0) return 0;
  const width = Math.round(Math.max(min, Math.min(max, Number(value) || fallback)));
  stage.style.setProperty("--evidence-panel-width", `${width}px`);
  stage.dataset.evidencePanelWidth = String(width);
  handle.setAttribute("aria-valuemin", String(min));
  handle.setAttribute("aria-valuemax", String(max));
  handle.setAttribute("aria-valuenow", String(width));
  handle.setAttribute("aria-disabled", String(max <= min));
  if (persist) {
    try { localStorage.setItem(evidencePanelWidthStorageKey, JSON.stringify({ width })); } catch { /* storage may be disabled */ }
  }
  scheduleMediaFrameFit(true);
  scheduleTimelineResizeRender(true);
  return width;
}

function restoreEvidencePanelWidth() {
  const stage = $("#reviewStage");
  if (!stage || window.innerWidth <= 900) return 0;
  const stored = storedEvidencePanelWidth();
  const fallback = Number(stage.dataset.evidencePanelDefaultWidth) || evidencePanelWidthLimits().fallback;
  if (!stage.dataset.evidencePanelDefaultWidth && !stored && !stage.dataset.evidencePanelWidth) {
    stage.dataset.evidencePanelDefaultWidth = String(Math.round(fallback));
  }
  const desired = Number(stage.dataset.evidencePanelWidth) || stored || fallback;
  return setEvidencePanelWidth(desired);
}

function bindEvidencePanelResizer() {
  const handle = $("#reviewEvidenceResizer");
  const stage = $("#reviewStage");
  if (!handle || !stage || handle.dataset.bound === "true") return;
  handle.dataset.bound = "true";
  handle.addEventListener("pointerdown", (event) => {
    if (window.innerWidth <= 900 || event.button !== 0 || handle.getAttribute("aria-disabled") === "true") return;
    event.preventDefault();
    const startX = event.clientX;
    const startWidth = Math.round($("#evidencePanel")?.getBoundingClientRect().width || restoreEvidencePanelWidth());
    let pendingWidth = startWidth;
    let resizeFrame = null;
    handle.classList.add("dragging");
    document.body.classList.add("evidence-panel-resizing");
    handle.setPointerCapture?.(event.pointerId);
    const applyPendingWidth = () => {
      resizeFrame = null;
      setEvidencePanelWidth(pendingWidth);
    };
    const move = (moveEvent) => {
      pendingWidth = startWidth - (moveEvent.clientX - startX);
      if (resizeFrame === null) resizeFrame = requestAnimationFrame(applyPendingWidth);
    };
    const finish = () => {
      if (resizeFrame !== null) cancelAnimationFrame(resizeFrame);
      setEvidencePanelWidth(pendingWidth, { persist: true });
      handle.classList.remove("dragging");
      document.body.classList.remove("evidence-panel-resizing");
      handle.removeEventListener("pointermove", move);
      handle.removeEventListener("pointerup", finish);
      handle.removeEventListener("pointercancel", finish);
    };
    handle.addEventListener("pointermove", move);
    handle.addEventListener("pointerup", finish);
    handle.addEventListener("pointercancel", finish);
  });
  handle.addEventListener("keydown", (event) => {
    if (window.innerWidth <= 900 || !["ArrowLeft", "ArrowRight"].includes(event.key)) return;
    event.preventDefault();
    const current = Math.round($("#evidencePanel")?.getBoundingClientRect().width || restoreEvidencePanelWidth());
    setEvidencePanelWidth(current + (event.key === "ArrowLeft" ? 16 : -16), { persist: true });
  });
  handle.addEventListener("dblclick", () => {
    try { localStorage.removeItem(evidencePanelWidthStorageKey); } catch { /* storage may be disabled */ }
    const defaultWidth = Number(stage.dataset.evidencePanelDefaultWidth) || evidencePanelWidthLimits().fallback;
    delete stage.dataset.evidencePanelWidth;
    stage.style.removeProperty("--evidence-panel-width");
    requestAnimationFrame(() => setEvidencePanelWidth(defaultWidth));
  });
  if (window.ResizeObserver) {
    const observer = new ResizeObserver(() => {
      if (!document.body.classList.contains("evidence-panel-resizing")) restoreEvidencePanelWidth();
    });
    observer.observe(stage);
  }
  window.addEventListener("resize", restoreEvidencePanelWidth);
  requestAnimationFrame(restoreEvidencePanelWidth);
}

function setReviewLowerPanelMode(mode, { scroll = false, compact = false } = {}) {
  let normalized = ["collapsed", "review", "timeline"].includes(mode) ? mode : "collapsed";
  // The compact completion overview is useful below a landscape preview, but
  // in portrait it incorrectly triggers the precision split without an
  // explicit user action. Keep portrait review focused until 精剪 is opened.
  if (normalized === "timeline" && compact && $("#reviewView")?.dataset.reviewLayout === "portrait") {
    normalized = "collapsed";
    compact = false;
  }
  if (normalized === "timeline" && !timelineHasRenderableContent(currentJob)) return reviewLowerPanelMode;
  reviewLowerPanelMode = normalized;
  timelineExpanded = normalized === "timeline" && !compact;
  document.body.dataset.ctCoverEditorOpen = String(
    normalized === "timeline" && document.body.dataset.ctCoverReview === "true",
  );
  if (normalized === "timeline" && currentJob) updateTimeline();
  timelinePanel?.classList.toggle("hidden", normalized !== "timeline");
  document.body.dataset.timelineExpanded = String(timelineExpanded);
  document.body.dataset.precisionEditing = String(timelineExpanded);
  const toggle = $("#timelineExpandToggle");
  if (toggle) {
    toggle.setAttribute("aria-expanded", String(timelineExpanded));
    toggle.textContent = timelineExpanded
      ? (precisionTimelineKind() === "output" ? "返回版本列表" : "返回审核列表")
      : "精细查看";
  }
  if (!timelineExpanded) {
    $("#reviewView")?.style.removeProperty("--viewer-track-height");
  }
  syncReviewWorkbench();
  syncPortraitWorkspaceControls();
  syncPortraitPrecisionWorkbench();
  syncTimelineVisibilityLayout();
  scheduleTimelineResizeRender(true);
  scheduleMediaFrameFit(true);
  if (scroll && normalized !== "collapsed") {
    (normalized === "timeline" ? timelinePanel : $("#reviewWorkbench"))?.scrollIntoView({ behavior: "smooth", block: "nearest" });
  }
  return reviewLowerPanelMode;
}

function setTimelineExpanded(expanded, { scroll = false, collapsePanel = false } = {}) {
  return setReviewLowerPanelMode(expanded ? "timeline" : collapsePanel ? "collapsed" : "review", { scroll });
}

function bindPortraitVideoResizer() {
  const handle = $("#portraitVideoResizer");
  const reviewView = $("#reviewView");
  if (!handle || !reviewView || handle.dataset.bound === "true") return;
  handle.dataset.bound = "true";
  handle.addEventListener("pointerdown", (event) => {
    if (window.innerWidth <= 900 || reviewView.dataset.reviewLayout !== "portrait" || !timelineExpanded || event.button !== 0) return;
    event.preventDefault();
    const startX = event.clientX;
    const startWidth = Math.round($("#viewerShell")?.getBoundingClientRect().width || restorePortraitVideoWidth());
    let pendingWidth = startWidth;
    let resizeFrame = null;
    handle.classList.add("dragging");
    document.body.classList.add("portrait-video-resizing");
    handle.setPointerCapture?.(event.pointerId);
    const applyPendingWidth = () => {
      resizeFrame = null;
      setPortraitVideoWidth(pendingWidth);
    };
    const move = (moveEvent) => {
      pendingWidth = startWidth - (moveEvent.clientX - startX);
      if (resizeFrame === null) resizeFrame = requestAnimationFrame(applyPendingWidth);
    };
    const finish = () => {
      if (resizeFrame !== null) cancelAnimationFrame(resizeFrame);
      const width = setPortraitVideoWidth(pendingWidth, { persist: true });
      handle.classList.remove("dragging");
      document.body.classList.remove("portrait-video-resizing");
      handle.removeEventListener("pointermove", move);
      handle.removeEventListener("pointerup", finish);
      handle.removeEventListener("pointercancel", finish);
      if (width) scheduleTimelineResizeRender(true);
    };
    handle.addEventListener("pointermove", move);
    handle.addEventListener("pointerup", finish);
    handle.addEventListener("pointercancel", finish);
  });
  handle.addEventListener("keydown", (event) => {
    if (!["ArrowLeft", "ArrowRight"].includes(event.key) || window.innerWidth <= 900 || reviewView.dataset.reviewLayout !== "portrait" || !timelineExpanded) return;
    event.preventDefault();
    const current = Number(reviewView.dataset.portraitVideoWidth) || restorePortraitVideoWidth();
    const width = setPortraitVideoWidth(current + (event.key === "ArrowLeft" ? 16 : -16), { persist: true });
    if (width) scheduleTimelineResizeRender(true);
  });
  handle.addEventListener("dblclick", () => {
    if (reviewView.dataset.reviewLayout !== "portrait" || !timelineExpanded) return;
    forgetPortraitVideoWidth();
    reviewView.style.removeProperty("--portrait-video-width");
    delete reviewView.dataset.portraitVideoWidth;
    restorePortraitVideoWidth();
    scheduleTimelineResizeRender(true);
  });
  window.addEventListener("resize", () => {
    if (reviewView.dataset.reviewLayout === "portrait" && timelineExpanded) restorePortraitVideoWidth();
  });
}

bindEvidencePanelResizer();
bindPortraitVideoResizer();

function escapeHtml(value) {
  return String(value ?? "").replace(/[&<>'"]/g, (character) => ({
    "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;",
  })[character]);
}

function cleanDisplayText(value, fallback = "") {
  const text = String(value ?? "").trim();
  if (/^(?:|none|null|undefined|nan)$/i.test(text)) return fallback;
  const suffixCleaned = text.replace(/\s*[·｜|:/-]\s*(?:none|null|undefined|nan)\s*$/i, "").trim();
  if (!suffixCleaned) return fallback;
  if (suffixCleaned !== text && /^(?:agent\s*)?审核预览$/i.test(suffixCleaned)) return fallback;
  return suffixCleaned;
}

function applyMediaAspect(container, width, height) {
  if (!container) return;
  const mediaWidth = Number(width || 0);
  const mediaHeight = Number(height || 0);
  if (mediaWidth <= 0 || mediaHeight <= 0) return;
  const aspect = mediaWidth / mediaHeight;
  container.style.setProperty("--media-aspect", String(aspect));
  container.dataset.mediaAspect = `${mediaWidth} / ${mediaHeight}`;
  container.classList.toggle("portrait", aspect < .82);
  container.classList.toggle("square", aspect >= .82 && aspect <= 1.18);
  if (container === viewerShell) {
    syncReviewLayoutForMedia(aspect);
    scheduleMediaFrameFit(true);
  }
}

function activeMediaAspect() {
  const stored = String(viewerShell?.dataset.mediaAspect || "").split("/").map(Number);
  if (stored.length === 2 && stored[0] > 0 && stored[1] > 0) return stored[0] / stored[1];
  const decodedWidth = Number(mainVideo?.videoWidth || 0);
  const decodedHeight = Number(mainVideo?.videoHeight || 0);
  if (decodedWidth > 0 && decodedHeight > 0) return decodedWidth / decodedHeight;
  const jobWidth = Number(currentJob?.videoInfo?.width || 0);
  const jobHeight = Number(currentJob?.videoInfo?.height || 0);
  if (jobWidth > 0 && jobHeight > 0) return jobWidth / jobHeight;
  return 16 / 9;
}

function fitMediaFrame() {
  if (!viewerShell || !mediaFrame || viewerShell.offsetParent === null && document.fullscreenElement !== viewerShell) return;
  const reviewView = $("#reviewView");
  const reviewStage = $("#reviewStage");
  const portraitWorkspace = reviewView?.dataset.reviewLayout === "portrait"
    && document.fullscreenElement !== viewerShell
    && reviewStage;
  const transport = viewerShell.querySelector(":scope > .player-controls");
  const transportHeight = Math.max(0, Math.ceil(transport?.getBoundingClientRect().height || 0));
  const outputEvidenceStrip = viewerShell.querySelector(":scope > #outputEvidenceStrip:not(.hidden)");
  const outputEvidenceStripHeight = Math.max(0, Math.ceil(outputEvidenceStrip?.getBoundingClientRect().height || 0));
  let availableWidth = Math.max(0, viewerShell.clientWidth);
  let availableHeight = Math.max(0, viewerShell.clientHeight - transportHeight - outputEvidenceStripHeight);
  if (portraitWorkspace) {
    const stageStyle = getComputedStyle(reviewStage);
    const horizontalPadding = (Number.parseFloat(stageStyle.paddingLeft) || 0) + (Number.parseFloat(stageStyle.paddingRight) || 0);
    const verticalPadding = (Number.parseFloat(stageStyle.paddingTop) || 0) + (Number.parseFloat(stageStyle.paddingBottom) || 0);
    availableWidth = Math.max(0, reviewStage.clientWidth - horizontalPadding);
    availableHeight = Math.max(0, reviewStage.clientHeight - verticalPadding - transportHeight - outputEvidenceStripHeight);
  }
  if (availableWidth < 2 || availableHeight < 2) return;
  const aspect = Math.max(.2, Math.min(5, activeMediaAspect()));
  let width = Math.min(availableWidth, availableHeight * aspect, portraitWorkspace ? 440 : Number.POSITIVE_INFINITY);
  let height = width / aspect;
  if (height > availableHeight) {
    height = availableHeight;
    width = height * aspect;
  }
  width = Math.max(1, Math.floor(width));
  height = Math.max(1, Math.floor(height));
  if (portraitWorkspace) {
    reviewView.style.setProperty("--portrait-shell-width", `${width}px`);
    reviewView.style.setProperty("--portrait-shell-height", `${height + transportHeight + outputEvidenceStripHeight}px`);
  } else {
    reviewView?.style.removeProperty("--portrait-shell-width");
    reviewView?.style.removeProperty("--portrait-shell-height");
  }
  // The transport spans the review stage, while portrait media is centered
  // inside it. Expose the rendered media box so portrait-only chrome (most
  // importantly the seek rail) can align with the visible video rather than
  // with the much wider stage.
  viewerShell.style.setProperty("--media-rendered-width", `${width}px`);
  viewerShell.style.setProperty("--media-rendered-height", `${height}px`);
  const fitKey = `${width}x${height}@${aspect.toFixed(6)}`;
  if (mediaFrame.dataset.fitKey !== fitKey) {
    mediaFrame.dataset.fitKey = fitKey;
    mediaFrame.style.width = `${width}px`;
    mediaFrame.style.height = `${height}px`;
    mediaFrame.style.setProperty("--decoded-media-aspect", String(aspect));
  }
  syncEvidencePlacement();
}

function scheduleMediaFrameFit(force = false) {
  if (force && mediaFitFrame !== null) {
    cancelAnimationFrame(mediaFitFrame);
    mediaFitFrame = null;
  }
  if (mediaFitFrame !== null) return;
  mediaFitFrame = requestAnimationFrame(() => {
    mediaFitFrame = null;
    fitMediaFrame();
  });
}

function syncTimelineVisibilityLayout() {
  const reviewView = $("#reviewView");
  if (!reviewView) return;
  const hidden = !timelinePanel || timelinePanel.classList.contains("hidden");
  reviewView.classList.toggle("timeline-hidden", hidden);
  const available = Boolean(currentJob) && (document.body.classList.contains("voice-workspace-open")
    || document.body.classList.contains("person-workspace-open")
    || directorStage === "events" || directorStage === "compose");
  syncReviewPanelSwitch(available, reviewPanelDeckAvailable(available));
  syncPortraitWorkspaceControls();
  scheduleMediaFrameFit(true);
}

function syncEvidencePlacement() {
  const evidence = $("#evidencePanel");
  if (!evidence) return;
  // Evidence now owns a stable column beside the player. Clear the legacy
  // pillar-box/overlay state so media aspect changes never move it over video.
  evidence.classList.remove(
    "evidence-in-gutter",
    "evidence-gutter-left",
    "evidence-gutter-right",
    "evidence-compact",
    "evidence-expanded",
  );
  evidence.style.removeProperty("--evidence-frame-top");
  evidence.style.removeProperty("--evidence-frame-height");
  evidence.style.removeProperty("--evidence-gutter-width");
  syncPortraitPrecisionWorkbench();
}

function syncPortraitPrecisionWorkbench() {
  const workbench = $("#portraitPrecisionWorkbench");
  if (!workbench) return;
  const active = $("#reviewView")?.dataset.reviewLayout === "portrait" && timelineExpanded;
  workbench.setAttribute("aria-hidden", String(!active));
  if (!active) return;

  const outputReview = timelineOutputAxisActive();
  const evidence = $("#evidencePanel");
  const selected = Boolean(evidence && !evidence.classList.contains("evidence-placeholder"));
  workbench.dataset.phase = selected ? "inspect" : "select";
  const propertiesButton = $("#portraitWorkbenchOpenProperties");
  propertiesButton?.classList.toggle("hidden", outputReview || !selected);
  if (propertiesButton) propertiesButton.disabled = outputReview || !selected;
  const title = selected
    ? $("#clipTitle")?.textContent?.trim()
    : $("#timelineEventSummaryTitle")?.textContent?.trim();
  const time = selected
    ? $("#clipTime")?.textContent?.trim()
    : $("#timelineCurrent")?.textContent?.trim();
  const reason = outputReview
    ? "这里显示当前成片位置对应的源镜头，右侧播放器用于检查最终 9:16 构图。"
    : selected
      ? $("#clipReason")?.textContent?.trim()
      : "在下方时间线选择片段，这里会显示源镜头与边界信息；右侧始终用于检查 9:16 成片构图。";
  if ($("#portraitWorkbenchTitle")) $("#portraitWorkbenchTitle").textContent = title || "选择时间线中的镜头";
  if ($("#portraitWorkbenchTime")) $("#portraitWorkbenchTime").textContent = time || "尚未选择";
  if ($("#portraitWorkbenchReason")) $("#portraitWorkbenchReason").textContent = reason;
  if ($("#portraitWorkbenchMode")) $("#portraitWorkbenchMode").textContent = outputReview ? "成片对应源镜头" : selected ? "已选源镜头" : "源片时间线";
  if ($("#portraitWorkbenchPosition")) $("#portraitWorkbenchPosition").textContent = time || "尚未定位";

  const frame = $("#portraitWorkbenchFrame");
  const thumbnails = [...document.querySelectorAll("#timelineThumbnails .timeline-thumbnail")];
  const selectedThumbnail = thumbnails.find((item) => item.classList.contains("selected-frame"));
  const sourceItem = outputReview ? null : (currentEventSegment || currentCandidate || currentEventGroup);
  const sourceSegments = currentEventSegment || currentCandidate ? [sourceItem] : currentEventGroup?.segments || [];
  const rangeStart = sourceSegments.length ? Math.min(...sourceSegments.map((item) => Number(item.start) || 0)) : null;
  const rangeEnd = sourceSegments.length ? Math.max(...sourceSegments.map((item) => Number(item.end) || Number(item.start) || 0)) : null;
  const rangeTarget = rangeStart !== null && rangeEnd !== null ? (rangeStart + rangeEnd) / 2 : timelineAbsoluteTime();
  const inSelectedRange = rangeStart === null ? [] : thumbnails.filter((item) => {
    const value = Number(item.dataset.timelineFrameTime);
    return Number.isFinite(value) && value >= rangeStart && value <= rangeEnd;
  });
  const thumbnail = (selectedThumbnail && (rangeStart === null || inSelectedRange.includes(selectedThumbnail)) ? selectedThumbnail : null)
    || inSelectedRange.sort((left, right) => Math.abs(Number(left.dataset.timelineFrameTime) - rangeTarget) - Math.abs(Number(right.dataset.timelineFrameTime) - rangeTarget))[0]
    || (rangeStart === null ? thumbnails.find((item) => item.classList.contains("playback-current")) : null)
    || (rangeStart === null ? [...thumbnails].sort((left, right) => Math.abs(Number(left.dataset.timelineFrameTime) - rangeTarget) - Math.abs(Number(right.dataset.timelineFrameTime) - rangeTarget))[0] : null);
  if (!frame) return;
  const frameBadge = frame.querySelector(".portrait-workbench-frame-badge");
  if (frameBadge) frameBadge.textContent = outputReview ? "对应源镜头" : "源镜头预览";
  const ready = Boolean(thumbnail?.style.backgroundImage);
  frame.dataset.state = ready ? "ready" : "empty";
  if (ready) {
    frame.style.backgroundImage = thumbnail.style.backgroundImage;
    const thumbnailWidth = Number.parseFloat(thumbnail.style.width) || thumbnail.getBoundingClientRect().width || 1;
    const thumbnailHeight = Number.parseFloat(thumbnail.style.height) || thumbnail.getBoundingClientRect().height || 1;
    const frameBounds = frame.getBoundingClientRect();
    const previewScale = Math.max(1, frameBounds.width / thumbnailWidth, frameBounds.height / thumbnailHeight);
    const scalePair = (value) => String(value || "").split(/\s+/).map((part) => {
      const number = Number.parseFloat(part);
      return Number.isFinite(number) ? `${number * previewScale}px` : part;
    }).join(" ");
    frame.style.backgroundSize = scalePair(thumbnail.style.backgroundSize);
    frame.style.backgroundPosition = scalePair(thumbnail.style.backgroundPosition);
    frame.setAttribute("aria-label", `${outputReview ? "当前版本画面" : "当前源镜头预览"}，${time || "当前播放位置"}`);
  } else {
    frame.style.removeProperty("background-image");
    frame.style.removeProperty("background-size");
    frame.style.removeProperty("background-position");
    frame.setAttribute("aria-label", `${outputReview ? "当前版本" : "源镜头"}缩略图正在准备`);
  }
}

function bindAdaptiveMediaLayout() {
  if (!viewerShell || !mediaFrame) return;
  if (window.ResizeObserver) {
    mediaLayoutObserver?.disconnect();
    mediaLayoutObserver = new ResizeObserver(() => scheduleMediaFrameFit());
    mediaLayoutObserver.observe(viewerShell);
    const reviewStage = $("#reviewStage");
    if (reviewStage) mediaLayoutObserver.observe(reviewStage);
    const reviewView = $("#reviewView");
    if (reviewView) mediaLayoutObserver.observe(reviewView);
  }
  if (timelinePanel && window.MutationObserver) {
    new MutationObserver(syncTimelineVisibilityLayout).observe(timelinePanel, { attributes: true, attributeFilter: ["class"] });
  }
  const evidence = $("#evidencePanel");
  if (evidence && window.MutationObserver) {
    new MutationObserver(() => scheduleMediaFrameFit()).observe(evidence, { attributes: true, attributeFilter: ["class"] });
  }
  syncTimelineVisibilityLayout();
  scheduleMediaFrameFit(true);
}

bindAdaptiveMediaLayout();

function sourcePreviewUrl(job = currentJob) {
  if (!job) return "";
  const base = String(job.previewUrl || job.sourceUrl || "").trim();
  if (!base || base === "undefined" || base === "null") return "";
  const separator = base.includes("?") ? "&" : "?";
  const assetState = job.previewReady ? "proxy" : "source";
  return `${base}${separator}asset=${assetState}&v=${encodeURIComponent(job.updatedAt || "latest")}`;
}

function stopSourcePreviewPolling() {
  clearTimeout(sourcePreviewPollTimer);
  sourcePreviewPollTimer = null;
}

function setPlayerSurfaceState(state) {
  if (!viewerShell) return;
  const nextState = ["empty", "loading", "ready", "error"].includes(String(state))
    ? String(state)
    : "empty";
  viewerShell.dataset.mediaState = nextState;
}

function switchToReadySourcePreview() {
  if (!currentJob?.previewReady || !["source", "candidate", "segment"].includes(viewerMediaKind)) return;
  const resumeTime = Number(mainVideo.currentTime || 0);
  const wasPlaying = !mainVideo.paused;
  clearPlayerNotice();
  mainVideo.addEventListener("loadedmetadata", () => {
    mainVideo.currentTime = Math.min(Math.max(0, mainVideo.duration - .05), resumeTime);
    if (wasPlaying) safePlay();
  }, { once: true });
  setMainVideoSource(sourcePreviewUrl(currentJob));
}

function beginSourcePreviewPolling(job = currentJob, force = false) {
  stopSourcePreviewPolling();
  if (!job || job.previewReady) return;
  if (!force && isActiveJobStatus(job.status)) return;
  const jobId = job.id;
  job.previewPreparing = true;
  const poll = async () => {
    if (currentJob?.id !== jobId || !["source", "candidate", "segment"].includes(viewerMediaKind)) return;
    try {
      const state = await api(`/api/jobs/${jobId}/preview-status`);
      if (currentJob?.id !== jobId) return;
      currentJob.previewReady = Boolean(state.ready);
      currentJob.previewPreparing = Boolean(state.preparing);
      if (state.ready) {
        switchToReadySourcePreview();
        return;
      }
      if (state.error) {
        showPlayerNotice(`兼容播放代理生成失败：${state.error}`);
        return;
      }
    } catch { /* source playback can continue while the optional proxy retries */ }
    sourcePreviewPollTimer = setTimeout(poll, 2500);
  };
  sourcePreviewPollTimer = setTimeout(poll, 800);
}

function clearPlayerNotice() {
  $("#playerNotice")?.classList.add("hidden");
  $("#playerNotice p") && ($("#playerNotice p").textContent = "");
  const loader = $("#playerMediaLoader");
  if (loader) {
    loader.dataset.loaderActive = "false";
    loader.classList.add("hidden");
    clearGenerativeLoader(loader);
  }
}

function showPlayerNotice(message, title = "视频暂时无法播放") {
  const notice = $("#playerNotice");
  if (!notice) return;
  $("#playerNotice strong") && ($("#playerNotice strong").textContent = title);
  $("#playerNotice p") && ($("#playerNotice p").textContent = message);
  const loading = /正在|准备|等待.+(?:生成|完成)|切换.+代理|恢复.+预览/.test(`${title} ${message}`);
  setPlayerSurfaceState(loading ? "loading" : "error");
  const loader = $("#playerMediaLoader");
  if (loader) {
    loader.dataset.loaderActive = String(loading);
    loader.classList.toggle("hidden", !loading);
    if (loading) renderGenerativeLoader(loader, { kind: "image", variant: "scan", size: 84, radius: 12, label: title });
    else clearGenerativeLoader(loader);
  }
  notice.classList.remove("hidden");
}

function mediaSourceIdentity(value) {
  const source = String(value || "").trim();
  if (!source) return "";
  try {
    const url = new URL(source, window.location.href);
    // `v` only busts browser cache. A new job heartbeat used to change this
    // value and reload the exact same MP4 while it was already playing.
    url.searchParams.delete("v");
    return `${url.origin}${url.pathname}${url.search}`;
  } catch {
    return source.replace(/([?&])v=[^&]*/g, "$1").replace(/[?&]$/, "");
  }
}

function setMainVideoSource(source, { force = false } = {}) {
  if (!mainVideo) return false;
  const nextSource = String(source || "").trim();
  if (!nextSource) {
    setPlayerSurfaceState("empty");
    if (mainVideo.getAttribute("src")) {
      playbackRequestToken += 1;
      mainVideoAutoplayToken += 1;
      mainVideo.removeAttribute("src");
      mainVideo.load();
      return true;
    }
    return false;
  }
  const currentSource = mainVideo.getAttribute("src") || mainVideo.currentSrc || "";
  if (!force && mediaSourceIdentity(currentSource) === mediaSourceIdentity(nextSource)) return false;
  setPlayerSurfaceState("loading");
  playbackRequestToken += 1;
  mainVideoAutoplayToken += 1;
  mainVideo.src = nextSource;
  return true;
}

function browserFallbackForCurrentMedia() {
  if (!currentJob) return null;
  if (viewerMediaKind === "output" && currentOutput?.filename) {
    return {
      key: `${currentJob.id}:output:${currentOutput.filename}`,
      url: `/api/jobs/${currentJob.id}/outputs/${encodeURIComponent(currentOutput.filename)}/browser-preview`,
      label: "正在生成通用格式的成片预览…",
    };
  }
  if (["source", "candidate", "segment"].includes(viewerMediaKind)) {
    return {
      key: `${currentJob.id}:source`,
      url: `/api/jobs/${currentJob.id}/browser-preview`,
      label: "当前浏览器不支持 H.264，正在生成通用 WebM 预览…",
    };
  }
  return null;
}

async function safePlay({ allowMutedFallback = false } = {}) {
  const requestToken = ++playbackRequestToken;
  clearPlayerNotice();
  try {
    const pending = mainVideo.play();
    if (pending && typeof pending.then === "function") await pending;
    if (requestToken !== playbackRequestToken || mainVideo.paused) return false;
    return true;
  } catch (error) {
    // Changing source or pausing while play() is still pending is a normal
    // media lifecycle race, not a playback failure.  Do not show a scary
    // reload notice for the browser's AbortError in that case.
    const interrupted = error?.name === "AbortError"
      || /play\(\).*pause|interrupted by a call to pause/i.test(String(error?.message || ""));
    if (interrupted || requestToken !== playbackRequestToken) return false;
    const blocked = error?.name === "NotAllowedError";
    if (blocked && allowMutedFallback && !mainVideo.muted) {
      // Browsers generally reject programmatic playback with audio after the
      // upload request has crossed an async boundary. Start muted instead of
      // leaving a newly imported video looking broken; the player control
      // makes the muted state explicit and lets the user restore audio.
      mainVideo.muted = true;
      updatePlayerChrome();
      try {
        const retry = mainVideo.play();
        if (retry && typeof retry.then === "function") await retry;
        return requestToken === playbackRequestToken && !mainVideo.paused;
      } catch (retryError) {
        if (retryError?.name === "AbortError" || requestToken !== playbackRequestToken) return false;
      }
    }
    showPlayerNotice(
      blocked ? "浏览器阻止了带声音的自动播放，请点击“重新加载”或播放按钮继续。" : `浏览器未能开始播放：${error?.message || "未知媒体错误"}`,
      blocked ? "需要手动确认播放" : "视频播放失败",
    );
    return false;
  }
}

function requestMainVideoAutoplay() {
  if (!mainVideo?.getAttribute("src")) return;
  const autoplayToken = ++mainVideoAutoplayToken;
  const playWhenReady = () => {
    if (autoplayToken !== mainVideoAutoplayToken || !mainVideo.getAttribute("src")) return;
    safePlay({ allowMutedFallback: true });
  };
  if (mainVideo.readyState >= HTMLMediaElement.HAVE_FUTURE_DATA) {
    playWhenReady();
  } else {
    mainVideo.addEventListener("canplay", playWhenReady, { once: true });
  }
}

function formatTime(seconds) {
  return window.ClipTalkWorkspaceState.formatTime(seconds);
}

function formatPreciseTimecode(seconds) {
  const total = Math.max(0, Math.round((Number(seconds) || 0) * 100));
  const hours = Math.floor(total / 360000);
  const minutes = Math.floor(total % 360000 / 6000);
  const rest = (total % 6000 / 100).toFixed(2).padStart(5, "0");
  return hours
    ? `${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}:${rest}`
    : `${String(minutes).padStart(2, "0")}:${rest}`;
}

function formatFrameTimecode(seconds, frameRate = 30) {
  const fps = Math.max(1, Math.round(Number(frameRate) || 30));
  const totalFrames = Math.max(0, Math.round((Number(seconds) || 0) * fps));
  const frames = totalFrames % fps;
  const wholeSeconds = Math.floor(totalFrames / fps);
  const hours = Math.floor(wholeSeconds / 3600);
  const minutes = Math.floor(wholeSeconds % 3600 / 60);
  const secondsPart = wholeSeconds % 60;
  return `${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}:${String(secondsPart).padStart(2, "0")}:${String(frames).padStart(2, "0")}`;
}

function parseTimecodeValue(value) {
  const textValue = String(value ?? "").trim().replace(/，/g, ".");
  if (!textValue) return NaN;
  if (/^\d+(?:\.\d+)?$/.test(textValue)) return Number(textValue);
  const parts = textValue.split(":");
  if (parts.length < 2 || parts.length > 3 || parts.some((part) => !/^\d+(?:\.\d+)?$/.test(part))) return NaN;
  const values = parts.map(Number);
  if (values.slice(1).some((part) => part >= 60)) return NaN;
  return parts.length === 2
    ? values[0] * 60 + values[1]
    : values[0] * 3600 + values[1] * 60 + values[2];
}

function formatClock(seconds) {
  const value = Math.max(0, Math.floor(Number(seconds) || 0));
  const hours = Math.floor(value / 3600);
  const minutes = Math.floor(value % 3600 / 60);
  const rest = String(value % 60).padStart(2, "0");
  return hours ? `${hours}:${String(minutes).padStart(2, "0")}:${rest}` : `${minutes}:${rest}`;
}

function progressContract(job = currentJob) {
  const facts = job?.progressFacts && typeof job.progressFacts === "object" ? job.progressFacts : {};
  const workflow = facts.workflow && typeof facts.workflow === "object" ? facts.workflow : {};
  const stage = facts.stage && typeof facts.stage === "object" ? facts.stage : {};
  const timing = facts.timing && typeof facts.timing === "object" ? facts.timing : {};
  const activity = facts.activity && typeof facts.activity === "object" ? facts.activity : {};
  return { workflow, stage, timing, activity };
}

function processingElapsedSeconds(job = currentJob) {
  if (!job) return 0;
  const timing = progressContract(job).timing;
  const hasPersistedTiming = timing.processingTimingVersion != null || job.processingTimingVersion != null;
  if (!hasPersistedTiming && !isPipelineRunningStatus(job.status)) return null;
  const persisted = Number(timing.processingElapsedSeconds ?? job.processingElapsedSeconds ?? 0);
  const base = Number.isFinite(persisted) ? Math.max(0, persisted) : 0;
  if (!isPipelineRunningStatus(job.status)) return base;
  const activeSince = Date.parse(String(timing.processingActiveSince || job.processingActiveSince || ""));
  return Number.isFinite(activeSince)
    ? base + Math.max(0, (Date.now() - activeSince) / 1000)
    : base;
}

function processingElapsedLabel(job = currentJob) {
  if (job?.status === "queued") return "处理用时尚未开始";
  const seconds = processingElapsedSeconds(job);
  if (seconds == null) return "处理用时不可用";
  const elapsed = formatClock(seconds);
  return isPipelineRunningStatus(job?.status) ? `任务已运行 ${elapsed}` : `处理用时 ${elapsed}`;
}

function workflowProgress(job = currentJob) {
  const raw = progressContract(job).workflow.fraction;
  const fallback = job?.progress;
  const value = Number(raw === null || raw === undefined ? fallback : raw);
  return Number.isFinite(value) ? Math.max(0, Math.min(1, value)) : 0;
}

function stageDisplayMode(job = currentJob) {
  const contractMode = String(progressContract(job).stage.mode || "");
  if (["determinate", "indeterminate", "finalizing", "completed"].includes(contractMode)) return contractMode;
  const legacyMode = String(job?.progressMode || "");
  if (legacyMode === "determinate") return "determinate";
  if (String(job?.etaMode || "") === "finalizing" || String(job?.etaMode || "") === "quality_check") return "finalizing";
  if (legacyMode === "completed" || job?.status === "completed") return "completed";
  return "indeterminate";
}

function progressEtaText(job, waiting = false) {
  if (job?.status === "queued") return "排队时间不计入处理用时";
  if (job?.status === "cancelling") return "停止请求已提交，正在收尾";
  if (job?.status === "awaiting_model_decision") return "等待你选择重试、降级继续或取消";
  const timing = progressContract(job).timing;
  const rawEta = timing.etaSeconds ?? job?.etaSeconds;
  const eta = Number(rawEta);
  const etaMode = String(timing.etaMode || job?.etaMode || "");
  if (stageDisplayMode(job) === "completed" && job?.status === "running") return "正在进入下一阶段";
  if (etaMode === "waiting_first_sample") return "首批完成后开始估算速度";
  if (etaMode === "finalizing") return "正在整理标点、情绪和说话人";
  if (etaMode === "unavailable") return "本阶段耗时取决于模型响应";
  if (etaMode === "encoding") return "FFmpeg 实时编码中";
  if (etaMode === "quality_check") return "正在检查视频完整性";
  if (rawEta !== null && rawEta !== undefined && Number.isFinite(eta)) {
    const lastUpdate = Date.parse(String(timing.lastProgressAt || job?.lastProgressAt || job?.updatedAt || ""));
    const liveEta = Number.isFinite(lastUpdate) && isPipelineRunningStatus(job?.status)
      ? Math.max(0, eta - (Date.now() - lastUpdate) / 1000)
      : Math.max(0, eta);
    if (liveEta < 1) return etaMode === "stage_average" ? "本阶段即将完成" : "即将进入下一阶段";
    return `${etaMode === "stage_average" ? "本阶段约" : "预计剩余约"} ${formatClock(liveEta)}`;
  }
  const stage = progressContract(job).stage;
  const completed = Number(stage.completed ?? job?.stageCompleted);
  const total = Number(stage.total ?? job?.stageTotal);
  if (Number.isFinite(completed) && Number.isFinite(total) && total > 0) {
    const unit = String(stage.unit || job?.stageUnit || "项");
    if (completed >= total) return "本项计数已完成 · 正在进入下一步";
    if (completed > 0) return "正在采集本阶段耗时";
    const firstResult = unit === "%" ? "首个进度样本" : `首${unit}`;
    return `等待${firstResult}结果`;
  }
  return waiting ? "模型处理中 · 暂无法估算剩余时间" : "等待当前阶段结果";
}

function stageProgressFact(job, fallbackPercent = 0, waiting = false) {
  if (job?.status === "queued") return "等待后台开始处理";
  if (job?.status === "cancelling") return "正在停止当前任务";
  if (job?.status === "awaiting_model_decision") return "等待处理模型阶段";
  const { stage, timing } = progressContract(job);
  const etaMode = String(timing.etaMode || job?.etaMode || "");
  const mode = stageDisplayMode(job);
  if (etaMode === "encoding") return `编码 ${fallbackPercent}%`;
  if (etaMode === "quality_check") return "正在检查成片";
  if (mode === "completed") return "当前阶段已完成";
  if (etaMode === "waiting_first_sample") return "正在准备首批采样帧";
  const completed = Number(stage.completed ?? job?.stageCompleted);
  const total = Number(stage.total ?? job?.stageTotal);
  if (mode === "finalizing") {
    if (Number.isFinite(completed) && Number.isFinite(total) && total > 0) {
      const unit = String(stage.unit || job?.stageUnit || "项");
      return `已处理 ${completed}/${total} ${unit} · 正在整理结果`;
    }
    return "正在整理结果";
  }
  if (Number.isFinite(completed) && Number.isFinite(total) && total > 0) {
    const unit = String(stage.unit || job?.stageUnit || "项");
    return unit === "%" ? `已处理 ${completed}%` : `已完成 ${completed}/${total} ${unit}`;
  }
  return waiting ? "模型处理中 · 暂无法估算进度" : `阶段 ${fallbackPercent}%`;
}

function measuredStageProgress(job) {
  const contract = progressContract(job).stage;
  const raw = contract.fraction ?? job?.stageProgress;
  if (raw === null || raw === undefined || raw === "") return null;
  const value = Number(raw);
  return Number.isFinite(value) ? Math.max(0, Math.min(1, value)) : null;
}

function stageProgressIsDeterminate(job) {
  if (stageDisplayMode(job) !== "determinate") return false;
  const stage = progressContract(job).stage;
  const total = Number(stage.total ?? job?.stageTotal);
  const seconds = Number(stage.totalSeconds ?? job?.stageTotalSeconds);
  return measuredStageProgress(job) !== null
    && ((Number.isFinite(total) && total > 0) || (Number.isFinite(seconds) && seconds > 0));
}

const speechEmotionLabels = {
  happy: "开心", sad: "悲伤", angry: "愤怒", fearful: "紧张",
  disgusted: "厌恶", surprised: "惊讶", neutral: "中性", unknown: "未知情绪",
};
const speechEventLabels = {
  applause: "掌声", laughter: "笑声", cry: "哭声", cough: "咳嗽",
  sneeze: "喷嚏", breath: "呼吸", bgm: "背景音乐", speech: "对白",
};

function audioEvidenceLabels(evidence) {
  if (!evidence) return [];
  return [
    ...(evidence.speakers || []),
    ...(evidence.emotions || []).map((item) => speechEmotionLabels[item] || item),
    ...(evidence.audioEvents || []).map((item) => speechEventLabels[item] || item),
  ].filter(Boolean).slice(0, 5);
}

function audioEvidenceMarkup(evidence) {
  const labels = audioEvidenceLabels(evidence);
  return labels.length ? `<em class="speech-evidence">${labels.map((item) => `<i>${escapeHtml(item)}</i>`).join("")}</em>` : "";
}

function saveTimelineLayerPreferences() {
  try {
    localStorage.setItem(timelineLayerStorageKey, JSON.stringify({
      cuts: timelineCutsVisible,
    }));
  } catch { /* storage may be disabled */ }
}

function updateTimelineLayerButtons() {
  const cutsButton = $("#timelineCutsToggle");
  cutsButton?.classList.toggle("active", timelineCutsVisible);
  cutsButton?.setAttribute("aria-pressed", String(timelineCutsVisible));
}

function itemSpeakers(item) {
  const evidence = item?.audioEvidence || {};
  const speakers = Array.isArray(evidence.speakers) ? evidence.speakers : (evidence.speakers ? [evidence.speakers] : []);
  const turns = Array.isArray(evidence.speakerTurns) ? evidence.speakerTurns : [];
  return [...new Set([...speakers, ...turns.map((turn) => turn?.speaker), item?.speaker].filter(Boolean).map(String))];
}

function seekTimeline(second) {
  const value = Math.max(0, Number(second) || 0);
  if (currentJob) setTimelineView(Math.max(0, value - 20), value + 20);
  if (timelineOutputAxisActive()) {
    seekCurrentMediaTime(value, { autoplay: false });
    return;
  }
  if (viewerMediaKind === "output") showSource();
  if (mainVideo.readyState >= 1) mainVideo.currentTime = Math.min(Math.max(0, mainVideo.duration - 0.05), value);
  else mainVideo.addEventListener("loadedmetadata", () => { mainVideo.currentTime = value; }, { once: true });
  updateTimelinePlayhead();
}

function compositionTransitionOverlap(segments, index) {
  if (!Array.isArray(segments) || index <= 0 || index >= segments.length) return 0;
  const current = segments[index] || {};
  const transition = current.transitionIn || {};
  if (!["dissolve", "fade_black"].includes(transition.type)) return 0;
  const previous = segments[index - 1] || {};
  const previousDuration = segmentOutputDuration(previous);
  const currentDuration = segmentOutputDuration(current);
  if (!previousDuration || !currentDuration) return 0;
  const requested = Math.max(.08, Number(transition.duration) || .18);
  return Math.max(0, Math.min(.4, requested, previousDuration / 3, currentDuration / 3));
}

function segmentSourceRange(segment) {
  const rawStart = Number(segment?.sourceStart ?? segment?.start);
  const start = Number.isFinite(rawStart) ? Math.max(0, rawStart) : 0;
  const rawEnd = Number(segment?.sourceEnd ?? segment?.end);
  const end = Number.isFinite(rawEnd) ? Math.max(start, rawEnd) : start;
  return { start, end };
}

function segmentOutputDuration(segment) {
  const { start: sourceStart, end: sourceEnd } = segmentSourceRange(segment);
  const removed = (Array.isArray(segment?.silenceCuts) ? segment.silenceCuts : []).reduce((sum, item) => {
    const start = Math.max(sourceStart, Number(item?.start) || sourceStart);
    const end = Math.min(sourceEnd, Number(item?.end) || start);
    const retained = Math.max(0, Math.min(end - start, Number(item?.retained) || 0));
    return sum + Math.max(0, end - start - retained);
  }, 0);
  return Math.max(0, sourceEnd - sourceStart - removed) / Math.max(1, Number(segment?.playbackRate) || 1);
}

function segmentOutputOffsetForSource(segment, sourceTime) {
  const { start, end } = segmentSourceRange(segment);
  const target = Math.max(start, Math.min(end, Number(sourceTime) || start));
  const removedBefore = (Array.isArray(segment?.silenceCuts) ? segment.silenceCuts : []).reduce((sum, item) => {
    const left = Math.max(start, Number(item?.start) || start);
    const right = Math.min(target, Number(item?.end) || left);
    const retained = Math.max(0, Math.min(right - left, Number(item?.retained) || 0));
    return sum + Math.max(0, right - left - retained);
  }, 0);
  return Math.max(0, target - start - removedBefore) / Math.max(1, Number(segment?.playbackRate) || 1);
}

function compositionSchedule(composed) {
  const segments = Array.isArray(composed?.segments) ? composed.segments : [];
  // A generated cover intro occupies output time but has no matching source
  // frame. Keep the first source clip aligned after that lead-in.
  let composedEnd = Math.max(0, Number(composed?.coverIntro?.duration || composed?.leadInDuration || 0));
  return segments.map((segment, index) => {
    const { start: sourceStart, end: sourceEnd } = segmentSourceRange(segment);
    const sourceDuration = Math.max(0, sourceEnd - sourceStart);
    const overlap = compositionTransitionOverlap(segments, index);
    const outputStart = Math.max(0, composedEnd - overlap);
    const outputDuration = segmentOutputDuration(segment);
    const outputEnd = outputStart + outputDuration;
    composedEnd = outputEnd;
    return { segment, index, sourceStart, sourceEnd, sourceDuration, outputDuration, overlap, outputStart, outputEnd };
  });
}

function compositionTimeForSource(composed, segmentIndex, sourceTime) {
  const entry = compositionSchedule(composed)[Number(segmentIndex)];
  if (!entry) return 0;
  return entry.outputStart + segmentOutputOffsetForSource(entry.segment, sourceTime);
}

function compositionSourceTimeAtOutputTime(composed, outputTime) {
  const schedule = compositionSchedule(composed);
  const time = Math.max(0, Number(outputTime) || 0);
  // During a dissolve both ranges are visible. Prefer the incoming segment so
  // the source marker follows the image that is taking over the frame.
  const entry = [...schedule].reverse().find((item) => time >= item.outputStart - .001)
    || schedule[0];
  if (!entry) return 0;
  const targetOffset = Math.max(0, Math.min(entry.outputDuration, time - entry.outputStart));
  let low = entry.sourceStart;
  let high = entry.sourceEnd;
  // Silence removal and playback-rate changes make the mapping piecewise.
  // Invert the existing monotonic source->output mapping instead of assuming
  // one output second always equals one source second.
  for (let iteration = 0; iteration < 24; iteration += 1) {
    const middle = (low + high) / 2;
    if (segmentOutputOffsetForSource(entry.segment, middle) < targetOffset) low = middle;
    else high = middle;
  }
  return Math.max(entry.sourceStart, Math.min(entry.sourceEnd, (low + high) / 2));
}

function compositionSourceFrameAtOutputTime(composed, outputTime) {
  const time = Math.max(0, Number(outputTime) || 0);
  const schedule = compositionSchedule(composed);
  const entry = [...schedule].reverse().find((item) =>
    time >= item.outputStart - .001 && time <= item.outputEnd + .001,
  );
  if (!entry) return null;
  return { entry, sourceTime: compositionSourceTimeAtOutputTime(composed, time) };
}

function seekCurrentMediaTime(second, { autoplay = true } = {}) {
  const target = Math.max(0, Number(second) || 0);
  const requestedSource = mediaSourceIdentity(mainVideo?.getAttribute("src") || mainVideo?.currentSrc || "");
  const apply = () => {
    const activeSource = mediaSourceIdentity(mainVideo?.getAttribute("src") || mainVideo?.currentSrc || "");
    if (requestedSource && activeSource && requestedSource !== activeSource) return;
    const duration = Number(mainVideo.duration);
    const bounded = Number.isFinite(duration) && duration > 0
      ? Math.min(Math.max(0, duration - .05), target)
      : target;
    mainVideo.currentTime = bounded;
    updateTimelinePlayhead();
    if (autoplay) safePlay();
  };
  if (mainVideo.readyState >= 1) apply();
  else mainVideo.addEventListener("loadedmetadata", apply, { once: true });
}

function seekComposedMedia(composed, segmentIndex, sourceTime, kind = "output") {
  const entry = compositionSchedule(composed)[Number(segmentIndex)];
  if (!entry) return;
  const sourceTarget = Math.max(entry.sourceStart, Math.min(entry.sourceEnd, Number(sourceTime) || entry.sourceStart));
  const outputTarget = compositionTimeForSource(composed, entry.index, sourceTarget);
  if (currentJob) {
    const timelineTarget = timelineOutputAxisActive() ? outputTarget : sourceTarget;
    setTimelineView(Math.max(0, timelineTarget - 20), timelineTarget + 20);
  }
  if (kind === "event") {
    const sameEvent = viewerMediaKind === "event" && currentEventGroup
      && String(currentEventGroup.id || "") === String(composed?.id || "");
    if (sameEvent) seekCurrentMediaTime(outputTarget);
    else previewEventGroup(composed, { seekTime: outputTarget });
    return;
  }
  const sameOutput = viewerMediaKind === "output" && currentOutput
    && String(currentOutput.filename || "") === String(composed?.filename || "");
  if (sameOutput) seekCurrentMediaTime(outputTarget);
  else selectOutput(composed.filename, true, outputTarget);
}

function selectOutputTimelineSegment(segmentIndex, outputTime = null) {
  if (!timelineOutputAxisActive() || !currentOutput) return;
  const schedule = timelineOutputComparisonModel(currentJob, currentOutput)?.schedule || [];
  if (Number(segmentIndex) < 0) {
    timelineSelectedOutputSegmentIndex = null;
    timelineFrameSelectionTime = null;
    seekCurrentMediaTime(Math.max(0, Number(outputTime) || 0));
    renderTimelineEventSummary();
    updateTimelinePlayhead();
    return;
  }
  const index = Math.max(0, Math.min(schedule.length - 1, Number(segmentIndex) || 0));
  const entry = schedule[index];
  if (!entry) return;
  timelineSelectedOutputSegmentIndex = index;
  timelineFrameSelectionTime = null;
  const target = Number.isFinite(Number(outputTime))
    ? Math.max(entry.outputStart, Math.min(entry.outputEnd, Number(outputTime)))
    : entry.outputStart;
  seekCurrentMediaTime(target);
  renderTimelineEventSummary();
  updateTimelinePlayhead();
}

function speakerMatches(item) {
  return timelineSpeakerFilter === "all" || itemSpeakers(item).includes(timelineSpeakerFilter);
}

function updateSpeakerFilterOptions(job = currentJob) {
  const select = $("#speakerFilter");
  if (!select) return;
  const filter = select.closest(".speaker-filter");
  if (!job) {
    filter?.classList.add("hidden");
    return;
  }
  const speakers = new Set();
  (job.candidates || []).forEach((item) => itemSpeakers(item).forEach((speaker) => speakers.add(speaker)));
  (job.eventGroups || []).forEach((group) => (group.segments || []).forEach((item) => itemSpeakers(item).forEach((speaker) => speakers.add(speaker))));
  (timelineTranscript || []).forEach((item) => item.speaker && speakers.add(String(item.speaker)));
  const values = ["all", ...[...speakers].sort()];
  if (!values.includes(timelineSpeakerFilter)) timelineSpeakerFilter = "all";
  select.innerHTML = values.map((value) => `<option value="${escapeHtml(value)}">${value === "all" ? "全部" : escapeHtml(value)}</option>`).join("");
  select.value = timelineSpeakerFilter;
  filter?.classList.toggle("hidden", speakers.size < 2);
}

function renderTimelineInsights(job = currentJob) {
  const panel = $("#timelineInsights");
  const list = $("#timelineInsightsList");
  if (!panel || !list || !job) return;
  const insights = [];
  const contentMode = String(job.taskMode || "") === "content_extract";
  (job.eventGroups || []).filter((group) => group.assemblyStrategy !== "manual" && !/时间轴选区高光|用户从时间轴创建/.test(String(group.title || group.summary || ""))).forEach((group) => {
    const first = group.segments?.[0];
    if (first) insights.push({ type: contentMode ? "内容片段" : "事件编排", title: group.title, reason: group.summary, score: group.score, start: first.start, end: group.segments[group.segments.length - 1]?.end || first.end, _speakers: group.segments.flatMap(itemSpeakers) });
  });
  (job.candidates || []).forEach((candidate) => insights.push({ type: contentMode ? "匹配候选" : "高光候选", title: candidate.title, reason: candidate.reason, score: candidate.score, start: candidate.start, end: candidate.end, _speakers: itemSpeakers(candidate) }));
  const visible = insights.filter((item) => timelineSpeakerFilter === "all" || item._speakers?.includes(timelineSpeakerFilter)).slice(0, 10);
  panel.classList.toggle("hidden", !visible.length);
  const count = $("#timelineInsightsCount");
  if (count) count.textContent = `${visible.length} 项`;
  list.innerHTML = visible.map((item) => `<button type="button" class="timeline-insight" data-insight-start="${Number(item.start) || 0}" data-insight-end="${Number(item.end) || 0}"><span>${escapeHtml(item.type)}</span><strong>${escapeHtml(item.title)}</strong><small>${escapeHtml(item.reason || "模型根据多模态证据判定")}</small><b>${Math.round(Number(item.score) || 0)}</b></button>`).join("");
  list?.querySelectorAll("[data-insight-start]").forEach((button) => button.addEventListener("click", () => seekTimeline(Number(button.dataset.insightStart))));
}

function taskWorkflowForJob(job = currentJob) {
  const contentMode = String(job?.taskMode || "highlight") === "content_extract";
  const workflowKey = workflowKindForJob(job);
  const stage = String(job?.stage || "queued");
  const status = String(job?.status || "");
  const publicSteps = Array.isArray(job?.presentation?.steps)
    ? job.presentation.steps
    : Array.isArray(job?.workflow?.steps) ? job.workflow.steps : [];
  if (publicSteps.length) {
    const currentIndex = publicSteps.findIndex((item) => item.state === "current");
    const complete = publicSteps.every((item) => item.state === "complete");
    return {
      contentMode,
      stepItems: publicSteps,
      steps: publicSteps.map((item) => item.label),
      currentIndex: currentIndex >= 0 ? currentIndex : (complete ? publicSteps.length : 0),
      complete,
    };
  }
  const steps = workflowKey === "person_edit"
    ? ["准备画面", "发现人物", "连续轨迹扫描", "生成人物卡", "确认出镜", "生成结果"]
    : workflowKey === "speaker_edit"
      ? ["准备声音", "识别语音", "区分说话人", "生成试听样本", "选择发言", "生成结果"]
      : contentMode
        ? ["读取素材", "识别对白", "建立轻量索引", "搜索内容", "局部画面复检", "等待确认"]
    : ["读取素材", "理解视听内容", "发现候选", "精修镜头", "组织事件", "生成版本"];
  const contentStageIndex = {
    queued: 0, starting: 0, probing: 0,
    audio_analysis: 1, speech_recognition: 1, speech_analysis: 1, content_transcription: 1,
    content_sampling: 2, content_indexing: 2, content_recognition: 2,
    content_index_ready: 3, content_search: 3, content_person_description: 3,
    content_active_speaker: 3,
    content_refinement: 4,
    content_search_ready: 5, awaiting_content_confirmation: 5,
    rendering: 6, render: 6, completed: 6,
  };
  const highlightStageIndex = {
    queued: 0, starting: 0, probing: 0,
    audio_analysis: 1, speech_recognition: 1, speech_analysis: 1,
    sampling: 2, content_classification: 2, coarse_vlm: 2,
    refine_vlm: 3,
    event_grouping: 4, event_director: 4,
    edit_planning: 5, edit_planning_complete: 5, rendering: 5, render: 5,
    auto_composition: 5, awaiting_confirmation: 5, completed: 6,
  };
  const personStageIndex = {
    queued: 0, starting: 0, probing: 0, content_sampling: 0,
    content_indexing: 1, content_recognition: 1,
    content_index_ready: 2, content_search: 2, content_person_description: 2,
    content_active_speaker: 3, content_refinement: 3,
    content_search_ready: 4, awaiting_content_confirmation: 4,
    rendering: 5, render: 5, completed: 6,
  };
  const speakerStageIndex = {
    queued: 0, starting: 0, probing: 0, voice_discovery_available: 0,
    audio_analysis: 1, speech_recognition: 1, speech_analysis: 1,
    voice_discovery: 2, voice_discovery_ready: 4,
    content_search_ready: 4, awaiting_content_confirmation: 4,
    rendering: 5, render: 5, completed: 6,
  };
  const stageMap = workflowKey === "person_edit" ? personStageIndex
    : workflowKey === "speaker_edit" ? speakerStageIndex
      : contentMode ? contentStageIndex : highlightStageIndex;
  let currentIndex = stageMap[stage];
  if (currentIndex === undefined) currentIndex = 0;
  if (contentMode && status === "awaiting_content_confirmation") currentIndex = ["person_edit", "speaker_edit"].includes(workflowKey) ? 4 : 5;
  if (!contentMode && status === "awaiting_confirmation") currentIndex = 5;
  if (status === "completed") currentIndex = steps.length;
  return { contentMode, steps, currentIndex, complete: currentIndex >= steps.length };
}

function thinkingConfigForJob(job) {
  if (!job) return null;
  if (["briefing", "brief_confirmation"].includes(job.status)) {
    return { state: "composing", label: "正在整理剪辑需求，等待确认后开始分析" };
  }
  if (job.status === "cancelling") {
    return { state: "solving", label: "正在停止当前分析任务" };
  }
  if (job.status === "awaiting_model_decision") {
    return { state: "solving", label: "模型阶段需要选择重试或降级" };
  }
  if (job.status !== "running") return null;
  const stage = String(job.stage || "");
  if (["queued", "starting", "probing"].includes(stage)) {
    return { state: "connecting", label: "正在读取素材并准备分析环境" };
  }
  if (stage === "audio_analysis") {
    return { state: "listening", label: "正在分析音频、语音能量与声音事件" };
  }
  if (["speech_recognition", "speech_analysis"].includes(stage)) {
    return { state: "listening", label: "正在理解对白、情绪与声音事件" };
  }
  if (stage === "content_transcription") {
    return { state: "listening", label: "正在建立可检索的对白与匿名说话人索引" };
  }
  if (["content_sampling", "content_indexing", "content_recognition"].includes(stage)) {
    return { state: "searching", label: stage === "content_recognition" ? "正在建立本次检索需要的内容索引" : "正在准备本次检索需要的素材证据" };
  }
  if (["content_index_ready", "content_search", "content_person_description", "content_active_speaker", "content_refinement"].includes(stage)) {
    return { state: "solving", label: "正在执行本次所需分析、检索证据并精修匹配边界" };
  }
  if (["sampling", "content_classification", "coarse_vlm"].includes(stage)) {
    return { state: "searching", label: stage === "content_classification" ? "正在识别内容类型与叙事结构" : "正在通看全片并寻找精彩瞬间" };
  }
  if (stage === "refine_vlm") {
    return { state: "solving", label: "正在判断候选价值并精修镜头边界" };
  }
  if (["event_grouping", "event_director"].includes(stage)) {
    return { state: "shaping", label: "正在把精彩镜头编排成事件高光" };
  }
  if (["rendering", "render"].includes(stage)) {
    return { state: "composing", label: String(job.taskMode || "") === "content_extract" ? "正在合成已确认内容并检查输出" : "正在合成高光成片并检查输出" };
  }
  if (stage === "edit_planning") {
    return { state: "solving", label: "正在设计局部镜头、叙事结构与排列顺序" };
  }
  return null;
}

function friendlyModelLabel(value) {
  const text = String(value || "系统");
  if (/sensevoice|speech|audio|语音|音频/i.test(text)) return "对白分析";
  if (/vlm|vision|visual|视觉/i.test(text)) return "画面分析";
  if (/llm|plan|规划/i.test(text)) return "剪辑规划";
  if (/ffmpeg|render|渲染|编码/i.test(text)) return "本地渲染";
  return text === "系统" ? "本地流程" : text;
}

function updatePipelineThinkingOrb(job) {
  const element = $("#pipelineThinkingOrb");
  if (!element) return;
  const config = thinkingConfigForJob(job);
  element.classList.toggle("hidden", !config);
  if (!config) {
    window.ThinkingOrbsBridge?.clear(element);
    return;
  }
  element.dataset.orbState = config.state;
  element.dataset.orbLabel = config.label;
  window.ThinkingOrbsBridge?.render(element, { ...config, size: 40, theme: "light" });
}

function updateDirectorThinkingOrb(job = currentJob) {
  const element = $("#directorThinkingOrb");
  if (!element) return;
  const active = thinkingConfigForJob(job);
  const config = active || (job?.status === "briefing"
    ? { state: "composing", label: "正在理解剪辑需求" }
    : { state: "shaping", label: job ? "等待下一步操作" : "等待素材" });
  element.dataset.orbState = config.state;
  element.dataset.orbLabel = config.label;
  window.ThinkingOrbsBridge?.render(element, { ...config, size: 30, theme: "light" });
}

function updateAnalysisConsole(job) {
  const percent = Math.round((Number(job.progress) || 0) * 100);
  const rawStageProgress = measuredStageProgress(job);
  const stageFraction = rawStageProgress !== null ? rawStageProgress : 0;
  const stagePercent = Math.round(Math.max(0, Math.min(1, stageFraction)) * 100);
  const rendering = job.status === "running" && ["rendering", "render"].includes(String(job.stage || ""));
  const activePipeline = isPipelineRunningStatus(job.status);
  const determinate = stageProgressIsDeterminate(job);
  const waiting = activePipeline && !determinate;
  updateJobElapsedClock(job);
  const progressOrb = $("#progressOrb");
  progressOrb?.style.setProperty("--progress", `${percent}%`);
  progressOrb?.classList.toggle("rendering-mode", rendering);
  // Keep the orb alive during every active analysis stage. Previously only
  // rendering rotated it, so refine_vlm (for example 52%) looked frozen even
  // while the worker was advancing through candidates.
  progressOrb?.classList.toggle("active-mode", activePipeline && !rendering);
  const activeEarlyStage = ["queued", "starting", "probing", "audio_analysis", "speech_recognition", "speech_analysis"].includes(String(job.stage || ""));
  const percentElement = $("#jobPercent");
  if (percentElement) percentElement.textContent = rendering
    ? `合成中 · ${stagePercent}%`
    : waiting
      ? "处理中"
      : job.status === "running" && activeEarlyStage
      ? `总体 ${percent}% · 阶段 ${stagePercent}%`
      : `${percent}%`;
  $("#jobProgress")?.classList.toggle("indeterminate", waiting || rendering);
  $("#jobProgress")?.classList.toggle("active", activePipeline);
  $("#jobProgress")?.classList.toggle("waiting", waiting);
  const stageProgressElement = $("#jobStageProgress");
  if (stageProgressElement) {
    const countText = stageProgressFact(job, stagePercent, waiting);
    const completedSeconds = Number(job.stageCompletedSeconds);
    const totalSeconds = Number(job.stageTotalSeconds);
    stageProgressElement.textContent = Number.isFinite(completedSeconds) && Number.isFinite(totalSeconds) && totalSeconds > 0
      ? `${countText} · ${completedSeconds.toFixed(1)} / ${totalSeconds.toFixed(1)} 秒`
      : countText;
  }
  const modelElement = $("#jobModel");
  if (modelElement) modelElement.textContent = friendlyModelLabel(job.model);
  const etaElement = $("#jobEta");
  if (etaElement) etaElement.textContent = progressEtaText(job, waiting);
  if ($("#jobDetail")) $("#jobDetail").textContent = job.currentAction || job.detail || job.stage || "准备中";
  $("#jobStatus")?.classList.toggle("waiting-mode", waiting);
  const workflow = taskWorkflowForJob(job);
  const stageList = $("#jobStageList");
  if (stageList) {
    const signature = `${workflow.contentMode ? "content" : "highlight"}:${workflow.steps.join("|")}`;
    if (stageList.dataset.workflowSignature !== signature) {
      stageList.dataset.workflowSignature = signature;
      stageList.innerHTML = workflow.steps.map((label) => `<li><i></i><span>${label}</span></li>`).join("");
    }
    stageList.querySelectorAll("li").forEach((item, index) => {
      item.classList.toggle("done", workflow.complete || index < workflow.currentIndex);
      item.classList.toggle("current", !workflow.complete && index === workflow.currentIndex && !["failed", "cancelled", "awaiting_model_decision"].includes(job.status));
    });
  }
  updatePipelineThinkingOrb(job);
}

function updateJobElapsedClock(job = currentJob) {
  const elapsedElement = $("#jobElapsed");
  if (!elapsedElement || !job) return;
  elapsedElement.textContent = processingElapsedLabel(job);
}

function renderAnalysisDecision(job) {
  const container = $("#analysisDecision");
  const consolePanel = $("#jobStatus");
  if (!container || !consolePanel) return;
  const pending = job.status === "awaiting_model_decision" ? job.pendingDecision : null;
  consolePanel.classList.toggle("decision-mode", Boolean(pending));
  container.classList.toggle("hidden", !pending);
  if (!pending) {
    container.innerHTML = "";
    return;
  }
  const speechStage = pending.stage === "speech_analysis";
  const reason = String(pending.error || "当前阶段没有返回可用结果");
  const retryLabel = speechStage ? "重试语音分析" : "重试当前阶段";
  const fallbackLabel = speechStage ? "继续视觉分析" : "降级继续";
  const fallbackTitle = speechStage
    ? "跳过语音分析，继续分析画面"
    : "使用当前阶段的降级规则继续分析";
  const guidance = speechStage
    ? "语音仅用于辅助判断，不影响继续进行视觉高光分析。"
    : "已完成的画面和分析检查点均已保留。";
  const actions = speechStage
    ? `<button type="button" class="primary" data-model-decision="fallback" title="${escapeHtml(fallbackTitle)}">${fallbackLabel}</button><button type="button" data-model-decision="retry">${retryLabel}</button>`
    : `<button type="button" class="primary" data-model-decision="retry">${retryLabel}</button><button type="button" class="warning" data-model-decision="fallback" title="${escapeHtml(fallbackTitle)}">${fallbackLabel}</button>`;
  container.innerHTML = `<p><b>${escapeHtml(pending.stageLabel || "模型阶段")}未完成。</b> ${guidance}</p><small class="analysis-decision-reason"><b>原因</b>${escapeHtml(reason)}</small>${actions}<button type="button" data-model-decision="cancel">取消任务</button>`;
  container?.querySelectorAll("[data-model-decision]").forEach((button) => button.addEventListener("click", () => resolveModelDecision(button.dataset.modelDecision)));
}

async function resolveModelDecision(action) {
  const interrupted = currentJob?.status === "failed" && currentJob?.stage === "interrupted";
  if (!currentJob || (currentJob.status !== "awaiting_model_decision" && !interrupted) || actionBusy) return;
  const actionToken = captureJobAction();
  const pending = currentJob.pendingDecision || {};
  const speechStage = pending.stage === "speech_analysis";
  const labels = {
    retry: speechStage ? "重新运行语音分析？" : "重试当前模型阶段？",
    fallback: speechStage ? "跳过语音辅助，继续视觉分析？" : "按降级规则继续？",
    cancel: "取消当前任务？",
  };
  const details = [
    pending.error ? `失败原因：${pending.error}` : "当前阶段没有返回可用结果",
    "已完成的画面和分析检查点会保留",
    speechStage && action === "fallback"
      ? "将继续分析画面；本次结果不使用对白、情绪、声音事件和说话人信息"
      : "当前操作只影响未完成阶段",
  ];
  if (!await requestActionConfirmation({ title: "分析阶段需要处理", summary: labels[action], details })) return;
  actionBusy = true;
  $("#analysisDecision")?.querySelectorAll("button").forEach((button) => { button.disabled = true; });
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/analysis-decision`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: { action },
    });
    if (!commitJobAction(job, actionToken)) return;
    if (jobNeedsPolling(job)) pollJob();
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) actionBusy = false; }
}

function updatePlayerChrome() {
  const duration = Number(mainVideo.duration || 0);
  const current = Number(mainVideo.currentTime || 0);
  const play = $("#playerPlay");
  const previous = $("#playerPrevious");
  const next = $("#playerNext");
  const mute = $("#playerMute");
  const fullscreen = $("#playerFullscreen");
  const viewerShell = $("#viewerShell");
  const clock = $("#playerClock");
  const seek = $("#playerSeek");
  const segmentNavigationAvailable = Number(currentJob?.candidates?.length || 0) > 1;
  const playbackAvailable = Boolean(currentJob && (mainVideo.currentSrc || mainVideo.src));
  if (play) {
    const playing = !mainVideo.paused;
    play.dataset.state = playing ? "playing" : "paused";
    play.dataset.tooltip = playing ? "暂停" : "播放";
    play.setAttribute("aria-label", playing ? "暂停" : "播放");
    play.disabled = !playbackAvailable;
    play.setAttribute("aria-disabled", String(!playbackAvailable));
    play.textContent = "";
  }
  [previous, next].forEach((button) => {
    if (!button) return;
    button.disabled = !segmentNavigationAvailable;
    button.setAttribute("aria-disabled", String(!segmentNavigationAvailable));
  });
  viewerShell?.querySelector(".player-segment-group")?.classList.toggle("hidden", !segmentNavigationAvailable);
  if (mute) {
    const muted = mainVideo.muted || mainVideo.volume === 0;
    mute.dataset.state = muted ? "muted" : "on";
    mute.dataset.tooltip = muted ? "取消静音" : "静音";
    mute.setAttribute("aria-label", muted ? "取消静音" : "静音");
    mute.setAttribute("aria-pressed", String(muted));
  }
  if (fullscreen) {
    const active = Boolean(viewerShell && document.fullscreenElement === viewerShell);
    fullscreen.dataset.state = active ? "exit" : "enter";
    fullscreen.dataset.tooltip = active ? "退出全屏" : "进入全屏";
    fullscreen.setAttribute("aria-label", active ? "退出全屏" : "进入全屏");
    fullscreen.setAttribute("aria-pressed", String(active));
  }
  if (clock) clock.textContent = `${formatClock(current)} / ${formatClock(duration)}`;
  if (seek) {
    const progress = duration > 0 ? Math.max(0, Math.min(1, current / duration)) : 0;
    seek.value = String(Math.round(progress * 1000));
    seek.style.setProperty("--player-progress", `${progress * 100}%`);
  }
  const volume = $("#playerVolume");
  if (volume) volume.style.setProperty("--player-volume", `${Math.max(0, Math.min(1, Number(mainVideo.volume || 0))) * 100}%`);
}

function timelineDurationValue() {
  if (timelineOutputAxisActive()) return timelineOutputDurationValue(currentOutput);
  return Number(waveformData?.duration || currentJob?.videoInfo?.duration || mainVideo.duration || 0);
}

function timelineOutputDurationValue(output = currentOutput) {
  if (!output?.segments?.length) return Number(output?.duration || 0);
  const schedule = compositionSchedule(output);
  return Number(output.duration) || Number(schedule.at(-1)?.outputEnd || 0);
}

function timelineHasOutputComparison() {
  return Boolean(viewerMediaKind === "output" && currentOutput);
}

function timelineOutputAxisActive() {
  return timelineHasOutputComparison() && timelineCoordinateSpace === "output";
}

function setTimelineCoordinateSpace(_space) {
  if (!timelineHasOutputComparison()) return;
  timelineCoordinateSpace = _space;
  timelineReviewFollow = false;
  timelineFrameSelectionTime = null;
  timelineViewStart = 0;
  timelineViewEnd = timelineOutputDurationValue(currentOutput);
  timelineMediaRenderKey = "";
  waveformRenderKey = "";
  updateTimeline();
}

function timelineViewRange() {
  const duration = timelineDurationValue();
  const start = Math.max(0, Math.min(duration, timelineViewStart || 0));
  const end = Math.max(start, Math.min(duration, timelineViewEnd || duration));
  return { start, end: end > start ? end : duration, duration: Math.max(0.001, (end > start ? end : duration) - start) };
}

function timelinePercentInView(second) {
  const view = timelineViewRange();
  return (Number(second) - view.start) / view.duration * 100;
}

function clampTimelineValue(value, minimum, maximum) {
  return Math.max(minimum, Math.min(maximum, Number(value) || 0));
}

function timelineDensityProfile(viewportHeight, _detailMode) {
  const height = Math.max(190, Number(viewportHeight) || 260);
  const compact = height < 250;
  const expanded = height >= 360;
  return {
    name: compact ? "compact" : expanded ? "expanded" : "standard",
    maximumLabelLanes: compact ? 1 : 2,
    // Shot cards keep their source-time centre. Dense cards may only move to
    // another vertical lane; they are never pushed left or right away from
    // the frame range they describe.
    maximumShotLanes: compact ? 1 : (expanded ? 3 : 2),
    labelLaneHeight: compact ? 32 : expanded ? 40 : 36,
    relationHeight: compact ? 10 : expanded ? 14 : 12,
    shotLaneHeight: compact ? 34 : expanded ? 44 : 40,
    pictureMinimum: compact ? 40 : expanded ? 56 : 48,
    pictureMaximum: compact ? 52 : expanded ? 80 : 68,
    audioMinimum: compact ? 48 : expanded ? 72 : 58,
    audioMaximum: compact ? 64 : expanded ? 112 : 88,
    rulerHeight: 22,
  };
}

function timelineTrackLayout(viewportHeight, detailMode, labelLanes = 1, shotLanes = 1, layoutKind = "hierarchy") {
  const height = Math.max(190, Number(viewportHeight) || 260);
  const profile = timelineDensityProfile(height, detailMode);
  const trackHeight = Math.max(150, height - profile.rulerHeight);
  if (layoutKind === "source-tracks") {
    const pictureHeight = clampTimelineValue(
      Math.round(trackHeight * .56), 76, Math.max(76, trackHeight - 64),
    );
    const audioHeight = Math.max(64, trackHeight - pictureHeight);
    return {
      ...profile,
      height,
      trackHeight,
      eventLabelsHeight: 0,
      labelLaneHeight: 0,
      shotLaneHeight: 0,
      eventCardHeight: 0,
      shotCardHeight: 0,
      eventRowHeight: 0,
      shotRowHeight: 0,
      pictureTop: 0,
      pictureHeight,
      audioTop: pictureHeight,
      audioHeight,
    };
  }
  const singleContentRow = ["content-review", "content-review-history"].includes(layoutKind);
  const splitContentRows = layoutKind === "content-review-history";
  let pictureHeight = clampTimelineValue(
    Math.round(trackHeight * (singleContentRow ? .29 : .2)),
    profile.pictureMinimum,
    singleContentRow ? Math.round(profile.pictureMaximum * 1.6) : profile.pictureMaximum,
  );
  // Audio carries real waveform and speech-energy evidence, so it receives a
  // full quarter of the review surface. Event and shot hierarchy stays
  // readable but no longer consumes nearly two thirds of the timeline.
  let audioHeight = clampTimelineValue(
    Math.round(trackHeight * (singleContentRow ? .34 : .26)),
    profile.audioMinimum,
    singleContentRow ? Math.round(profile.audioMaximum * 1.7) : profile.audioMaximum,
  );
  const minimumHierarchy = singleContentRow
    ? (splitContentRows ? 72 : 58)
    : (profile.name === "compact" ? 72 : 96);
  let hierarchyHeight = Math.max(minimumHierarchy, trackHeight - pictureHeight - audioHeight);
  let overflow = pictureHeight + audioHeight + hierarchyHeight - trackHeight;
  if (overflow > 0) {
    const audioReduction = Math.min(overflow, Math.max(0, audioHeight - profile.audioMinimum));
    audioHeight -= audioReduction;
    overflow -= audioReduction;
    const pictureReduction = Math.min(overflow, Math.max(0, pictureHeight - profile.pictureMinimum));
    pictureHeight -= pictureReduction;
  }
  hierarchyHeight = Math.max(minimumHierarchy, trackHeight - pictureHeight - audioHeight);
  audioHeight += Math.max(0, trackHeight - pictureHeight - audioHeight - hierarchyHeight);
  const eventRowHeight = singleContentRow ? hierarchyHeight : Math.round(hierarchyHeight * .55);
  const shotRowHeight = singleContentRow ? 0 : hierarchyHeight - eventRowHeight;
  const eventLabelsHeight = Math.max(32, eventRowHeight - (singleContentRow ? 4 : profile.relationHeight));
  const labelLaneHeight = eventLabelsHeight / Math.max(1, labelLanes);
  const shotLaneHeight = singleContentRow ? 0 : Math.max(28, (shotRowHeight - 4) / Math.max(1, shotLanes));
  const eventCardHeight = clampTimelineValue(labelLaneHeight - 8, 32, 44);
  const shotCardHeight = singleContentRow ? 0 : clampTimelineValue(shotLaneHeight - 8, 24, 36);
  return {
    ...profile,
    height,
    trackHeight,
    eventLabelsHeight,
    labelLaneHeight,
    shotLaneHeight,
    eventCardHeight,
    shotCardHeight,
    eventRowHeight,
    shotRowHeight,
    pictureTop: eventRowHeight + shotRowHeight,
    pictureHeight,
    audioTop: eventRowHeight + shotRowHeight + pictureHeight,
    audioHeight,
  };
}

function applyTimelineTrackGeometry(presentation, layout) {
  const labels = $("#timelineTrackLabels");
  if (!labels || !layout) return;
  const kind = String(presentation?.layoutKind || "source-tracks");
  let rows;
  if (kind === "source-tracks") {
    rows = [layout.pictureHeight, layout.audioHeight];
  } else if (kind === "content-review-history") {
    const first = Math.floor(layout.eventRowHeight / 2);
    rows = [first, layout.eventRowHeight - first, layout.pictureHeight, layout.audioHeight];
  } else if (kind === "content-review") {
    rows = [layout.eventRowHeight, layout.pictureHeight, layout.audioHeight];
  } else {
    rows = [layout.eventRowHeight, layout.shotRowHeight, layout.pictureHeight, layout.audioHeight];
  }
  const labelCount = labels.children.length;
  if (rows.length !== labelCount) {
    const sharedHeight = Math.max(32, layout.trackHeight / Math.max(1, labelCount));
    rows = Array.from({ length: labelCount }, () => sharedHeight);
  }
  labels.style.setProperty(
    "grid-template-rows",
    rows.map((value) => `${Math.max(0, value)}px`).join(" "),
    "important",
  );
  labels.style.setProperty("padding", `0 0 ${layout.rulerHeight}px`, "important");
  labels.dataset.trackCount = String(labelCount);
}

function currentTimelineLinkedIntervals() {
  const normalize = (item) => {
    const { start, end } = segmentSourceRange(item);
    return Number.isFinite(start) && Number.isFinite(end) && end > start ? { start, end } : null;
  };
  if (currentEventSegment) return [normalize(currentEventSegment)].filter(Boolean);
  if (currentCandidate) return [normalize(currentCandidate)].filter(Boolean);
  if (currentEventGroup?.segments?.length) return currentEventGroup.segments.map(normalize).filter(Boolean);
  if (currentOutput?.segments?.length) {
    if (timelineOutputAxisActive()) {
      return compositionSchedule(currentOutput).map((entry) => ({ start: entry.outputStart, end: entry.outputEnd }));
    }
    return currentOutput.segments.map(normalize).filter(Boolean);
  }
  return [];
}

function timelineEventSegmentAtTime(time) {
  const value = Number(time);
  if (!Number.isFinite(value) || !currentJob?.eventGroups?.length) return null;
  const matches = timelineEventDisplayModel(currentJob).groups.flatMap((group) =>
    (group.segments || []).filter((segment) => value >= Number(segment.start) && value <= Number(segment.end))
      .map((segment) => ({ group, segment })),
  );
  return matches.sort((left, right) =>
    (Number(left.segment.end) - Number(left.segment.start)) - (Number(right.segment.end) - Number(right.segment.start)),
  )[0] || null;
}

function timelineAbsoluteTime() {
  if (timelineOutputAxisActive()) return Number(mainVideo.currentTime || 0);
  const composed = viewerMediaKind === "event" ? currentEventGroup : viewerMediaKind === "output" ? currentOutput : null;
  if (composed?.segments?.length) {
    return compositionSourceTimeAtOutputTime(composed, Number(mainVideo.currentTime || 0));
  }
  if (viewerMediaKind === "output" && currentOutput) return Number(currentOutput.start) + Number(mainVideo.currentTime || 0);
  return Number(mainVideo.currentTime || 0);
}

function drawWaveform(force = false) {
  const trackContent = timelineTrackContent || timelineViewport;
  if (!trackContent || !waveformCanvas || timelinePanel?.classList.contains("hidden")) return;
  const bounds = trackContent.getBoundingClientRect();
  if (!bounds.width) return;
  const ratio = window.devicePixelRatio || 1;
  const view = timelineViewRange();
  const renderKey = [
    waveformJobId, Math.round(bounds.width), Math.round(bounds.height), ratio,
    view.start.toFixed(3), view.end.toFixed(3), timelineVisualMode,
    viewerMediaKind, currentOutput?.filename || "", timelineCoordinateSpace,
    document.documentElement.dataset.theme || "",
    waveformData?.schemaVersion || 0,
    waveformData?.minimums?.length || waveformData?.peaks?.length || 0,
  ].join("|");
  if (!force && waveformRenderKey === renderKey) return;
  waveformRenderKey = renderKey;
  waveformCanvas.width = Math.round(bounds.width * ratio);
  waveformCanvas.height = Math.round(bounds.height * ratio);
  const context = waveformCanvas.getContext("2d");
  if (!context) return;
  context.scale(ratio, ratio);
  context.clearRect(0, 0, bounds.width, bounds.height);
  const minimums = waveformData?.minimums || [];
  const maximums = waveformData?.maximums || [];
  const legacyPeaks = waveformData?.peaks || [];
  const sampleCount = minimums.length && minimums.length === maximums.length ? minimums.length : legacyPeaks.length;
  const timelineStyle = timelineViewport ? getComputedStyle(timelineViewport) : null;
  const timelineColor = (name, fallback) => timelineStyle?.getPropertyValue(name)?.trim() || fallback;
  const waveformGuideColor = timelineColor("--timeline-wave-guide", "rgba(116, 151, 135, .32)");
  const waveformPrimaryColor = timelineColor("--timeline-wave-primary", "#72b487");
  const waveformMutedColor = timelineColor("--timeline-wave-muted", "rgba(112, 143, 130, .24)");
  const rulerTextColor = timelineColor("--timeline-ruler-text", "#91a8a0");
  const cssPixels = (name, fallback) => {
    const parsed = Number.parseFloat(timelineStyle?.getPropertyValue(name) || "");
    return Number.isFinite(parsed) ? parsed : fallback;
  };
  const fallbackLayout = timelineTrackLayout(
    bounds.height,
    timelineViewport?.dataset.timelineMode === "detail",
    Math.max(1, Number(timelineViewport?.dataset.eventLabelLanes || 1)),
    Math.max(1, Number(timelineViewport?.dataset.shotMarkerLanes || 1)),
    timelineViewport?.dataset.trackLayout || "hierarchy",
  );
  // The waveform uses the exact same track geometry as labels, shots and
  // thumbnails. This avoids the previous duplicate constants drifting apart
  // after the user resized the video/timeline split.
  const audioRegionTop = cssPixels("--timeline-audio-track-top", fallbackLayout.audioTop);
  const rulerHeight = cssPixels("--timeline-ruler-height", fallbackLayout.rulerHeight);
  const audioRegionBottom = Math.max(audioRegionTop + 12, bounds.height - rulerHeight);
  const audioRegionHeight = Math.max(12, audioRegionBottom - audioRegionTop);
  const waveformHeight = Math.min(96, Math.max(20, audioRegionHeight - 8));
  const waveformTop = audioRegionTop + Math.max(0, (audioRegionHeight - waveformHeight) / 2);
  const waveformBottom = waveformTop + waveformHeight;
  const center = (waveformTop + waveformBottom) / 2;
  const amplitude = Math.max(4, waveformHeight / 2 - 3);
  context.fillStyle = waveformGuideColor;
  context.fillRect(0, center - 0.5, bounds.width, 1);
  context.fillRect(0, waveformTop, bounds.width, 1);
  context.fillRect(0, waveformBottom, bounds.width, 1);
  if (sampleCount && !timelineOutputAxisActive()) {
    const fullDuration = timelineDurationValue();
    const comparisonIntervals = viewerMediaKind === "output" && currentOutput?.segments?.length
      ? currentTimelineLinkedIntervals()
      : [];
    const visibleFrom = Math.max(0, Math.floor(view.start / fullDuration * sampleCount));
    const visibleTo = Math.min(sampleCount, Math.ceil(view.end / fullDuration * sampleCount));
    const visibleLength = Math.max(1, visibleTo - visibleFrom);
    const columns = Math.max(1, Math.min(Math.floor(bounds.width), visibleLength));
    const normalization = Math.max(.04, Number(waveformData?.normalizationPeak) ||
      legacyPeaks.reduce((peak, value) => Math.max(peak, Number(value) || 0), 0) ||
      maximums.reduce((peak, value, index) => Math.max(peak, Math.abs(Number(value) || 0), Math.abs(Number(minimums[index]) || 0)), 0));
    for (let column = 0; column < columns; column += 1) {
      const from = visibleFrom + Math.floor(column / columns * visibleLength);
      const to = Math.max(from + 1, visibleFrom + Math.floor((column + 1) / columns * visibleLength));
      let minimum = 0;
      let maximum = 0;
      for (let index = from; index < Math.min(to, sampleCount); index += 1) {
        if (minimums.length) {
          minimum = Math.min(minimum, Number(minimums[index]) || 0);
          maximum = Math.max(maximum, Number(maximums[index]) || 0);
        } else {
          const peak = Number(legacyPeaks[index]) || 0;
          minimum = Math.min(minimum, -peak);
          maximum = Math.max(maximum, peak);
        }
      }
      const upper = center - Math.min(1, maximum / normalization) * amplitude;
      const lower = center - Math.max(-1, minimum / normalization) * amplitude;
      const x = column / columns * bounds.width;
      const width = Math.max(1, bounds.width / columns + .15);
      const columnTime = view.start + (column + .5) / columns * view.duration;
      const usedByOutput = !comparisonIntervals.length || comparisonIntervals.some((range) =>
        columnTime >= range.start && columnTime <= range.end,
      );
      context.fillStyle = usedByOutput ? waveformPrimaryColor : waveformMutedColor;
      context.fillRect(x, upper, width, Math.max(1, lower - upper));
    }
  } else if (sampleCount && timelineOutputAxisActive()) {
    const sourceDuration = Number(waveformData?.duration || currentJob?.videoInfo?.duration || 0);
    const columns = Math.max(1, Math.min(Math.floor(bounds.width), Math.ceil(view.duration * 12)));
    const normalization = Math.max(.04, Number(waveformData?.normalizationPeak) ||
      legacyPeaks.reduce((peak, value) => Math.max(peak, Number(value) || 0), 0) ||
      maximums.reduce((peak, value, index) => Math.max(peak, Math.abs(Number(value) || 0), Math.abs(Number(minimums[index]) || 0)), 0));
    for (let column = 0; column < columns; column += 1) {
      const outputTime = view.start + (column + .5) / columns * view.duration;
      const mapped = compositionSourceFrameAtOutputTime(currentOutput, outputTime);
      if (!mapped || sourceDuration <= 0) continue;
      const sampleIndex = Math.max(0, Math.min(sampleCount - 1, Math.floor(mapped.sourceTime / sourceDuration * sampleCount)));
      const minimum = minimums.length ? Number(minimums[sampleIndex] || 0) : -Number(legacyPeaks[sampleIndex] || 0);
      const maximum = maximums.length ? Number(maximums[sampleIndex] || 0) : Number(legacyPeaks[sampleIndex] || 0);
      const upper = center - Math.min(1, maximum / normalization) * amplitude;
      const lower = center - Math.max(-1, minimum / normalization) * amplitude;
      const x = column / columns * bounds.width;
      const width = Math.max(1, bounds.width / columns + .15);
      context.fillStyle = waveformPrimaryColor;
      context.fillRect(x, upper, width, Math.max(1, lower - upper));
    }
  }
  const duration = timelineDurationValue();
  if (duration > 0) {
    const view = timelineViewRange();
    const targetTicks = Math.max(4, Math.min(10, Math.floor(bounds.width / 90)));
    const rawStep = view.duration / targetTicks;
    const magnitude = 10 ** Math.floor(Math.log10(Math.max(.001, rawStep)));
    const normalizedStep = rawStep / magnitude;
    const niceFactor = normalizedStep <= 1 ? 1 : normalizedStep <= 2 ? 2 : normalizedStep <= 5 ? 5 : 10;
    const tickStep = niceFactor * magnitude;
    const firstTick = Math.ceil((view.start - .0001) / tickStep) * tickStep;
    context.fillStyle = rulerTextColor;
    context.font = "600 12px ui-monospace, SFMono-Regular, Menlo, monospace";
    for (let tick = firstTick; tick <= view.end + .0001; tick += tickStep) {
      const cleanTick = Math.round(tick * 1000) / 1000;
      const x = (cleanTick - view.start) / view.duration * bounds.width;
      context.fillRect(Math.round(x), bounds.height - 13, 1, 4);
      const label = formatTime(cleanTick);
      const measured = context.measureText(label).width;
      context.fillText(label, Math.max(2, Math.min(bounds.width - measured - 2, x - measured / 2)), bounds.height - 2);
    }
  }
}

function updateTimelinePlayhead() {
  if (!timelinePanel && !$("#timelineCurrent") && !$("#timelinePlayhead")) return;
  const duration = timelineDurationValue();
  const value = Math.max(0, Math.min(duration || 0, timelineAbsoluteTime()));
  const view = timelineViewRange();
  const current = $("#timelineCurrent");
  const playhead = $("#timelinePlayhead");
  const overviewPlayhead = $("#timelineOverviewPlayhead");
  const locateButton = $("#timelineLocatePlayhead");
  const outsideView = duration > 0 && (value < view.start || value > view.end);
  if (current) {
    current.textContent = formatTime(value);
    if (viewerMediaKind === "output" && currentOutput) {
      const outputTime = Number(mainVideo.currentTime || 0);
      const mapped = compositionSourceFrameAtOutputTime(currentOutput, outputTime);
      const kind = currentOutputIsReviewSample(currentOutput) ? "样片" : "成片";
      current.title = mapped
        ? `${kind} ${formatTime(outputTime)} · 源片 ${formatTime(mapped.sourceTime)}`
        : `${kind} ${formatTime(outputTime)}`;
      current.setAttribute("aria-label", current.title);
    } else {
      current.removeAttribute("title");
      current.removeAttribute("aria-label");
    }
  }
  if (playhead) playhead.style.left = duration > 0 ? `${timelinePercentInView(value)}%` : "0%";
  playhead?.classList.toggle("hidden", outsideView);
  if (overviewPlayhead) overviewPlayhead.style.left = duration > 0 ? `${value / duration * 100}%` : "0%";
  locateButton?.classList.toggle("hidden", !outsideView);
  if (locateButton && outsideView) {
    locateButton.textContent = `回到播放头 ${formatTime(value)}`;
    locateButton.title = `播放位置 ${formatTime(value)} 不在当前浏览范围内`;
  }
  timelineViewport?.classList.toggle("playhead-outside", outsideView);
  const visibleFrames = [...document.querySelectorAll("#timelineThumbnails [data-timeline-frame-time]")];
  visibleFrames.forEach((frame) => frame.classList.remove("playback-current"));
  if (!outsideView && visibleFrames.length) {
    const nearest = visibleFrames.reduce((best, frame) =>
      Math.abs(Number(frame.dataset.timelineFrameTime) - value) < Math.abs(Number(best.dataset.timelineFrameTime) - value) ? frame : best,
    );
    nearest?.classList.add("playback-current");
  }
  const outputSchedule = viewerMediaKind === "output" && currentOutput?.segments?.length
    ? compositionSchedule(currentOutput)
    : [];
  const outputTime = Number(mainVideo.currentTime || 0);
  const playingEntry = [...outputSchedule].reverse().find((entry) =>
    outputTime >= entry.outputStart - .001 && outputTime <= entry.outputEnd + .001,
  ) || null;
  timelineTrackContent?.querySelectorAll("[data-output-segment-index]").forEach((element) => {
    element.classList.toggle("playback-active", Boolean(
      playingEntry && Number(element.dataset.outputSegmentIndex) === Number(playingEntry.index),
    ));
  });
  updateTimelineSpeakerPlayhead();
  updateTimelinePersonPlayhead();
  syncPortraitPrecisionWorkbench();
}

function updateTimelineSelection() {
  const duration = timelineDurationValue();
  const selection = $("#timelineSelection");
  const actions = $("#timelineSelectionActions");
  const summary = $("#timelineSelectionSummary");
  const confirmButton = $("#timelineSelectionConfirm");
  const cancelButton = $("#timelineSelectionCancel");
  const startInput = $("#timelineSelectionStartInput");
  const endInput = $("#timelineSelectionEndInput");
  const previewButton = $("#timelineSelectionPreview");
  const composeButton = $("#timelineSelectionCompose");
  const contentToggle = $("#timelineContentToggleSelected");
  const contentEarlier = $("#timelineContentMoveEarlier");
  const contentLater = $("#timelineContentMoveLater");
  if (!selection) return;
  [contentToggle, contentEarlier, contentLater].forEach((button) => button?.classList.add("hidden"));
  if (timelineOutputAxisActive()) {
    selection.classList.add("hidden");
    actions?.classList.add("hidden");
    return;
  }
  const contentEdit = contentBoundaryTimelineEdit
    && String(contentBoundaryTimelineEdit.jobId || "") === String(currentJob?.id || "")
    && contentBoundaryTimelineEdit.editor?.isConnected
    ? contentBoundaryTimelineEdit : null;
  if (contentEdit) {
    const view = timelineViewRange();
    const start = Number(contentEdit.start) || 0;
    const end = Math.max(start, Number(contentEdit.end) || start);
    const visible = duration > 0 && end > view.start && start < view.end;
    selection.classList.toggle("hidden", !visible);
    selection.classList.add("manual", "content-boundary-edit", "boundary-editable");
    selection.classList.remove("readonly");
    if (visible) {
      selection.style.left = `${timelinePercentInView(start)}%`;
      selection.style.width = `${Math.max(0, end - start) / view.duration * 100}%`;
    }
    actions?.classList.remove("hidden", "selection-confirmed");
    if (summary) summary.textContent = `修剪「${contentEdit.match?.title || "内容片段"}」· ${formatTime(start)} → ${formatTime(end)} · ${(end - start).toFixed(2)} 秒`;
    if (startInput) {
      startInput.disabled = false;
      startInput.step = String(1 / contentBoundaryFrameRate(contentEdit.scopedJob));
      if (document.activeElement !== startInput) startInput.value = start.toFixed(3);
    }
    if (endInput) {
      endInput.disabled = false;
      endInput.step = String(1 / contentBoundaryFrameRate(contentEdit.scopedJob));
      if (document.activeElement !== endInput) endInput.value = end.toFixed(3);
    }
    if (previewButton) previewButton.textContent = "预览修剪";
    if (confirmButton) {
      confirmButton.disabled = false;
      confirmButton.textContent = "保存边界";
    }
    if (cancelButton) {
      cancelButton.disabled = false;
      cancelButton.textContent = "关闭编辑";
    }
    composeButton?.classList.add("hidden");
    syncContentTimelineEditActions(contentEdit.match);
    return;
  }
  selection.classList.remove("content-boundary-edit");
  if (previewButton) previewButton.textContent = "预览";
  if (confirmButton) confirmButton.textContent = "确认选区";
  if (cancelButton) cancelButton.textContent = "取消";
  const contentManualRange = precisionTimelineKind() === "content"
    && String(currentJob?.status || "") === "awaiting_content_confirmation";
  composeButton?.classList.toggle("hidden", contentManualRange);
  const manualSelection = currentJob?.manualSelection;
  const manualEvent = currentEventGroup?.assemblyStrategy === "manual";
  const manualSourceItem = manualSelection || (currentCandidate?.manual ? currentCandidate : null) || (manualEvent ? currentEventSegment : null) || (currentEventSegment?.manual ? currentEventSegment : null);
  const item = currentEventSegment || currentCandidate || (!currentOutput?.segments?.length ? currentOutput : null) || manualSelection;
  const manual = Boolean(manualSourceItem && !currentOutput && (!currentEventSegment || currentEventSegment.manual || manualEvent) && (!currentCandidate || currentCandidate.manual));
  const view = timelineViewRange();
  // Normal event and shot selection is already expressed inside its own
  // track. Reserve the cross-track overlay for an editable manual range so
  // event state never washes over thumbnails or the audio waveform.
  if (!manual || !item || duration <= 0 || Number(item.end) <= view.start || Number(item.start) >= view.end) {
    selection.classList.add("hidden");
    actions?.classList.add("hidden");
    return;
  }
  selection.classList.remove("hidden");
  selection.classList.toggle("manual", Boolean(manualSelection && !currentEventSegment && !currentCandidate && !currentOutput));
  selection.style.left = `${timelinePercentInView(item.start)}%`;
  selection.style.width = `${Math.max(0, Number(item.end) - Number(item.start)) / view.duration * 100}%`;
  const manualBoundaryEditable = Boolean(manual && pendingTimelineSelection && ["awaiting_confirmation", "awaiting_content_confirmation", "completed"].includes(currentJob?.status));
  selection.classList.toggle("readonly", !manualBoundaryEditable);
  selection.classList.toggle("boundary-editable", manualBoundaryEditable);
  if (manual) {
    // Manual source selections are input state, but remain visible so the
    // user can inspect and fine-tune the exact range before confirming it.
    selection.classList.remove("hidden");
    selection.classList.add("manual");
    actions?.classList.remove("hidden");
    actions?.classList.toggle("selection-confirmed", !pendingTimelineSelection);
    if (confirmButton) {
      confirmButton.disabled = !pendingTimelineSelection;
      confirmButton.textContent = contentManualRange ? "加入当前探索结果" : "确认选区";
    }
    if (cancelButton) cancelButton.disabled = !pendingTimelineSelection;
    if (startInput) startInput.disabled = !pendingTimelineSelection;
    if (endInput) endInput.disabled = !pendingTimelineSelection;
    if (startInput && document.activeElement !== startInput) startInput.value = Number(manualSourceItem.start).toFixed(2);
    if (endInput && document.activeElement !== endInput) endInput.value = Number(manualSourceItem.end).toFixed(2);
    if (summary) summary.textContent = `${pendingTimelineSelection ? (contentManualRange ? "待加入结果 · " : "待确认 · ") : ""}${formatTime(manualSourceItem.start)} → ${formatTime(manualSourceItem.end)} · ${Number(manualSourceItem.end - manualSourceItem.start).toFixed(1)} 秒`;
    return;
  }
  actions?.classList.toggle("hidden", !manual);
  if (manual && summary) summary.textContent = `${formatTime(manualSelection.start)} → ${formatTime(manualSelection.end)} · ${Number(manualSelection.end - manualSelection.start).toFixed(1)} 秒`;
}

function setTimelineView(start, end, { keepReviewFocus = false } = {}) {
  const duration = timelineDurationValue();
  if (duration <= 0) return;
  if (!keepReviewFocus) timelineReviewFollow = false;
  const minimum = Math.min(2, duration);
  let span = Math.max(minimum, Math.min(duration, Number(end) - Number(start)));
  let nextStart = Math.max(0, Math.min(duration - span, Number(start)));
  timelineViewStart = nextStart;
  timelineViewEnd = nextStart + span;
  updateTimeline();
}

function zoomTimeline(factor, center = timelineAbsoluteTime()) {
  const view = timelineViewRange();
  const duration = timelineDurationValue();
  const span = Math.max(2, Math.min(duration, view.duration * factor));
  const ratio = Math.max(0, Math.min(1, (center - view.start) / view.duration));
  setTimelineView(center - span * ratio, center + span * (1 - ratio));
}

function timelineCanPan() {
  const duration = timelineDurationValue();
  const view = timelineViewRange();
  return duration > 0 && view.duration < duration - .01;
}

function applyPendingTimelinePan() {
  timelinePanFrame = null;
  const drag = timelinePanDrag;
  if (!drag || drag.pendingStart == null) return;
  const nextStart = drag.pendingStart;
  drag.pendingStart = null;
  setTimelineView(nextStart, nextStart + drag.viewDuration);
}

function moveTimelinePan(event) {
  const drag = timelinePanDrag;
  if (!drag) return;
  const delta = event.clientX - drag.startX;
  if (Math.abs(delta) >= 4) drag.moved = true;
  if (!drag.moved) return;
  event.preventDefault();
  const shift = -delta / Math.max(1, drag.width) * drag.viewDuration;
  drag.pendingStart = drag.viewStart + shift;
  timelineSpaceDidPan = timelineSpaceDidPan || drag.spaceMode;
  if (timelinePanFrame === null) timelinePanFrame = requestAnimationFrame(applyPendingTimelinePan);
}

function finishTimelinePan(event, cancelled = false) {
  const drag = timelinePanDrag;
  if (!drag) return;
  if (timelinePanFrame !== null) {
    cancelAnimationFrame(timelinePanFrame);
    timelinePanFrame = null;
    applyPendingTimelinePan();
  }
  timelinePanDrag = null;
  document.removeEventListener("pointermove", moveTimelinePan);
  document.removeEventListener("pointerup", finishTimelinePan);
  document.removeEventListener("pointercancel", cancelTimelinePan);
  timelineViewport?.classList.remove("panning");
  document.body.classList.remove("timeline-panning");
  if (drag.moved) timelineSuppressClickUntil = Date.now() + 300;
  if (!cancelled && !drag.moved && drag.allowSeek && event) activateTimelinePoint(event);
}

function cancelTimelinePan(event) {
  finishTimelinePan(event, true);
}

function beginTimelinePan(event, { allowSeek = false, spaceMode = false } = {}) {
  if (!timelineViewport || !currentJob || ![0, 1].includes(event.button)) return;
  const trackContent = timelineTrackContent || timelineViewport;
  const bounds = trackContent.getBoundingClientRect();
  const view = timelineViewRange();
  event.preventDefault();
  timelinePanDrag = {
    startX: event.clientX,
    width: Math.max(1, bounds.width),
    viewStart: view.start,
    viewDuration: view.duration,
    pendingStart: null,
    moved: false,
    allowSeek,
    spaceMode,
  };
  timelineViewport.classList.add("panning");
  document.body.classList.add("timeline-panning");
  document.addEventListener("pointermove", moveTimelinePan);
  document.addEventListener("pointerup", finishTimelinePan);
  document.addEventListener("pointercancel", cancelTimelinePan);
}

function moveTimelineOverview(event) {
  const drag = timelineOverviewDrag;
  if (!drag) return;
  const delta = event.clientX - drag.startX;
  if (Math.abs(delta) >= 3) drag.moved = true;
  if (!drag.moved) return;
  event.preventDefault();
  const shift = delta / Math.max(1, drag.width) * drag.totalDuration;
  setTimelineView(drag.viewStart + shift, drag.viewStart + shift + drag.viewDuration);
}

function finishTimelineOverview(event, cancelled = false) {
  const drag = timelineOverviewDrag;
  if (!drag) return;
  timelineOverviewDrag = null;
  document.removeEventListener("pointermove", moveTimelineOverview);
  document.removeEventListener("pointerup", finishTimelineOverview);
  document.removeEventListener("pointercancel", cancelTimelineOverview);
  $("#timelineOverview")?.classList.remove("dragging");
  document.body.classList.remove("timeline-panning");
  if (cancelled || drag.moved || drag.startedOnWindow || !event) return;
  const center = Math.max(0, Math.min(1, (event.clientX - drag.left) / Math.max(1, drag.width))) * drag.totalDuration;
  setTimelineView(center - drag.viewDuration / 2, center + drag.viewDuration / 2);
}

function cancelTimelineOverview(event) {
  finishTimelineOverview(event, true);
}

function beginTimelineOverview(event) {
  if (event.button !== 0 || !timelineCanPan()) return;
  const overview = event.currentTarget;
  const bounds = overview.getBoundingClientRect();
  const view = timelineViewRange();
  const windowElement = $("#timelineViewWindow");
  const startedOnWindow = Boolean(event.target === windowElement || event.target.closest?.("#timelineViewWindow"));
  event.preventDefault();
  if (!startedOnWindow) {
    const center = Math.max(0, Math.min(1, (event.clientX - bounds.left) / Math.max(1, bounds.width))) * timelineDurationValue();
    setTimelineView(center - view.duration / 2, center + view.duration / 2);
  }
  const nextView = timelineViewRange();
  timelineOverviewDrag = {
    startX: event.clientX,
    left: bounds.left,
    width: Math.max(1, bounds.width),
    totalDuration: timelineDurationValue(),
    viewStart: nextView.start,
    viewDuration: nextView.duration,
    startedOnWindow,
    moved: false,
  };
  overview.classList.add("dragging");
  document.body.classList.add("timeline-panning");
  document.addEventListener("pointermove", moveTimelineOverview);
  document.addEventListener("pointerup", finishTimelineOverview);
  document.addEventListener("pointercancel", cancelTimelineOverview);
}

function currentTimelineReviewRange() {
  if (currentEventSegment) {
    return {
      start: Number(currentEventSegment.start) || 0,
      end: Number(currentEventSegment.end) || Number(currentEventSegment.start) || 0,
    };
  }
  const segments = currentEventGroup?.segments || [];
  if (segments.length) {
    const intervals = timelineMergedIntervals(currentEventGroup);
    const fullStart = intervals[0]?.start ?? Math.min(...segments.map((item) => Number(item.start) || 0));
    const fullEnd = intervals.at(-1)?.end ?? Math.max(...segments.map((item) => Number(item.end) || 0));
    const contentDuration = timelineIntervalDuration(intervals);
    // A single viewport cannot truthfully represent shots separated by a long
    // empty gap. Focus the interval nearest the current composed-event play
    // position; the overview strip still shows every source interval.
    if (intervals.length > 1 && fullEnd - fullStart > Math.max(120, contentDuration * 4)) {
      const anchor = timelineAbsoluteTime();
      return intervals.reduce((nearest, interval) => {
        const distance = anchor < interval.start ? interval.start - anchor : anchor > interval.end ? anchor - interval.end : 0;
        return !nearest || distance < nearest.distance ? { ...interval, distance } : nearest;
      }, null);
    }
    return {
      start: fullStart,
      end: fullEnd,
    };
  }
  if (currentCandidate) {
    return { start: Number(currentCandidate.start) || 0, end: Number(currentCandidate.end) || Number(currentCandidate.start) || 0 };
  }
  if (currentOutput?.segments?.length) {
    const intervals = timelineMergedIntervals(currentOutput);
    const fullStart = intervals[0]?.start ?? 0;
    const fullEnd = intervals.at(-1)?.end ?? fullStart;
    const contentDuration = timelineIntervalDuration(intervals);
    // A composed video can jump between distant source ranges. When those
    // ranges cannot be read together at a useful scale, focus the source
    // interval that corresponds to the segment currently playing.
    if (intervals.length > 1 && fullEnd - fullStart > Math.max(120, contentDuration * 4)) {
      const anchor = timelineAbsoluteTime();
      return intervals.reduce((nearest, interval) => {
        const distance = anchor < interval.start ? interval.start - anchor : anchor > interval.end ? anchor - interval.end : 0;
        return !nearest || distance < nearest.distance ? { ...interval, distance } : nearest;
      }, null);
    }
    return { start: fullStart, end: fullEnd };
  }
  return null;
}

function isManualTimelineEventGroup(group) {
  return group?.assemblyStrategy === "manual"
    || /时间轴选区高光|用户从时间轴创建/.test(String(group?.title || ""))
    || /用户从时间轴创建/.test(String(group?.summary || ""));
}

function timelineMergedIntervals(group) {
  const ranges = (group?.segments || [])
    .map((segment) => segmentSourceRange(segment))
    .filter((range) => range.end > range.start)
    .sort((left, right) => left.start - right.start);
  return ranges.reduce((merged, range) => {
    const previous = merged.at(-1);
    if (!previous || range.start > previous.end + .05) merged.push({ ...range });
    else previous.end = Math.max(previous.end, range.end);
    return merged;
  }, []);
}

function timelineIntervalDuration(intervals) {
  return intervals.reduce((sum, range) => sum + Math.max(0, range.end - range.start), 0);
}

function timelineIntervalIntersection(left, right) {
  let total = 0;
  let leftIndex = 0;
  let rightIndex = 0;
  while (leftIndex < left.length && rightIndex < right.length) {
    const overlap = Math.min(left[leftIndex].end, right[rightIndex].end) - Math.max(left[leftIndex].start, right[rightIndex].start);
    if (overlap > 0) total += overlap;
    if (left[leftIndex].end <= right[rightIndex].end) leftIndex += 1;
    else rightIndex += 1;
  }
  return total;
}

function timelineTitleTokens(value) {
  const normalized = String(value || "").toLowerCase().replace(/[\s\p{P}\p{S}]+/gu, "");
  if (!normalized) return new Set();
  if (normalized.length < 2) return new Set([normalized]);
  return new Set(Array.from({ length: normalized.length - 1 }, (_, index) => normalized.slice(index, index + 2)));
}

function timelineTitleSimilarity(left, right) {
  const leftTokens = timelineTitleTokens(left);
  const rightTokens = timelineTitleTokens(right);
  if (!leftTokens.size || !rightTokens.size) return 0;
  let intersection = 0;
  leftTokens.forEach((token) => { if (rightTokens.has(token)) intersection += 1; });
  return intersection / Math.max(1, new Set([...leftTokens, ...rightTokens]).size);
}

function timelineGroupsAreDisplayDuplicates(left, right) {
  const leftIntervals = timelineMergedIntervals(left);
  const rightIntervals = timelineMergedIntervals(right);
  const leftDuration = timelineIntervalDuration(leftIntervals);
  const rightDuration = timelineIntervalDuration(rightIntervals);
  if (!leftDuration || !rightDuration) return false;
  const intersection = timelineIntervalIntersection(leftIntervals, rightIntervals);
  const leftCoverage = intersection / leftDuration;
  const rightCoverage = intersection / rightDuration;
  const durationBalance = Math.min(leftDuration, rightDuration) / Math.max(leftDuration, rightDuration);

  // A short event can be fully contained in a longer story event while the
  // longer event still owns several unique shots. Treating containment as a
  // duplicate hides those shots and makes the timeline disagree with its
  // event/shot summary. Collapse only when both groups cover essentially the
  // same source time, with a looser path for near-identical titled analyses.
  if (leftCoverage >= .96 && rightCoverage >= .96) return true;
  return leftCoverage >= .82
    && rightCoverage >= .82
    && durationBalance >= .72
    && timelineTitleSimilarity(left?.title, right?.title) >= .22;
}

function timelineRangesOverlap(left, right) {
  const leftRange = segmentSourceRange(left);
  const rightRange = segmentSourceRange(right);
  const start = Math.max(leftRange.start, rightRange.start);
  const end = Math.min(leftRange.end, rightRange.end);
  return Math.max(0, end - start);
}

function timelineOutputUsesGroup(output, group) {
  if (!output?.segments?.length || !group) return false;
  const groupId = String(group.id || "");
  const groupSegments = Array.isArray(group.segments) ? group.segments : [];
  const groupSegmentIds = new Set(groupSegments.flatMap((segment) =>
    [segment.id, segment.candidateId].filter(Boolean).map(String),
  ));
  const candidateIndices = new Set(groupSegments
    .map((segment) => Number(segment.candidateIndex ?? segment.index))
    .filter(Number.isFinite));
  const groupTitle = String(group.title || "");
  return output.segments.some((segment) => {
    const explicitGroupIds = [segment.groupId, segment.chapterId, segment.eventGroupId]
      .filter(Boolean).map(String);
    if (groupId && explicitGroupIds.includes(groupId)) return true;
    if ([segment.id, segment.candidateId].filter(Boolean).map(String).some((id) => groupSegmentIds.has(id))) return true;
    const candidateIndex = Number(segment.candidateIndex ?? segment.index);
    if (Number.isFinite(candidateIndex) && candidateIndices.has(candidateIndex)) return true;
    const segmentRange = segmentSourceRange(segment);
    const segmentDuration = Math.max(.001, segmentRange.end - segmentRange.start);
    const overlap = groupSegments.reduce((maximum, sourceSegment) =>
      Math.max(maximum, timelineRangesOverlap(segment, sourceSegment)), 0);
    if (overlap / segmentDuration >= .45) return true;
    return Boolean(groupTitle && segment.chapterTitle
      && timelineTitleSimilarity(groupTitle, segment.chapterTitle) >= .58);
  });
}

function timelineOutputComparisonModel(job = currentJob, output = currentOutput) {
  if (!job || !output) return null;
  const eventDisplay = timelineEventDisplayModel(job);
  const usedGroupIds = new Set(eventDisplay.entries
    .filter((entry) => entry.groups.some((group) => timelineOutputUsesGroup(output, group)))
    .map((entry) => String(entry.group.id)));
  const composedSchedule = compositionSchedule(output);
  const outputDuration = Math.max(0, timelineOutputDurationValue(output));
  const fallbackSourceStart = Number(output.start);
  const fallbackSourceEnd = Number(output.end);
  const schedule = composedSchedule.length ? composedSchedule : outputDuration > 0 ? [{
    segment: {
      id: `output-${String(output.filename || "version")}`,
      title: cleanDisplayText(output.displayTitle) || cleanDisplayText(output.title, "生成版本"),
      role: "完整版本",
      start: Number.isFinite(fallbackSourceStart) ? fallbackSourceStart : 0,
      end: Number.isFinite(fallbackSourceEnd) ? fallbackSourceEnd : 0,
    },
    index: 0,
    sourceStart: Number.isFinite(fallbackSourceStart) ? fallbackSourceStart : 0,
    sourceEnd: Number.isFinite(fallbackSourceEnd) ? fallbackSourceEnd : 0,
    sourceDuration: Number.isFinite(fallbackSourceStart) && Number.isFinite(fallbackSourceEnd)
      ? Math.max(0, fallbackSourceEnd - fallbackSourceStart) : 0,
    outputDuration,
    overlap: 0,
    outputStart: 0,
    outputEnd: outputDuration,
    fallback: true,
  }] : [];
  const outputSegments = schedule.map((entry) => {
    const event = timelineOutputSegmentEvent(entry.segment, entry.index, eventDisplay, output);
    return {
      ...entry.segment,
      // The rendered version owns this axis. Preserve source coordinates as
      // metadata for the explicit "查看源片" action only.
      start: entry.outputStart,
      end: entry.outputEnd,
      sourceStart: entry.sourceStart,
      sourceEnd: entry.sourceEnd,
      _eventGroupId: event.id,
      _eventTitle: event.title,
      _shotTitle: String(entry.segment.shotTitle || normalizedTimelineShotRole(entry.segment)),
      _output: output,
      _outputSegmentIndex: entry.index,
      _compositionOrder: entry.index + 1,
      _outputStart: entry.outputStart,
      _outputEnd: entry.outputEnd,
      filename: output.filename,
    };
  });
  const leadInDuration = Math.max(0, Number(schedule[0]?.outputStart || output.coverIntro?.duration || output.leadInDuration || 0));
  const timelineSegments = leadInDuration > .001 ? [{
    id: `output-intro-${String(output.filename || "version")}`,
    title: "封面片头",
    role: "片头",
    start: 0,
    end: leadInDuration,
    sourceStart: null,
    sourceEnd: null,
    _shotTitle: "封面片头",
    _output: output,
    _outputIntro: true,
    _outputSegmentIndex: -1,
    _compositionOrder: 0,
    _outputStart: 0,
    _outputEnd: leadInDuration,
    filename: output.filename,
  }, ...outputSegments] : outputSegments;
  const eventBuckets = new Map();
  outputSegments.forEach((segment) => {
    const key = String(segment._eventGroupId || `segment-${segment._outputSegmentIndex}`);
    if (!eventBuckets.has(key)) eventBuckets.set(key, {
      id: key,
      title: String(segment._eventTitle || segment.chapterTitle || segment.title || "成片片段"),
      segments: [],
    });
    eventBuckets.get(key).segments.push(segment);
  });
  const outputEvents = [...eventBuckets.values()].map((group, eventIndex) => {
    const start = Math.min(...group.segments.map((segment) => Number(segment.start) || 0));
    const end = Math.max(...group.segments.map((segment) => Number(segment.end) || start));
    const first = group.segments[0];
    const syntheticGroup = { ...group, start, end, summary: "当前版本中的连续成片段落" };
    return {
      ...first,
      id: `output-event-${group.id}`,
      start,
      end,
      _group: syntheticGroup,
      _output: null,
      _outputEvent: output,
      _outputEventOrder: eventIndex + 1,
      _outputSegmentIndex: Number(first?._outputSegmentIndex || 0),
      _comparisonSource: false,
      _usedInOutput: true,
    };
  });
  return { eventDisplay, usedGroupIds, schedule, outputSegments: timelineSegments, outputEvents };
}

function timelineGenericSignalTitle(value) {
  const title = String(value || "").replace(/\s+/g, "").trim();
  if (!title) return true;
  if ([
    "画面变化热点", "声音能量热点", "声音变化热点", "视觉变化热点",
    "画面变化", "视觉变化", "声音变化", "音频变化", "声音峰值", "音频峰值",
    "视觉转折", "声音高潮", "视听高潮", "热点事件", "视觉高光", "高光片段",
    "精彩片段", "候选片段", "精彩镜头",
  ].includes(title)) return true;
  return /^(?:画面|视觉|声音|音频|能量|视听)(?:变化|峰值|转折|高潮|热点|变化热点|能量热点)$/.test(title);
}

function timelineOutputSegmentEvent(segment, segmentIndex, eventDisplay, output) {
  const explicitIds = [segment.eventGroupId, segment.groupId, segment.chapterId]
    .filter(Boolean).map(String);
  const explicitGroup = eventDisplay.entries.find((entry) =>
    entry.groups.some((group) => explicitIds.includes(String(group.id))),
  )?.group;
  const inferredGroup = explicitGroup || eventDisplay.entries.find((entry) =>
    entry.groups.some((group) => timelineOutputUsesGroup({ segments: [segment] }, group)),
  )?.group;
  if (inferredGroup && !timelineGenericSignalTitle(inferredGroup.title)) {
    return { id: String(inferredGroup.id), title: String(inferredGroup.title) };
  }
  const timelineEvent = (output?.timelineEvents || []).find((event) =>
    (event.segmentIndices || []).map(Number).includes(Number(segmentIndex))
    || explicitIds.includes(String(event.eventGroupId || event.id || "")),
  );
  if (timelineEvent && !timelineGenericSignalTitle(timelineEvent.title)) {
    return {
      id: String(timelineEvent.eventGroupId || timelineEvent.id || `output_event_${segmentIndex}`),
      title: String(timelineEvent.title),
    };
  }
  const directTitle = String(segment.eventTitle || segment.chapterTitle || "");
  if (!timelineGenericSignalTitle(directTitle)) {
    return { id: String(segment.eventGroupId || segment.groupId || segment.chapterId || `output_event_${segmentIndex}`), title: directTitle };
  }
  return {
    id: `legacy_unclassified_${segment.candidateId || segment.id || segmentIndex}`,
    title: `历史镜头 ${String(segmentIndex + 1).padStart(2, "0")}（待重新分析）`,
  };
}

function timelineEventDisplayModel(job = currentJob) {
  const recommendedIds = new Set((job?.recommendedGroupIds || []).map(String));
  const sourceGroups = (job?.eventGroups || [])
    .filter((group) => !isManualTimelineEventGroup(group))
    .sort((left, right) => {
      const leftStart = timelineMergedIntervals(left)[0]?.start ?? (Number(left.start) || 0);
      const rightStart = timelineMergedIntervals(right)[0]?.start ?? (Number(right.start) || 0);
      return leftStart - rightStart;
    });
  const buckets = [];
  sourceGroups.forEach((group) => {
    const bucket = buckets.find((candidate) => candidate.groups.some((item) => timelineGroupsAreDisplayDuplicates(item, group)));
    if (bucket) bucket.groups.push(group);
    else buckets.push({ groups: [group] });
  });
  const entries = buckets.map((bucket) => {
    const groups = [...bucket.groups].sort((left, right) => {
      const recommendation = Number(recommendedIds.has(String(right.id))) - Number(recommendedIds.has(String(left.id)));
      if (recommendation) return recommendation;
      const score = Number(right.score || 0) - Number(left.score || 0);
      if (score) return score;
      return Number(right.segments?.length || 0) - Number(left.segments?.length || 0);
    });
    const group = groups[0];
    return {
      group,
      groups,
      aliasIds: new Set(groups.map((item) => String(item.id))),
      recommended: groups.some((item) => recommendedIds.has(String(item.id))),
      duplicateCount: Math.max(0, groups.length - 1),
    };
  }).sort((left, right) => (timelineMergedIntervals(left.group)[0]?.start || 0) - (timelineMergedIntervals(right.group)[0]?.start || 0));
  const aliasToCanonical = new Map();
  entries.forEach((entry) => entry.aliasIds.forEach((id) => aliasToCanonical.set(id, String(entry.group.id))));
  return {
    entries,
    groups: entries.map((entry) => entry.group),
    aliasToCanonical,
    recommendedIds: new Set(entries.filter((entry) => entry.recommended).map((entry) => String(entry.group.id))),
    duplicateCount: entries.reduce((sum, entry) => sum + entry.duplicateCount, 0),
  };
}

function normalizedTimelineShotRole(item) {
  const raw = String(item?.role || "").trim();
  const normalized = raw.toLowerCase();
  const priorities = [
    [/高潮|climax/, "高潮"],
    [/反应|reaction/, "反应"],
    [/结果|结尾|收束|result|ending/, "结果"],
    [/发展|development/, "发展"],
    [/铺垫|上下文|context/, "铺垫"],
    [/建立|开场|核心镜头|opening|hook/, "建立"],
  ];
  return priorities.find(([pattern]) => pattern.test(normalized))?.[1] || "镜头";
}

function timelineEventSequenceNumber(group) {
  if (!group) return 0;
  const display = timelineEventDisplayModel(currentJob);
  const canonicalId = display.aliasToCanonical.get(String(group.id)) || String(group.id);
  return display.entries.findIndex((entry) => String(entry.group.id) === canonicalId) + 1;
}

function timelinePresentationModel(job = currentJob, outputComparison = null) {
  const contentMode = String(job?.taskMode || "") === "content_extract";
  const speakerMode = workflowKindForJob(job) === "speaker_edit";
  const personMode = workflowKindForJob(job) === "person_edit";
  const comparingOutput = Boolean(outputComparison);
  const reviewingOutput = Boolean(viewerMediaKind === "output" && currentOutput);
  const reviewingOutputSource = Boolean(reviewingOutput && timelineCoordinateSpace === "source");
  if (comparingOutput || (reviewingOutput && timelineCoordinateSpace !== "source")) {
    const reviewSample = reviewingOutput && currentOutputIsReviewSample(currentOutput);
    const outputTitle = reviewSample ? "审核样片时间轴" : "成片版本时间轴";
    const outputHint = reviewSample
      ? "按审核样片的最终播放顺序显示；点击片段继续播放样片"
      : "按成片版本的最终播放顺序显示；点击片段继续播放当前版本";
    if (speakerMode) {
      return {
        contentMode: true,
        layoutKind: "content-review",
        title: outputTitle,
        trackLabels: ["成片发言", "画面", "音频"],
        hint: outputHint,
        emptyTitle: "选择已采用发言",
        emptyReason: "点击发言片段可从对应位置继续播放当前版本。",
        ariaLabel: reviewSample ? "审核样片播放时间线" : "成片版本播放时间线",
      };
    }
    if (personMode) {
      return {
        contentMode: true,
        layoutKind: "content-review",
        title: outputTitle,
        trackLabels: ["成片出镜", "画面", "音频"],
        hint: outputHint,
        emptyTitle: "选择已采用出镜片段",
        emptyReason: "点击人物出镜片段可从对应位置继续播放当前版本。",
        ariaLabel: reviewSample ? "审核样片播放时间线" : "成片版本播放时间线",
      };
    }
    return contentMode ? {
      contentMode: true,
      layoutKind: "content-review",
      title: outputTitle,
      trackLabels: [reviewSample ? "样片片段" : "成片片段", "画面", "音频"],
      hint: outputHint,
      emptyTitle: "选择已采用片段",
      emptyReason: "点击片段可从对应位置继续播放当前版本。",
      ariaLabel: reviewSample ? "审核样片播放时间线" : "成片版本播放时间线",
    } : {
      contentMode: false,
      layoutKind: "hierarchy",
      title: outputTitle,
      trackLabels: ["事件", "镜头", "画面", "音频"],
      hint: outputHint,
      emptyTitle: "选择成片事件或镜头",
      emptyReason: "点击事件或镜头可从对应位置继续播放当前版本。",
      ariaLabel: reviewSample ? "审核样片播放时间线" : "成片版本播放时间线",
    };
  }
  if (!contentMode) {
    const hasSemanticHierarchy = Boolean(
      (job?.eventGroups || []).some((group) => Array.isArray(group?.segments) && group.segments.length)
      || (job?.candidates || []).length,
    );
    if (!hasSemanticHierarchy) {
      return {
        contentMode: false,
        layoutKind: "source-tracks",
        title: "源视频时间线",
        trackLabels: ["画面", "音频"],
        hint: "提交剪辑要求后，事件和镜头会在这里展开",
        emptyTitle: "等待剪辑要求",
        emptyReason: "当前先展示源视频画面和音频；提交要求后再增加实际存在的分析轨道。",
        ariaLabel: "源视频时间线",
      };
    }
    return {
      contentMode: false,
      layoutKind: "hierarchy",
      title: reviewingOutputSource ? "成片来源时间线" : "智能剪辑时间线",
      trackLabels: ["事件", "镜头", "画面", "音频"],
      hint: "点击事件查看内容说明",
      emptyTitle: "选择事件或镜头",
      emptyReason: "点击下方时间轴中的事件或镜头，在这里查看内容说明、时间范围和判断依据。",
      ariaLabel: "视频审核时间轴",
    };
  }
  if (speakerMode) {
    return {
      contentMode: true,
      layoutKind: "source-tracks",
      title: "说话人时间线",
      trackLabels: ["画面", "音频"],
      hint: "完整发言按说话人显示在下方独立轨道；点击发言可试听并定位源视频",
      emptyTitle: "选择说话人或发言片段",
      emptyReason: "点击说话人轨道中的发言，在这里查看时间范围和转写内容。",
      ariaLabel: "说话人发言时间线",
    };
  }
  if (personMode) {
    return {
      contentMode: true,
      layoutKind: "source-tracks",
      title: "人物出镜时间线",
      trackLabels: ["画面", "音频"],
      hint: "完整出镜按人物显示在下方独立轨道；点击片段可定位源视频",
      emptyTitle: "选择人物或出镜片段",
      emptyReason: "点击人物轨道中的出镜片段，在这里查看时间范围和画面证据。",
      ariaLabel: "人物出镜时间线",
    };
  }
  const activeSearchId = String(job?.contentSearchSession?.activeSearchId || job?.contentSearch?.id || "");
  const hasRetainedSearchResults = contentSearchRecordsForJob(job).some((record) => {
    const retainedCount = Math.max(
      Number(record?.candidateCount || 0),
      Number(record?.candidates?.length || 0),
      Number(record?.timelineCandidates?.length || 0),
    );
    return String(record?.id || "") !== activeSearchId && retainedCount > 0;
  });
  return {
    contentMode: true,
    layoutKind: hasRetainedSearchResults ? "content-review-history" : "content-review",
    title: reviewingOutputSource ? "内容视频来源时间线" : "内容检索时间轴",
    trackLabels: hasRetainedSearchResults
      ? ["当前检索", "已保留", "画面", "音频"]
      : ["匹配片段", "画面", "音频"],
    hint: hasRetainedSearchResults
      ? "当前结果与以前保留的结果分轨显示；点击片段查看对应检索与证据"
      : "点击匹配片段查看内容与证据",
    emptyTitle: "选择匹配片段",
    emptyReason: "点击下方时间轴中的匹配片段，在这里查看时间范围、命中内容和判断证据。",
    ariaLabel: "内容检索时间轴",
  };
}

function applyTimelinePresentation(presentation) {
  const title = $("#timelineTitle");
  const labels = $("#timelineTrackLabels");
  if (title) title.textContent = presentation.title;
  const trackLabels = Array.isArray(presentation.trackLabels) ? presentation.trackLabels : [];
  if (labels) labels.innerHTML = trackLabels.map((label) => {
    const kind = label.includes("音频")
      ? "audio"
      : label.includes("字幕")
        ? "subtitle"
        : label.includes("音乐")
          ? "music"
          : label.includes("画面")
            ? "video"
            : "content";
    return `<span data-track-kind="${kind}">${escapeHtml(label)}</span>`;
  }).join("");
  if (timelinePanel) timelinePanel.setAttribute("aria-label", presentation.ariaLabel);
  if (timelineViewport) timelineViewport.dataset.trackLayout = presentation.layoutKind;
  if ($("#timelineHint")) $("#timelineHint").textContent = presentation.hint;
  if ($("#evidencePanel")?.classList.contains("evidence-placeholder")) {
    if ($("#clipTitle")) $("#clipTitle").textContent = presentation.emptyTitle;
    if ($("#clipReason")) $("#clipReason").textContent = presentation.emptyReason;
  }
}

function renderTimelineEventSummary() {
  const root = $("#timelineEventSummary");
  const titleNode = $("#timelineEventSummaryTitle");
  const textNode = $("#timelineEventSummaryText");
  const timeNode = $("#timelineEventSummaryTime");
  const typeNode = $("#timelineEventSummaryType");
  const focusButton = $("#timelineFocusReview");
  if (!root || !titleNode || !textNode || !timeNode || !typeNode) return;

  let title = "";
  let detail = "";
  let start = 0;
  let end = 0;
  let type = "当前事件";
  const currentEventNumber = timelineEventSequenceNumber(currentEventGroup);
  const contentMode = String(currentJob?.taskMode || "") === "content_extract";
  const groupPrefix = contentMode ? "片段 P" : "事件 E";
  const currentEventRecommended = currentEventGroup
    ? (currentJob?.recommendedGroupIds || []).map(String).includes(String(currentEventGroup.id))
    : false;
  if (currentOutput && viewerMediaKind === "output") {
    const schedule = timelineOutputComparisonModel(currentJob, currentOutput)?.schedule || [];
    const located = locateJobOutput(currentOutput.filename);
    const presentation = located ? autoVersionPresentation(currentJob, located.version) : {};
    title = cleanDisplayText(currentOutput.displayTitle)
      || cleanDisplayText(presentation.displayName)
      || cleanDisplayText(currentOutput.title, contentMode ? "内容视频" : "高光成片");
    detail = schedule.map((entry, index) =>
      `${String(index + 1).padStart(2, "0")} ${contentMode ? String(entry.segment.chapterTitle || entry.segment.title || "匹配片段") : normalizedTimelineShotRole(entry.segment)}`,
    ).join(" → ");
    start = 0;
    end = timelineOutputDurationValue(currentOutput);
    type = `${presentation.sourceLabel || (contentMode ? "内容视频" : "AI 成片")} · 播放时间轴`;
  } else if (currentEventSegment && currentEventGroup) {
    const role = String(currentEventSegment.role || "精彩镜头");
    title = `${currentEventGroup.title} · ${role}`;
    detail = String(currentEventSegment.reason || currentEventGroup.summary || "该事件中的精彩镜头");
    start = Number(currentEventSegment.start) || 0;
    end = Number(currentEventSegment.end) || start;
    type = `${currentEventNumber ? `${groupPrefix}${currentEventNumber} · ` : ""}${contentMode ? "当前内容片段" : "当前镜头"}${currentEventRecommended && !contentMode ? " · AI 推荐" : ""}`;
  } else if (currentEventGroup) {
    title = String(currentEventGroup.title || (contentMode ? "匹配片段" : "精彩事件"));
    detail = String(currentEventGroup.summary || currentEventGroup.reason || (contentMode ? "点击片段可继续查看对应依据" : "点击事件镜头可继续查看对应依据"));
    const segments = currentEventGroup.segments || [];
    start = segments.length ? Math.min(...segments.map((item) => Number(item.start) || 0)) : Number(currentEventGroup.start) || 0;
    end = segments.length ? Math.max(...segments.map((item) => Number(item.end) || 0)) : Number(currentEventGroup.end) || start;
    type = `${currentEventNumber ? `${groupPrefix}${currentEventNumber} · ` : ""}${segments.length} 个${contentMode ? "内容片段" : "镜头"}${contentMode ? "" : (currentEventRecommended ? " · AI 推荐" : " · 备选")}`;
  } else if (currentCandidate) {
    title = String(currentCandidate.title || (contentMode ? "匹配片段" : "精彩镜头"));
    detail = String(currentCandidate.reason || currentCandidate.summary || currentCandidate.matchedEvidence || (contentMode ? "与检索要求匹配的内容片段" : "系统找到的精彩镜头"));
    start = Number(currentCandidate.start) || 0;
    end = Number(currentCandidate.end) || start;
    type = contentMode ? "当前匹配片段" : "当前候选镜头";
  }

  const visible = Boolean(title);
  root.classList.toggle("hidden", !visible);
  const outputTimeline = Boolean(currentOutput && viewerMediaKind === "output");
  root.classList.toggle("output-summary", outputTimeline);
  if (focusButton) focusButton.disabled = !visible;
  if (!visible) return;
  typeNode.textContent = type;
  titleNode.textContent = title;
  if (outputTimeline) {
    const model = timelineOutputComparisonModel(currentJob, currentOutput);
    const schedule = model?.schedule || [];
    textNode.innerHTML = schedule.map((entry, index) => {
      const label = contentMode
        ? String(entry.segment.chapterTitle || entry.segment.title || `片段 ${index + 1}`)
        : String(entry.segment.storyFunction || entry.segment.role || entry.segment.title || `镜头 ${index + 1}`);
      const selected = Number(timelineSelectedOutputSegmentIndex) === index;
      return `<button type="button" data-timeline-output-summary-segment="${index}" class="${selected ? "active" : ""}" aria-pressed="${selected}"><b>${String(index + 1).padStart(2, "0")}</b><span>${escapeHtml(label)}</span></button>`;
    }).join('<i aria-hidden="true">→</i>');
    if (Number.isInteger(timelineSelectedOutputSegmentIndex)) {
      const selected = schedule[timelineSelectedOutputSegmentIndex];
      if (selected && Number(selected.sourceEnd) > Number(selected.sourceStart)) {
        textNode.insertAdjacentHTML("beforeend", `<button type="button" class="timeline-output-source-link" data-timeline-output-source="${timelineSelectedOutputSegmentIndex}">查看源片</button>`);
      }
    }
    textNode.querySelectorAll("[data-timeline-output-summary-segment]").forEach((button) => button.addEventListener("click", () => {
      selectOutputTimelineSegment(Number(button.dataset.timelineOutputSummarySegment));
    }));
    textNode.querySelector("[data-timeline-output-source]")?.addEventListener("click", (event) => {
      const index = Number(event.currentTarget.dataset.timelineOutputSource);
      const selected = schedule[index];
      if (selected) previewCompositionSourceSegment(currentOutput, index, selected.sourceStart);
    });
  } else {
    textNode.textContent = detail;
  }
  if (outputTimeline) {
    const schedule = timelineOutputComparisonModel(currentJob, currentOutput)?.schedule || [];
    const outputDuration = Number(currentOutput.duration)
      || Number(schedule.at(-1)?.outputEnd || 0);
    timeNode.textContent = contentMode
      ? `${schedule.length} 个片段 · 内容视频 ${outputDuration.toFixed(1)} 秒`
      : `${schedule.length} 个镜头 · 成片 ${outputDuration.toFixed(1)} 秒`;
    timeNode.title = "时间位置与播放器中的当前版本完全一致";
  } else if (currentEventGroup && !currentEventSegment) {
    const segments = currentEventGroup.segments || [];
    const contentDuration = segments.reduce((sum, segment) => sum + Math.max(0, Number(segment.duration || (Number(segment.end) - Number(segment.start)) || 0)), 0);
    timeNode.textContent = `${segments.length} 个${contentMode ? "片段" : "镜头"} · 有效内容 ${contentDuration.toFixed(1)} 秒`;
    timeNode.title = segments.length ? `源视频范围 ${formatTime(start)} → ${formatTime(end)}` : "";
  } else {
    timeNode.textContent = `${formatTime(start)} → ${formatTime(end)} · ${Math.max(0, end - start).toFixed(1)} 秒`;
    timeNode.removeAttribute("title");
  }
}

function updateTimelineReviewControls() {
  const duration = timelineDurationValue();
  const view = timelineViewRange();
  const fitButton = $("#timelineFit");
  const focusButton = $("#timelineFocusReview");
  const outputComparison = timelineHasOutputComparison();
  const coordinateSwitch = $("#timelineCoordinateSwitch");
  const outputAxisButton = $("#timelineOutputAxis");
  const sourceAxisButton = $("#timelineSourceAxis");
  const clockLabel = $("#timelineClockLabel");
  const reviewRange = currentTimelineReviewRange();
  if (!reviewRange) timelineReviewFollow = false;
  const fullView = duration <= 0 || view.duration >= duration - .25;
  const focusActive = Boolean(timelineReviewFollow && reviewRange && !fullView);
  const eventNumber = timelineEventSequenceNumber(currentEventGroup);
  const contentMode = String(currentJob?.taskMode || "") === "content_extract";
  const focusTarget = currentOutput?.segments?.length
    ? (contentMode ? "当前版本" : "当前成片")
    : contentMode
      ? (currentEventSegment || currentCandidate || eventNumber ? "当前片段" : "匹配片段")
      : currentEventSegment ? "当前镜头" : eventNumber ? `E${eventNumber}` : currentCandidate ? "当前镜头" : "当前事件";
  fitButton?.classList.toggle("active", fullView);
  coordinateSwitch?.classList.add("hidden");
  outputAxisButton?.classList.toggle("active", outputComparison);
  sourceAxisButton?.classList.remove("active");
  outputAxisButton?.setAttribute("aria-pressed", String(outputComparison));
  sourceAxisButton?.setAttribute("aria-pressed", "false");
  if (clockLabel) clockLabel.textContent = outputComparison
    ? (currentOutputIsReviewSample(currentOutput) ? "样片" : "成片")
    : "源片";
  fitButton?.setAttribute("aria-pressed", String(fullView));
  focusButton?.classList.toggle("active", focusActive);
  focusButton?.setAttribute("aria-pressed", String(focusActive));
  if (focusButton) {
    focusButton.classList.toggle("hidden", outputComparison || !reviewRange);
    focusButton.disabled = outputComparison || !reviewRange;
    focusButton.textContent = focusActive ? `已聚焦 ${focusTarget}` : `聚焦 ${focusTarget}`;
  }
  timelineViewport?.classList.toggle("timeline-zoomed", !fullView);
  timelineViewport?.classList.toggle("manual-select-mode", timelineManualSelectMode);
  $("#timelineOverview")?.classList.toggle("timeline-zoomed", !fullView);
  const manualSelectionGroup = $("#timelineManualSelectToggle")?.closest(".timeline-tool-group");
  manualSelectionGroup?.classList.toggle("hidden", outputComparison);
  if (outputComparison && timelineManualSelectMode) {
    timelineManualSelectMode = false;
    $("#timelineManualSelectToggle")?.classList.remove("active");
    $("#timelineManualSelectToggle")?.setAttribute("aria-pressed", "false");
    timelineViewport?.classList.remove("manual-select-mode");
  }
  if (timelineViewport) {
    timelineViewport.title = outputComparison
      ? "点击片段或时间位置可继续播放当前版本；查看原始素材请使用“查看源片”"
      : timelineManualSelectMode
      ? "拖动生成选区；按住空格拖动或使用中键可左右移动"
      : contentMode
        ? fullView
          ? (currentOutput ? "点击已采用片段，回到源视频中的原始位置" : "点击匹配片段查看；需要时使用“聚焦”按钮放大")
          : "点击匹配片段预览；时间轴缩放与移动由你控制"
        : fullView ? (currentOutput ? "点击成片镜头可从对应位置预览；需要时聚焦当前成片" : "点击事件查看；需要时使用“聚焦”按钮放大")
        : "点击事件或镜头预览；时间轴缩放与移动由你控制";
  }
  if (!pendingTimelineSelection) {
    const hint = $("#timelineHint");
    if (hint) hint.textContent = outputComparison
      ? "位置和播放头对应当前版本；选择片段后可明确查看源片"
      : timelineManualSelectMode
      ? "拖动生成选区，按住空格拖动可平移"
      : contentMode
        ? fullView
          ? (currentOutput ? "编号表示成片顺序，位置和播放头表示源片时间" : "点击匹配片段查看证据，使用“聚焦”按钮放大")
          : "点击片段预览，拖动左右移动"
        : fullView ? (currentOutput ? "已采用事件与镜头叠加在源视频时间轴上；点击可回看原始画面" : "点击事件查看，使用“聚焦”按钮放大")
        : "点击播放，拖动左右移动";
  }
}

function focusCurrentTimelineReview() {
  const range = currentTimelineReviewRange();
  const duration = timelineDurationValue();
  if (!range || duration <= 0) return;
  timelineReviewFollow = true;
  const contentSpan = Math.max(1, range.end - range.start);
  const span = Math.min(duration, Math.max(30, contentSpan * 1.55));
  const center = (range.start + range.end) / 2;
  setTimelineView(center - span / 2, center + span / 2, { keepReviewFocus: true });
}

function refreshTimelineAfterReviewSelection() {
  // Selecting or previewing an event/shot must never change the user's zoom
  // window. Only the explicit focus control is allowed to reframe the axis.
  timelineReviewFollow = false;
  updateTimeline();
}

function renderTimelineOverview(items) {
  const duration = timelineDurationValue();
  const view = timelineViewRange();
  const clips = $("#timelineOverviewClips");
  const windowElement = $("#timelineViewWindow");
  if (!clips || !windowElement) return;
  if (duration <= 0) {
    clips.innerHTML = "";
    return;
  }
  const recommendedGroups = new Set((currentJob?.recommendedGroupIds || []).map(String));
  const recommendedCandidates = new Set((currentJob?.recommendedIndices || []).map(Number));
  clips.innerHTML = duration > 0 ? items.map((item) =>
    `<i class="timeline-overview-clip${item.filename ? " output" : ""}${item._comparisonSource ? (item._usedInOutput ? " used-in-output" : " unused-in-output") : ""}${item._group && recommendedGroups.has(String(item._group.id)) || !item._group && recommendedCandidates.has(Number(item.index)) ? " recommended" : ""}" style="left:${Number(item.start) / duration * 100}%;width:${Math.max(.15, (Number(item.end) - Number(item.start)) / duration * 100)}%"></i>`
  ).join("") : "";
  windowElement.className = "timeline-view-window";
  windowElement.style.left = `${view.start / duration * 100}%`;
  windowElement.style.width = `${view.duration / duration * 100}%`;
}

function downsampleTimelinePoints(points, view, minimumPixelGap) {
  const trackContent = timelineTrackContent || timelineViewport;
  const width = Math.max(1, trackContent?.clientWidth || 1);
  let previousX = -Infinity;
  return points.filter((time) => {
    const x = (Number(time) - view.start) / view.duration * width;
    if (x - previousX < minimumPixelGap) return false;
    previousX = x;
    return true;
  });
}

function renderTimelineMediaAssets(force = false) {
  const track = $("#timelineThumbnails");
  const cuts = $("#timelineSceneCuts");
  const trackContent = timelineTrackContent || timelineViewport;
  if (!trackContent || !track || !cuts) return;
  const sprite = timelineAssets?.sprite;
  const view = timelineViewRange();
  const renderKey = [
    currentJob?.id, Math.round(trackContent.clientWidth), Math.round(trackContent.clientHeight),
    view.start.toFixed(3), view.end.toFixed(3), timelineVisualMode,
    timelineCutsVisible, timelineAssets?.spriteUrl || "",
    sprite?.items?.length || 0, timelineAssets?.sceneCuts?.length || 0,
    currentEventGroup?.id || "", currentEventSegment?.id || "", currentCandidate?.index ?? "",
    currentOutput?.filename || "", timelineCoordinateSpace, Number.isFinite(timelineFrameSelectionTime) ? timelineFrameSelectionTime.toFixed(3) : "",
  ].join("|");
  if (!force && timelineMediaRenderKey === renderKey) return;
  timelineMediaRenderKey = renderKey;
  if (!sprite?.items?.length || !timelineAssets?.spriteUrl) {
    const loading = timelineAssetsLoadingJobId === currentJob?.id
      || $("#timelineThumbnailState")?.dataset.tone === "loading"
      || (!timelineAssets && Boolean(currentJob?.id));
    timelineViewport.dataset.thumbnailState = loading ? "loading" : "empty";
    track.innerHTML = loading ? '<span class="timeline-media-loader" data-generative-loader="image" data-loader-variant="tiles" data-loader-size="100%" data-loader-radius="6" data-loader-label="正在生成时间轴缩略图"></span>' : "";
    if (loading) syncGenerativeLoaders(track);
  } else {
    timelineViewport.dataset.thumbnailState = timelineAssets?.partial ? "loading" : "ready";
    const displayHeight = Math.max(32, Math.round((track.getBoundingClientRect().height || 43) - 8));
    const scale = displayHeight / Number(sprite.tileHeight);
    const displayWidth = Number(sprite.tileWidth) * scale;
    const desired = Math.max(1, Math.min(sprite.items.length, Math.ceil(trackContent.clientWidth / (displayWidth + 2))));
    const selected = [];
    const selectedIndices = new Set();
    const outputAxis = timelineOutputAxisActive();
    for (let index = 0; index < desired; index += 1) {
      const target = view.start + view.duration * (index + 0.5) / desired;
      const mapped = outputAxis ? compositionSourceFrameAtOutputTime(currentOutput, target) : null;
      if (outputAxis && !mapped) {
        selected.push({ item: null, timelineTime: target, sourceTime: null });
        continue;
      }
      const sourceTarget = mapped ? mapped.sourceTime : target;
      const item = sprite.items.reduce((best, current) =>
        Math.abs(Number(current.time) - sourceTarget) < Math.abs(Number(best.time) - sourceTarget) ? current : best
      );
      if (outputAxis || !selectedIndices.has(item.index)) {
        selectedIndices.add(item.index);
        selected.push({ item, timelineTime: outputAxis ? target : Number(item.time) || 0, sourceTime: Number(item.time) || 0 });
      }
    }
    const linkedIntervals = currentTimelineLinkedIntervals();
    track.innerHTML = selected.map(({ item, timelineTime, sourceTime }) => {
      const frameTime = Number(timelineTime) || 0;
      const selectedFrame = Number.isFinite(timelineFrameSelectionTime) && Math.abs(frameTime - timelineFrameSelectionTime) < .05;
      if (!item) {
        return `<button type="button" class="timeline-thumbnail output-gap${selectedFrame ? " selected-frame" : ""}" data-timeline-frame-time="${frameTime}" aria-label="预览 ${formatTime(frameTime)} 的片头画面" title="${formatTime(frameTime)} · 当前画面没有对应源片帧" style="width:${displayWidth}px;height:${displayHeight}px"></button>`;
      }
      const isPartial = Boolean(timelineAssets?.partial);
      const quality = isPartial ? "" : item.black ? "black-frame" : item.blurred ? "blurred-frame" : "";
      const faces = isPartial ? 0 : Number(item.faces || 0);
      const linked = linkedIntervals.some((range) => frameTime >= range.start && frameTime <= range.end);
      const hint = isPartial ? "时间轴关键帧正在补充" : item.black ? "疑似黑帧" : item.blurred ? "画面清晰度较低" : `动作强度 ${Math.round(Number(item.motion || 0) * 100)}`;
      const classes = ["timeline-thumbnail", quality, faces ? "face-frame" : "", linked ? "linked" : "", selectedFrame ? "selected-frame" : ""].filter(Boolean).join(" ");
      const timeTitle = outputAxis
        ? `成片 ${formatTime(frameTime)} · 源片 ${formatTime(sourceTime)}`
        : formatTime(frameTime);
      return `<button type="button" class="${classes}" data-timeline-frame-time="${frameTime}" data-source-frame-time="${sourceTime}" data-faces="${faces || ""}" aria-label="预览 ${formatTime(frameTime)} 的画面" title="${timeTitle} · ${hint}${faces ? ` · 检测到 ${faces} 张人脸` : ""}" style="--motion:${Math.min(1, Number(item.motion || 0) * 4)};width:${displayWidth}px;height:${displayHeight}px;background-image:url('${timelineAssets.spriteUrl}');background-size:${Number(sprite.spriteWidth) * scale}px ${Number(sprite.spriteHeight) * scale}px;background-position:${-Number(item.column) * Number(sprite.tileWidth) * scale}px ${-Number(item.row) * Number(sprite.tileHeight) * scale}px"></button>`;
    }).join("");
    track.querySelectorAll("[data-timeline-frame-time]").forEach((button) => button.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      const time = Number(button.dataset.timelineFrameTime || 0);
      timelineFrameSelectionTime = time;
      if (timelineOutputAxisActive()) seekCurrentMediaTime(time);
      else {
        const match = timelineEventSegmentAtTime(time);
        if (match) previewEventSegment(match.group, match.segment);
        else {
          showSource();
          seekSourceTime(time);
          renderTimelineEventSummary();
        }
      }
      renderTimelineMediaAssets(true);
    }));
  }
  const outputCuts = timelineOutputAxisActive()
    ? compositionSchedule(currentOutput).flatMap((entry) => (timelineAssets?.sceneCuts || [])
      .filter((time) => Number(time) >= entry.sourceStart && Number(time) <= entry.sourceEnd)
      .map((time) => entry.outputStart + segmentOutputOffsetForSource(entry.segment, Number(time))))
    : (timelineAssets?.sceneCuts || []);
  const visibleCuts = outputCuts
    .filter((time) => Number(time) >= view.start && Number(time) <= view.end);
  const cutGap = view.duration / Math.max(1, trackContent.clientWidth) > .32 ? 10 : 3;
  cuts.innerHTML = timelineCutsVisible ? downsampleTimelinePoints(visibleCuts, view, cutGap)
    .map((time) => `<i class="timeline-scene-cut" style="left:${timelinePercentInView(time)}%"></i>`)
    .join("") : "";
  cuts.classList.toggle("layer-hidden", !timelineCutsVisible);
  updateTimelinePlayhead();
}

function setTimelineThumbnailState(message = "", tone = "") {
  const state = $("#timelineThumbnailState");
  if (!state) return;
  setGenerativeInlineStatus(state, message, tone || "neutral", "glyph");
}

function setWaveformState(message = "", tone = "neutral") {
  if (timelineViewport) timelineViewport.dataset.waveformState = tone || "neutral";
  setGenerativeInlineStatus("#waveformState", message, tone, "signal");
}

async function loadTimelineTranscript(job) {
  if (!job || transcriptLoadingJobId === job.id || timelineTranscriptJobId === job.id || Date.now() < transcriptRetryAt) return;
  transcriptLoadingJobId = job.id;
  try {
    const data = await api(`/api/jobs/${job.id}/transcript`);
    if (currentJob?.id === job.id) {
      timelineTranscript = data.segments || [];
      if (data.available) {
        timelineTranscriptJobId = job.id;
        transcriptRetryAt = 0;
      } else {
        transcriptRetryAt = Date.now() + 5000;
      }
      if (currentCandidate) renderClipEvidence(currentCandidate, "candidate");
      else if (currentEventSegment) renderClipEvidence(currentEventSegment, "segment");
      else if (currentEventGroup) renderClipEvidence(currentEventGroup, "event");
      contentReviewRootsForSearch(currentJob).forEach((contentSearchRoot) => {
        syncContentSearchSubtitleControls(
          contentSearchRoot,
          contentSearchJobForRoot(contentSearchRoot, currentJob),
        );
      });
    }
  } catch {
    if (currentJob?.id === job.id) {
      timelineTranscript = [];
      timelineTranscriptJobId = null;
      transcriptRetryAt = Date.now() + 5000;
    }
  } finally {
    if (transcriptLoadingJobId === job.id) transcriptLoadingJobId = null;
  }
}

async function loadTimelineAssets(job) {
  if (!job || timelineAssetsLoadingJobId === job.id || Date.now() < timelineAssetsRetryAt) return;
  if (timelineAssetsJobId === job.id && timelineAssets && !timelineAssets.generating && !timelineAssets.retryable) return;
  timelineAssetsLoadingJobId = job.id;
  if (timelineAssetsJobId !== job.id) timelineAssets = null;
  try {
    const data = await api(`/api/jobs/${job.id}/timeline-assets`);
    if (currentJob?.id !== job.id) return;
    if (data.ready === false) {
      const failed = Boolean(data.generationError);
      const delay = failed ? Math.max(3000, Number(data.retryAfterSeconds || 10) * 1000) : 1000;
      timelineAssetsRetryAt = Date.now() + delay;
      setTimelineThumbnailState(
        failed ? "画面缩略图生成失败，稍后自动重试" : "正在准备画面缩略图…",
        failed ? "error" : "loading",
      );
      renderTimelineMediaAssets(true);
      window.setTimeout(() => {
        if (currentJob?.id === job.id) updateTimeline();
      }, delay + 50);
      return;
    }
    timelineAssetsJobId = job.id;
    timelineAssets = data;
    const failed = Boolean(data.generationError);
    const delay = failed ? 10000 : 1000;
    timelineAssetsRetryAt = data.generating || data.retryable ? Date.now() + delay : 0;
    if (data.partial) {
      const count = Number(data.frameCount || data.sprite?.items?.length || 0);
      const target = Number(data.frameTarget || 0);
      setTimelineThumbnailState(
        failed ? `已显示 ${count} 张画面，后续生成失败，稍后重试` : `正在补充画面 ${count}${target ? ` / ${target}` : ""}`,
        failed ? "error" : "loading",
      );
    } else {
      setTimelineThumbnailState("");
    }
    updateTimeline();
    if ($("#candidateDrawer")?.classList.contains("open")) renderCandidateDrawer(job);
    if (data.generating || data.retryable) {
      window.setTimeout(() => {
        if (currentJob?.id === job.id) updateTimeline();
      }, delay + 50);
    }
  } catch {
    if (currentJob?.id === job.id) {
      timelineAssetsRetryAt = Date.now() + 2500;
      setTimelineThumbnailState("暂时无法读取画面，正在重试", "error");
      window.setTimeout(() => {
        if (currentJob?.id === job.id) updateTimeline();
      }, 2550);
    }
  } finally {
    if (timelineAssetsLoadingJobId === job.id) timelineAssetsLoadingJobId = null;
    if (secondaryEditorOpen() && currentJob?.id === job.id) renderSecondaryEditorTimeline();
  }
}

function renderTimelineProposalPreview(job = currentJob) {
  const track = $("#timelineProposalTrack");
  const outputsRoot = $("#timelineProposalOutputs");
  const sourceRoot = $("#timelineProposalPreview");
  if (!track || !outputsRoot || !sourceRoot) return;
  const proposal = job?.pendingEditProposal;
  const pending = proposal?.status === "pending";
  const preview = pending && proposal.preview && typeof proposal.preview === "object" ? proposal.preview : {};
  let outputs = Array.isArray(preview.outputs) ? preview.outputs.filter((output) => Array.isArray(output?.schedule) && output.schedule.length) : [];
  if (!outputs.length && Array.isArray(preview.schedule) && preview.schedule.length) {
    outputs = [{ id: "proposal_reel", label: "提案成片", duration: Number(preview.totalOutputDuration || preview.durationAfter || 0), schedule: preview.schedule }];
  }
  // Jobs created by the previous proposal schema only carry source ranges.
  // Present them as a simple sequential strip instead of dropping the track.
  if (!outputs.length && Array.isArray(preview.ranges) && preview.ranges.length) {
    let cursor = 0;
    const schedule = preview.ranges.map((range, index) => {
      const duration = Math.max(.1, Number(range.end) - Number(range.start));
      const entry = {
        objectId: String(range.id || index), objectType: String(range.targetType || "segment"), groupId: "",
        label: range.label || `片段 ${index + 1}`, state: range.state || "adjusted",
        sourceStart: Number(range.start) || 0, sourceEnd: Number(range.end) || 0,
        outputStart: cursor, outputEnd: cursor + duration, effectiveDuration: duration,
        transitionOverlap: 0, order: index + 1, outputId: "legacy_proposal",
      };
      cursor += duration;
      return entry;
    });
    outputs = [{ id: "legacy_proposal", label: "提案片段", duration: cursor, schedule }];
  }
  track.classList.toggle("hidden", !pending || !outputs.length);
  if ($("#timelineProposalTitle")) $("#timelineProposalTitle").textContent = proposal?.title || "剪辑编排预览";
  if ($("#timelineProposalDuration")) $("#timelineProposalDuration").textContent = `${outputs.length > 1 ? `${outputs.length} 条 · ` : ""}${formatTime(Number(preview.totalOutputDuration || outputs.reduce((sum, output) => sum + Number(output.duration || 0), 0)))}`;
  outputsRoot.innerHTML = pending ? outputs.map((output, outputIndex) => {
    const duration = Math.max(.001, Number(output.duration) || Math.max(...output.schedule.map((entry) => Number(entry.outputEnd) || 0), .001));
    const blocks = output.schedule.map((entry, index) => {
      const start = Math.max(0, Number(entry.outputStart) || 0);
      const end = Math.max(start, Number(entry.outputEnd) || start);
      const left = start / duration * 100;
      const width = Math.max(.65, (end - start) / duration * 100);
      const state = String(entry.state || "unchanged");
      const speed = Number(entry.playbackRate || 1);
      const technique = [speed !== 1 ? `${speed}×` : "", Number(entry.transitionOverlap || 0) > 0 ? `${entry.transitionType === "fade_black" ? "淡黑" : "叠化"} ${Number(entry.transitionOverlap).toFixed(1)}s` : ""].filter(Boolean).join(" · ");
      const title = [`镜头 ${index + 1}`, entry.label, `成片 ${formatTime(start)}→${formatTime(end)}`, `源片 ${formatTime(entry.sourceStart)}→${formatTime(entry.sourceEnd)}`, technique].filter(Boolean).join(" · ");
      return `<button type="button" class="proposal-schedule-block state-${escapeHtml(state)}${activeProposalSourceRange?.proposalId === proposal.id && activeProposalSourceRange?.outputId === String(output.id) && Number(activeProposalSourceRange?.index) === index ? " active" : ""}" data-proposal-output="${escapeHtml(String(output.id || outputIndex))}" data-proposal-index="${index}" style="left:${left}%;width:${width}%" title="${escapeHtml(title)}" aria-label="${escapeHtml(title)}"><b>${String(index + 1).padStart(2, "0")}</b><span>${escapeHtml(entry.label || `镜头 ${index + 1}`)}</span>${technique ? `<em>${escapeHtml(technique)}</em>` : ""}</button>`;
    }).join("");
    return `<div class="proposal-output-row" data-proposal-output-row="${escapeHtml(String(output.id || outputIndex))}"><div class="proposal-output-label"><b>${escapeHtml(outputs.length > 1 ? `输出 ${outputIndex + 1}` : "成片顺序")}</b><span>${escapeHtml(output.label || "提案成片")} · ${formatTime(duration)}</span></div><div class="proposal-output-line"><i aria-hidden="true"></i>${blocks}</div></div>`;
  }).join("") : "";
  outputsRoot.querySelectorAll("[data-proposal-index]").forEach((button) => button.addEventListener("click", () => {
    const output = outputs.find((item, index) => String(item.id || index) === String(button.dataset.proposalOutput));
    const index = Number(button.dataset.proposalIndex);
    const entry = output?.schedule?.[index];
    if (!entry) return;
    activeProposalSourceRange = {
      proposalId: String(proposal.id || ""), outputId: String(output.id || ""), index,
      start: Number(entry.sourceStart) || 0, end: Number(entry.sourceEnd) || Number(entry.sourceStart) || 0,
    };
    showSource({ autoplay: false });
    candidatePreviewEnd = Number(entry.sourceEnd) || null;
    const padding = Math.max(3, Math.min(12, (Number(entry.sourceEnd) - Number(entry.sourceStart)) * 1.2));
    setTimelineView(Math.max(0, Number(entry.sourceStart) - padding), Number(entry.sourceEnd) + padding);
    seekSourceTime(Number(entry.sourceStart) || 0);
    updateTimeline();
  }));

  const active = pending && activeProposalSourceRange?.proposalId === String(proposal?.id || "") ? activeProposalSourceRange : null;
  const view = timelineViewRange();
  const visible = active && Number(active.end) > view.start && Number(active.start) < view.end;
  sourceRoot.classList.toggle("hidden", !visible);
  sourceRoot.setAttribute("aria-hidden", String(!visible));
  if (!visible) {
    sourceRoot.innerHTML = "";
  } else {
    const start = Math.max(view.start, Number(active.start) || 0);
    const end = Math.min(view.end, Number(active.end) || start);
    sourceRoot.innerHTML = `<i class="proposal-source-highlight" style="left:${timelinePercentInView(start)}%;width:${Math.max(.35, (end - start) / view.duration * 100)}%"><span>提案镜头来源</span></i>`;
  }
}

function contentSearchTimelineItems(job = currentJob) {
  const activeSearchId = String(job?.contentSearchSession?.activeSearchId || job?.contentSearch?.id || "");
  const items = contentSearchRecordsForJob(job).flatMap((record, searchIndex) => {
    const candidates = Array.isArray(record?.candidates) && record.candidates.length
      ? record.candidates
      : Array.isArray(record?.timelineCandidates) ? record.timelineCandidates : [];
    const historical = String(record?.id || "") !== activeSearchId;
    const query = String(record?.intent?.query || record?.instruction || `检索 ${searchIndex + 1}`).trim();
    return candidates.filter((candidate) => (
      Number(candidate?.end) > Number(candidate?.start)
      && String(candidate?.reviewStatus || "") !== "rejected"
    )).map((candidate, candidateIndex) => ({
      ...candidate,
      index: candidateIndex,
      _contentSearchId: String(record?.id || ""),
      _contentSearchQuery: query,
      _contentSearchOrdinal: searchIndex + 1,
      _contentSearchHistorical: historical,
    }));
  });
  const currentItems = items.filter((item) => !item._contentSearchHistorical);
  const retainedItems = items.filter((item) => item._contentSearchHistorical);
  currentItems.forEach((current) => {
    const contained = retainedItems.filter((retained) => (
      Number(current.start) <= Number(retained.start) + .05
      && Number(current.end) >= Number(retained.end) - .05
    ));
    current._containedHistoricalCount = contained.length;
    current._containedHistoricalIds = contained.map((item) => String(item.id || ""));
    contained.forEach((retained) => {
      retained._containedByCurrentId = String(current.id || "");
    });
  });
  return items;
}

function coverTimelineVariantSettings(job, variantId) {
  const variants = job?.coverTimelineDraft?.variants;
  const state = variants && typeof variants === "object" ? variants[String(variantId)] : null;
  const fallback = String(job?.coverDraft?.approvedVariantId || "") === String(variantId || "")
    ? Number(job?.coverIntroDraft?.duration || 1) : 1;
  return { duration: Math.round(Math.max(.5, Math.min(5, Number(state?.duration || fallback))) * 10) / 10 };
}

function coverTimelineCurrentVariantId(job) {
  const coverId = String(job?.currentCoverVersionId || "");
  const version = (job?.coverVersions || []).find((item) => String(item?.id || "") === coverId);
  return String(version?.variantId || job?.coverDraft?.approvedVariantId || "");
}

function coverTimelineRequirement(job) {
  const instruction = (job?.messages || [])
    .filter((message) => String(message?.role || "") === "user")
    .map((message) => String(message?.text || ""))
    .join("\n");
  const subjectMatch = instruction.match(
    /(?:用|使用|采用|选用|选取|选择|截取)\s*(?:第?\s*\d+(?:\.\d+)?\s*(?:秒钟|秒|s)\s*(?:左右|附近|前后)?\s*(?:出现|看到|有)?(?:的)?\s*)?(.{1,60}?)(?:的)?(?:照片|图片|肖像|头像|画面|镜头|帧)?\s*(?:来)?(?:作为|用作|做成|当作)\s*(?:视频)?封面/i,
  );
  const timeMatch = instruction.match(
    /(?:用|使用|采用|选用|选取|选择|截取)\s*(?:第)?\s*(\d+(?:\.\d+)?)\s*(?:秒钟|秒|s)[^。；;\n]{0,80}?(?:作为|用作|做成|当作)\s*(?:视频)?封面/i,
  );
  const subject = String(
    job?.coverDraft?.subject || job?.brief?.coverSubject
    || window.ClipTalkActiveCoverRequirement?.subject || subjectMatch?.[1] || "",
  ).replace(/^(?:出现|看到|看见|有|画面中|视频中)(?:的)?/, "").trim();
  const draftTime = job?.coverDraft?.requestedSourceTime;
  const activeTime = window.ClipTalkActiveCoverRequirement?.sourceTime;
  const sourceTime = draftTime !== null && draftTime !== undefined && Number.isFinite(Number(draftTime))
    ? Number(draftTime)
    : activeTime !== null && activeTime !== undefined && Number.isFinite(Number(activeTime))
      ? Number(activeTime) : timeMatch ? Number(timeMatch[1]) : null;
  return { subject, sourceTime };
}

function coverTimelineVariantCompliance(requirement, variant) {
  const rawSourceTime = variant?.sourceTime;
  const sourceTime = rawSourceTime === null || rawSourceTime === undefined || rawSourceTime === ""
    ? Number.NaN : Number(rawSourceTime);
  const sourceTimeMissing = Number.isFinite(requirement.sourceTime) && !Number.isFinite(sourceTime);
  const sourceTimeMismatch = Number.isFinite(requirement.sourceTime) && Number.isFinite(sourceTime)
    && Math.abs(requirement.sourceTime - sourceTime) > .75;
  const verification = variant?.subjectVerification && typeof variant.subjectVerification === "object"
    ? variant.subjectVerification : {};
  const personMissing = Boolean(requirement.subject) && verification.personDetected === false;
  return {
    valid: !sourceTimeMissing && !sourceTimeMismatch && !personMissing,
    sourceTime,
    sourceTimeMissing,
    sourceTimeMismatch,
    personMissing,
    identityPending: Boolean(requirement.subject) && verification.personDetected !== false,
  };
}

function normalizeCoverTimelineDuration(value) {
  const number = Number(value);
  return Math.round(Math.max(.5, Math.min(5, Number.isFinite(number) ? number : 1)) * 10) / 10;
}

function syncMainCoverTimelineEditor(root, rawDuration) {
  if (!root) return 1;
  const duration = normalizeCoverTimelineDuration(rawDuration);
  const videoDuration = Math.max(0, Number(root.dataset.videoDuration || 0));
  const videoRangeMode = String(root.dataset.videoRangeMode || "output");
  root.style.setProperty("--cover-width", `${Math.min(42, 18 + duration * 4.8)}%`);
  const range = root.querySelector("[data-cover-timeline-duration]");
  const number = root.querySelector("[data-cover-timeline-duration-number]");
  if (range) range.value = String(duration);
  if (number) number.value = duration.toFixed(1);
  const setText = (selector, text) => {
    const element = root.querySelector(selector);
    if (element) element.textContent = text;
  };
  setText("[data-cover-timeline-duration-label]", `${duration.toFixed(1)} 秒`);
  setText("[data-cover-timeline-cover-range]", `0–${duration.toFixed(1)}s`);
  const videoRangeText = videoRangeMode === "pending"
    ? "生成成片后合入"
    : videoDuration > 0
      ? `${duration.toFixed(1)}–${(duration + videoDuration).toFixed(1)}s`
      : "成片时长待同步";
  setText("[data-cover-timeline-video-range]", videoRangeText);
  return duration;
}

async function persistMainCoverTimelineVariant(variantId, duration, contentHash = "") {
  if (!currentJob?.id || !variantId) throw new Error("封面候选尚未准备完成");
  return apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/cover-timeline`, {
    method: "PATCH", headers: { "Content-Type": "application/json" },
    body: { variantId, duration, contentHash },
  });
}

async function selectMainCoverTimelineVariant(event) {
  const button = event.currentTarget;
  if (!currentJob || button.disabled) return;
  const variantId = String(button.dataset.coverVariantId || "");
  const variant = (currentJob.coverDraft?.variants || []).find((item) => String(item?.variantId || "") === variantId);
  if (!variant) return;
  button.disabled = true;
  try {
    const settingsState = coverTimelineVariantSettings(currentJob, variantId);
    const { job } = await persistMainCoverTimelineVariant(variantId, settingsState.duration, String(variant.contentHash || ""));
    renderJob(job);
    window.requestAnimationFrame(() => $("#timelineCoverTrack")?.scrollIntoView({ block: "nearest" }));
  } catch (error) {
    showToast(error.message || "无法切换封面草稿");
    button.disabled = false;
  }
}

async function saveMainCoverTimelineDuration(event) {
  const input = event.currentTarget;
  const editor = input.closest("[data-cover-timeline-editor]");
  const variantId = String(editor?.dataset.coverVariantId || "");
  const contentHash = String(editor?.dataset.contentHash || "");
  const duration = syncMainCoverTimelineEditor(editor, input.value);
  try {
    const { job } = await persistMainCoverTimelineVariant(variantId, duration, contentHash);
    currentJob = job;
    const state = $("#timelineCoverDraftState");
    if (state) state.textContent = `已保存 · ${duration.toFixed(1)} 秒`;
  } catch (error) {
    showToast(error.message || "无法保存当前封面参数");
  }
}

async function confirmMainCoverTimelineVariant(event) {
  const button = event.currentTarget;
  const editor = button.closest("[data-cover-timeline-editor]");
  const variantId = String(editor?.dataset.coverVariantId || "");
  const contentHash = String(editor?.dataset.contentHash || "");
  const duration = syncMainCoverTimelineEditor(editor, editor?.querySelector("[data-cover-timeline-duration]")?.value);
  if (!currentJob?.id || !variantId || button.disabled) return;
  button.disabled = true;
  button.textContent = "正在确认…";
  try {
    await persistMainCoverTimelineVariant(variantId, duration, contentHash);
    const agent = currentJob.agent || {};
    if (
      String(agent.status || "") === "action_required"
      && String(agent.currentStepTool || "") === "review_cover_variants"
      && typeof window.ClipTalkConfirmCoverTimelineSelection === "function"
    ) {
      await window.ClipTalkConfirmCoverTimelineSelection({ variantId, contentHash });
      showToast("最终封面已保存；生成成片或导出时会作为片头合入。", "success");
    } else {
      const { job } = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/cover-timeline/activate`, {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: { variantId, duration, contentHash },
      });
      renderJob(job);
      showToast("已设为最终封面；视频尚未重新合成。", "success");
    }
  } catch (error) {
    button.disabled = false;
    button.textContent = "保存为当前封面";
    showToast(error.message || "无法确认当前封面");
  }
}

async function exportMainCoverTimeline(event) {
  const button = event.currentTarget;
  const editor = button.closest("[data-cover-timeline-editor]");
  const duration = syncMainCoverTimelineEditor(editor, editor?.querySelector("[data-cover-timeline-duration]")?.value);
  if (!currentJob?.id || button.disabled) return;
  button.disabled = true;
  button.textContent = "正在提交…";
  try {
    await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/cover-intro/render`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: { duration },
    });
    button.textContent = "正在生成…";
    showToast("已开始生成带封面片头的成片版本，原版本保持不变。", "success");
    pollJob();
  } catch (error) {
    button.disabled = false;
    button.textContent = "生成带片头成片";
    showToast(error.message || "无法导出封面片头视频");
  }
}

function reviseMainCoverRequirement() {
  if (window.ClipTalkReviseAgentGoal?.()) return true;
  const latestGoal = [...(currentJob?.messages || [])].reverse().find((message) => message?.role === "user")?.text || "";
  window.setChatInputDraft?.(latestGoal);
  $("#chatInput")?.focus();
  return Boolean(latestGoal);
}

async function retryMainCoverCandidates(event) {
  const button = event.currentTarget;
  if (button.disabled) return;
  if (typeof window.ClipTalkRetryCoverCandidates !== "function") {
    showToast("当前封面计划无法直接重建，请修改封面要求后重新生成", "error");
    reviseMainCoverRequirement();
    return;
  }
  const previousLabel = button.textContent;
  button.disabled = true;
  button.textContent = "正在重新取帧…";
  try {
    await window.ClipTalkRetryCoverCandidates();
    showToast("正在按原封面要求重新提取候选", "success");
  } catch (error) {
    if (button.isConnected) {
      button.disabled = false;
      button.textContent = previousLabel;
    }
    showToast(error.message || "无法重新生成封面候选", "error");
  }
}

function renderTimelineCoverTrack(job = currentJob) {
  const track = $("#timelineCoverTrack");
  const variantsRoot = $("#timelineCoverVariants");
  const editor = $("#timelineCoverEditor");
  if (!track || !variantsRoot || !editor) return;
  // Cover candidates are task-scoped.  Older snapshots without jobId are
  // deliberately not rendered here, otherwise a late response or reused
  // workspace can make the previous task's cover appear in this track.
  const draftBelongsToJob = String(job?.coverDraft?.jobId || "") === String(job?.id || "");
  const variants = draftBelongsToJob
    ? (job?.coverDraft?.variants || []).filter((item) => item?.variantId && item?.previewUrl)
    : [];
  const coverReviewFocused = Boolean(
    variants.length
    && ["review_ready", "auto_selected", "candidates_ready"].includes(String(job?.coverDraft?.status || ""))
  );
  document.body.dataset.ctCoverReview = String(coverReviewFocused);
  document.body.dataset.ctCoverEditorOpen = String(coverReviewFocused && reviewLowerPanelMode === "timeline");
  document.querySelector("#workspace")?.classList.toggle("cover-review-focused", coverReviewFocused);
  track.classList.toggle("hidden", !variants.length);
  if (!variants.length) {
    variantsRoot.innerHTML = "";
    editor.innerHTML = "";
    return;
  }
  const requirement = coverTimelineRequirement(job);
  const complianceById = new Map(variants.map((variant) => [
    String(variant.variantId || ""), coverTimelineVariantCompliance(requirement, variant),
  ]));
  const validVariantCount = [...complianceById.values()].filter((item) => item.valid).length;
  track.dataset.validation = validVariantCount ? "review" : "blocked";
  variantsRoot.dataset.state = validVariantCount ? "ready" : "empty";
  const requirementSummary = $("#timelineCoverRequirementSummary");
  const requirementLabels = [
    Number.isFinite(requirement.sourceTime) ? `来源 ${formatTime(requirement.sourceTime)} 附近` : "",
    requirement.subject ? `人物“${requirement.subject}”` : "",
  ].filter(Boolean);
  const directionLabels = {
    source_clean: "干净源帧", source_editorial: "编辑构图", source_cinematic: "电影质感",
  };
  if (requirementSummary) {
    const requirements = requirementLabels.join(" · ");
    requirementSummary.textContent = validVariantCount
      ? `${requirements || "请检查构图和文字"}。有效候选 ${validVariantCount}/${variants.length}，不符合要求的候选不可选择。`
      : `${requirements || "当前封面要求"}：没有可确认的候选，请修改要求并重新生成。`;
    requirementSummary.classList.toggle("hidden", validVariantCount === 0);
  }
  if (!validVariantCount) {
    const retryLabel = Number.isFinite(requirement.sourceTime)
      ? `重新从 ${formatTime(requirement.sourceTime)} 附近取帧`
      : "按原要求重新生成候选";
    const rejected = variants.map((variant, index) => {
      const compliance = complianceById.get(String(variant.variantId || ""));
      const source = Number.isFinite(compliance?.sourceTime) ? `来源 ${formatTime(compliance.sourceTime)}` : "来源时间未知";
      return `<figure><img src="${escapeHtml(variant.previewUrl)}" alt="已淘汰封面候选 ${index + 1}"><figcaption><b>${escapeHtml(directionLabels[variant.direction] || `候选 ${index + 1}`)}</b><small>${escapeHtml(`${source} · 不符合要求`)}</small></figcaption></figure>`;
    }).join("");
    variantsRoot.innerHTML = `<section class="timeline-cover-empty"><small>需要重新生成</small><h3>没有找到符合要求的封面</h3><p>${escapeHtml(requirementLabels.length ? `要求：${requirementLabels.join(" · ")}` : "当前候选均未通过来源检查")}</p><div><button type="button" class="primary" data-cover-timeline-retry>${escapeHtml(retryLabel)}</button><button type="button" data-cover-timeline-revise>修改封面要求</button></div></section><details class="timeline-cover-rejected"><summary>查看已淘汰候选 <b>${variants.length}</b></summary><div>${rejected}</div></details>`;
    editor.innerHTML = "";
    variantsRoot.querySelector("[data-cover-timeline-retry]")?.addEventListener("click", retryMainCoverCandidates);
    variantsRoot.querySelector("[data-cover-timeline-revise]")?.addEventListener("click", reviseMainCoverRequirement);
    const draftState = $("#timelineCoverDraftState");
    if (draftState) draftState.textContent = "需要重新生成";
    return;
  }
  const activeId = String(job.coverTimelineDraft?.activeVariantId || job.coverDraft?.selectedVariantId || variants[0].variantId);
  const active = variants.find((item) => String(item.variantId) === activeId) || variants[0];
  const currentVariantId = coverTimelineCurrentVariantId(job);
  const duration = coverTimelineVariantSettings(job, active.variantId).duration;
  const generatedOutputs = jobOutputVersions(job).flatMap((version) => Array.isArray(version.outputs) ? version.outputs : []);
  const selectedOutput = currentOutput?.filename ? currentOutput : generatedOutputs[0];
  const hasGeneratedOutput = Boolean(selectedOutput?.filename || generatedOutputs.length);
  const videoDuration = Math.max(0, Number(selectedOutput?.duration || 0));
  const videoRangeMode = hasGeneratedOutput ? "output" : "pending";
  const videoRangeText = hasGeneratedOutput
    ? (videoDuration > 0 ? `${duration.toFixed(1)}–${(duration + videoDuration).toFixed(1)}s` : "成片时长待同步")
    : "生成成片后合入";
  variantsRoot.innerHTML = variants.map((variant, index) => {
    const variantId = String(variant.variantId || "");
    const variantDuration = coverTimelineVariantSettings(job, variantId).duration;
    const compliance = complianceById.get(variantId);
    const sourceLabel = Number.isFinite(compliance?.sourceTime) ? `来源 ${formatTime(compliance.sourceTime)}` : "来源时间未知";
    const validationLabel = compliance?.sourceTimeMismatch || compliance?.sourceTimeMissing
      ? "来源不符" : compliance?.personMissing ? "未检测到人物" : compliance?.identityPending ? "人物待确认" : "可使用";
    return `<button type="button" class="timeline-cover-variant${variantId === String(active.variantId) ? " active" : ""}${variantId === currentVariantId ? " current" : ""}${compliance?.valid ? "" : " invalid"}" data-cover-variant-id="${escapeHtml(variantId)}" ${compliance?.valid ? "" : "disabled"}><img src="${escapeHtml(variant.previewUrl)}" alt="封面候选 ${index + 1}"><span><b>${escapeHtml(directionLabels[variant.direction] || `方案 ${index + 1}`)}</b><small>${escapeHtml(`${sourceLabel} · ${validationLabel}`)}</small><em>${variantDuration.toFixed(1)}s</em></span></button>`;
  }).join("");
  const isCurrent = String(active.variantId) === currentVariantId;
  const requiredSubject = requirement.subject;
  const requiredSourceTime = requirement.sourceTime;
  const activeCompliance = complianceById.get(String(active.variantId || ""));
  const activeSourceTime = activeCompliance?.sourceTime;
  const sourceTimeMismatch = activeCompliance?.sourceTimeMismatch || activeCompliance?.sourceTimeMissing;
  const subjectVerification = active?.subjectVerification && typeof active.subjectVerification === "object"
    ? active.subjectVerification : {};
  const personDetected = subjectVerification.personDetected;
  const identityNotice = sourceTimeMismatch
    ? `<section class="timeline-cover-identity" data-state="failed"><small>候选不可使用</small><strong>来源与封面要求不匹配</strong><p>${Number.isFinite(activeSourceTime) ? `当前候选来自 ${activeSourceTime.toFixed(1)} 秒，` : "当前候选缺少来源时间，"}要求使用 ${requiredSourceTime.toFixed(1)} 秒附近画面。请修改要求并重新生成候选。</p></section>`
    : requiredSubject
    ? `<section class="timeline-cover-identity" data-state="${personDetected === false ? "failed" : "review"}"><small>人物核验</small><strong>${personDetected === false ? "画面中未检测到人物" : "检测到人物，身份仍需你确认"}</strong><p>${personDetected === false ? `该候选不能作为“${escapeHtml(requiredSubject)}”的封面，请选择其他画面。` : `请确认画面人物确实是“${escapeHtml(requiredSubject)}”。系统不会仅凭有人脸就认定具体身份。`}</p></section>`
    : "";
  const activeValid = Boolean(activeCompliance?.valid);
  const footerAction = !activeValid
    ? `<span class="timeline-cover-unavailable" role="status">当前候选不可保存</span><button type="button" class="primary" data-cover-timeline-revise>修改封面要求</button>`
    : isCurrent
      ? `<span class="timeline-cover-saved" role="status">当前封面已保存</span>`
      : `<button type="button" class="primary" data-cover-timeline-confirm>${requiredSubject ? "确认人物无误并保存封面" : "保存为当前封面"}</button>`;
  editor.innerHTML = `<figure class="timeline-cover-preview"><img src="${escapeHtml(active.previewUrl)}" alt="当前编辑的封面草稿"></figure><section class="timeline-cover-settings" data-cover-timeline-editor data-cover-variant-id="${escapeHtml(active.variantId)}" data-content-hash="${escapeHtml(active.contentHash || "")}" data-video-duration="${videoDuration}" data-video-range-mode="${videoRangeMode}" style="--cover-width:${Math.min(42, 18 + duration * 4.8)}%"><header><div><strong>${escapeHtml(directionLabels[active.direction] || "封面草稿")}</strong><small>${requiredSubject ? `要求人物：${escapeHtml(requiredSubject)}` : "检查构图与封面文字后保存。"}</small></div><span data-cover-timeline-duration-label>${duration.toFixed(1)} 秒</span></header>${identityNotice}<label class="timeline-cover-duration"><span>片头停留时长</span><input type="range" min="0.5" max="5" step="0.1" value="${duration}" data-cover-timeline-duration><input type="number" min="0.5" max="5" step="0.1" value="${duration.toFixed(1)}" data-cover-timeline-duration-number aria-label="片头停留秒数"><em>秒</em></label><div class="timeline-cover-sequence"><span class="cover"><b>封面片头</b><small data-cover-timeline-cover-range>0-${duration.toFixed(1)}s</small></span><span class="video"><b>${hasGeneratedOutput ? "当前样片" : "待生成样片"}</b><small data-cover-timeline-video-range>${videoRangeText}</small></span></div><footer><span>${activeValid ? "保存后会更新当前任务封面；需要片头时，后续样片会使用这张封面。" : "该候选与当前要求不一致，请修改要求后重新生成。"}</span><div><button type="button" data-cover-timeline-open-preview>大图预览</button>${footerAction}</div></footer></section>`;
  variantsRoot.querySelectorAll("[data-cover-variant-id]").forEach((button) => button.addEventListener("click", selectMainCoverTimelineVariant));
  editor.querySelectorAll("[data-cover-timeline-duration], [data-cover-timeline-duration-number]").forEach((input) => {
    input.addEventListener("input", (inputEvent) => syncMainCoverTimelineEditor(inputEvent.currentTarget.closest("[data-cover-timeline-editor]"), inputEvent.currentTarget.value));
    input.addEventListener("change", saveMainCoverTimelineDuration);
  });
  editor.querySelector("[data-cover-timeline-open-preview]")?.addEventListener("click", () => {
    const direction = directionLabels[active.direction] || "封面候选";
    const source = Number.isFinite(activeSourceTime) ? `来源 ${formatTime(activeSourceTime)}` : "来源时间未知";
    openCoverImagePreview(active.previewUrl, direction, `${source}${requiredSubject ? ` · 要求人物“${requiredSubject}”` : ""}`);
  });
  editor.querySelector("[data-cover-timeline-revise]")?.addEventListener("click", reviseMainCoverRequirement);
  const confirm = editor.querySelector("[data-cover-timeline-confirm]");
  if (confirm) {
    confirm.addEventListener("click", confirmMainCoverTimelineVariant);
  }
  editor.querySelector("[data-cover-timeline-export]")?.addEventListener("click", exportMainCoverTimeline);
  const draftState = $("#timelineCoverDraftState");
  if (draftState) draftState.textContent = isCurrent ? "当前封面" : validVariantCount ? "待确认" : "没有有效候选";
}

window.addEventListener("cliptalk:cover-requirement", () => {
  if (currentJob) renderTimelineCoverTrack(currentJob);
});

function timelineHasRenderableContent(job = currentJob) {
  if (!job) return false;
  const sourceDuration = Number(waveformData?.duration || job?.videoInfo?.duration || mainVideo?.duration || 0);
  if (viewerMediaKind === "source" && sourceDuration > 0) return true;
  if (viewerMediaKind === "output" && currentOutput && timelineOutputDurationValue(currentOutput) > 0) return true;
  if (currentOutput?.segments?.length) return true;
  if (viewerMediaKind === "output" && String(job.taskMode || "") === "content_extract" && contentSearchTimelineItems(job).length) return true;
  if (jobOutputVersions(job).some((version) => (
    (version.segments || []).length
    || (version.outputs || []).some((output) => (output.segments || []).length || (output.schedule || []).length)
  ))) return true;
  if (currentCandidate) return true;
  if ((job.eventGroups || []).some((group) => (group.segments || []).length)) return true;
  if ((job.candidates || []).length && ["awaiting_confirmation", "awaiting_content_confirmation"].includes(String(job.status || ""))) return true;
  if (String(job.status || "") === "awaiting_content_confirmation" && contentSearchTimelineItems(job).length) return true;
  if (job.pendingEditProposal?.status === "pending") {
    const preview = job.pendingEditProposal.preview || {};
    if ((preview.schedule || []).length || (preview.ranges || []).length) return true;
    if ((preview.outputs || []).some((output) => (output.schedule || []).length)) return true;
  }
  if ($("#timelineSpeakerSegments")?.children.length || $("#timelinePersonSegments")?.children.length) return true;
  if (!$("#timelineCoverTrack")?.classList.contains("hidden")) return true;
  return false;
}

function updateTimeline() {
  if (!timelinePanel) return;
  const previousTimelineCoordinateSpace = timelineCoordinateSpace;
  const placeholderTimeline = $("#ctTimelinePlaceholder");
  if (!currentJob) {
    timelinePanel.classList.add("hidden");
    placeholderTimeline?.classList.remove("hidden");
    document.body.dataset.ctTimelineReady = "false";
    return;
  }
  const hasRenderableTimeline = timelineHasRenderableContent(currentJob);
  timelinePanel.classList.toggle("hidden", !hasRenderableTimeline || reviewLowerPanelMode !== "timeline");
  placeholderTimeline?.classList.toggle("hidden", hasRenderableTimeline || reviewLowerPanelMode !== "timeline");
  document.body.dataset.ctTimelineReady = String(hasRenderableTimeline);
  if (!hasRenderableTimeline) return;
  placeholderTimeline?.classList.add("hidden");
  const duration = timelineDurationValue();
  const contentMode = String(currentJob.taskMode || "") === "content_extract";
  const contentTrimGroup = $("#timelineContentTrimGroup");
  const contentTrimButton = $("#timelineContentTrim");
  const contentTrimState = contentMode && currentCandidate?.id && !currentOutput
    ? contentBoundaryEntryState(currentCandidate)
    : null;
  contentTrimGroup?.classList.toggle("hidden", !contentTrimState);
  if (contentTrimButton) {
    contentTrimButton.disabled = !contentTrimState || contentTrimState.disabled || actionBusy;
    contentTrimButton.textContent = contentTrimState?.kind === "edit" ? "调整当前片段" : (contentTrimState?.label || "调整当前片段");
    contentTrimButton.title = contentTrimState?.reason || "请先预览一个内容片段";
    contentTrimButton.dataset.boundaryEntryKind = contentTrimState?.kind || "unavailable";
    contentTrimButton.dataset.boundarySearchId = contentTrimState?.searchId || "";
  }
  const durationNode = $("#timelineDuration");
  if (durationNode) durationNode.textContent = formatTime(duration);
  const eventDisplay = timelineEventDisplayModel(currentJob);
  const displayEventGroups = eventDisplay.groups;
  const outputComparison = timelineOutputAxisActive()
    ? timelineOutputComparisonModel(currentJob, currentOutput)
    : null;
  const dedicatedSourceTracks = !outputComparison
    && ["speaker_edit", "person_edit"].includes(workflowKindForJob(currentJob));
  const timelinePresentation = timelinePresentationModel(currentJob, outputComparison);
  const contentReviewSingleLayer = ["content-review", "content-review-history"].includes(timelinePresentation.layoutKind);
  const contentHistorySplit = timelinePresentation.layoutKind === "content-review-history";
  applyTimelinePresentation(timelinePresentation);
  timelineCoordinateSpace = outputComparison ? "output" : "source";
  if (!outputComparison && previousTimelineCoordinateSpace === "output") {
    timelineViewStart = 0;
    timelineViewEnd = duration;
  }
  timelineViewport?.setAttribute("data-coordinate-space", timelineCoordinateSpace);
  timelineViewport?.removeAttribute("data-composed-hierarchy");
  timelineViewport?.classList.toggle("output-comparison-mode", Boolean(outputComparison));
  timelinePanel?.classList.toggle("output-comparison-mode", Boolean(outputComparison));
  const legendItems = [...timelinePanel.querySelectorAll(":scope > footer > span")].slice(0, 4);
  const retainedContentTimelineItems = contentMode && !outputComparison
    ? contentSearchTimelineItems(currentJob).filter((item) => item._contentSearchHistorical)
    : [];
  const legendLabels = outputComparison
    ? ["成片事件", "播放顺序", "当前播放", "成片位置"]
    : (contentMode
        ? retainedContentTimelineItems.length
          ? ["当前检索", "已保留检索", "当前预览", "播放位置"]
          : ["匹配片段", "已选片段", "当前预览", "播放位置"]
        : ["事件镜头", "AI 推荐", "当前选中", "播放位置"]);
  legendItems.forEach((node, index) => {
    const textNode = [...node.childNodes].find((child) => child.nodeType === 3);
    if (textNode) textNode.textContent = legendLabels[index];
  });
  legendItems[1]?.querySelector("i")?.classList.toggle("legend-retained", Boolean(retainedContentTimelineItems.length));
  let items;
  if (dedicatedSourceTracks) {
    // In dedicated speaker/person source review, the complete source evidence
    // already lives in the fixed tracks below. Persisted eventGroups represent
    // an earlier generated selection and must not leak back into source mode.
    items = [];
  } else if (outputComparison) {
    // Generated versions use their own playback axis. Event labels are
    // synthesized from the final sequence rather than reusing source-time
    // groups, so reordered and speed-adjusted clips remain truthful.
    items = contentReviewSingleLayer
      ? [...outputComparison.outputSegments]
      : [...outputComparison.outputEvents, ...outputComparison.outputSegments];
  } else if (viewerMediaKind === "candidate" && currentCandidate) {
    items = currentJob.candidates || [];
  } else if (currentJob.status === "awaiting_content_confirmation") {
    items = contentSearchTimelineItems(currentJob);
  } else if (viewerMediaKind === "output" && contentMode && contentSearchTimelineItems(currentJob).length) {
    // Some Agent review samples only persist the playable MP4 and version-level
    // metadata. Keep the "watch output" state useful by showing the confirmed
    // content-search ranges as the source timeline instead of an empty strip.
    items = contentSearchTimelineItems(currentJob);
  } else if (contentMode && contentSearchTimelineItems(currentJob).length) {
    // Completed content-search tasks should still show their matched source
    // ranges on the timeline while the user is reviewing the source video.
    items = contentSearchTimelineItems(currentJob);
  } else if (currentJob.status === "awaiting_confirmation" && currentJob.eventGroups?.length) {
    items = displayEventGroups.flatMap((group) => group.segments.map((segment) => ({ ...segment, _group: group })));
  } else if (currentJob.status === "awaiting_confirmation") {
    const excluded = new Set((currentJob.reviewExcludedCandidates || []).map((index) => Number(index)));
    items = (currentJob.candidates || []).filter((candidate) => !excluded.has(Number(candidate.index)));
  } else if (currentJob.eventGroups?.length) {
    items = displayEventGroups.flatMap((group) => (group.segments || []).map((segment) => ({
      ...segment, _group: group, _sourceEvent: true,
    })));
  } else {
    items = currentOutput?.segments?.length
      ? compositionSchedule(currentOutput).map((entry) => ({
        ...entry.segment,
        _output: currentOutput,
        _outputSegmentIndex: entry.index,
        _compositionOrder: entry.index + 1,
        filename: currentOutput.filename,
      }))
      : [];
  // A manually dragged source range is an input selection, not an additional
  // highlight layer. Keep it in state/chat, but do not render it over the
  // original event/candidate timeline.
  }
  items = items.filter((item) => !item.manual).filter(speakerMatches);
  // Event groups are stored in recommendation/priority order, not source-time
  // order. A review timeline must read left-to-right, so use chronological
  // order for display and derive stable 1..N shot numbers from that order.
  items = [...items].sort((left, right) => {
    const time = Number(left.start) - Number(right.start);
    if (Math.abs(time) > .001) return time;
    if (Boolean(left._output) !== Boolean(right._output)) return left._output ? 1 : -1;
    return Number(left.end) - Number(right.end);
  });
  const view = timelineViewRange();
  const recommended = new Set(currentJob.recommendedIndices || []);
  const selectedContentMatches = new Set(
    (currentJob.contentSearch?.defaultSelectedIds || currentJob.contentSearch?.reviewDraft?.selectedMatchIds || []).map(String),
  );
  const contentDraft = currentJob.contentSearch?.reviewDraft || {};
  const contentSourceOrder = items
    .filter((item) => selectedContentMatches.has(String(item.id || "")))
    .map((item) => String(item.id));
  const contentCustomOrder = (contentDraft.orderedMatchIds || []).map(String)
    .filter((id) => selectedContentMatches.has(id));
  const contentOrderedIds = String(contentDraft.orderMode || "source") === "source"
    ? contentSourceOrder
    : [...new Set([...contentCustomOrder, ...contentSourceOrder])];
  const contentOutputOrder = new Map(contentOrderedIds.map((id, index) => [id, index + 1]));
  const recommendedEventIds = eventDisplay.recommendedIds;
  const chronologicalEventGroups = displayEventGroups;
  const eventSequence = new Map(chronologicalEventGroups.map((group, index) => [String(group.id), index + 1]));
  const timelineRelationForItem = (item, position = 0) => {
    const directEventNumber = outputComparison && item?._outputEvent
      ? Number(item._outputEventOrder || 0)
      : Number(eventSequence.get(String(item?._group?.id || "")) || 0);
    if (directEventNumber) return { eventNumber: directEventNumber, relationKey: `event-${directEventNumber}` };
    if (item?._output) {
      const relatedEventIndex = eventDisplay.entries.findIndex((entry) =>
        entry.groups.some((group) => timelineOutputUsesGroup({ segments: [item] }, group)),
      );
      if (relatedEventIndex >= 0) {
        const eventNumber = relatedEventIndex + 1;
        return { eventNumber, relationKey: `event-${eventNumber}` };
      }
    }
    if (currentJob.status === "awaiting_confirmation") {
      return { eventNumber: 0, relationKey: `candidate-${position + 1}` };
    }
    return { eventNumber: 0, relationKey: "" };
  };
  const activeCanonicalEventId = eventDisplay.aliasToCanonical.get(String(currentEventGroup?.id || "")) || String(currentEventGroup?.id || "");
  const timelineItemIsActive = (item) => Boolean(
    currentEventSegment?.id != null && item?.id != null && String(currentEventSegment.id) === String(item.id)
    || currentCandidate?.index != null && item?.index != null && Number(currentCandidate.index) === Number(item.index)
    || contentMode && currentCandidate?.id != null && item?.id != null && String(currentCandidate.id) === String(item.id)
    || currentOutput?.filename && item?.filename && String(currentOutput.filename) === String(item.filename)
  );
  const timelineItemTitle = (item, position = 0) => {
    if (!item) return `镜头 ${position + 1}`;
    // Planned montage segments carry the original event/chapter title. Keep it
    // visible on the timeline instead of replacing it with the LLM role
    // (development/climax/etc.).
    if (item._group?.title) return item._group.title;
    if (item.chapterTitle) return item.chapterTitle;
    if (item.title) return item.title;
    const roleTitles = {
      hook: "开场", context: "上下文", development: "发展",
      climax: "高潮", reaction: "人物反应", result: "结尾",
      opening: "开场", ending: "结尾",
    };
    return roleTitles[String(item.role || "").toLowerCase()] || item.role || `镜头 ${position + 1}`;
  };
  const timelineShotRole = normalizedTimelineShotRole;
  const timelineShotCaption = (item) => {
    const candidateIndex = Number(item?.candidateIndex ?? item?.index);
    const candidate = Number.isFinite(candidateIndex)
      ? (currentJob.candidates || []).find((entry) => Number(entry.index) === candidateIndex)
      : null;
    return String(
      candidate?.title || item?.title || (contentMode ? item?.chapterTitle || item?._group?.title : "")
      || timelineShotRole(item) || (contentMode ? "匹配片段" : "镜头"),
    ).trim();
  };
  const labels = $("#timelineLabels");
  const trackWidth = Math.max(320, Math.round(timelineTrackContent?.getBoundingClientRect().width || timelineViewport?.getBoundingClientRect().width || 1000));
  const viewportHeight = Math.max(190, Math.round(timelineViewport?.getBoundingClientRect().height || 260));
  timelineLabelLayoutWidth = trackWidth;
  timelineLabelLayoutHeight = viewportHeight;
  const isFullTimelineView = view.duration >= duration - .25;
  const pixelsPerSecond = trackWidth / Math.max(.001, view.duration);
  const timelineDetailMode = !isFullTimelineView && (timelineReviewFollow || view.duration <= 180 || pixelsPerSecond >= 4);
  const densityProfile = timelineDensityProfile(viewportHeight, timelineDetailMode);
  let labelLaneHeight = densityProfile.labelLaneHeight;
  let shotLaneHeight = densityProfile.shotLaneHeight;
  const labelEntries = duration > 0 ? items.map((item, position) => {
    const groupSegments = item._group?.segments || [];
    const groupIntervals = outputComparison && item._outputEvent
      ? [{ start: Number(item.start) || 0, end: Number(item.end) || Number(item.start) || 0 }]
      : groupSegments.length
        ? timelineMergedIntervals(item._group)
        : [{ start: Number(item.start) || 0, end: Number(item.end) || Number(item.start) || 0 }];
    const visibleIntervals = groupIntervals.filter((range) => range.end > view.start && range.start < view.end);
    const groupStart = groupIntervals[0]?.start ?? (Number(item.start) || 0);
    const groupEnd = groupIntervals.at(-1)?.end ?? (Number(item.end) || groupStart);
    if (groupEnd <= view.start || groupStart >= view.end) return null;
    const sourceFirstSegment = [...groupSegments].sort((left, right) => Number(left.start) - Number(right.start))[0];
    if (item._group && !item._outputEvent && String(sourceFirstSegment?.id || "") !== String(item.id || "")) return null;
    // Once an event layer exists, rendered output segments stay in the shot
    // row. Repeating their role labels in the event row produces duplicate,
    // overlapping callouts and hides the original event titles.
    if (outputComparison && !contentReviewSingleLayer && item._output && !item._group) return null;
    const title = timelineItemTitle(item, position);
    const labelEnd = groupEnd;
    const detail = item._group?.summary || item.summary || item.reason || "";
    const characterCount = Array.from(String(title)).length;
    const contentMatchId = String(item._group?.contentMatchId || item.candidateId || item.id || "");
    const isRecommendedEvent = outputComparison
      ? Boolean(item._outputEvent)
      : contentMode
      ? (item._group
          ? recommendedEventIds.has(String(item._group.id)) || selectedContentMatches.has(contentMatchId)
          : !item._contentSearchHistorical && selectedContentMatches.has(contentMatchId))
      : item._group
        ? recommendedEventIds.has(String(item._group.id))
        : recommended.has(Number(item.index));
    const eventNumber = item._group
      ? (outputComparison && item._outputEvent
          ? Number(item._outputEventOrder || 0)
          : Number(eventSequence.get(String(item._group.id)) || 0))
      : 0;
    const relationKey = eventNumber ? `event-${eventNumber}` : `candidate-${position + 1}`;
    const duplicateCount = item._group
      ? Number(eventDisplay.entries.find((entry) => String(entry.group.id) === String(item._group.id))?.duplicateCount || 0)
      : 0;
    const minimumWidth = timelineDetailMode ? 200 : 132;
    const maximumWidth = timelineDetailMode ? 420 : 300;
    const width = Math.min(maximumWidth, Math.max(minimumWidth, characterCount * 13 + 72 + (isRecommendedEvent ? 34 : 0)));
    const scopeFragments = visibleIntervals.map((range) => ({
      left: Math.max(0, timelinePercentInView(Math.max(view.start, range.start))),
      width: Math.max(.25, (Math.min(view.end, range.end) - Math.max(view.start, range.start)) / view.duration * 100),
    }));
    const scopeLeft = Math.min(...scopeFragments.map((fragment) => fragment.left));
    const scopeRight = Math.max(...scopeFragments.map((fragment) => fragment.left + fragment.width));
    const anchor = Math.max(0, Math.min(trackWidth, (scopeLeft + scopeRight) / 200 * trackWidth));
    const left = Math.max(0, Math.min(trackWidth - Math.min(width, trackWidth), anchor - width / 2));
    const active = item._group
      ? activeCanonicalEventId === String(item._group.id || "")
      : contentMode
        ? String(currentCandidate?.id || "") === String(item.id || "")
        : Number(currentCandidate?.index) === Number(item.index);
    const usedInOutput = Boolean(outputComparison && item._group
      && outputComparison.usedGroupIds.has(String(item._group.id)));
    return {
      item, position, title, detail, labelEnd, width: Math.min(width, trackWidth), anchor, left, active,
      eventNumber, relationKey, isRecommendedEvent, usedInOutput, duplicateCount, groupStart, groupEnd, scopeLeft, scopeRight,
      outputOrder: Number(contentOutputOrder.get(contentMatchId) || 0),
      scopeFragments,
      shotCount: groupSegments.length || 1,
      eventDuration: groupSegments.reduce((sum, segment) => sum + Math.max(0, Number(segment.duration || (Number(segment.end) - Number(segment.start)) || 0)), 0),
    };
  }).filter(Boolean).sort((left, right) => Number(left.item.start) - Number(right.item.start)) : [];

  const maximumLabelLanes = contentHistorySplit ? 2 : densityProfile.maximumLabelLanes;
  const laneEnds = [];
  labelEntries.forEach((entry) => {
    const fixedLane = contentHistorySplit ? (entry.item?._contentSearchHistorical ? 1 : 0) : null;
    const place = () => {
      let best = null;
      const lanes = fixedLane == null
        ? Array.from({ length: maximumLabelLanes }, (_, lane) => lane)
        : [fixedLane];
      for (const lane of lanes) {
        const occupied = Number(laneEnds[lane] || -6);
        const left = Math.max(entry.left, occupied + 6);
        if (left + entry.width > trackWidth) continue;
        const shift = Math.abs(left - entry.left);
        if (!best || shift < best.shift) best = { lane, left, shift };
      }
      return best;
    };
    let placement = place();
    if (!placement && !timelineDetailMode) {
      entry.narrow = true;
      entry.width = 72;
      entry.left = Math.max(0, Math.min(trackWidth - entry.width, entry.anchor - entry.width / 2));
      placement = place();
    }
    if (!placement && !timelineDetailMode) {
      entry.narrow = false;
      entry.compact = true;
      entry.width = 42;
      entry.left = Math.max(0, Math.min(trackWidth - entry.width, entry.anchor - entry.width / 2));
      placement = place();
    }
    if (!placement) {
      const lane = fixedLane == null
        ? (laneEnds.length < maximumLabelLanes ? laneEnds.length : laneEnds.indexOf(Math.min(...laneEnds)))
        : fixedLane;
      placement = { lane: Math.max(0, lane), left: Math.max(0, Math.min(trackWidth - entry.width, entry.left)), shift: 0 };
    }
    entry.lane = placement.lane;
    entry.left = placement.left;
    laneEnds[entry.lane] = Math.max(laneEnds[entry.lane] || 0, entry.left + entry.width);
  });
  const labelLanes = contentHistorySplit
    ? 2
    : Math.max(1, Math.min(maximumLabelLanes, laneEnds.length || 1));
  const labelVerticalOffset = contentHistorySplit ? 0 : -4;
  const hasSourceReviewItems = items.some((item) => !item._output);
  const numberedShots = contentReviewSingleLayer
    ? []
    : outputComparison
    ? items.filter((item) => item._output && !item._outputIntro)
    : items.filter((item) => !hasSourceReviewItems || !item._output);
  numberedShots.forEach((item, index) => {
    item._timelineShotNumber = Number(item._compositionOrder || index + 1);
    item._timelineShotAnchor = Math.max(0, Math.min(trackWidth, timelinePercentInView(item.start) / 100 * trackWidth));
    item._timelineShotMarkerText = String(item._timelineShotNumber).padStart(2, "0");
    item._timelineShotMarkerRole = timelineShotCaption(item);
    item._timelineShotClusterCount = 0;
    item._timelineShotMarkerHidden = false;
  });

  const shotLaneIntervals = Array.from({ length: densityProfile.maximumShotLanes }, () => []);
  numberedShots.forEach((item) => {
    const markerText = String(item._timelineShotMarkerText || item._timelineShotNumber || "");
    const markerRole = String(item._timelineShotMarkerRole || "镜头");
    const badgeWidth = Math.min(trackWidth, Math.min(210, Math.max(112, markerText.length * 10.5 + Array.from(markerRole).length * 12 + 38)));
    const visibleStart = Math.max(view.start, Number(item.start) || 0);
    const visibleEnd = Math.min(view.end, Number(item.end) || visibleStart);
    const rangeStart = Math.max(0, Math.min(trackWidth, timelinePercentInView(visibleStart) / 100 * trackWidth));
    const rangeEnd = Math.max(rangeStart, Math.min(trackWidth, timelinePercentInView(visibleEnd) / 100 * trackWidth));
    const anchor = Math.max(0, Math.min(trackWidth, (rangeStart + rangeEnd) / 2));
    // Preserve the temporal centre. Clamping happens only at viewport edges;
    // the SVG anchor line below points back to the exact source-time centre.
    const badgeLeft = Math.max(0, Math.min(trackWidth - badgeWidth, anchor - badgeWidth / 2));
    const badgeRight = badgeLeft + badgeWidth;
    const laneScores = shotLaneIntervals.map((intervals) => intervals.reduce((score, interval) => (
      score + Math.max(0, Math.min(badgeRight, interval.right) - Math.max(badgeLeft, interval.left))
    ), 0));
    let lane = laneScores.findIndex((score) => score === 0);
    if (lane < 0) lane = laneScores.indexOf(Math.min(...laneScores));
    item._timelineShotLane = Math.max(0, lane);
    item._timelineShotBadgeLeft = badgeLeft;
    item._timelineShotBadgeWidth = badgeWidth;
    item._timelineShotMarkerTarget = anchor;
    item._timelineShotRangeStart = rangeStart;
    item._timelineShotRangeEnd = rangeEnd;
    shotLaneIntervals[item._timelineShotLane].push({ left: badgeLeft, right: badgeRight });
  });
  const usedShotLanes = numberedShots.length
    ? Math.max(...numberedShots.map((item) => Number(item._timelineShotLane || 0))) + 1
    : 1;
  const shotLanes = Math.max(1, Math.min(densityProfile.maximumShotLanes, usedShotLanes));
  const layout = timelineTrackLayout(
    viewportHeight, timelineDetailMode, labelLanes, shotLanes, timelinePresentation.layoutKind,
  );
  applyTimelineTrackGeometry(timelinePresentation, layout);
  labelLaneHeight = layout.labelLaneHeight;
  shotLaneHeight = layout.shotLaneHeight;
  const relationLaneHeight = layout.relationHeight;
  const eventTrackHeight = layout.eventRowHeight + layout.shotRowHeight;
  timelineViewport?.style.setProperty("--timeline-event-label-lanes", String(labelLanes));
  timelineViewport?.style.setProperty("--timeline-event-labels-height", `${layout.eventLabelsHeight}px`);
  timelineViewport?.style.setProperty("--timeline-event-card-height", `${layout.eventCardHeight}px`);
  timelineViewport?.style.setProperty("--timeline-event-row-height", `${layout.eventRowHeight}px`);
  timelineViewport?.style.setProperty("--timeline-event-relations-height", `${relationLaneHeight}px`);
  timelineViewport?.style.setProperty("--timeline-shot-track-height", `${layout.shotRowHeight}px`);
  timelineViewport?.style.setProperty("--timeline-shot-card-height", `${layout.shotCardHeight}px`);
  timelineViewport?.style.setProperty("--timeline-event-track-height", `${eventTrackHeight}px`);
  timelineViewport?.style.setProperty("--timeline-picture-track-top", `${layout.pictureTop}px`);
  timelineViewport?.style.setProperty("--timeline-picture-track-height", `${layout.pictureHeight}px`);
  timelineViewport?.style.setProperty("--timeline-audio-track-top", `${layout.audioTop}px`);
  timelineViewport?.style.setProperty("--timeline-audio-track-height", `${layout.audioHeight}px`);
  timelineViewport?.style.setProperty("--timeline-ruler-height", `${layout.rulerHeight}px`);
  timelineViewport?.style.setProperty("--timeline-review-min-height", "190px");
  timelineViewport?.setAttribute("data-event-label-lanes", String(labelLanes));
  timelineViewport?.setAttribute("data-event-relation-height", String(relationLaneHeight));
  timelineViewport?.setAttribute("data-shot-marker-lanes", String(shotLanes));
  timelineViewport?.setAttribute("data-timeline-mode", timelineDetailMode ? "detail" : "overview");
  timelineViewport?.setAttribute("data-timeline-density", layout.name);
  const relations = $("#timelineEventRelations");
  if (relations) {
    relations.setAttribute("viewBox", `0 0 ${trackWidth} ${viewportHeight}`);
    relations.setAttribute("width", String(trackWidth));
    relations.setAttribute("height", String(viewportHeight));
    const relationTargets = numberedShots.map((item) => {
      const position = items.indexOf(item);
      const relation = timelineRelationForItem(item, position);
      if (Number(item.end) <= view.start || Number(item.start) >= view.end) return null;
      const shotLane = Number(item._timelineShotLane || 0);
      const shotTop = layout.eventRowHeight + shotLane * shotLaneHeight + Math.max(2, (shotLaneHeight - layout.shotCardHeight) / 2);
      const markerCenter = Number(item._timelineShotBadgeLeft || 0) + Number(item._timelineShotBadgeWidth || 0) / 2;
      return {
        item,
        position,
        relationKey: relation.relationKey,
        x: Math.max(0, Math.min(trackWidth, markerCenter)),
        y: shotTop,
        lineY: Math.min(layout.eventRowHeight + layout.shotRowHeight - 3, shotTop + layout.shotCardHeight + 3),
        rangeStart: Number(item._timelineShotRangeStart || 0),
        rangeEnd: Number(item._timelineShotRangeEnd || 0),
      };
    }).filter(Boolean);
    const bandMarkup = contentReviewSingleLayer ? "" : labelEntries.flatMap((entry) => {
      const stateClasses = `${entry.isRecommendedEvent ? " recommended" : ""}${entry.active ? " active" : ""}${outputComparison ? (entry.usedInOutput ? " used-in-output" : " unused-in-output") : ""}`;
      return entry.scopeFragments.map((fragment) => {
        const x = fragment.left / 100 * trackWidth;
        const width = Math.max(3, fragment.width / 100 * trackWidth);
        return `<rect class="timeline-event-band${stateClasses}" data-timeline-relation="${entry.relationKey}" x="${x.toFixed(2)}" y="${layout.eventRowHeight.toFixed(2)}" width="${width.toFixed(2)}" height="${layout.shotRowHeight.toFixed(2)}" rx="6" ry="6"></rect>`;
      });
    }).join("");
    const shotRangeMarkup = relationTargets.map((target) => {
      const parentEntry = labelEntries.find((entry) => entry.relationKey === target.relationKey);
      const isActive = timelineItemIsActive(target.item);
      const stateClasses = `${parentEntry?.isRecommendedEvent ? " recommended" : ""}${isActive ? " active" : ""}${outputComparison && target.item._output ? " used-in-output" : ""}`;
      const start = Math.max(0, Math.min(trackWidth, target.rangeStart));
      const end = Math.max(start, Math.min(trackWidth, target.rangeEnd));
      const markerTarget = Math.max(start, Math.min(end || start, Number(target.item._timelineShotMarkerTarget || target.x)));
      return `<g class="timeline-shot-range${stateClasses}"${target.relationKey ? ` data-timeline-relation="${target.relationKey}"` : ""}><line class="timeline-shot-range-line" x1="${start.toFixed(2)}" y1="${target.lineY.toFixed(2)}" x2="${end.toFixed(2)}" y2="${target.lineY.toFixed(2)}"></line><line class="timeline-shot-centre-guide" x1="${target.x.toFixed(2)}" y1="${(target.y + layout.shotCardHeight).toFixed(2)}" x2="${markerTarget.toFixed(2)}" y2="${target.lineY.toFixed(2)}"></line><circle class="timeline-shot-range-end start" cx="${start.toFixed(2)}" cy="${target.lineY.toFixed(2)}" r="2.5"></circle><circle class="timeline-shot-range-end end" cx="${end.toFixed(2)}" cy="${target.lineY.toFixed(2)}" r="2.5"></circle></g>`;
    }).join("");
    const curveMarkup = contentMode ? "" : labelEntries.flatMap((entry) => {
      const stateClasses = `${entry.isRecommendedEvent ? " recommended" : ""}${entry.active ? " active" : ""}${outputComparison ? (entry.usedInOutput ? " used-in-output" : " unused-in-output") : ""}`;
      const labelTop = Math.max(0, entry.lane * labelLaneHeight + Math.max(0, (labelLaneHeight - layout.eventCardHeight) / 2) + labelVerticalOffset);
      const startX = Math.max(0, Math.min(trackWidth, entry.left + entry.width / 2));
      const startY = labelTop + layout.eventCardHeight;
      return relationTargets.filter((target) => target.relationKey === entry.relationKey).map((target) => {
        const verticalGap = Math.max(8, target.y - startY);
        const bend = Math.max(8, Math.min(34, verticalGap * .52));
        const path = `M ${startX.toFixed(2)} ${startY.toFixed(2)} C ${startX.toFixed(2)} ${(startY + bend).toFixed(2)}, ${target.x.toFixed(2)} ${(target.y - bend).toFixed(2)}, ${target.x.toFixed(2)} ${target.y.toFixed(2)}`;
        return `<path class="timeline-event-curve${stateClasses}" data-timeline-relation="${entry.relationKey}" d="${path}"></path>`;
      });
    }).join("");
    const connectionPointMarkup = contentMode ? "" : labelEntries.flatMap((entry) => {
      const targets = relationTargets.filter((target) => target.relationKey === entry.relationKey);
      if (!targets.length) return [];
      const stateClasses = `${entry.isRecommendedEvent ? " recommended" : ""}${entry.active ? " active" : ""}${outputComparison ? (entry.usedInOutput ? " used-in-output" : " unused-in-output") : ""}`;
      const labelTop = Math.max(0, entry.lane * labelLaneHeight + Math.max(0, (labelLaneHeight - layout.eventCardHeight) / 2) + labelVerticalOffset);
      const startX = Math.max(0, Math.min(trackWidth, entry.left + entry.width / 2));
      const startY = labelTop + layout.eventCardHeight;
      const eventPoint = `<circle class="timeline-connection-point event-point${stateClasses}" data-timeline-relation="${entry.relationKey}" cx="${startX.toFixed(2)}" cy="${startY.toFixed(2)}" r="3.6"></circle>`;
      const shotPoints = targets.map((target) => `<circle class="timeline-connection-point shot-point${stateClasses}" data-timeline-relation="${entry.relationKey}" cx="${target.x.toFixed(2)}" cy="${target.y.toFixed(2)}" r="3.2"></circle>`);
      return [eventPoint, ...shotPoints];
    }).join("");
    relations.innerHTML = `${bandMarkup}${shotRangeMarkup}${curveMarkup}${connectionPointMarkup}`;
  }
  const linkedRanges = $("#timelineLinkedRanges");
  if (linkedRanges) {
    const linkedKind = currentOutput?.segments?.length ? "output" : currentEventSegment || currentCandidate ? "shot" : "event";
    linkedRanges.innerHTML = currentTimelineLinkedIntervals()
      .filter((range) => range.end > view.start && range.start < view.end)
      .map((range, index) => {
        const start = Math.max(view.start, range.start);
        const end = Math.min(view.end, range.end);
        return `<i class="timeline-linked-range ${linkedKind}"${linkedKind === "output" ? ` data-output-segment-index="${index}"` : ""} style="left:${timelinePercentInView(start)}%;width:${Math.max(.2, (end - start) / view.duration * 100)}%"></i>`;
      }).join("");
  }
  if (labels) labels.innerHTML = labelEntries.map((entry) => {
    const anchorOffset = Math.max(8, Math.min(entry.width - 8, entry.anchor - entry.left));
    const range = `${formatTime(entry.groupStart)} → ${formatTime(entry.groupEnd)}`;
    const retainedSearch = contentMode && Boolean(entry.item?._contentSearchHistorical);
    const retainedQuery = String(entry.item?._contentSearchQuery || "上次检索");
    const retainedQueryShort = Array.from(retainedQuery).length > 10
      ? `${Array.from(retainedQuery).slice(0, 10).join("")}…`
      : retainedQuery;
    const sequenceLabel = entry.item?._outputIntro
      ? "片头"
      : contentMode
      ? (outputComparison
          ? `P${String(entry.eventNumber || entry.position + 1).padStart(2, "0")}`
          : retainedSearch ? "保留" : entry.outputOrder ? `P${String(entry.outputOrder).padStart(2, "0")}` : "候选")
      : entry.eventNumber ? `E${entry.eventNumber}` : `C${entry.position + 1}`;
    const kindLabel = contentMode
      ? retainedSearch ? `已保留检索“${retainedQuery}”的匹配片段` : `当前检索匹配片段 ${sequenceLabel}`
      : entry.eventNumber ? `事件 ${sequenceLabel}` : `候选 ${sequenceLabel}`;
    const duplicateFact = entry.duplicateCount ? `已折叠 ${entry.duplicateCount} 个重复分析结果` : "";
    const containmentFact = retainedSearch && entry.item?._containedByCurrentId
      ? "位于当前检索片段的时间范围内"
      : Number(entry.item?._containedHistoricalCount || 0) > 0
        ? `时间范围覆盖 ${Number(entry.item._containedHistoricalCount)} 个已保留片段`
        : "";
    const labelTop = entry.lane * labelLaneHeight + Math.max(0, (labelLaneHeight - layout.eventCardHeight) / 2) + labelVerticalOffset;
    const comparisonClass = outputComparison ? (entry.usedInOutput ? " used-in-output" : " unused-in-output") : "";
    const statusBadge = retainedSearch
      ? `<em title="${escapeHtml(retainedQuery)}">上次 · ${escapeHtml(retainedQueryShort)}</em>`
      : outputComparison
      ? (entry.usedInOutput && !entry.compact && !entry.narrow ? "<em>已采用</em>" : "")
      : (entry.isRecommendedEvent && !entry.compact && !entry.narrow ? `<em>${contentMode ? "已选" : "推荐"}</em>` : "");
    const countFact = contentMode ? "" : `${entry.shotCount} 个镜头`;
    return `<button type="button" class="timeline-label${contentMode ? " content-match" : ""}${retainedSearch ? " retained-search" : ""}${entry.item?._containedByCurrentId ? " contained-by-current" : ""}${Number(entry.item?._containedHistoricalCount || 0) > 0 ? " contains-retained" : ""}${entry.narrow ? " narrow" : ""}${entry.compact ? " compact" : ""}${entry.isRecommendedEvent ? " recommended" : ""}${entry.active ? " active" : ""}${comparisonClass}" data-timeline-label-position="${entry.position}" data-timeline-relation="${entry.relationKey}" data-timeline-search-track="${retainedSearch ? "retained" : "current"}"${entry.item?._contentSearchId ? ` data-content-search-id="${escapeHtml(entry.item._contentSearchId)}"` : ""} style="--timeline-label-left:${entry.left}px;--timeline-label-width:${entry.width}px;--timeline-label-top:${Math.max(0, labelTop)}px;--timeline-label-anchor:${anchorOffset}px" title="${escapeHtml([kindLabel, entry.title, countFact, outputComparison ? (entry.usedInOutput ? `当前${contentMode ? "内容视频" : "成片"}已采用` : `当前${contentMode ? "内容视频" : "成片"}未采用`) : "", range, containmentFact, duplicateFact, entry.detail].filter(Boolean).join(" · "))}" aria-label="${escapeHtml([kindLabel, entry.title, retainedSearch ? "已保留但未自动加入本次生成" : outputComparison ? (entry.usedInOutput ? "当前版本已采用" : "当前版本未采用") : (entry.isRecommendedEvent ? (contentMode ? "已选" : "AI 推荐") : "备选"), containmentFact, duplicateFact, entry.detail].filter(Boolean).join("，"))}"><b>${sequenceLabel}</b><span>${escapeHtml(entry.title)}</span>${statusBadge}</button>`;
  }).join("");
  const highlightTimelineRelation = (relationKey, highlighted) => {
    timelineTrackContent?.querySelectorAll("[data-timeline-relation]").forEach((element) => {
      element.classList.toggle("relation-hover", Boolean(highlighted && element.dataset.timelineRelation === relationKey));
    });
  };
  const bindTimelineRelationHover = (root) => root?.querySelectorAll("[data-timeline-relation]").forEach((button) => {
    const toggle = (active) => highlightTimelineRelation(button.dataset.timelineRelation, active);
    button.addEventListener("pointerenter", () => toggle(true));
    button.addEventListener("pointerleave", () => toggle(false));
    button.addEventListener("focus", () => toggle(true));
    button.addEventListener("blur", () => toggle(false));
  });
  labels?.querySelectorAll("[data-timeline-label-position]").forEach((button) => button.addEventListener("click", () => {
    const item = items[Number(button.dataset.timelineLabelPosition)];
    timelineFrameSelectionTime = null;
    if (item?._output) selectOutputTimelineSegment(Number(item._outputSegmentIndex), Number(item.start));
    else if (item?._outputEvent) selectOutputTimelineSegment(Number(item._outputSegmentIndex), Number(item.start));
    else if (contentMode && !item?._group) {
      previewContentMatch(item, { autoplay: true, loadEvidence: false, searchId: item?._contentSearchId || "" });
    }
    else if (item?._group) outputComparison || contentMode
      ? previewEventSegment(item._group, item)
      : previewEventGroup(item._group);
    else if (currentJob.status === "awaiting_confirmation") previewCandidate(Number(item.index));
    else if (item?.filename) selectOutput(item.filename, true);
    else seekTimeline(Number(item?.start || 0));
  }));
  bindTimelineRelationHover(labels);
  const clips = $("#timelineClips");
  if (clips) clips.innerHTML = duration > 0 && !contentReviewSingleLayer ? items.map((item, position) => {
    if (outputComparison && !item._output) return "";
    const isCandidate = currentJob.status === "awaiting_confirmation";
    const active = timelineItemIsActive(item);
    const groupActive = Boolean(item._group && activeCanonicalEventId === String(item._group.id || ""));
    const candidateRecommended = !item._group && recommended.has(item.index);
    const classes = ["timeline-clip", item._sourceEvent ? "source-event" : (isCandidate ? "candidate" : "output"), outputComparison && item._output ? "comparison-output-shot" : "", candidateRecommended ? "recommended" : "", groupActive ? "group-active" : "", active ? "active" : ""].filter(Boolean).join(" ");
    if (Number(item.end) <= view.start || Number(item.start) >= view.end) return "";
    const shotCaption = timelineShotCaption(item);
    const clipTitle = [item._group?.title || item.chapterTitle || item.title, shotCaption, item.role || "精彩镜头"].filter(Boolean).join(" · ");
    const clipDescription = item.reason || item.summary || item._group?.summary || "";
    const shotNumber = Number(item._timelineShotNumber || 0);
    const shotLane = Number(item._timelineShotLane || 0);
    const clipTop = layout.eventRowHeight + shotLane * shotLaneHeight + Math.max(2, (shotLaneHeight - layout.shotCardHeight) / 2);
    const { eventNumber, relationKey } = timelineRelationForItem(item, position);
    const outputSegmentAttribute = item._output ? ` data-output-segment-index="${Number(item._outputSegmentIndex)}"` : "";
    return `<button type="button" class="${classes}" data-timeline-position="${position}"${outputSegmentAttribute} data-event-sequence="${eventNumber || ""}"${relationKey ? ` data-timeline-relation="${relationKey}"` : ""} style="left:${timelinePercentInView(item.start)}%;width:${Math.max(.2, (Number(item.end) - Number(item.start)) / view.duration * 100)}%;--timeline-clip-top:${clipTop}px" title="${escapeHtml([outputComparison ? `${contentMode ? "内容视频" : "成片"}顺序 ${String(shotNumber).padStart(2, "0")}` : (eventNumber ? `${contentMode ? "片段 P" : "事件 E"}${eventNumber}` : ""), clipTitle, `${formatTime(item.start)} → ${formatTime(item.end)}`, clipDescription].filter(Boolean).join(" · "))}" aria-label="${escapeHtml([outputComparison ? `${contentMode ? "内容视频" : "成片"}顺序 ${shotNumber}` : (eventNumber ? `${contentMode ? "片段 P" : "事件 E"}${eventNumber}` : ""), clipTitle, clipDescription].filter(Boolean).join("，"))}"></button>`;
  }).join("") : "";
  clips?.querySelectorAll("[data-timeline-position]").forEach((button) => button.addEventListener("click", (event) => {
    event.stopPropagation();
    const item = items[Number(button.dataset.timelinePosition)];
    timelineFrameSelectionTime = null;
    if (item?._output) selectOutputTimelineSegment(Number(item._outputSegmentIndex), Number(item.start));
    else if (item._group) previewEventSegment(item._group, item);
    else if (currentJob.status === "awaiting_confirmation") previewCandidate(Number(item.index));
    else selectOutput(item.filename, true);
  }));
  bindTimelineRelationHover(clips);
  const markers = $("#timelineShotMarkers");
  if (markers) markers.innerHTML = duration > 0 && !contentReviewSingleLayer ? items.map((item, position) => {
    if (outputComparison && !item._output) return "";
    const shotNumber = Number(item._timelineShotNumber || 0);
    const showMarker = shotNumber && Number(item.end) > view.start && Number(item.start) < view.end;
    if (!showMarker) return "";
    const { eventNumber, relationKey } = timelineRelationForItem(item, position);
    const active = timelineItemIsActive(item);
    const groupActive = Boolean(item._group && activeCanonicalEventId === String(item._group.id || ""));
    const markerText = String(item._timelineShotMarkerText || shotNumber);
    const markerRole = String(item._timelineShotMarkerRole || timelineShotRole(item));
    const markerWidth = Number(item._timelineShotBadgeWidth || 112);
    const markerTop = layout.eventRowHeight + Number(item._timelineShotLane || 0) * shotLaneHeight + Math.max(2, (shotLaneHeight - layout.shotCardHeight) / 2);
    const markerClasses = ["timeline-shot-marker", outputComparison && item._output ? "comparison-output-shot" : "", groupActive ? "group-active" : "", active ? "active" : ""].filter(Boolean).join(" ");
    const visibleMarkerNumber = `${contentMode ? (outputComparison ? "顺序" : "片段") : "镜头"} ${markerText}`;
    const markerContent = `<b>${escapeHtml(visibleMarkerNumber)}</b><em>${escapeHtml(markerRole)}</em>`;
    const title = [eventNumber ? `${contentMode ? "片段 P" : "事件 E"}${eventNumber}` : "", `${contentMode ? "内容片段" : "镜头"} ${shotNumber}`, markerRole, `${formatTime(item.start)} → ${formatTime(item.end)}`, item.reason || item.summary || item._group?.summary || ""].filter(Boolean).join(" · ");
    return `<button type="button" class="${markerClasses}" data-timeline-marker-position="${position}"${item._output ? ` data-output-segment-index="${Number(item._outputSegmentIndex)}"` : ""}${relationKey ? ` data-timeline-relation="${relationKey}"` : ""} style="--timeline-shot-marker-left:${Number(item._timelineShotBadgeLeft || 0)}px;--timeline-shot-marker-top:${markerTop}px;--timeline-shot-marker-width:${markerWidth}px;--timeline-shot-anchor-offset:${Number(item._timelineShotMarkerTarget || 0) - Number(item._timelineShotBadgeLeft || 0)}px" title="${escapeHtml(title)}" aria-label="${escapeHtml(title)}">${markerContent}</button>`;
  }).join("") : "";
  markers?.querySelectorAll("[data-timeline-marker-position]").forEach((button) => button.addEventListener("click", (event) => {
    event.stopPropagation();
    const item = items[Number(button.dataset.timelineMarkerPosition)];
    timelineFrameSelectionTime = null;
    if (item?._output) selectOutputTimelineSegment(Number(item._outputSegmentIndex), Number(item.start));
    else if (item?._group) previewEventSegment(item._group, item);
    else if (currentJob.status === "awaiting_confirmation") previewCandidate(Number(item.index));
    else if (item?.filename) selectOutput(item.filename, true);
    else seekTimeline(Number(item?.start || 0));
  }));
  bindTimelineRelationHover(markers);
  updateTimelineSelection();
  updateTimelinePlayhead();
  renderTimelineOverview(items);
  updateSpeakerFilterOptions(currentJob);
  renderTimelineInsights(currentJob);
  renderTimelineMediaAssets();
  drawWaveform();
  renderTimelineEventSummary();
  updateTimelineReviewControls();
  renderTimelineProposalPreview(currentJob);
  renderTimelineCoverTrack(currentJob);
  renderTimelineSpeakerTrack();
  renderTimelinePersonTrack();
  syncPrecisionTimelineToolbar();
}

window.ClipTalkOpenCoverTimeline = () => {
  if (!currentJob || !(currentJob.coverDraft?.variants || []).length) {
    showToast("封面候选尚未准备完成");
    return false;
  }
  setReviewLowerPanelMode("timeline", { scroll: true });
  renderTimelineCoverTrack(currentJob);
  window.requestAnimationFrame(() => $("#timelineCoverTrack")?.scrollIntoView({ behavior: "smooth", block: "nearest" }));
  return true;
};

async function loadWaveform(job) {
  if (!job || waveformLoadingJobId === job.id || Date.now() < waveformRetryAt) return;
  const loadedForJob = waveformJobId === job.id && waveformData && (
    waveformData.schemaVersion || waveformData.hasAudio === false
    || waveformData.rms?.length || waveformData.maximums?.length || waveformData.peaks?.length
  );
  if (loadedForJob) return;
  waveformLoadingJobId = job.id;
  waveformJobId = job.id;
  waveformData = job.videoInfo ? { duration: Number(job.videoInfo.duration), hasAudio: job.videoInfo.has_audio, peaks: [], rms: [] } : null;
  const token = ++waveformRequestToken;
  setWaveformState("正在准备音频波形…", "loading");
  const slowNotice = window.setTimeout(() => {
    if (token === waveformRequestToken && currentJob?.id === job.id) {
      setWaveformState("仍在准备音频波形，长视频需要更多时间", "loading");
    }
  }, 12000);
  updateTimeline();
  try {
    const data = await api(`/api/jobs/${job.id}/waveform`);
    if (token !== waveformRequestToken || currentJob?.id !== job.id) {
      if (waveformJobId === job.id && !(waveformData?.schemaVersion)) waveformJobId = null;
      return;
    }
    waveformJobId = job.id;
    waveformData = data;
    waveformRetryAt = 0;
    if (!timelineViewEnd) timelineViewEnd = Number(data.duration);
    setWaveformState(data.hasAudio ? "音频波形" : "源视频没有音轨", "success");
    updateTimeline();
  } catch (error) {
    if (token !== waveformRequestToken) return;
    if (currentJob?.id === job.id) {
      waveformJobId = null;
      waveformRetryAt = Date.now() + 5000;
    }
    setWaveformState(`波形不可用：${error.message}`, "error");
    updateTimeline();
  } finally {
    window.clearTimeout(slowNotice);
    if (waveformLoadingJobId === job.id) waveformLoadingJobId = null;
    if (secondaryEditorOpen() && currentJob?.id === job.id) renderSecondaryEditorTimeline();
  }
}

let activeActionConfirmationCancel = null;
let activeInputPromptCancel = null;
let coverImagePreviewReleaseFocus = null;
let coverImagePreviewKeyHandler = null;

function activateModalFocus(modal, { initialFocus = null, additionalActive = [], restoreFocus = true } = {}) {
  if (!modal) return () => {};
  const returnTarget = document.activeElement instanceof window.HTMLElement ? document.activeElement : null;
  const returnFocusTarget = returnTarget?.closest("#sidebarHistoryDrawer")
    ? document.querySelector("#sidebarHistoryToggle")
    : returnTarget;
  window.ClipTalkAppShell?.closeHistoryDrawer?.();
  const activeRoots = new Set([modal, ...additionalActive.filter(Boolean)]);
  // Include the global rail explicitly. Compatibility layouts can briefly
  // nest it inside the workspace while a modal is opening.
  const isolationCandidates = [...new Set([
    ...document.body.children,
    document.querySelector("#appSidebar"),
  ].filter(Boolean))];
  const isolated = isolationCandidates
    .filter((node) => ![...activeRoots].some((root) => node === root || node.contains(root)))
    .map((node) => ({ node, inert: node.inert }));
  isolated.forEach(({ node }) => { node.inert = true; });
  const focusable = () => [...modal.querySelectorAll(
    'button:not(:disabled), [href], input:not(:disabled), select:not(:disabled), textarea:not(:disabled), [tabindex]:not([tabindex="-1"])',
  )].filter((node) => !node.closest(".hidden") && node.getClientRects().length);
  const onKeydown = (event) => {
    if (event.key !== "Tab") return;
    const items = focusable();
    if (!items.length) {
      event.preventDefault();
      modal.focus();
      return;
    }
    const first = items[0];
    const last = items.at(-1);
    if (event.shiftKey && document.activeElement === first) {
      event.preventDefault();
      last.focus();
    } else if (!event.shiftKey && document.activeElement === last) {
      event.preventDefault();
      first.focus();
    }
  };
  modal.addEventListener("keydown", onKeydown);
  window.requestAnimationFrame(() => {
    const target = typeof initialFocus === "string" ? modal.querySelector(initialFocus) : initialFocus;
    (target instanceof window.HTMLElement ? target : focusable()[0] || modal).focus();
  });
  return (shouldRestore = restoreFocus) => {
    modal.removeEventListener("keydown", onKeydown);
    isolated.forEach(({ node, inert }) => { node.inert = inert; });
    if (shouldRestore && returnFocusTarget?.isConnected) returnFocusTarget.focus();
  };
}

function closeCoverImagePreview() {
  const modal = $("#coverImagePreview");
  if (!modal || modal.classList.contains("hidden")) return;
  modal.classList.add("hidden");
  modal.setAttribute("aria-hidden", "true");
  modal.dataset.overlayState = "closed";
  $("#coverImagePreviewImage")?.removeAttribute("src");
  if (coverImagePreviewKeyHandler) document.removeEventListener("keydown", coverImagePreviewKeyHandler);
  coverImagePreviewKeyHandler = null;
  coverImagePreviewReleaseFocus?.();
  coverImagePreviewReleaseFocus = null;
}

function openCoverImagePreview(url, title = "封面大图预览", meta = "") {
  const modal = $("#coverImagePreview");
  const image = $("#coverImagePreviewImage");
  if (!modal || !image || !url) return false;
  window.ClipTalkPrepareWorkspaceOverlay?.("cover");
  closeCoverImagePreview();
  image.src = url;
  image.alt = `${title}的大图预览`;
  $("#coverImagePreviewTitle").textContent = title;
  $("#coverImagePreviewMeta").textContent = meta;
  modal.classList.remove("hidden");
  modal.setAttribute("aria-hidden", "false");
  modal.dataset.overlayState = "open";
  coverImagePreviewReleaseFocus = activateModalFocus(modal, {
    initialFocus: modal.querySelector(".cover-image-preview-card header button"),
  });
  coverImagePreviewKeyHandler = (event) => {
    if (event.key === "Escape") closeCoverImagePreview();
  };
  document.addEventListener("keydown", coverImagePreviewKeyHandler);
  modal.querySelectorAll("[data-cover-image-preview-close]").forEach((button) => {
    button.onclick = closeCoverImagePreview;
  });
  return true;
}

function dismissActionConfirmation() {
  if (activeActionConfirmationCancel) activeActionConfirmationCancel();
  else {
    $("#actionConfirm")?.classList.add("hidden");
    $("#actionConfirm")?.setAttribute("aria-hidden", "true");
  }
}

function dismissInputPrompt() {
  if (activeInputPromptCancel) activeInputPromptCancel();
  else {
    $("#inputPrompt")?.classList.add("hidden");
    $("#inputPrompt")?.setAttribute("aria-hidden", "true");
  }
}

function requestInputPrompt({
  kicker = "填写信息",
  title = "请输入",
  summary = "",
  label = "内容",
  value = "",
  placeholder = "",
  hint = "",
  confirmLabel = "保存",
  required = false,
  maxLength = 120,
  options = [],
  selectLabel = "选择目标",
  selectHint = "",
} = {}) {
  dismissInputPrompt();
  return new Promise((resolve) => {
    const modal = $("#inputPrompt");
    const form = $("#inputPromptForm");
    const input = $("#inputPromptInput");
    const select = $("#inputPromptSelect");
    if (!modal || !form || !input || !select) return resolve(null);
    const selecting = Array.isArray(options) && options.length > 0;
    $("#inputPromptKicker").textContent = kicker;
    $("#inputPromptTitle").textContent = title;
    $("#inputPromptSummary").textContent = summary;
    $("#inputPromptLabel").textContent = label;
    $("#inputPromptHint").textContent = hint;
    $("#inputPromptSelectLabel").textContent = selectLabel;
    $("#inputPromptSelectHint").textContent = selectHint;
    $("#inputPromptOk").textContent = confirmLabel;
    $("#inputPromptField").classList.toggle("hidden", selecting);
    $("#inputPromptSelectField").classList.toggle("hidden", !selecting);
    input.value = String(value ?? "");
    input.placeholder = placeholder;
    input.required = Boolean(required);
    input.maxLength = Math.max(1, Number(maxLength) || 120);
    select.innerHTML = selecting
      ? options.map((option) => `<option value="${escapeHtml(String(option.value))}">${escapeHtml(String(option.label))}</option>`).join("")
      : "";
    if (selecting && value != null && [...select.options].some((option) => option.value === String(value))) {
      select.value = String(value);
    }
    modal.classList.remove("hidden");
    modal.setAttribute("aria-hidden", "false");
    const releaseFocus = activateModalFocus(modal, { initialFocus: selecting ? select : input });
    const finish = (result) => {
      modal.classList.add("hidden");
      modal.setAttribute("aria-hidden", "true");
      cleanup();
      resolve(result);
    };
    const onSubmit = (event) => {
      event.preventDefault();
      const result = selecting ? select.value : input.value.trim();
      if (required && !result) {
        input.focus();
        return;
      }
      finish(result);
    };
    const onCancel = () => finish(null);
    const onKey = (event) => { if (event.key === "Escape") onCancel(); };
    const cleanup = () => {
      form.removeEventListener("submit", onSubmit);
      $("#inputPromptCancel").removeEventListener("click", onCancel);
      $("#inputPromptClose").removeEventListener("click", onCancel);
      document.removeEventListener("keydown", onKey);
      if (activeInputPromptCancel === onCancel) activeInputPromptCancel = null;
      releaseFocus();
    };
    activeInputPromptCancel = onCancel;
    form.addEventListener("submit", onSubmit);
    $("#inputPromptCancel").addEventListener("click", onCancel);
    $("#inputPromptClose").addEventListener("click", onCancel);
    document.addEventListener("keydown", onKey);
  });
}

function requestTextInput(options) {
  return requestInputPrompt(options);
}

function requestChoice({ options, ...configuration }) {
  return requestInputPrompt({ ...configuration, options });
}

function requestActionConfirmation({ title, summary, details = [], warning = "", confirmLabel = "确认执行", tone = "", orderMode = null, orderItems = [], showOrderOptions = false, selectionItems = [], onDraftChange = null, exportSnapshot = null }) {
  dismissActionConfirmation();
  return new Promise((resolve) => {
    const modal = $("#actionConfirm");
    if (!modal) return resolve(false);
    if (tone) modal.dataset.tone = tone;
    else delete modal.dataset.tone;
    $("#actionConfirmTitle").textContent = title;
    $("#actionConfirmSummary").textContent = summary;
    $("#actionConfirmDetails").innerHTML = details.map((item) => `<li>${escapeHtml(item)}</li>`).join("");
    const exportOptions = exportSnapshot ? window.ClipTalkDelivery.mountOptions($("#actionConfirmDetails"), exportSnapshot) : null;
    $("#actionConfirmWarning").textContent = warning;
    const orderWrap = $("#actionConfirmOrderWrap");
    const orderSelect = $("#actionConfirmOrder");
    const orderHint = $("#actionConfirmOrderHint");
    const orderList = $("#actionConfirmOrderList");
    const selectionList = $("#actionConfirmSelectionList");
    let mutableOrder = orderItems.map((item) => ({ ...item }));
    const orderHints = {
      selection: "按照你当前勾选/加入选区的先后合成，不会自动调换。",
      source: "按镜头在源视频中的时间先后排序，适合纪实、访谈和过程记录。",
      llm_recommend: "根据内容连贯性调整所选片段顺序，完成后可以再次确认。",
    };
    orderWrap?.classList.toggle("content-order-enabled", Boolean(orderMode && showOrderOptions));
    orderWrap?.classList.toggle("hidden", !orderMode || !showOrderOptions);
    orderList?.classList.toggle("hidden", !orderItems.length);
    if (selectionList) {
      selectionList.classList.toggle("hidden", !selectionItems.length);
      selectionList.innerHTML = selectionItems.map((item, index) => `<label class="${item.disabled ? "disabled" : ""}"><input type="checkbox" data-confirm-selection="${index}" value="${escapeHtml(item.value)}" ${item.checked && !item.disabled ? "checked" : ""} ${item.disabled ? "disabled" : ""}><span><b>${escapeHtml(item.label)}</b><small>${escapeHtml(item.meta || (item.disabled ? "当前不可保留" : "正式成片"))}</small></span></label>`).join("");
    }
    const renderOrderList = () => {
      if (!orderList) return;
      const selectedMode = orderSelect?.value || orderMode;
      const editable = selectedMode === "selection";
      const heading = editable ? "可手动调整镜头顺序" : selectedMode === "llm_recommend" ? "已选镜头 · 智能排序后再次确认" : "已选镜头 · 将按视频时间排序";
      orderList.innerHTML = `<small>${heading}</small>${mutableOrder.map((item, index) => `<div><b>${index + 1}</b><span>${escapeHtml(item.label || item.title || `镜头 ${index + 1}`)}<em>${escapeHtml(item.meta || "")}</em></span>${editable ? `<button type="button" data-order-up="${index}" ${index === 0 ? "disabled" : ""}>↑</button><button type="button" data-order-down="${index}" ${index === mutableOrder.length - 1 ? "disabled" : ""}>↓</button>` : ""}</div>`).join("")}`;
      orderList?.querySelectorAll("[data-order-up]").forEach((button) => button.addEventListener("click", () => { const index = Number(button.dataset.orderUp); [mutableOrder[index - 1], mutableOrder[index]] = [mutableOrder[index], mutableOrder[index - 1]]; renderOrderList(); onDraftChange?.({ orderMode: orderSelect?.value || orderMode, orderedItems: mutableOrder }); }));
      orderList?.querySelectorAll("[data-order-down]").forEach((button) => button.addEventListener("click", () => { const index = Number(button.dataset.orderDown); [mutableOrder[index], mutableOrder[index + 1]] = [mutableOrder[index + 1], mutableOrder[index]]; renderOrderList(); onDraftChange?.({ orderMode: orderSelect?.value || orderMode, orderedItems: mutableOrder }); }));
    };
    if (orderSelect && orderMode) orderSelect.value = orderMode;
    renderOrderList();
    if (orderHint) orderHint.textContent = orderHints[orderSelect?.value || orderMode] || "";
    const confirmButton = $("#actionConfirmOk");
    confirmButton.textContent = confirmLabel;
    const syncSelectionState = () => {
      if (selectionItems.length) confirmButton.disabled = !selectionList?.querySelector("input:not(:disabled):checked");
      else confirmButton.disabled = false;
    };
    selectionList?.querySelectorAll("input").forEach((input) => input.addEventListener("change", syncSelectionState));
    syncSelectionState();
    modal.classList.remove("hidden");
    modal.setAttribute("aria-hidden", "false");
    const releaseFocus = activateModalFocus(modal, { initialFocus: $("#actionConfirmCancel") });
    const finish = (value) => {
      modal.classList.add("hidden");
      modal.setAttribute("aria-hidden", "true");
      cleanup();
      resolve(value);
    };
    const cleanup = () => {
      exportOptions?.remove();
      $("#actionConfirmOk").removeEventListener("click", onOk);
      $("#actionConfirmCancel").removeEventListener("click", onCancel);
      $("#actionConfirmClose").removeEventListener("click", onCancel);
      orderSelect?.removeEventListener("change", onOrderChange);
      selectionList?.querySelectorAll("input").forEach((input) => input.removeEventListener("change", syncSelectionState));
      confirmButton.disabled = false;
      delete modal.dataset.tone;
      document.removeEventListener("keydown", onKey);
      if (activeActionConfirmationCancel === onCancel) activeActionConfirmationCancel = null;
      releaseFocus();
    };
    const onOk = () => {
      if (exportOptions) return finish({ confirmed: true, exportSnapshot: exportOptions.read() });
      if (selectionItems.length) {
        const selectedValues = [...selectionList.querySelectorAll("input:not(:disabled):checked")].map((input) => input.value);
        if (!selectedValues.length) return;
        return finish({ confirmed: true, selectedValues });
      }
      return finish(orderMode ? { confirmed: true, orderMode: orderSelect?.value || orderMode, orderedItems: mutableOrder } : true);
    };
    const onCancel = () => finish(false);
    activeActionConfirmationCancel = onCancel;
    const onOrderChange = () => {
      if (orderHint) orderHint.textContent = orderHints[orderSelect?.value || orderMode] || "";
      renderOrderList();
      onDraftChange?.({ orderMode: orderSelect?.value || orderMode, orderedItems: mutableOrder });
    };
    const onKey = (event) => { if (event.key === "Escape") finish(false); };
    $("#actionConfirmOk").addEventListener("click", onOk);
    $("#actionConfirmCancel").addEventListener("click", onCancel);
    $("#actionConfirmClose").addEventListener("click", onCancel);
    orderSelect?.addEventListener("change", onOrderChange);
    document.addEventListener("keydown", onKey);
  });
}

function setDirectorState(text, running = false) {
  const state = $("#directorState");
  const dot = $("#directorDot");
  if (state) state.textContent = text;
  dot?.classList.toggle("running", running);
}

function directorStatusForJob(job = currentJob) {
  return displayStatusForJob(job);
}

function jobOutputMetrics(job = currentJob) {
  const entries = orderedJobOutputs(job);
  const formalEntries = entries.filter(({ item, version }) => (
    !currentOutputIsReviewSample(item, version) && !item?.previewOnly && !version?.previewOnly
  ));
  const reviewEntries = entries.filter(({ item, version }) => (
    currentOutputIsReviewSample(item, version) || item?.previewOnly || version?.previewOnly
  ));
  const formalVersionCount = new Set(formalEntries.map(({ item, version }, index) => (
    String(version?.id || version?.number || item?.versionId || `formal-${index}`)
  ))).size;
  return {
    formalVersionCount,
    formalVideoCount: formalEntries.length,
    reviewSampleCount: reviewEntries.length,
    videoCount: entries.length,
  };
}

function jobResultSummary(job = currentJob) {
  const metrics = jobOutputMetrics(job);
  if (!metrics.videoCount) return "";
  return [
    metrics.formalVersionCount ? `${metrics.formalVersionCount} 个正式版本` : "",
    metrics.reviewSampleCount ? `${metrics.reviewSampleCount} 个审核样片` : "",
    metrics.formalVideoCount > metrics.formalVersionCount ? `${metrics.formalVideoCount} 条成片` : "",
  ].filter(Boolean).join(" · ");
}

function displayStatusForJob(job = currentJob) {
  if (!job) return { text: "等待素材", running: false, className: "empty" };
  if (String(job.stage || "") === "agent_handed_off") {
    const workflowLabel = {
      highlight: "智能高光", content_search: "内容检索",
      person_edit: "人物聚焦", speaker_edit: "发言剪辑",
    }[String(job.agentHandoffWorkflowKind || "")] || "同源新任务";
    return { text: `Agent 已转到${workflowLabel}任务 · 当前任务结果已保留`, running: false, className: "agent-handed-off" };
  }
  const unifiedPresentation = window.ClipTalkWorkspaceState?.derivePresentation?.(job);
  if (unifiedPresentation) {
    const preservedResult = ["failed", "cancelled"].includes(unifiedPresentation.key)
      ? jobResultSummary(job) : "";
    return {
      text: `${unifiedPresentation.label}${preservedResult ? ` · 已保留 ${preservedResult}` : ""}`,
      running: unifiedPresentation.running,
      className: unifiedPresentation.key,
    };
  }
  const execution = executionForJob(job);
  const status = String(execution.status || job.status || "");
  const result = jobResultSummary(job);
  const workflow = workflowKindForJob(job);
  const vocabulary = workflowVocabulary(job);
  const suffix = result ? ` · ${result}` : "";
  const agent = job.agent && typeof job.agent === "object" ? job.agent : {};
  const agentStatus = String(agent.status || "");
  if (isAgentInstructionDraft(job)) {
    return { text: "待输入剪辑要求", running: false, className: "agent-draft" };
  }
  const agentCompleted = Math.max(0, Number(agent.completedSteps) || 0);
  const agentTotal = Math.max(0, Number(agent.totalSteps) || 0);
  if (String(job.status || "") === "awaiting_agent_plan") {
    if (agentStatus === "awaiting_confirmation") return { text: "请确认剪辑计划", running: false, className: "agent-awaiting-confirmation" };
    if (["approved", "running"].includes(agentStatus)) {
      const progress = agentTotal ? ` · 已完成 ${Math.min(agentCompleted, agentTotal)}/${agentTotal} 步` : "";
      return { text: `Agent 正在执行：${agent.currentStepTitle || "准备下一步"}${progress}`, running: true, className: "agent-running" };
    }
    if (agentStatus === "action_required") return { text: `需要确认：${agent.currentStepTitle || "待确认步骤"}`, running: false, className: "agent-action-required" };
    if (agentStatus === "preview_ready") return { text: `审核结果已生成${agentTotal ? ` · ${agentCompleted}/${agentTotal} 步` : ""}`, running: false, className: "agent-completed" };
    if (agentStatus === "no_result") return { text: "未找到可用于自动剪辑的可靠内容", running: false, className: "agent-no-result" };
    if (agentStatus === "failed") return { text: "剪辑计划执行失败 · 请查看失败步骤", running: false, className: "failed" };
    if (agentStatus === "cancelled") return { text: "剪辑计划已停止", running: false, className: "cancelled" };
    const planningActive = String(agent.workspaceStatus || "") === "planning";
    return {
      text: planningActive ? "正在生成剪辑计划" : "等待你描述剪辑目标",
      running: planningActive,
      className: "agent-planning",
    };
  }
  if (execution.outcome === "no_acceptable_output") return { text: "自动成片未通过质量检查 · 候选仍可审核", running: false, className: "quality-rejected" };
  if (execution.outcome === "partial_output" && !execution.active) return { text: `部分完成${suffix}`, running: false, className: "partial" };
  if (status === "failed") return { text: `处理失败${result ? ` · 已保留 ${result}` : ""}`, running: false, className: "failed" };
  if (status === "cancelled") return { text: `已取消${suffix}`, running: false, className: "cancelled" };
  if (status === "cancelling") return { text: `正在停止${suffix}`, running: false, className: "cancelling" };
  if (execution.actionRequired === "resolve_model_stage") return { text: "等待你处理模型阶段", running: false, className: "awaiting-model-decision" };
  if (execution.actionRequired === "confirm_brief") return { text: "等待你确认需求", running: false, className: "brief-confirmation" };
  // Automatic composition intentionally runs in the background while the
  // event review status remains awaiting_confirmation. Its live state must
  // win over the outer review status or the UI falsely looks idle.
  if (execution.active && ["auto_composition", "quality_review"].includes(execution.operation)) {
    const completed = Math.max(0, Number(execution.progress?.completed ?? job.autoComposition?.completedVersions) || 0);
    const total = Math.max(0, Number(execution.progress?.total ?? job.autoComposition?.totalVersions) || 0);
    return {
      text: total > 0 ? `${execution.operation === "quality_review" ? "正在审片" : "正在生成成片"} · 已完成 ${Math.min(completed, total)}/${total} 个` : (execution.detail || "正在生成成片"),
      running: true,
      className: "running",
    };
  }
  if (execution.actionRequired === "review_content") {
    const reviewLabel = workflow === "person_edit" ? "等待核对出镜片段"
      : workflow === "speaker_edit" ? "等待核对发言片段" : "等待确认内容片段";
    return { text: `${reviewLabel}${suffix}`, running: false, className: "awaiting-content-confirmation" };
  }
  if (execution.actionRequired === "review_highlights") return { text: `等待确认高光${suffix}`, running: false, className: "awaiting-confirmation" };
  if (status === "completed") return { text: `${vocabulary.output}已完成${suffix}`, running: false, className: "completed" };
  if (execution.operation === "content_search" && execution.active) {
    const activeLabel = workflow === "person_edit" ? "正在建立人物出镜轨迹"
      : workflow === "speaker_edit" ? "正在整理说话人发言" : "正在检索内容";
    return { text: execution.detail || activeLabel, running: true, className: "running" };
  }
  if (String(job.status || "") === "briefing") return { text: "正在理解需求", running: true, className: "briefing" };
  if (status === "queued") return { text: `排队中 · 尚未开始处理${suffix}`, running: false, className: "queued" };
  const rendering = ["rendering", "render", "edit_planning", "auto_composition"].includes(String(job.stage || ""));
  return { text: execution.detail || (rendering ? "正在生成成片" : "正在处理"), running: execution.active, className: "running" };
}

function updateDirectorState(job = currentJob) {
  const status = directorStatusForJob(job);
  setDirectorState(status.text, status.running);
}

function thinkingMessageMarkup(config, job = null) {
  if (!config) return "";
  // A newly submitted chat instruction may switch workflows. Until the
  // server routes it, do not inherit the previous job's specialist label.
  const roleLabel = job ? workflowAssistantLabel(job) : "视频剪辑助手";
  return `<article class="chat-message assistant thinking-message" role="status">
    <span class="avatar thinking-avatar"><span class="thinking-orb-slot" data-thinking-orb data-orb-state="${escapeHtml(config.state)}" data-orb-size="30" data-orb-theme="light" data-orb-label="${escapeHtml(config.label)}"></span></span>
    <div class="bubble" data-border-beam data-beam-size="pulse-inner" data-beam-color="sunset" data-beam-theme="dark" data-beam-strength="0.58" data-beam-duration="2.25" data-beam-brightness="1.18" data-beam-saturation="1" data-beam-hue-range="16" data-beam-radius="13"><small>${roleLabel}</small><p>${escapeHtml(config.label)}</p></div>
  </article>`;
}

function compactConversationMessages(messages = []) {
  const compacted = [];
  const retryKinds = new Set(["retry", "notice"]);
  messages.forEach((message) => {
    const text = String(message?.text || "").trim();
    const kind = String(message?.kind || "");
    if (kind === "notice" && text === "正在按已确认的证据类型检索；未授权的识别能力不会运行。") return;
    const previous = compacted[compacted.length - 1];
    const sameTurn = String(previous?.conversationTurnId || "") === String(message?.conversationTurnId || "");
    if (previous && retryKinds.has(kind) && sameTurn
      && String(previous.role || "") === String(message?.role || "")
      && String(previous.kind || "") === kind
      && String(previous.text || "").trim() === text) {
      previous.repeatCount = Number(previous.repeatCount || 1) + 1;
      previous.lastCreatedAt = message.createdAt || previous.lastCreatedAt;
      return;
    }
    const next = { ...message, repeatCount: 1 };
    compacted.push(next);
  });
  return compacted;
}

function isSupersededConversationSetupMessage(message, job = currentJob) {
  const text = String(message?.text || "").trim();
  const kind = String(message?.kind || "");
  const stillWaitingForInstruction = String(job?.status || "") === "awaiting_agent_instruction"
    && !job?.instructionSubmitted;
  if (message?.role === "user"
    && /(?:已建立智能剪辑\s*Agent\s*草稿[；;]\s*等待输入剪辑要求|等待描述剪辑要求)/.test(text)) return true;
  if (message?.role === "assistant"
    && kind === "notice"
    && /^视频已上传并建立\s*Agent\s*素材工作区。?请在对话框描述剪辑要求/.test(text)) return true;
  if (stillWaitingForInstruction && !message?.inherited) return false;
  if (message?.role === "assistant"
    && kind === "brief-summary"
    && /^素材范围与剪辑目标已记录。?智能剪辑\s*Agent\s*将生成计划/.test(text)) return true;
  return false;
}

function conversationDisplayMessage(message) {
  const next = { ...message };
  const text = String(next.text || "").trim();
  const uploadedAgentGoal = text.match(/^上传\s+.+?，交给智能剪辑\s*Agent[：:]\s*([\s\S]+?)[；;]\s*素材范围[：:]/);
  if (uploadedAgentGoal?.[1]?.trim()) next.text = uploadedAgentGoal[1].trim();
  return next;
}

function visibleConversationMessages(messages = [], job = currentJob) {
  return compactConversationMessages(messages)
    .filter((message) => !isSupersededConversationSetupMessage(message, job))
    .map(conversationDisplayMessage);
}

function analysisActivityLoader(job = {}) {
  const status = String(job.status || "");
  if (status === "queued") return { variant: "orbit", label: "等待后台开始处理" };
  if (status === "cancelling") return { variant: "orbit", label: "正在停止当前任务" };
  const context = [job.stage, job.detail, job.currentAction, job.model]
    .filter(Boolean).join(" ").toLowerCase();
  if (/(?:sensevoice|speech|audio|语音|音频|对白|声音)/.test(context)) {
    return { variant: "signal", label: "正在分析语音和声音信号" };
  }
  if (/(?:ffmpeg|render|compose|output|渲染|合成|生成成片|编码)/.test(context)) {
    return { variant: "rotor", label: "正在渲染高光成片" };
  }
  if (/(?:vlm|vision|visual|视觉|候选|镜头|画面|模型响应)/.test(context)) {
    return { variant: "matrix", label: "正在分析候选画面" };
  }
  if (/(?:llm|plan|planning|编排|规划|叙事|结构)/.test(context)) {
    return { variant: "aperture", label: "正在规划高光叙事结构" };
  }
  return { variant: "orbit", label: "AI 分析流程正在运行" };
}

function inlineAnalysisProgressMarkup(job) {
  const execution = executionForJob(job);
  const contract = progressContract(job);
  const queued = execution.status === "queued";
  const percent = Math.round(workflowProgress(job) * 100);
  const detail = friendlyProgressDetail(queued
    ? "任务已提交，正在等待后台开始处理"
    : contract.activity.detail || job?.currentAction || job?.detail || job?.stage || "准备分析");
  const rawStageProgress = measuredStageProgress(job);
  const stageFraction = rawStageProgress !== null ? rawStageProgress : 0;
  const stagePercent = Math.round(Math.max(0, Math.min(1, stageFraction)) * 100);
  const workflow = taskWorkflowForJob(job);
  const phaseIndex = workflow.currentIndex;
  const phaseItems = workflow.steps;
  const stageMode = stageDisplayMode(job);
  const showMeasuredWorkflow = String(job?.taskMode || "") !== "content_extract"
    && (stageMode === "determinate" || stageMode === "completed");
  const workflowLabel = queued ? "排队中" : showMeasuredWorkflow ? `${percent}%` : "进行中";
  const waiting = queued || execution.active
    && !["determinate", "completed"].includes(stageMode);
  const stageFact = stageProgressFact(job, stagePercent, waiting);
  const activity = analysisActivityLoader(job);
  const stageLabel = queued ? "等待处理" : contract.stage.label || "当前阶段";
  const model = friendlyModelLabel(contract.activity.model || job?.model);
  const stageBarPercent = stageMode === "completed" ? 100 : stagePercent;
  const cancelling = execution.status === "cancelling";
  const activityRunning = queued || execution.active;
  const actionLabel = executionCancelLabel(job);
  const beamTheme = document.documentElement.dataset.theme === "light" ? "light" : "dark";
  return `<section id="inlineAnalysisProgress" class="inline-analysis-progress stage-${stageMode}${waiting ? " indeterminate" : ""}${queued ? " queued" : ""}${showMeasuredWorkflow ? "" : " workflow-unmeasured"}" data-stage-mode="${stageMode}" data-border-beam data-beam-size="pulse-inner" data-beam-color="sunset" data-beam-theme="${beamTheme}" data-beam-strength="0.84" data-beam-duration="2.05" data-beam-brightness="1.42" data-beam-saturation="1.06" data-beam-hue-range="14" data-beam-radius="13" aria-label="AI 分析进度" aria-busy="${activityRunning}"><div class="inline-progress-orb"><span>流程</span><b data-inline-percent>${workflowLabel}</b></div><div class="inline-progress-copy"><div class="inline-workflow-head"><span>当前进度</span><b data-inline-workflow-percent>${workflowLabel}</b></div><i class="inline-progress-track inline-workflow-track"><b data-inline-bar style="width:${showMeasuredWorkflow ? percent : 0}%"></b></i><div class="inline-current-stage"><div class="inline-progress-heading"><span class="inline-progress-activity" data-inline-activity data-generative-loader="inline" data-loader-active="${activityRunning}" data-loader-variant="${activity.variant}" data-loader-size="46" data-loader-speed="1.08" data-loader-label="${escapeHtml(activity.label)}"></span><div><small data-inline-stage-label>分析阶段 · ${escapeHtml(stageLabel)}</small><strong data-inline-detail>${escapeHtml(detail)}</strong></div></div><div class="inline-progress-meta"><span data-inline-stage-progress>${escapeHtml(stageFact)}</span><span data-inline-model>${escapeHtml(model)}</span><span data-inline-elapsed>${processingElapsedLabel(job)}</span><span data-inline-eta>${escapeHtml(progressEtaText(job, waiting))}</span></div><i class="inline-stage-progress-track" aria-hidden="true"><b data-inline-stage-bar style="width:${stageBarPercent}%"></b></i></div></div><ol class="inline-stage-chain">${phaseItems.map((label, index) => `<li class="${queued ? "" : workflow.complete || index < phaseIndex ? "done" : index === phaseIndex ? "current" : ""}">${label}</li>`).join("")}</ol><button type="button" class="inline-progress-cancel" data-inline-cancel${cancelling ? " disabled" : ""}>${actionLabel}</button></section>`;
}

function autoCompositionVersionFacts(job) {
  const state = job?.autoComposition || {};
  const stateVersions = Array.isArray(state.versions) ? state.versions.length : 0;
  const savedAutoVersions = jobOutputVersions(job).filter((version) =>
    ["vlm", "narrative", "emotion", "information"].includes(String(version?.strategyKey || ""))
  ).length;
  const explicitCompleted = Number(state.generatedVariantCount ?? state.completedVersions);
  const completed = Math.max(0, Number.isFinite(explicitCompleted) ? explicitCompleted : 0, stateVersions, savedAutoVersions);
  const configuredTotal = Number(job?.request?.autoVariantCount);
  const explicitTotal = Number(state.totalVersions);
  const authoritativeTotal = Number.isFinite(explicitTotal) && explicitTotal > 0
    ? explicitTotal
    : Number.isFinite(configuredTotal) && configuredTotal > 0 ? configuredTotal : 1;
  const total = Math.max(
    completed,
    authoritativeTotal,
    1,
  );
  const reportedProgress = Number(state.progress);
  const reviewPhases = ["review_vlm", "review_llm", "repair_plan", "repair_render", "review_compare", "evidence_recovery", "repair_replan"];
  const reviewProgress = Math.max(0, Math.min(1, Number(state.reviewProgress) || 0));
  const ratio = state.status === "completed"
    ? 1
    : reviewPhases.includes(String(state.phase || ""))
      ? Math.min(.99, .85 + reviewProgress * .14)
      : Math.max(
        completed / total,
        Number.isFinite(reportedProgress) ? reportedProgress : 0,
      );
  const currentVersionProgress = Math.round(Math.max(0, Math.min(1, Number(state.currentVersionProgress) || 0)) * 100);
  return {
    completed,
    total,
    currentVersionProgress,
    percent: Math.round(Math.max(0, Math.min(1, ratio)) * 100),
  };
}

function autoCompositionProgressHint(job, facts) {
  const state = job?.autoComposition || {};
  const currentVersion = Math.max(1, Number(state.currentVersion) || facts.completed + 1);
  if (state.status === "queued") return `等待生成第 ${currentVersion}/${facts.total} 个独立方案`;
  if (state.phase === "llm_plan") return `已完成 ${facts.completed}/${facts.total} 个独立方案，正在规划后续剪法`;
  if (state.phase === "quality_check") {
    return `已完成 ${facts.completed}/${facts.total} 个独立方案，正在检查第 ${currentVersion} 个方案的可播放性`;
  }
  if (state.phase === "review_vlm") return `正在检查动态成片、真实音轨和剪切点 · 审片 ${Math.round((Number(state.reviewProgress) || 0) * 100)}%`;
  if (state.phase === "review_llm") return `正在校准故事、节奏、声音连续性和目标匹配 · 审片 ${Math.round((Number(state.reviewProgress) || 0) * 100)}%`;
  if (state.phase === "repair_plan") return `正在制定第 ${Number(state.qualityRepair?.round || 1)}/${Number(state.qualityRepair?.maxRounds || 2)} 轮局部修复方案`;
  if (state.phase === "repair_render") return `已保留初版，正在生成第 ${Number(state.qualityRepair?.round || 1)}/${Number(state.qualityRepair?.maxRounds || 2)} 轮局部返修`;
  if (state.phase === "review_compare") return "正在复审返修版并与初版比较";
  if (state.phase === "evidence_recovery") return "局部修复仍未通过，正在补检问题镜头附近的动作与结果";
  if (state.phase === "repair_replan") return "补检完成，正在避开无解镜头并从完整候选中重新编排";
  const renderedSeconds = Number(state.renderedSeconds);
  const renderTotalSeconds = Number(state.renderTotalSeconds);
  const renderFact = Number.isFinite(renderedSeconds) && Number.isFinite(renderTotalSeconds) && renderTotalSeconds > 0
    ? `已处理 ${formatTime(renderedSeconds)} / ${formatTime(renderTotalSeconds)}`
    : `当前版本 ${facts.currentVersionProgress}%`;
  return `已完成 ${facts.completed}/${facts.total} 个独立方案，${renderFact}`;
}

const AUTO_COMPOSITION_ACTIVITY_KINDS = new Set([
  "auto-compose", "edit-plan", "composition-review", "composition-review-result",
]);

function isAutoCompositionActivityMessage(message, job = currentJob) {
  if (!message || message.inherited || message.role === "user") return false;
  const kind = String(message.kind || "");
  const value = String(message.text || "").trim();
  if (AUTO_COMPOSITION_ACTIVITY_KINDS.has(kind)) return true;
  if (kind === "result" && /^AI\s*样片\s*V\d+\s*已就绪/.test(value)) return true;
  if (kind === "recommendation" && /^事件整理完成[：:]/.test(value)) {
    return Boolean(job?.autoComposition || (job?.messages || []).some((item) => AUTO_COMPOSITION_ACTIVITY_KINDS.has(String(item?.kind || ""))));
  }
  return false;
}

function autoCompositionActivityTime(value) {
  const date = new Date(String(value || ""));
  if (!Number.isFinite(date.getTime())) return "--:--:--";
  return new Intl.DateTimeFormat("zh-CN", {
    hour: "2-digit", minute: "2-digit", second: "2-digit", hour12: false,
  }).format(date);
}

function autoCompositionActivityFacts(message) {
  const kind = String(message?.kind || "");
  const original = String(message?.text || "").trim();
  if (kind === "recommendation") return { label: "事件候选已整理", tone: "complete", text: original };
  if (kind === "edit-plan") return { label: "剪辑方案已生成", tone: "complete", text: original };
  if (kind === "composition-review") return { label: "质量审片", tone: "warning", text: original };
  if (kind === "composition-review-result") return { label: "质量修复结果", tone: "complete", text: original };
  if (kind === "result") {
    return {
      label: "样片渲染完成",
      tone: "complete",
      text: original.replace(/^AI\s*样片\s*V\d+\s*已就绪[：:]?\s*/, ""),
    };
  }
  const finished = /已完成|已就绪|完成[：:]|已保留/.test(original);
  return { label: finished ? "自动成片阶段完成" : "自动成片", tone: finished ? "complete" : "running", text: original };
}

function autoCompositionActivityMessages(job) {
  return compactConversationMessages(job?.messages || [])
    .filter((message) => isAutoCompositionActivityMessage(message, job));
}

function autoCompositionActivityItemMarkup(message, index, total, running) {
  const facts = autoCompositionActivityFacts(message);
  const isLatest = index === total - 1;
  const tone = running && isLatest ? "running" : facts.tone;
  const time = autoCompositionActivityTime(message.createdAt || message.lastCreatedAt);
  const fullTime = String(message.createdAt || message.lastCreatedAt || "");
  return `<li class="auto-compose-activity-item ${tone}${isLatest ? " latest" : ""}"><i aria-hidden="true"></i><div><strong>${escapeHtml(facts.label)}</strong><p>${escapeHtml(facts.text)}</p></div><time datetime="${escapeHtml(fullTime)}" title="${escapeHtml(fullTime)}">${escapeHtml(time)}</time></li>`;
}

function autoCompositionActivityMarkup(job, running) {
  const activities = autoCompositionActivityMessages(job);
  if (!activities.length) return `<div class="auto-compose-activity-empty">首条阶段记录将在后台开始执行后显示。</div>`;
  const allItems = activities.map((message, index) => autoCompositionActivityItemMarkup(message, index, activities.length, running)).join("");
  const recentStart = Math.max(0, activities.length - 2);
  const recentItems = activities.slice(recentStart).map((message, index) =>
    autoCompositionActivityItemMarkup(message, recentStart + index, activities.length, running)
  ).join("");
  return `<details class="auto-compose-log"${autoCompositionLogExpanded ? " open" : ""}><summary><span>完整执行记录</span><b>${activities.length} 条</b></summary><ol>${allItems}</ol></details><ol class="auto-compose-activity-recent">${recentItems}</ol>`;
}

function autoCompositionArchiveMarkup(job) {
  const activities = autoCompositionActivityMessages(job);
  if (!activities.length) return "";
  const items = activities.map((message, index) =>
    autoCompositionActivityItemMarkup(message, index, activities.length, false)
  ).join("");
  return `<details class="auto-compose-log auto-compose-result-log"><summary><span>查看完整执行记录</span><b>${activities.length} 条</b></summary><ol>${items}</ol></details>`;
}

function autoCompositionDockShouldShow(job) {
  const status = String(job?.autoComposition?.status || "");
  return ["queued", "running"].includes(status);
}

function autoCompositionProgressMarkup(job) {
  const state = job?.autoComposition || {};
  const detail = state.detail || "自动成片在后台生成";
  const facts = autoCompositionVersionFacts(job);
  const { percent } = facts;
  const reviewing = ["review_vlm", "review_llm", "repair_plan", "repair_render", "review_compare", "evidence_recovery", "repair_replan"].includes(String(state.phase || ""));
  const running = ["queued", "running"].includes(String(state.status || ""));
  const stateLabel = state.status === "completed" ? "全部完成" : state.status === "partial" ? "部分完成" : state.status === "queued" ? "等待后台启动" : reviewing ? "成片审片" : "后台运行";
  const versionHint = autoCompositionProgressHint(job, facts);
  return `<section class="auto-compose-progress ${running ? "is-running" : "is-finished"}" data-border-beam data-beam-size="pulse-inner" data-beam-color="sunset" data-beam-theme="dark" data-beam-strength="0.88" data-beam-duration="1.85" data-beam-brightness="1.46" data-beam-saturation="1.08" data-beam-hue-range="14" data-beam-radius="11" role="progressbar" aria-label="自动成片进度" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${percent}" aria-busy="${running}"><div class="auto-compose-progress-head"><span class="thinking-orb-slot" data-thinking-orb data-orb-state="${running ? "composing" : "working"}" data-orb-active="${running}" data-orb-size="24" data-orb-theme="light" data-orb-label="自动成片"></span><div><small>实时执行记录 · ${stateLabel}</small><strong data-auto-compose-detail>${escapeHtml(detail)}</strong></div><b data-auto-compose-count>${percent}%</b></div><div class="auto-compose-progress-track"><i data-auto-compose-bar style="width:${percent}%"></i></div><p data-auto-compose-versions>${escapeHtml(versionHint)}</p>${autoCompositionActivityMarkup(job, running)}</section>`;
}

function ensureAutoCompositionDock() {
  let dock = document.querySelector("#autoCompositionDock");
  if (dock) return dock;
  const panel = document.querySelector(".chat-panel") || document.body;
  dock = document.createElement("aside");
  dock.id = "autoCompositionDock";
  dock.className = "auto-composition-dock hidden";
  dock.setAttribute("aria-live", "polite");
  dock.setAttribute("aria-label", "自动成片实时执行记录");
  const composer = panel.querySelector("#chatForm");
  if (composer) panel.insertBefore(dock, composer);
  else panel.append(dock);
  return dock;
}

function renderAutoCompositionDock(job = currentJob) {
  const dock = ensureAutoCompositionDock();
  if (!autoCompositionDockShouldShow(job)) {
    dock.classList.add("hidden");
    dock.innerHTML = "";
    return;
  }
  if (autoCompositionDockJobId !== String(job?.id || "")) {
    autoCompositionDockJobId = String(job?.id || "");
    autoCompositionLogExpanded = false;
  }
  dock.classList.remove("hidden");
  dock.dataset.jobId = String(job?.id || "");
  dock.innerHTML = autoCompositionProgressMarkup(job);
  const details = dock.querySelector(".auto-compose-log");
  details?.addEventListener("toggle", () => { autoCompositionLogExpanded = details.open; });
  syncThinkingOrbs(dock);
  syncBorderBeams(dock);
}

function updateAutoCompositionProgress(job = currentJob) {
  const dock = ensureAutoCompositionDock();
  if (!autoCompositionDockShouldShow(job)) {
    renderAutoCompositionDock(job);
    return;
  }
  const panel = dock.querySelector(".auto-compose-progress");
  if (!panel || !job?.autoComposition) {
    renderAutoCompositionDock(job);
    return;
  }
  const state = job.autoComposition;
  const facts = autoCompositionVersionFacts(job);
  const { percent } = facts;
  const countNode = panel.querySelector("[data-auto-compose-count]");
  const detailNode = panel.querySelector("[data-auto-compose-detail]");
  const bar = panel.querySelector("[data-auto-compose-bar]");
  const versionNode = panel.querySelector("[data-auto-compose-versions]");
  if (countNode) countNode.textContent = `${percent}%`;
  if (detailNode) detailNode.textContent = state.detail || "自动成片在后台生成";
  if (bar) bar.style.width = `${percent}%`;
  if (versionNode) versionNode.textContent = autoCompositionProgressHint(job, facts);
  panel.setAttribute("aria-valuenow", String(percent));
}

function updateInlineAnalysisProgress(job = currentJob) {
  const panel = $("#inlineAnalysisProgress");
  if (!panel || !job) return;
  const contract = progressContract(job);
  const execution = executionForJob(job);
  const queued = execution.status === "queued";
  const percent = Math.round(workflowProgress(job) * 100);
  const detail = friendlyProgressDetail(queued
    ? "任务已提交，正在等待后台开始处理"
    : contract.activity.detail || job.currentAction || job.detail || job.stage || "准备分析");
  const rawStageProgress = measuredStageProgress(job);
  const stageFraction = rawStageProgress !== null ? rawStageProgress : 0;
  const stagePercent = Math.round(Math.max(0, Math.min(1, stageFraction)) * 100);
  const stageMode = stageDisplayMode(job);
  const showMeasuredWorkflow = String(job?.taskMode || "") !== "content_extract"
    && (stageMode === "determinate" || stageMode === "completed");
  const workflowLabel = queued ? "排队中" : showMeasuredWorkflow ? `${percent}%` : "进行中";
  const percentNode = panel.querySelector("[data-inline-percent]");
  const workflowPercentNode = panel.querySelector("[data-inline-workflow-percent]");
  const detailNode = panel.querySelector("[data-inline-detail]");
  const stageLabelNode = panel.querySelector("[data-inline-stage-label]");
  const stageProgressNode = panel.querySelector("[data-inline-stage-progress]");
  const modelNode = panel.querySelector("[data-inline-model]");
  const etaNode = panel.querySelector("[data-inline-eta]");
  const bar = panel.querySelector("[data-inline-bar]");
  const stageBar = panel.querySelector("[data-inline-stage-bar]");
  const activityNode = panel.querySelector("[data-inline-activity]");
  const orb = panel.querySelector(".inline-progress-orb");
  if (orb) orb.style.setProperty("--inline-progress", `${showMeasuredWorkflow ? percent : 0}%`);
  if (percentNode) percentNode.textContent = workflowLabel;
  if (workflowPercentNode) workflowPercentNode.textContent = workflowLabel;
  if (detailNode) detailNode.textContent = detail;
  if (stageLabelNode) stageLabelNode.textContent = `分析阶段 · ${queued ? "等待处理" : contract.stage.label || "处理中"}`;
  const waiting = queued || execution.active
    && !["determinate", "completed"].includes(stageMode);
  panel.classList.toggle("queued", queued);
  panel.classList.toggle("indeterminate", waiting);
  panel.classList.toggle("stage-indeterminate", stageMode === "indeterminate");
  panel.classList.toggle("stage-finalizing", stageMode === "finalizing");
  panel.classList.toggle("stage-determinate", stageMode === "determinate");
  panel.classList.toggle("stage-completed", stageMode === "completed");
  panel.classList.toggle("workflow-unmeasured", !showMeasuredWorkflow);
  panel.dataset.stageMode = stageMode;
  panel.setAttribute("aria-busy", String(waiting));
  if (stageProgressNode) stageProgressNode.textContent = stageProgressFact(job, stagePercent, waiting);
  if (modelNode) modelNode.textContent = contract.activity.model || job.model || "系统";
  if (etaNode) etaNode.textContent = progressEtaText(job, waiting);
  if (bar) bar.style.width = `${showMeasuredWorkflow ? percent : 0}%`;
  if (stageBar) stageBar.style.width = `${stageMode === "completed" ? 100 : stagePercent}%`;
  if (activityNode) {
    const activity = analysisActivityLoader(job);
    const activityRunning = queued || execution.active;
    activityNode.dataset.loaderActive = String(activityRunning);
    activityNode.classList.toggle("hidden", !activityRunning);
    const changed = activityNode.dataset.loaderVariant !== activity.variant || activityNode.dataset.loaderLabel !== activity.label;
    if (changed) {
      activityNode.dataset.loaderVariant = activity.variant;
      activityNode.dataset.loaderLabel = activity.label;
    }
    if (changed || !activityNode.firstElementChild) {
      renderGenerativeLoader(activityNode, { kind: "inline", variant: activity.variant, size: 46, speed: .9, label: activity.label });
    }
  }
  const elapsed = panel.querySelector("[data-inline-elapsed]");
  if (elapsed) elapsed.textContent = processingElapsedLabel(job);
  const workflow = taskWorkflowForJob(job);
  const phaseIndex = workflow.currentIndex;
  panel.querySelectorAll(".inline-stage-chain li").forEach((item, index) => {
    item.classList.toggle("done", !queued && (workflow.complete || index < phaseIndex));
    item.classList.toggle("current", !queued && !workflow.complete && index === phaseIndex);
  });
}

function commandThinkingConfig(value) {
  if (/(?:合成|组合|编排|章节|顺序|移动|合并|拆分|总时长|重新平衡)/.test(value)) {
    return { state: "shaping", label: "正在重新编排镜头与事件结构" };
  }
  if (/(?:换掉|替换|调整|增加|延长|缩短|删除|复制|命名|改名|裁剪|选区)/.test(value)) {
    return { state: "solving", label: "正在理解修改要求并检查时间边界" };
  }
  return { state: "composing", label: "正在理解你的要求" };
}

function appendTransientThinking(value) {
  const container = $("#chatMessages");
  if (!container) return [];
  const config = commandThinkingConfig(value);
  container.insertAdjacentHTML("beforeend", `<article class="chat-message user transient-message"><span class="avatar">你</span><div class="bubble"><small>你</small><p>${escapeHtml(value)}</p></div></article>${thinkingMessageMarkup(config)}`);
  const nodes = [...container.querySelectorAll(".transient-message, .thinking-message")].slice(-2);
  syncThinkingOrbs(container);
  container.scrollTop = container.scrollHeight;
  return nodes;
}

function replaceConversationContent(markup) {
  const root = ensureChatMessages();
  let stageHost = $("#chatStageHost");
  if (!stageHost) {
    stageHost = document.createElement("div");
    stageHost.id = "chatStageHost";
    stageHost.className = "chat-stage-host review-stage-host";
    stageHost.setAttribute("aria-live", "polite");
    ($("#reviewWorkbench") || $("#reviewView") || document.body).append(stageHost);
  }
  root.innerHTML = markup;
  return root;
}

function hideTaskSetupView() {
  const portal = $("#taskSetupPortal");
  const view = $("#taskSetupView");
  if (portal) portal.innerHTML = "";
  view?.classList.add("hidden");
  $("#uploadView")?.classList.remove("task-setup-active");
}

function initialConversation() {
  hideTaskSetupView();
  const draftStatus = $("#newTaskDraftStatus");
  if (draftStatus) {
    draftStatus.dataset.state = "empty";
    draftStatus.textContent = "等待添加素材";
  }
  replaceConversationContent(`
    <article class="chat-message assistant"><span class="avatar">AI</span><div class="bubble"><small>AI</small><p>先添加视频，再告诉我你想怎么剪。</p><div class="empty-chat-prompts" aria-label="剪辑示例"><span>试试</span><button type="button" data-empty-prompt="保留完整发言">保留完整发言</button><button type="button" data-empty-prompt="提取高光">提取高光</button><button type="button" data-empty-prompt="按描述找片段">按描述找片段</button></div></div></article>`);
}

function showPendingNewTaskInstruction(instruction) {
  const value = String(instruction || "").trim();
  if (!value) return;
  const draftStatus = $("#newTaskDraftStatus");
  if (draftStatus) {
    draftStatus.dataset.state = "ready";
    draftStatus.textContent = "剪辑要求已暂存 · 选择视频后自动继续";
  }
  replaceConversationContent(`
    <article class="chat-message user"><span class="avatar">你</span><div class="bubble"><small>待创建任务</small><p>${escapeHtml(value)}</p></div></article>
    <article class="chat-message assistant"><span class="avatar">AI</span><div class="bubble"><small>下一步</small><p>要求已暂存。选择视频后，我会创建独立任务并自动开始整理剪辑计划。</p></div></article>`);
  const messages = $("#chatMessages");
  if (messages) messages.scrollTop = messages.scrollHeight;
}

function contentScopeBaseRange(kind, duration) {
  const total = Math.max(0, Number(duration) || 0);
  const edge = Math.min(total * .15, 600);
  const ranges = {
    all: [0, total], opening: [0, edge], front_half: [0, total * .5],
    middle: [total * .25, total * .75], back_half: [total * .5, total],
    ending: [Math.max(0, total - edge), total],
  };
  return ranges[kind] || null;
}

function contentClockSeconds(value) {
  const text = String(value || "").trim();
  if (!text) return null;
  if (text.includes(":")) {
    const parts = text.split(":").map(Number);
    if (parts.some((item) => !Number.isFinite(item))) return null;
    if (parts.length === 2) return parts[0] * 60 + parts[1];
    if (parts.length === 3) return parts[0] * 3600 + parts[1] * 60 + parts[2];
  }
  const hours = Number(text.match(/(\d+(?:\.\d+)?)\s*小时/)?.[1] || 0);
  const minutes = Number(text.match(/(\d+(?:\.\d+)?)\s*(?:分钟|分)/)?.[1] || 0);
  const seconds = Number(text.match(/(\d+(?:\.\d+)?)\s*秒/)?.[1] || 0);
  return hours || minutes || seconds ? hours * 3600 + minutes * 60 + seconds : null;
}

function contentTextRange(text, duration) {
  const value = String(text || "");
  const total = Math.max(0, Number(duration) || 0);
  const token = "(\\d{1,2}:\\d{2}(?::\\d{2}(?:\\.\\d+)?)?|\\d+(?:\\.\\d+)?\\s*(?:小时|分钟|分|秒))";
  const isDurationTarget = (index) => /(?:每|各|单)(?:个|条|段)?(?:片段|镜头|成片|视频)?\s*$|(?:总|目标|成片|片段|单段)?时长(?:控制在|约为|为)?\s*$/
    .test(value.slice(Math.max(0, Number(index) - 16), Number(index)));
  let match = [...value.matchAll(new RegExp(`${token}\\s*(?:到|至|[-~～])\\s*${token}`, "ig"))]
    .find((item) => !isDurationTarget(item.index));
  if (match) {
    const start = contentClockSeconds(match[1]);
    const end = contentClockSeconds(match[2]);
    if (start !== null && end !== null && end > start) return [Math.max(0, start), Math.min(total, end)];
  }
  match = value.match(new RegExp(`(?:最后|末尾|结尾)\\s*${token}`, "i"));
  if (match) { const span = contentClockSeconds(match[1]); if (span !== null) return [Math.max(0, total - span), total]; }
  match = value.match(new RegExp(`(?:开头|开始|最前)\\s*${token}`, "i"));
  if (match) { const span = contentClockSeconds(match[1]); if (span !== null) return [0, Math.min(total, span)]; }
  // A bare unit duration such as “每段 20 秒左右” describes output length,
  // not a source-video position. Clock timestamps are unambiguous; unit-based
  // positions must carry an explicit location cue such as “视频第 20 秒左右”.
  match = value.match(/(\d{1,2}:\d{2}(?::\d{2}(?:\.\d+)?)?)\s*(?:附近|左右|前后)/i)
    || value.match(new RegExp(`(?:在|视频(?:的)?(?:第)?|素材(?:的)?(?:第)?|时间点|进度(?:到)?|第)\\s*${token}\\s*(?:附近|左右|前后|处)`, "i"));
  if (match) { const center = contentClockSeconds(match[1]); if (center !== null) return [Math.max(0, center - 120), Math.min(total, center + 120)]; }
  if (/前半段|前一半/.test(value)) return [0, total * .5];
  if (/后半段|后一半/.test(value)) return [total * .5, total];
  if (/(?:视频)?中间|中段/.test(value)) return [total * .25, total * .75];
  if (/(?:视频)?开头|片头/.test(value)) return contentScopeBaseRange("opening", total);
  if (/(?:视频)?结尾|片尾/.test(value)) return contentScopeBaseRange("ending", total);
  return null;
}

function setBriefCreationError(message = "") {
  const errorNode = $("#briefCreateError");
  if (!errorNode) return;
  errorNode.textContent = String(message || "");
  errorNode.classList.toggle("hidden", !message);
}

function syncBriefSettingsUi() {
  const duration = Math.max(0, Number($("#localPreviewVideo")?.duration) || 0);
  const kind = String($("#briefSourceScope")?.value || "all");
  const custom = kind === "custom";
  $("#briefCustomScope")?.classList.toggle("hidden", !custom);
  if (custom) {
    const endInput = $("#briefScopeEnd");
    if (endInput && !endInput.value && duration > 0) endInput.value = formatTime(duration);
  }
  const baseRange = kind === "custom" ? null : contentScopeBaseRange(kind, duration);
  const summary = $("#briefScopeSummary");
  if (summary) summary.textContent = baseRange
    ? (kind === "all" ? `使用完整源视频 · ${formatTime(duration)}` : `${formatTime(baseRange[0])} → ${formatTime(baseRange[1])}`)
    : "填写本次任务允许使用的开始和结束时间";

}

function activeBriefOptions(instruction = "") {
  const duration = Math.max(0, Number($("#localPreviewVideo")?.duration) || 0);
  if (!duration) throw new Error("视频信息尚未读取完成，请稍候再试。");
  const selectedKind = String($("#briefSourceScope")?.value || "all");
  let selectedRange = contentScopeBaseRange(selectedKind, duration);
  if (selectedKind === "custom") {
    const start = contentClockSeconds($("#briefScopeStart")?.value);
    const end = contentClockSeconds($("#briefScopeEnd")?.value);
    if (start === null || end === null || start < 0 || end <= start || end > duration + .05) {
      throw new Error(`自定义素材范围必须在 00:00 到 ${formatTime(duration)} 之间，且结束时间晚于开始时间。`);
    }
    selectedRange = [start, Math.min(duration, end)];
  }
  if (!selectedRange) selectedRange = [0, duration];
  // The UI appends a display-only “素材范围：…” suffix to the submitted
  // message. Never parse that metadata as a user-authored time constraint;
  // otherwise “全片” can be converted back into the previous 0–99.6s range.
  const instructionForTimeParsing = String(instruction || "").replace(/(?:素材范围|范围)\s*[:：].*$/i, "");
  // “开头放封面/片头加标题” describes edit order, not a source range.
  // Only numeric time cues can narrow the selected source scope.
  const timeCueText = instructionForTimeParsing.replace(/\b(?:9:16|16:9|4:5|1:1)\b/g, "");
  const hasNumericTimeCue = /\d{1,2}:\d{2}|\d+(?:\.\d+)?\s*(?:小时|分钟|分|秒)/i.test(timeCueText);
  const textRange = hasNumericTimeCue ? contentTextRange(timeCueText, duration) : null;
  const effectiveStart = textRange ? Math.max(selectedRange[0], textRange[0]) : selectedRange[0];
  const effectiveEnd = textRange ? Math.min(selectedRange[1], textRange[1]) : selectedRange[1];
  if (effectiveEnd <= effectiveStart) {
    throw new Error("指令中的时间条件与所选素材范围没有交集，请修改其中一个范围。");
  }
  const effectiveKind = textRange || selectedKind === "custom" ? "custom" : selectedKind;
  return {
    duration,
    sourceScope: {
      kind: effectiveKind,
      start: effectiveKind === "all" ? "" : Number(effectiveStart.toFixed(3)),
      end: effectiveKind === "all" ? "" : Number(effectiveEnd.toFixed(3)),
    },
    effectiveStart,
    effectiveEnd,
  };
}

function setBriefCreationStatus(label, detail = "") {
  const badge = $("#briefReadyBadge");
  const hint = $("#briefSubmitHint");
  if (badge) badge.textContent = label;
  if (hint && detail) hint.textContent = detail;
}

function workflowDisplayName(workflowKind) {
  return ({
    highlight: "智能高光",
    content_search: "内容检索",
    person_edit: "人物聚焦",
    speaker_edit: "发言剪辑",
  })[String(workflowKind || "")] || "视频剪辑";
}

const BRIEF_CAPABILITY_GROUPS = Object.freeze({
  highlight: {
    title: "智能高光",
    summary: "选择成片组织方式；默认沿用现有高光流程。",
    options: [
      { id: "highlight-auto", label: "自动高光", detail: "通看素材并生成多个可审核版本", workflow: "highlight", badge: "当前", recommendation: "通看全部素材，选出精彩且信息完整的片段，按节奏生成一版可审核高光视频。" },
      { id: "shortform", label: "短视频钩子", detail: "优先安排有吸引力的开场", skill: "cliptalk-shortform-hook-director", placeholder: "例如：剪成 60 秒短视频，前三秒突出产品亮点", recommendation: "剪成一条 60 秒短视频，前三秒突出核心亮点，随后用紧凑节奏展开主要内容。" },
      { id: "interview", label: "访谈精华", detail: "按主题组织问答并保留完整观点", skill: "cliptalk-interview-editor", placeholder: "例如：剪成 3 分钟访谈精华，按主题组织回答并添加字幕", recommendation: "剪成一版访谈精华，按主题组织问答，保留完整观点并添加清晰字幕。" },
    ],
  },
  content_search: {
    title: "内容检索",
    summary: "按具体目标查找证据；默认沿用现有内容检索流程。",
    options: [
      { id: "content-single", label: "指定内容", detail: "查找动作、物品、对白或屏幕文字", workflow: "content_search", badge: "当前", recommendation: "找出视频中与“核心主题”相关的画面、对白和屏幕文字，并按出现顺序整理。" },
      { id: "content-multi", label: "多主题组合", detail: "每类内容分别查找，缺失时明确提示", skill: "cliptalk-multi-topic-assembler", placeholder: "例如：分别找出外观、内饰和驾驶画面，每类至少保留一段", recommendation: "分别找出产品外观、细节和使用场景，每类至少保留一段；找不到时明确说明。" },
    ],
  },
  person_edit: {
    title: "人物聚焦",
    summary: "先建立人物卡，再选择保留、排除或多人同框。",
    options: [
      { id: "person-discover", label: "识别后选择人物", detail: "进入人物卡核对并选择出镜片段", workflow: "person_edit", badge: "当前", recommendation: "识别视频中的主要人物，整理每个人的代表画面，让我核对后选择需要保留的人物。" },
      { id: "person-directed", label: "直接描述人物要求", detail: "按姓名、外观或人物关系组织画面", skill: "cliptalk-person-editor", placeholder: "例如：只保留雷军出镜的画面，删除其他人物单独出镜片段", recommendation: "只保留目标人物清晰出镜的画面，删除其他人物单独出镜和主体不明确的片段。" },
    ],
  },
  speaker_edit: {
    title: "发言剪辑",
    summary: "先区分声音，再选择保留发言或整理问答。",
    options: [
      { id: "speaker-discover", label: "识别后选择说话人", detail: "试听声音卡后选择对应发言", workflow: "speaker_edit", badge: "当前", recommendation: "识别视频中的说话人，整理每个人的代表发言，让我试听后选择需要保留的声音。" },
      { id: "speaker-directed", label: "直接描述发言要求", detail: "保留或删除指定说话人的内容", skill: "cliptalk-speaker-editor", placeholder: "例如：只保留主持人的提问和嘉宾的完整回答", recommendation: "保留主持人的提问和嘉宾的完整回答，删除无关对话、重复表达和过长停顿。" },
    ],
  },
  revision: {
    title: "剪辑调整",
    summary: "调整已有视频或审核样片的结构、节奏和镜头。",
    options: [
      { id: "revision-edit", label: "删减与重排", detail: "缩短、删除、恢复或调整片段顺序", skill: "cliptalk-revision-editor", requiresResult: true, recommendation: "检查当前样片，删除重复或无效画面，调整片段顺序和节奏，同时保留主要信息。" },
      { id: "revision-diagnose", label: "检查剪辑问题", detail: "诊断画面、状态、字幕或导出异常", skill: "cliptalk-edit-diagnostics", requiresResult: true, recommendation: "检查当前样片的画面、字幕、播放状态和导出结果，列出问题片段并给出修复建议。" },
    ],
  },
  format: {
    title: "版式适配",
    summary: "先完成内容剪辑，再对审核样片进行字幕和画幅处理。",
    options: [
      { id: "subtitle-edit", label: "添加或修改字幕", detail: "调整文字、时间和字幕轨道", skill: "cliptalk-subtitle-editor", requiresResult: true, recommendation: "为当前样片校对字幕文字和时间，修正错字、断句与不同步的字幕。" },
      { id: "subtitle-layout", label: "字幕排版", detail: "设置位置、安全边距、换行和双语顺序", skill: "cliptalk-caption-layout-director", requiresResult: true, recommendation: "优化当前样片的字幕位置、字号、换行和安全边距，确保横竖屏下都清晰易读。" },
      { id: "reframe-smart", label: "完整画面适配", detail: "使用虚化补边适配横屏、竖屏或方形", skill: "cliptalk-smart-reframe", requiresResult: true, recommendation: "将当前样片适配为竖屏 9:16，完整保留原画面，并使用自然的虚化背景补齐空白区域。" },
      { id: "reframe-dynamic", label: "动态画幅重构", detail: "按镜头主体和安全区动态调整构图", skill: "cliptalk-dynamic-reframe-director", requiresResult: true, recommendation: "将当前样片重构为竖屏 9:16，逐镜头跟随主要人物或产品，并避开字幕安全区。" },
    ],
  },
  package: {
    title: "视觉包装",
    summary: "为已有视频或审核样片补充封面、片头和图文元素。",
    options: [
      { id: "cover", label: "生成视频封面", detail: "从素材证据生成三种可选封面", skill: "cliptalk-cover-director", placeholder: "例如：为视频生成封面，标题写“新品实测”", recommendation: "从视频中选择主体清晰的画面制作封面，标题突出核心主题，并生成三种可选方案。" },
      { id: "motion", label: "制作动态图文", detail: "生成标题卡、动画片头或说明画面", skill: "cliptalk-local-motion-renderer", placeholder: "例如：制作一个两秒标题片头，文字为“产品实测”", recommendation: "为当前视频制作一段简洁的动态图文片头，突出核心主题，并保持与原视频风格一致。" },
      { id: "cover-intro", label: "封面加入片头", detail: "确认封面后将其合入视频开头", skill: "cliptalk-cover-intro-composer", requiresResult: true, recommendation: "将已确认的封面作为视频片头，停留 1 秒后自然过渡到正片，并保持原视频时序。" },
      { id: "graphics", label: "标题与图文包装", detail: "添加标签、参数、价格、水印或提示文字", skill: "cliptalk-graphics-packager", requiresResult: true, recommendation: "为当前样片添加简洁的标题和重点信息卡，统一字体、颜色与位置，避免遮挡主体和字幕。" },
      { id: "broll", label: "添加补充画面", detail: "保留主音频并加入相关切换画面", skill: "cliptalk-broll-overlay-editor", requiresResult: true, recommendation: "保留当前主音频，在相关内容处加入匹配的补充画面，并保证切换自然、语义一致。" },
      { id: "audio", label: "优化人声与音频", detail: "响度、降噪、淡入淡出和人声优先混音", skill: "cliptalk-audio-polish-mixer", requiresResult: true, recommendation: "优化当前样片的人声清晰度和整体响度，降低背景噪声，并为开头和结尾添加自然淡入淡出。" },
    ],
  },
  delivery: {
    title: "导出交付",
    summary: "先确认审核样片或成片，之后才能进行交付。",
    options: [
      { id: "delivery-platform", label: "按平台导出", detail: "根据发布平台生成最终文件和质量报告", skill: "cliptalk-platform-delivery-exporter", requiresResult: true, recommendation: "按常用短视频平台规格导出当前成片，并在导出前检查分辨率、画幅、字幕和音量。" },
      { id: "delivery-draft", label: "导出剪辑草稿", detail: "生成可交给外部剪辑工具的本地草稿包", skill: "cliptalk-local-draft-exporter", requiresResult: true, recommendation: "将当前时间线导出为可继续编辑的剪辑草稿包，保留片段顺序、字幕和素材引用。" },
      { id: "delivery-qc", label: "成片质量检查", detail: "检查黑帧、静帧、静音和音视频流异常", skill: "cliptalk-delivery-qc", requiresResult: true, recommendation: "全面检查当前成片中的黑帧、静帧、静音、字幕遮挡和音视频异常，并定位问题时间段。" },
    ],
  },
});

function selectedDraftCapability(job = currentJob) {
  if (!isAgentInstructionDraft(job) || pendingCapabilityCategory === "auto" || !pendingCapabilityOption) return null;
  const group = BRIEF_CAPABILITY_GROUPS[pendingCapabilityCategory];
  const option = group?.options.find((item) => item.id === pendingCapabilityOption);
  return option ? { category: pendingCapabilityCategory, group, option } : null;
}

window.ClipTalkDraftCapabilitySnapshot = () => {
  const selected = selectedDraftCapability(currentJob);
  return selected ? {
    active: true,
    category: selected.category,
    categoryLabel: selected.group.title,
    optionId: selected.option.id,
    optionLabel: selected.option.label,
  } : { active: false };
};

function showBriefIntentConfirmation(detail = {}) {
  const panel = $("#briefIntentClarification");
  if (!panel) return;
  const recommendation = detail.recommendation || detail.decision || {};
  const recommended = String(recommendation.workflowKind || "");
  const title = panel.querySelector("strong");
  const copy = panel.querySelector("p");
  if (title) title.textContent = detail.code === "intent_model_unavailable" ? "AI 判断暂不可用" : "需要确认剪辑方向";
  if (copy) copy.textContent = recommended
    ? `AI 倾向于“${workflowDisplayName(recommended)}”（${Math.round(Number(recommendation.confidence || 0) * 100)}%），但当前信息不足以安全自动执行。`
    : (detail.message || "请选择最接近你目标的处理方式。");
  panel.querySelectorAll("[data-intent-choice]").forEach((button) => {
    const isRecommended = button.dataset.intentChoice === recommended;
    button.classList.toggle("recommended", isRecommended);
    button.setAttribute("aria-label", `${button.textContent}${isRecommended ? "，AI 推荐" : ""}`);
  });
  panel.classList.remove("hidden");
  panel.scrollIntoView({ behavior: "smooth", block: "nearest" });
  syncWorkspaceState({ routingConfirmation: true, home: false });
}

async function classifyBriefWorkflow(instruction) {
  actionBusy = true;
  setComposerSending(true);
  if (chatInput) chatInput.disabled = true;
  $("#chatLegacyModeButton")?.classList.add("hidden");
  setBriefCreationStatus("AI 判断中", "正在理解你的目标并选择合适的剪辑方式……");
  try {
    const response = await apiJson("/api/workflow-intent/classify", {
      method: "POST", body: { text: instruction },
    });
    const decision = response.decision || {};
    if (decision.needsConfirmation || !decision.workflowKind) {
      showBriefIntentConfirmation({ ...response, decision });
      setBriefCreationStatus("等待确认", "选择一种剪辑方式后才会开始上传视频");
      return "";
    }
    setBriefCreationStatus("AI 已判断", `${workflowDisplayName(decision.workflowKind)} · ${Math.round(Number(decision.confidence || 0) * 100)}%`);
    return String(decision.workflowKind);
  } catch (error) {
    showBriefIntentConfirmation(error.detail || { code: error.code, message: error.message });
    setBriefCreationStatus("等待选择", "模型不可用时不会使用关键词规则静默启动任务");
    return "";
  } finally {
    actionBusy = false;
    setComposerSending(false);
    if (chatInput) chatInput.disabled = false;
  }
}

function showBriefCard(file) {
  pendingCreateIntentMode = "auto";
  pendingCreateWorkflowKind = "";
  const conversation = replaceConversationContent(window.ClipTalkTaskCreation.briefMarkup(file.name));
  const portal = $("#taskSetupPortal");
  const setupView = $("#taskSetupView");
  const briefCard = conversation.querySelector(".brief-card");
  const modePicker = briefCard?.querySelector(".brief-template-picker");
  if (modePicker) conversation.appendChild(modePicker);
  if (portal && briefCard) {
    portal.replaceChildren(briefCard);
    setupView?.classList.remove("hidden");
    $("#uploadView")?.classList.add("task-setup-active");
  }
  // The setup card and mode picker are intentionally re-parented into two
  // columns. Query the document so both halves remain one accessible control.
  const briefRoot = document;
  const skillLoadPromise = Promise.resolve(window.ClipTalkAgentWorkspace?.loadSkills());
  const agentGoalInput = briefRoot.querySelector("#briefAgentGoal");
  const agentSkillSelect = briefRoot.querySelector("#briefAgentSkillSelect");
  const setTopCapability = (category) => {
    briefRoot.querySelectorAll("[data-capability-category]").forEach((button) => {
      const selected = button.dataset.capabilityCategory === category;
      button.classList.toggle("selected", selected);
      button.setAttribute("aria-pressed", String(selected));
    });
  };

  const updateCapabilitySelection = (title, detail, mode = "manual") => {
    const selection = $("#briefCapabilitySelection");
    if (!selection) return;
    selection.dataset.mode = mode;
    selection.querySelector("strong").textContent = title;
    selection.querySelector("span").textContent = detail;
  };

  const hideLegacySettings = () => {
    ["#briefHighlightSettings", "#briefContentSettings", "#briefSpeakerSettings", "#briefPersonSettings"]
      .forEach((selector) => $(selector)?.classList.add("hidden"));
  };

  const activateAgentPath = ({ category = "auto", preserveSkill = false, keepDetail = false } = {}) => {
    pendingCreateWorkflowKind = "";
    pendingCreateIntentMode = "auto";
    $("#briefWorkflowHelp")?.classList.remove("template-selected");
    hideLegacySettings();
    setTopCapability(category);
    if (!preserveSkill && agentSkillSelect) agentSkillSelect.value = "";
    if (!keepDetail) $("#briefCapabilityDetail")?.classList.add("hidden");
    if (category === "auto") {
      updateCapabilitySelection("自动选择", "系统会在方案中说明使用了哪些能力", "auto");
      if (agentGoalInput) agentGoalInput.placeholder = "例如：剪成 3 分钟访谈精华，按主题组织回答，删除重复表达并生成字幕预览";
    }
    setBriefCreationStatus("视频已就绪", "填写目标后生成计划；确认前不会分析或渲染");
    setBriefCreationError("");
  };

  function renderCapabilityGroup(category, selectedId = "") {
    const group = BRIEF_CAPABILITY_GROUPS[category];
    const detail = $("#briefCapabilityDetail");
    if (!group || !detail) {
      detail?.classList.add("hidden");
      return;
    }
    detail.innerHTML = `<header><div><small>具体功能</small><strong>${escapeHtml(group.title)}</strong></div><p>${escapeHtml(group.summary)}</p></header><div class="brief-capability-options" role="radiogroup" aria-label="${escapeHtml(group.title)}具体功能">${group.options.map((option) => `<button type="button" data-capability-option="${escapeHtml(option.id)}" aria-pressed="${String(option.id === selectedId)}"${option.requiresResult ? " disabled aria-disabled=\"true\"" : ""}><span><strong>${escapeHtml(option.label)}</strong><small>${escapeHtml(option.detail)}</small></span>${option.requiresResult ? "<b>需先有审核样片</b>" : option.badge ? `<b>${escapeHtml(option.badge)}</b>` : "<b>可选择</b>"}</button>`).join("")}</div>`;
    detail.classList.remove("hidden");
    detail.querySelectorAll("[data-capability-option]").forEach((button) => {
      button.addEventListener("click", async () => {
        const option = group.options.find((item) => item.id === button.dataset.capabilityOption);
        if (!option || option.requiresResult) return;
        if (option.workflow) {
          selectWorkflow(option.workflow, { renderDetail: false, selectedId: option.id });
          return;
        }
        await selectAgentCapability(category, option);
      });
    });
  }

  async function selectAgentCapability(category, option) {
    await skillLoadPromise;
    if (!agentSkillSelect || ![...agentSkillSelect.options].some((item) => item.value === option.skill)) {
      setBriefCreationError("这项能力当前不可用，请在设置中检查 Skill 状态。", "capability_unavailable");
      return;
    }
    pendingCreateWorkflowKind = "";
    pendingCreateIntentMode = "auto";
    agentSkillSelect.value = option.skill;
    hideLegacySettings();
    $("#briefWorkflowHelp")?.classList.remove("template-selected");
    setTopCapability(category);
    renderCapabilityGroup(category, option.id);
    updateCapabilitySelection(`${BRIEF_CAPABILITY_GROUPS[category].title} · ${option.label}`, "已指定主要能力；系统仍可组合必要的辅助能力");
    if (agentGoalInput) {
      agentGoalInput.placeholder = option.placeholder || `请补充“${option.label}”的具体要求`;
      agentGoalInput.focus({ preventScroll: true });
    }
    setBriefCreationStatus("已选择具体功能", `补充要求后生成计划 · ${option.label}`);
    setBriefCreationError("");
  }

  const selectWorkflow = (workflowKind, { renderDetail = true, selectedId = "" } = {}) => {
    pendingCreateWorkflowKind = workflowKind;
    pendingCreateIntentMode = workflowKind === "highlight"
      ? "highlight" : workflowKind === "content_search" ? "content_extract" : "content_extract";
    if (agentSkillSelect) agentSkillSelect.value = "";
    setTopCapability(workflowKind);
    const carriedInstruction = String($("#briefAutoInstruction")?.value || agentGoalInput?.value || "").trim();
    const workflowInstruction = workflowKind === "highlight"
      ? $("#briefHighlightInstruction")
      : workflowKind === "content_search" ? $("#briefContentInstruction") : null;
    if (workflowInstruction && carriedInstruction && !workflowInstruction.value.trim()) {
      workflowInstruction.value = carriedInstruction;
    }
    $("#briefWorkflowHelp")?.classList.add("template-selected");
    $("#briefHighlightSettings")?.classList.toggle("hidden", workflowKind !== "highlight");
    $("#briefContentSettings")?.classList.toggle("hidden", workflowKind !== "content_search");
    $("#briefSpeakerSettings")?.classList.toggle("hidden", workflowKind !== "speaker_edit");
    $("#briefPersonSettings")?.classList.toggle("hidden", workflowKind !== "person_edit");
    const copy = {
      highlight: "可直接开始；高光主题会作为附加约束",
      content_search: "在检索要求框中描述内容，系统会自动选择必要证据",
      person_edit: "无需填写指令，确认素材范围后开始识别画面人物",
      speaker_edit: "无需填写指令，可指定预计人数后开始识别说话人",
    }[workflowKind] || "先选择一种处理方式";
    updateCapabilitySelection(workflowDisplayName(workflowKind), "使用现有快速模式；可在具体功能中切换处理方式");
    if (chatInput) {
      chatInput.placeholder = "任务开始后可继续补充要求……";
      chatInput.disabled = true;
    }
    const submitHint = $("#briefSubmitHint");
    if (submitHint) submitHint.textContent = {
      content_search: "填写上方检索要求后开始，结果会进入内容检索审核",
      highlight: "可以直接开始，也可以在对话区补充高光主题",
      person_edit: "确认素材范围后开始识别画面人物",
      speaker_edit: "确认素材范围和预计人数后开始识别说话人",
    }[workflowKind] || "先选择一种剪辑方式，再在对应设置中开始处理";
    setBriefCreationStatus("已选择", copy);
    setBriefCreationError("");
    updateComposerBeam();
    if (renderDetail) {
      const defaultOption = BRIEF_CAPABILITY_GROUPS[workflowKind]?.options.find((option) => option.workflow === workflowKind);
      renderCapabilityGroup(workflowKind, selectedId || defaultOption?.id || "");
    } else {
      renderCapabilityGroup(workflowKind, selectedId);
    }
    if (workflowKind === "content_search") {
      requestAnimationFrame(() => $("#briefContentInstruction")?.focus({ preventScroll: true }));
    }
  };
  const examples = document.createElement("div");
  examples.className = "brief-goal-examples";
  examples.setAttribute("aria-label", "剪辑要求示例");
  for (const [label, goal] of [["访谈精华", "剪成 3 分钟访谈精华，保留完整观点，去掉重复表达并添加字幕"], ["竖屏发布", "制作 60 秒竖屏短视频，保留重点内容，使用虚化背景补边并添加字幕"], ["查找内容", "查找视频中关于产品介绍的完整片段，保留上下文"]]) {
    const example = document.createElement("button");
    example.type = "button";
    example.textContent = label;
    example.onclick = () => {
      activateAgentPath();
      agentGoalInput.value = goal;
      agentGoalInput.dispatchEvent(new Event("input", { bubbles: true }));
      agentGoalInput.focus();
    };
    examples.append(example);
  }
  agentGoalInput?.closest("label")?.after(examples);
  const agentExecutionMode = briefRoot.querySelector("#briefAgentExecutionMode");
  const submitAgent = () => createAgentJobFromBrief({
    goal: agentGoalInput?.value || "", skillId: agentSkillSelect?.value || "",
    executionMode: agentExecutionMode?.value || "autonomous_review",
  });
  briefRoot.querySelector("[data-start-agent]")?.addEventListener("click", submitAgent);
  briefRoot.querySelector("[data-open-agent-registry]")?.addEventListener("click", () => {
    window.ClipTalkAgentWorkspace?.openRegistry();
  });
  agentGoalInput?.addEventListener("focus", () => {
    if (pendingCreateWorkflowKind) activateAgentPath();
  });
  agentGoalInput?.addEventListener("input", () => setBriefCreationError(""));
  agentGoalInput?.addEventListener("keydown", (event) => {
    if (event.key === "Enter" && (event.ctrlKey || event.metaKey)) {
      event.preventDefault();
      submitAgent();
    }
  });
  agentSkillSelect?.addEventListener("change", () => {
    const selectedLabel = String(agentSkillSelect.selectedOptions?.[0]?.textContent || "").split(" · ")[0] || "手动指定";
    activateAgentPath({ category: "manual", preserveSkill: true });
    updateCapabilitySelection(agentSkillSelect.value ? `高级指定 · ${selectedLabel}` : "自动选择", agentSkillSelect.value ? "已从高级设置锁定底层 Skill" : "系统会在方案中说明使用了哪些能力", agentSkillSelect.value ? "manual" : "auto");
    if (!agentSkillSelect.value) setTopCapability("auto");
  });
  briefRoot.querySelectorAll("[data-workflow-choice]").forEach((button) => button.addEventListener("click", () => selectWorkflow(button.dataset.workflowChoice)));
  briefRoot.querySelectorAll("[data-capability-category]:not([data-workflow-choice])").forEach((button) => button.addEventListener("click", () => {
    const category = button.dataset.capabilityCategory || "auto";
    if (category === "auto") {
      activateAgentPath();
      agentGoalInput?.focus({ preventScroll: true });
      return;
    }
    activateAgentPath({ category, keepDetail: true });
    renderCapabilityGroup(category);
    const group = BRIEF_CAPABILITY_GROUPS[category];
    updateCapabilitySelection(group.title, "请选择下方具体功能");
    setBriefCreationStatus("选择具体功能", group.summary);
  }));
  briefRoot.querySelectorAll("[data-start-workflow]").forEach((button) => button.addEventListener("click", () => {
    if (button.dataset.startWorkflow === "highlight") createJobFromBrief({ allowDefaultHighlight: true });
    else if (button.dataset.startWorkflow === "speaker_edit") createVoiceDiscoveryJobFromUpload({ autoStart: true });
    else if (button.dataset.startWorkflow === "person_edit") createPersonDiscoveryJobFromUpload();
  }));
  const contentInstructionInput = briefRoot.querySelector("#briefContentInstruction");
  const submitContentSearch = () => createJobFromBrief({ instructionOverride: contentInstructionInput?.value || "" });
  briefRoot.querySelector("[data-start-content-search]")?.addEventListener("click", submitContentSearch);
  contentInstructionInput?.addEventListener("input", () => {
    setBriefCreationError("");
  });
  contentInstructionInput?.addEventListener("keydown", (event) => {
    if (event.key === "Enter" && (event.ctrlKey || event.metaKey)) {
      event.preventDefault();
      submitContentSearch();
    }
  });
  const autoInstructionInput = briefRoot.querySelector("#briefAutoInstruction");
  const submitAutoWorkflow = () => createJobFromBrief({
    instructionOverride: autoInstructionInput?.value || "",
  });
  briefRoot.querySelector("[data-start-auto-workflow]")?.addEventListener("click", submitAutoWorkflow);
  autoInstructionInput?.addEventListener("input", () => setBriefCreationError(""));
  autoInstructionInput?.addEventListener("keydown", (event) => {
    if (event.key === "Enter" && (event.ctrlKey || event.metaKey)) {
      event.preventDefault();
      submitAutoWorkflow();
    }
  });

  briefRoot.querySelectorAll("[data-intent-choice]").forEach((button) => button.addEventListener("click", () => {
    pendingCreateWorkflowKind = button.dataset.intentChoice || "";
    pendingCreateIntentMode = pendingCreateWorkflowKind || "auto";
    $("#briefIntentClarification")?.classList.add("hidden");
    if (["person_edit", "speaker_edit"].includes(pendingCreateWorkflowKind)) {
      selectWorkflow(pendingCreateWorkflowKind);
    } else createJobFromBrief();
  }));
  ["#briefSourceScope"].forEach((selector) => {
    $(selector)?.addEventListener("change", () => {
      syncBriefSettingsUi();
      setBriefCreationError("");
    });
  });
  ["#briefScopeStart", "#briefScopeEnd"].forEach((selector) => {
    $(selector)?.addEventListener("input", () => setBriefCreationError(""));
  });
  syncBriefSettingsUi();
  if (chatInput) {
    chatInput.disabled = true;
    chatInput.value = "";
    chatInput.placeholder = "任务开始后可继续补充要求……";
  }
  updateComposerBeam();
  if (briefCard) {
    requestAnimationFrame(() => {
      briefCard.scrollIntoView({ behavior: "smooth", block: "start" });
    });
  }
  syncStageNavigationPlacement();
  setAssistantPanelExpanded(true);
  syncUnifiedWorkspaceView();
}
// Kept as a compatibility fallback for embedded clients that still open the
// legacy creation card explicitly; the default upload path now uses a draft.
window.ClipTalkLegacyBriefCard = showBriefCard;
async function prepareResumableUpload(file, onProgress, { creationSessionId = currentCreationSessionId } = {}) {
  const sessionPart = String(creationSessionId || "legacy");
  const storageKey = `cliptalk-upload:${sessionPart}:${file.name}:${file.size}:${file.lastModified}`;
  let upload = null;
  const remembered = localStorage.getItem(storageKey);
  if (remembered) {
    try { ({ upload } = await api(`/api/uploads/${remembered}`)); }
    catch { localStorage.removeItem(storageKey); }
  }
  if (!upload || Number(upload.size) !== file.size) {
    ({ upload } = await apiJson("/api/uploads", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { filename: file.name, size: file.size },
    }));
    localStorage.setItem(storageKey, upload.id);
  }
  let offset = Number(upload.offset || 0);
  const chunkSize = 4 * 1024 * 1024;
  while (offset < file.size) {
    const chunk = file.slice(offset, Math.min(file.size, offset + chunkSize));
    try {
      ({ upload } = await api(`/api/uploads/${upload.id}`, {
        method: "PATCH", headers: { "Upload-Offset": String(offset), "Content-Type": "application/offset+octet-stream" },
        body: chunk,
      }));
    } catch (error) {
      // The server may have committed a chunk even if its response was lost.
      // Re-read the durable offset before deciding which bytes to retry.
      try { ({ upload } = await api(`/api/uploads/${upload.id}`)); }
      catch { throw error; }
    }
    const nextOffset = Number(upload.offset || 0);
    if (nextOffset <= offset) throw new Error("分片上传没有取得进展，请检查网络后重试");
    offset = nextOffset;
    onProgress?.(offset / file.size);
  }
  return { id: upload.id, storageKey };
}

async function createAgentJobFromBrief({ goal = "", skillId = "", executionMode = "autonomous_review" } = {}) {
  if (!await requireSetupCapability("agent")) return;
  if (!videoInput.files.length || actionBusy) return;
  const normalizedGoal = String(goal || "").trim();
  if (normalizedGoal.length < 5) {
    setBriefCreationError("请具体描述想得到的成片；例如内容重点、目标时长、人物或呈现方式。");
    $("#briefAgentGoal")?.focus();
    return;
  }
  let briefOptions;
  try {
    briefOptions = activeBriefOptions(normalizedGoal);
  } catch (error) {
    setBriefCreationError(error.message);
    return;
  }
  const createGeneration = workspaceGeneration;
  const file = videoInput.files[0];
  const goalInput = $("#briefAgentGoal");
  const skillSelect = $("#briefAgentSkillSelect");
  const executionModeSelect = $("#briefAgentExecutionMode");
  const submitButton = $("[data-start-agent]");
  actionBusy = true;
  setBriefCreationError("");
  syncWorkspaceState({ uploading: true, home: false });
  if (goalInput) goalInput.disabled = true;
  if (skillSelect) skillSelect.disabled = true;
  if (executionModeSelect) executionModeSelect.disabled = true;
  if (submitButton) submitButton.disabled = true;
  setComposerSending(true);
  setBriefCreationStatus("上传中", "正在建立 Agent 素材工作区；此时不会启动内容分析");
  try {
    const resumable = file.size >= 16 * 1024 * 1024
      ? await prepareResumableUpload(file, (fraction) => {
          setBriefCreationStatus("上传中", `正在上传视频 ${Math.round(fraction * 100)}%`);
        })
      : null;
    const form = window.ClipTalkTaskCreation.buildForm({
      file, uploadSessionId: resumable?.id || "", instruction: normalizedGoal,
      taskMode: "content_extract", sourceScope: briefOptions.sourceScope,
      entryWorkflow: "agent", workflowKind: "content_search",
    });
    if (resumable) setBriefCreationStatus("校验中", "视频上传完成，正在校验媒体并建立工作区……");
    const { job } = await api("/api/jobs", { method: "POST", body: form });
    if (resumable) localStorage.removeItem(resumable.storageKey);
    if (createGeneration !== workspaceGeneration || !homeNavigationRequested) return;
    homeNavigationRequested = false;
    renderJob(job);
    await window.ClipTalkAgentWorkspace?.submitGoal(normalizedGoal, {
      skillId: String(skillId || ""), executionMode: String(executionMode || "autonomous_review"),
    });
  } catch (error) {
    if (createGeneration !== workspaceGeneration) return;
    setBriefCreationError(error.message || "Agent 任务创建失败");
    setBriefCreationStatus("视频已就绪", "检查配置或网络后重新生成计划");
  } finally {
    if (createGeneration === workspaceGeneration) {
      actionBusy = false;
      setComposerSending(false);
      if (!currentJob && goalInput) goalInput.disabled = false;
      if (!currentJob && skillSelect) skillSelect.disabled = false;
      if (!currentJob && executionModeSelect) executionModeSelect.disabled = false;
      if (!currentJob && submitButton) submitButton.disabled = false;
      updateComposerBeam();
      if (!currentJob) syncWorkspaceState({ home: false });
    }
  }
}

async function createJobFromBrief({ allowDefaultHighlight = false, instructionOverride = null } = {}) {
  if (!await requireSetupCapability("create")) return;
  if (!videoInput.files.length || actionBusy) return;
  const createGeneration = workspaceGeneration;
  let taskMode = pendingCreateIntentMode || "auto";
  const contentInstructionInput = $("#briefContentInstruction");
  const highlightInstructionInput = $("#briefHighlightInstruction");
  const autoInstructionInput = $("#briefAutoInstruction");
  const contentSubmitButton = $("[data-start-content-search]");
  const autoSubmitButton = $("[data-start-auto-workflow]");
  const inlineInstruction = pendingCreateWorkflowKind === "highlight"
    ? highlightInstructionInput?.value || autoInstructionInput?.value
    : pendingCreateWorkflowKind === "content_search"
      ? contentInstructionInput?.value || autoInstructionInput?.value
      : autoInstructionInput?.value || chatInput?.value;
  let contentInstruction = String(instructionOverride ?? inlineInstruction ?? "").trim();
  if (!contentInstruction && allowDefaultHighlight && pendingCreateWorkflowKind === "highlight") {
    contentInstruction = "自动分析所选素材并生成高光视频";
  }
  if (!contentInstruction) {
    const usesContentDialog = pendingCreateWorkflowKind === "content_search" && contentInstructionInput;
    setBriefCreationError(usesContentDialog
      ? "请先在检索要求框中描述想找的内容。"
      : "请先在对话框描述你想得到的剪辑结果。");
    (usesContentDialog ? contentInstructionInput : chatInput)?.focus();
    return;
  }
  let briefOptions;
  try {
    briefOptions = activeBriefOptions(contentInstruction);
  } catch (error) {
    setBriefCreationError(error.message);
    return;
  }
  const useHighlightSettings = pendingCreateWorkflowKind === "highlight"
    && !$("#briefHighlightSettings")?.classList.contains("hidden");
  const highlightTargetValue = useHighlightSettings
    ? String($("#briefHighlightTargetSeconds")?.value || "").trim()
    : "";
  const highlightTargetSeconds = highlightTargetValue === "" ? "" : Number(highlightTargetValue);
  const highlightVariantCount = useHighlightSettings
    ? Math.max(1, Math.min(4, Number($("#briefHighlightVariantCount")?.value || 3)))
    : "";
  if (useHighlightSettings
    && highlightTargetValue !== ""
    && (!Number.isFinite(highlightTargetSeconds) || highlightTargetSeconds < 4)) {
    setBriefCreationError("目标成片时长至少为 4 秒，或留空让系统自动确定。");
    return;
  }
  if (!pendingCreateWorkflowKind) {
    const predictedWorkflow = await classifyBriefWorkflow(contentInstruction);
    if (!predictedWorkflow || createGeneration !== workspaceGeneration) return;
    pendingCreateWorkflowKind = predictedWorkflow;
    taskMode = predictedWorkflow === "highlight" ? "highlight" : "content_extract";
    pendingCreateIntentMode = taskMode;
    $("#chatMessages")?.querySelectorAll("[data-workflow-choice]").forEach((button) => {
      const selected = button.dataset.workflowChoice === predictedWorkflow;
      button.classList.toggle("selected", selected);
      button.setAttribute("aria-pressed", String(selected));
    });
  }
  if (taskMode === "content_extract") {
    if (briefOptions.duration > 600 && briefOptions.effectiveEnd - briefOptions.effectiveStart >= briefOptions.duration - .2) {
      const confirmed = await requestActionConfirmation({
        title: "确认检索全片",
        summary: `当前视频长 ${formatTime(briefOptions.duration)}，全片检索会处理更多索引证据。`,
        details: ["可以在任务设置中选择前半段、后半段或自定义范围", "系统会根据描述自动选择必要的识别能力"],
      });
      if (!confirmed) return;
    }
  }
  actionBusy = true;
  setBriefCreationError("");
  syncWorkspaceState({ uploading: true, home: false });
  if (chatInput) chatInput.disabled = true;
  if (contentInstructionInput) contentInstructionInput.disabled = true;
  if (highlightInstructionInput) highlightInstructionInput.disabled = true;
  if (autoInstructionInput) autoInstructionInput.disabled = true;
  if (contentSubmitButton) contentSubmitButton.disabled = true;
  if (autoSubmitButton) autoSubmitButton.disabled = true;
  setComposerSending(true);
  setBriefCreationStatus("上传中", "正在上传并校验源视频……");
  try {
    const file = videoInput.files[0];
    const resumable = file.size >= 16 * 1024 * 1024
      ? await prepareResumableUpload(file, (fraction) => {
          setBriefCreationStatus("上传中", `正在上传视频 ${Math.round(fraction * 100)}%`);
        })
      : null;
    const form = window.ClipTalkTaskCreation.buildForm({
      file, uploadSessionId: resumable?.id || "", instruction: contentInstruction, taskMode,
      sourceScope: briefOptions.sourceScope, workflowKind: pendingCreateWorkflowKind,
      targetSeconds: useHighlightSettings ? highlightTargetSeconds : "",
      variantCount: useHighlightSettings ? highlightVariantCount : "",
    });
    if (resumable) setBriefCreationStatus("校验中", "视频上传完成，正在校验媒体信息……");
    const { job } = await api("/api/jobs", { method: "POST", body: form });
    if (resumable) localStorage.removeItem(resumable.storageKey);
    // The user may have returned home or started another task while the
    // upload was in flight. Never let that old upload reopen itself.
    if (createGeneration !== workspaceGeneration || !homeNavigationRequested) return;
    homeNavigationRequested = false;
    if (chatInput) chatInput.value = "";
    if (contentInstructionInput) contentInstructionInput.value = "";
    renderJob(job);
    pollJob();
  } catch (error) {
    if (createGeneration !== workspaceGeneration) return;
    if (error.code === "intent_confirmation_required") {
      syncWorkspaceState({ routingConfirmation: true, home: false });
      $("#briefIntentClarification")?.classList.remove("hidden");
      $("#briefIntentClarification")?.scrollIntoView({ behavior: "smooth", block: "nearest" });
    } else {
      setBriefCreationError(error.message);
    }
    setBriefCreationStatus("视频已就绪", "修正后在下方重新发送即可");
  } finally {
    if (createGeneration === workspaceGeneration) {
      actionBusy = false;
      setComposerSending(false);
      if (!currentJob && chatInput) {
        chatInput.disabled = true;
      }
      if (!currentJob && contentInstructionInput) contentInstructionInput.disabled = false;
      if (!currentJob && highlightInstructionInput) highlightInstructionInput.disabled = false;
      if (!currentJob && autoInstructionInput) autoInstructionInput.disabled = false;
      if (!currentJob && contentSubmitButton) contentSubmitButton.disabled = false;
      if (!currentJob && autoSubmitButton) autoSubmitButton.disabled = false;
      updateComposerBeam();
      if (!currentJob) syncWorkspaceState({
        home: false,
        routingConfirmation: !$("#briefIntentClarification")?.classList.contains("hidden"),
      });
    }
  }
}

async function createVoiceDiscoveryJobFromUpload({ autoStart = false } = {}) {
  if (!await requireSetupCapability("create")) return;
  if (!videoInput.files.length || actionBusy) return;
  const createGeneration = workspaceGeneration;
  const file = videoInput.files[0];
  const expectedVoiceCount = Number($("#briefExpectedVoiceCount")?.value || 0);
  let briefOptions;
  try {
    briefOptions = Number($("#localPreviewVideo")?.duration || 0) > 0
      ? activeBriefOptions("识别本视频中的说话人")
      : { sourceScope: { kind: "all" } };
  } catch (error) {
    setBriefCreationError(error.message);
    return;
  }
  actionBusy = true;
  setBriefCreationError("");
  syncWorkspaceState({ uploading: true, home: false });
  if (chatInput) chatInput.disabled = true;
  setComposerSending(true);
  setBriefCreationStatus("上传中", "正在上传视频；完成后可先设置说话人数。");
  setVoiceProfileStatus("正在正式上传视频；上传完成后请先选择预计说话人数。");
  try {
    const resumable = file.size >= 16 * 1024 * 1024
      ? await prepareResumableUpload(file, (fraction) => {
          const percent = Math.round(fraction * 100);
          setBriefCreationStatus("上传中", `正在上传视频 ${percent}%`);
          setVoiceProfileStatus(`正在上传视频 ${percent}%；完成后再开始识别。`);
        })
      : null;
    const form = window.ClipTalkTaskCreation.buildForm({
      file,
      uploadSessionId: resumable?.id || "",
      instruction: "识别本视频中的说话人",
      taskMode: "content_extract",
      sourceScope: briefOptions.sourceScope,
      entryWorkflow: "voice_discovery",
      workflowKind: "speaker_edit",
    });
    if (resumable) {
      setBriefCreationStatus("校验中", "视频上传完成，正在校验媒体信息……");
      setVoiceProfileStatus("视频上传完成，正在校验音视频信息……");
    }
    const { job } = await api("/api/jobs", { method: "POST", body: form });
    if (resumable) localStorage.removeItem(resumable.storageKey);
    if (createGeneration !== workspaceGeneration || !homeNavigationRequested) return;
    homeNavigationRequested = false;
    if (chatInput) chatInput.value = "";
    renderJob(job);
    openVoiceProfiles();
    if ($("#expectedVoiceCount")) $("#expectedVoiceCount").value = String(expectedVoiceCount);
    if (autoStart) {
      setVoiceProfileStatus("视频已上传，正在区分不同说话人……", "success");
      await discoverCurrentVoices();
    } else {
      setVoiceProfileStatus("视频已上传。请选择预计说话人数，然后点击“开始识别说话人”。", "success");
    }
  } catch (error) {
    if (createGeneration !== workspaceGeneration) return;
    setBriefCreationError(error.message);
    setBriefCreationStatus("视频已就绪", "可以重试“发言剪辑”，无需填写指令");
    setVoiceProfileStatus(`无法开始说话人识别：${error.message}`, "error");
  } finally {
    if (createGeneration === workspaceGeneration) {
      actionBusy = false;
      setComposerSending(false);
      if (!currentJob && chatInput) chatInput.disabled = false;
      updateComposerBeam();
      if (!currentJob) syncWorkspaceState({ home: false });
    }
  }
}

async function createPersonDiscoveryJobFromUpload() {
  if (!await requireSetupCapability("create")) return;
  if (!videoInput.files.length || actionBusy) return;
  const createGeneration = workspaceGeneration;
  const file = videoInput.files[0];
  let briefOptions;
  try {
    briefOptions = Number($("#localPreviewVideo")?.duration || 0) > 0
      ? activeBriefOptions("提取所选画面人物的所有出镜片段")
      : { sourceScope: { kind: "all" } };
  } catch (error) {
    setBriefCreationError(error.message);
    return;
  }
  actionBusy = true;
  setBriefCreationError("");
  syncWorkspaceState({ uploading: true, home: false });
  if (chatInput) chatInput.disabled = true;
  setComposerSending(true);
  setBriefCreationStatus("上传中", "正在上传视频；随后会建立画面人物卡和连续轨迹。");
  try {
    const resumable = file.size >= 16 * 1024 * 1024
      ? await prepareResumableUpload(file, (fraction) => setBriefCreationStatus("上传中", `正在上传视频 ${Math.round(fraction * 100)}%`))
      : null;
    const form = window.ClipTalkTaskCreation.buildForm({
      file, uploadSessionId: resumable?.id || "",
      instruction: "提取所选画面人物的所有出镜片段",
      taskMode: "content_extract", sourceScope: briefOptions.sourceScope,
      entryWorkflow: "person_discovery", workflowKind: "person_edit",
    });
    const { job } = await api("/api/jobs", { method: "POST", body: form });
    if (resumable) localStorage.removeItem(resumable.storageKey);
    if (createGeneration !== workspaceGeneration || !homeNavigationRequested) return;
    homeNavigationRequested = false;
    renderJob(job);
    openPersonProfiles();
    pollJob();
  } catch (error) {
    if (createGeneration !== workspaceGeneration) return;
    setBriefCreationError(error.message);
    setBriefCreationStatus("视频已就绪", "可以重试识别画面人物");
  } finally {
    if (createGeneration === workspaceGeneration) {
      actionBusy = false;
      setComposerSending(false);
      if (!currentJob && chatInput) chatInput.disabled = false;
      updateComposerBeam();
      if (!currentJob) syncWorkspaceState({ home: false });
    }
  }
}

function candidateReviewLabels(candidate) {
  const labels = [];
  const evidenceCount = Array.isArray(candidate?.evidence) ? candidate.evidence.length : 0;
  const audio = candidate?.audioEvidence || {};
  if (evidenceCount) labels.push(`画面 ${evidenceCount}`);
  if (audio.transcriptExcerpt || audio.speakerTurns?.length) labels.push("对白");
  if (audio.audioEvents?.length) labels.push("声音");
  if (Number(candidate?.score || 0) < 70 || !evidenceCount) labels.push("优先复核");
  return labels;
}

function sortedCandidatesForReview(job) {
  const candidates = [...(job?.candidates || [])].filter(speakerMatches);
  if (candidateReviewSort === "time") return candidates.sort((left, right) => Number(left.start) - Number(right.start));
  if (candidateReviewSort === "review") return candidates.sort((left, right) => {
    const risk = (item) => Number(item.score || 0) < 70 || !item.evidence?.length ? 0 : 1;
    return risk(left) - risk(right) || Number(left.start) - Number(right.start);
  });
  return candidates.sort((left, right) => Number(right.score || 0) - Number(left.score || 0) || Number(left.start) - Number(right.start));
}

function timelineSegmentsMatch(left = [], right = [], tolerance = 0.12) {
  if (!Array.isArray(left) || !Array.isArray(right) || left.length !== right.length) return false;
  return left.every((segment, index) => {
    const other = right[index];
    if (!other) return false;
    return Math.abs(Number(segment.start) - Number(other.start)) <= tolerance
      && Math.abs(Number(segment.end) - Number(other.end)) <= tolerance;
  });
}

function timelineEditedAfterLatestOutput(job, generatedVersions = []) {
  const edits = Array.isArray(job?.timelineUndo) ? job.timelineUndo : [];
  if (!edits.length || !generatedVersions.length) return false;
  const latestOutputTime = generatedVersions.reduce((latest, version) => {
    const timestamps = [version?.createdAt, ...(version?.outputs || []).map((output) => output?.versionCreatedAt)]
      .map((value) => Date.parse(String(value || "")))
      .filter(Number.isFinite);
    return Math.max(latest, ...timestamps, 0);
  }, 0);
  if (!latestOutputTime) return false;
  return edits.some((edit) => {
    const editTime = Date.parse(String(edit?.createdAt || ""));
    return Number.isFinite(editTime) && editTime > latestOutputTime;
  });
}

function contentMatchTypeLabel(match = {}) {
  const labels = { speech: "对白", visual: "画面", ocr: "屏幕文字", audio: "声音", person: "匿名人物", audiovisual: "多模态", spoken_question: "口头问题", screen_question: "画面问题", audiovisual_question: "音画问题" };
  const evidenceType = String(match.evidenceType || "").toLowerCase();
  if (labels[evidenceType]) return labels[evidenceType];
  const modalities = [...new Set((match.matchedModalities || []).map((value) => String(value).toLowerCase()).filter(Boolean))];
  if (modalities.length > 1) return "多模态";
  return labels[modalities[0]] || "画面";
}

function contentQuestionSourceKey(match = {}) {
  const source = String(match.questionSource || "").toLowerCase();
  if (source === "spoken" || source === "screen" || source === "both") return source;
  const sources = [...new Set((match.questionSources || []).map((value) => String(value).toLowerCase()))];
  return sources.includes("spoken") && sources.includes("screen") ? "both" : sources[0] || "";
}

function contentQuestionSourceLabel(source = "") {
  return ({ spoken: "口头提问", screen: "画面问题", both: "口头 + 画面" })[String(source).toLowerCase()] || "";
}

function contentMatchMethodLabel(match = {}) {
  const method = String(match.matchType || "").toLowerCase();
  const labels = {
    exact_quote: "对白原句匹配",
    semantic_speech: "对白语义匹配",
    lexical_speech: "对白关键词匹配",
    visual_semantic: "画面语义匹配",
    visual_refined: "连续画面复核",
    visual_dense_fallback: "局部逐帧复检",
    ocr_text: "屏幕文字匹配",
    audio_event: "声音事件匹配",
    anonymous_person: "匿名人物轨迹匹配",
    labeled_person_speaking: "已标记人物发言复核",
    voice_identity: "加密声纹身份匹配",
    current_voice_cluster: "本视频说话人匹配",
    dialogue_response_block: "问答图完整回答",
    screen_question: "画面问题 OCR 识别",
    dialogue_role_turn: "对话角色轮次",
    predicate_match: "原子条件证据匹配",
    multi_predicate: "多条件时间约束匹配",
    multi_evidence: "多类证据交叉匹配",
  };
  return labels[method] || `${contentMatchTypeLabel(match)}证据匹配`;
}

function contentBoundarySourceLabel(source = "") {
  const normalized = String(source || "").toLowerCase();
  const withContext = normalized.endsWith("_with_context");
  const base = withContext ? normalized.slice(0, -"_with_context".length) : normalized;
  const labels = {
    word_timestamps: "字词时间戳",
    grounded_segments: "对白片段定位",
    speech_sentences: "完整句子边界",
    speech_aligned: "对白时间轴",
    visual: "画面窗口",
    visual_window: "画面窗口",
    visual_dense_frames: "连续画面复核",
    targeted_dense_frames: "局部画面复检",
    ocr_stable_range: "文字稳定区间",
    screen_question_card_shot: "问题卡完整镜头",
    screen_question_readable_window: "问题文字可读区间",
    question_evidence_union: "音画问题证据合并",
    audio_window: "声音事件窗口",
    person_track: "匿名人物轨迹",
    grounding_dino_evidence: "画面目标定位",
    speaker_turn_with_visual_verification: "说话人轮次与连续画面复核",
    direct_active_speaker_visual: "人物近景口型与语音活动交叉定位",
    active_speaker_asd: "本地视听主动说话人逐帧定位",
    active_speaker_asd_refined: "候选级主动说话人精修",
    person_track_feedback_refined: "人物逐帧轨迹反馈精修",
    dialogue_word_timestamps: "完整回答词级边界",
    dialogue_turn_timestamps: "完整回答轮次边界",
    dialogue_word_alignment_refined: "反馈后词级边界精修",
    predicate_temporal_join: "条件时间关系连接",
    merged_evidence: "多类证据合并",
    user_manual_trim: "人工调整",
    user_boundary_feedback: "用户反馈",
    feedback_sentence_retry: "句子边界复检",
    voiceprint_speaker_turns: "声纹 Speaker 发言轮次",
  };
  const label = labels[base] || "索引证据窗口";
  return withContext ? `${label}并保留上下文` : label;
}

function contentPersonLabelingMarkup(job, targetState = {}, allowTargetSelection = false) {
  const persons = Array.isArray(job?.contentIndex?.persons) ? job.contentIndex.persons : [];
  // A content index may contain person clusters for many unrelated queries.
  // Do not expose those cards for topic, dialogue, or question searches unless
  // the current query explicitly requires a person target.
  if (!persons.length || !allowTargetSelection) return "";
  const normalizedTarget = typeof targetState === "string"
    ? { personIds: targetState ? [targetState] : [], matchMode: "any" }
    : (targetState || {});
  const targetPersonIds = new Set((normalizedTarget.personIds || []).map(String));
  const speaking = String(normalizedTarget.activity || "") === "speaking";
  const inferredMode = normalizedTarget.matchMode || "any";
  const personWorkflow = workflowKindForJob(job) === "person_edit";
  const anyLabel = speaking ? "任一人发言" : "任一人物出现";
  const allLabel = speaking ? "所有人都发言（同一对话）" : "所有人物同时出现";
  const history = Array.isArray(job?.contentPersonTargetHistory) ? job.contentPersonTargetHistory.slice(-8).reverse() : [];
  const historyMarkup = history.length ? `<div class="content-person-history"><strong>已确认人物</strong>${history.map((item) => {
    const ids = (item.personIds || []).map(String).filter(Boolean);
    const labels = (item.labels || ids).filter(Boolean);
    const historyActivity = item.activity === "speaking" ? "speaking" : "appearance";
    const relationLabel = historyActivity === "speaking"
      ? (item.matchMode === "all" ? "同一对话中全部发言" : "任一人物发言")
      : (item.matchMode === "all" ? "所有人物同时出现" : "任一人物出现");
    return `<div class="content-person-history-row"><span>${escapeHtml(labels.join("、"))}<small>${relationLabel}</small></span><button type="button" data-person-history-target="${escapeHtml(ids.join(","))}" data-person-history-mode="${escapeHtml(item.matchMode || "any")}" data-person-history-activity="${historyActivity}">查看这些片段</button></div>`;
  }).join("")}</div>` : "";
  return `<details class="content-person-panel" data-person-target-panel data-person-target-activity="${speaking ? "speaking" : "appearance"}" ${targetPersonIds.size ? "" : "open"}>
    <summary><span>确认视频中的人物</span><small>${persons.length} 个画面人物簇 · 可能包含同一人物的重复卡片</small></summary>
    <div class="content-person-grid">${persons.map((person) => {
      const isTarget = targetPersonIds.has(String(person.id || ""));
      const speaker = person.primarySpeaker
        ? `${escapeHtml(person.primarySpeaker)} · ${Math.round(Number(person.speakerConfidence || 0) * 100)}%${person.speakerReviewRequired ? " · 需复核" : ""}`
        : "尚未关联说话人";
      return `<article class="content-person-card${isTarget ? " target" : ""}" data-content-person="${escapeHtml(person.id)}"${isTarget ? ' data-content-person-target-state="true"' : ""}>
        <button type="button" class="content-person-preview" data-person-preview="${escapeHtml(person.id)}" data-person-preview-time="${escapeHtml(person.representativeTime ?? 0)}" aria-label="预览${escapeHtml(person.label || person.defaultLabel || "匿名人物")}的代表画面"><img src="${escapeHtml(person.thumbnailUrl || "")}" alt="${escapeHtml(person.label || person.defaultLabel || "匿名人物")}" loading="lazy"><em>预览</em></button>
        <div><small>${isTarget ? "本次检索人物 · " : ""}${escapeHtml(person.defaultLabel || person.id)}</small><strong>${escapeHtml(person.label || person.defaultLabel || person.id)}${isTarget ? " · 已选择" : ""}</strong><span>${speaker}</span></div>
        <div class="content-person-actions">${allowTargetSelection ? `<label class="content-person-choice"><input type="checkbox" data-person-target value="${escapeHtml(person.id)}" ${isTarget ? "checked" : ""}><span>${isTarget ? "已选择" : "选择人物"}</span></label>` : ""}<button type="button" data-person-label="${escapeHtml(person.id)}" data-person-current-label="${escapeHtml(person.label || "")}">${person.userLabeled ? "修改标签" : "添加标签（可选）"}</button></div>
      </article>`;
    }).join("")}</div>${allowTargetSelection ? `<div class="content-person-target-controls" data-person-target-controls data-selection-count="${targetPersonIds.size}">
      <div><strong data-person-selection-summary>已选 ${targetPersonIds.size} 人</strong><small>${speaking ? "选择发言匹配方式" : "选择画面匹配方式"}</small></div>
      <div class="content-person-match-modes${targetPersonIds.size > 1 ? "" : " hidden"}" role="radiogroup" aria-label="人物匹配方式">
        <label><input type="radio" name="content-person-match-mode" data-person-match-mode value="any" ${inferredMode === "any" ? "checked" : ""}><span>${anyLabel}</span></label>
        <label><input type="radio" name="content-person-match-mode" data-person-match-mode value="all" ${inferredMode === "all" ? "checked" : ""}><span>${allLabel}</span></label>
      </div>
      ${personWorkflow ? `<label class="content-person-activity"><input type="checkbox" data-person-speaking-only ${speaking ? "checked" : ""}><span>只保留所选人物正在说话的片段</span><small>开启后才会运行人物与声音关联；默认提取所有出镜画面</small></label>` : ""}
      <button type="button" class="primary" data-person-target-confirm>${speaking ? "识别并整理发言片段" : "直接整理出镜片段"}</button>
    </div>` : ""}
    ${historyMarkup}<p>${allowTargetSelection ? "可选择一个或多个人物；选择两人并使用“所有人都发言（同一对话）”可查找两人对话。添加项目内标签是可选操作。" : ""} 建议使用项目内标签或匿名编号。系统不会把外观推断当成真实身份或性别事实。</p>
  </details>`;
}

function contentDialogueModeNeedsChoice(search = {}, predicate = null) {
  if (!predicate) return false;
  if (search.dialogueModeNeedsChoice === true || predicate.dialogueModeNeedsChoice === true) return true;
  if (search.dialogueModeNeedsChoice === false || predicate.dialogueModeNeedsChoice === false) return false;
  const query = String(search.intent?.query || search.instruction || predicate.value || "");
  const explicit = /(?:仅|只)(?:要|找|保留|输出|截取)?.{0,5}(?:问题|提问|回答)|完整(?:问答|对答)|问题.{0,5}(?:和|与|及|连同).{0,5}回答|(?:问题|回答).{0,5}拆分|包含.{0,5}(?:前置)?提问|不包含.{0,5}(?:提问|回答)/.test(query);
  return !explicit;
}

function contentBasketActionRelevant(job = currentJob) {
  return contentSearchRecordsForJob(job).length > 1 || contentBasketItems(job).length > 0;
}

function formatSearchElapsed(milliseconds) {
  const value = Math.max(0, Number(milliseconds) || 0);
  if (value < 1000) return value > 0 ? "不到 1 秒" : "即时完成";
  return `${(value / 1000).toFixed(value >= 10000 ? 1 : 2)} 秒`;
}

function effectiveContentSearchCompleteness(search = {}) {
  const raw = search.completeness && typeof search.completeness === "object" ? search.completeness : {};
  const schema = String(search.intent?.schemaVersion || "");
  const voiceCandidateSearch = [
    "voice-target-intent-v1", "current-voice-target-intent-v1", "current-voice-target-intent-v2",
  ].includes(schema);
  if (!voiceCandidateSearch) return raw;
  const candidates = Array.isArray(search.candidates) ? search.candidates : [];
  const candidateEvents = Array.isArray(search.candidateEvents) ? search.candidateEvents : [];
  const optionalIds = candidates
    .filter((item) => item.requiresReview || item.reviewStatus === "pending")
    .map((item) => String(item.id || ""))
    .filter(Boolean);
  return {
    ...raw,
    status: "complete",
    complete: true,
    reviewComplete: true,
    pendingCandidateIds: [],
    pendingCount: 0,
    optionalCandidateIds: optionalIds,
    possibleCount: optionalIds.length,
    occurrenceCount: Number(raw.occurrenceCount ?? candidates.length),
    clipCount: Number(raw.clipCount ?? (candidateEvents.length || candidates.length)),
  };
}

function contentSearchReviewMarkup(job, search = job.contentSearch || {}, { historicalExpanded = false } = {}) {
  const activeSearchId = String(job?.contentSearchSession?.activeSearchId || job?.contentSearch?.id || "");
  const isCurrentSearch = !search?.id || String(search.id) === activeSearchId;
  const personWorkflow = workflowKindForJob(job) === "person_edit";
  const retainedSearchesMarkup = isCurrentSearch && !personWorkflow
    ? contentRetainedSearchesMarkup(job, String(search?.id || activeSearchId))
    : "";
  const historicalSearchClass = isCurrentSearch ? "" : ` historical${historicalExpanded ? "" : " collapsed"}`;
  const historicalSearchToggle = isCurrentSearch
    ? ""
    : `<button type="button" class="content-history-toggle" data-content-search-history-toggle aria-expanded="true">收起已保留结果</button>`;
  const restoreButtonMarkup = contentRestoreButtonMarkup(job, search?.id || "");
  const candidates = Array.isArray(search.candidates) ? search.candidates : [];
  const candidateEvents = Array.isArray(search.candidateEvents) ? search.candidateEvents : [];
  const candidateById = new Map(candidates.map((candidate) => [String(candidate.id), candidate]));
  const eventByMatchId = new Map();
  const reviewCandidates = candidateEvents.length
    ? candidateEvents.flatMap((event) => (event.matchIds || []).map((id, eventIndex) => {
      const candidate = candidateById.get(String(id));
      if (candidate) eventByMatchId.set(String(id), { event, eventIndex, eventSize: (event.matchIds || []).length });
      return candidate;
    }).filter(Boolean))
    : candidates;
  const reliableCandidates = candidates.filter((match) => String(match.confidenceTier || (match.requiresReview ? "possible" : "reliable")) === "reliable");
  const possibleCandidates = candidates.filter((match) => String(match.confidenceTier || (match.requiresReview ? "possible" : "reliable")) !== "reliable");
  const pendingPossibleCandidates = possibleCandidates.filter((match) => {
    const status = String(match.reviewStatus || (match.requiresReview ? "pending" : "confirmed"));
    return status === "pending";
  });
  const keptPossibleCandidates = possibleCandidates.filter((match) => String(match.reviewStatus || "") === "kept");
  const reliableCount = reliableCandidates.length;
  const pendingPossibleCount = pendingPossibleCandidates.length;
  const keptPossibleCount = keptPossibleCandidates.length;
  const questionSourceCounts = candidates.reduce((counts, match) => {
    const source = contentQuestionSourceKey(match);
    if (source) counts[source] = (counts[source] || 0) + 1;
    return counts;
  }, {});
  const hasQuestionSources = Object.keys(questionSourceCounts).length > 0;
  const localFilterState = contentSearchFilterState.get(`${job?.id || ""}:${search?.id || ""}`) || {};
  const questionSourceFilter = String(localFilterState.questionSource || "all");
  const boundaryRetryIds = new Set((job.contentSearchFeedback?.boundaryRetryMatchIds || search.feedback?.boundaryRetryMatchIds || []).map(String));
  const reviewDraft = search.reviewDraft && search.id && search.reviewDraft.searchId === search.id
    ? search.reviewDraft : {};
  const defaults = new Set((reviewDraft.selectedMatchIds || search.defaultSelectedIds || []).map(String));
  const selectableCandidates = candidates.filter((match) => {
    const status = String(match.reviewStatus || (match.requiresReview ? "pending" : "confirmed"));
    return status !== "rejected";
  });
  const allSelectableSelected = selectableCandidates.length > 0
    && selectableCandidates.every((match) => defaults.has(String(match.id)));
  const bulkControlsMarkup = candidates.length ? `<div class="content-exhaustive-controls content-exhaustive-controls-bottom"><span>选择片段${candidates.length > 50 ? ` · 首屏显示 50 段，共 ${candidates.length} 段` : ""}</span><button type="button" data-content-select="toggle" aria-pressed="${allSelectableSelected ? "true" : "false"}">${allSelectableSelected ? "取消全部" : "选择全部"}</button></div>` : "";
  const questionSourceControls = hasQuestionSources ? `<label class="content-question-source-filter"><span>问题来源</span><select data-content-question-source><option value="all" ${questionSourceFilter === "all" ? "selected" : ""}>全部（${Object.values(questionSourceCounts).reduce((sum, value) => sum + value, 0)}）</option><option value="spoken" ${questionSourceFilter === "spoken" ? "selected" : ""}>口头提问（${questionSourceCounts.spoken || 0}）</option><option value="screen" ${questionSourceFilter === "screen" ? "selected" : ""}>画面问题（${questionSourceCounts.screen || 0}）</option><option value="both" ${questionSourceFilter === "both" ? "selected" : ""}>口头 + 画面（${questionSourceCounts.both || 0}）</option></select></label>` : "";
  const draftOutputMode = String(reviewDraft.outputMode || "single_reel");
  const draftOrderMode = String(reviewDraft.orderMode || "source");
  const stats = search.retrievalStats || {};
  const exhaustive = String(search.resultMode || search.queryPlan?.result?.mode || "top_k") === "exhaustive";
  const coverageComplete = Boolean(search.coverageComplete ?? stats.coverageComplete);
  const denseSearchCompleted = Boolean(
    stats.denseSearchRequested
    || (stats.fallbackReasons || []).includes("user_requested_dense_search"),
  );
  const completeness = effectiveContentSearchCompleteness(search);
  const completenessStatus = String(completeness.status || (coverageComplete ? "complete" : "incomplete"));
  const strictComplete = !exhaustive || completenessStatus === "complete";
  const occurrenceCount = Number(completeness.occurrenceCount ?? candidates.length);
  const clipCount = Number(completeness.clipCount ?? candidates.length);
  const exhaustiveTitle = completenessStatus === "complete"
    ? `检索覆盖完成 · 找到 ${occurrenceCount} 处证据，整理为 ${clipCount} 段`
    : `已找到 ${reliableCount + keptPossibleCount} 个可用片段 · 检索覆盖待确认`;
  const completenessRows = [
    ...(Array.isArray(completeness.channels) ? completeness.channels : []),
    ...(pendingPossibleCount ? [{ id: "possible_results", label: "待试听复核（可选）", complete: true, detail: `${pendingPossibleCount} 个证据较弱片段尚未保留` }] : []),
    ...(keptPossibleCount ? [{ id: "kept_possible_results", label: "已试听保留", complete: true, detail: `${keptPossibleCount} 个证据较弱片段已由你确认保留` }] : []),
    ...(completeness.expectedOccurrenceCount == null ? [] : [{
      id: "expected_count", label: `明确预期至少 ${Number(completeness.expectedOccurrenceCount)} 处`,
      complete: Boolean(completeness.expectedCountSatisfied), detail: `当前检出 ${occurrenceCount} 处`,
    }]),
  ];
  const completenessMarkup = exhaustive ? `<details class="content-completeness ${escapeHtml(completenessStatus)}"><summary><strong>${completenessStatus === "complete" ? "全片检索完整度已确认" : "检索范围仍有未完成分析"}</strong><span>查看检索诊断</span></summary><div><strong>完整性报告</strong><span>“处”是独立证据，“段”是整理后的输出片段。</span></div><ul>${completenessRows.map((item) => `<li class="${item.complete ? "complete" : "incomplete"}"><b>${item.complete ? "✓" : "!"}</b><span><strong>${escapeHtml(item.label || "检查项")}</strong><small>${escapeHtml(item.detail || "")}</small></span></li>`).join("")}</ul>${(completeness.warnings || []).length ? `<p>${completeness.warnings.map(escapeHtml).join(" ")}</p>` : ""}</details>` : "";
  const evidenceHitCount = Number(stats.evidenceHitCount ?? stats.localRecallCount ?? 0);
  const semanticBatchText = Number(stats.semanticBatchCount || 0) > 1
    ? ` · 语义复核 ${Number(stats.semanticBatchesCompleted || 0)}/${Number(stats.semanticBatchCount || 0)} 批`
    : "";
  const evidenceReuseText = Number(stats.reusedEvidenceCount || 0) > 0
    ? ` · 复用 ${Number(stats.reusedEvidenceCount || 0)} 条历史证据${Number(stats.reusedDuration || 0) > 0 ? ` / ${formatTime(Number(stats.reusedDuration || 0))}` : ""}`
    : Number(stats.availableReusableEvidenceCount || 0) > 0
      ? ` · 已检查 ${Number(stats.availableReusableEvidenceCount || 0)} 条可复用证据`
      : "";
  const predicateReuseText = Number(stats.predicateCacheHits || 0) > 0
    ? ` · ${Number(stats.predicateCacheHits || 0)} 个条件直接复用`
    : "";
  const adaptiveRounds = Number(stats.semanticAdaptiveRounds || 0);
  const adaptiveStopReason = String(stats.adaptiveStopReason || "");
  const adaptiveStopLabels = {
    target_satisfied: "已达到目标数量",
    target_satisfied_after_visual_scan: "补检画面后达到目标数量",
    candidate_pool_exhausted: "已复核全部召回候选",
    search_space_exhausted: "已检查全部相关范围",
  };
  const adaptiveText = stats.adaptiveExpansionTriggered
    ? ` · 已自动补检 ${Math.max(1, adaptiveRounds - 1)} 轮${adaptiveStopLabels[adaptiveStopReason] ? `，${adaptiveStopLabels[adaptiveStopReason]}` : ""}`
    : stats.earlyStop && adaptiveStopReason === "target_satisfied"
      ? ` · 达到目标数量后停止${Number(stats.semanticUnitsSkippedAfterRanking || 0) ? `，剩余 ${Number(stats.semanticUnitsSkippedAfterRanking)} 个低优先级候选未调用模型` : ""}`
      : "";
  const voiceCandidateSearch = [
    "voice-target-intent-v1", "current-voice-target-intent-v1", "current-voice-target-intent-v2",
  ].includes(String(search.intent?.schemaVersion || ""));
  const statsText = voiceCandidateSearch
    ? `已检查全片说话人时间轴 · ${formatSearchElapsed(stats.totalMilliseconds)}`
    : stats.cacheHit
    ? `已复用上次检索结果 · ${formatSearchElapsed(stats.totalMilliseconds)}`
    : `已引用 ${evidenceHitCount} 条索引证据${semanticBatchText}${evidenceReuseText}${predicateReuseText}${adaptiveText} · ${formatSearchElapsed(stats.totalMilliseconds)}`;
  const calls = stats.callBreakdown || {};
  const callRows = [
    ["意图理解", calls.intent], ["文本语义复核", calls.textRerank], ["候选画面复核", calls.visionVerify],
  ].filter(([, value]) => value && Number(value.used || 0) > 0);
  const reuseTrace = Number(stats.modelCallsSaved || 0) > 0
    ? `<span><b>跨检索复用</b><small>预计减少 ${Number(stats.modelCallsSaved || 0)} 次模型调用 · 新分析 ${formatTime(Number(stats.newlyAnalyzedDuration || 0))}</small></span>`
    : "";
  const adaptiveTrace = adaptiveRounds > 0
    ? `<span><b>${stats.adaptiveExpansionTriggered ? "自动补检已执行" : "首轮检索已满足"}</b><small>目标 ${Number(stats.adaptiveTargetCount || 0)} 段 · 已复核 ${Number(stats.semanticVerifiedUnitCount || 0)}/${Number(stats.semanticPoolUnitCount || stats.semanticCandidateCount || 0)} 个语义候选${adaptiveStopLabels[adaptiveStopReason] ? ` · ${escapeHtml(adaptiveStopLabels[adaptiveStopReason])}` : ""}</small></span>`
    : "";
  const modelTraceMarkup = `<details class="content-model-trace"><summary>检索诊断</summary><div>${callRows.length ? callRows.map(([label, value]) => `<span><b>${escapeHtml(label)}</b><small>调用 ${Number(value.used || 0)} 次 · ${escapeHtml(value.reason || "按需调用")}</small></span>`).join("") : "<span><b>未调用远程模型</b><small>复用状态或本地索引完成</small></span>"}${adaptiveTrace}${reuseTrace}</div></details>`;
  const scope = search.intent?.searchScope || search.scope || stats.searchScope || {};
  const scopeOriginLabel = {
    fresh_default: "新话题默认全片",
    explicit_ui: "用户指定范围",
    explicit_text: "按本次文字中的时间",
    inherited: "沿用引用的历史检索范围",
    continuation: "继续当前检索范围",
  }[String(scope.origin || "")];
  const scopeMarkup = Number(scope.end) > Number(scope.start)
    ? `<p class="content-search-scope">检索范围 ${formatTime(scope.start)} → ${formatTime(scope.end)}，范围长度 ${formatTime(Number(scope.end) - Number(scope.start))}${scopeOriginLabel ? ` · ${scopeOriginLabel}` : ""}${scope.source === "intersection" ? "，已取时间条件交集" : ""}</p>`
    : "";
  const historyMarkup = "";
  const execution = search.executionPlan || search.intent?.executionPlan || {};
  const queryPredicates = search.queryPlan?.predicates || search.intent?.queryPlan?.predicates || search.intent?.predicates || [];
  const questionPredicate = queryPredicates.find((predicate) => String(predicate?.kind || "") === "question.evidence");
  const dialoguePredicate = questionPredicate
    ? null
    : queryPredicates.find((predicate) => String(predicate?.kind || "") === "speech.dialogue_role");
  const questionOnlyQuery = Boolean(questionPredicate);
  const capabilityLabels = questionOnlyQuery
    ? { speech: "口头问题", visual: "画面", ocr: "画面问题", audio: "声音", person: "人物轨迹" }
    : { speech: "对白", visual: "画面", ocr: "屏幕文字", audio: "声音", person: "人物轨迹" };
  const personCapabilityRequested = queryPredicates.some((predicate) =>
    ["person.speaking", "person.appearance"].includes(String(predicate?.kind || ""))
    || String(predicate?.subjectPersonRef || predicate?.subjectPersonId || predicate?.personRef || "").trim(),
  ) || ["person_target", "active_speaker_link"].includes(String(search.clarification?.kind || ""));
  const allowedCapabilities = Array.isArray(execution.allowedCapabilities) ? execution.allowedCapabilities : [];
  const executionWarnings = Array.isArray(execution.warnings) ? execution.warnings.filter(Boolean) : [];
  const warningMarkup = executionWarnings.length ? `<div class="content-coverage-warning"><strong>覆盖提醒</strong><p>${executionWarnings.map(escapeHtml).join(" ")}</p></div>` : "";
  const personOperation = execution.operations?.["person.track_face"] || {};
  const personSampling = job.contentIndex?.personSampling || {};
  const personCoverageMarkup = allowedCapabilities.includes("person") && personCapabilityRequested && (
    Number(personSampling.requestedFrameCount || 0) > 0 || Number(personOperation.sampleCount || 0) > 0
  ) ? `<small>人物扫描 ${Number(personSampling.extractedFrameCount || personOperation.sampleCount || 0)}/${Number(personSampling.requestedFrameCount || personOperation.sampleCount || 0)} 帧 · 最大间隔 ${formatTime(Number(personOperation.maximumSampleGapUs || 0) / 1000000)} · ${personOperation.coverageComplete ? "已覆盖全范围" : "仍有不确定区间"}</small>` : "";
  const executionHint = !allowedCapabilities.length
    ? "系统正在把描述编译为可执行的查找条件"
    : personCapabilityRequested && allowedCapabilities.includes("person")
      ? "已根据检索条件选择必要证据；本次人物检索使用连续轨迹扫描"
      : questionOnlyQuery
        ? "只检查本次问题检索需要的证据来源"
        : "只运行上方列出的查找依据";
  const executionMarkup = voiceCandidateSearch
    ? `<div class="recognition-coverage"><span>本次片段依据</span><b>说话人分组</b><b>对白时间轴</b><small>按已识别的匿名声音整理发言范围，不把“索引证据数”当作声音识别质量。</small></div>${warningMarkup}`
    : `<div class="recognition-coverage"><span>系统自动选择的查找依据</span>${allowedCapabilities.length ? allowedCapabilities.map((value) => `<b>${escapeHtml(capabilityLabels[value] || value)}</b>`).join("") : "<b>正在理解</b>"}<small>${executionHint}</small>${personCoverageMarkup}</div>${warningMarkup}`;
  const diagnosticsMarkup = `<details class="content-review-diagnostics"><summary>查看检索详情</summary><div><p>${escapeHtml(statsText)}</p>${executionMarkup}${modelTraceMarkup}${completenessMarkup}</div></details>`;
  const questionInterpretation = questionPredicate
    ? `<p class="content-search-scope">问题检索：${String(questionPredicate.source || questionPredicate.questionSource || "all") === "spoken" ? "只查口头提问" : String(questionPredicate.source || questionPredicate.questionSource || "all") === "screen" ? "只查画面问题" : "口头提问 + 画面问题"}；只输出问题片段，不包含回答</p>`
    : "";
  const dialogueMode = String(search.dialogueMode || dialoguePredicate?.dialogueMode || (dialoguePredicate?.includePrompt ? "qa_pair" : dialoguePredicate?.role === "questioner" ? "question_only" : "answer_only"));
  const dialogueInterpretation = dialoguePredicate
    ? `<p class="content-search-scope">系统理解：${dialoguePredicate.role === "answerer" ? "回答者的完整回答" : "指定对话角色的发言"}${dialoguePredicate.includePrompt ? "，包含前置提问" : "，不包含前置提问"}${String(dialoguePredicate.interruptionPolicy || "") === "bridge_backchannel" ? "；短附和保持回答连续" : ""}</p>`
    : "";
  const dialogueModeMarkup = dialoguePredicate && isCurrentSearch && contentDialogueModeNeedsChoice(search, dialoguePredicate) ? `<label class="content-dialogue-mode"><span>问答结果</span><select data-content-dialogue-mode><option value="qa_pair" ${dialogueMode === "qa_pair" ? "selected" : ""}>完整问答</option><option value="question_only" ${dialogueMode === "question_only" ? "selected" : ""}>仅问题</option><option value="answer_only" ${dialogueMode === "answer_only" ? "selected" : ""}>仅回答</option><option value="qa_split" ${dialogueMode === "qa_split" ? "selected" : ""}>问题/回答拆分</option></select><small>当前描述未限定问答范围。切换只重新整理已有对话证据</small></label>` : "";
  const searchClarification = search.clarification && typeof search.clarification === "object" ? search.clarification : null;
  const plannedTarget = search.queryPlan?.personTarget || search.intent?.queryPlan?.personTarget || search.intent?.personTarget;
  const persistedTarget = job.request?.contentSearchPersonTarget || job.contentSearchPersonTarget;
  const legacyTargetPersonId = String(
    (searchClarification?.options || []).find((option) => option?.personId)?.personId
    || search.queryPlan?.predicates?.find((predicate) => predicate?.personId)?.personId
    || search.intent?.queryPlan?.predicates?.find((predicate) => predicate?.personId)?.personId
    || job.contentSearchTargetPersonId || job.request?.contentSearchTargetPersonId || "",
  );
  const targetState = plannedTarget || persistedTarget || {
    personIds: legacyTargetPersonId ? [legacyTargetPersonId] : [], matchMode: legacyTargetPersonId ? "any" : "",
  };
  const clarificationPersonId = String(
    (searchClarification?.options || []).find((option) => option?.personId)?.personId || "",
  );
  const targetPersonId = String(
    (searchClarification?.kind === "active_speaker_link" ? clarificationPersonId : "")
    || (targetState.personIds || [])[0] || legacyTargetPersonId || "",
  );
  const personPredicates = [
    ...(search.queryPlan?.predicates || []),
    ...(search.intent?.queryPlan?.predicates || []),
    ...(search.intent?.predicates || []),
  ];
  const personRequested = personPredicates.some((predicate) => ["person.speaking", "person.appearance"].includes(String(predicate?.kind || ""))
      || String(predicate?.subjectPersonRef || predicate?.subjectPersonId || predicate?.personRef || "").trim())
    || (Array.isArray(search.intent?.personRefs) && search.intent.personRefs.length > 0)
    || Boolean((searchClarification?.kind || "") === "person_target")
    || Boolean((searchClarification?.kind || "") === "active_speaker_link");
  const personMarkup = contentPersonLabelingMarkup(job, targetState, personRequested);
  if (!candidates.length) {
    const clarification = searchClarification;
    const clarificationValidationErrors = Array.isArray(clarification?.validationErrors)
      ? clarification.validationErrors.filter((item) => item && typeof item === "object")
      : [];
    const blockedByInvalidPlan = search.status === "needs_clarification" && clarificationValidationErrors.length > 0;
    const describedSpeakerBindingError = clarificationValidationErrors.some((item) =>
      String(item.code || "") === "person_speaking_requires_person_ref",
    );
    const scanState = String(search.scanProgress?.state || "");
    const activelyScanning = ["queued", "indexing", "scanning", "running"].includes(String(search.status || ""))
      || ["scanning", "resolving_person", "matching_person_description", "active_speaker"].includes(scanState);
    const activeScanPercent = Math.max(0, Math.min(100, Number(search.scanProgress?.coveredPercent || 0)));
    const indexedPersons = Array.isArray(job?.contentIndex?.persons) ? job.contentIndex.persons : [];
    const planPredicates = search.queryPlan?.predicates || search.intent?.queryPlan?.predicates || search.intent?.predicates || [];
    const needsPersonConfirmation = personRequested && indexedPersons.length > 0
      && !indexedPersons.some((person) => person?.userLabeled)
      && planPredicates.some((predicate) => String(predicate?.kind || "").startsWith("person.") && !predicate?.personId);
    const targetPerson = indexedPersons.find((person) => String(person?.id || "") === targetPersonId);
    const speakerLinkGuidance = clarification?.kind === "active_speaker_link" && targetPerson
      ? `本次目标是“${targetPerson.label || targetPerson.defaultLabel || targetPerson.id}”。${targetPerson.userLabeled ? "项目内标签已经生效" : "当前使用匿名人物编号，无需先修改名称"}。系统已先尝试音画口型匹配，但证据仍不足；下面的人工关联仅是兜底步骤。请预览音画后确认对应的音频 Speaker。`
      : "";
    const blockedGuidance = describedSpeakerBindingError
      ? "系统暂时无法把人物描述与说话证据关联。请重新发送该要求，系统会重新解析并继续检索。"
      : "当前描述还不能形成可靠的检索条件。请调整要求后重新发送。";
    const guidance = activelyScanning
      ? "可靠结果会在证据验证完成后出现。扫描期间可以离开当前页面，任务进度会保留。"
      : blockedByInvalidPlan ? blockedGuidance
      : speakerLinkGuidance || clarification?.message || search.clarification || (needsPersonConfirmation
      ? "人物、对白和画面索引已经完成，但系统还不能可靠判断哪些人物卡是目标。请选择一个或多个人物、确认匹配方式后开始检索；添加项目内标签是可选操作。"
      : scope.isNarrow ? "当前范围内没有可靠匹配。可以修改描述，或扩大范围后重新查找。" : "当前已授权的证据中没有可靠匹配。可以换一个更具体的描述重新查找。");
    const blockedStateMarkup = blockedByInvalidPlan
      ? '<div class="content-search-blocked-state" role="status"><strong>检索尚未开始</strong><span>正在等待条件修正，重新提交后会继续显示识别与扫描进度。</span></div>'
      : "";
    const clarificationOptions = Array.isArray(clarification?.options) ? clarification.options : [];
    const speakerCount = new Set(clarificationOptions.map((option) => String(option?.speakerRef || "")).filter(Boolean)).size;
    const targetPersonLabel = targetPerson?.label || targetPerson?.defaultLabel || targetPerson?.id || "目标人物";
    const speakerExplanation = clarification?.kind === "active_speaker_link"
      ? `<div class="content-speaker-explanation"><div><b>画面人物簇 ${indexedPersons.length} 个</b><span>逐字稿 Speaker ${speakerCount} 组</span></div><p>这两种分组不等于真实人数。画面簇包含未说话和背景人物；Speaker 只是逐字稿中的发言分组，也可能把同一个人拆开。请以预览的音画内容为准。</p></div>`
      : "";
    const clarificationButtons = clarification?.kind === "active_speaker_link"
      ? `${speakerExplanation}${clarificationOptions.map((option) => `<article class="content-speaker-option"><div><strong>音频 ${escapeHtml(option.speakerRef || "Speaker")}</strong><small>${formatTime(option.start)} → ${formatTime(option.end)}</small><p>${escapeHtml(option.transcript || "该时间段没有可展示的对白")}</p></div><div><button type="button" data-content-speaker-preview="${escapeHtml(option.id || "")}" data-content-preview-start="${escapeHtml(option.start ?? 0)}" data-content-preview-end="${escapeHtml(option.end ?? option.start ?? 0)}" data-content-preview-speaker="${escapeHtml(option.speakerRef || "Speaker")}">预览音画</button><button type="button" class="primary" data-content-speaker-confirm="${escapeHtml(option.speakerRef || "")}" data-content-person-id="${escapeHtml(option.personId || "")}">确认 ${escapeHtml(targetPersonLabel)} 对应 ${escapeHtml(option.speakerRef || "Speaker")}</button></div></article>`).join("")}`
      : clarificationOptions.map((option) => `<button type="button" class="content-evidence-option${option.recommended ? " primary" : ""}${option.disabled ? " unavailable" : ""}" ${option.disabled ? "disabled aria-disabled=\"true\"" : `data-content-evidence-choice="${escapeHtml(option.id || "")}" data-content-evidence-mode="${escapeHtml(option.evidenceMode || option.id || "")}" data-content-capabilities="${escapeHtml((option.capabilities || []).join(","))}" data-content-instruction="${escapeHtml(option.instruction || "")}"`} title="${escapeHtml(option.disabledReason || "")}"><span>${escapeHtml(option.label || "继续")}</span>${option.disabledReason ? `<small>${escapeHtml(option.disabledReason)}</small>` : ""}</button>`).join("");
    const capabilityAlternativeHint = clarification?.kind === "evidence_type" && clarification?.alternativeHint
      ? `<p class="content-capability-alternative">${escapeHtml(clarification.alternativeHint)}</p>`
      : "";
    const expansionButtons = (search.expansionOptions || []).map((option) => `<button type="button" data-content-expansion="${escapeHtml(option.id || "")}">${escapeHtml(option.label || "继续检索")}</button>`).join("");
    const denseButton = allowedCapabilities.includes("visual")
      && search.status !== "needs_clarification"
      && (!denseSearchCompleted || (exhaustive && !coverageComplete))
      ? `<button type="button" data-content-feedback="missed_content" title="会检查更多画面，处理时间可能增加">${denseSearchCompleted ? "继续检查未覆盖画面" : "继续检查剩余画面"}</button>`
      : "";
    const scanTitle = search.scanProgress?.phaseLabel
      ? escapeHtml(String(search.scanProgress.phaseLabel))
      : Number.isFinite(Number(search.scanProgress?.coveredPercent))
      ? `正在扫描本次范围 · ${activeScanPercent.toFixed(1)}%`
      : "正在准备并扫描本次检索范围";
    return `<article class="chat-message assistant content-search-message" data-conversation-key="search:${escapeHtml(search.id || "")}"><span class="avatar">AI</span><div class="recommendation-wrap"><section class="content-search-review empty${historicalSearchClass}" data-content-search-id="${escapeHtml(search.id || "")}"><header><div><small>${isCurrentSearch ? "当前检索" : "已保留结果"}</small><strong>${activelyScanning ? scanTitle : blockedByInvalidPlan ? "检索条件需要调整" : search.status === "needs_clarification" ? escapeHtml(clarification?.question || "需要确认查找依据") : needsPersonConfirmation ? "请确认目标人物" : "没有可靠匹配"}</strong><p>${escapeHtml(search.intent?.query || search.instruction || "")}</p>${scopeMarkup}${dialogueInterpretation}${historicalSearchToggle}</div></header>${blockedStateMarkup}${retainedSearchesMarkup}${diagnosticsMarkup}${isCurrentSearch ? personMarkup : ""}<p>${escapeHtml(guidance)}</p>${isCurrentSearch && !activelyScanning ? `<div class="content-search-secondary">${search.status === "needs_clarification" ? clarificationButtons : `${expansionButtons}${denseButton}`}</div>${capabilityAlternativeHint}` : !isCurrentSearch ? `<footer class="content-history-actions">${restoreButtonMarkup}</footer>` : ""}${historyMarkup}</section></div></article>`;
  }
  const recoveryAvailable = isCurrentSearch && allowedCapabilities.includes("visual")
    && (exhaustive ? !coverageComplete : !denseSearchCompleted);
  const recoveryOpen = exhaustive && !coverageComplete || reliableCount === 0;
  const recoveryMarkup = recoveryAvailable
    ? `<details class="content-search-recovery"${recoveryOpen ? " open" : ""}><summary>${exhaustive ? (denseSearchCompleted ? "仍有未覆盖画面" : "扫描尚未完整") : "结果可能有遗漏？"}</summary><div><p>${exhaustive ? "当前结果尚未覆盖完整检索范围。可以继续检查尚不确定的画面区域，处理时间会相应增加。" : "如果仍有遗漏，可以继续检查剩余画面，处理时间会相应增加。"}</p><button type="button" data-content-feedback="missed_content">${denseSearchCompleted ? "继续检查未覆盖画面" : "继续检查剩余画面"}</button></div></details>`
    : "";
  const possibleToggleMarkup = pendingPossibleCount
    ? `<button type="button" class="content-possible-toggle" data-content-show-possible aria-expanded="false"><span>查看待试听复核</span><b>${pendingPossibleCount} 个内容段</b><small>证据较弱，尚未人工保留</small></button>`
    : "";
  const keptPossibleMarkup = keptPossibleCount
    ? `<div class="content-kept-possible"><strong>${keptPossibleCount} 个证据较弱片段已试听保留</strong><span>它们会按你的选择参与生成，不再作为“默认未选中”的候选折叠。</span></div>`
    : "";
  const descriptorResolution = search.retrievalStats?.personDescriptionResolution;
  const descriptorEvidence = Array.isArray(descriptorResolution?.evidence) ? descriptorResolution.evidence : [];
  const descriptorReliableCount = descriptorEvidence.filter((item) => String(item?.status || "") === "reliable").length;
  const descriptorPossibleCount = descriptorEvidence.filter((item) => String(item?.status || "") === "possible").length;
  const descriptorSummaryMarkup = descriptorEvidence.length
    ? `<div class="content-person-evidence-summary"><strong>人物描述采用多帧匹配</strong><span>${descriptorReliableCount} 个人物证据充分${descriptorPossibleCount ? `，${descriptorPossibleCount} 个人物进入待复核` : ""}；片段仍需通过主动说话人证据。</span></div>`
    : "";
  const personClipFlowMarkup = personWorkflow && isCurrentSearch
    ? `<ol class="person-edit-mini-flow" aria-label="人物聚焦步骤"><li class="complete"><small>1</small><span>选择人物</span></li><li class="current" aria-current="step"><small>2</small><span>核对出镜</span></li><li><small>3</small><span>合成视频</span></li></ol>`
    : "";
  const personReviewHandoffMarkup = personWorkflow && isCurrentSearch
    ? `<section class="person-review-handoff" aria-label="人物出镜片段整理结果"><b>${candidates.length}</b><div><small>已进入出镜核对</small><strong>${candidates.length} 个出镜片段就在下方</strong><span>逐段预览并取消不需要的片段；确认后再合成视频。</span></div><button type="button" data-return-person-selection>更换人物</button></section>`
    : "";
  return `<article class="chat-message assistant content-search-message" data-conversation-key="search:${escapeHtml(search.id || "")}"><span class="avatar">AI</span><div class="recommendation-wrap">
    <section class="content-search-review${personWorkflow ? " person-review" : ""}${historicalSearchClass}" data-content-search-id="${escapeHtml(search.id || "")}">
      <header><div><small>${isCurrentSearch ? (personWorkflow ? "人物聚焦 · 第 2 步" : "当前检索 · 确认片段") : "已保留结果 · 选择片段"}</small><strong>${personWorkflow ? "人物出镜片段已整理" : exhaustive ? exhaustiveTitle : "相关片段检索完成"} · ${reliableCount} 个匹配片段${keptPossibleCount ? `，${keptPossibleCount} 个已试听保留` : ""}${pendingPossibleCount ? `，${pendingPossibleCount} 个待复核` : ""}</strong><p>${escapeHtml(search.intent?.query || search.instruction || "")}</p><p class="content-search-stats">本次找到 ${candidates.length} 个片段${exhaustive && !strictComplete ? " · 尚未检查完整段素材" : ""}</p>${questionInterpretation}${dialogueInterpretation}${dialogueModeMarkup}${scopeMarkup}${historicalSearchToggle}</div><b>${personWorkflow ? "核对出镜" : "核对片段"}</b></header>
      ${personClipFlowMarkup}
      ${personReviewHandoffMarkup}
      ${retainedSearchesMarkup}
      ${isCurrentSearch && !personWorkflow ? personMarkup : ""}
      ${descriptorSummaryMarkup}
      ${diagnosticsMarkup}
      ${questionSourceControls}
      ${keptPossibleMarkup}
      ${possibleToggleMarkup}
      <div class="content-match-list">${reviewCandidates.map((match, index) => {
        const reviewStatus = String(match.reviewStatus || (match.requiresReview ? "pending" : "confirmed"));
        const pending = reviewStatus === "pending";
        const rejected = reviewStatus === "rejected";
        const scopeDuration = Math.max(0, Number(scope.end) - Number(scope.start));
        const unusuallyWide = Number(match.duration || Number(match.end) - Number(match.start)) > Math.max(30, scopeDuration * .25);
        const checked = !pending && !rejected && !unusuallyWide
          && (String(match.reviewStatus || "") === "kept" || (strictComplete && defaults.has(String(match.id))) || (!exhaustive && !defaults.size && index < 3));
        const evidence = match.transcriptExcerpt || match.audioEvidence?.transcriptExcerpt || match.text || match.matchedEvidence || match.reason || "画面索引匹配";
        const type = contentMatchTypeLabel(match);
        const methodLabel = contentMatchMethodLabel(match);
        const boundaryRange = match.boundaryVerification?.verifiedRange;
        const boundaryState = boundaryRange && (Number(boundaryRange[0]) !== Number(match.start) || Number(boundaryRange[1]) !== Number(match.end))
          ? "pending" : String(match.boundaryVerification?.status || "");
        const boundaryFailure = match.boundaryVerification?.reason;
        const boundaryLabel = boundaryState === "pending" && boundaryFailure === "incomplete_expression"
          ? "仅部分语句有证据，完整表达需审核"
          : boundaryState === "pending" && match.boundaryVerification?.diagnostics?.rejectedIntervals?.unsupported_predicates
            ? "内容条件证据不足，需审核"
          : boundaryState === "pending" && match.boundaryVerification?.diagnostics?.rejectedIntervals?.invalid_boundary
            ? "返回边界与源时间码不一致，需审核"
          : match.expressionCompleteness === "pending" && boundaryState !== "human_confirmed"
          ? "完整表达待核验，需审核后合成"
          : match.expressionCompleteness === "verified" && boundaryState === "verified" ? "完整回答范围已核验"
          : boundaryState === "verified" ? "已抽样核验（非逐帧）"
          : boundaryState === "human_confirmed" ? "人工确认范围"
          : boundaryState === "pending" ? "待核验，不能自动合成"
          : contentBoundarySourceLabel(match.boundarySource);
        const voiceEvidence = match.voiceMatchEvidence;
        const voiceScoreMarkup = voiceEvidence?.score != null
          ? `<span>·</span>声纹分 ${Number(voiceEvidence.score).toFixed(3)}${voiceEvidence.margin == null ? "" : ` · 领先 ${Number(voiceEvidence.margin).toFixed(3)}`}`
          : voiceEvidence?.clusterMinimumSimilarity != null
            ? `<span>·</span>声音簇一致度 ${Number(voiceEvidence.clusterMinimumSimilarity).toFixed(3)}`
            : "";
        const evidenceKinds = [...new Set((match.matchedModalities || [match.evidenceType]).filter(Boolean))];
        const evidenceCount = Array.isArray(match.evidenceRefs) ? match.evidenceRefs.length : 0;
        const matchedPeople = [...new Set((match.matchedPersonLabels || match.matchedPersonIds || []).filter(Boolean))];
        const questionSource = contentQuestionSourceKey(match);
        const questionSourceChip = questionSource ? `<i class="question-source">${escapeHtml(contentQuestionSourceLabel(questionSource))}</i>` : "";
        const appearanceEvidence = match.personDescriptionEvidence
          || Object.values(match.personDescriptionEvidenceByPerson || {})[0];
        const activeEvidenceRows = [
          ...(match.activeSpeakerEvidence ? [match.activeSpeakerEvidence] : []),
          ...Object.values(match.activeSpeakerEvidenceByPerson || {}),
        ].filter((item) => item && typeof item === "object");
        const activeEvidence = activeEvidenceRows.sort((left, right) => Number(right.asdScore ?? right.vlmScore ?? 0) - Number(left.asdScore ?? left.vlmScore ?? 0))[0];
        const appearanceChip = appearanceEvidence?.confidence != null
          ? `<i class="appearance-match ${String(appearanceEvidence.status || "") === "possible" ? "possible" : ""}" title="${escapeHtml(appearanceEvidence.reason || "人物多张代表画面的外观描述匹配")}">外观描述 ${Math.round(Number(appearanceEvidence.confidence) * 100)}%</i>`
          : "";
        const activeScore = activeEvidence?.asdScore ?? activeEvidence?.vlmScore;
        const activeSpeakerChip = activeEvidence
          ? `<i class="active-speaker-match" title="目标人物人脸轨迹、语音活动和音画同步证据">主动说话${activeScore == null ? "已验证" : ` ${Math.round(Number(activeScore) * 100)}%`}</i>`
          : "";
        const subjectStatusLabels = { verified: "对象已验证", contextual: "对象语境支持", unverified: "对象未验证", ignored: "未使用对象约束" };
        const subjectChip = match.subjectDescription
          ? `<i class="subject-${escapeHtml(String(match.subjectStatus || "unverified"))}" title="${escapeHtml((subjectStatusLabels[String(match.subjectStatus || "unverified")] || "对象证据") + "：" + String(match.subjectDescription))}">${escapeHtml(subjectStatusLabels[String(match.subjectStatus || "unverified")] || "对象证据")}</i>`
          : "";
        const containedLocalCount = Number(match.containedCandidateIds?.length || 0);
        const removedBroadCount = Number(match.containingCandidateIds?.length || 0);
        const containmentChip = containedLocalCount > 0
          ? `<i class="content-containment-match" title="较小的重复命中已作为此完整片段的证据保留，不再重复列为可选片段">已归并 ${containedLocalCount} 个局部命中</i>`
          : removedBroadCount > 0
            ? `<i class="content-containment-match" title="宽泛召回区间已移除，当前保留精确命中的边界">已排除 ${removedBroadCount} 个宽泛区间</i>`
            : "";
        const evidenceChips = `<span class="content-evidence-chips">${questionSourceChip}${subjectChip}${appearanceChip}${activeSpeakerChip}${containmentChip}${matchedPeople.map((value) => `<i class="person">${escapeHtml(value)}</i>`).join("")}${evidenceKinds.map((value) => `<i>${escapeHtml(({speech:"对白",visual:"画面",ocr:"屏幕文字",audio:"声音",person:"匿名人物"})[String(value).toLowerCase()] || value)}</i>`).join("")}<button type="button" class="content-evidence-count" data-content-evidence="${escapeHtml(match.id)}">${evidenceCount ? `查看证据（${evidenceCount}）` : "查看相关证据"}</button></span>`;
        const inputId = `content-match-${String(search.id || "search").replace(/[^a-zA-Z0-9_-]/g, "-")}-${index}`;
        const reviewReasons = (Array.isArray(match.reviewReasons) ? match.reviewReasons : []).filter(Boolean);
        const transcriptIsContext = (match.speechUnits || []).some((unit) => Number(unit.start) < Number(match.start) - .5 || Number(unit.end) > Number(match.end) + .5);
        const evidenceLabel = transcriptIsContext && match.transcriptExcerpt ? "相邻对白上下文" : "匹配证据";
        const boundaryQueued = boundaryRetryIds.has(String(match.id));
        const canAdjustBoundary = isCurrentSearch && !rejected;
        const adjustBoundaryButton = canAdjustBoundary
          ? `<button type="button" data-content-boundary-open="${escapeHtml(match.id)}" title="在当前结果卡内直接调整入点和出点">直接修剪</button>`
          : "";
        const reviewButtons = !isCurrentSearch ? "" : boundaryQueued
          ? `<span class="content-feedback-pending">自动重新识别中</span>`
          : pending
          ? `${adjustBoundaryButton}<button type="button" data-content-feedback="review_keep" data-content-match-id="${escapeHtml(match.id)}">试听后保留</button><button type="button" data-content-feedback="review_reject" data-content-match-id="${escapeHtml(match.id)}">排除此片段</button>`
          : rejected
            ? `<button type="button" data-content-feedback="review_keep" data-content-match-id="${escapeHtml(match.id)}">恢复并保留</button>`
            : `<button type="button" data-content-feedback="not_relevant" data-content-match-id="${escapeHtml(match.id)}">不相关</button>${adjustBoundaryButton}`;
        const boundaryEditor = canAdjustBoundary ? `
          <section class="content-boundary-editor hidden" data-content-boundary-editor="${escapeHtml(match.id)}"
            data-boundary-start="${Number(match.start) || 0}" data-boundary-end="${Number(match.end) || 0}"
            data-boundary-original-start="${Number(match.start) || 0}" data-boundary-original-end="${Number(match.end) || 0}">
            <header>
              <div><strong>调整片段边界</strong><small>只修改当前片段，不会重新检索或改变其他片段。</small></div>
            </header>
            <section class="content-boundary-range" aria-label="边界修改摘要">
              <div><span>当前范围</span><em>${formatTime(match.start)} → ${formatTime(match.end)}</em></div>
              <div class="content-boundary-range-track" aria-hidden="true"><i></i><b></b><i></i></div>
              <div><span>修改后</span><strong data-boundary-summary>${formatTime(match.start)} → ${formatTime(match.end)}</strong></div>
            </section>
            <div class="content-boundary-edges">
              <fieldset data-boundary-edge="start">
                <legend><b>入点</b><span data-boundary-start-label>${formatTime(match.start)}</span></legend>
                <label class="content-boundary-time-input"><span>时间（秒）</span><input type="number" min="0" value="${Number(match.start) || 0}" data-boundary-value="start" inputmode="decimal" aria-label="入点时间，单位秒"></label>
                <div class="content-boundary-nudges" aria-label="微调入点">
                  <button type="button" data-boundary-adjust="start:-1" aria-label="入点前移一秒">−1秒</button>
                  <button type="button" data-boundary-adjust="start:-frame" aria-label="入点前移一帧">−1帧</button>
                  <button type="button" class="content-boundary-playhead" data-boundary-playhead="start" aria-label="将当前播放位置设为入点">当前帧</button>
                  <button type="button" data-boundary-adjust="start:frame" aria-label="入点后移一帧">+1帧</button>
                  <button type="button" data-boundary-adjust="start:1" aria-label="入点后移一秒">+1秒</button>
                </div>
              </fieldset>
              <fieldset data-boundary-edge="end">
                <legend><b>出点</b><span data-boundary-end-label>${formatTime(match.end)}</span></legend>
                <label class="content-boundary-time-input"><span>时间（秒）</span><input type="number" min="0" value="${Number(match.end) || 0}" data-boundary-value="end" inputmode="decimal" aria-label="出点时间，单位秒"></label>
                <div class="content-boundary-nudges" aria-label="微调出点">
                  <button type="button" data-boundary-adjust="end:-1" aria-label="出点前移一秒">−1秒</button>
                  <button type="button" data-boundary-adjust="end:-frame" aria-label="出点前移一帧">−1帧</button>
                  <button type="button" class="content-boundary-playhead" data-boundary-playhead="end" aria-label="将当前播放位置设为出点">当前帧</button>
                  <button type="button" data-boundary-adjust="end:frame" aria-label="出点后移一帧">+1帧</button>
                  <button type="button" data-boundary-adjust="end:1" aria-label="出点后移一秒">+1秒</button>
                </div>
              </fieldset>
            </div>
            <div class="content-boundary-status"><span data-boundary-frame-rate>正在读取源视频帧率…</span><strong data-boundary-change>尚未修改</strong></div>
            <footer>
              <div class="content-boundary-secondary-actions"><button type="button" data-boundary-auto>重新识别边界</button>${match.manualBoundary ? `<button type="button" data-boundary-reset>恢复自动边界</button>` : ""}</div>
              <div class="content-boundary-primary-actions"><button type="button" data-boundary-cancel>取消</button><button type="button" data-boundary-preview>试看片段</button><button type="button" class="primary" data-boundary-save disabled>保存修改</button></div>
            </footer>
          </section>` : "";
        const confidenceTier = String(match.confidenceTier || (pending ? "possible" : "reliable"));
        const highConfidence = confidenceTier === "reliable" && !rejected;
        const hidePossible = confidenceTier === "possible" && reviewStatus !== "kept";
        const eventContext = eventByMatchId.get(String(match.id));
        const event = eventContext?.event;
        const eventPrefix = eventContext?.eventIndex === 0
          ? `<details class="content-candidate-event" open data-candidate-event="${escapeHtml(event.id || "")}"><summary><span><small>${personWorkflow ? "出镜事件" : "发言事件"} ${Number(event.number || 0)}</small><strong>${escapeHtml(event.title || "连续事件")}</strong><em>${formatTime(event.start)} → ${formatTime(event.end)}</em></span><b>${Number(event.segmentCount || 0)} 个子片段 · 有效 ${Number(event.clipDuration || 0).toFixed(1)} 秒</b></summary><div class="content-candidate-event-segments">`
          : "";
        const eventSuffix = eventContext?.eventIndex === eventContext?.eventSize - 1 ? "</div></details>" : "";
        return `${eventPrefix}<article class="content-match-row${pending ? " review-pending" : ""}${rejected ? " review-rejected" : ""}${confidenceTier === "possible" ? " content-candidate-possible" : ""}${hidePossible ? " hidden" : ""}${index >= 50 ? " content-candidate-overflow hidden" : ""}" data-content-match-row="${escapeHtml(match.id)}" data-content-question-source="${escapeHtml(questionSource || "other")}" data-content-reliable="${highConfidence ? "true" : "false"}"><div class="content-match-main"><input id="${inputId}" type="checkbox" data-content-match data-content-review-status="${escapeHtml(reviewStatus)}" value="${escapeHtml(match.id)}" ${checked ? "checked" : ""} ${rejected ? "disabled" : ""} aria-label="选择${escapeHtml(match.title || `匹配片段 ${index + 1}`)}"><div class="content-match-copy${canAdjustBoundary ? " boundary-clickable" : ""}"${canAdjustBoundary ? ` data-content-card-preview="${escapeHtml(match.id)}" role="button" tabindex="0" title="播放片段"` : ""}><div class="content-match-title"><label for="${inputId}"><strong>${escapeHtml(match.title || `匹配片段 ${index + 1}`)}</strong>${rejected ? `<i>已排除</i>` : reviewStatus === "kept" && confidenceTier === "possible" ? `<i>已试听保留</i>` : ""}</label><b class="confidence-${escapeHtml(confidenceTier)}" title="按可定位证据强度分层，不是统计概率">${match.subjectDescription && ["unverified", "contextual"].includes(String(match.subjectStatus || "unverified")) ? "需确认对象" : confidenceTier === "reliable" ? "匹配片段" : "可能相关"}</b></div><p class="content-match-meta">${formatTime(match.start)} → ${formatTime(match.end)}<span>·</span>${Number(match.duration || 0).toFixed(1)} 秒<span>·</span>${type}${match.speaker ? `<span>·</span>${escapeHtml(match.speaker)}` : ""}</p><details class="content-match-details"><summary>查看匹配依据</summary>${evidenceChips}<p class="content-match-diagnostics">${escapeHtml(methodLabel)}<span>·</span>边界：${escapeHtml(boundaryLabel)}${voiceScoreMarkup}</p>${reviewReasons.length ? `<p class="content-match-review-reasons">判断依据：${reviewReasons.map(escapeHtml).join("；")}</p>` : ""}<p class="content-match-evidence"><small>${evidenceLabel}</small>${escapeHtml(evidence)}</p></details></div></div><div class="content-match-buttons" aria-label="片段操作"><button type="button" class="content-match-preview" data-content-preview="${escapeHtml(match.id)}">播放</button>${reviewButtons}</div>${boundaryEditor}</article>${eventSuffix}`;
      }).join("")}</div>${candidates.length > 50 ? `<button type="button" class="content-show-more" data-content-show-more>再显示 50 段</button>` : ""}
      ${bulkControlsMarkup}
      ${historyMarkup}
      ${isCurrentSearch ? `<footer class="content-search-actions"><div class="content-selection-summary" data-content-selection-summary></div><details class="content-generation-settings"><summary>生成设置</summary><div class="content-output-controls"><label data-content-output-wrap><span>输出方式</span><select data-content-output-mode><option value="single_reel" ${draftOutputMode === "single_reel" ? "selected" : ""}>合成一条视频</option><option value="separate_events" ${draftOutputMode === "separate_events" ? "selected" : ""}>每段分别生成</option></select></label><label data-content-order-wrap><span>合成顺序</span><select data-content-order-mode><option value="source" ${draftOrderMode === "source" ? "selected" : ""}>按视频时间</option><option value="selection" ${draftOrderMode === "selection" ? "selected" : ""}>自定义排列</option><option value="llm_recommend" ${draftOrderMode === "llm_recommend" ? "selected" : ""}>智能排序</option></select></label><small data-content-order-hint>保持视频中的时间先后，适合过程记录和访谈。</small></div><label class="content-subtitle-toggle"><input type="checkbox" data-content-subtitle ${reviewDraft.subtitleEnabled ? "checked" : ""}><span><b>添加 AI 字幕</b><small data-content-subtitle-status>正在检查所选片段的对白…</small></span></label></details><div class="content-search-submit-actions">${contentBasketActionRelevant(job) ? `<button type="button" data-content-basket-add>加入成片清单</button>` : ""}<button type="button" class="primary" data-confirm-content>${personWorkflow ? "核对完成，合成视频" : "用所选片段剪辑"}</button></div></footer>${recoveryMarkup}` : `<footer class="content-history-actions"><span>勾选只影响这次检索，不会自动加入成片清单</span>${contentBasketActionRelevant(job) ? `<button type="button" data-content-basket-add>加入成片清单</button>` : ""}${restoreButtonMarkup}</footer>`}
      ${personWorkflow ? `<p class="content-search-safety">人物以画面描述区分，不进行实名识别。</p>` : ""}
    </section></div></article>`;
}

function contentOutputResultMarkup(job, selectedVersions = null, { historical = false, compact = false } = {}) {
  if (taskModePresentation(job).key !== "content_extract") return "";
  const versions = (selectedVersions || jobOutputVersions(job)).filter((version) => (version.outputs || []).length);
  if (!versions.length) return "";
  const buttons = versions.flatMap((version) => (version.outputs || []).map((output) => {
    const versionLabel = `V${Number(version.number || 1)}`;
    const label = `${outputVersionIdentity(version, output)} · 内容视频`;
    const segmentCount = Number(output.segmentCount || output.segments?.length || 0);
    const sourceQuery = compact ? "" : String(version.contentSearchInstruction || version.sourceLabel || "").trim();
    const reviewLabel = [version.qualityStatus, output.qualityStatus].includes("needs_review") ? "待复核" : "已生成";
    return `<div class="auto-version-entry"><button type="button" class="auto-version-button" data-auto-output="${escapeHtml(output.filename || "")}" data-auto-version="${escapeHtml(version.id || versionLabel)}"><span>${escapeHtml(label)} <em>${reviewLabel}</em></span><small>${sourceQuery ? `${escapeHtml(sourceQuery)} · ` : ""}${segmentCount ? `${segmentCount} 个已选片段 · ` : ""}${Number(output.duration || 0).toFixed(1)} 秒 · 点击播放</small></button></div>`;
  }));
  const singleVersion = versions.length === 1 ? `V${Number(versions[0].number || 1)}` : "";
  return `<section class="auto-compose-result-card content-output-result-card${historical ? " historical" : ""}${compact ? " compact" : ""}"><strong>${compact && singleVersion ? `${singleVersion} 已生成` : `${historical ? "以前生成的内容视频" : "内容视频已生成"} · ${versions.length} 个版本`}</strong><p>${compact ? "对应本次确认的片段。" : historical ? "来自之前确认的片段，不会被本次检索覆盖。" : "仅包含最终确认的时间范围。"} 点击版本可预览，当前版本可从播放器顶部下载。</p><div>${buttons.join("")}</div></section>`;
}

function contentSearchRecordsForJob(job = currentJob) {
  const records = Array.isArray(job?.contentSearchRecords) ? job.contentSearchRecords : [];
  const source = records.length ? records : (job?.contentSearch ? [job.contentSearch] : []);
  return source.map((record) => {
    const cached = contentSearchDetailCache.get(`${job?.id || ""}:${record?.id || ""}`);
    return cached ? { ...record, ...cached, candidateDetailsLoaded: true } : record;
  });
}

function contentRestoreButtonMarkup(job, searchId) {
  const capabilities = interactionCapabilitiesForJob(job);
  const disabled = !capabilities.canRestoreContentSearch;
  const label = disabled ? "任务处理中，暂不能恢复" : "恢复为当前检索并编辑";
  const reason = disabled ? capabilities.reason : "将这次历史结果恢复为当前检索并继续编辑";
  return `<button type="button" data-content-search-restore="${escapeHtml(searchId || "")}" ${disabled ? "disabled aria-disabled=\"true\"" : ""} title="${escapeHtml(reason)}">${label}</button>`;
}

function contentSearchHistorySummaryMarkup(search, { current = false, expandable = true, job = currentJob } = {}) {
  const count = Number(search?.candidateCount ?? search?.candidates?.length ?? 0);
  const created = search?.createdAt ? new Date(search.createdAt) : null;
  const time = created && Number.isFinite(created.getTime())
    ? created.toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" }) : "早期记录";
  const status = String(search?.status || "");
  const statusLabel = status === "needs_clarification" ? "等待确认"
    : status === "confirmed" ? "已生成过"
      : status === "queued" || status === "indexing" ? "未完成"
        : count ? `${count} 段` : "无匹配";
  const recordLabel = current ? "当前检索" : search?.recordType === "assembly" ? "合并生成" : "已保留结果";
  const title = search?.recordType === "assembly" ? `来自 ${Number(search?.basketSnapshot?.sourceQueries?.length || 0) || "多"} 次检索` : (search?.intent?.query || search?.instruction || "未命名检索");
  if (!current && expandable && search?.recordType !== "assembly") {
    const basketLocked = !interactionCapabilitiesForJob(job).canMutateContentBasket;
    return `<article class="chat-message assistant content-search-message content-search-history-message retained" data-conversation-key="search:${escapeHtml(search?.id || "")}"><div class="recommendation-wrap"><section class="content-search-history-summary retained" data-content-search-id="${escapeHtml(search?.id || "")}"><div class="content-retained-search-copy"><small>${escapeHtml(time)} · ${recordLabel}</small><strong>${escapeHtml(title)}</strong><span>${escapeHtml(statusLabel)} · 不会被后续检索覆盖</span></div><div class="content-retained-search-actions"><button type="button" data-content-history-open="${escapeHtml(search?.id || "")}">查看片段</button>${count ? `<button type="button" class="primary" data-content-history-add="${escapeHtml(search?.id || "")}" ${basketLocked ? `disabled aria-disabled="true" title="${escapeHtml(unavailableInteractionMessage(job))}"` : ""}>${basketLocked ? "任务处理中" : "加入成片清单"}</button>` : ""}</div></section></div></article>`;
  }
  return `<article class="chat-message assistant content-search-message content-search-history-message${current ? " current" : ""}" data-conversation-key="search:${escapeHtml(search?.id || "")}"><div class="recommendation-wrap"><section class="content-search-history-summary" data-content-search-id="${escapeHtml(search?.id || "")}"><button type="button" ${expandable ? `data-content-history-open="${escapeHtml(search?.id || "")}"` : "data-content-review-focus"} aria-expanded="false"><span><small>${escapeHtml(time)} · ${recordLabel}</small><strong>${escapeHtml(title)}</strong></span><b>${escapeHtml(statusLabel)}</b><em>${expandable ? "查看片段" : "正在核对"}</em></button></section></div></article>`;
}

function contentRetainedSearchesMarkup(job = currentJob, currentSearchId = "") {
  const retained = contentSearchRecordsForJob(job).filter((record) => (
    String(record?.id || "") !== String(currentSearchId || "")
    && record?.recordType !== "assembly"
    && Number(record?.candidateCount ?? record?.candidates?.length ?? 0) > 0
  ));
  if (!retained.length) return "";
  const total = retained.reduce((sum, record) => sum + Number(record?.candidateCount ?? record?.candidates?.length ?? 0), 0);
  const basketLocked = !interactionCapabilitiesForJob(job).canMutateContentBasket;
  return `<section class="content-retained-searches" aria-label="已保留的检索结果"><header><div><small>已保留的检索</small><strong>之前的结果仍然可用</strong><span>本次检索不会覆盖它们；可以查看，也可以加入成片清单一起生成。</span></div><b>${retained.length} 次 · ${total} 段</b></header><div class="content-retained-search-list">${[...retained].reverse().map((record) => {
    const count = Number(record?.candidateCount ?? record?.candidates?.length ?? 0);
    const query = record?.intent?.query || record?.instruction || "未命名检索";
    return `<article data-retained-search-id="${escapeHtml(record?.id || "")}"><span><strong>${escapeHtml(query)}</strong><small>已保留 ${count} 段</small></span><div><button type="button" data-content-history-open="${escapeHtml(record?.id || "")}">查看片段</button><button type="button" data-content-history-add="${escapeHtml(record?.id || "")}" ${basketLocked ? `disabled aria-disabled="true" title="${escapeHtml(unavailableInteractionMessage(job))}"` : ""}>${basketLocked ? "任务处理中" : "加入成片清单"}</button></div></article>`;
  }).join("")}</div></section>`;
}

function contentBasketItems(job = currentJob) {
  const basket = job?.contentSelectionBasket;
  if (basket?.entryMode !== "explicit") return [];
  return Array.isArray(basket.items) ? basket.items : [];
}

function contentBasketHas(job, searchId, matchId) {
  return contentBasketItems(job).some((item) => String(item.searchId) === String(searchId) && String(item.matchId) === String(matchId));
}

function contentBasketTimingSummary(items = []) {
  const ranges = items.map((item) => ({
    start: Number(item.start) || 0,
    end: Math.max(Number(item.start) || 0, Number(item.end) || 0),
  })).filter((item) => item.end > item.start).sort((left, right) => left.start - right.start || left.end - right.end);
  const merged = [];
  let overlapCount = 0;
  ranges.forEach((range) => {
    const previous = merged[merged.length - 1];
    if (!previous || range.start >= previous.end - .04) {
      merged.push({ ...range });
      return;
    }
    overlapCount += 1;
    previous.end = Math.max(previous.end, range.end);
  });
  const uniqueDuration = merged.reduce((sum, item) => sum + item.end - item.start, 0);
  const rawDuration = ranges.reduce((sum, item) => sum + item.end - item.start, 0);
  return { uniqueDuration, rawDuration, overlapCount, ranges: merged };
}

function setContentBasketLocal(job, searchId, matchId, selected, candidate = null) {
  if (!interactionCapabilitiesForJob(job).canMutateContentBasket) {
    showToast(unavailableInteractionMessage(job, "当前不能修改成片清单"), "error");
    return;
  }
  const items = [...contentBasketItems(job)];
  const index = items.findIndex((item) => String(item.searchId) === String(searchId) && String(item.matchId) === String(matchId));
  if (selected && index < 0) items.push({
    originJobId: String(job?.id || ""), searchId: String(searchId), matchId: String(matchId), title: candidate?.title || "匹配片段",
    start: Number(candidate?.start) || 0, end: Number(candidate?.end) || 0,
    duration: Math.max(0, Number(candidate?.end) - Number(candidate?.start)),
    sourceQuery: contentSearchRecordsForJob(job).find((item) => String(item.id) === String(searchId))?.instruction || "检索",
  });
  if (!selected && index >= 0) items.splice(index, 1);
  job.contentSelectionBasket = {
    ...(job.contentSelectionBasket || {}),
    schemaVersion: "content-selection-basket-v2", entryMode: "explicit", initialized: true, items,
  };
  renderContentSelectionBasket(job);
  syncVisibleContentBasketAddButtons(job);
  scheduleContentBasketSave(job);
}

function syncContentBasketAddButton(root, job = currentJob, searchId = "") {
  const button = root?.querySelector?.("[data-content-basket-add]");
  if (!button) return;
  const capabilities = interactionCapabilitiesForJob(job);
  const selectedIds = [...root.querySelectorAll("[data-content-match]:checked:not(:disabled)")].map((input) => String(input.value));
  const newCount = selectedIds.filter((matchId) => !contentBasketHas(job, searchId, matchId)).length;
  button.disabled = !capabilities.canMutateContentBasket || !selectedIds.length || !newCount;
  button.title = capabilities.canMutateContentBasket ? "" : capabilities.reason;
  button.textContent = !capabilities.canMutateContentBasket
    ? "任务处理中，暂不能加入"
    : !selectedIds.length
    ? "选择片段后加入合并生成"
    : newCount
      ? `加入合并生成 · ${newCount} 段`
      : "所选已加入待合并片段";
}

function syncVisibleContentBasketAddButtons(job = currentJob) {
  document.querySelectorAll(".content-search-review[data-content-search-id]").forEach((root) => {
    syncContentBasketAddButton(root, job, String(root.dataset.contentSearchId || ""));
  });
}

function addSelectedContentToBasket(root, job, searchId, findMatch) {
  if (!interactionCapabilitiesForJob(job).canMutateContentBasket) {
    showToast(unavailableInteractionMessage(job, "当前不能修改成片清单"), "error");
    return;
  }
  const selectedInputs = [...root.querySelectorAll("[data-content-match]:checked:not(:disabled)")];
  if (!selectedInputs.length) {
    showToast("请先选择要加入合并生成的片段");
    return;
  }
  const added = appendContentMatchesToBasket(
    job,
    searchId,
    selectedInputs.map((input) => String(input.value || "")),
    findMatch,
  );
  if (!added) {
    showToast("所选片段已在待合并片段中");
    syncContentBasketAddButton(root, job, searchId);
    return;
  }
  showToast(`已明确加入 ${added} 段；普通勾选不会自动进入待合并片段`);
}

function appendContentMatchesToBasket(job, searchId, matchIds, findMatch) {
  if (!interactionCapabilitiesForJob(job).canMutateContentBasket) return 0;
  const items = [...contentBasketItems(job)];
  const record = contentSearchRecordsForJob(job).find((item) => String(item?.id || "") === String(searchId));
  let added = 0;
  [...new Set((matchIds || []).map(String).filter(Boolean))].forEach((matchId) => {
    if (items.some((item) => String(item.searchId) === String(searchId) && String(item.matchId) === matchId)) return;
    const candidate = findMatch(matchId);
    if (!candidate) return;
    items.push({
      originJobId: String(job?.id || ""), searchId: String(searchId), matchId, title: candidate.title || "匹配片段",
      start: Number(candidate.start) || 0, end: Number(candidate.end) || 0,
      duration: Math.max(0, Number(candidate.end) - Number(candidate.start)),
      sourceQuery: record?.intent?.query || record?.instruction || "检索",
    });
    added += 1;
  });
  if (!added) return 0;
  job.contentSelectionBasket = {
    ...(job.contentSelectionBasket || {}),
    schemaVersion: "content-selection-basket-v2", entryMode: "explicit", initialized: true, items,
  };
  renderContentSelectionBasket(job);
  syncVisibleContentBasketAddButtons(job);
  scheduleContentBasketSave(job);
  return added;
}

function contentSearchDefaultSelectionIds(search) {
  const candidates = Array.isArray(search?.candidates) ? search.candidates : [];
  const draftIds = search?.reviewDraft?.searchId === search?.id && Array.isArray(search.reviewDraft.selectedMatchIds)
    ? search.reviewDraft.selectedMatchIds.map(String)
    : null;
  const confirmedIds = Array.isArray(search?.confirmedMatchIds) ? search.confirmedMatchIds.map(String) : null;
  const defaultIds = Array.isArray(search?.defaultSelectedIds) ? search.defaultSelectedIds.map(String) : null;
  const explicitIds = draftIds ?? confirmedIds ?? defaultIds;
  if (explicitIds) {
    const allowed = new Set(candidates.filter((candidate) => String(candidate?.reviewStatus || "") !== "rejected").map((candidate) => String(candidate.id)));
    return explicitIds.filter((id) => allowed.has(id));
  }
  const exhaustive = String(search?.resultMode || search?.queryPlan?.result?.mode || "top_k") === "exhaustive";
  return candidates.filter((candidate, index) => {
    const status = String(candidate?.reviewStatus || (candidate?.requiresReview ? "pending" : "confirmed"));
    return status === "kept" || (!exhaustive && status !== "pending" && status !== "rejected" && index < 3);
  }).map((candidate) => String(candidate.id));
}

async function addRetainedContentSearchToBasket(searchId, button, job = currentJob) {
  if (!searchId || !job) return;
  if (!interactionCapabilitiesForJob(job).canMutateContentBasket) {
    showToast(unavailableInteractionMessage(job, "当前不能修改成片清单"), "error");
    return;
  }
  button.disabled = true;
  try {
    const search = await ensureContentSearchDetails(searchId, job);
    const candidates = Array.isArray(search?.candidates) ? search.candidates : [];
    const selectedIds = contentSearchDefaultSelectionIds(search);
    if (!selectedIds.length) {
      showToast("这次检索没有默认保留的片段，请先查看片段并手动选择");
      return;
    }
    const added = appendContentMatchesToBasket(
      job,
      searchId,
      selectedIds,
      (matchId) => candidates.find((candidate) => String(candidate.id) === String(matchId)),
    );
    const query = search?.intent?.query || search?.instruction || "上次检索";
    showToast(added ? `已将“${query}”中的 ${added} 段加入成片清单` : `“${query}”的所选片段已在成片清单中`);
  } catch (error) {
    showToast(`加入成片清单失败：${error.message}`);
  } finally {
    if (button.isConnected) button.disabled = false;
  }
}

function scheduleContentBasketSave(job = currentJob) {
  if (!interactionCapabilitiesForJob(job).canMutateContentBasket) return;
  window.clearTimeout(contentBasketSaveTimer);
  const generation = ++contentBasketSaveGeneration;
  contentBasketSaveTimer = window.setTimeout(async () => {
    if (!job?.id || generation !== contentBasketSaveGeneration) return;
    const basket = job.contentSelectionBasket || {};
    try {
      const response = await apiJson(`/api/jobs/${encodeURIComponent(job.id)}/content-search/basket`, {
        method: "PUT", headers: { "Content-Type": "application/json" },
        body: {
          revision: Number.isFinite(Number(basket.revision)) ? Number(basket.revision) : null,
          items: (basket.items || []).map(({ originJobId, searchId, matchId }) => ({ originJobId: originJobId || job.id, searchId, matchId })),
          outputMode: basket.outputMode || "single_reel",
          orderMode: basket.orderMode || "source",
          subtitleMode: basket.subtitleMode || "none",
          subtitleStyle: basket.subtitleStyle || "clean",
          targetSeconds: Number(basket.targetSeconds) > 0 ? Number(basket.targetSeconds) : null,
        },
      });
      if (currentJob?.id !== job.id || generation !== contentBasketSaveGeneration) return;
      currentJob.contentSelectionBasket = response.basket;
      renderContentSelectionBasket(currentJob);
      syncVisibleContentBasketAddButtons(currentJob);
    } catch (error) {
      if (generation === contentBasketSaveGeneration) showToast(`待合并片段保存失败：${error.message}`);
    }
  }, 180);
}

function renderContentSelectionBasket(job = currentJob) {
  const root = $("#contentSelectionBasket");
  if (!root) return;
  const visible = taskModePresentation(job || {}).key === "content_extract";
  const items = visible ? contentBasketItems(job) : [];
  root.classList.toggle("hidden", !visible || !items.length);
  if (!visible || !items.length) { root.innerHTML = ""; return; }
  const timing = contentBasketTimingSummary(items);
  const sources = new Set(items.map((item) => String(item.sourceQuery || "")).filter(Boolean)).size;
  const outputMode = String(job?.contentSelectionBasket?.outputMode || "single_reel");
  const orderMode = String(job?.contentSelectionBasket?.orderMode || "source");
  const target = Number(job?.contentSelectionBasket?.targetSeconds || 0);
  const capabilities = interactionCapabilitiesForJob(job);
  const locked = !capabilities.canMutateContentBasket;
  const lockedAttributes = locked ? `disabled aria-disabled="true" title="${escapeHtml(capabilities.reason)}"` : "";
  root.innerHTML = `<details class="content-basket-details"><summary><span class="content-basket-copy"><small>跨检索合并预览</small><strong>${items.length} 段 · 实际 ${timing.uniqueDuration.toFixed(1)} 秒${target > 0 ? ` · 目标约 ${target.toFixed(1)} 秒` : ""}</strong><span>${locked ? "任务完成后可继续修改和生成" : `来自 ${sources || 1} 次检索${timing.overlapCount ? ` · ${timing.overlapCount} 处时间重叠` : ""} · 生成成功后清空`}</span></span><em>查看明细</em></summary><ol>${items.map((item) => `<li><span><small>${escapeHtml(item.sourceQuery || "检索")}</small><strong>${escapeHtml(item.title || "匹配片段")}</strong><em>${formatTime(item.start)} → ${formatTime(item.end)} · ${Math.max(0, Number(item.duration) || Number(item.end) - Number(item.start) || 0).toFixed(1)} 秒</em></span><button type="button" data-content-basket-remove data-search-id="${escapeHtml(item.searchId || "")}" data-match-id="${escapeHtml(item.matchId || "")}" ${lockedAttributes}>移出</button></li>`).join("")}</ol></details><div class="content-basket-generation-settings"><label><span>输出方式</span><select data-content-basket-output ${lockedAttributes}><option value="single_reel" ${outputMode === "single_reel" ? "selected" : ""}>合成一条视频</option><option value="separate_events" ${outputMode === "separate_events" ? "selected" : ""}>每段分别导出</option></select></label><label><span>排列顺序</span><select data-content-basket-order ${lockedAttributes}><option value="source" ${orderMode === "source" ? "selected" : ""}>按源视频时间</option><option value="selection" ${orderMode === "selection" ? "selected" : ""}>按加入顺序</option><option value="ai_plan" ${orderMode === "ai_plan" ? "selected" : ""}>AI 推荐顺序</option></select></label></div><div class="content-basket-actions"><button type="button" data-content-basket-clear ${lockedAttributes}>清空</button><button type="button" class="primary" data-content-basket-confirm ${lockedAttributes}>${locked ? "任务完成后生成" : "生成清单内容"}</button></div>`;
  const basketLabel = root.querySelector(".content-basket-copy > small");
  if (basketLabel && !basketLabel.textContent.includes("成片清单")) basketLabel.textContent = `成片清单 · ${basketLabel.textContent}`;
  const details = root.querySelector(".content-basket-details");
  details?.addEventListener("toggle", () => {
    const label = details.querySelector("summary > em");
    if (label) label.textContent = details.open ? "收起明细" : "查看明细";
  });
  root.querySelectorAll("[data-content-basket-remove]").forEach((button) => button.addEventListener("click", () => {
    setContentBasketLocal(job, button.dataset.searchId, button.dataset.matchId, false);
  }));
  root.querySelector("[data-content-basket-output]")?.addEventListener("change", (event) => {
    job.contentSelectionBasket.outputMode = String(event.currentTarget.value || "single_reel");
    scheduleContentBasketSave(job);
  });
  root.querySelector("[data-content-basket-order]")?.addEventListener("change", (event) => {
    job.contentSelectionBasket.orderMode = String(event.currentTarget.value || "source");
    scheduleContentBasketSave(job);
  });
  root.querySelector("[data-content-basket-clear]")?.addEventListener("click", () => {
    job.contentSelectionBasket = {
      ...(job.contentSelectionBasket || {}),
      schemaVersion: "content-selection-basket-v2", entryMode: "explicit", initialized: true, items: [],
    };
    renderContentSelectionBasket(job);
    syncVisibleContentBasketAddButtons(job);
    scheduleContentBasketSave(job);
  });
  root.querySelector("[data-content-basket-confirm]")?.addEventListener("click", () => confirmContentSelectionBasket(job));
}

function conversationMessageMarkup(message, assistantRoleLabel) {
  const role = message.role === "user" ? "user" : message.kind === "error" ? "error" : "assistant";
  let displayText = String(message.text || "");
  const legacyDraftNotice = displayText.match(/^我已拿到《([^》]+)》[。.]?.*/);
  if (role === "assistant" && message.kind === "notice" && legacyDraftNotice) {
    displayText = `视频《${legacyDraftNotice[1]}》已添加。请描述想保留的内容，或选择下方剪辑方式。我会先整理一份可确认的剪辑计划。`;
  }
  const repeatLabel = Number(message.repeatCount || 1) > 1 ? `<em class="message-repeat">重复 ${message.repeatCount} 次</em>` : "";
  const originWorkflowLabel = {
    highlight: "智能高光",
    content_search: "内容检索",
    person_edit: "人物聚焦",
    speaker_edit: "发言剪辑",
  }[String(message.originWorkflowKind || "")] || "";
  const originTag = message.inherited
    ? `<em class="message-origin">${escapeHtml(originWorkflowLabel ? `${originWorkflowLabel} · 历史任务` : "历史任务")}</em>`
    : "";
  const planTag = message.planLabel ? `<em class="message-origin" title="${escapeHtml(message.planGoal || "")}">${escapeHtml(message.planLabel)}</em>` : "";
  const metaClass = originTag || repeatLabel || planTag ? "message-meta" : "message-role-only";
  const technicalRecord = /同源分析数据|Agent 将自动|媒体完整性检查|安全时间线草案/.test(displayText);
  const historicalRecord = role === "assistant" && message.kind !== "warning" && (
    technicalRecord || (["notice", "revision", "agent_plan"].includes(message.kind) && displayText.length > 120));
  const messageBody = historicalRecord
    ? `<details class="message-details"><summary>查看处理记录</summary><p>${escapeHtml(displayText)}</p></details>`
    : `<p>${escapeHtml(displayText)}</p>`;
  return `<article class="chat-message ${role}" data-conversation-key="message:${escapeHtml(message.id || "")}"><span class="avatar">${role === "user" ? "你" : "AI"}</span><div class="bubble"><small class="${metaClass}"><span class="message-role-label">${role === "user" ? "你" : assistantRoleLabel}</span>${originTag}${repeatLabel}${planTag}</small>${messageBody}</div></article>`;
}

function normalizedConversationInstruction(value) {
  return String(value || "").toLowerCase().replace(/[\s，。！？：；、“”‘’]/g, "");
}

function contentSearchPlacement(messages, records) {
  const placements = new Map();
  const unresolved = [];
  const usedUsers = new Set();
  const add = (index, search) => placements.set(index, [...(placements.get(index) || []), search]);
  [...records].sort((left, right) => String(left?.createdAt || "").localeCompare(String(right?.createdAt || ""))).forEach((search) => {
    const linkedIndices = messages.map((message, index) => String(message.contentSearchId || "") === String(search?.id || "") ? index : -1).filter((index) => index >= 0);
    const resultIndex = [...linkedIndices].reverse().find((index) => messages[index]?.role !== "user" && messages[index]?.kind === "content-search");
    let anchor = resultIndex ?? linkedIndices[linkedIndices.length - 1] ?? -1;
    if (anchor < 0 && search?.conversationTurnId) {
      anchor = messages.map((message) => String(message.conversationTurnId || "")).lastIndexOf(String(search.conversationTurnId));
    }
    if (anchor < 0 && search?.createdAt) {
      const searchTime = Date.parse(String(search.createdAt));
      const wanted = normalizedConversationInstruction(search?.instruction);
      for (let index = messages.length - 1; index >= 0; index -= 1) {
        const message = messages[index];
        if (message.role !== "user" || usedUsers.has(index)) continue;
        const messageTime = Date.parse(String(message.createdAt || ""));
        const text = normalizedConversationInstruction(message.text);
        if (Number.isFinite(searchTime) && Number.isFinite(messageTime) && messageTime > searchTime) continue;
        if (wanted && !text.includes(wanted) && !wanted.includes(text)) continue;
        anchor = index;
        usedUsers.add(index);
        while (anchor + 1 < messages.length && messages[anchor + 1].role !== "user") anchor += 1;
        break;
      }
    }
    if (anchor < 0) unresolved.push(search);
    else add(anchor, search);
  });
  return { placements, unresolved };
}

function conversationTimelineMarkup(job, messages, assistantRoleLabel) {
  const records = contentSearchRecordsForJob(job);
  const { placements, unresolved } = contentSearchPlacement(messages, records);
  const currentId = String(job?.contentSearchSession?.activeSearchId || job?.contentSearch?.id || "");
  const searchMarkup = (search) => {
    const current = String(search?.id || "") === currentId;
    const editableCurrent = current && job.status === "awaiting_content_confirmation";
    if (editableCurrent) return contentSearchReviewMarkup(job, search);
    if (!expandedContentSearchIds.has(String(search?.id || ""))) {
      return contentSearchHistorySummaryMarkup(search, { current, job });
    }
    const readonlyJob = current
      ? { ...job, contentSearchSession: { ...(job.contentSearchSession || {}), activeSearchId: "__readonly__" } }
      : job;
    return contentSearchReviewMarkup(readonlyJob, search, { historicalExpanded: true });
  };
  const versions = jobOutputVersions(job).filter((version) => (version.outputs || []).length);
  const versionsById = new Map(versions.map((version) => [String(version.id || ""), version]));
  const versionsByNumber = new Map(versions.map((version) => [Number(version.number || 0), version]));
  const placedVersionIds = new Set();
  const versionForMessage = (message) => {
    const explicit = versionsById.get(String(message?.outputVersionId || ""));
    if (explicit && !placedVersionIds.has(String(explicit.id || ""))) return explicit;
    if (String(message?.role || "") !== "assistant" || String(message?.kind || "") !== "result") return null;
    const match = String(message?.text || "").match(/(?:已保存为|内容视频\s*)\s*V(\d+)/i);
    const legacy = match ? versionsByNumber.get(Number(match[1])) : null;
    return legacy && !placedVersionIds.has(String(legacy.id || "")) ? legacy : null;
  };
  const versionMarkup = (version) => {
    placedVersionIds.add(String(version.id || ""));
    return `<article class="chat-message assistant content-version-message" data-conversation-key="output-version:${escapeHtml(version.id || `v${version.number || 0}`)}"><div class="recommendation-wrap">${contentOutputResultMarkup(job, [version], { compact: true })}</div></article>`;
  };
  const parts = [];
  // Do not expose internal reconciliation failures such as "unresolved early
  // history" in the user-facing chat. If the active search has no reliable
  // conversation anchor, keep it visible; older unmatched searches remain
  // accessible through retained/history result surfaces instead of appearing as
  // a confusing diagnostic block in the current dialogue.
  unresolved
    .filter((search) => String(search?.id || "") === currentId)
    .forEach((search) => parts.push(searchMarkup(search)));
  messages.forEach((message, index) => {
    parts.push(conversationMessageMarkup(message, assistantRoleLabel));
    const version = versionForMessage(message);
    if (version) parts.push(versionMarkup(version));
    (placements.get(index) || []).forEach((search) => parts.push(searchMarkup(search)));
  });
  const placedIds = new Set([...placements.values()].flat().map((search) => String(search?.id || "")));
  unresolved.forEach((search) => placedIds.add(String(search?.id || "")));
  records.filter((search) => !placedIds.has(String(search?.id || ""))).forEach((search) => parts.push(searchMarkup(search)));
  return parts.join("");
}

function contentSearchForRoot(root, job = currentJob) {
  const searchId = String(root?.dataset?.contentSearchId || "");
  return contentSearchRecordsForJob(job).find((item) => String(item?.id || "") === searchId)
    || (searchId && String(job?.contentSearch?.id || "") === searchId ? job.contentSearch : null)
    || job?.contentSearch
    || {};
}

function contentSearchJobForRoot(root, job = currentJob) {
  return { ...job, contentSearch: contentSearchForRoot(root, job) };
}

async function ensureContentSearchDetails(searchId, job = currentJob) {
  const record = contentSearchRecordsForJob(job).find((item) => String(item?.id || "") === String(searchId));
  if (!record || record.candidateDetailsLoaded !== false) return record;
  const requestedJobId = String(job.id || "");
  const payload = await api(`/api/jobs/${encodeURIComponent(requestedJobId)}/content-search/history/${encodeURIComponent(searchId)}`);
  const detail = payload.search || {};
  if (String(currentJob?.id || "") !== requestedJobId) return detail;
  const target = contentSearchRecordsForJob(currentJob).find((item) => String(item?.id || "") === String(searchId));
  if (target) Object.assign(target, detail, { candidateDetailsLoaded: true });
  return target || detail;
}

function collectContentReviewDraft(root, job = currentJob, overrides = {}) {
  const search = job?.contentSearch || {};
  const previous = search.reviewDraft?.searchId === search.id ? search.reviewDraft : {};
  const selectedMatchIds = [...root.querySelectorAll("[data-content-match]:checked")].map((input) => String(input.value));
  const selectedSet = new Set(selectedMatchIds);
  let orderedMatchIds = Array.isArray(overrides.orderedMatchIds)
    ? overrides.orderedMatchIds.map(String).filter((id) => selectedSet.has(id))
    : (previous.orderedMatchIds || []).map(String).filter((id) => selectedSet.has(id));
  orderedMatchIds = [...new Set([...orderedMatchIds, ...selectedMatchIds])];
  return {
    searchId: String(search.id || ""), selectedMatchIds, orderedMatchIds,
    outputMode: String(overrides.outputMode || root.querySelector("[data-content-output-mode]")?.value || previous.outputMode || "single_reel"),
    orderMode: String(overrides.orderMode || root.querySelector("[data-content-order-mode]")?.value || previous.orderMode || "source"),
    subtitleEnabled: Boolean(root.querySelector("[data-content-subtitle]")?.checked),
    subtitleStyle: String(previous.subtitleStyle || "clean"),
  };
}

async function saveContentReviewDraft(root, job = currentJob, overrides = {}) {
  if (!root || !job?.id || !job.contentSearch?.id) return false;
  const draft = collectContentReviewDraft(root, job, overrides);
  const generation = ++contentReviewDraftGeneration;
  try {
    const response = await apiJson(`/api/jobs/${encodeURIComponent(job.id)}/content-search/review-draft`, {
      method: "PATCH", headers: { "Content-Type": "application/json" }, body: draft,
    });
    if (generation !== contentReviewDraftGeneration || currentJob?.id !== job.id) return false;
    const target = contentSearchRecordsForJob(currentJob).find((item) => String(item?.id || "") === draft.searchId);
    if (target) {
      target.reviewDraft = response.reviewDraft;
      target.defaultSelectedIds = [...response.reviewDraft.selectedMatchIds];
    }
    return true;
  } catch (error) {
    if (generation === contentReviewDraftGeneration && currentJob?.id === job.id) showToast(`选择草稿保存失败：${error.message}`);
    return false;
  }
}

function scheduleContentReviewDraftSave(root, job = currentJob, overrides = {}) {
  window.clearTimeout(contentReviewDraftTimer);
  contentReviewDraftTimer = window.setTimeout(() => saveContentReviewDraft(root, job, overrides), 220);
}

function syncContentSearchSelectionSummary(root, job = currentJob) {
  if (!root) return;
  const selected = [...root.querySelectorAll("[data-content-match]:checked")];
  const ids = new Set(selected.map((input) => String(input.value)));
  const matches = (job?.contentSearch?.candidates || []).filter((item) => ids.has(String(item.id)));
  const timing = contentBasketTimingSummary(matches);
  const summary = root.querySelector("[data-content-selection-summary]");
  const exhaustive = String(job?.contentSearch?.resultMode || "top_k") === "exhaustive";
  const completeness = effectiveContentSearchCompleteness(job?.contentSearch || {});
  const strictComplete = !exhaustive || completeness.status === "complete";
  const pendingCount = Number(completeness.pendingCount || 0);
  const gateMessage = !strictComplete
    ? pendingCount
      ? ` · 还有 ${pendingCount} 项需复核`
      : " · 尚未证明找全，可确认风险后生成"
    : selected.length ? " · 确认后才会生成" : " · 请至少选择一段";
  if (summary) summary.innerHTML = `<strong>已选 ${selected.length} 段</strong><span>实际约 ${timing.uniqueDuration.toFixed(1)} 秒${timing.overlapCount ? ` · ${timing.overlapCount} 处重叠` : ""}${gateMessage}</span>`;
  const confirm = root.querySelector("[data-confirm-content]");
  if (confirm && !actionBusy) {
    confirm.disabled = selected.length === 0;
    confirm.title = pendingCount
      ? `点击查看尚未处理的 ${pendingCount} 个候选`
      : strictComplete ? "" : "点击后可确认接受可能遗漏，并按当前已选片段生成";
  }
  syncReviewActionDock();
}

function syncContentGenerationSettingsVisibility(root) {
  const settings = root?.querySelector(".content-generation-settings");
  if (!settings) return;
  const outputVisible = !root.querySelector("[data-content-output-wrap]")?.classList.contains("hidden");
  const orderVisible = !root.querySelector("[data-content-order-wrap]")?.classList.contains("hidden");
  const subtitleVisible = !root.querySelector(".content-subtitle-toggle")?.classList.contains("hidden");
  settings.classList.toggle("hidden", !outputVisible && !orderVisible && !subtitleVisible);
}

function syncContentSearchOutputControls(root, job = currentJob) {
  const output = root.querySelector("[data-content-output-mode]");
  const order = root.querySelector("[data-content-order-mode]");
  const outputWrap = root.querySelector("[data-content-output-wrap]");
  const wrap = root.querySelector("[data-content-order-wrap]");
  const hint = root.querySelector("[data-content-order-hint]");
  if (!output || !order || !outputWrap || !wrap || !hint) return;
  const selectedCount = root.querySelectorAll("[data-content-match]:checked").length;
  const multiple = selectedCount > 1;
  if (!multiple) output.value = "single_reel";
  const separate = output.value === "separate_events";
  outputWrap.classList.toggle("hidden", !multiple);
  output.disabled = !multiple;
  wrap.classList.toggle("hidden", separate || !multiple);
  order.disabled = separate || !multiple;
  const hints = {
    source: "保持源视频中的时间先后，适合过程记录和访谈。",
    selection: "确认时可逐项上移或下移，系统严格采用你的排列。",
    llm_recommend: "根据内容连贯性调整所选片段顺序，完成后可以再次确认。",
  };
  hint.classList.toggle("hidden", !multiple);
  hint.textContent = separate ? "每个片段独立输出，不涉及合成顺序。" : hints[order.value] || hints.source;
  syncContentGenerationSettingsVisibility(root);
  syncContentSearchSelectionSummary(root, job);
}

function contentSearchSubtitleAvailability(root, job = currentJob) {
  const selectedIds = new Set(
    [...root.querySelectorAll("[data-content-match]:checked")].map((input) => String(input.value)),
  );
  const candidates = (job?.contentSearch?.candidates || []).filter((item) => selectedIds.has(String(item.id)));
  const ranges = candidates.map((item) => ({ start: Number(item.start) || 0, end: Number(item.end) || 0 }));
  if (job?.videoInfo?.has_audio === false) {
    return { available: false, count: 0, message: "源视频没有音轨，无需添加字幕。" };
  }
  const directSegments = Array.isArray(job?.speechAnalysis?.segments) ? job.speechAnalysis.segments : [];
  const loadedSegments = timelineTranscriptJobId === job?.id && Array.isArray(timelineTranscript) ? timelineTranscript : [];
  const candidateSegments = candidates.flatMap((item) => Array.isArray(item.speechUnits) ? item.speechUnits : []);
  const transcript = [...directSegments, ...loadedSegments, ...candidateSegments];
  const seen = new Set();
  const matching = transcript.filter((segment) => {
    const text = String(segment?.text || "").trim();
    const start = Number(segment?.start);
    const end = Number(segment?.end);
    const key = `${start}:${end}:${text}`;
    if (seen.has(key) || !/[\p{L}\p{N}]/u.test(text) || !Number.isFinite(start) || !Number.isFinite(end)) return false;
    seen.add(key);
    return ranges.some((range) => Math.min(range.end, end) - Math.max(range.start, start) >= .08);
  });
  if (matching.length) {
    return { available: true, count: matching.length, message: `所选片段检测到 ${matching.length} 段可转写对白。` };
  }
  const speechAnalyzed = Array.isArray(job?.speechAnalysis?.segments)
    || Boolean(job?.speechAnalysis?.status)
    || (job?.recognition?.attemptedModalities || []).includes("speech")
    || (job?.recognition?.completedModalities || []).includes("speech");
  return {
    available: false,
    count: 0,
    message: speechAnalyzed || transcript.length
      ? "所选片段没有可转写对白，无需添加字幕。"
      : "当前检索没有对白转写，无法添加 AI 字幕。",
  };
}

function syncContentSearchSubtitleControls(root, job = currentJob) {
  const input = root?.querySelector("[data-content-subtitle]");
  const status = root?.querySelector("[data-content-subtitle-status]");
  const label = input?.closest(".content-subtitle-toggle");
  if (!input || !status || !label) return;
  const availability = contentSearchSubtitleAvailability(root, job);
  input.disabled = !availability.available;
  if (!availability.available) input.checked = false;
  label.classList.toggle("unavailable", !availability.available);
  label.classList.toggle("hidden", !availability.available);
  status.textContent = availability.message;
  label.title = availability.message;
  syncContentGenerationSettingsVisibility(root);
}

function chatMessageReferencesPlayhead(text = chatInput?.value || "") {
  const value = String(text || "").trim();
  if (!value) return false;
  return /播放头|当前位置|当前(?:时间|位置|画面|帧|这?(?:一)?段)|这里|这儿|此处|此时|这个(?:位置|时间点)|这一帧|从这(?:里|儿|个位置)?|到这(?:里|儿|个位置)?|\b(?:playhead|current position|current frame|from here|to here|at this point)\b/i.test(value);
}

function chatUiContextEntries() {
  if (!currentJob) return [];
  const contentMode = taskModePresentation(currentJob).key === "content_extract";
  const chatRoot = $("#chatMessages");
  const eventGroupIds = [...(chatRoot?.querySelectorAll(".event-group-check:checked") || [])].map((input) => String(input.value));
  const eventSegmentIds = [...(chatRoot?.querySelectorAll(".rail-segment-check:checked") || [])].map((input) => ({
    groupId: String(input.dataset.groupId || input.closest("[data-event-group]")?.dataset.eventGroup || ""),
    segmentId: String(input.value || input.dataset.segmentId || ""),
  })).filter((item) => item.segmentId);
  const viewer = {
    kind: viewerMediaKind,
    ...(currentCandidate?.index != null ? { candidateIndex: Number(currentCandidate.index) } : {}),
    ...(currentEventGroup?.id ? { groupId: String(currentEventGroup.id) } : {}),
    ...(currentEventSegment?.id ? { segmentId: String(currentEventSegment.id) } : {}),
    ...(currentOutput?.filename ? { outputFilename: String(currentOutput.filename) } : {}),
  };
  const viewerLabels = {
    source: "正在看源视频", candidate: "正在预览高光候选", event: "正在预览事件",
    segment: "正在预览镜头", output: contentMode ? "正在预览内容视频" : "正在预览成片", content: "正在预览检索片段",
  };
  const selection = currentJob.manualSelection || pendingTimelineSelection;
  const referencesPlayhead = !contentMode && chatMessageReferencesPlayhead();
  const hasCompositionTargets = eventGroupIds.length > 0
    || eventSegmentIds.length > 1
    || timelineChatSelections.length > 1;
  const hasNonDefaultComposition = outputAssemblyMode !== "single_reel";
  const contentRoot = activeContentReviewRoot(currentJob);
  const contentDraft = contentRoot ? collectContentReviewDraft(contentRoot, currentJob) : currentJob.contentSearch?.reviewDraft;
  const contentIds = contentDraft?.orderedMatchIds || contentDraft?.selectedMatchIds || [];
  return [
    referencesPlayhead ? { key: "playhead", label: `${viewerMediaKind === "output" ? "样片" : "源视频"}播放头 ${formatTime(mainVideo.currentTime)}`, value: { playheadSeconds: Number(mainVideo.currentTime || 0) } } : null,
    viewerMediaKind !== "source" ? { key: "viewer", label: viewerLabels[viewerMediaKind] || "当前预览对象", value: { viewer } } : null,
    contentMode && currentJob.contentSearch?.id ? { key: "contentMatches", label: `本次已选 ${contentIds.length} 段 · 按所选顺序`, value: { searchId: currentJob.contentSearch.id, selected: { contentMatchIds: contentIds } } } : null,
    eventGroupIds.length ? { key: "eventGroups", label: `已勾选 ${eventGroupIds.length} 个事件`, value: { selected: { eventGroupIds } } } : null,
    eventSegmentIds.length ? { key: "eventSegments", label: `已勾选 ${eventSegmentIds.length} 个镜头`, value: { selected: { eventSegmentIds } } } : null,
    selection ? { key: "timelineSelection", label: `时间选区 ${formatTime(selection.start)}–${formatTime(selection.end)}`, value: { timelineSelection: { start: Number(selection.start), end: Number(selection.end) } } } : null,
    timelineChatSelections.length ? { key: "timelineSelections", label: `待发送 ${timelineChatSelections.length} 个时间段`, value: { timelineSelections: timelineChatSelections.map((item) => ({ ...item })) } } : null,
    !contentMode && (hasCompositionTargets || hasNonDefaultComposition)
      ? { key: "composition", label: outputAssemblyMode === "separate_events" ? "分别导出" : "合成一条", value: { composition: { outputMode: outputAssemblyMode, orderMode: "selection" } } }
      : null,
  ].filter(Boolean);
}

function collectChatUiContext() {
  const result = {};
  chatUiContextEntries().forEach((entry) => {
    if (ignoredChatContextKeys.has(entry.key)) return;
    Object.entries(entry.value).forEach(([key, value]) => {
      if (key === "selected") result.selected = { ...(result.selected || {}), ...value };
      else result[key] = value;
    });
  });
  return result;
}

window.ClipTalkCollectAssistantContext = () => ({
  ...collectChatUiContext(), jobId: currentJob?.id,
  timeDomain: viewerMediaKind === "output" ? "preview" : "source",
});
window.ClipTalkRenderAssistantHistory = () => { if (currentJob) renderConversation(currentJob); };
window.ClipTalkOpenContentEvidence = () => openCandidateDrawer();

const legacyWorkflowChoices = [
  {
    key: "highlight",
    label: "智能高光",
    detail: "通看全片，自动发现精彩事件并生成高光版本。",
    badge: "01",
  },
  {
    key: "content_search",
    label: "内容检索",
    detail: "按你的描述查找画面、动作、对白、文字或声音。",
    badge: "02",
  },
  {
    key: "person_edit",
    label: "人物聚焦",
    detail: "识别画面人物，再选择要保留的人物出镜片段。",
    badge: "03",
  },
  {
    key: "speaker_edit",
    label: "发言剪辑",
    detail: "区分匿名声音，试听代表片段后选择目标发言。",
    badge: "04",
  },
];

const draftCapabilityCategories = [
  { key: "auto", label: "自动选择", detail: "描述想要的结果，由系统组合适合的能力。", icon: "auto", badge: "推荐" },
  ...legacyWorkflowChoices.map((choice) => ({ ...choice, icon: choice.key, badge: choice.key === "highlight" ? "常用" : "" })),
  { key: "revision", label: "剪辑调整", detail: "调整已有视频或样片的结构与节奏。", icon: "revision", badge: "编辑" },
  { key: "format", label: "版式适配", detail: "调整字幕并适配横屏、竖屏或方形。", icon: "format", badge: "适配" },
  { key: "package", label: "视觉包装", detail: "制作封面、片头、图文和补充画面。", icon: "package", badge: "包装" },
  { key: "delivery", label: "导出交付", detail: "按平台导出、交付草稿或检查成片。", icon: "delivery", badge: "交付" },
];

const capabilityCategoryIcons = Object.freeze({
  auto: '<svg viewBox="0 0 24 24"><circle cx="5" cy="6" r="1.8"/><circle cx="19" cy="6" r="1.8"/><circle cx="12" cy="18" r="1.8"/><path d="M6.8 6h4.3a3 3 0 0 1 3 3v2M17.2 6h-.7a2.5 2.5 0 0 0-2.5 2.5V11M12 16.2V11"/></svg>',
  highlight: '<svg viewBox="0 0 24 24"><path d="M4 8.5h16v11H4zM4 8.5 6 4h15l-2 4.5M9 4 7 8.5M15 4l-2 4.5"/><path d="m10 12 4 2.3-4 2.2v-4.5Z"/></svg>',
  content_search: '<svg viewBox="0 0 24 24"><circle cx="10.5" cy="10.5" r="5.5"/><path d="m15 15 4.5 4.5"/></svg>',
  person_edit: '<svg viewBox="0 0 24 24"><path d="M8 3H4v4M16 3h4v4M8 21H4v-4M16 21h4v-4"/><circle cx="12" cy="9" r="2.7"/><path d="M7.5 17c.6-2.7 2.1-4 4.5-4s3.9 1.3 4.5 4"/></svg>',
  speaker_edit: '<svg viewBox="0 0 24 24"><rect x="9" y="3.5" width="6" height="11" rx="3"/><path d="M6.5 11.5a5.5 5.5 0 0 0 11 0M12 17v3M9 20h6"/></svg>',
  revision: '<svg viewBox="0 0 24 24"><path d="M4 6h7M15 6h5M4 12h3M11 12h9M4 18h9M17 18h3"/><circle cx="13" cy="6" r="2"/><circle cx="9" cy="12" r="2"/><circle cx="15" cy="18" r="2"/></svg>',
  format: '<svg viewBox="0 0 24 24"><path d="M8 3H4v4M16 3h4v4M8 21H4v-4M16 21h4v-4"/><path d="M8 9h8M8 12h6M8 15h8"/></svg>',
  package: '<svg viewBox="0 0 24 24"><rect x="3.5" y="5" width="17" height="14" rx="2"/><path d="m6.5 16 3.5-3.5 2.5 2.5 2.2-2.2 2.8 3.2M16.5 8.5h.01"/></svg>',
  delivery: '<svg viewBox="0 0 24 24"><path d="M12 3v11m0 0 4-4m-4 4-4-4"/><path d="M5 17v3h14v-3"/></svg>',
});

function capabilityCategoryIconMarkup(icon) {
  const key = capabilityCategoryIcons[icon] ? icon : "auto";
  return capabilityCategoryIcons[key].replace("<svg", `<svg data-capability-icon="${key}" aria-hidden="true"`);
}

function capabilityCategoryCardMarkup(choice) {
  return `<button type="button" class="legacy-workflow-card capability-category-card ${escapeHtml(choice.key)}${pendingCapabilityCategory === choice.key ? " selected" : ""}" data-capability-category-switch="${escapeHtml(choice.key)}" aria-pressed="${String(pendingCapabilityCategory === choice.key)}">
    <i class="legacy-workflow-thumb" aria-hidden="true">${capabilityCategoryIconMarkup(choice.icon)}</i>
    <span><strong>${escapeHtml(choice.label)}</strong><small>${escapeHtml(choice.detail)}</small></span>
    <b aria-hidden="true">${escapeHtml(choice.badge || "")}</b>
  </button>`;
}

function pendingCapabilitySelection() {
  const group = pendingCapabilityCategory === "auto" ? null : BRIEF_CAPABILITY_GROUPS[pendingCapabilityCategory];
  const option = group?.options.find((item) => item.id === pendingCapabilityOption) || null;
  return { group, option };
}

function assistantActionDockMarkup(job) {
  const { group, option } = pendingCapabilitySelection();
  const canRun = Boolean(job && option?.skill);
  const workflowPending = Boolean(pendingWorkflowSwitch);
  const category = pendingCapabilityCategory || "auto";
  const icon = capabilityCategoryIconMarkup(category);
  const categoryLabel = group?.title || "自动选择";
  const optionLabel = option?.label || (group ? "选择具体功能" : "根据描述匹配剪辑能力");
  const stateLabel = workflowPending ? "继续设置" : option ? "已选择" : group ? "待选择" : "推荐";
  return `<div class="assistant-action-summary">
    <i class="assistant-action-icon ${escapeHtml(category)}" aria-hidden="true">${icon}</i>
    <button type="button" class="assistant-action-copy" data-open-capability-drawer aria-haspopup="dialog">
      <small>处理方式 · ${escapeHtml(stateLabel)}</small>
      <strong>${escapeHtml(categoryLabel)}${option ? ` / ${escapeHtml(optionLabel)}` : ""}</strong>
      <span>${escapeHtml(option ? option.detail : group ? group.summary : "直接描述目标，系统会在执行前说明采用的能力。")}</span>
    </button>
    <button type="button" class="assistant-action-change" ${option ? "data-change-draft-capability" : "data-open-capability-drawer"}>${group ? "更换" : "选择"}</button>
  </div>
  ${canRun ? `<button type="button" class="assistant-action-primary" data-run-draft-capability>${option.id === "cover" ? "生成封面方案" : `开始${escapeHtml(option.label)}`}</button>` : ""}`;
}

function syncCapabilityDrawer(canChoose) {
  const drawer = $("#ctV4AssistantQuickStart");
  const scrim = $("#capabilityDrawerScrim");
  const assistant = $("#assistantPanel");
  const open = Boolean(canChoose && legacyWorkflowPickerOpen);
  drawer?.classList.toggle("hidden", !open);
  drawer?.setAttribute("aria-hidden", String(!open));
  scrim?.classList.toggle("hidden", !open);
  assistant?.classList.toggle("capability-drawer-open", open);
  if (!open) return;
  requestAnimationFrame(() => {
    drawer?.querySelector("[data-workflow-launch-instruction], [data-capability-category-switch], [data-workflow-launch-scope], [data-change-draft-capability]")?.focus({ preventScroll: true });
  });
}

function openCapabilityDrawer(trigger = document.activeElement) {
  capabilityDrawerReturnFocus = trigger && typeof trigger.focus === "function" ? trigger : null;
  legacyWorkflowPickerOpen = true;
  quickWorkflowValidation = "";
  renderQuickWorkflowPicker(currentJob);
}

function closeCapabilityDrawer({ restoreFocus = true } = {}) {
  legacyWorkflowPickerOpen = false;
  renderQuickWorkflowPicker(currentJob);
  if (restoreFocus) {
    const returnTarget = capabilityDrawerReturnFocus?.isConnected
      ? capabilityDrawerReturnFocus
      : $("#assistantActionDock [data-open-capability-drawer], #assistantActionDock [data-change-draft-capability]");
    returnTarget?.focus?.({ preventScroll: true });
  }
  capabilityDrawerReturnFocus = null;
}

function draftCapabilityDetailMarkup(job) {
  if (pendingCapabilityCategory === "auto") {
    return `<section class="draft-capability-auto"><small>当前方式</small><strong>自动选择</strong><p>直接在下方描述成片目标。生成方案时会说明采用的能力，你仍可在执行前确认。</p></section>`;
  }
  const group = BRIEF_CAPABILITY_GROUPS[pendingCapabilityCategory];
  if (!group) return "";
  const selectedOption = group.options.find((item) => item.id === pendingCapabilityOption);
  if (selectedOption) {
    const isCover = selectedOption.id === "cover";
    const guidance = isCover
      ? `<ol><li><strong>描述封面重点</strong><span>说明要突出的人物、产品或情绪。</span></li><li><strong>补充标题文字</strong><span>没有指定文案时，系统会根据视频内容建议。</span></li><li><strong>生成并选择方案</strong><span>生成三张候选封面，确认后再保存。</span></li></ol>`
      : `<p class="draft-capability-focus-note">在下方输入框补充具体要求，系统会先整理可确认方案。</p>`;
    return `<section class="draft-capability-detail draft-capability-focus${isCover ? " is-cover" : ""}" aria-live="polite">
      <header><div><small>${escapeHtml(group.title)} · 已选择</small><strong>${escapeHtml(selectedOption.label)}</strong></div><button type="button" data-change-draft-capability>更换功能</button></header>
      <p>${escapeHtml(selectedOption.detail)}</p>
      ${guidance}
      <footer><button type="button" class="primary" data-run-draft-capability>${isCover ? "生成封面方案" : `开始${escapeHtml(selectedOption.label)}`}</button></footer>
    </section>`;
  }
  const hasReviewResult = orderedJobOutputs(job).some(({ item }) => Boolean(item?.filename));
  return `<section class="draft-capability-detail" aria-live="polite">
    <header><div><small>具体功能</small><strong>${escapeHtml(group.title)}</strong></div><p>${escapeHtml(group.summary)}</p></header>
    <div class="draft-capability-options" role="radiogroup" aria-label="${escapeHtml(group.title)}具体功能">
      ${group.options.map((option) => {
        const available = !option.requiresResult || hasReviewResult;
        const selected = option.id === pendingCapabilityOption
          || (!pendingCapabilityOption && option.workflow === pendingWorkflowSwitch);
        const action = option.workflow
          ? `data-workflow-switch="${escapeHtml(option.workflow)}"`
          : `data-draft-capability-skill="${escapeHtml(option.skill || "")}" data-draft-capability-option="${escapeHtml(option.id)}"`;
        return `<button type="button" ${action} aria-pressed="${String(selected)}"${available ? "" : " disabled aria-disabled=\"true\""}><span><strong>${escapeHtml(option.label)}</strong><small>${escapeHtml(option.detail)}</small></span><b>${available ? (option.badge || "选择") : "需先有审核样片"}</b></button>`;
      }).join("")}
    </div>
  </section>`;
}

function legacyWorkflowPickerMarkup() {
  if (selectedDraftCapability(currentJob)) return draftCapabilityDetailMarkup(currentJob);
  const automatic = draftCapabilityCategories.find((choice) => choice.key === "auto");
  const editing = draftCapabilityCategories.filter((choice) => ["highlight", "content_search", "person_edit", "speaker_edit"].includes(choice.key));
  const finishing = draftCapabilityCategories.filter((choice) => ["revision", "format", "package", "delivery"].includes(choice.key));
  return `<header>
    <strong>选择处理方式</strong>
    <p>使用自动选择，或先选方向再选择具体功能。</p>
  </header>
  <div class="legacy-workflow-grid capability-category-grid" role="radiogroup" aria-label="选择剪辑能力类别">
    ${capabilityCategoryCardMarkup(automatic)}
    <div class="capability-section-label"><strong>智能剪辑</strong><small>从素材发现并组织内容</small></div>
    ${editing.map(capabilityCategoryCardMarkup).join("")}
    <div class="capability-section-label"><strong>视频再加工</strong><small>处理已有视频或审核样片</small></div>
    ${finishing.map(capabilityCategoryCardMarkup).join("")}
  </div>
  ${draftCapabilityDetailMarkup(currentJob)}`;
}

function workflowLaunchLabel(workflowKind) {
  return {
    highlight: "智能高光",
    content_search: "内容检索",
    person_edit: "人物聚焦",
    speaker_edit: "发言剪辑",
  }[workflowKind] || "快速剪辑";
}

function agentDraftPlaceholder() {
  return "描述你想怎么剪……";
}

function quickWorkflowPlaceholder(workflowKind) {
  return {
    highlight: "可补充高光主题，例如“突出产品质感”。",
    content_search: "输入要查找的内容，例如“所有汽车画面”。",
    person_edit: "可补充人物特征，例如“穿白衣的女生”。",
    speaker_edit: "可补充声音条件，例如“只保留主持人发言”。",
  }[workflowKind] || "描述新任务要求……";
}

function draftCapabilityRecommendation(option = pendingCapabilitySelection().option) {
  return String(option?.recommendation || option?.placeholder || "")
    .replace(/^例如[：:]\s*/, "")
    .trim();
}

function applyDraftCapabilityRecommendation(option) {
  if (!chatInput) return false;
  const recommendation = draftCapabilityRecommendation(option);
  // Before upload, keep the recommendation as an optional suggestion so a
  // generated example is never mistaken for a committed creation request.
  const shouldInsert = Boolean(currentJob && !chatInput.value.trim() && recommendation);
  if (shouldInsert) {
    chatInput.value = recommendation;
    chatInput.dispatchEvent(new Event("input", { bubbles: true }));
  } else {
    syncComposerSuggestion(currentJob);
  }
  return shouldInsert;
}

function composerSuggestionText(job = currentJob) {
  const recommendation = draftCapabilityRecommendation();
  if (recommendation) return recommendation;
  if (!isAgentInstructionDraft(job)) return "";
  const filename = String(job.filename || job.sourceFilename || "");
  const briefText = [
    filename,
    job?.detail,
    job?.brief?.objective,
    job?.brief?.narrativeGoal,
    job?.request?.contentInstruction,
    job?.request?.instruction,
    Array.isArray(job?.messages) ? job.messages.map((message) => message?.text || "").join(" ") : "",
  ].join(" ").toLowerCase();
  const duration = Number(job?.videoInfo?.duration || job?.request?.sourceScope?.end || 0);
  const selectedWorkflow = String(pendingWorkflowSwitch || job?.request?.workflowKind || job?.workflowKind || "");
  if (selectedWorkflow === "speaker_edit") return "识别本视频中的说话人，整理每个人的代表片段，并让我选择要保留的发言。";
  if (selectedWorkflow === "person_edit") return "识别视频里主要出镜人物，按人物整理可用片段，并生成一版人物精剪样片。";
  if (selectedWorkflow === "highlight" && pendingWorkflowSwitch) return "自动找出最有冲击力的高光镜头，按节奏合成一版可审核样片。";
  if (/咖啡|coffee|品牌|访谈|创始人|founder/.test(briefText)) {
    return "帮我剪一个 3 分钟的品牌故事短片，突出创始人的初心、产品理念和未来愿景。";
  }
  if (/小米|汽车|su7|车|发布会/.test(briefText)) {
    return "找出所有汽车相关画面，合成竖屏视频，并在顶部添加对应字幕。";
  }
  if (/冰箱|空调|洗衣机|家电|产品宣传/.test(briefText)) {
    return "找出产品新老替换和核心卖点画面，合成竖屏产品宣传短片，并生成审核样片。";
  }
  if (/学生|学校|课堂|采访|人物/.test(briefText)) {
    return "剪成人物访谈精华，保留清晰问题和完整回答，并生成带字幕的审核样片。";
  }
  if (duration >= 600) return "通看全片，找出最适合做短视频的高光片段，先生成一版审核样片。";
  return "找出这段视频里最有信息量的画面，按源视频顺序合成一版可审核短片。";
}

function syncComposerSuggestion(job = currentJob) {
  const suggestion = $("#composerSuggestion");
  if (!suggestion) return;
  const currentText = String(chatInput?.value || "").trim();
  const recommendation = draftCapabilityRecommendation();
  const mayAppendRecommendation = Boolean(currentText && recommendation && currentText !== recommendation);
  const text = chatInput && !chatInput.disabled && !actionBusy && (!currentText || mayAppendRecommendation)
    ? composerSuggestionText(job)
    : "";
  suggestion.classList.toggle("hidden", !text);
  suggestion.dataset.suggestionMode = mayAppendRecommendation ? "append" : "replace";
  suggestion.textContent = text ? `${mayAppendRecommendation ? "添加建议" : "试试"}：“${text}”` : "";
  resizeChatComposerInput();
}

function workflowLaunchMarkup() {
  if (!pendingWorkflowSwitch) return "";
  const currentScope = currentJob?.request?.sourceScope || {};
  const hasCurrentScope = Number(currentScope.end) > Number(currentScope.start)
    && Boolean(currentScope.isNarrow);
  const startLabel = {
    highlight: "开始智能高光",
    content_search: "开始内容检索",
    person_edit: "开始人物聚焦",
    speaker_edit: "开始发言剪辑",
  }[pendingWorkflowSwitch] || "";
  const selected = legacyWorkflowChoices.find((choice) => choice.key === pendingWorkflowSwitch);
  const instructionLabel = {
    highlight: "剪辑要求",
    content_search: "检索内容",
    person_edit: "人物要求",
    speaker_edit: "发言要求",
  }[pendingWorkflowSwitch] || "补充要求";
  const currentInstruction = String(pendingWorkflowOptions.instruction ?? chatInput?.value ?? "").trim();
  const validation = quickWorkflowValidation
    ? `<p class="workflow-launch-validation" role="alert">${escapeHtml(quickWorkflowValidation)}</p>`
    : "";
  const directStart = startLabel
    ? `<button type="button" class="primary workflow-launch-start" data-start-workflow-switch>${startLabel}</button>`
    : "";
  return `<section class="workflow-launch-context quick-workflow-launch" aria-label="${escapeHtml(workflowLaunchLabel(pendingWorkflowSwitch))}启动设置">
    <header>
      <div><small>已选择</small><strong>${escapeHtml(workflowLaunchLabel(pendingWorkflowSwitch))}</strong>${selected?.detail ? `<p>${escapeHtml(selected.detail)}</p>` : ""}</div>
      <button type="button" data-cancel-workflow-switch aria-label="取消快速剪辑设置">×</button>
    </header>
    <div class="workflow-launch-fields">
      <label>素材范围<select data-workflow-launch-scope><option value="all" ${pendingWorkflowOptions.scope === "all" ? "selected" : ""}>整个源视频</option>${hasCurrentScope ? `<option value="current" ${pendingWorkflowOptions.scope === "current" ? "selected" : ""}>沿用当前范围 ${formatTime(currentScope.start)}–${formatTime(currentScope.end)}</option>` : ""}</select></label>
      ${pendingWorkflowSwitch === "highlight" ? `<label>目标时长（秒）<input type="number" min="4" step="1" placeholder="自动" value="${escapeHtml(pendingWorkflowOptions.targetSeconds)}" data-workflow-target-seconds></label><label>版本数<select data-workflow-variant-count>${[1,2,3,4].map((value) => `<option value="${value}" ${Number(pendingWorkflowOptions.variantCount) === value ? "selected" : ""}>${value} 个</option>`).join("")}</select></label>` : ""}
      ${pendingWorkflowSwitch === "speaker_edit" ? `<label>预计人数<input type="number" min="0" max="32" step="1" placeholder="自动判断" value="${Number(pendingWorkflowOptions.expectedSpeakerCount) || ""}" data-workflow-speaker-count></label>` : ""}
    </div>
    <label class="workflow-launch-instruction">
      <span>${escapeHtml(instructionLabel)}</span>
      <textarea rows="4" maxlength="500" data-workflow-launch-instruction placeholder="${escapeHtml(quickWorkflowPlaceholder(pendingWorkflowSwitch))}">${escapeHtml(currentInstruction)}</textarea>
      <small>已填入建议，可直接修改；按 Enter 开始，Shift+Enter 换行。</small>
    </label>
    ${validation}
    ${directStart ? `<footer>${directStart}</footer>` : ""}
  </section>`;
}

function bindWorkflowLaunchControls(root) {
  const instructionInput = root.querySelector("[data-workflow-launch-instruction]");
  const syncInstruction = () => {
    if (!instructionInput || !chatInput) return String(instructionInput?.value || "").trim();
    pendingWorkflowOptions.instruction = instructionInput.value;
    chatInput.value = instructionInput.value;
    chatInput.dispatchEvent(new Event("input", { bubbles: true }));
    return instructionInput.value.trim();
  };
  if (instructionInput && chatInput && !chatInput.value.trim() && instructionInput.value.trim()) {
    chatInput.value = instructionInput.value;
    resizeChatComposerInput();
    updateComposerBeam();
  }
  root.querySelector("[data-cancel-workflow-switch]")?.addEventListener("click", () => {
    pendingWorkflowSwitch = "";
    document.querySelector(".agent-draft-modes")?.classList.remove("workflow-choice-made");
    pendingWorkflowOptions = { scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3 };
    quickWorkflowValidation = "";
    if (chatInput) chatInput.placeholder = isAgentInstructionDraft(currentJob)
      ? agentDraftPlaceholder()
      : "请输入指令或补充需求……";
    renderQuickWorkflowPicker(currentJob);
    renderChatContextBar();
  });
  root.querySelector("[data-workflow-launch-scope]")?.addEventListener("change", (event) => { pendingWorkflowOptions.scope = event.currentTarget.value; });
  root.querySelector("[data-workflow-target-seconds]")?.addEventListener("input", (event) => { pendingWorkflowOptions.targetSeconds = event.currentTarget.value; });
  root.querySelector("[data-workflow-variant-count]")?.addEventListener("change", (event) => { pendingWorkflowOptions.variantCount = Number(event.currentTarget.value) || 3; });
  root.querySelector("[data-workflow-speaker-count]")?.addEventListener("input", (event) => { pendingWorkflowOptions.expectedSpeakerCount = Number(event.currentTarget.value) || 0; });
  instructionInput?.addEventListener("input", () => {
    if (quickWorkflowValidation && instructionInput.value.trim()) {
      quickWorkflowValidation = "";
      root.querySelector(".workflow-launch-validation")?.remove();
    }
    syncInstruction();
  });
  instructionInput?.addEventListener("keydown", (event) => {
    if (event.key === "Enter" && !event.shiftKey && !event.isComposing && event.keyCode !== 229) {
      event.preventDefault();
      createSameSourceWorkflow(pendingWorkflowSwitch, syncInstruction());
    }
  });
  root.querySelectorAll("[data-workflow-target-seconds], [data-workflow-speaker-count]").forEach((input) => input.addEventListener("keydown", (event) => {
    if (event.key === "Enter") {
      event.preventDefault();
      createSameSourceWorkflow(pendingWorkflowSwitch, syncInstruction());
    }
  }));
  root.querySelector("[data-start-workflow-switch]")?.addEventListener("click", () => {
    createSameSourceWorkflow(pendingWorkflowSwitch, syncInstruction());
  });
}

function renderQuickWorkflowPicker(job = currentJob) {
  const picker = $("#quickWorkflowPicker");
  const button = $("#chatLegacyModeButton");
  const actionDock = $("#assistantActionDock");
  const creatingNewTask = Boolean(!job
    && studio?.classList.contains("new-task-workbench")
    && !studio?.classList.contains("home-mode"));
  const canChooseQuickWorkflow = creatingNewTask || isAgentInstructionDraft(job);
  const visible = Boolean(canChooseQuickWorkflow && legacyWorkflowPickerOpen);
  if (button) button.setAttribute("aria-expanded", String(Boolean(canChooseQuickWorkflow && legacyWorkflowPickerOpen)));
  if (actionDock) {
    actionDock.classList.toggle("hidden", !canChooseQuickWorkflow);
    actionDock.innerHTML = canChooseQuickWorkflow ? assistantActionDockMarkup(job) : "";
  }
  if (!picker) return;
  picker.classList.toggle("hidden", !visible);
  syncCapabilityDrawer(canChooseQuickWorkflow);
  if (!visible) {
    picker.innerHTML = "";
    syncComposerSuggestion(job);
    return;
  }
  // Before a source is uploaded, choosing a quick workflow records the
  // preference but must not render start controls that require a persisted
  // job. The same choice becomes actionable as soon as the upload draft is
  // created.
  const showLaunchControls = Boolean(job && pendingWorkflowSwitch);
  picker.innerHTML = showLaunchControls ? workflowLaunchMarkup() : legacyWorkflowPickerMarkup();
  if (showLaunchControls) bindWorkflowLaunchControls(picker);
  syncComposerSuggestion(job);
}

function renderChatContextBar() {
  const bar = $("#chatContextBar");
  if (!bar) return;
  const entries = chatUiContextEntries().filter((entry) => !ignoredChatContextKeys.has(entry.key));
  bar.classList.toggle("hidden", !currentJob || !entries.length);
  bar.innerHTML = entries.length ? `<small>本次会附带</small>${entries.map((entry) => `<button type="button" data-remove-chat-context="${escapeHtml(entry.key)}" title="不在下一条消息中附带这项">${escapeHtml(entry.label)}<span aria-hidden="true">×</span></button>`).join("")}` : "";
  bar.querySelectorAll("[data-remove-chat-context]").forEach((button) => button.addEventListener("click", () => {
    ignoredChatContextKeys.add(button.dataset.removeChatContext);
    renderChatContextBar();
  }));
}

function editProposalMarkup(proposal) {
  if (!proposal || proposal.status !== "pending") return "";
  const preview = proposal.preview || {};
  const before = Number(preview.durationBefore || 0);
  const after = Number(preview.durationAfter || 0);
  const durationFact = before || after
    ? `<span>预计时长 ${before.toFixed(1)}s → ${after.toFixed(1)}s</span>`
    : "";
  const changes = (proposal.changes || []).slice(0, 8);
  return `<article class="chat-message assistant edit-proposal-message"><span class="avatar">AI</span><section class="edit-proposal-card" data-edit-proposal="${escapeHtml(proposal.id || "")}">
    <header><div><small>AI 剪辑提案 · 尚未应用</small><strong>${escapeHtml(proposal.title || "剪辑修改")}</strong></div><b>预览</b></header>
    <p>${escapeHtml(proposal.summary || "请检查这些修改，确认后才会写入正式时间轴。")}</p>
    <div class="edit-proposal-facts">${durationFact}<span>${changes.length} 项修改</span></div>
    ${changes.length ? `<ul>${changes.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>` : ""}
    <p class="edit-proposal-warning">虚线范围是临时时间线预览；取消不会改变现有剪辑。</p>
    <footer><button type="button" class="primary" data-apply-edit-proposal>确认应用</button><button type="button" data-modify-edit-proposal>继续修改</button><button type="button" data-cancel-edit-proposal>取消提案</button></footer>
  </section></article>`;
}

async function resolveEditProposal(action) {
  const proposal = currentJob?.pendingEditProposal;
  if (!proposal?.id || actionBusy) return;
  if (action === "modify") {
    chatInput.value = `修改这个提案：`;
    chatInput.focus();
    chatInput.setSelectionRange(chatInput.value.length, chatInput.value.length);
    return;
  }
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const endpoint = `/api/jobs/${encodeURIComponent(actionToken.jobId)}/edit-proposals/${encodeURIComponent(proposal.id)}${action === "apply" ? "/apply" : ""}`;
    const { job } = await api(endpoint, { method: action === "apply" ? "POST" : "DELETE" });
    if (!commitJobAction(job, actionToken)) return;
    if (jobNeedsPolling(job)) pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message || "生成失败，请检查所选片段后重试。", "error");
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function renderConversation(job) {
  const messagesEl = ensureChatMessages();
  // Scope event binding to the DOM root that was just rendered. Opening a
  // home task can overlap with a conversation refresh; querying the global
  // selector again during that handoff could return null and abort rendering.
  const chatRoot = messagesEl;
  const hadConversation = Boolean(chatRoot.querySelector("[data-conversation-key], .chat-message, .content-search-review"));
  const distanceFromBottom = chatRoot.scrollHeight - chatRoot.scrollTop - chatRoot.clientHeight;
  const keepPinnedToBottom = !hadConversation || distanceFromBottom < 56;
  const anchorNode = [...chatRoot.querySelectorAll("[data-conversation-key]")]
    .find((node) => node.getBoundingClientRect().bottom >= chatRoot.getBoundingClientRect().top + 1);
  const scrollAnchor = anchorNode
    ? { key: anchorNode.dataset.conversationKey, offset: anchorNode.getBoundingClientRect().top - chatRoot.getBoundingClientRect().top }
    : null;
  const previousScrollTop = chatRoot.scrollTop;
  let stageHost = $("#chatStageHost");
  if (!stageHost) {
    stageHost = document.createElement("div");
    stageHost.id = "chatStageHost";
    stageHost.className = "chat-stage-host review-stage-host";
    stageHost.setAttribute("aria-live", "polite");
    ($("#reviewWorkbench") || $("#reviewView") || document.body).append(stageHost);
  }
  // Preserve the live progress node while replacing conversation markup. It
  // may currently be a direct child of #chatMessages (during analysis), so
  // innerHTML would otherwise detach it and the next poll could no longer
  // find #jobStatus.
  const preservedAnalysisConsole = document.getElementById("jobStatus");
  const messages = visibleConversationMessages(window.ClipTalkAgentWorkspace?.conversationMessages?.(job) || job.messages || [], job);
  const currentMessages = messages.filter((message) => !message.inherited && !isAutoCompositionActivityMessage(message, job));
  const currentUserInstructions = new Set(currentMessages
    .filter((message) => message.role === "user")
    .map((message) => normalizedConversationInstruction(message.text))
    .filter(Boolean));
  const inheritedMessages = messages.filter((message) => Boolean(message.inherited)
    && !(message.role === "user" && currentUserInstructions.has(normalizedConversationInstruction(message.text))));
  const assistantRoleLabel = workflowAssistantLabel(job);
  const contentMode = taskModePresentation(job).key === "content_extract";
  // Older jobs were created before pendingSelectionGroupIds was persisted.
  // Infer the latest manual timeline group from the confirmation notice so a
  // refresh still shows only the clips the user just selected.
  const manualGroups = (job.eventGroups || []).filter((group) => group.assemblyStrategy === "manual");
  const latestSelectionNotice = [...messages].reverse().find((message) => message.role === "assistant" && /已准备好你选中的多个时间轴片段|已找到相同的时间轴选区/.test(String(message.text || "")));
  const inferredPendingSelectionGroupIds = job.pendingSelectionGroupIds?.length
    ? job.pendingSelectionGroupIds.map(String)
    : (latestSelectionNotice && manualGroups.length
      ? [String(manualGroups[manualGroups.length - 1].id)]
      : []);
  let eventTimelineSummary = null;
  // Keep inherited parent-task messages out of the active chat stream. They
  // are useful for audit/debugging, but showing "previous task dialogue" above
  // the current user request makes the new task look contaminated by the old
  // one. Current task messages and retained result cards remain visible.
  void inheritedMessages;
  let html = (contentMode
    ? conversationTimelineMarkup(job, currentMessages, assistantRoleLabel)
    : currentMessages.map((message) => conversationMessageMarkup(message, assistantRoleLabel)).join(""));
  html += editProposalMarkup(job.pendingEditProposal);
  if (job.status === "brief_confirmation") {
    const brief = job.brief || {};
    html += `<article class="chat-message assistant brief-confirmation-message"><span class="avatar">AI</span><div class="brief-wrap"><div class="bubble"><small>剪辑需求待确认</small><p>我先把你的要求整理成下面这份简报。检查并修改后，确认开始分析。</p></div>${briefEditorMarkup(brief, "chat")}</div></article>`;
  } else if (!contentMode && job.status === "awaiting_confirmation" && job.eventGroups?.length) {
    const pendingSelection = inferredPendingSelectionGroupIds.length > 0;
    const eventGroupsForReview = pendingSelection
      ? job.eventGroups.filter((group) => inferredPendingSelectionGroupIds.includes(String(group.id)))
      : job.eventGroups.filter((group) => group.assemblyStrategy !== "manual");
    const recommended = new Set(pendingSelection ? inferredPendingSelectionGroupIds : (job.recommendedGroupIds || []));
    const target = Number(job.totalTargetSeconds || job.request?.totalTargetSeconds || 0);
    const visibleGroupIds = new Set(eventGroupsForReview.map((group) => String(group.id)));
    const visibleRecommended = [...recommended].filter((id) => visibleGroupIds.has(String(id)));
    const timelineSegmentCount = eventGroupsForReview.reduce((sum, group) => sum + (group.segments || []).length, 0);
    const groupedCandidateCount = new Set(eventGroupsForReview.flatMap((group) =>
      (group.availableSegments || group.segments || []).map((segment) => String(segment.candidateIndex ?? segment.id))
    )).size;
    const refinedCandidateCount = Number(job.candidates?.length || 0) || groupedCandidateCount || timelineSegmentCount;
    const currentTimelineGroups = visibleRecommended
      .map((id) => eventGroupsForReview.find((group) => String(group.id) === String(id)))
      .filter(Boolean);
    const currentTimelineSegments = currentTimelineGroups.flatMap((group) => group.segments || []);
    const generatedVersions = jobOutputVersions(job).filter((version) => (version.outputs || []).length);
    const generatedOutputs = generatedVersions.flatMap((version) => version.outputs || []);
    const timelineEditedSinceOutput = timelineEditedAfterLatestOutput(job, generatedVersions);
    const timelineHasMatchingOutput = generatedOutputs.some((output) => timelineSegmentsMatch(currentTimelineSegments, output.segments || []));
    const timelineHasUnrenderedChanges = !pendingSelection
      && currentTimelineSegments.length > 0
      && generatedOutputs.length > 0
      && timelineEditedSinceOutput
      && !timelineHasMatchingOutput;
    const allocated = pendingSelection
      ? eventGroupsForReview.reduce((sum, group) => sum + (group.segments || []).reduce((inner, segment) => inner + Number(segment.duration || (Number(segment.end) - Number(segment.start)) || 0), 0), 0)
      : currentTimelineSegments.reduce((sum, segment) => sum + Number(segment.duration || (Number(segment.end) - Number(segment.start)) || 0), 0);
    const targetTolerance = target ? Math.max(4, target * Number(job.durationTolerance || .1)) : 0;
    const timelineOverTarget = Boolean(target && allocated > target + targetTolerance);
    const timelineHasOptionalFullVersion = !pendingSelection
      && currentTimelineSegments.length > 0
      && generatedOutputs.length > 0
      && !timelineEditedSinceOutput
      && !timelineHasMatchingOutput
      && timelineOverTarget;
    const eventAutoStatus = String(job.autoComposition?.status || "");
    const eventAutoGenerating = ["queued", "running"].includes(eventAutoStatus);
    const timelineDescription = pendingSelection
      ? "这些是你刚从时间轴选中的片段。可以逐个预览并调整顺序，确认无误后直接合成；不会自动带入其他事件。"
      : eventAutoGenerating
        ? `当前时间轴展示 ${timelineSegmentCount} 个事件镜头；系统已选择 ${visibleRecommended.length} 个事件，正在后台自动生成审核样片。生成完成后会出现在播放器和版本菜单中。`
      : timelineHasUnrenderedChanges
        ? `时间轴已修改；现有 ${generatedVersions.length} 个成片版本仍基于修改前的方案，生成新版本后才会得到当前 ${allocated.toFixed(1)} 秒内容。`
        : timelineHasOptionalFullVersion
          ? (timelineOverTarget
            ? `现有 ${generatedVersions.length} 个成片版本已按 ${target.toFixed(1)} 秒目标取舍并重编排；上方 ${allocated.toFixed(1)} 秒是完整推荐事件池，并非尚未完成的成片。`
            : `现有 ${generatedVersions.length} 个成片版本已根据推荐事件完成重编排；如需逐段完全一致的 ${allocated.toFixed(1)} 秒版本，可另行生成。`)
          : `当前时间轴展示 ${timelineSegmentCount} 个事件镜头；系统已选择 ${visibleRecommended.length} 个事件用于成片。可点击时间轴预览，满意后直接生成。`;
    const timelineRenderNotice = timelineHasUnrenderedChanges
      ? `<div class="timeline-render-pending"><span><strong>当前修改尚未生成</strong><small>已有版本不会被覆盖，将新增一个完整对应当前时间轴的版本</small></span><button type="button" data-render-current-timeline>生成当前时间轴版本</button></div>`
      : timelineHasOptionalFullVersion
        ? `<div class="timeline-render-pending optional"><span><strong>${timelineOverTarget ? "可选：全部推荐事件版" : "可选：精确时间轴版"}</strong><small>${timelineOverTarget ? `将新增 ${allocated.toFixed(1)} 秒版本，比目标长 ${(allocated - target).toFixed(1)} 秒；现有目标时长版本不会被覆盖` : "将新增一个与推荐时间轴逐段一致的版本；现有版本不会被覆盖"}</small></span><button type="button" data-render-current-timeline>${timelineOverTarget ? "生成全部推荐事件版" : "生成精确时间轴版"}</button></div>`
        : "";
    eventTimelineSummary = {
      allocated,
      description: timelineDescription,
      generatedVersionCount: generatedVersions.length,
      groupIds: currentTimelineGroups.map((group) => group.id),
      pendingSelection,
      timelineEditedSinceOutput,
      timelineHasOptionalFullVersion,
      timelineHasUnrenderedChanges,
      timelineOverTarget,
      timelineSegmentCount,
      target,
      visibleRecommendedCount: visibleRecommended.length,
    };
    const budgetClass = target && Math.abs(allocated - target) > target * Number(job.durationTolerance || .1) ? " over" : "";
    html += `<article class="chat-message assistant recommendation-message"><span class="avatar">AI</span><div class="recommendation-wrap">
      <section class="recommendation-card event-recommendation">
        <header><div><small>${pendingSelection ? "已选时间轴片段" : "候选镜头与事件归组"}</small><strong>${pendingSelection ? `已准备 ${timelineSegmentCount} 个镜头` : `找到 ${refinedCandidateCount} 个候选镜头 · 归并为 ${eventGroupsForReview.length} 个事件`}</strong></div><b>${pendingSelection ? "待确认顺序" : `推荐 ${visibleRecommended.length} 个事件`}</b></header>
        <div class="duration-budget${budgetClass}"><span><b>${pendingSelection ? "已选片段" : timelineEditedSinceOutput ? "当前时间轴" : timelineOverTarget ? "完整推荐事件池" : "推荐时间轴"} ${allocated.toFixed(1)} 秒</b>${target ? ` / 单条目标 ${target.toFixed(1)} 秒` : " · AI 推荐"}</span><i><b style="width:${target ? Math.min(100, allocated / target * 100) : 100}%"></b></i></div>
        <p>${timelineDescription}</p>
        ${eventAutoGenerating ? "" : timelineRenderNotice}<div class="review-selection-summary" data-selection-summary>正在计算选择结果…</div>
        <div class="event-group-list">${eventGroupsForReview.map((group, groupIndex) => `<article class="event-group-row${recommended.has(group.id) ? " recommended" : ""}" data-event-group="${escapeHtml(group.id)}">
          <header><input class="event-group-check" type="checkbox" value="${escapeHtml(group.id)}" ${recommended.has(group.id) ? "checked" : ""}><span><strong>${escapeHtml(group.title)}</strong><small>${group.segments.length} 个镜头 · ${Number(group.actualDuration).toFixed(1)} 秒</small></span><b>${Math.round(group.score)}</b><button type="button" class="add-selection-event" ${job.manualSelection ? "" : "disabled"}>加选区</button><button type="button" class="rename-event">命名</button><button type="button" class="preview-event">组合预览</button></header>
          <p>${escapeHtml(group.summary)}</p>
          <details ${groupIndex === 0 ? "open" : ""}><summary>展开事件镜头</summary><div class="event-segments">${group.segments.map((segment, segmentIndex) => `<div class="event-segment" data-segment-id="${escapeHtml(segment.id)}"><span><b>${segmentIndex + 1}. ${escapeHtml(segment.role)}</b><small>${formatTime(segment.start)} → ${formatTime(segment.end)} · ${Number(segment.duration).toFixed(1)} 秒 · ${segment.transitionIn?.type === "dissolve" ? "短叠化" : "硬切"}</small></span><button type="button" class="preview-segment">看</button><button type="button" class="move-segment-up" ${segmentIndex === 0 ? "disabled" : ""}>↑</button><button type="button" class="move-segment-down" ${segmentIndex === group.segments.length - 1 ? "disabled" : ""}>↓</button><button type="button" class="move-segment-group">移</button><button type="button" class="delete-segment">删</button></div>`).join("")}</div></details>
        </article>`).join("")}</div>
        <div class="recommendation-actions">${eventAutoGenerating ? '<span class="auto-compose-inline-status">自动成片进行中，完成后会生成可预览版本</span>' : `<button type="button" class="confirm-event-groups primary">将已选事件合成 1 条</button><button type="button" class="confirm-all-events">将全部事件合成 1 条</button><button type="button" class="export-event-groups">分别导出已选事件</button>${target ? `<button type="button" class="rebalance-budget">调整到约 ${target.toFixed(0)} 秒</button>` : ""}<button type="button" class="create-event-from-selection" ${job.manualSelection ? "" : "disabled"}>用选区新建事件</button>`}</div>
      </section></div></article>`;
  } else if (job.status === "awaiting_confirmation" && job.candidates?.length) {
    const recommended = new Set(job.recommendedIndices || []);
    const reviewCandidates = sortedCandidatesForReview(job);
    html += `<article class="chat-message assistant recommendation-message"><span class="avatar">AI</span><div class="recommendation-wrap">
      <section class="recommendation-card">
        <header><div><small>智能推荐</small><strong>发现 ${job.candidates.length} 个有效候选</strong></div><b>推荐 ${recommended.size} 条</b></header>
        <p>${job.analysisCacheHit ? "已复用相同视频和要求的分析缓存，无需重复调用模型。" : "每条时长来自各自视觉事件边界。"} 已默认勾选综合评分较高的推荐项，你可以调整选择后再裁剪。</p><div class="review-selection-summary" data-selection-summary>正在计算选择结果…</div>
    <div class="candidate-review-toolbar"><span>审核排序</span><button type="button" data-candidate-sort="score" class="${candidateReviewSort === "score" ? "active" : ""}">评分</button><button type="button" data-candidate-sort="time" class="${candidateReviewSort === "time" ? "active" : ""}">时间</button><button type="button" data-candidate-sort="review" class="${candidateReviewSort === "review" ? "active" : ""}">优先复核</button><em>N/P 切换高光 · R 排除 · Enter 确认</em></div>
        <div class="candidate-list">${reviewCandidates.map((candidate) => { const selected = recommended.has(candidate.index) && !locallyExcludedCandidates.has(Number(candidate.index)); return `<div class="candidate-row${selected ? " recommended" : ""}${locallyExcludedCandidates.has(Number(candidate.index)) ? " excluded" : ""}" data-candidate-row="${candidate.index}"><label><input type="checkbox" value="${candidate.index}" ${selected ? "checked" : ""}><span><strong>${escapeHtml(candidate.title)}</strong><small>${formatTime(candidate.start)} → ${formatTime(candidate.end)} · ${Number(candidate.duration).toFixed(1)} 秒${candidateReviewLabels(candidate).length ? ` · ${candidateReviewLabels(candidate).join(" · ")}` : ""}</small></span><b>${Math.round(candidate.score)}</b></label><button type="button" class="rename-candidate" data-candidate-index="${candidate.index}">命名</button><button type="button" class="candidate-menu" data-candidate-index="${candidate.index}">操作</button><button type="button" class="preview-candidate" data-candidate-index="${candidate.index}">预览</button></div>`; }).join("")}</div>
        <div class="recommendation-actions"><button type="button" class="confirm-selected primary">生成所选片段</button><button type="button" class="confirm-all">全部生成</button>${job.candidates.length > 3 ? '<button type="button" class="confirm-top3">只生成评分前 3</button>' : ""}</div>
      </section>
    </div></article>`;
  } else if (job.status === "awaiting_confirmation" && job.manualSelection) {
    const selection = job.manualSelection;
    html += `<article class="chat-message assistant recommendation-message"><span class="avatar">AI</span><div class="recommendation-wrap"><section class="recommendation-card manual-selection-card"><header><div><small>手动时间段</small><strong>${formatTime(selection.start)} → ${formatTime(selection.end)}</strong></div><b>${Number(selection.duration || selection.end - selection.start).toFixed(1)} 秒</b></header><p>这是你从源视频时间轴直接选中的范围，不依赖 AI 高光候选，可以直接合成为成片。</p><div class="recommendation-actions"><button type="button" class="confirm-manual-selection primary">合成这个时间段</button></div></section></div></article>`;
  }
  const hasGeneratedOutputs = Boolean((job.outputs || []).length || (job.outputVersions || []).some((version) => (version.outputs || []).length));
  const execution = executionForJob(job);
  const autoCompositionComplete = job.autoComposition?.status === "completed";
  const autoCompositionRunning = execution.active
    && ["auto_composition", "quality_review"].includes(execution.operation);
  if (job.autoComposition?.status === "partial" && hasGeneratedOutputs) {
    const facts = autoCompositionVersionFacts(job);
    html += `<article class="chat-message assistant auto-compose-partial-message"><span class="avatar">AI</span><div class="recommendation-wrap"><section class="auto-compose-partial"><small>自动成片 · 部分完成</small><strong>已生成 ${facts.completed} 个可播放版本</strong><p>其他剪辑版本生成失败，已有成片仍可正常预览和下载。${job.autoComposition.error ? ` 原因：${escapeHtml(job.autoComposition.error)}` : ""}</p></section></div></article>`;
  }
  if (execution.outcome === "no_acceptable_output") {
    const rejected = Math.max(0, Number(execution.result?.qualityRejectedCount) || 0);
    const manualDraft = Boolean(job.autoComposition?.manualReviewRequired);
    const reasons = (execution.result?.qualityReasons || []).filter(Boolean).slice(0, 4);
    const issues = (execution.result?.qualityIssues || []).filter((item) => item?.description).slice(0, 4);
    const repair = execution.result?.qualityRepair || {};
    const localRepairAttempts = Math.max(0, Number(repair.localRepairAttempts) || 0);
    const repairAttemptLabel = localRepairAttempts
      ? `${localRepairAttempts} 轮局部修复`
      : "可执行的局部修复";
    const repairCopy = repair.status === "insufficient_evidence"
      ? `系统已尝试${repairAttemptLabel}、问题镜头补检和重新编排；${repair.detail || "源素材仍缺少可验证的完整动作或结果"}。`
      : repair.status === "not_improved"
      ? "系统已自动返修并复审，但返修版没有比原版更好，因此仅标记为需人工复核。"
      : repair.status === "blocked"
        ? `返修方案在渲染前被预检拦截：${repair.detail || "会偏离目标时长或剪辑硬约束"}。`
        : repair.status === "failed"
          ? `系统尝试返修，但${repair.detail || "返修或复审没有完成"}。`
          : repair.status === "skipped"
            ? `系统没有执行返修：${repair.detail || "没有可安全自动修改的镜头"}。`
            : "";
    const issueMarkup = issues.length
      ? `<ol class="quality-issue-list">${issues.map((issue) => {
          const severity = String(issue.severity || "major");
          const severityLabel = severity === "critical" ? "关键" : severity === "major" ? "主要" : "次要";
          const time = Number(issue.outputTime);
          const timeLabel = Number.isFinite(time) && time >= 0 ? `成片 ${formatTime(time)}` : "涉及所选镜头";
          const duplicate = Number(issue.duplicateCount || 1) > 1 ? ` · 已合并 ${Number(issue.duplicateCount)} 条重复诊断` : "";
          const matches = orderedJobOutputs(job).filter(({ item, version }) => {
            const report = item.reviewReport || version?.reviewReport || {};
            const recorded = [...(report.issues || []), ...(item.qualityGate?.issues || []), ...(version?.qualityGate?.issues || [])];
            return recorded.some((entry) => entry.description === issue.description && Number(entry.outputTime) === time);
          });
          const location = matches.length === 1 && Number.isFinite(time) && time >= 0
            ? `<button type="button" data-quality-output="${escapeHtml(matches[0].item.filename)}" data-quality-time="${time}">定位问题片段</button>`
            : '<small>请在对应版本的审片详情中核对时间</small>';
          return `<li><span class="${escapeHtml(severity)}">${severityLabel}</span><div><small>${escapeHtml(timeLabel + duplicate)}</small><p>${escapeHtml(issue.description)}</p>${location}</div></li>`;
        }).join("")}</ol>`
      : (reasons.length ? `<ul>${reasons.map((reason) => `<li>${escapeHtml(reason)}</li>`).join("")}</ul>` : "");
    const recoveryAction = repair.status === "insufficient_evidence"
      ? ""
      : '<button type="button" class="primary" data-replan-after-quality>重新规划剪辑方案</button>';
    const qualityHeading = manualDraft
      ? `${rejected || 1} 个独立样片未达到自动推荐标准`
      : (rejected ? `${rejected} 个独立样片均未达到展示标准` : "本轮没有可展示的自动样片");
    const qualityDescription = manualDraft
      ? "所有已成功渲染的独立方案都已保留。未通过版本会明确标记为“需人工复核”，不会自动推荐或导出，但仍可逐个预览比较。"
      : "独立方案会分别审片；返修版不计入方案数量。只有无法播放或渲染失败的样片会撤回，其他版本均保留供比较。";
    html += `<article class="chat-message assistant quality-gate-result-message"><span class="avatar">AI</span><div class="recommendation-wrap"><section class="quality-gate-result-card"><small>自动成片 · 检查完成</small><strong>${qualityHeading}</strong><p>${qualityDescription}</p>${issueMarkup}${repairCopy ? `<p class="quality-repair-state">${escapeHtml(repairCopy)}</p>` : ""}${reasons.length && issues.length ? `<details><summary>查看检查详情</summary><ul>${reasons.map((reason) => `<li>${escapeHtml(reason)}</li>`).join("")}</ul></details>` : ""}<div><button type="button" data-review-events-after-quality>返回事件审核</button>${recoveryAction}</div></section></div></article>`;
  }
  if (contentMode && hasGeneratedOutputs && !job.pendingSelectionGroupIds?.length && !contentSearchRecordsForJob(job).length) html += contentOutputResultMarkup(job);
  if (!contentMode && ["completed", "awaiting_confirmation"].includes(job.status) && hasGeneratedOutputs && !job.pendingSelectionGroupIds?.length && !autoCompositionComplete) {
    const quickActions = [
      autoCompositionRunning ? "" : `<button type="button" data-reedit-job>返回事件审核</button>`,
      job.status === "completed" ? `<button type="button" data-alternative-job>换一种剪法</button>`
        : "",
    ].filter(Boolean).join("");
    html += quickActions ? `<div class="quick-actions">${quickActions}</div>` : "";
    if (job.status === "completed") {
      html += '<section class="completed-dialog-actions"><strong>需要调整这条成片？</strong><p>可以直接换一种剪法；只有需要逐镜头调整时再打开时间线。</p><div><button type="button" data-alternative-job>换一种剪法</button><button type="button" data-toggle-precision-workspace>查看时间线</button><button type="button" data-reedit-job>重新选择镜头</button></div></section>';
    }
  }
  if (autoCompositionComplete && hasGeneratedOutputs) {
    const automaticKeys = new Set(["vlm", "narrative", "emotion", "information", "review_repair"]);
    const allGeneratedVersions = jobOutputVersions(job).filter((version) => (version.outputs || []).length);
    const automaticGeneratedVersions = allGeneratedVersions.filter((version) =>
      version.generationBatchId || version.previewOnly || automaticKeys.has(String(version.strategyKey || ""))
    );
    const generatedVersions = (automaticGeneratedVersions.length ? automaticGeneratedVersions : allGeneratedVersions)
      .sort((left, right) => Number(left.number || 0) - Number(right.number || 0));
    const declaredBatches = Array.isArray(job.autoComposition?.batches) ? job.autoComposition.batches : [];
    const batchIds = [...new Set(generatedVersions.map((version) => String(version.generationBatchId || "legacy")).filter(Boolean))];
    const batches = batchIds.map((batchId, index) => {
      const batch = declaredBatches.find((item) => String(item.id || "") === batchId) || { id: batchId, mode: "legacy" };
      const members = generatedVersions.filter((version) => String(version.generationBatchId || "legacy") === batchId);
      const independent = members.filter((version) => String(version.variantKind || (version.previewOnly ? "independent" : "formal_export")) === "independent");
      const previewCount = members.filter((version) => version.previewOnly).length;
      const replanTarget = Number(batch.targetSeconds);
      const standardSingleBatch = batchIds.length === 1 && !["replan", "recovery", "legacy"].includes(String(batch.mode || ""));
      const heading = standardSingleBatch
        ? "本批样片"
        : batch.mode === "replan"
        ? `${Number.isFinite(replanTarget) && replanTarget > 0 ? `${replanTarget.toFixed(0)} 秒` : "高光"}重排批次 ${index + 1}`
        : batch.mode === "recovery" ? `补充批次 ${index + 1}` : batch.mode === "legacy" ? "历史自动成片" : `自动成片批次 ${index + 1}`;
      const evidenceLabel = batch.evidenceSource === "existing_analysis" ? " · 复用已有分析" : "";
      const passedCount = members.filter((version) => outputVersionQuality(version, version.outputs?.[0]) === "passed").length;
      const reviewCount = members.length - passedCount;
      const stats = `${independent.length} 个独立方案 · ${passedCount} 个通过${reviewCount ? ` · ${reviewCount} 个需复核` : ""}${evidenceLabel}`;
      return `<section class="auto-version-batch compact"><header><div><strong>${escapeHtml(heading)}</strong>${standardSingleBatch ? "" : `<small>${escapeHtml(stats)}</small>`}</div>${previewCount && batchId !== "legacy" ? `<button type="button" data-delete-auto-batch="${escapeHtml(batchId)}">删除本批样片</button>` : ""}</header></section>`;
    });
    const hasUnrenderedTimeline = Boolean(eventTimelineSummary?.timelineHasUnrenderedChanges);
    const hasOptionalFullTimeline = Boolean(eventTimelineSummary?.timelineHasOptionalFullVersion);
    const duplicatePlansSkipped = Math.max(0, Number(job.autoComposition?.duplicatePlansSkipped) || 0);
    const duplicatePlansReplaced = Math.max(0, Number(job.autoComposition?.duplicatePlansReplaced) || 0);
    const dedupeDescription = duplicatePlansReplaced
      ? `检测到 ${duplicatePlansReplaced} 个方案与已有成片重复，已自动改用其他高分事件。`
      : duplicatePlansSkipped
        ? `AI 原计划生成 ${generatedVersions.length + duplicatePlansSkipped} 个版本，其中 ${duplicatePlansSkipped} 个与已有成片重复，已自动合并。`
      : "";
    const passedVersionCount = generatedVersions.filter((version) => outputVersionQuality(version, version.outputs?.[0]) === "passed").length;
    const reviewVersionCount = generatedVersions.length - passedVersionCount;
    const resultTitle = hasUnrenderedTimeline
      ? "自动成片已完成 · 当前时间轴有修改"
      : "自动成片已完成";
    const resultDescription = hasUnrenderedTimeline
      ? `这些版本基于修改前的时间轴，不包含当前 ${eventTimelineSummary.allocated.toFixed(1)} 秒调整；请先生成当前时间轴版本。${dedupeDescription ? ` ${dedupeDescription}` : ""} 历史版本详情仍可在右侧“成片版本”查看。`
      : hasOptionalFullTimeline
        ? `${dedupeDescription ? `${dedupeDescription} ` : ""}${eventTimelineSummary.target ? `AI 已按 ${Number(eventTimelineSummary.target).toFixed(0)} 秒目标取舍并重编排事件镜头` : "AI 已根据推荐事件重编排镜头"}，现有版本不是未完成任务。${eventTimelineSummary.timelineOverTarget ? `如需包含完整 ${eventTimelineSummary.allocated.toFixed(1)} 秒推荐事件，可在上方另行生成。` : "如需与推荐时间轴逐段完全一致，可在上方另行生成。"} 请在右侧“成片版本”预览比较。`
        : `${dedupeDescription || "AI 已根据画面、声音、对白和情绪线索生成多种剪辑版本。"} 版本详情、质检状态和预览入口已集中到右侧“成片版本”。`;
    const recovery = job.autoComposition?.recovery || {};
    const recoveryAction = recovery.canRegenerateMissingVariants
      ? `<button type="button" class="primary" data-regenerate-auto-variants data-target-count="${Number(recovery.targetVariantCount || 3)}">补生成缺少的 ${Number(recovery.missingVariantCount || 0)} 个版本</button>` : "";
    const executionArchive = autoCompositionArchiveMarkup(job);
    html += `<section class="auto-compose-result-card compact${hasUnrenderedTimeline ? " timeline-stale" : ""}"><strong>${resultTitle}</strong><p>${resultDescription}</p><div class="auto-compose-result-summary"><span><b>${generatedVersions.length}</b> 个版本</span><span class="passed"><b>${passedVersionCount}</b> 个通过</span>${reviewVersionCount ? `<span class="review"><b>${reviewVersionCount}</b> 个需复核</span>` : ""}</div><div class="auto-version-batches compact">${batches.join("")}</div>${executionArchive}<div class="auto-compose-actions"><button type="button" class="primary" data-open-output-versions>查看成片版本</button>${recoveryAction}</div></section>`;
  }
  if (["cancelled", "failed"].includes(job.status)) {
    const sourceIncomplete = job.failureCode === "source_incomplete";
    const workflowKey = workflowKindForJob(job);
    const retryFacts = {
      highlight: { name: "高光分析", detail: "保留剪辑要求和已有成片，从中断的高光分析步骤恢复。" },
      content_search: { name: "内容检索", detail: "保留检索条件、历史结果和可复用索引，从中断的检索步骤恢复。" },
      person_edit: { name: "人物扫描", detail: "保留人物卡、已选人物和可复用轨迹，从中断的人物步骤恢复。" },
      speaker_edit: { name: "说话人识别", detail: "保留声音标签和已有试听结果，从中断的声音识别步骤恢复。" },
    }[workflowKey] || { name: "当前分析", detail: "保留已有结果，从中断步骤恢复。" };
    html += `<section class="retry-analysis-card" data-border-beam data-beam-size="pulse-inner" data-beam-color="sunset" data-beam-theme="dark" data-beam-strength="0.52" data-beam-duration="2.5" data-beam-brightness="1.18" data-beam-saturation="1" data-beam-hue-range="14" data-beam-radius="10"><strong>${sourceIncomplete ? "源视频文件不完整" : job.status === "cancelled" ? `${retryFacts.name}已停止` : `${retryFacts.name}未完成`}</strong><p>${sourceIncomplete ? "当前文件无法覆盖完整时间轴，不能继续复用。请返回全部任务并重新上传完整的原始视频。" : retryFacts.detail}</p><button type="button" class="${sourceIncomplete ? "return-home-from-failure" : "reanalyze-job"}">${sourceIncomplete ? "返回并重新上传" : `↻ 恢复${retryFacts.name}`}</button></section>`;
  }
  if (analysisConsoleVisible(job)) html += inlineAnalysisProgressMarkup(job);
  if ((workflowKindForJob(job) === "speaker_edit" && !job.contentSearch?.id) || job.narratorSelectionPending?.active) {
    html += `<article class="chat-message assistant inline-speaker-message" data-conversation-key="speaker-discovery"><span class="avatar">AI</span><section id="inlineCurrentVoices" class="inline-current-voices" aria-live="polite"></section></article>`;
  }
  // The inline progress card already communicates the current stage while a
  // pipeline is active. Avoid rendering a second, lower-priority thinking
  // bubble underneath it.
  if (!analysisConsoleVisible(job)) html += thinkingMessageMarkup(thinkingConfigForJob(job), job);
  // Keep the stage host node stable. Replacing it on every poll invalidated
  // listeners and briefly left moved rail nodes detached from the document.
  messagesEl.innerHTML = html;
  if (preservedAnalysisConsole && preservedAnalysisConsole.parentElement !== messagesEl) {
    messagesEl.append(preservedAnalysisConsole);
  }
  // This progress surface is rebuilt from streamed job state. Mount its
  // activity loader synchronously instead of waiting for a DOM observer.
  syncGenerativeLoaders(messagesEl);
  syncOutputVersionBeams(job, messagesEl);
  syncBorderBeams(messagesEl);
  renderInlineCurrentVoices(job.voiceDiscovery || { status: "not_started" });
  renderAutoCompositionDock(job);
  messagesEl.querySelector("[data-inline-cancel]")?.addEventListener("click", cancelCurrentJob);
  messagesEl.querySelectorAll("[data-quality-output]").forEach((button) => button.addEventListener("click", () => {
    selectOutput(button.dataset.qualityOutput, true, Number(button.dataset.qualityTime));
  }));
  messagesEl.querySelector("[data-review-events-after-quality]")?.addEventListener("click", () => {
    setDirectorStage("events");
    messagesEl.querySelector(".event-recommendation")?.scrollIntoView({ behavior: "smooth", block: "start" });
  });
  messagesEl.querySelector("[data-replan-after-quality]")?.addEventListener("click", () => {
    const groupIds = (job.recommendedGroupIds || []).map(String).filter(Boolean);
    if (!groupIds.length) return void showToast("当前没有可重新规划的推荐事件，请先在事件审核中选择镜头。", "error");
    requestAutoPlans(job, "all_pool", 1, groupIds, true);
  });
  messagesEl.querySelector("[data-apply-edit-proposal]")?.addEventListener("click", () => resolveEditProposal("apply"));
  messagesEl.querySelector("[data-modify-edit-proposal]")?.addEventListener("click", () => resolveEditProposal("modify"));
  messagesEl.querySelector("[data-cancel-edit-proposal]")?.addEventListener("click", () => resolveEditProposal("cancel"));
  updateInlineAnalysisProgress(job);
  messagesEl.querySelectorAll(".content-search-review").forEach((contentSearchRoot) => {
    const scopedJob = contentSearchJobForRoot(contentSearchRoot, job);
    const searchId = String(scopedJob.contentSearch?.id || "");
    const findMatch = (id) => (scopedJob.contentSearch?.candidates || []).find((item) => String(item.id) === String(id));
    const clearDuplicateNotice = () => {
      if (String(duplicateContentCompositionNotice?.searchId || "") === searchId) duplicateContentCompositionNotice = null;
    };
    contentSearchRoot.addEventListener("change", clearDuplicateNotice);
    contentSearchRoot.addEventListener("input", clearDuplicateNotice);
    contentSearchRoot.querySelector("[data-return-person-selection]")?.addEventListener("click", () => {
      setDirectorStage("events");
      openPersonProfiles();
    });
    contentSearchRoot.querySelectorAll("[data-content-preview]").forEach((button) => button.addEventListener("click", async () => {
      const requestedJobId = String(job.id || "");
      let detailed;
      try {
        detailed = await ensureContentSearchDetails(searchId, job);
      } catch (error) {
        if (String(currentJob?.id || "") !== requestedJobId || !button.isConnected) return;
        showToast(`读取检索详情失败：${error.message}`);
        detailed = scopedJob.contentSearch;
      }
      if (String(currentJob?.id || "") !== requestedJobId || !button.isConnected) return;
      previewContentMatch(
        (detailed?.candidates || []).find((item) => String(item.id) === String(button.dataset.contentPreview)),
        { searchId },
      );
    }));
    contentSearchRoot.querySelectorAll("[data-content-evidence]").forEach((button) => {
      const openEvidence = async (event) => {
        if (event.type === "keydown" && !["Enter", " "].includes(event.key)) return;
        event.preventDefault();
        event.stopPropagation();
        const requestedJobId = String(job.id || "");
        let detailed;
        try {
          detailed = await ensureContentSearchDetails(searchId, job);
        } catch (error) {
          if (String(currentJob?.id || "") !== requestedJobId || !button.isConnected) return;
          showToast(`读取检索详情失败：${error.message}`);
          detailed = scopedJob.contentSearch;
        }
        if (String(currentJob?.id || "") !== requestedJobId || !button.isConnected) return;
        previewContentEvidence(
          (detailed?.candidates || []).find((item) => String(item.id) === String(button.dataset.contentEvidence)),
          { autoplay: false, searchId },
        );
      };
      button.addEventListener("click", openEvidence);
      button.addEventListener("keydown", openEvidence);
    });
    wireContentBoundaryEditors(contentSearchRoot, scopedJob);
    contentSearchRoot.querySelector("[data-content-output-mode]")?.addEventListener("change", () => {
      syncContentSearchOutputControls(contentSearchRoot, scopedJob);
      scheduleContentReviewDraftSave(contentSearchRoot, scopedJob);
    });
    const questionSourceSelect = contentSearchRoot.querySelector("[data-content-question-source]");
    const applyQuestionSourceFilter = () => {
      const source = String(questionSourceSelect?.value || "all");
      contentSearchFilterState.set(`${job?.id || ""}:${searchId}`, { questionSource: source });
      contentSearchRoot.querySelectorAll("[data-content-match-row]").forEach((row) => {
        row.hidden = source !== "all" && String(row.dataset.contentQuestionSource || "other") !== source;
      });
    };
    questionSourceSelect?.addEventListener("change", applyQuestionSourceFilter);
    if (questionSourceSelect) applyQuestionSourceFilter();
    contentSearchRoot.querySelector("[data-content-dialogue-mode]")?.addEventListener("change", async (event) => {
      const select = event.currentTarget;
      const requestedJobId = String(job.id || "");
      const previous = select.dataset.previousValue || select.value;
      select.dataset.previousValue = select.value;
      select.disabled = true;
      try {
        const response = await apiJson(`/api/jobs/${encodeURIComponent(job.id)}/content-search/dialogue-mode`, {
          method: "PATCH",
          headers: { "Content-Type": "application/json" },
          body: { searchId, dialogueMode: select.value },
        });
        if (response?.job && String(currentJob?.id || "") === requestedJobId && select.isConnected) {
          currentJob = response.job;
          renderJob(currentJob);
        }
      } catch (error) {
        if (String(currentJob?.id || "") !== requestedJobId || !select.isConnected) return;
        select.value = previous;
        showToast(`问答模式切换失败：${error.message}`);
      } finally {
        if (select.isConnected) select.disabled = false;
      }
    });
    contentSearchRoot.querySelector("[data-content-order-mode]")?.addEventListener("change", () => {
      syncContentSearchOutputControls(contentSearchRoot, scopedJob);
      scheduleContentReviewDraftSave(contentSearchRoot, scopedJob);
    });
    contentSearchRoot.querySelector("[data-content-subtitle]")?.addEventListener("change", () => {
      syncContentSearchSubtitleControls(contentSearchRoot, scopedJob);
      scheduleContentReviewDraftSave(contentSearchRoot, scopedJob);
    });
    contentSearchRoot.querySelectorAll("[data-content-match]").forEach((input) => input.addEventListener("change", async () => {
      if (input.dataset.contentReviewStatus === "pending") {
        if (!input.checked) return;
        input.disabled = true;
        const saved = await sendContentSearchFeedback("review_keep", input.value, findMatch(input.value), { skipConfirmation: true, searchId });
        if (!saved && input.isConnected) {
          input.checked = false;
          input.disabled = false;
        }
        if (saved) syncContentBasketAddButton(contentSearchRoot, currentJob, searchId);
        return;
      }
      syncContentSearchSubtitleControls(contentSearchRoot, scopedJob);
      syncContentSearchOutputControls(contentSearchRoot, scopedJob);
      syncContentSearchSelectionSummary(contentSearchRoot, scopedJob);
      syncContentBasketAddButton(contentSearchRoot, currentJob, searchId);
      renderChatContextBar();
      if (String(searchId) === String(currentJob?.contentSearch?.id || "")) scheduleContentReviewDraftSave(contentSearchRoot, scopedJob);
    }));
    syncContentSearchOutputControls(contentSearchRoot, scopedJob);
    syncContentSearchSubtitleControls(contentSearchRoot, scopedJob);
    syncContentBasketAddButton(contentSearchRoot, currentJob, searchId);
    contentSearchRoot.querySelectorAll("[data-content-select]").forEach((button) => button.addEventListener("click", async () => {
      if (button.dataset.contentSelect === "toggle") {
        const next = button.getAttribute("aria-pressed") !== "true";
        const inputs = [...contentSearchRoot.querySelectorAll("[data-content-match]")]
          .filter((input) => !input.disabled);
        inputs.forEach((input) => { input.checked = next; });
        button.setAttribute("aria-pressed", String(next));
        button.textContent = next ? "取消全部" : "选择全部";
        if (next) {
          button.disabled = true;
          const requestedJobId = String(job.id || "");
          try {
            await apiJson(`/api/jobs/${encodeURIComponent(job.id)}/content-search/bulk-keep`, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: { searchId, matchIds: inputs.map((input) => input.value) },
            });
            if (String(currentJob?.id || "") !== requestedJobId || !contentSearchRoot.isConnected) return;
            const liveSearch = contentSearchForRoot(contentSearchRoot, currentJob);
            scopedJob.contentSearch = liveSearch;
            const keptIds = new Set(inputs.map((input) => String(input.value)));
            (liveSearch.candidates || []).forEach((match) => {
              if (!keptIds.has(String(match.id))) return;
              match.reviewStatus = "kept";
              match.requiresReview = false;
              match.selected = true;
            });
            inputs.forEach((input) => {
              if (input.dataset.contentReviewStatus !== "pending") return;
              input.dataset.contentReviewStatus = "kept";
              input.setAttribute("aria-label", `选择${input.getAttribute("aria-label")?.replace(/^勾选并保留/, "") || "片段"}`);
              const row = input.closest("[data-content-match-row]");
              row?.classList.remove("review-pending");
              row?.querySelector(".content-match-title label i")?.remove();
            });
            syncContentSearchSubtitleControls(contentSearchRoot, scopedJob);
            syncContentSearchOutputControls(contentSearchRoot, scopedJob);
            syncContentSearchSelectionSummary(contentSearchRoot, scopedJob);
            syncContentBasketAddButton(contentSearchRoot, currentJob, searchId);
            renderChatContextBar();
          } catch (error) {
            if (String(currentJob?.id || "") !== requestedJobId || !contentSearchRoot.isConnected) return;
            inputs.forEach((input) => { input.checked = false; });
            button.setAttribute("aria-pressed", "false");
            button.textContent = "选择全部";
            showToast(`批量选择失败：${error.message || "服务暂时不可用"}`);
          } finally {
            if (button.isConnected) button.disabled = false;
          }
          return;
        }
      }
      syncContentSearchSubtitleControls(contentSearchRoot, scopedJob);
      syncContentSearchOutputControls(contentSearchRoot, scopedJob);
      syncContentSearchSelectionSummary(contentSearchRoot, scopedJob);
      syncContentBasketAddButton(contentSearchRoot, currentJob, searchId);
      renderChatContextBar();
      if (String(searchId) === String(currentJob?.contentSearch?.id || "")) scheduleContentReviewDraftSave(contentSearchRoot, scopedJob);
    }));
    contentSearchRoot.querySelector("[data-content-show-more]")?.addEventListener("click", (event) => {
      const hidden = [...contentSearchRoot.querySelectorAll(".content-candidate-overflow.hidden")];
      hidden.slice(0, 50).forEach((row) => row.classList.remove("hidden"));
      if (hidden.length <= 50) event.currentTarget.remove();
    });
    contentSearchRoot.querySelector("[data-content-show-possible]")?.addEventListener("click", (event) => {
      const button = event.currentTarget;
      const expanded = button.getAttribute("aria-expanded") !== "true";
      button.setAttribute("aria-expanded", String(expanded));
      contentSearchRoot.querySelectorAll(".content-candidate-possible").forEach((row) => {
        const status = String(row.querySelector("[data-content-match]")?.dataset.contentReviewStatus || "");
        if (status !== "kept") row.classList.toggle("hidden", !expanded);
      });
      const label = button.querySelector("span");
      if (label) label.textContent = expanded ? "收起待试听复核" : "查看待试听复核";
    });
    contentSearchRoot.querySelector("[data-content-basket-add]")?.addEventListener("click", () => {
      addSelectedContentToBasket(contentSearchRoot, currentJob, searchId, findMatch);
    });
    contentSearchRoot.querySelector("[data-confirm-content]")?.addEventListener("click", () => confirmContentSearch(contentSearchRoot, scopedJob));
  });
  messagesEl.querySelectorAll("[data-content-feedback]").forEach((button) => button.addEventListener("click", () => {
    const matchId = button.dataset.contentMatchId || null;
    const root = button.closest(".content-search-review");
    const scopedJob = contentSearchJobForRoot(root, job);
    const match = scopedJob.contentSearch?.candidates?.find((item) => String(item.id) === String(matchId));
    sendContentSearchFeedback(button.dataset.contentFeedback, matchId, match, { searchId: scopedJob.contentSearch?.id });
  }));
  messagesEl.querySelectorAll("[data-content-history-open]").forEach((button) => button.addEventListener("click", async () => {
    const searchId = String(button.dataset.contentHistoryOpen || "");
    if (!searchId) return;
    const requestedJobId = String(job.id || "");
    button.disabled = true;
    try {
      const detail = await ensureContentSearchDetails(searchId, job);
      if (String(currentJob?.id || "") !== requestedJobId || !button.isConnected) return;
      contentSearchDetailCache.set(`${job.id}:${searchId}`, { ...detail, candidateDetailsLoaded: true });
      expandedContentSearchIds.add(searchId);
      renderConversation(currentJob || job);
      window.requestAnimationFrame(() => {
        const target = [...messagesEl.querySelectorAll(".content-search-review[data-content-search-id]")]
          .find((card) => String(card.dataset.contentSearchId || "") === searchId);
        target?.scrollIntoView({ behavior: "smooth", block: "center" });
      });
    } catch (error) {
      if (String(currentJob?.id || "") !== requestedJobId || !button.isConnected) return;
      button.disabled = false;
      showToast(`读取已保留结果详情失败：${error.message}`);
    }
  }));
  messagesEl.querySelectorAll("[data-content-history-add]").forEach((button) => button.addEventListener("click", () => {
    addRetainedContentSearchToBasket(String(button.dataset.contentHistoryAdd || ""), button, currentJob || job);
  }));
  messagesEl.querySelectorAll("[data-content-search-history-toggle]").forEach((button) => button.addEventListener("click", () => {
    const card = button.closest(".content-search-review");
    const searchId = String(card?.dataset?.contentSearchId || "");
    if (!searchId) return;
    expandedContentSearchIds.delete(searchId);
    renderConversation(currentJob || job);
  }));
  messagesEl.querySelectorAll("[data-content-search-restore]").forEach((button) => button.addEventListener("click", () => restoreContentSearch(button.dataset.contentSearchRestore)));
  messagesEl.querySelectorAll("[data-person-label]").forEach((button) => button.addEventListener("click", () => {
    updateContentPersonLabel(button.dataset.personLabel, button.dataset.personCurrentLabel || "");
  }));
  messagesEl.querySelectorAll("[data-person-target]").forEach((input) => input.addEventListener("change", () => {
    syncContentPersonTargetControls(input.closest("[data-person-target-panel]"));
  }));
  messagesEl.querySelectorAll("[data-person-match-mode]").forEach((input) => input.addEventListener("change", () => {
    syncContentPersonTargetControls(input.closest("[data-person-target-panel]"));
  }));
  messagesEl.querySelector("[data-person-target-confirm]")?.addEventListener("click", (event) => {
    const panel = event.currentTarget.closest("[data-person-target-panel]");
    const personIds = [...panel.querySelectorAll("[data-person-target]:checked")].map((input) => input.value);
    const matchMode = panel.querySelector("[data-person-match-mode]:checked")?.value || (personIds.length === 1 ? "any" : "");
    const speakingControl = panel.querySelector("[data-person-speaking-only]");
    const activity = speakingControl ? (speakingControl.checked ? "speaking" : "appearance") : "";
    if (!personIds.length) return void showToast("请至少选择一个人物");
    if (!matchMode) return void showToast("请选择多人匹配方式");
    selectContentPersonTarget(personIds, matchMode, event.currentTarget, activity);
  });
  messagesEl.querySelectorAll("[data-person-target-panel]").forEach((panel) => syncContentPersonTargetControls(panel, { initial: true }));
  messagesEl.querySelectorAll("[data-person-history-target]").forEach((button) => button.addEventListener("click", () => {
    const personIds = String(button.dataset.personHistoryTarget || "").split(",").filter(Boolean);
    selectContentPersonTarget(
      personIds,
      button.dataset.personHistoryMode || "any",
      button,
      button.dataset.personHistoryActivity || "appearance",
    );
  }));
  messagesEl.querySelectorAll("[data-person-preview]").forEach((button) => button.addEventListener("click", () => {
    const person = job.contentIndex?.persons?.find((item) => String(item.id) === String(button.dataset.personPreview));
    const time = Number(button.dataset.personPreviewTime || person?.representativeTime || 0);
    showSource({ autoplay: false });
    viewerMediaKind = "source";
    seekSourceTime(time);
    showToast(`已定位到${person?.label || person?.defaultLabel || "人物"}的代表画面`);
  }));
  messagesEl.querySelectorAll(".content-person-preview img").forEach((image) => image.addEventListener("error", () => {
    image.closest(".content-person-preview")?.classList.add("thumbnail-unavailable");
    image.remove();
  }, { once: true }));
  messagesEl.querySelectorAll("[data-content-evidence-choice]").forEach((button) => button.addEventListener("click", () => {
    const instruction = String(button.dataset.contentInstruction || job.contentSearch?.instruction || job.request?.contentInstruction || "继续当前检索");
    const capabilities = String(button.dataset.contentCapabilities || "").split(",").filter(Boolean);
    sendChat(instruction, {
      evidenceMode: button.dataset.contentEvidenceMode || button.dataset.contentEvidenceChoice,
      allowedCapabilities: capabilities,
    });
  }));
  messagesEl.querySelectorAll("[data-content-speaker-preview]").forEach((button) => button.addEventListener("click", () => {
    showToast(`正在预览 ${button.dataset.contentPreviewSpeaker || "Speaker 候选"}`);
    previewContentMatch({
      id: button.dataset.contentSpeakerPreview,
      title: button.dataset.contentPreviewSpeaker || "Speaker 候选",
      start: Number(button.dataset.contentPreviewStart || 0),
      end: Number(button.dataset.contentPreviewEnd || button.dataset.contentPreviewStart || 0),
      evidenceType: "speech",
    });
  }));
  messagesEl.querySelectorAll("[data-content-speaker-confirm]").forEach((button) => button.addEventListener("click", () => {
    confirmContentPersonSpeaker(button.dataset.personId, button.dataset.contentSpeakerConfirm, button);
  }));
  messagesEl.querySelectorAll("[data-content-expansion]").forEach((button) => button.addEventListener("click", () => {
    const option = job.contentSearch?.expansionOptions?.find((item) => String(item.id) === String(button.dataset.contentExpansion));
    if (!option) return;
    const instruction = String(job.contentSearch?.instruction || job.request?.contentInstruction || "继续当前检索");
    const currentCapabilities = job.contentSearch?.executionPlan?.allowedCapabilities || job.request?.contentAllowedCapabilities || [];
    const capabilities = [...new Set([...currentCapabilities, ...(option.addCapabilities || [])])];
    const payload = {
      evidenceMode: capabilities.length > 1 ? "mixed" : (job.contentSearch?.executionPlan?.evidenceMode || job.request?.contentEvidenceMode),
      allowedCapabilities: capabilities,
    };
    if (option.scopeKind === "all") Object.assign(payload, {
      searchScopeKind: "all", searchScopeStart: 0,
      searchScopeEnd: Number(job.videoInfo?.duration || 0) || null,
    });
    sendChat(instruction, payload);
  }));
  messagesEl.querySelector("[data-content-expand]")?.addEventListener("click", () => {
    const instruction = String(job.contentSearch?.instruction || job.request?.contentInstruction || "重新查找相关内容");
    sendChat(instruction, {
      searchScopeKind: "all", searchScopeStart: 0,
      searchScopeEnd: Number(job.videoInfo?.duration || 0) || null,
      evidenceMode: job.contentSearch?.executionPlan?.evidenceMode || job.request?.contentEvidenceMode,
      allowedCapabilities: job.contentSearch?.executionPlan?.allowedCapabilities || job.request?.contentAllowedCapabilities,
    });
  });
  messagesEl.querySelector("[data-content-continue]")?.addEventListener("click", () => {
    sendChat(String(job.contentSearch?.instruction || job.request?.contentInstruction || "继续当前检索"), {
      evidenceMode: job.contentSearch?.executionPlan?.evidenceMode || job.request?.contentEvidenceMode,
      allowedCapabilities: job.contentSearch?.executionPlan?.allowedCapabilities || job.request?.contentAllowedCapabilities,
    });
  });
  messagesEl.querySelector("[data-open-output-versions]")?.addEventListener("click", () => {
    setDirectorStage("compose");
    renderOutputs(job);
    requestAnimationFrame(() => {
      const selectedVersion = $("#clipStrip .clip-version-button.active") || $("#clipStrip .clip-version-button");
      selectedVersion?.focus({ preventScroll: true });
    });
  });
  messagesEl.querySelectorAll("[data-auto-output]").forEach((button) => button.addEventListener("click", () => {
    const filename = button.dataset.autoOutput;
    if (!filename) return;
    messagesEl.querySelectorAll(".auto-version-button").forEach((item) => item.classList.toggle("active", item === button));
    setOutputVersionBeamSelection(button);
    selectOutput(filename, true);
  }));
  messagesEl.querySelector("[data-regenerate-auto-variants]")?.addEventListener("click", (event) => {
    regenerateMissingAutoVariants(Number(event.currentTarget.dataset.targetCount || 3));
  });
  messagesEl.querySelectorAll("[data-delete-auto-batch]").forEach((button) => button.addEventListener("click", () => {
    deleteAutoCompositionBatch(button.dataset.deleteAutoBatch);
  }));
  const chatComposeButton = $("#chatComposeButton");
  const chatComposeRecommendedButton = $("#chatComposeRecommendedButton");
  const chatComposePlanButton = $("#chatComposePlanButton");
  const chatComposeSubtitle = $("#chatComposeSubtitleMode");
  const canChatCompose = job.status === "awaiting_confirmation" && Boolean(job.eventGroups?.length || job.candidates?.length || job.manualSelection);
  chatComposeButton?.classList.toggle("hidden", !canChatCompose);
  chatComposeRecommendedButton?.classList.toggle("hidden", !canChatCompose || chatInput?.dataset.timelineCompose !== "true");
  chatComposePlanButton?.classList.toggle("hidden", !canChatCompose || !job.eventGroups?.length);
  if (chatComposeButton && chatInput?.dataset.timelineCompose !== "true") chatComposeButton.textContent = "✦ 合成所选片段";
  chatComposeSubtitle?.classList.toggle("hidden", true);
  if (chatComposeButton) chatComposeButton.onclick = () => {
    if (job.manualSelection && (!job.eventGroups?.length && !job.candidates?.length || chatInput?.dataset.timelineCompose === "true")) {
      if (chatInput?.dataset.timelineCompose !== "true") fillChatWithTimelineSelection(job.manualSelection);
      sendChat();
      return;
    }
    const savedSubtitleMode = String(job.brief?.subtitlePreference || job.request?.subtitleMode || "none");
    const subtitleMode = savedSubtitleMode === "ask" ? "none" : savedSubtitleMode;
    if (job.eventGroups?.length) {
      const ids = job.recommendedGroupIds?.length ? job.recommendedGroupIds : job.eventGroups.map((group) => group.id);
      confirmEventGroups(ids, "single_reel", null, subtitleMode);
    } else {
      const indices = job.recommendedIndices?.length ? job.recommendedIndices : job.candidates.map((candidate) => candidate.index);
      confirmCandidates(indices, "single_reel");
    }
  };
  if (chatComposeRecommendedButton) chatComposeRecommendedButton.onclick = () => {
    const savedSubtitleMode = String(job.brief?.subtitlePreference || job.request?.subtitleMode || "none");
    const subtitleMode = savedSubtitleMode === "ask" ? "none" : savedSubtitleMode;
    if (job.eventGroups?.length) {
      const ids = job.recommendedGroupIds?.length ? job.recommendedGroupIds : job.eventGroups.map((group) => group.id);
      confirmEventGroups(ids, "single_reel", null, subtitleMode);
    } else {
      const indices = job.recommendedIndices?.length ? job.recommendedIndices : job.candidates.map((candidate) => candidate.index);
      confirmCandidates(indices, "single_reel");
    }
  };
  if (chatComposePlanButton) chatComposePlanButton.onclick = () => setDirectorStage("compose");
  if (job.status === "awaiting_confirmation" && job.eventGroups?.length) {
    const card = chatRoot.querySelector(".event-recommendation");
    if (card) {
      card?.querySelectorAll(".event-group-list, .recommendation-actions, .review-selection-summary").forEach((node) => node.remove());
      const paragraph = card.querySelector(":scope > p");
      if (paragraph && eventTimelineSummary) paragraph.textContent = eventTimelineSummary.pendingSelection
        ? "已记录你从时间轴选中的片段。可以逐个预览并调整顺序，确认无误后直接合成。"
        : eventTimelineSummary.description;
    }
  }
  normalizeSegmentActionLabels(chatRoot);
  syncThinkingOrbs(chatRoot);
  // The message list is replaced on every poll. A second frame makes the
  // canvas mount reliable even when the deferred orb bundle finishes one
  // frame after the conversation render.
  requestAnimationFrame(() => syncThinkingOrbs(chatRoot));
  chatRoot.querySelectorAll("[data-prompt]").forEach((button) => button.addEventListener("click", () => sendChat(button.dataset.prompt)));
  chatRoot.querySelectorAll("[data-content-edit-query]").forEach((button) => button.addEventListener("click", () => {
    chatInput.value = "";
    chatInput.placeholder = "输入新的查找条件，例如：找到蓝色衣服的人说话的片段";
    chatInput.focus();
  }));
  chatRoot.querySelectorAll("[data-candidate-sort]").forEach((button) => button.addEventListener("click", () => {
    candidateReviewSort = button.dataset.candidateSort || "score";
    renderConversation(currentJob);
  }));
  chatRoot.querySelectorAll(".candidate-list input[type=\"checkbox\"]").forEach((input) => input.addEventListener("change", () => {
    const index = Number(input.value);
    if (input.checked) locallyExcludedCandidates.delete(index);
    else locallyExcludedCandidates.add(index);
    input.closest(".candidate-row")?.classList.toggle("excluded", !input.checked);
    persistReviewExclusions();
    updateReviewSelectionSummary(chatRoot);
  }));
  chatRoot.querySelectorAll("[data-reedit-job]").forEach((button) => button.addEventListener("click", reopenCurrentJobForEditing));
  chatRoot.querySelectorAll("[data-alternative-job]").forEach((button) => button.addEventListener("click", () => requestAlternativeCut(job)));
  chatRoot.querySelectorAll("[data-toggle-precision-workspace]").forEach((button) => button.addEventListener("click", () => {
    const expanded = !timelineExpanded;
    setTimelineExpanded(expanded, { scroll: expanded });
    button.textContent = expanded ? "收起时间线" : "查看时间线";
  }));
  chatRoot.querySelector("[data-render-current-timeline]")?.addEventListener("click", () => {
    if (!eventTimelineSummary?.groupIds?.length) return void showToast("当前时间轴没有可生成的事件镜头");
    const savedSubtitleMode = String(job.brief?.subtitlePreference || job.request?.subtitleMode || "none");
    const subtitleMode = savedSubtitleMode === "ask" ? "none" : savedSubtitleMode;
    confirmEventGroups(eventTimelineSummary.groupIds, "single_reel", null, subtitleMode);
  });
  chatRoot.querySelector(".reanalyze-job")?.addEventListener("click", () => reanalyzeJob(job));
  chatRoot.querySelector(".inline-cancel")?.addEventListener("click", cancelCurrentJob);
  bindBriefEditor(job, chatRoot);
  chatRoot.querySelector(".confirm-event-groups")?.addEventListener("click", () => {
    const checked = new Set([...chatRoot.querySelectorAll(".event-group-check:checked")].map((input) => input.value));
    const ids = [...eventGroupSelectionOrder.filter((id) => checked.has(String(id))), ...[...checked].filter((id) => !eventGroupSelectionOrder.includes(id))];
    confirmEventGroups(ids, "single_reel");
  });
  chatRoot.querySelector(".confirm-all-events")?.addEventListener("click", () => confirmEventGroups(job.eventGroups.map((group) => group.id), "single_reel"));
  chatRoot.querySelector(".export-event-groups")?.addEventListener("click", () => {
    const checked = new Set([...chatRoot.querySelectorAll(".event-group-check:checked")].map((input) => input.value));
    const ids = [...eventGroupSelectionOrder.filter((id) => checked.has(String(id))), ...[...checked].filter((id) => !eventGroupSelectionOrder.includes(id))];
    confirmEventGroups(ids, "separate_events");
  });
  chatRoot.querySelector(".create-event-from-selection")?.addEventListener("click", createEventFromSelection);
  chatRoot.querySelector(".confirm-manual-selection")?.addEventListener("click", () => {
    if (!currentJob?.manualSelection) return;
    if (chatInput?.dataset.timelineCompose !== "true") fillChatWithTimelineSelection(currentJob.manualSelection);
    sendChat();
  });
  chatRoot.querySelector(".rebalance-budget")?.addEventListener("click", () => sendChat(`单条成片目标时长调整为 ${Number(job.totalTargetSeconds || job.request?.totalTargetSeconds)} 秒`));
  chatRoot.querySelectorAll(".event-group-row").forEach((row) => {
    const group = job.eventGroups?.find((item) => item.id === row.dataset.eventGroup);
    if (!group) return;
    row.querySelector(".preview-event")?.addEventListener("click", () => previewEventGroup(group));
    row.querySelector(".rename-event")?.addEventListener("click", () => renameEventGroup(group));
    row.querySelector(".add-selection-event")?.addEventListener("click", () => addSelectionToEventGroup(group));
    row.querySelectorAll(".event-segment").forEach((segmentRow, segmentIndex) => {
      const segment = group.segments.find((item) => item.id === segmentRow.dataset.segmentId);
      segmentRow.querySelector(".preview-segment")?.addEventListener("click", () => previewEventSegment(group, segment));
      segmentRow.querySelector(".delete-segment")?.addEventListener("click", () => deleteEventSegment(group, segment));
      segmentRow.querySelector(".move-segment-up")?.addEventListener("click", () => reorderEventSegment(group, segmentIndex, segmentIndex - 1));
      segmentRow.querySelector(".move-segment-down")?.addEventListener("click", () => reorderEventSegment(group, segmentIndex, segmentIndex + 1));
      segmentRow.querySelector(".move-segment-group")?.addEventListener("click", () => moveEventSegment(group, segment));
    });
  });
  chatRoot.querySelectorAll(".event-group-check").forEach((input) => input.addEventListener("change", () => { recordEventGroupSelection(input.value, input.checked); updateReviewSelectionSummary(chatRoot); renderChatContextBar(); }));
  updateReviewSelectionSummary(chatRoot);
  chatRoot.querySelector(".confirm-selected")?.addEventListener("click", () => {
    const indices = [...chatRoot.querySelectorAll(".candidate-list input:checked")].map((input) => Number(input.value));
    confirmCandidates(indices);
  });
  chatRoot.querySelector(".confirm-all")?.addEventListener("click", () => confirmCandidates(job.candidates.map((candidate) => candidate.index)));
  chatRoot.querySelector(".confirm-top3")?.addEventListener("click", () => {
    const indices = [...job.candidates].sort((left, right) => right.score - left.score).slice(0, 3).map((candidate) => candidate.index);
    confirmCandidates(indices);
  });
  chatRoot.querySelectorAll(".preview-candidate").forEach((button) => button.addEventListener("click", () => previewCandidate(Number(button.dataset.candidateIndex))));
  chatRoot.querySelectorAll(".rename-candidate").forEach((button) => button.addEventListener("click", async () => {
    const index = Number(button.dataset.candidateIndex);
    const candidate = job.candidates.find((item) => Number(item.index) === index);
    const title = await requestTextInput({
      kicker: "候选镜头",
      title: "修改候选名称",
      summary: "名称只用于当前任务，不会改变片段边界或分析结果。",
      label: "候选名称",
      value: candidate?.title || "",
      required: true,
      confirmLabel: "保存名称",
    });
    if (title?.trim() && title.trim() !== candidate?.title) sendChat(`第 ${index + 1} 条改名为${title.trim()}`);
  }));
  chatRoot.querySelectorAll(".candidate-menu").forEach((button) => button.addEventListener("click", async () => {
    const index = Number(button.dataset.candidateIndex);
    const action = await requestChoice({
      kicker: "候选操作",
      title: `处理第 ${index + 1} 条候选`,
      summary: "所有修改都会先反映在审核列表中。",
      selectLabel: "操作方式",
      options: [
        { value: "copy", label: "复制候选" },
        { value: "split", label: "拆分为两段" },
        { value: "move", label: "移动到其他位置" },
        { value: "delete", label: "删除候选" },
      ],
      confirmLabel: "继续",
    });
    if (action === "delete") sendChat(`删除第 ${index + 1} 条`);
    else if (action === "copy") sendChat(`复制第 ${index + 1} 条`);
    else if (action === "split") sendChat(`把第 ${index + 1} 条拆成两段`);
    else if (action === "move") {
      const target = await requestTextInput({
        kicker: "调整顺序",
        title: "移动候选",
        summary: `当前共有 ${job.candidates.length} 条候选。`,
        label: "目标位置",
        value: String(Math.min(job.candidates.length, index + 2)),
        hint: `请输入 1–${job.candidates.length}。`,
        required: true,
        confirmLabel: "移动",
      });
      const position = Number(target);
      if (Number.isInteger(position) && position >= 1 && position <= job.candidates.length) {
        sendChat(`把第 ${index + 1} 条移动到第 ${position} 条`);
      } else if (target !== null) showToast(`目标位置必须是 1–${job.candidates.length} 的整数。`);
    }
  }));
  if (keepPinnedToBottom) {
    chatRoot.scrollTop = chatRoot.scrollHeight;
    requestAnimationFrame(() => {
      if (chatRoot.isConnected) chatRoot.scrollTop = chatRoot.scrollHeight;
    });
  } else if (scrollAnchor) {
    const restored = [...chatRoot.querySelectorAll("[data-conversation-key]")]
      .find((node) => node.dataset.conversationKey === scrollAnchor.key);
    if (restored) chatRoot.scrollTop += restored.getBoundingClientRect().top - chatRoot.getBoundingClientRect().top - scrollAnchor.offset;
    else chatRoot.scrollTop = Math.min(previousScrollTop, Math.max(0, chatRoot.scrollHeight - chatRoot.clientHeight));
  } else {
    chatRoot.scrollTop = Math.min(previousScrollTop, Math.max(0, chatRoot.scrollHeight - chatRoot.clientHeight));
  }
  renderChatContextBar();
  renderContentSelectionBasket(job);
}

async function reanalyzeJob(job) {
  if (!job || actionBusy || !["cancelled", "failed"].includes(job.status)) return;
  const workflowKey = workflowKindForJob(job);
  const operation = {
    highlight: "高光分析", content_search: "内容检索",
    person_edit: "人物发现与轨迹扫描", speaker_edit: "说话人识别",
  }[workflowKey] || "当前分析";
  if (!await requestActionConfirmation({ title: `恢复${operation}`, summary: `系统会恢复中断前的${operation}步骤。`, details: ["不需要重新上传视频", "会保留已有成片、对话和可复用证据", "只重置当前操作的未完成状态"] })) return;
  const actionToken = captureJobAction(job);
  actionBusy = true;
  try {
    const { job: updated } = await api(`/api/jobs/${encodeURIComponent(job.id)}/reanalyze`, { method: "POST" });
    if (!commitJobAction(updated, actionToken)) return;
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) actionBusy = false; }
}

function renderEvidencePlaceholder({ time, title, reason } = {}) {
  const evidence = $("#evidencePanel");
  if (!evidence) return;
  clearOutputEvidenceStrip();
  const job = currentJob;
  const groupCount = Number(job?.eventGroups?.length || 0);
  const candidateCount = Number(job?.candidates?.length || 0);
  const outputCount = jobOutputCount(job);
  const running = isPipelineRunningStatus(job?.status);
  const defaultTime = running
    ? "AI 分析中"
    : groupCount || candidateCount
      ? "时间轴已就绪"
      : outputCount
        ? `${outputCount} 个成片版本`
        : "审核说明";
  const defaultTitle = running ? "正在发现事件与镜头" : "选择事件或镜头";
  const defaultReason = running
    ? "分析结果会逐步出现在下方时间轴。完成后点击事件或镜头，即可在这里查看说明和判断依据。"
    : groupCount || candidateCount
      ? `下方时间轴已展示 ${groupCount || candidateCount} 个可审核项目。点击事件或镜头查看详情，只有选择“缩放到当前事件”才会改变时间轴范围。`
      : outputCount
        ? "可从播放器顶部选择成片预览，也可以回到源视频继续查看时间轴。"
        : "点击下方时间轴中的事件或镜头，在这里查看内容说明、时间范围和判断依据。";
  evidence.classList.remove(
    "hidden",
    "candidate-mode",
    "montage-mode",
    "output-mode",
    "evidence-in-gutter",
    "evidence-gutter-left",
    "evidence-gutter-right",
    "evidence-compact",
    "evidence-expanded",
  );
  evidence.classList.add("evidence-placeholder");
  hideContentDetailModeSwitch();
  $("#clipTime").textContent = time || defaultTime;
  $("#clipTitle").textContent = title || defaultTitle;
  $("#clipScore").textContent = "";
  $("#clipReason").textContent = reason || defaultReason;
  const transcript = $("#clipTranscript");
  if (transcript) {
    transcript.innerHTML = "";
    transcript.classList.add("hidden");
  }
  if ($("#clipEvidenceMeta")) $("#clipEvidenceMeta").innerHTML = "";
  if ($("#clipEvidence")) $("#clipEvidence").innerHTML = "";
  if ($("#outputExplanation")) {
    $("#outputExplanation").innerHTML = "";
    $("#outputExplanation").classList.add("hidden");
  }
  $("#addToChatButton")?.classList.add("hidden");
  $("#keepButton")?.classList.add("hidden");
  $("#replaceButton")?.classList.add("hidden");
  syncEvidencePlacement();
}

function showSource({ autoplay = true, preserveMappedTime = false, seekTime = null } = {}) {
  if (!currentJob) return;
  const explicitSourceTime = seekTime === null || seekTime === undefined || seekTime === "" ? NaN : Number(seekTime);
  const mappedSourceTime = preserveMappedTime && ["output", "event"].includes(viewerMediaKind)
    ? timelineAbsoluteTime()
    : null;
  const requestedSourceTime = Number.isFinite(explicitSourceTime)
    ? Math.max(0, explicitSourceTime)
    : Number.isFinite(mappedSourceTime) ? Math.max(0, mappedSourceTime) : null;
  const sourceJobId = String(currentJob.id || "");
  sourcePreviewRetryToken = 0;
  candidatePreviewToken += 1;
  currentOutput = null;
  currentCandidate = null;
  currentEventGroup = null;
  currentEventSegment = null;
  candidatePreviewEnd = null;
  viewerMediaKind = "source";
  timelineCoordinateSpace = "source";
  timelineSelectedOutputSegmentIndex = null;
  syncOutputPreviewMode();
  clearOutputVersionSelectionState();
  clearPlayerNotice();
  applyMediaAspect(viewerShell, currentJob.videoInfo?.width, currentJob.videoInfo?.height);
  const sourceUrl = sourcePreviewUrl();
  if (!sourceUrl) {
    setMainVideoSource("");
    showPlayerNotice("当前任务暂时没有可用的源视频地址");
  } else {
    const restoreSourcePosition = () => {
      if (viewerMediaKind !== "source" || String(currentJob?.id || "") !== sourceJobId || requestedSourceTime === null) return;
      const duration = Number(mainVideo.duration || currentJob?.videoInfo?.duration || 0);
      const bounded = duration > 0
        ? Math.min(Math.max(0, duration - .05), requestedSourceTime)
        : requestedSourceTime;
      mainVideo.currentTime = bounded;
      const view = timelineViewRange();
      if (bounded < view.start || bounded > view.end) {
        const timelineDuration = timelineDurationValue();
        const span = Math.min(timelineDuration, Math.max(30, view.duration));
        setTimelineView(bounded - span / 2, bounded + span / 2);
      } else {
        updateTimeline();
      }
      updateTimelinePlayhead();
    };
    if (requestedSourceTime !== null) mainVideo.addEventListener("loadedmetadata", restoreSourcePosition, { once: true });
    const sourceChanged = setMainVideoSource(sourceUrl);
    if (requestedSourceTime !== null && !sourceChanged && mainVideo.readyState >= 1) {
      mainVideo.removeEventListener("loadedmetadata", restoreSourcePosition);
      restoreSourcePosition();
    }
    if (autoplay) requestMainVideoAutoplay();
  }
  beginSourcePreviewPolling();
  $("#viewerBadge").textContent = "源视频";
  renderOutputPreviewSelector(currentJob);
  $("#reviewKicker").textContent = "源视频";
  $("#reviewTitle").textContent = currentJob.filename;
  $("#downloadButton")?.classList.add("hidden");
  $("#coverDownloadButton")?.classList.add("hidden");
  $("#packageDownloadButton")?.classList.add("hidden");
  $("#coverIntroButton")?.classList.add("hidden");
  $("#finalizePreviewButton")?.classList.add("hidden");
  syncOneOffFinalizeAction(currentJob);
  $("#subtitleButton")?.classList.add("hidden");
  $("#keepButton")?.classList.add("hidden");
  $("#replaceButton")?.classList.add("hidden");
  $("#subtitleButton")?.classList.add("hidden");
  renderEvidencePlaceholder();
  document.querySelectorAll(".candidate-row").forEach((row) => row.classList.remove("previewing"));
  document.querySelectorAll(".clip-card").forEach((card) => card.classList.remove("active"));
  updateTimeline();
  syncReviewSelectionClasses();
}

function jobOutputVersions(job = currentJob) {
  if (!job) return [];
  if (job.outputVersions?.length) return [...job.outputVersions].sort((a, b) => Number(b.number || 0) - Number(a.number || 0));
  if (!job.outputs?.length) return [];
  return [{ id: "v001", number: 1, createdAt: job.updatedAt, outputMode: job.outputMode, outputs: job.outputs }];
}

function outputVersionIdentity(version, output) {
  if (String(version?.id || "") === "agent-review-previews" || String(output?.outputKind || "") === "agent_review_preview") {
    const outputs = Array.isArray(version?.outputs) ? version.outputs : [];
    const outputIndex = outputs.findIndex((item) => item === output || String(item?.filename || "") === String(output?.filename || ""));
    return `审核样片 ${Math.max(0, outputIndex) + 1}`;
  }
  const versionLabel = `V${Number(version?.number || 1)}`;
  const outputs = Array.isArray(version?.outputs) ? version.outputs : [];
  if (outputs.length <= 1) return versionLabel;
  const outputIndex = outputs.findIndex((item) => item === output || String(item?.filename || "") === String(output?.filename || ""));
  return `${versionLabel}.${Math.max(0, outputIndex) + 1}`;
}

function outputVersionDownloadLinkMarkup(version, output, { className = "" } = {}) {
  const identity = outputVersionIdentity(version, output);
  const previewOnly = Boolean(output?.previewOnly || version?.previewOnly);
  const shortLabel = previewOnly ? "下载样片" : "下载 MP4";
  const classes = ["output-version-download", className].filter(Boolean).join(" ");
  const downloadUrl = String(output?.downloadUrl || "").trim();
  if (!downloadUrl || downloadUrl === "undefined" || downloadUrl === "null") {
    return `<span class="${escapeHtml(classes)} unavailable" aria-disabled="true">${escapeHtml(identity)} 下载未就绪</span>`;
  }
  const filename = String(output?.downloadFilename || output?.filename || `${identity}.mp4`);
  const qualityLabel = previewOnly ? "审核样片" : "正式 MP4";
  return `<a class="${escapeHtml(classes)}" href="${escapeHtml(downloadUrl)}" download="${escapeHtml(filename)}" data-output-download="${escapeHtml(output?.filename || "")}" title="下载 ${escapeHtml(identity)} 的${qualityLabel}" aria-label="下载 ${escapeHtml(identity)} 的${qualityLabel}">${escapeHtml(shortLabel)}</a>`;
}

async function createCoverIntroOutput(version, output) {
  if (!currentJob || actionBusy || !output?.coverIntroAvailable) return;
  const confirmed = await requestActionConfirmation({
    title: "生成带封面片头的新版本",
    summary: "在当前成片前增加 1 秒静止封面，并保存为新的 MP4 版本。",
    details: ["原成片不会被覆盖", "封面和发布包仍会单独保留", "视频需要重新编码，画面播放时会看到封面"],
    confirmLabel: "生成 1 秒封面片头版",
  });
  if (!confirmed) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  const button = $("#coverIntroButton");
  if (button) button.textContent = "正在生成片头…";
  try {
    const { job } = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/outputs/${encodeURIComponent(output.filename)}/cover-intro`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: { duration: 1.0 },
    });
    if (!commitJobAction(job, actionToken)) return;
    showToast("已开始生成封面片头版，原成片保持不变", "success");
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message || "封面片头生成失败");
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
    if (button) button.textContent = "生成封面片头版";
  }
}

function jobOutputCount(job = currentJob) {
  const orderedOutputCount = orderedJobOutputs(job).length;
  if (orderedOutputCount) return orderedOutputCount;
  const explicitCount = job?.outputCount;
  // Number(null) is 0. Treat an absent count as absent instead of allowing it
  // to hide outputVersions that are already persisted on the job.
  if (explicitCount !== null && explicitCount !== undefined && explicitCount !== "" && Number.isFinite(Number(explicitCount))) {
    return Number(explicitCount);
  }
  return 0;
}

function agentReviewPreviewsForJob(job = currentJob) {
  const raw = [
    ...(Array.isArray(job?.agentReviewPreviews) ? job.agentReviewPreviews : []),
    ...(Array.isArray(job?.agentPreviewOutputs)
      ? job.agentPreviewOutputs.filter((item) => String(item?.outputKind || "") === "agent_review_preview")
      : []),
  ];
  const seen = new Set();
  return raw.map((item, index) => {
    if (!item || typeof item !== "object") return item;
    const fallback = `审核样片 ${index + 1}`;
    return {
      ...item,
      title: cleanDisplayText(item.title, fallback),
      displayTitle: cleanDisplayText(item.displayTitle, cleanDisplayText(item.title, fallback)),
    };
  }).filter((item) => {
    if (!item || typeof item !== "object") return false;
    const key = String(item.filename || item.previewUrl || item.videoUrl || item.sessionId || item.id || "");
    if (!key || seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}

function agentReviewPreviewMeta(item) {
  const duration = Number(item?.duration || 0);
  const revision = Number(item?.revision || 0);
  const clipCount = Number(item?.clipCount);
  const quality = String(item?.qualityStatus || "");
  const qualityLabel = quality === "passed" ? "时间线检查通过"
    : quality === "needs_review" ? "有项目需人工复核"
      : "质量信息待确认";
  return [
    duration > 0 ? `${duration.toFixed(1)} 秒` : "",
    Number.isFinite(clipCount) && clipCount > 0 ? `${clipCount} 个镜头` : "",
    revision > 0 ? `草稿 r${revision}` : "",
    qualityLabel,
    "审核样片",
  ].filter(Boolean).join(" · ");
}

function agentReviewPreviewMarkup(previews) {
  return `<div class="agent-review-preview-list">${previews.map((item, index) => `
    <article class="agent-review-preview-card" data-agent-review-preview-card="${index}">
      <span><strong>${escapeHtml(cleanDisplayText(item.displayTitle, cleanDisplayText(item.title, `审核样片 ${index + 1}`)))}</strong><small>${escapeHtml(agentReviewPreviewMeta(item))}</small></span>
      <div class="agent-review-preview-actions">
        <button type="button" data-agent-review-preview="${index}">预览样片</button>
        <button type="button" class="primary" data-agent-review-export="${index}" ${item.timelineReady === false || !String(item.sessionId || item.sourceEditSessionId || "") ? "disabled title=\"时间线数据尚未就绪\"" : ""}>生成成片</button>
      </div>
    </article>
  `).join("")}</div>`;
}

async function exportAgentReviewPreview(item = {}) {
  if (!currentJob?.id || actionBusy) return;
  const actionToken = captureJobAction();
  item = window.structuredClone(item);
  const sessionId = String(item.sourceEditSessionId || item.sessionId || "");
  let session = (currentJob.editSessions || []).find((value) => String(value?.id || "") === sessionId);
  if (!session) {
    const payload = await api(`/api/jobs/${encodeURIComponent(actionToken.jobId)}`);
    if (!jobActionStillCurrent(actionToken)) return void showToast("任务已切换，请重新选择样片");
    currentJob = payload.job;
    session = (currentJob.editSessions || []).find((value) => String(value?.id || "") === sessionId);
  }
  if (!session?.id || !session.clips?.length) return void showToast("该样片缺少可导出的时间线，请重新运行当前计划");
  if (session.pendingProposal) return void showToast("请先打开精剪时间线并应用待确认的修改");
  if (session.previewStatus !== "ready" || Number(session.previewRevision) !== Number(session.revision)
      || (item.revision != null && Number(item.revision) !== Number(session.revision))) {
    return void showToast("该样片不是当前时间线的最新预览，请进入编辑并重新生成审核样片");
  }
  session = window.structuredClone(session);
  const sessionSnapshot = JSON.stringify([session.revision, session.subtitleDraftId, session.subtitleEnabled, session.subtitleStyle]);
  const quality = String(item.qualityStatus || "");
  const risky = quality !== "passed";
  const issues = (item.preflight?.issues || []).filter((value) => value?.message).slice(0, 3);
  const duration = Number(session.duration || item.duration || 0);
  const confirmed = await requestActionConfirmation({
    title: "生成成片",
    summary: `当前样片 · ${session.clips.length} 个镜头${duration > 0 ? ` · ${formatTime(duration)}` : ""}`,
    details: risky ? issues.map((value) => String(value.message)) : [],
    warning: risky && issues.length ? "请确认以上提示后继续。" : "",
    confirmLabel: "开始生成",
  });
  if (!confirmed) return;
  const latest = (currentJob?.editSessions || []).find(value => value.id === session.id);
  if (!jobActionStillCurrent(actionToken) || !latest || latest.pendingProposal
      || sessionSnapshot !== JSON.stringify([latest.revision, latest.subtitleDraftId, latest.subtitleEnabled, latest.subtitleStyle])) {
    return void showToast("任务、时间线或字幕已变化，本次生成已取消，请重新审核");
  }
  const warningCodes = (item.preflight?.issues || [])
    .filter((value) => value?.severity === "warning" && value?.code)
    .map((value) => String(value.code));
  actionBusy = true;
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/edit-sessions/${encodeURIComponent(session.id)}/render`, {
      method: "POST",
      body: {
        revision: Number(session.revision ?? item.revision ?? 0),
        subtitleMode: session.subtitleEnabled && session.subtitleDraftId ? "burn" : "none",
        subtitleStyle: session.subtitleStyle || "clean",
        subtitleDraftId: session.subtitleDraftId || null,
        versionLabel: cleanDisplayText(item.title, cleanDisplayText(session.title, "Agent 精剪版")).slice(0, 80),
        acknowledgedWarningCodes: warningCodes,
      },
    });
    if (!commitJobAction(payload.job, actionToken)) return;
    showToast("成片版本已开始生成；当前样片会继续保留", "success");
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message || "成片版本提交失败");
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

// Agent plan surfaces use the same reviewed export flow as the result rail.
// Keeping one entry point prevents a second set of quality-confirmation rules.
window.ClipTalkExportAgentReviewPreview = exportAgentReviewPreview;

function bindAgentReviewPreviewActions(root, previews) {
  root?.querySelectorAll("[data-agent-review-preview]").forEach((button) => {
    button.addEventListener("click", () => {
      const item = previews[Number(button.dataset.agentReviewPreview || 0)] || {};
      window.ClipTalkOpenAgentPreview?.(item)
        ?.catch?.((error) => showToast(error.message || "审核样片暂时无法打开"));
    });
  });
  root?.querySelectorAll("[data-agent-review-export]").forEach((button) => {
    button.addEventListener("click", () => {
      const item = previews[Number(button.dataset.agentReviewExport || 0)] || {};
      exportAgentReviewPreview(item).catch((error) => showToast(error.message || "成片版本提交失败"));
    });
  });
}

function outputVersionQuality(version, output = null) {
  const explicit = String(output?.qualityStatus || version?.qualityStatus || "");
  if (["failed", "warning"].includes(explicit)) return "needs_review";
  if (["passed", "needs_review", "review_unavailable", "pending"].includes(explicit)) return explicit;
  const gate = output?.qualityGate || version?.qualityGate || {};
  if (gate.passed === true) return "passed";
  if (["degraded", "review_unavailable"].includes(String(version?.reviewStatus || output?.reviewStatus || ""))) return "review_unavailable";
  if (gate.passed === false || version?.manualReviewRequired || output?.manualReviewRequired) return "needs_review";
  return "pending";
}

function outputVersionQualityLabel(version, output = null) {
  const quality = outputVersionQuality(version, output);
  if (quality === "passed") return version?.recommended ? "AI 推荐" : "质量通过";
  if (quality === "needs_review") return "需人工复核";
  if (quality === "review_unavailable") return "审片未完成";
  return "质量待确认";
}

function autoVersionPresentation(job, version) {
  if (version?.displayName) {
    return {
      displayName: version.displayName,
      sourceLabel: version.sourceLabel || (version.masterReady ? "高清成片" : "AI 样片"),
      strategyDescription: version.strategyDescription || "保留该版本已确认的剪辑方案",
    };
  }
  const legacyFormalTitle = String(version?.outputs?.[0]?.title || "");
  if (/正式导出/.test(legacyFormalTitle)) {
    return {
      displayName: legacyFormalTitle.replace(/\s*[·｜|]\s*正式导出.*$/, "").trim() || "高清成片",
      sourceLabel: "成片版本",
      strategyDescription: "按已确认样片的镜头与顺序进行高清渲染",
    };
  }
  const index = Math.max(0, Number(version?.number || 1) - 1);
  const automaticVersions = Array.isArray(job?.autoComposition?.versions) ? job.autoComposition.versions : [];
  const raw = automaticVersions[index];
  if (raw && typeof raw === "object" && raw.displayName) return raw;
  if (automaticVersions.length && index >= automaticVersions.length) {
    return { displayName: "当前时间轴", sourceLabel: "手动生成", strategyDescription: "按当前审核时间轴生成" };
  }
  const label = typeof raw === "string" ? raw : "";
  if (index === 0 || /VLM/i.test(label)) return { displayName: "完整事件版", sourceLabel: "视觉推荐", strategyDescription: "保留事件完整过程" };
  if (/情绪/.test(label)) return { displayName: "情绪集中版", sourceLabel: "剪辑规划", strategyDescription: "优先保留情绪高点" };
  if (/信息|密度/.test(label)) return { displayName: "信息精简版", sourceLabel: "剪辑规划", strategyDescription: "优先保留关键信息" };
  return { displayName: "节奏连贯版", sourceLabel: "剪辑规划", strategyDescription: "强化镜头前后衔接" };
}

function orderedJobOutputs(job = currentJob) {
  const previewOutputWithUrl = (item) => {
    const output = { ...item, previewOnly: true };
    const filename = String(output.filename || "");
    if (filename && !output.previewUrl && job?.id) {
      output.previewUrl = `/api/jobs/${encodeURIComponent(job.id)}/outputs/${encodeURIComponent(filename)}`;
      output.videoUrl = output.previewUrl;
    }
    return output;
  };
  const entries = [...jobOutputVersions(job)]
    .sort((left, right) => Number(left.number || 0) - Number(right.number || 0))
    .flatMap((version) => [
      ...(version.outputs || []).map((item) => ({ item, version })),
      ...(version.previewOutputs || []).map((item) => ({ item: previewOutputWithUrl(item), version: { ...version, previewOnly: true } })),
    ]);
  const agentPreviews = (Array.isArray(job?.agentPreviewOutputs) ? job.agentPreviewOutputs : [])
    .map((item, index) => {
      if (!item || typeof item !== "object") return item;
      const fallback = `审核样片 ${index + 1}`;
      return {
        ...item,
        title: cleanDisplayText(item.title, fallback),
        displayTitle: cleanDisplayText(item.displayTitle, cleanDisplayText(item.title, fallback)),
      };
    });
  const agentVersion = {
    id: "agent-review-previews", number: 0, displayName: "Agent 审核预览",
    sourceLabel: "Agent", previewOnly: true, variantKind: "independent", outputs: agentPreviews,
  };
  entries.push(...agentPreviews.map((item) => ({ item, version: agentVersion })));
  const seen = new Set();
  return entries.filter(({ item }) => {
    const key = String(item?.filename || item?.previewUrl || item?.videoUrl || "");
    if (!key || seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}

window.ClipTalkOrderedJobOutputs = (job = currentJob) => orderedJobOutputs(job);
window.ClipTalkVersionAction = async (filename, action) => {
  const entry = locateJobOutput(filename);
  if (!entry) return showToast("该版本已不可用，请重新选择版本");
  if (action === "preview") return selectOutput(filename, true);
  const agentPreview = entry.version?.id === "agent-review-previews";
  if (action === "edit") {
    if (!agentPreview) return openSecondaryEditor(entry.version?.id, entry.output.filename);
    const session = sourceEditSessionForOutput(entry.output);
    if (!session?.id) return showToast("该样片缺少可编辑的时间线，请重新生成样片");
    return window.ClipTalkOpenAgentTimeline?.({ sessionId: session.id, reviewPendingProposal: true });
  }
  if (action === "keep") return saveCurrentOutputToLibrary(entry.output);
  if (action === "export") {
    if (agentPreview) return exportAgentReviewPreview(entry.output);
    if (entry.output.previewOnly || entry.version?.previewOnly) {
      return finalizePreviewVersion(entry.version, entry.output);
    }
    const url = String(entry.output.downloadUrl || "").trim();
    if (!url || ["null", "undefined"].includes(url)) return showToast("该版本的下载文件尚未就绪");
    const link = document.createElement("a");
    link.href = url;
    link.download = entry.output.downloadFilename || entry.output.filename;
    document.body.append(link);
    link.click();
    link.remove();
  }
};

function renderOutputPreviewSelector(job = currentJob) {
  const select = $("#videoViewSelect");
  if (!select) return;
  const outputs = orderedJobOutputs(job);
  const headerPicker = $("#headerVersionPicker");
  if (headerPicker) {
    if (!job || !outputs.length) {
      headerPicker.innerHTML = "";
      headerPicker.classList.add("hidden");
    } else {
      const sourceActive = viewerMediaKind !== "output";
      headerPicker.innerHTML = `<span class="header-version-label">查看</span><button type="button" class="header-version-button${sourceActive ? " active" : ""}" data-header-source aria-selected="${sourceActive ? "true" : "false"}">源视频</button>${outputs.map(({ item, version }) => { const active = currentOutput?.filename === item.filename; const identity = outputVersionIdentity(version, item); return `<button type="button" class="header-version-button${active ? " active" : ""}" data-header-output="${escapeHtml(item.filename || "")}" aria-selected="${active ? "true" : "false"}">${escapeHtml(identity)}</button>`; }).join("")}`;
      headerPicker.classList.remove("hidden");
      headerPicker.querySelector("[data-header-source]")?.addEventListener("click", () => showSource({ preserveMappedTime: true }));
      headerPicker.querySelectorAll("[data-header-output]").forEach((button) => button.addEventListener("click", () => selectOutput(button.dataset.headerOutput, true)));
    }
  }
  if (!job) {
    select.classList.add("hidden");
    select.innerHTML = "";
    return;
  }
  if (!outputs.length) {
    select.classList.add("hidden");
    select.innerHTML = "<option value=\"source\">源视频</option>";
    return;
  }
  const outputOptions = outputs.map(({ item, version }) => {
    const presentation = autoVersionPresentation(job, version);
    const strategyName = cleanDisplayText(item.displayName)
      || cleanDisplayText(presentation.displayName)
      || cleanDisplayText(item.title);
    const kind = String(version.variantKind || (version.previewOnly ? "independent" : "formal_export"));
    const kindLabel = kind === "formal_export" ? "高清" : kind === "repair" ? "返修" : "样片";
    const qualityLabel = outputVersionQualityLabel(version, item);
    const reviewSample = currentOutputIsReviewSample(item, version);
    const identity = outputVersionIdentity(version, item);
    const label = reviewSample
      ? `${identity}${strategyName ? ` · ${strategyName}` : ""} · ${qualityLabel}`
      : `${identity}${strategyName ? ` · ${strategyName}` : ""} · ${kindLabel} · ${qualityLabel}`;
    return `<option value="${escapeHtml(item.filename)}">${escapeHtml(label)}</option>`;
  }).join("");
  select.innerHTML = `<option value="source">源视频</option>${outputOptions}`;
  const selected = viewerMediaKind === "output" ? currentOutput?.filename : "source";
  select.value = selected && outputs.some(({ item }) => item.filename === selected) ? selected : "source";
  select.classList.remove("hidden");
  const picker = $("#clipVersionPicker");
  if (picker) {
    const selectedFilename = viewerMediaKind === "output" ? String(currentOutput?.filename || "") : "";
    picker.querySelectorAll("[data-clip-picker-output]").forEach((button) => {
      const active = String(button.dataset.clipPickerOutput || "") === selectedFilename;
      button.classList.toggle("active", active);
      button.setAttribute("aria-selected", active ? "true" : "false");
    });
    const sourceButton = picker.querySelector("[data-clip-picker-source]");
    const activeSource = !selectedFilename;
    sourceButton?.classList.toggle("active", activeSource);
    sourceButton?.setAttribute("aria-selected", activeSource ? "true" : "false");
  }
  syncCurrentOutputEditAction(job);
}

function locateJobOutput(filename, job = currentJob) {
  const located = orderedJobOutputs(job)
    .find(({ item }) => String(item?.filename || "") === String(filename || ""));
  return located ? { output: located.item, version: located.version } : null;
}

function sourceEditSessionForOutput(output, job = currentJob) {
  if (!output || !job) return null;
  const sessions = Array.isArray(job.editSessions) ? job.editSessions : [];
  const explicitId = String(output.sourceEditSessionId || output.editSessionId || output.sessionId || "");
  if (explicitId) {
    const explicit = sessions.find((session) => String(session?.id || "") === explicitId);
    if (explicit) return explicit;
  }
  const sourceFilename = String(output.sourceOutputFilename || "");
  if (!sourceFilename) return null;
  return sessions.find((session) => {
    const sessionId = String(session?.id || "");
    const previewFilename = String(session?.previewPath || "").split(/[\\/]/).pop() || "";
    return (previewFilename && previewFilename === sourceFilename)
      || (sessionId && (sourceFilename === sessionId || sourceFilename.startsWith(`${sessionId}-`)));
  }) || null;
}

function syncCurrentOutputEditAction(job = currentJob) {
  const button = $("#secondaryEditCurrentButton");
  if (!button) return;
  const located = viewerMediaKind === "output" && currentOutput?.filename
    ? locateJobOutput(currentOutput.filename, job)
    : null;
  const realVersionIds = new Set(jobOutputVersions(job).map((version) => String(version?.id || "")));
  const versionId = String(located?.version?.id || "");
  const sourceSession = sourceEditSessionForOutput(located?.output, job);
  const editableVersion = Boolean(
    versionId && realVersionIds.has(versionId)
    && located?.output?.filename && located?.output?.segments?.length
  );
  const editableSession = Boolean(sourceSession?.id && located?.output?.segments?.length);
  const editable = editableVersion || editableSession;
  button.classList.toggle("hidden", !editable);
  button.disabled = !editable;
  button.removeAttribute("data-secondary-edit-version");
  button.removeAttribute("data-secondary-edit-output");
  button.removeAttribute("data-secondary-edit-session");
  if (!editable) {
    button.onclick = null;
    return;
  }
  if (editableSession && !editableVersion) {
    button.dataset.secondaryEditSession = String(sourceSession.id);
    button.textContent = "返回版本编辑";
    button.title = "返回生成当前审核预览的 Agent 精剪时间线";
    button.setAttribute("aria-label", "返回生成当前审核预览的 Agent 精剪时间线");
    button.onclick = () => window.ClipTalkOpenAgentTimeline({ sessionId: button.dataset.secondaryEditSession })
      .catch((error) => showToast(error.message));
    return;
  }
  const versionLabel = `V${Number(located.version.number || 1)}`;
  button.dataset.secondaryEditVersion = versionId;
  button.dataset.secondaryEditOutput = String(located.output.filename);
  button.textContent = `编辑 ${versionLabel}`;
  button.title = `打开${versionLabel}的精剪时间线；修改会导出为高清新版本，不覆盖当前成片`;
  button.setAttribute("aria-label", `打开当前预览${versionLabel}成片的精剪时间线`);
  button.onclick = () => openSecondaryEditor(
    button.dataset.secondaryEditVersion,
    button.dataset.secondaryEditOutput,
  );
}

function transcriptForItem(item) {
  if (!item || !Array.isArray(timelineTranscript) || !timelineTranscript.length) return [];
  const { start, end } = segmentSourceRange(item);
  return timelineTranscript
    .filter((segment) => Number(segment.end) > start && Number(segment.start) < end)
    .sort((left, right) => Number(left.start) - Number(right.start));
}

function sourceEventContextForSegment(segment) {
  if (!segment || !currentJob?.eventGroups?.length) return null;
  const segmentId = String(segment.id || "");
  const candidateIndex = Number(segment.candidateIndex ?? segment.index);
  const { start: sourceStart, end: sourceEnd } = segmentSourceRange(segment);
  for (const group of currentJob.eventGroups) {
    const match = (group.segments || []).find((candidate) => {
      if (segmentId && String(candidate.id || "") === segmentId) return true;
      const sameCandidate = Number.isFinite(candidateIndex)
        && Number(candidate.candidateIndex ?? candidate.index) === candidateIndex;
      const sameRange = Math.abs(Number(candidate.start) - sourceStart) < .05
        && Math.abs(Number(candidate.end) - sourceEnd) < .05;
      return sameCandidate && sameRange;
    });
    if (match) return { group, segment: match };
  }
  return null;
}

function previewCompositionSourceSegment(composed, segmentIndex, sourceTime) {
  const segment = composed?.segments?.[Number(segmentIndex)];
  if (!segment) return;
  beginSourceInspection();
  const isEventGroup = (currentJob?.eventGroups || []).some((group) => String(group.id || "") === String(composed?.id || ""));
  const context = isEventGroup
    ? { group: composed, segment }
    : sourceEventContextForSegment(segment);
  previewEventSegment(context?.group || null, context?.segment || segment, { seekTime: sourceTime });
}

function renderClipTranscript(item, kind = "candidate") {
  const root = $("#clipTranscript");
  if (!root) return;
  const composed = kind === "event" || kind === "output";
  const segments = composed
    ? (item?.segments || []).flatMap((sourceSegment, composedIndex) => transcriptForItem(sourceSegment)
      .map((segment) => ({ segment, composedIndex })))
    : transcriptForItem(item).map((segment) => ({ segment, composedIndex: null }));
  const unique = [];
  const seen = new Set();
  segments.forEach((entry) => {
    const segment = entry.segment;
    const key = `${entry.composedIndex ?? "source"}:${Number(segment.start).toFixed(3)}:${Number(segment.end).toFixed(3)}`;
    if (!seen.has(key)) { seen.add(key); unique.push(entry); }
  });
  if (!unique.length) {
    root.classList.add("hidden");
    root.innerHTML = "";
    return;
  }
  root.classList.remove("hidden");
  root.innerHTML = `<header><strong>对应转写</strong><span>${unique.length} 段 · 点击定位</span></header>${unique.slice(0, 12).map((entry) => {
    const segment = entry.segment;
    const sourceStart = Number(segment.start) || 0;
    const sourceEnd = Number(segment.end) || sourceStart;
    const displayStart = composed ? compositionTimeForSource(item, entry.composedIndex, sourceStart) : sourceStart;
    const displayEnd = composed ? compositionTimeForSource(item, entry.composedIndex, sourceEnd) : sourceEnd;
    const timePrefix = kind === "output" ? "成片 " : kind === "event" ? "组合 " : "";
    const primaryAttributes = composed
      ? `data-composed-segment="${entry.composedIndex}" data-composed-source-time="${sourceStart}"`
      : `data-transcript-seek="${sourceStart}"`;
    const sourceAction = composed ? `<button type="button" class="clip-source-link" data-source-segment="${entry.composedIndex}" data-source-time="${sourceStart}" title="在源视频中查看 ${formatTime(sourceStart)} → ${formatTime(sourceEnd)}" aria-label="在源视频中查看该段转写">源片段</button>` : "";
    return `<div class="clip-transcript-row">
      <button type="button" class="clip-transcript-line" ${primaryAttributes}>
        <small>${timePrefix}${formatTime(displayStart)} → ${formatTime(displayEnd)}${segment.speaker ? ` · ${escapeHtml(segment.speaker)}` : ""}</small>
        <span class="clip-transcript-copy">${escapeHtml(segment.text || "声音片段")}</span>
      </button>${sourceAction}
    </div>`;
  }).join("")}`;
  root?.querySelectorAll("[data-transcript-seek]").forEach((button) => button.addEventListener("click", () => seekTimeline(Number(button.dataset.transcriptSeek))));
  root?.querySelectorAll("[data-composed-segment]").forEach((button) => button.addEventListener("click", () => {
    seekComposedMedia(item, Number(button.dataset.composedSegment), Number(button.dataset.composedSourceTime), kind);
  }));
  root?.querySelectorAll("[data-source-segment]").forEach((button) => button.addEventListener("click", () => {
    previewCompositionSourceSegment(item, Number(button.dataset.sourceSegment), Number(button.dataset.sourceTime));
  }));
}

function fallbackOutputEditingExplanation(item = {}, version = null) {
  const segments = Array.isArray(item.segments) ? item.segments : [];
  const target = Number(item.targetSeconds);
  const actual = Number(item.duration || item.effectiveDuration || 0);
  const sourceOrdered = segments.every((segment, index) => !index
    || segmentSourceRange(segments[index - 1]).start <= segmentSourceRange(segment).start);
  const storyPath = [...new Set(segments.map((segment) => segment.storyFunction || segment.role || "精彩镜头"))];
  const review = version?.reviewReport || item.reviewReport || {};
  const quality = item.qualityReport || {};
  return {
    title: "为什么这样剪",
    summary: item.editorialNarrative || item.reason || (segments.length
      ? `本版由 ${segments.length} 个不重复镜头组成，优先保证事件和表达完整。`
      : "该审核样片的镜头明细尚未同步；可以正常预览，但系统不会用 0 个镜头误报其结构。"),
    strategy: {
      name: cleanDisplayText(item.displayName)
        || cleanDisplayText(version?.displayName)
        || cleanDisplayText(item.title, "高光成片"),
      description: cleanDisplayText(item.strategyDescription)
        || cleanDisplayText(version?.strategyDescription, "保留真实精彩内容"),
    },
    intent: { focus: currentJob?.brief?.focus || [], include: currentJob?.brief?.includeRules || [], exclude: currentJob?.brief?.excludeRules || [] },
    selection: { eventCount: Number(item.eventCount || item.chapterCount || 0), shotCount: segments.length || null, eventTitles: [], reason: "优先保留与用户重点匹配、且可以形成完整表达的镜头。" },
    ordering: { label: sourceOrdered ? "按源视频时间顺序" : "按 AI 叙事顺序", reason: sourceOrdered ? "保持原视频的因果与时间关系。" : "依据事件职责和情绪递进重新排列。", storyPath },
    boundaries: { adjustmentCount: Number(item.boundaryAdjustments?.length || 0), reason: item.boundaryAdjustments?.length ? "已按完整对白、动作或自然停顿校正边界。" : "使用模型精修并经本地验证的自然边界。" },
    techniques: { summary: "根据镜头内容决定原速、硬切、转场和声音衔接；完整对白与高潮默认保持原速。" },
    duration: { targetSeconds: Number.isFinite(target) && target > 0 ? target : null, actualSeconds: actual, statusLabel: item.durationStatus === "under_target" ? "短于目标" : item.durationStatus === "over_target" ? "长于目标" : "已进入目标区间", reason: item.durationDeviationReason || "优先保证表达完整。" },
    omissions: item.eventReductionReason ? [item.eventReductionReason] : ["没有使用重复或低价值拖尾强行凑时长。"],
    quality: { score: review.overallScore ?? quality.score, summary: review.summary || (quality.passed === false ? "仍有需要复核的项目。" : "已通过剪辑时间线与渲染文件检查。"), recommended: Boolean(version?.recommended), recommendationReason: version?.recommendationReason || "" },
  };
}

function renderOutputEditingExplanation(item, version = null) {
  const root = $("#outputExplanation");
  if (!root) return;
  if (!item) {
    root.innerHTML = "";
    root.classList.add("hidden");
    return;
  }
  const explanation = item.editingExplanation || fallbackOutputEditingExplanation(item, version);
  const selection = explanation.selection || {};
  const ordering = explanation.ordering || {};
  const timing = explanation.duration || {};
  const boundaries = explanation.boundaries || {};
  const quality = explanation.quality || {};
  const review = version?.reviewReport || item.reviewReport || {};
  const intent = explanation.intent || {};
  const eventNames = (selection.eventTitles || []).filter(Boolean);
  const focus = (intent.focus || []).filter(Boolean);
  const requirements = [
    ...(intent.include || []).map((value) => `保留 ${value}`),
    ...(intent.exclude || []).map((value) => `排除 ${value}`),
  ];
  const qualityScore = Number(review.overallScore ?? review.calibratedScore ?? quality.score);
  const qualityLabel = Number.isFinite(qualityScore) ? `综合审片 ${Math.round(qualityScore)}/100` : "已完成综合审片";
  const dimensionLabels = {
    content: "内容", narrative: "叙事", rhythm: "节奏",
    continuity: "连续性", audiovisual: "音画", goalMatch: "目标匹配",
  };
  const reviewDimensions = Object.entries(dimensionLabels).map(([key, label]) => ({
    label,
    score: Number(review.scores?.[key]),
  })).filter((entry) => Number.isFinite(entry.score));
  const rubricScore = Number(review.rubricScore);
  const reviewPenalty = Number(review.deterministicPenalty);
  const calibrationLabel = [
    Number.isFinite(rubricScore) ? `维度加权 ${rubricScore.toFixed(1)}` : "",
    Number.isFinite(reviewPenalty) && reviewPenalty > 0 ? `根因校准 -${reviewPenalty.toFixed(1)}` : "",
  ].filter(Boolean).join(" · ");
  const targetLabel = timing.targetSeconds
    ? `${Number(timing.actualSeconds || 0).toFixed(1)} 秒 / 目标 ${Number(timing.targetSeconds).toFixed(1)} 秒`
    : `${Number(timing.actualSeconds || 0).toFixed(1)} 秒 / 自动时长`;
  const omissions = (explanation.omissions || []).filter(Boolean);
  const eventCountKnown = selection.eventCount !== null && selection.eventCount !== undefined && Number(selection.eventCount) > 0;
  const shotCountKnown = selection.shotCount !== null && selection.shotCount !== undefined && Number(selection.shotCount) > 0;
  const eventShotLabel = [
    eventCountKnown ? `${Number(selection.eventCount)} 个事件` : "",
    shotCountKnown ? `${Number(selection.shotCount)} 个镜头` : "镜头明细待同步",
  ].filter(Boolean).join(" · ");
  const orderLabel = escapeHtml(ordering.label || "按当前剪辑顺序");
  root.innerHTML = `
    <div class="output-explanation-toggle-card">
      <span><small>剪辑说明</small><strong>默认收起，按需查看</strong></span>
      <button type="button" data-toggle-output-explanation aria-expanded="false">查看剪辑说明</button>
    </div>
    <div class="output-explanation-body hidden">
      <header class="output-explanation-head">
        <div><small>剪辑说明</small><strong>${escapeHtml(explanation.title || "为什么这样剪")}</strong></div>
        <span>${quality.recommended ? "推荐版本 · " : ""}依据可查看</span>
      </header>
      <p class="output-explanation-summary">${escapeHtml(explanation.summary || "根据已验证的画面、声音和用户要求生成。")}</p>
      <div class="output-decision-summary">
        <span><small>选片</small><strong>${escapeHtml(eventShotLabel)}</strong></span>
        <span><small>顺序</small><strong>${orderLabel}</strong></span>
        <span><small>时长 / 质检</small><strong>${escapeHtml(targetLabel)} · ${escapeHtml(qualityLabel)}</strong></span>
      </div>
      <details class="output-explanation-details">
        <summary><span>查看完整剪辑依据</span><em>选片、顺序、边界、取舍与质检</em></summary>
        <div class="output-decision-ledger">
          <section><small>选片</small><strong>${escapeHtml(eventShotLabel)}</strong><p>${escapeHtml(eventNames.length ? eventNames.slice(0, 3).join("、") : selection.reason || "保留高价值且不重复的镜头")}</p></section>
          <section><small>顺序</small><strong>${orderLabel}</strong><p>${escapeHtml((ordering.storyPath || []).length ? ordering.storyPath.join(" → ") : ordering.reason || "保持叙事关系")}</p></section>
          <section><small>时长</small><strong>${escapeHtml(targetLabel)}</strong><p>${escapeHtml(`${timing.statusLabel || "已校验"}。${timing.reason || "优先保证表达完整。"}`)}</p></section>
          <section><small>边界与手法</small><strong>${Number(boundaries.adjustmentCount || 0)} 处安全校正</strong><p>${escapeHtml(explanation.techniques?.summary || boundaries.reason || "使用自然切点")}</p></section>
        </div>
        ${(focus.length || requirements.length) ? `<div class="output-intent-line"><small>对应需求</small><span>${[...focus.map((value) => `重点 ${value}`), ...requirements].slice(0, 8).map((value) => `<b>${escapeHtml(value)}</b>`).join("")}</span></div>` : ""}
        <section class="output-quality-detail"><header><span>取舍与质检</span><b>${escapeHtml(qualityLabel)}</b></header>${reviewDimensions.length ? `<div class="output-review-dimensions" aria-label="综合审片分项">${reviewDimensions.map((entry) => `<span><small>${escapeHtml(entry.label)}</small><b>${Math.round(entry.score)}</b></span>`).join("")}</div>` : ""}${calibrationLabel ? `<p class="output-review-calibration">${escapeHtml(calibrationLabel)}；同一根因只计一次，关键问题另由质量门决定是否可用。</p>` : ""}<p>${escapeHtml(quality.recommendationReason || quality.summary || "已完成成片检查。")}</p>${omissions.length ? `<ul>${omissions.map((value) => `<li>${escapeHtml(value)}</li>`).join("")}</ul>` : ""}</section>
      </details>
    </div>`;
  root.querySelector("[data-toggle-output-explanation]")?.addEventListener("click", (event) => {
    const button = event.currentTarget;
    const body = root.querySelector(".output-explanation-body");
    const expanded = body?.classList.toggle("hidden") === false;
    button?.setAttribute("aria-expanded", String(expanded));
    if (button) button.textContent = expanded ? "收起剪辑说明" : "查看剪辑说明";
  });
  root.classList.remove("hidden");
}

function conciseEvidenceText(value, maxLength = 110) {
  const text = String(value || "").replace(/\s+/g, " ").trim();
  if (!text) return "";
  if (text.length <= maxLength) return text;
  return `${text.slice(0, Math.max(0, maxLength - 1)).trim()}…`;
}

function outputEvidenceSummary(output = {}, version = null) {
  const explanation = output.editingExplanation || fallbackOutputEditingExplanation(output, version);
  const review = version?.reviewReport || output.reviewReport || {};
  const qualityScore = Number(
    review.overallScore
      ?? review.calibratedScore
      ?? output.qualityReport?.score
      ?? output.score,
  );
  const schedule = compositionSchedule(output);
  const segments = Array.isArray(output.segments) ? output.segments : [];
  const duration = Number(output.duration || output.effectiveDuration || 0);
  const sourceStarts = schedule.map((entry) => Number(entry.sourceStart)).filter(Number.isFinite);
  const sourceEnds = schedule.map((entry) => Number(entry.sourceEnd)).filter(Number.isFinite);
  const sourceRange = sourceStarts.length && sourceEnds.length
    ? `${formatTime(Math.min(...sourceStarts))} → ${formatTime(Math.max(...sourceEnds))}`
    : "";
  const selection = explanation.selection || {};
  const ordering = explanation.ordering || {};
  const timing = explanation.duration || {};
  const reason = conciseEvidenceText(
    explanation.summary
      || output.editorialNarrative
      || output.reason
      || selection.reason
      || "已根据用户目标、可用片段和质检结果生成当前成片。",
  );
  return {
    explanation,
    reason,
    shotCount: segments.length || schedule.length || Number(selection.shotCount || 0),
    duration,
    sourceRange,
    orderLabel: ordering.label || (schedule.length ? "按当前成片顺序" : "按当前版本"),
    durationLabel: duration > 0 ? `${duration.toFixed(1)} 秒` : "时长已校验",
    qualityLabel: Number.isFinite(qualityScore) ? `审片 ${Math.round(qualityScore)}/100` : "已质检",
    timingLabel: timing.targetSeconds
      ? `${Number(timing.actualSeconds || duration || 0).toFixed(1)} 秒 / 目标 ${Number(timing.targetSeconds).toFixed(1)} 秒`
      : `${Number(timing.actualSeconds || duration || 0).toFixed(1)} 秒 / 自动时长`,
  };
}

function clearOutputEvidenceStrip() {
  const strip = $("#outputEvidenceStrip");
  if (!strip) return;
  if (!strip.classList.contains("hidden") || strip.innerHTML) {
    strip.innerHTML = "";
    strip.classList.add("hidden");
    scheduleMediaFrameFit(true);
  }
}

function renderOutputEvidenceStrip(output, version = null, { contentMode = false } = {}) {
  const strip = $("#outputEvidenceStrip");
  if (!strip || !output) return;
  if (document.body.classList.contains("ct-workbench-v4")) {
    clearOutputEvidenceStrip();
    return;
  }
  const summary = outputEvidenceSummary(output, version);
  const versionIdentity = outputVersionIdentity(version, output);
  const explanation = summary.explanation || {};
  const selection = explanation.selection || {};
  const boundaries = explanation.boundaries || {};
  const techniques = explanation.techniques || {};
  const omissions = (explanation.omissions || []).filter(Boolean);
  const review = version?.reviewReport || output.reviewReport || {};
  const dimensionLabels = {
    content: "内容", narrative: "叙事", rhythm: "节奏",
    continuity: "连续性", audiovisual: "音画", goalMatch: "目标匹配",
  };
  const reviewDimensions = Object.entries(dimensionLabels).map(([key, label]) => ({
    label,
    score: Number(review.scores?.[key]),
  })).filter((entry) => Number.isFinite(entry.score));
  const rubricScore = Number(review.rubricScore);
  const reviewPenalty = Number(review.deterministicPenalty);
  const calibrationLabel = [
    Number.isFinite(rubricScore) ? `维度加权 ${rubricScore.toFixed(1)}` : "",
    Number.isFinite(reviewPenalty) && reviewPenalty > 0 ? `根因校准 -${reviewPenalty.toFixed(1)}` : "",
  ].filter(Boolean).join(" · ");
  const chips = [
    `${versionIdentity} · ${contentMode ? "内容视频" : "成片"}依据`,
    summary.shotCount ? `${summary.shotCount} 个${contentMode ? "内容片段" : "镜头"}` : "",
    summary.durationLabel,
    summary.qualityLabel,
  ].filter(Boolean);
  const timelineActionLabel = currentOutputIsReviewSample(output, version)
    ? "查看样片时间线"
    : "查看成片时间线";
  strip.innerHTML = `
    <div class="output-evidence-strip-copy">
      <div class="output-evidence-strip-chips">${chips.map((chip) => `<span>${escapeHtml(chip)}</span>`).join("")}</div>
      <p>${escapeHtml(summary.reason)}</p>
    </div>
    <div class="output-evidence-strip-actions">
      <button type="button" data-output-evidence-timeline>${timelineActionLabel}</button>
      <details class="output-evidence-popover">
        <summary>查看说明</summary>
        <div class="output-evidence-popover-body">
          <header><small>成片说明</small><strong>${escapeHtml(explanation.title || "为什么这样剪")}</strong></header>
          <p>${escapeHtml(explanation.summary || summary.reason)}</p>
          <dl>
            <div><dt>选片</dt><dd>${escapeHtml(selection.reason || `${summary.shotCount || 0} 个镜头已采用`)}</dd></div>
            <div><dt>顺序</dt><dd>${escapeHtml(summary.orderLabel)}</dd></div>
            <div><dt>时长</dt><dd>${escapeHtml(summary.timingLabel)}</dd></div>
            ${summary.sourceRange ? `<div><dt>源片范围</dt><dd>${escapeHtml(summary.sourceRange)}</dd></div>` : ""}
            <div><dt>边界</dt><dd>${escapeHtml(boundaries.reason || `${Number(boundaries.adjustmentCount || 0)} 处安全校正`)}</dd></div>
            <div><dt>手法</dt><dd>${escapeHtml(techniques.summary || "按镜头内容处理节奏、转场与声音衔接")}</dd></div>
          </dl>
          ${reviewDimensions.length || calibrationLabel ? `<section class="output-evidence-quality"><small>质检</small>${reviewDimensions.length ? `<div>${reviewDimensions.map((entry) => `<span><b>${escapeHtml(entry.label)}</b>${Math.round(entry.score)}</span>`).join("")}</div>` : ""}${calibrationLabel ? `<p>${escapeHtml(calibrationLabel)}</p>` : ""}</section>` : ""}
          ${omissions.length ? `<section><small>取舍</small><ul>${omissions.slice(0, 3).map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul></section>` : ""}
        </div>
      </details>
    </div>
  `;
  strip.querySelector("[data-output-evidence-timeline]")?.addEventListener("click", () => {
    setReviewLowerPanelMode("timeline", { scroll: true, compact: false });
    updateTimeline();
  });
  strip.classList.remove("hidden");
  scheduleMediaFrameFit(true);
}

function renderClipEvidence(item, kind = "candidate", version = null) {
  if (contentBoundaryTimelineEdit) closeContentBoundaryTimelineEdit({ restorePreview: false });
  hideContentDetailModeSwitch();
  if (kind !== "output") clearOutputEvidenceStrip();
  const meta = $("#clipEvidenceMeta");
  const list = $("#clipEvidence");
  if (!meta || !list) return;
  renderOutputEditingExplanation(kind === "output" ? item : null, version);
  renderClipTranscript(item, kind);
  const evidence = item?.audioEvidence || {};
  const chips = [];
  if (kind !== "output") chips.push(`判断依据 ${(item?.evidence || []).length} 项`);
  if (evidence.transcriptExcerpt || evidence.speakerTurns?.length) chips.push("SenseVoice 已对齐");
  itemSpeakers(item).forEach((speaker) => chips.push(`说话人 ${speaker}`));
  (evidence.emotions || []).slice(0, 2).forEach((emotion) => chips.push(`情绪 ${emotion}`));
  (evidence.audioEvents || []).slice(0, 2).forEach((event) => chips.push(`声音 ${event}`));
  if (kind === "candidate" || kind === "event" || kind === "segment") chips.push("尚未写入成片");
  if (kind === "output" && version) chips.push(`版本 V${Number(version.number || 1)}`);
  if (kind === "output" && item?.sourceDuration) chips.push(`源片段 ${Number(item.sourceDuration).toFixed(1)} 秒 → 成片 ${Number(item.effectiveDuration || item.duration || 0).toFixed(1)} 秒`);
  if (kind === "output" && item?.cutaways?.length) chips.push(`插入镜头 ${item.cutaways.length} 个`);
  meta.innerHTML = chips.map((chip) => `<span>${escapeHtml(chip)}</span>`).join("");
  if ((kind === "event" || kind === "output") && item.segments?.length) {
    const timePrefix = kind === "output" ? "成片 " : "组合 ";
    const schedule = compositionSchedule(item);
    list.innerHTML = schedule.map((entry, index) => {
      const segment = entry.segment;
      const sourceRange = `${formatTime(entry.sourceStart)} → ${formatTime(entry.sourceEnd)}`;
      const rate = Number(segment.playbackRate || 1);
      const transition = segment.transitionIn?.type === "fade_black" ? "淡黑" : segment.transitionIn?.type === "dissolve" ? "叠化" : "硬切";
      const bridge = segment.audioBridge?.type === "j_cut" ? "J-cut" : segment.audioBridge?.type === "l_cut" ? "L-cut" : "同步音画";
      return `<li class="evidence-item-row"><button type="button" class="evidence-main-button" data-composed-segment="${index}" data-composed-source-time="${entry.sourceStart}"><span class="evidence-item-index">${String(index + 1).padStart(2, "0")}</span><span class="evidence-item-content"><strong>${escapeHtml(segment.storyFunction || segment.role || "精彩镜头")}</strong><small>${timePrefix}${formatTime(entry.outputStart)} → ${formatTime(entry.outputEnd)} · ${rate.toFixed(rate === 1 ? 0 : 2)}× · ${transition} · ${bridge}</small>${segment.reason ? `<em>${escapeHtml(segment.reason)}</em>` : ""}</span></button><button type="button" class="clip-source-link" data-source-segment="${index}" data-source-time="${entry.sourceStart}" title="在源视频中查看 ${sourceRange}" aria-label="在源视频中查看${escapeHtml(segment.role || `镜头 ${index + 1}`)}">源片段</button></li>`;
    }).join("");
  } else {
    list.innerHTML = (item?.evidence || []).map((text, index) => `<li><button type="button" class="evidence-main-button" data-evidence-seek="${Number(item.start) || 0}"><small class="evidence-source-label">画面证据 ${String(index + 1).padStart(2, "0")}</small><span class="evidence-item-copy">${escapeHtml(text)}</span></button></li>`).join("");
  }
  list?.querySelectorAll("[data-evidence-seek]").forEach((button) => button.addEventListener("click", () => seekTimeline(Number(button.dataset.evidenceSeek))));
  list?.querySelectorAll("[data-composed-segment]").forEach((button) => button.addEventListener("click", () => {
    seekComposedMedia(item, Number(button.dataset.composedSegment), Number(button.dataset.composedSourceTime), kind);
  }));
  list?.querySelectorAll("[data-source-segment]").forEach((button) => button.addEventListener("click", () => {
    previewCompositionSourceSegment(item, Number(button.dataset.sourceSegment), Number(button.dataset.sourceTime));
  }));
}

async function downloadVideoAsset(url, filename) {
  if (fragmentDownloadBusy) return;
  fragmentDownloadBusy = true;
  const button = $("#keepButton");
  const previousLabel = button?.textContent || "下载该片段";
  if (button) button.textContent = "正在准备下载…";
  try {
    const response = await fetch(url, { credentials: "same-origin" });
    if (!response.ok) throw new Error(`片段下载失败（${response.status}）`);
    const blob = await response.blob();
    const blobUrl = URL.createObjectURL(blob);
    const link = document.createElement("a");
    link.href = blobUrl;
    link.download = filename || "高光片段.mp4";
    document.body.appendChild(link);
    link.click();
    link.remove();
    window.setTimeout(() => URL.revokeObjectURL(blobUrl), 1000);
  } catch (error) {
    showToast(error.message || "片段下载失败");
  } finally {
    fragmentDownloadBusy = false;
    if (button) button.textContent = (currentCandidate || currentEventSegment || currentEventGroup) ? "下载该片段" : previousLabel;
  }
}

function downloadCurrentFragment() {
  if (!currentJob) return;
  if (currentEventGroup && !currentEventSegment) {
    const title = currentEventGroup.title || "事件高光";
    return downloadVideoAsset(
      `/api/jobs/${currentJob.id}/event-groups/${encodeURIComponent(currentEventGroup.id)}/preview?download=1`,
      `${title}.mp4`,
    );
  }
  const item = currentEventSegment || currentCandidate;
  if (!item) return;
  const start = Number(item.start);
  const end = Number(item.end);
  const title = currentEventSegment
    ? `${currentEventGroup?.title || "事件"}-${item.role || "精彩镜头"}`
    : (item.title || "高光片段");
  const url = `/api/jobs/${currentJob.id}/fragment?start=${encodeURIComponent(start.toFixed(3))}&end=${encodeURIComponent(end.toFixed(3))}&title=${encodeURIComponent(title)}`;
  downloadVideoAsset(url, `${title}.mp4`);
}

function oneOffOutputEntries(job = currentJob) {
  return orderedJobOutputs(job).map(({ item, version }) => {
    const presentation = autoVersionPresentation(job, version);
    const previewOnly = Boolean(item.previewOnly || version.previewOnly);
    return {
      item,
      version,
      previewOnly,
      label: cleanDisplayText(item.displayTitle)
        || cleanDisplayText(presentation.displayName)
        || cleanDisplayText(item.title, outputVersionIdentity(version, item)),
    };
  });
}

function syncOneOffFinalizeAction(job = currentJob) {
  const button = $("#finalizeOneOffButton");
  if (!button) return;
  const entries = job ? oneOffOutputEntries(job) : [];
  const visible = job?.storageMode === "one_off" && entries.length > 0 && !isActiveJobStatus(job.status);
  button.classList.toggle("hidden", !visible);
  if (visible) {
    const formalCount = entries.filter((entry) => !entry.previewOnly).length;
    button.textContent = formalCount ? "保存成片并清理素材" : "请先生成成片";
    button.title = formalCount ? "选择要保存的正式成片，然后清理一次性工程" : "审核样片不能作为最终文件，请先生成成片";
    button.onclick = finalizeOneOffTask;
  } else {
    button.onclick = null;
  }
}

function syncLibrarySaveAction(job = currentJob, output = currentOutput) {
  let button = $("#saveToLibraryButton");
  if (!button) {
    const download = $("#downloadButton");
    if (!download?.parentElement) return;
    button = document.createElement("button");
    button.id = "saveToLibraryButton";
    button.className = "download-button library-save-button hidden";
    button.type = "button";
    download.before(button);
  }
  const located = output?.filename ? locateJobOutput(output.filename, job) : null;
  const selectedOutput = located?.output || output;
  const previewOnly = Boolean(selectedOutput?.previewOnly || located?.version?.previewOnly);
  const visible = Boolean(
    job?.id
    && selectedOutput?.filename
    && selectedOutput.capabilities?.canKeep !== false
    && !previewOnly
    && String(selectedOutput.filename).toLowerCase().endsWith(".mp4")
  );
  const kept = Boolean(selectedOutput?.kept);
  button.classList.toggle("hidden", !visible);
  button.classList.toggle("is-kept", visible && kept);
  button.disabled = !visible || kept || button.dataset.saving === "true";
  button.textContent = button.dataset.saving === "true" ? "正在保存…" : kept ? "✓ 已长期保留" : "长期保留";
  button.title = kept
    ? "这份高清成片已保存为独立副本；删除原工程不会影响它"
    : "保存一份独立高清副本到成片库，不会清理当前可编辑工程";
  button.setAttribute("aria-label", kept ? "当前高清成片已长期保留" : "将当前高清成片长期保留");
  button.onclick = visible && !kept ? () => saveCurrentOutputToLibrary(selectedOutput) : null;
}

async function saveCurrentOutputToLibrary(output = currentOutput) {
  if (!currentJob?.id || !output?.filename || output.previewOnly || actionBusy) return { ok: false, error: "当前不能保存，请等待当前操作完成" };
  if (output.capabilities?.canKeep === false) return { ok: false, error: output.capabilities.disabledReason?.keep || "当前文件不可保存" };
  const actionToken = captureJobAction();
  const button = $("#saveToLibraryButton");
  actionBusy = true;
  if (button) button.dataset.saving = "true";
  syncLibrarySaveAction(currentJob, output);
  try {
    const { job } = await apiJson(
      `/api/jobs/${encodeURIComponent(actionToken.jobId)}/outputs/${encodeURIComponent(output.filename)}/keep`,
      { method: "POST", headers: { "Content-Type": "application/json" }, body: { kept: true } },
    );
    if (!commitJobAction(job, actionToken)) return { ok: true, jobId: actionToken.jobId, stale: true };
    try { await window.ClipTalkAppShell?.loadLibrary?.({ force: true }); }
    catch { /* The independent copy was saved; list refresh can be retried. */ }
    if (jobActionStillCurrent(actionToken)) showToast("已长期保留；删除原工程也不会影响这份成片。", "success");
    return { ok: true, jobId: actionToken.jobId, filename: output.filename };
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message || "成片保存失败");
    return { ok: false, error: error.message || "成片保存失败" };
  } finally {
    if (button) delete button.dataset.saving;
    if (jobActionStillCurrent(actionToken)) {
      actionBusy = false;
      syncLibrarySaveAction(currentJob, currentOutput);
    }
  }
}

async function finalizeOneOffTask() {
  if (!currentJob || currentJob.storageMode !== "one_off" || actionBusy) return;
  const entries = oneOffOutputEntries();
  if (!entries.length) return void showToast("当前还没有可保留的成片");
  const currentFilename = String(currentOutput?.filename || "");
  const formalEntries = entries.filter((entry) => !entry.previewOnly);
  const defaultFilename = formalEntries.some((entry) => entry.item.filename === currentFilename)
    ? currentFilename
    : String(formalEntries[0]?.item.filename || "");
  const confirmation = await requestActionConfirmation({
    title: "保存正式成片并清理一次性任务",
    summary: "勾选要保存的高清 MP4。成片副本保存成功后，源视频、时间线、分析结果和其他输出将被删除。",
    details: ["保存的 MP4 不依赖原工程", "字幕草稿不会单独保存", "清理后任务无法继续编辑"],
    warning: formalEntries.length ? "这是不可恢复的工程清理操作。" : "当前只有审核样片，请先生成成片。",
    confirmLabel: "保存并清理",
    selectionItems: entries.map((entry) => ({
      value: entry.item.filename,
      label: entry.label,
      meta: entry.previewOnly
        ? "审核样片不可保留 · 请先选择该样片并生成成片"
        : `${Number(entry.item.duration || 0).toFixed(1)} 秒 · 高清 MP4`,
      disabled: entry.previewOnly,
      checked: entry.item.filename === defaultFilename,
    })),
  });
  if (!confirmation?.confirmed || !confirmation.selectedValues?.length) return;
  const jobId = String(currentJob.id);
  actionBusy = true;
  syncOneOffFinalizeAction();
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/finalize-one-off`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: { filenames: confirmation.selectedValues },
    });
    resetWorkspace();
    await loadHomeTasks();
    showToast(`已保存 ${result.keptOutputs?.length || confirmation.selectedValues.length} 条正式成片，并清理一次性工程`, "success");
  } catch (error) {
    showToast(error.message || "一次性任务清理失败");
  } finally {
    actionBusy = false;
    syncOneOffFinalizeAction();
  }
}

function selectOutput(filename, autoplay = false, seekTime = null) {
  if (!currentJob) return;
  const located = locateJobOutput(filename);
  if (!located) return;
  sourceInspectionContext = null;
  $("#returnInspectedVersion")?.classList.add("hidden");
  const { output, version } = located;
  selectedOutputByJob.set(String(currentJob.id), output.filename);
  candidatePreviewToken += 1;
  currentOutput = output;
  currentCandidate = null;
  currentEventGroup = null;
  currentEventSegment = null;
  candidatePreviewEnd = null;
  viewerMediaKind = "output";
  syncOutputPreviewMode();
  timelineCoordinateSpace = "output";
  timelineSelectedOutputSegmentIndex = null;
  timelineManualSelectMode = false;
  timelineViewStart = 0;
  timelineViewEnd = timelineOutputDurationValue(output);
  timelineReviewFollow = false;
  timelineMediaRenderKey = "";
  waveformRenderKey = "";
  stopSourcePreviewPolling();
  const cacheKey = encodeURIComponent(output.versionCreatedAt || version.createdAt || output.filename || "output");
  clearPlayerNotice();
  applyMediaAspect(
    viewerShell,
    Number(output.width || 0) || currentJob.videoInfo?.width,
    Number(output.height || 0) || currentJob.videoInfo?.height,
  );
  const outputUrl = String(output.previewUrl || output.videoUrl || "").trim();
  if (!outputUrl || outputUrl === "undefined" || outputUrl === "null") {
    setMainVideoSource("");
    showPlayerNotice("当前成片暂时没有可用的预览地址");
  } else {
    const separator = outputUrl.includes("?") ? "&" : "?";
    setMainVideoSource(`${outputUrl}${separator}v=${cacheKey}`);
  }
  if ($("#mainVideo")) $("#mainVideo").poster = String(output.coverUrl || "");
  const presentation = autoVersionPresentation(currentJob, version);
  const contentMode = taskModePresentation(currentJob).key === "content_extract";
  const displayTitle = cleanDisplayText(output.displayTitle)
    || (currentJob.autoComposition?.status === "completed" && cleanDisplayText(presentation.displayName)
      ? `${cleanDisplayText(presentation.displayName)} · ${cleanDisplayText(presentation.sourceLabel, "AI")}`
      : cleanDisplayText(output.title, contentMode ? "内容视频" : "审核样片"));
  const qualityLabel = outputVersionQualityLabel(version, output);
  const kindLabel = version.variantKind === "formal_export" || (!version.previewOnly && !output.previewOnly) ? "高清" : version.variantKind === "repair" ? "返修" : "样片";
  const versionIdentity = outputVersionIdentity(version, output);
  const previewOnly = Boolean(output.previewOnly || version.previewOnly);
  const reviewSample = currentOutputIsReviewSample(output, version);
  $("#viewerBadge").textContent = reviewSample
    ? `${versionIdentity} · 审核样片预览`
    : contentMode
    ? `${versionIdentity} · 内容视频`
    : `${versionIdentity} · ${kindLabel} · ${qualityLabel}`;
  renderOutputPreviewSelector(currentJob);
  const viewSelect = $("#videoViewSelect");
  if (viewSelect && [...viewSelect.options].some((option) => option.value === filename)) viewSelect.value = filename;
  $("#reviewKicker").textContent = reviewSample ? "审核样片预览" : `${versionIdentity} · ${contentMode ? "内容视频" : "高光成片"}`;
  $("#reviewTitle").textContent = displayTitle;
  const download = $("#downloadButton");
  const downloadUrl = String(output.downloadUrl || "").trim();
  const downloadAvailable = Boolean(downloadUrl && downloadUrl !== "undefined" && downloadUrl !== "null");
  download.classList.toggle("hidden", !downloadAvailable);
  download.classList.toggle("review-sample-download", downloadAvailable && previewOnly);
  if (downloadAvailable) {
    const sampleIdentity = versionIdentity.startsWith("审核样片") ? versionIdentity : `${versionIdentity} 审核样片`;
    download.href = downloadUrl;
    download.download = output.downloadFilename || output.filename || `${versionIdentity}.mp4`;
    download.dataset.outputVersion = versionIdentity;
    download.textContent = previewOnly ? `下载 ${sampleIdentity}` : `下载 ${versionIdentity} MP4`;
    download.title = previewOnly
      ? `下载当前预览的 ${sampleIdentity}`
      : `下载当前预览的 ${versionIdentity} 高清成片`;
    download.setAttribute("aria-label", previewOnly
      ? `下载当前预览的 ${sampleIdentity}`
      : `下载当前预览的 ${versionIdentity} 高清 MP4`);
  } else {
    download.removeAttribute("href");
    download.removeAttribute("download");
    download.removeAttribute("data-output-version");
  }
  const coverDownload = $("#coverDownloadButton");
  const packageDownload = $("#packageDownloadButton");
  const coverIntro = $("#coverIntroButton");
  coverDownload?.classList.toggle("hidden", !output.coverUrl);
  packageDownload?.classList.toggle("hidden", !output.packageUrl);
  coverIntro?.classList.toggle("hidden", !output.coverIntroAvailable);
  if (coverDownload && output.coverUrl) {
    coverDownload.href = `${output.coverUrl}?download=1`;
    coverDownload.download = `${String(output.downloadFilename || output.filename).replace(/\.[^.]+$/, "")}_封面.jpg`;
  }
  if (packageDownload && output.packageUrl) {
    packageDownload.href = output.packageUrl;
    packageDownload.download = `${String(output.downloadFilename || output.filename).replace(/\.[^.]+$/, "")}_发布包.zip`;
  }
  if (coverIntro) coverIntro.onclick = () => createCoverIntroOutput(version, output);
  const finalize = $("#finalizePreviewButton");
  finalize?.classList.toggle("hidden", !previewOnly);
  if (finalize) {
    finalize.onclick = () => window.ClipTalkVersionAction(output.filename, "export");
    finalize.textContent = "生成成片";
    finalize.title = "生成当前样片的成片版本";
    finalize.setAttribute("aria-label", "生成当前样片的成片版本");
  }
  syncOneOffFinalizeAction(currentJob);
  syncLibrarySaveAction(currentJob, output);
  const subtitle = $("#subtitleButton");
  subtitle.dataset.subtitleUrl = `/api/jobs/${currentJob.id}/outputs/${encodeURIComponent(output.filename)}/subtitles?format=srt`;
  // Keep the subtitle download independent from the MP4 link.  Some browsers
  // reuse the previous attachment name when an anchor is updated in place;
  // setting an explicit .srt filename makes the downloaded file unambiguous.
  subtitle.dataset.downloadName = `${String(output.downloadFilename || output.filename).replace(/\.[^.]+$/, "")}.srt`;
  subtitle.onclick = async (event) => {
    event.preventDefault();
    if (subtitle.dataset.downloading === "true") return;
    subtitle.dataset.downloading = "true";
    const originalLabel = subtitle.textContent;
    subtitle.textContent = "正在准备 SRT…";
    try {
      const response = await fetch(subtitle.dataset.subtitleUrl, { credentials: "same-origin" });
      if (!response.ok) throw new Error(`字幕下载失败（${response.status}）`);
      const content = await response.text();
      const blob = new Blob([content], { type: "application/x-subrip;charset=utf-8" });
      const blobUrl = URL.createObjectURL(blob);
      const link = document.createElement("a");
      link.href = blobUrl;
      link.download = subtitle.dataset.downloadName || "subtitles.srt";
      document.body.appendChild(link);
      link.click();
      link.remove();
      window.setTimeout(() => URL.revokeObjectURL(blobUrl), 1000);
    } catch (error) {
      showToast(error.message || "字幕下载失败");
    } finally {
      subtitle.dataset.downloading = "false";
      subtitle.textContent = originalLabel;
    }
  };
  // 字幕下载暂时不在界面展示，保留生成逻辑以便后续重新启用。
  subtitle.classList.add("hidden");
  subtitle.textContent = "下载字幕 SRT";
  const evidencePanel = $("#evidencePanel");
  evidencePanel?.classList.add("hidden", "evidence-placeholder");
  evidencePanel?.classList.remove(
    "candidate-mode",
    "montage-mode",
    "output-mode",
    "evidence-in-gutter",
    "evidence-gutter-left",
    "evidence-gutter-right",
    "evidence-compact",
    "evidence-expanded",
    "content-boundary-view",
  );
  hideContentDetailModeSwitch();
  $("#clipTime").textContent = output.segments?.length
    ? `${output.segments.length} 个${contentMode ? "内容片段" : "镜头"} · ${contentMode ? "视频" : "成片"} ${Number(output.duration).toFixed(1)} 秒`
    : `${formatTime(output.start)} → ${formatTime(output.end)} · ${Number(output.duration).toFixed(1)} 秒`;
  $("#clipTitle").textContent = output.segments?.length ? (contentMode ? "内容视频结构" : "成片结构") : displayTitle;
  const qualityScore = Number(
    version?.reviewReport?.overallScore
      ?? output.reviewReport?.overallScore
      ?? output.qualityReport?.score
      ?? output.score,
  );
  $("#clipScore").textContent = Number.isFinite(qualityScore) ? `综合审片 ${Math.round(qualityScore)}/100` : outputVersionQualityLabel(version, output);
  $("#clipScore").title = "综合审片分：内容、叙事、节奏、连续性、音画与目标匹配的加权结果";
  $("#clipReason").textContent = output.reason;
  if ($("#clipEvidenceMeta")) $("#clipEvidenceMeta").innerHTML = "";
  if ($("#clipEvidence")) $("#clipEvidence").innerHTML = "";
  $("#clipTranscript")?.classList.add("hidden");
  if ($("#outputExplanation")) {
    $("#outputExplanation").innerHTML = "";
    $("#outputExplanation").classList.add("hidden");
  }
  renderOutputEvidenceStrip(output, version, { contentMode });
  // Versioning and archival are separate concepts.  A rendered output is
  // already part of V1/V2; this button only creates/removes an extra copy.
  // The legacy keep-library action was removed from the UI. Downloads remain
  // available through the dedicated MP4 button.
  $("#keepButton")?.classList.add("hidden");
  $("#replaceButton")?.classList.add("hidden");
  $("#replaceButton").disabled = false;
  $("#replaceButton").title = "";
  document.querySelectorAll(".clip-card").forEach((card) => card.classList.toggle("active", card.dataset.filename === filename));
  const selectedAutoVersion = [...document.querySelectorAll(".auto-version-button")]
    .find((button) => button.dataset.autoOutput === filename) || null;
  document.querySelectorAll(".auto-version-button").forEach((button) => {
    const selected = button === selectedAutoVersion;
    button.classList.toggle("active", selected);
    if (button.classList.contains("clip-version-button")) button.setAttribute("aria-pressed", selected ? "true" : "false");
  });
  setOutputVersionBeamSelection(selectedAutoVersion);
  timelineViewport?.classList.remove("output-version-switching");
  if (timelineViewport) {
    // Restart a short, low-amplitude transition so a version switch is
    // perceptible without changing the user's zoom window.
    void timelineViewport.offsetWidth;
    timelineViewport.classList.add("output-version-switching");
    window.setTimeout(() => timelineViewport?.classList.remove("output-version-switching"), 220);
  }
  updateTimeline();
  syncReviewSelectionClasses();
  if (seekTime !== null && Number.isFinite(Number(seekTime))) seekCurrentMediaTime(Number(seekTime), { autoplay });
  else if (autoplay) safePlay();
}

async function finalizePreviewVersion(version, output = null) {
  if (!currentJob || !version?.id || actionBusy) return;
  output = output || (version.outputs?.length === 1 ? version.outputs[0] : null);
  if (!output?.filename) return void showToast("请选择要导出的具体文件");
  const actionToken = captureJobAction();
  let snapshot = window.ClipTalkDelivery.capture(currentJob, version, output);
  const stillCurrent = () => jobActionStillCurrent(actionToken) && window.ClipTalkDelivery.isCurrent(snapshot, currentJob);
  const quality = outputVersionQuality(version, output);
  const review = version?.reviewReport || output?.reviewReport || {};
  const gate = version?.qualityGate || output?.qualityGate || review?.qualityGate || {};
  const risky = quality !== "passed";
  const issueDetails = (gate.reasons || []).slice(0, 3).map(String);
  const confirmed = await requestActionConfirmation({
    title: "生成成片",
    summary: `当前样片 · ${version.title || output?.title || "未命名版本"} · ${formatTime(Number(output?.duration) || 0)}`,
    details: risky ? issueDetails : [],
    warning: risky && issueDetails.length ? "请确认以上提示后继续。" : "",
    confirmLabel: "开始生成",
    exportSnapshot: snapshot,
  });
  if (!confirmed) return;
  if (!stillCurrent()) return void showToast("任务或版本已变化，本次生成已取消，请重新确认");
  snapshot = confirmed.exportSnapshot || snapshot;
  const { subtitleMode, subtitleStyle } = snapshot;
  let subtitleDraftId = null;
  let subtitleSnapshot = null;
  if (subtitleMode === "burn") {
    if (!output?.segments?.length) return void showToast("当前样片缺少可校对的时间线");
    const draft = await reviewSubtitlesBeforeRender([{ segments: snapshot.segments }], subtitleStyle);
    if (!draft) return;
    if (!stillCurrent()) return void showToast("任务或版本已变化，本次生成已取消，请重新确认");
    subtitleDraftId = draft.id;
    subtitleSnapshot = draft;
  }
  if (!stillCurrent()) return void showToast("任务或版本已变化，本次生成已取消，请重新确认");
  if (actionBusy) return;
  actionBusy = true;
  try {
    const { job } = await apiJson(`/api/jobs/${encodeURIComponent(snapshot.jobId)}/output-versions/${encodeURIComponent(snapshot.versionId)}/finalize`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: window.ClipTalkDelivery.requestBody(snapshot, subtitleDraftId, risky, subtitleSnapshot),
    });
    if (!commitJobAction(job, actionToken)) return;
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function regenerateMissingAutoVariants(targetVariantCount = 3) {
  if (!currentJob || actionBusy) return;
  const recovery = currentJob.autoComposition?.recovery || {};
  const missing = Number(recovery.missingVariantCount || 0);
  if (!missing) return void showToast("当前自动成片版本已齐全");
  if (!await requestActionConfirmation({
    title: "补生成缺少的自动版本",
    summary: `将补生成最多 ${missing} 个与已有时间线实质不同的版本。`,
    details: [],
    confirmLabel: "开始补生成",
  })) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const result = await apiJson(`/api/jobs/${actionToken.jobId}/auto-composition/regenerate`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { targetVariantCount: Math.max(1, Math.min(4, Number(targetVariantCount) || 3)) },
    });
    if (!commitJobAction(result.job, actionToken)) return;
    showToast(result.queued ? "补生成任务已开始" : (result.message || "当前版本已齐全"), "success");
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message || "补生成失败");
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function deleteAutoCompositionBatch(batchId) {
  if (!currentJob || !batchId || actionBusy) return;
  if (!await requestActionConfirmation({
    title: "删除这一批审核样片",
    summary: "删除本批审核样片，其他样片和成片版本不受影响。",
    details: ["删除后无法恢复这些样片", "源视频、分析证据和事件候选会保留"],
    warning: "这是不可恢复的样片删除操作。",
    confirmLabel: "删除本批样片",
  })) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const result = await apiJson(`/api/jobs/${actionToken.jobId}/auto-composition/batches/${encodeURIComponent(batchId)}`, { method: "DELETE" });
    if (!commitJobAction(result.job, actionToken)) return;
    showToast(`已删除 ${Number(result.deletedVersionCount || 0)} 个样片`, "success");
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message || "批次删除失败");
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function previewCandidate(index, { showEvidence = true } = {}) {
  if (!currentJob || !["awaiting_confirmation", "completed"].includes(currentJob.status)) return;
  const candidate = currentJob.candidates?.find((item) => item.index === index);
  if (!candidate) return;
  const previewToken = ++candidatePreviewToken;
  sourcePreviewRetryToken = 0;
  const needsSource = viewerMediaKind === "output" || !mainVideo.getAttribute("src");
  currentOutput = null;
  currentCandidate = candidate;
  currentEventGroup = null;
  currentEventSegment = null;
  candidatePreviewEnd = Number(candidate.end);
  viewerMediaKind = "candidate";
  syncOutputPreviewMode();
  clearOutputVersionSelectionState();
  beginSourcePreviewPolling();
  $("#viewerBadge").textContent = `候选 ${index + 1}`;
  $("#reviewKicker").textContent = "候选片段预览";
  $("#reviewTitle").textContent = candidate.title;
  $("#downloadButton")?.classList.add("hidden");
  $("#coverDownloadButton")?.classList.add("hidden");
  $("#packageDownloadButton")?.classList.add("hidden");
  $("#coverIntroButton")?.classList.add("hidden");
  $("#finalizePreviewButton")?.classList.add("hidden");
  syncOneOffFinalizeAction(currentJob);
  syncLibrarySaveAction(currentJob, null);
  $("#subtitleButton")?.classList.add("hidden");
  if (showEvidence) {
    $("#evidencePanel")?.classList.remove("hidden", "evidence-placeholder");
    $("#evidencePanel")?.classList.add("candidate-mode");
    $("#evidencePanel")?.classList.remove("montage-mode", "output-mode");
    $("#addToChatButton")?.classList.add("hidden");
    $("#clipTime").textContent = `${formatTime(candidate.start)} → ${formatTime(candidate.end)} · ${Number(candidate.duration).toFixed(1)} 秒`;
    $("#clipTitle").textContent = candidate.title;
    $("#clipScore").textContent = `${Math.round(candidate.score)}/100`;
    $("#clipReason").textContent = candidate.reason;
    $("#keepButton")?.classList.remove("hidden");
    $("#keepButton").textContent = "下载该片段";
    $("#replaceButton")?.classList.remove("hidden");
    $("#replaceButton").textContent = "从当前结果移除";
    renderClipEvidence(candidate, "candidate");
  }
  document.querySelectorAll(".candidate-row").forEach((row) => row.classList.toggle("previewing", Number(row.dataset.candidateRow) === index));
  const playRange = () => {
    if (previewToken !== candidatePreviewToken) return;
    const start = Math.max(0, Number(candidate.start));
    mainVideo.pause();
    $("#viewerBadge").textContent = `候选 ${index + 1} · 正在定位`;
    const playAfterSeek = () => {
      if (previewToken !== candidatePreviewToken) return;
      $("#viewerBadge").textContent = `候选 ${index + 1}`;
      safePlay();
    };
    mainVideo.addEventListener("seeked", playAfterSeek, { once: true });
    mainVideo.currentTime = Math.min(Math.max(0, mainVideo.duration - 0.05), start);
    if (Math.abs(mainVideo.currentTime - start) < 0.04) {
      mainVideo.removeEventListener("seeked", playAfterSeek);
      playAfterSeek();
    }
  };
  if (needsSource) {
    clearPlayerNotice();
    setMainVideoSource(sourcePreviewUrl());
    beginSourcePreviewPolling();
    mainVideo.addEventListener("loadedmetadata", playRange, { once: true });
  } else if (mainVideo.readyState >= 1) {
    playRange();
  } else {
    mainVideo.addEventListener("loadedmetadata", playRange, { once: true });
  }
  refreshTimelineAfterReviewSelection();
  syncReviewSelectionClasses();
}

function previewEventGroup(group, { seekTime = null, autoplay = true } = {}) {
  if (!currentJob || !group) return;
  candidatePreviewToken += 1;
  currentOutput = null;
  currentCandidate = null;
  currentEventSegment = null;
  currentEventGroup = group;
  candidatePreviewEnd = null;
  viewerMediaKind = "event";
  syncOutputPreviewMode();
  clearOutputVersionSelectionState();
  stopSourcePreviewPolling();
  clearPlayerNotice();
  setMainVideoSource(`/api/jobs/${currentJob.id}/event-groups/${encodeURIComponent(group.id)}/preview?v=${encodeURIComponent(group.updatedAt || group.id)}`);
  $("#viewerBadge").textContent = `事件组合 · ${group.segments.length} 个镜头`;
  $("#reviewKicker").textContent = "事件成片预览";
  $("#reviewTitle").textContent = group.title;
  $("#downloadButton")?.classList.add("hidden");
  $("#coverDownloadButton")?.classList.add("hidden");
  $("#packageDownloadButton")?.classList.add("hidden");
  $("#coverIntroButton")?.classList.add("hidden");
  $("#finalizePreviewButton")?.classList.add("hidden");
  $("#evidencePanel")?.classList.remove("hidden");
  $("#evidencePanel")?.classList.remove("evidence-placeholder");
  $("#evidencePanel")?.classList.add("candidate-mode");
  $("#evidencePanel")?.classList.remove("montage-mode");
  $("#evidencePanel")?.classList.remove("output-mode");
  $("#addToChatButton")?.classList.add("hidden");
  $("#clipTime").textContent = `${group.segments.length} 个镜头 · ${Number(group.actualDuration).toFixed(1)} 秒`;
  $("#clipTitle").textContent = group.title;
  $("#clipScore").textContent = `${Math.round(group.score)}/100`;
  $("#clipReason").textContent = group.summary;
  $("#keepButton")?.classList.remove("hidden");
  $("#keepButton").textContent = "下载该片段";
  $("#replaceButton")?.classList.add("hidden");
  renderClipEvidence(group, "event");
  refreshTimelineAfterReviewSelection();
  syncReviewSelectionClasses();
  if (seekTime !== null && Number.isFinite(Number(seekTime))) seekCurrentMediaTime(Number(seekTime), { autoplay });
  else if (autoplay) safePlay();
}

function previewEventSegment(group, segment, { seekTime = null } = {}) {
  if (!currentJob || !segment) return;
  const contentMode = String(currentJob.taskMode || "") === "content_extract";
  sourcePreviewRetryToken = 0;
  currentEventGroup = group;
  currentEventSegment = segment;
  currentCandidate = null;
  currentOutput = null;
  clearOutputVersionSelectionState();
  viewerMediaKind = "segment";
  timelineCoordinateSpace = "source";
  timelineSelectedOutputSegmentIndex = null;
  syncOutputPreviewMode();
  beginSourcePreviewPolling();
  candidatePreviewEnd = Number(segment.end);
  const target = Number.isFinite(Number(seekTime)) ? Number(seekTime) : Number(segment.start);
  if (!mainVideo.src.includes(`/api/jobs/${currentJob.id}/preview`)) {
    clearPlayerNotice();
    setMainVideoSource(sourcePreviewUrl());
    beginSourcePreviewPolling();
  }
  seekCurrentMediaTime(Math.max(Number(segment.start) || 0, Math.min(Number(segment.end) || target, target)));
  const groupTitle = group?.title || segment.chapterTitle || "成片镜头";
  $("#viewerBadge").textContent = `${groupTitle} · 源片段`;
  $("#reviewKicker").textContent = contentMode ? "内容来源片段" : "事件来源镜头";
  $("#reviewTitle").textContent = contentMode ? groupTitle : segment.role || segment.title || "源片段";
  $("#evidencePanel")?.classList.remove("hidden");
  $("#evidencePanel")?.classList.remove("evidence-placeholder");
  $("#evidencePanel")?.classList.add("candidate-mode");
  $("#evidencePanel")?.classList.remove("montage-mode", "output-mode");
  $("#addToChatButton")?.classList.add("hidden");
  $("#finalizePreviewButton")?.classList.add("hidden");
  const segmentDuration = Math.max(0, Number(segment.duration) || Number(segment.end) - Number(segment.start));
  $("#clipTime").textContent = `源视频 ${formatTime(segment.start)} → ${formatTime(segment.end)} · ${segmentDuration.toFixed(1)} 秒`;
  $("#clipTitle").textContent = contentMode ? groupTitle : segment.role || segment.title || "源片段";
  $("#clipScore").textContent = `${Math.round(segment.score || 0)}/100`;
  $("#clipReason").textContent = segment.reason || (contentMode ? "当前内容视频采用的源片段" : "事件组中的精彩镜头");
  $("#keepButton")?.classList.remove("hidden");
  $("#keepButton").textContent = "下载该片段";
  $("#replaceButton")?.classList.toggle("hidden", !group || contentMode);
  $("#replaceButton").textContent = "从当前结果移除";
  renderClipEvidence(segment, "segment");
  refreshTimelineAfterReviewSelection();
  syncReviewSelectionClasses();
}

async function renameEventGroup(group) {
  const title = await requestTextInput({
    kicker: "事件编排",
    title: "修改事件名称",
    summary: "名称会显示在当前审核列表和生成版本中。",
    label: "事件名称",
    value: group.title || "",
    required: true,
    confirmLabel: "保存名称",
  });
  if (!title?.trim() || title.trim() === group.title) return;
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups/${encodeURIComponent(group.id)}`, {
      method: "PATCH", headers: { "Content-Type": "application/json" }, body: { title: title.trim() },
    });
    commitJobAction(job, actionToken);
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
}

async function addSelectionToEventGroup(group) {
  const selection = currentJob?.manualSelection;
  if (!selection) return void showToast("请先在源视频时间轴上拖动选择一个范围");
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups/${encodeURIComponent(group.id)}/segments`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { start: selection.start, end: selection.end, role: selection.title || "用户补充镜头" },
    });
    if (!jobActionStillCurrent(actionToken)) return;
    currentEventGroup = group;
    commitJobAction(job, actionToken);
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
}

async function createEventFromSelection() {
  const selection = currentJob?.manualSelection;
  if (!selection) return void showToast("请先在源视频时间轴上拖动选择一个范围");
  const title = await requestTextInput({
    kicker: "建立事件",
    title: "为当前选区命名",
    summary: `${formatTime(selection.start)} → ${formatTime(selection.end)} 将成为新事件的第一个镜头。`,
    label: "事件名称",
    value: selection.title || "手动事件高光",
    required: true,
    confirmLabel: "建立事件",
  });
  if (!title?.trim()) return;
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  try {
    const { job, groupId } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { start: selection.start, end: selection.end, title: title.trim() },
    });
    if (!jobActionStillCurrent(actionToken)) return;
    currentEventGroup = job.eventGroups.find((item) => item.id === groupId) || null;
    commitJobAction(job, actionToken);
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
}

async function deleteEventSegment(group, segment) {
  if (!await requestActionConfirmation({
    title: "从事件中删除镜头",
    summary: `确认从“${group.title}”删除“${segment.role || segment.title || "这个镜头"}”？`,
    details: ["只修改当前事件编排，不会删除源视频或候选镜头。"],
    confirmLabel: "删除镜头",
  })) return;
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  try {
    const { job } = await api(`/api/jobs/${actionToken.jobId}/event-groups/${encodeURIComponent(group.id)}/segments/${encodeURIComponent(segment.id)}`, { method: "DELETE" });
    if (!jobActionStillCurrent(actionToken)) return;
    currentEventSegment = null;
    commitJobAction(job, actionToken);
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
}

async function deleteTimelineItem() {
  if (!currentJob || actionBusy) return;
  if (currentEventGroup && currentEventSegment) {
    return deleteEventSegment(currentEventGroup, currentEventSegment);
  }
  if (!currentCandidate) return;
  if (!await requestActionConfirmation({
    title: "从当前结果移除片段",
    summary: `确认从当前审核中删除“${currentCandidate.title}”？`,
    details: ["对应时间轴片段、缩略图和波形标记会从本次审核中移除。", "源视频不会被修改。"],
    confirmLabel: "从当前结果移除",
  })) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const excluded = new Set(locallyExcludedCandidates);
    excluded.add(Number(currentCandidate.index));
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/review-exclusions`, {
      method: "POST",
      body: { indices: [...excluded] },
    });
    if (!jobActionStillCurrent(actionToken)) return;
    locallyExcludedCandidates = excluded;
    currentCandidate = null;
    currentEventSegment = null;
    commitJobAction(job, actionToken);
    showSource();
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) actionBusy = false; }
}

async function reorderEventSegment(group, from, to) {
  if (to < 0 || to >= group.segments.length) return;
  const ids = group.segments.map((item) => item.id);
  const [moved] = ids.splice(from, 1);
  ids.splice(to, 0, moved);
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups/${encodeURIComponent(group.id)}/segments/reorder`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: { segmentIds: ids },
    });
    commitJobAction(job, actionToken);
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
}

async function moveEventSegment(group, segment) {
  const destinations = currentJob.eventGroups.filter((item) => item.id !== group.id);
  if (!destinations.length) return void showToast("当前没有其他事件组");
  const destinationId = await requestChoice({
    kicker: "移动镜头",
    title: "选择目标事件",
    summary: `“${segment.role || segment.title || "当前镜头"}”将从“${group.title}”移出。`,
    selectLabel: "目标事件",
    options: destinations.map((item) => ({ value: item.id, label: item.title })),
    confirmLabel: "移动镜头",
  });
  if (!destinationId) return;
  const destination = destinations.find((item) => String(item.id) === String(destinationId));
  if (!destination) return void showToast("目标事件已变化，请刷新后重试");
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups/${encodeURIComponent(group.id)}/segments/${encodeURIComponent(segment.id)}/move`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { destinationGroupId: destination.id },
    });
    if (!jobActionStillCurrent(actionToken)) return;
    currentEventGroup = destination;
    currentEventSegment = null;
    commitJobAction(job, actionToken);
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
}

function refreshManualSelection(lightweight = false) {
  const selection = currentJob?.manualSelection;
  if (!selection) return;
  selection.duration = Math.max(0, Number(selection.end) - Number(selection.start));
  candidatePreviewEnd = null;
  const name = selection.title ? `${selection.title} · ` : "";
  $("#viewerBadge").textContent = `${selection.title || "手动选区"} · ${Number(selection.duration).toFixed(1)} 秒`;
  $("#timelineHint").textContent = `${name}${formatTime(selection.start)} → ${formatTime(selection.end)}；可在对话中命名或处理`;
  if (lightweight) updateTimelineSelection();
  else updateTimeline();
}

function fillChatWithTimelineSelection(selection = currentJob?.manualSelection) {
  if (!selection || !chatInput) return;
  const selections = timelineChatSelections.length ? timelineChatSelections : [selection];
  const ranges = selections.map((item) => `${formatTime(Number(item.start) || 0)} → ${formatTime(Number(item.end) || 0)}`).join("、");
  chatInput.value = `合成时间轴选中的 ${selections.length} 个片段（${ranges}）`;
  chatInput.dataset.timelineCompose = "true";
  const composeButton = $("#chatComposeButton");
  if (composeButton) composeButton.textContent = `✦ 合成已选 ${selections.length} 段`;
  chatInput.dispatchEvent(new Event("input", { bubbles: true }));
  chatInput.focus();
}

async function selectTimelineItemForChat(item) {
  if (!currentJob || !["awaiting_confirmation", "completed"].includes(currentJob.status) || !item) return;
  const actionToken = captureJobAction();
  if (currentJob.status === "completed") {
    if (actionBusy) return;
    actionBusy = true;
    try {
      const { job } = await api(`/api/jobs/${actionToken.jobId}/reedit`, { method: "POST" });
      if (!jobActionStillCurrent(actionToken)) return;
      currentOutput = null;
      commitJobAction(job, actionToken);
    } catch (error) {
      if (jobActionStillCurrent(actionToken)) showToast(error.message);
      return;
    } finally {
      if (jobActionStillCurrent(actionToken)) actionBusy = false;
    }
  }
  const start = Number(item.start);
  const end = Number(item.end);
  if (!Number.isFinite(start) || !Number.isFinite(end) || end - start < 1) return;
  try {
    const result = await apiJson(`/api/jobs/${actionToken.jobId}/selection`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: { start, end },
    });
    if (!jobActionStillCurrent(actionToken)) return;
    currentJob.manualSelection = result.selection;
    timelineChatSelections = [...timelineChatSelections.filter((entry) => Math.abs(Number(entry.start) - start) > .05 || Math.abs(Number(entry.end) - end) > .05), result.selection];
    fillChatWithTimelineSelection(result.selection);
    refreshManualSelection(true);
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) console.warn("无法保存时间轴选区", error);
  }
}

function snapTimelineBoundary(value) {
  const rms = waveformData?.rms || [];
  const duration = timelineDurationValue();
  if (!timelineSnapEnabled || !waveformData?.hasAudio || !rms.length || duration <= 0) return value;
  const silencePoints = (waveformData.silences || []).map((item) => (Number(item.start) + Number(item.end)) / 2);
  const nearbySilence = silencePoints
    .map((time) => ({ time, distance: Math.abs(time - value) }))
    .filter((item) => item.distance <= 2.0)
    .sort((left, right) => left.distance - right.distance)[0];
  if (nearbySilence) return Math.round(nearbySilence.time * 1000) / 1000;
  const centerIndex = Math.round(Number(value) / duration * (rms.length - 1));
  const radius = Math.max(2, Math.round(2 / duration * rms.length));
  const local = rms.slice(Math.max(0, centerIndex - radius), Math.min(rms.length, centerIndex + radius + 1));
  const sorted = [...local].sort((left, right) => left - right);
  const threshold = Math.max(0.006, Number(sorted[Math.floor(sorted.length * 0.3)] || 0));
  let bestIndex = centerIndex;
  let bestCost = Infinity;
  for (let index = Math.max(0, centerIndex - radius); index <= Math.min(rms.length - 1, centerIndex + radius); index += 1) {
    const level = Number(rms[index]) || 0;
    if (level > threshold * 1.35) continue;
    const time = index / Math.max(1, rms.length - 1) * duration;
    const cost = Math.abs(time - value) + level / Math.max(threshold, .001) * .08;
    if (cost < bestCost) {
      bestCost = cost;
      bestIndex = index;
    }
  }
  return Math.round(bestIndex / Math.max(1, rms.length - 1) * duration * 1000) / 1000;
}

function moveBoundary(event) {
  if (!boundaryDrag) return;
  const target = boundaryDrag.target;
  const duration = timelineDurationValue();
  const value = timelineTimeFromPointer(event);
  const minimumDuration = boundaryDrag.kind === "content"
    ? 1 / contentBoundaryFrameRate(boundaryDrag.edit.scopedJob) : 1;
  if (boundaryDrag.boundary === "start") target.start = Math.min(Number(target.end) - minimumDuration, value);
  else target.end = Math.max(Number(target.start) + minimumDuration, value);
  if (boundaryDrag.kind === "content") {
    const frame = minimumDuration;
    target.start = Math.round(Math.max(0, target.start) / frame) * frame;
    target.end = Math.round(Math.min(duration, target.end) / frame) * frame;
    boundaryDrag.edit.editor.dataset.boundaryStart = String(target.start);
    boundaryDrag.edit.editor.dataset.boundaryEnd = String(target.end);
    syncContentBoundaryEditor(boundaryDrag.edit.editor, boundaryDrag.edit.scopedJob, boundaryDrag.edit.match, { preview: false });
    return;
  }
  target.start = Math.round(Math.max(0, target.start) * 20) / 20;
  target.end = Math.round(Math.min(duration, target.end) * 20) / 20;
  refreshManualSelection(true);
}

function finishBoundaryDrag() {
  if (!boundaryDrag || !currentJob) return;
  const drag = boundaryDrag;
  const target = drag.target;
  boundaryDrag = null;
  document.removeEventListener("pointermove", moveBoundary);
  document.removeEventListener("pointerup", finishBoundaryDrag);
  if (drag.kind === "content") {
    drag.edit.editor.dataset.boundaryStart = String(target.start);
    drag.edit.editor.dataset.boundaryEnd = String(target.end);
    syncContentBoundaryEditor(drag.edit.editor, drag.edit.scopedJob, drag.edit.match, { preview: true, seekEdge: drag.boundary });
    $("#timelineHint").textContent = "边界已调整；可继续试听，确认后点击“保存边界”";
    return;
  }
  if (!pendingTimelineSelection) {
    Object.assign(target, drag.original);
    refreshManualSelection();
    return;
  }
  const snapped = snapTimelineBoundary(Number(target[drag.boundary]));
  if (drag.boundary === "start") target.start = Math.min(Number(target.end) - 1, snapped);
  else target.end = Math.max(Number(target.start) + 1, snapped);
  pendingTimelineSelection = { start: Number(target.start), end: Number(target.end), duration: Number(target.end) - Number(target.start) };
  refreshManualSelection();
  $("#timelineHint").textContent = "手动选区已微调，请确认后再保存或合成";
}

function beginBoundaryDrag(event, boundary) {
  if (contentBoundaryTimelineEdit?.editor?.isConnected
      && String(contentBoundaryTimelineEdit.jobId || "") === String(currentJob?.id || "")) {
    event.preventDefault();
    event.stopPropagation();
    boundaryDrag = {
      boundary,
      target: contentBoundaryTimelineEdit,
      edit: contentBoundaryTimelineEdit,
      kind: "content",
      original: { start: contentBoundaryTimelineEdit.start, end: contentBoundaryTimelineEdit.end },
    };
    document.addEventListener("pointermove", moveBoundary);
    document.addEventListener("pointerup", finishBoundaryDrag, { once: true });
    return;
  }
  // Existing AI candidates and event segments are review-only on the
  // timeline. Boundary handles are reserved for a new manual range before
  // the user confirms it.
  if (!pendingTimelineSelection || !currentJob?.manualSelection || !["awaiting_confirmation", "awaiting_content_confirmation", "completed"].includes(currentJob.status)) return;
  const target = currentJob.manualSelection;
  event.preventDefault();
  event.stopPropagation();
  boundaryDrag = {
    boundary,
    target,
    kind: "manual",
    original: { start: Number(target.start), end: Number(target.end), duration: Number(target.duration) },
  };
  document.addEventListener("pointermove", moveBoundary);
  document.addEventListener("pointerup", finishBoundaryDrag, { once: true });
}

function timelineTimeFromPointer(event) {
  const trackContent = timelineTrackContent || timelineViewport;
  const bounds = trackContent.getBoundingClientRect();
  const view = timelineViewRange();
  return Math.max(0, Math.min(
    timelineDurationValue(),
    view.start + (event.clientX - bounds.left) / Math.max(1, bounds.width) * view.duration,
  ));
}

function activateTimelinePoint(event) {
  const time = timelineTimeFromPointer(event);
  const trackContent = timelineTrackContent || timelineViewport;
  const bounds = trackContent?.getBoundingClientRect();
  const timelineStyle = timelineViewport ? getComputedStyle(timelineViewport) : null;
  const audioTop = Number.parseFloat(timelineStyle?.getPropertyValue("--timeline-audio-track-top") || "") || Math.max(0, Number(bounds?.height || 0) * .7);
  const clickedAudio = Boolean(bounds && event.clientY - bounds.top >= audioTop);
  if (!clickedAudio) {
    seekSourceTime(time);
    return;
  }
  timelineFrameSelectionTime = null;
  const match = timelineEventSegmentAtTime(time);
  if (match) previewEventSegment(match.group, match.segment);
  else {
    showSource();
    seekSourceTime(time);
    updateTimeline();
  }
}

function moveTimelineRange(event) {
  if (!timelineRangeDrag || !currentJob) return;
  if (Math.abs(event.clientX - timelineRangeDrag.startX) >= 4) timelineRangeDrag.moved = true;
  if (!timelineRangeDrag.moved) return;
  const value = timelineTimeFromPointer(event);
  currentJob.manualSelection = {
    start: Math.round(Math.min(timelineRangeDrag.anchor, value) * 20) / 20,
    end: Math.round(Math.max(timelineRangeDrag.anchor, value) * 20) / 20,
  };
  currentJob.manualSelection.duration = currentJob.manualSelection.end - currentJob.manualSelection.start;
  refreshManualSelection(true);
}

async function finishTimelineRange(event) {
  if (!timelineRangeDrag || !currentJob) return;
  const drag = timelineRangeDrag;
  timelineRangeDrag = null;
  document.removeEventListener("pointermove", moveTimelineRange);
  document.removeEventListener("pointerup", finishTimelineRange);
  if (!drag.moved) {
    seekSourceTime(timelineTimeFromPointer(event));
    return;
  }
  const selection = currentJob.manualSelection;
  if (selection.end - selection.start < 1) {
    selection.end = Math.min(timelineDurationValue(), selection.start + 1);
    selection.start = Math.max(0, selection.end - 1);
  }
  selection.start = snapTimelineBoundary(selection.start);
  selection.end = snapTimelineBoundary(selection.end);
  if (selection.end - selection.start < 1) selection.end = Math.min(timelineDurationValue(), selection.start + 1);
  pendingTimelineOriginal = drag.original ? { ...drag.original } : null;
  pendingTimelineSelection = { start: selection.start, end: selection.end, duration: selection.end - selection.start };
  refreshManualSelection();
  $("#timelineHint").textContent = precisionTimelineKind() === "content"
    ? "新片段尚未加入：可拖动两端微调，然后加入当前探索结果"
    : "选区尚未保存：可拖动两端微调，确认后再合成";
}

async function confirmPendingTimelineSelection() {
  if (!pendingTimelineSelection || !currentJob) return true;
  if (precisionTimelineKind() === "content" && currentJob.status === "awaiting_content_confirmation") {
    const actionToken = captureJobAction();
    const selection = { ...pendingTimelineSelection };
    actionBusy = true;
    try {
      const result = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/content-search/manual-range`, {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: {
          searchId: currentJob.contentSearch?.id,
          start: selection.start,
          end: selection.end,
        },
      });
      pendingTimelineSelection = null;
      pendingTimelineOriginal = null;
      timelineManualSelectMode = false;
      $("#timelineManualSelectToggle")?.classList.remove("active");
      $("#timelineManualSelectToggle")?.setAttribute("aria-pressed", "false");
      timelineViewport?.classList.remove("manual-select-mode");
      if (!commitJobAction(result.job, actionToken)) return false;
      const added = (currentJob.contentSearch?.candidates || []).find((item) => String(item.id) === String(result.match?.id));
      if (added) {
        previewContentMatch(added, { autoplay: false });
        openContentBoundaryInspector(added);
      }
      showToast("新片段已加入当前探索结果", "success");
      return true;
    } catch (error) {
      if (jobActionStillCurrent(actionToken)) showToast(`加入片段失败：${error.message}`);
      return false;
    } finally {
      if (jobActionStillCurrent(actionToken)) actionBusy = false;
    }
  }
  try {
    const result = await apiJson(`/api/jobs/${currentJob.id}/selection`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { start: pendingTimelineSelection.start, end: pendingTimelineSelection.end },
    });
    currentJob.manualSelection = result.selection;
    pendingTimelineSelection = null;
    pendingTimelineOriginal = null;
    timelineChatSelections = [...timelineChatSelections.filter((entry) => Math.abs(Number(entry.start) - Number(result.selection.start)) > .05 || Math.abs(Number(entry.end) - Number(result.selection.end)) > .05), result.selection];
    refreshManualSelection();
    chatInput.placeholder = "已确认时间段，可直接合成或在对话中继续编辑";
    fillChatWithTimelineSelection(result.selection);
    return true;
  } catch (error) {
    showToast(error.message);
    return false;
  }
}

function cancelPendingTimelineSelection() {
  if (!pendingTimelineSelection) return;
  if (pendingTimelineOriginal) currentJob.manualSelection = { ...pendingTimelineOriginal };
  else delete currentJob.manualSelection;
  pendingTimelineSelection = null;
  pendingTimelineOriginal = null;
  updateTimeline();
  $("#timelineHint").textContent = "已取消本次手动选区";
}

function updatePendingTimelineBoundary(boundary, rawValue) {
  if (!pendingTimelineSelection || !currentJob) return;
  const duration = timelineDurationValue();
  const value = Number(rawValue);
  if (!Number.isFinite(value)) return;
  const next = { ...pendingTimelineSelection };
  if (boundary === "start") next.start = Math.max(0, Math.min(value, duration));
  else next.end = Math.max(0, Math.min(value, duration));
  if (next.end - next.start < 1) {
    showToast("手动选区必须至少保留 1 秒");
    updateTimelineSelection();
    return;
  }
  next.duration = next.end - next.start;
  pendingTimelineSelection = next;
  currentJob.manualSelection = { ...next };
  refreshManualSelection();
  $("#timelineHint").textContent = "时间已调整：确认选区后会填入对话框";
}

function beginTimelineRange(event) {
  if (!currentJob) return;
  if (!timelineManualSelectMode) {
    activateTimelinePoint(event);
    return;
  }
  if (!["awaiting_confirmation", "awaiting_content_confirmation", "completed"].includes(currentJob.status)) {
    showToast("请等待视频分析完成，进入候选审核后再选择时间段");
    seekSourceTime(timelineTimeFromPointer(event));
    return;
  }
  event.preventDefault();
  if (["output", "event"].includes(viewerMediaKind)) {
    const activeGroup = currentEventGroup;
    showSource();
    currentEventGroup = activeGroup;
  }
  currentOutput = null;
  currentCandidate = null;
  currentEventSegment = null;
  candidatePreviewEnd = null;
  renderEvidencePlaceholder({
    time: "手动选区",
    title: "拖动生成选区",
    reason: "在时间轴上拖出范围后，可以继续微调两端；确认选区后才会加入剪辑对话。",
  });
  document.querySelectorAll(".candidate-row").forEach((row) => row.classList.remove("previewing"));
  timelineRangeDrag = {
    anchor: timelineTimeFromPointer(event),
    startX: event.clientX,
    moved: false,
    original: currentJob.manualSelection ? { ...currentJob.manualSelection } : null,
  };
  document.addEventListener("pointermove", moveTimelineRange);
  document.addEventListener("pointerup", finishTimelineRange, { once: true });
}

function seekSourceTime(second) {
  beginSourceInspection();
  const value = Math.max(0, Math.min(Number(currentJob?.videoInfo?.duration) || timelineDurationValue(), Number(second) || 0));
  const seek = () => {
    mainVideo.currentTime = Math.min(Math.max(0, mainVideo.duration - 0.05), value);
    updateTimelinePlayhead();
  };
  if (["output", "event"].includes(viewerMediaKind)) {
    showSource({ autoplay: false, seekTime: value });
  } else if (mainVideo.readyState >= 1) {
    seek();
  } else {
    mainVideo.addEventListener("loadedmetadata", seek, { once: true });
  }
  if (currentCandidate) candidatePreviewEnd = value <= Number(currentCandidate.end) ? Number(currentCandidate.end) : null;
}

window.ClipTalkSeekSourceTime = (second) => seekSourceTime(second);

function candidateThumbnailStyle(candidate) {
  const sprite = timelineAssets?.sprite;
  if (!sprite?.items?.length || !timelineAssets?.spriteUrl) return "";
  const target = (Number(candidate.start) + Number(candidate.end)) / 2;
  const item = sprite.items.reduce((best, current) =>
    Math.abs(Number(current.time) - target) < Math.abs(Number(best.time) - target) ? current : best
  );
  const scale = 52 / Math.max(1, Number(sprite.tileHeight));
  return [
    `background-image:url('${timelineAssets.spriteUrl}')`,
    `background-size:${Number(sprite.spriteWidth) * scale}px ${Number(sprite.spriteHeight) * scale}px`,
    `background-position:${-Number(item.column) * Number(sprite.tileWidth) * scale}px ${-Number(item.row) * Number(sprite.tileHeight) * scale}px`,
  ].join(";");
}

let candidateDrawerReturnFocus = null;
let candidateDrawerEscapeHandler = null;
let candidateDrawerReleaseFocus = null;
let candidateDrawerCloseTimer = null;

function closeCandidateDrawer({ restoreFocus = false } = {}) {
  const drawer = $("#candidateDrawer");
  window.clearTimeout(candidateDrawerCloseTimer);
  candidateDrawerCloseTimer = null;
  drawer?.classList.remove("open");
  drawer?.setAttribute("aria-hidden", "true");
  if (drawer) drawer.dataset.overlayState = "closing";
  if (drawer) drawer.inert = true;
  $("#drawerBackdrop")?.classList.add("hidden");
  candidateDrawerReleaseFocus?.(false);
  candidateDrawerReleaseFocus = null;
  if (drawer && !drawer.classList.contains("hidden")) {
    candidateDrawerCloseTimer = window.setTimeout(() => {
      if (!drawer.classList.contains("open")) {
        drawer.classList.add("hidden");
        drawer.dataset.overlayState = "closed";
      }
      candidateDrawerCloseTimer = null;
    }, 220);
  }
  if (candidateDrawerEscapeHandler) document.removeEventListener("keydown", candidateDrawerEscapeHandler);
  candidateDrawerEscapeHandler = null;
  syncReviewWorkbench();
  if (restoreFocus && candidateDrawerReturnFocus?.isConnected) candidateDrawerReturnFocus.focus();
  candidateDrawerReturnFocus = null;
}

function showCandidateDrawerSurface(trigger = null) {
  const drawer = $("#candidateDrawer");
  const backdrop = $("#drawerBackdrop");
  if (!drawer) return;
  if (window.ClipTalkPrepareWorkspaceOverlay?.("candidate") === false) return;
  window.clearTimeout(candidateDrawerCloseTimer);
  candidateDrawerCloseTimer = null;
  candidateDrawerReturnFocus = trigger || (document.activeElement instanceof window.HTMLElement ? document.activeElement : $("#openCandidateDrawer"));
  drawer.inert = false;
  drawer.classList.remove("hidden");
  drawer.dataset.overlayState = "opening";
  drawer.getBoundingClientRect();
  drawer.classList.add("open");
  drawer.dataset.overlayState = "open";
  drawer.setAttribute("aria-hidden", "false");
  backdrop?.classList.remove("hidden");
  candidateDrawerReleaseFocus?.(false);
  candidateDrawerReleaseFocus = activateModalFocus(drawer, {
    initialFocus: $("#candidateDrawerSearch"),
    additionalActive: [backdrop].filter(Boolean),
    restoreFocus: false,
  });
  if (candidateDrawerEscapeHandler) document.removeEventListener("keydown", candidateDrawerEscapeHandler);
  candidateDrawerEscapeHandler = (event) => {
    if (event.key !== "Escape" || !drawer.classList.contains("open")) return;
    event.preventDefault();
    closeCandidateDrawer({ restoreFocus: true });
  };
  document.addEventListener("keydown", candidateDrawerEscapeHandler);
}

function resetCandidateDrawerContext() {
  closeCandidateDrawer();
  const drawer = $("#candidateDrawer");
  if (drawer) delete drawer.dataset.jobId;
  const search = $("#candidateDrawerSearch");
  if (search) search.value = "";
  const title = $("#candidateDrawerTitle");
  if (title) title.textContent = "候选镜头";
  const kicker = $("#candidateDrawerKicker");
  if (kicker) kicker.textContent = "源视频分析结果";
  const description = $("#candidateDrawerDescription");
  if (description) description.textContent = "查看从源视频中识别出的候选镜头。";
  const list = $("#candidateDrawerList");
  if (list) list.innerHTML = "";
}

function currentContentReviewRoot(job = currentJob) {
  return activeContentReviewRoot(job);
}

function contentDrawerSelectedIds(job = currentJob) {
  const reviewRoot = currentContentReviewRoot(job);
  const reviewInputs = [...reviewRoot?.querySelectorAll("[data-content-match]") || []];
  if (reviewInputs.length) {
    return new Set(reviewInputs.filter((input) => input.checked).map((input) => String(input.value)));
  }
  const search = job?.contentSearch || {};
  const saved = search.reviewDraft?.searchId === search.id
    ? search.reviewDraft.selectedMatchIds
    : search.defaultSelectedIds;
  if (Array.isArray(saved)) return new Set(saved.map(String));
  return new Set((search.candidates || []).filter((item) => item.selected).map((item) => String(item.id)));
}

function contentDrawerTargetLabels(job = currentJob) {
  const target = job?.request?.contentSearchPersonTarget || job?.contentSearchPersonTarget || job?.contentSearch?.intent?.personTarget || {};
  const ids = new Set((target.personIds || []).map(String));
  return (job?.contentIndex?.persons || [])
    .filter((person) => ids.has(String(person.id)))
    .map((person) => String(person.label || person.defaultLabel || person.id || ""))
    .filter(Boolean);
}

function syncContentDrawerSelection(job = currentJob) {
  const drawerList = $("#candidateDrawerList");
  const reviewRoot = currentContentReviewRoot(job);
  const checked = new Set([...drawerList?.querySelectorAll("[data-drawer-content-check]:checked") || []].map((input) => String(input.value)));
  if (reviewRoot) {
    reviewRoot.querySelectorAll("[data-content-match]").forEach((input) => {
      const next = checked.has(String(input.value));
      if (input.checked === next || input.disabled) return;
      input.checked = next;
      input.dispatchEvent(new Event("change", { bubbles: true }));
    });
  }
  const matches = (job?.contentSearch?.candidates || []).filter((item) => checked.has(String(item.id)));
  const duration = matches.reduce((sum, item) => sum + Math.max(0, Number(item.duration) || Number(item.end) - Number(item.start) || 0), 0);
  const count = checked.size;
  const countLabel = drawerList?.querySelector("[data-drawer-selected-count]");
  const durationLabel = drawerList?.querySelector("[data-drawer-selected-duration]");
  const compose = drawerList?.querySelector("[data-drawer-content-compose]");
  const toggle = drawerList?.querySelector("[data-drawer-content-toggle]");
  if (countLabel) countLabel.textContent = String(count);
  if (durationLabel) durationLabel.textContent = `${duration.toFixed(1)} 秒`;
  if (compose) {
    compose.disabled = !count;
    compose.textContent = count ? `合成所选 ${count} 段` : "请先选择片段";
  }
  if (toggle) {
    const allCount = drawerList.querySelectorAll("[data-drawer-content-check]").length;
    const allSelected = allCount > 0 && count === allCount;
    toggle.setAttribute("aria-pressed", String(allSelected));
    toggle.textContent = allSelected ? "取消全选" : "选择全部";
  }
}

function renderContentCandidateDrawer(job) {
  const search = job?.contentSearch || {};
  const query = String($("#candidateDrawerSearch")?.value || "").trim().toLowerCase();
  const allCandidates = Array.isArray(search.candidates) ? search.candidates : [];
  const candidates = allCandidates.filter((candidate) => !query || [
    candidate.title, candidate.reason, candidate.transcriptExcerpt,
    ...(candidate.matchedPersonLabels || candidate.matchedPersonIds || []),
  ].filter(Boolean).join(" ").toLowerCase().includes(query));
  const selectedIds = contentDrawerSelectedIds(job);
  const labels = contentDrawerTargetLabels(job);
  const drawerList = $("#candidateDrawerList");
  const title = $("#candidateDrawerTitle");
  const kicker = $("#candidateDrawerKicker");
  const description = $("#candidateDrawerDescription");
  if (!drawerList) return;
  if (kicker) kicker.textContent = labels.length ? "人物出镜片段" : "内容匹配片段";
  if (title) title.textContent = query
    ? `搜索结果（${candidates.length}/${allCandidates.length}）`
    : `${labels.length ? "人物出镜片段" : "内容匹配片段"}（${allCandidates.length}）`;
  if (description) description.textContent = labels.length
    ? `当前选择：${labels.join("、")}。以下是已复用人物轨迹整理出的全部片段，可逐段或连续播放，也可直接合成一条视频。`
    : "以下是本次检索得到的源片段，可逐段或连续播放，也可直接合成一条视频。";
  drawerList.innerHTML = candidates.length ? `<div class="drawer-batch-actions content-drawer-batch"><span>已选 <b data-drawer-selected-count>${selectedIds.size}</b> 段 · <em data-drawer-selected-duration>0.0 秒</em></span><div><button type="button" class="secondary" data-drawer-content-play-all>连续播放 ${allCandidates.length} 段</button><button type="button" class="secondary" data-drawer-content-toggle aria-pressed="false">选择全部</button><button type="button" data-drawer-content-compose>合成所选片段</button></div></div>${candidates.map((candidate, index) => {
    const duration = Math.max(0, Number(candidate.duration) || Number(candidate.end) - Number(candidate.start) || 0);
    return `<article class="drawer-candidate selectable${String(currentCandidate?.id || "") === String(candidate.id) ? " active" : ""}" data-drawer-content-candidate="${escapeHtml(candidate.id)}"><input class="drawer-candidate-check" data-drawer-content-check type="checkbox" value="${escapeHtml(candidate.id)}" ${selectedIds.has(String(candidate.id)) ? "checked" : ""} aria-label="选择${escapeHtml(candidate.title || `片段 ${index + 1}`)}"><div class="candidate-thumb" style="${candidateThumbnailStyle(candidate)}"><span>${formatTime(candidate.start)}</span></div><div class="drawer-candidate-copy"><span><strong>${index + 1}. ${escapeHtml(candidate.title || `匹配片段 ${index + 1}`)}</strong><b>轨迹</b></span><small>${formatTime(candidate.start)} → ${formatTime(candidate.end)} · ${duration.toFixed(1)} 秒</small><div class="drawer-candidate-actions"><button type="button" data-drawer-content-preview>播放此片段</button></div></div></article>`;
  }).join("")}` : query
    ? '<div class="rail-empty"><strong>没有匹配的片段</strong><p>片段仍然存在，只是没有内容符合当前搜索条件。</p><button type="button" data-candidate-search-clear>清除搜索</button></div>'
    : '<div class="rail-empty"><strong>暂无可用片段</strong><p>人物轨迹或内容检索完成后会在这里展示片段。</p></div>';
  drawerList.querySelectorAll("[data-drawer-content-candidate]").forEach((row) => {
    const match = candidates.find((item) => String(item.id) === String(row.dataset.drawerContentCandidate));
    row.querySelector("[data-drawer-content-preview]")?.addEventListener("click", () => previewContentMatch(match));
  });
  drawerList.querySelectorAll("[data-drawer-content-check]").forEach((input) => input.addEventListener("change", () => syncContentDrawerSelection(job)));
  drawerList.querySelector("[data-drawer-content-play-all]")?.addEventListener("click", () => previewContentMatchesSequentially(allCandidates));
  drawerList.querySelector("[data-drawer-content-toggle]")?.addEventListener("click", (event) => {
    const selectAll = event.currentTarget.getAttribute("aria-pressed") !== "true";
    drawerList.querySelectorAll("[data-drawer-content-check]").forEach((input) => { input.checked = selectAll; });
    syncContentDrawerSelection(job);
  });
  drawerList.querySelector("[data-drawer-content-compose]")?.addEventListener("click", () => composeContentCandidatesFromDrawer(job));
  drawerList.querySelector("[data-candidate-search-clear]")?.addEventListener("click", () => {
    const input = $("#candidateDrawerSearch");
    if (input) input.value = "";
    renderContentCandidateDrawer(currentJob);
  });
  syncContentDrawerSelection(job);
  const drawer = $("#candidateDrawer");
  if (drawer && job?.id) drawer.dataset.jobId = String(job.id);
}

async function composeContentCandidatesFromDrawer(job = currentJob) {
  const reviewRoot = currentContentReviewRoot(job);
  if (!reviewRoot) {
    closeCandidateDrawer();
    setDirectorStage("events");
    return void showToast("片段审核区尚未准备完成，请稍后重试。", "error");
  }
  syncContentDrawerSelection(job);
  const outputMode = reviewRoot.querySelector("[data-content-output-mode]");
  const orderMode = reviewRoot.querySelector("[data-content-order-mode]");
  if (outputMode) outputMode.value = "single_reel";
  if (orderMode) orderMode.value = "source";
  syncContentSearchOutputControls(reviewRoot, contentSearchJobForRoot(reviewRoot, job));
  closeCandidateDrawer();
  await confirmContentSearch(reviewRoot, contentSearchJobForRoot(reviewRoot, job));
}

function renderCandidateDrawer(job) {
  if (taskModePresentation(job).key === "content_extract") {
    renderContentCandidateDrawer(job);
    const drawer = $("#candidateDrawer");
    if ($("#candidateDrawerSearch")) $("#candidateDrawerSearch").oninput = () => {
      if (String(drawer?.dataset.jobId || "") !== String(currentJob?.id || "")) return resetCandidateDrawerContext();
      renderContentCandidateDrawer(currentJob);
    };
    return;
  }
  const query = String($("#candidateDrawerSearch")?.value || "").trim().toLowerCase();
  const allCandidates = Array.isArray(job?.candidates) ? job.candidates : [];
  const candidates = allCandidates.filter((candidate) => !query || [candidate.title, candidate.reason, candidate.audioEvidence?.transcriptExcerpt, ...itemSpeakers(candidate)].filter(Boolean).join(" ").toLowerCase().includes(query));
  const selectedGroups = new Set(job.recommendedGroupIds || []);
  const selectedCandidateIndices = new Set((job.eventGroups || [])
    .filter((group) => selectedGroups.has(group.id))
    .flatMap((group) => group.segments || [])
    .map((segment) => Number(segment.candidateIndex))
    .filter(Number.isFinite));
  const drawerList = $("#candidateDrawerList");
  if (!drawerList) return;
  const canReselect = job.status === "awaiting_confirmation" && Boolean(candidates.length);
  const drawerTitle = $("#candidateDrawerTitle");
  const drawerKicker = $("#candidateDrawerKicker");
  const drawerDescription = $("#candidateDrawerDescription");
  if (drawerKicker) drawerKicker.textContent = "源视频分析结果";
  if (drawerTitle) drawerTitle.textContent = query
    ? `搜索结果（${candidates.length}/${allCandidates.length}）`
    : `候选镜头（${allCandidates.length}）`;
  if (drawerDescription) drawerDescription.textContent = canReselect
    ? "预览并勾选镜头，可用所选内容重新编排成片。"
    : "这些镜头来自源视频分析，仅供预览和回看。";
  drawerList.innerHTML = candidates.length ? `${canReselect ? `<div class="drawer-batch-actions"><span>已选 <b>${selectedCandidateIndices.size}</b> 个镜头</span><button type="button" class="drawer-create-event" ${selectedCandidateIndices.size ? "" : "disabled"}>用所选镜头新建事件</button></div>` : ""}${candidates.map((candidate, index) => `
    <article class="drawer-candidate${canReselect ? " selectable" : ""}${currentCandidate?.index === candidate.index ? " active" : ""}" data-drawer-candidate="${candidate.index}">
      ${canReselect ? `<input class="drawer-candidate-check" type="checkbox" value="${candidate.index}" ${selectedCandidateIndices.has(Number(candidate.index)) ? "checked" : ""} aria-label="选择第 ${index + 1} 个镜头">` : ""}
      <div class="candidate-thumb" style="${candidateThumbnailStyle(candidate)}"><span>${formatTime(candidate.start)}</span></div>
      <div class="drawer-candidate-copy"><span><strong>${index + 1}. ${escapeHtml(candidate.title)}</strong><b>${Math.round(candidate.score)}</b></span>
        <small>${formatTime(candidate.start)} → ${formatTime(candidate.end)} · ${Number(candidate.duration).toFixed(1)} 秒</small>
        ${audioEvidenceMarkup(candidate.audioEvidence)}
        <div class="drawer-candidate-actions"><button type="button" class="drawer-preview">预览源镜头</button><button type="button" class="drawer-add" ${job.status === "awaiting_confirmation" && job.eventGroups?.length ? "" : "disabled"}>加入事件</button></div>
      </div>
    </article>`).join("")}` : query
    ? '<div class="rail-empty"><strong>没有匹配的镜头</strong><p>当前候选池仍然存在，只是没有镜头符合搜索条件。</p><button type="button" data-candidate-search-clear>清除搜索</button></div>'
    : '<div class="rail-empty"><strong>暂无可用镜头</strong><p>模型完成精修后会在这里展示候选池。</p></div>';
  drawerList?.querySelectorAll("[data-drawer-candidate]").forEach((row) => {
    const candidate = candidates.find((item) => Number(item.index) === Number(row.dataset.drawerCandidate));
    row.querySelector(".drawer-preview")?.addEventListener("click", () => previewCandidate(Number(candidate.index)));
    row.querySelector(".drawer-add")?.addEventListener("click", () => addCandidateToEvent(candidate));
  });
  const updateBatchSelection = () => {
    const count = drawerList.querySelectorAll(".drawer-candidate-check:checked").length;
    const action = drawerList.querySelector(".drawer-create-event");
    const label = drawerList.querySelector(".drawer-batch-actions span b");
    if (label) label.textContent = String(count);
    if (action) action.disabled = !count;
  };
  drawerList?.querySelectorAll(".drawer-candidate-check").forEach((input) => input.addEventListener("change", updateBatchSelection));
  drawerList.querySelector(".drawer-create-event")?.addEventListener("click", createEventFromCandidateSelection);
  drawerList.querySelector("[data-candidate-search-clear]")?.addEventListener("click", () => {
    const search = $("#candidateDrawerSearch");
    if (search) search.value = "";
    renderCandidateDrawer(currentJob);
  });
  const drawer = $("#candidateDrawer");
  if (drawer && job?.id) drawer.dataset.jobId = String(job.id);
  if ($("#candidateDrawerSearch")) $("#candidateDrawerSearch").oninput = () => {
    if (String($("#candidateDrawer")?.dataset.jobId || "") !== String(currentJob?.id || "")) return resetCandidateDrawerContext();
    renderCandidateDrawer(currentJob);
  };
}

function renderCandidateRail(job) {
  const body = $("#railBody");
  const rail = $("#reviewRail");
  const railTitle = $("#railTitle");
  const candidates = Array.isArray(job?.candidates) ? job.candidates : [];
  if (!body) return;
  if (rail && body.parentElement !== rail) rail.append(body);
  if (railTitle) railTitle.textContent = `素材（${candidates.length}）`;
  body.innerHTML = `<section class="candidate-rail-panel">
    <div class="rail-section-title"><strong>候选镜头</strong><button type="button" data-close-candidate-rail>返回审核</button></div>
    <p class="rail-summary">在这里快速预览素材；需要证据、对白和加入事件等详细操作时，再打开单个镜头详情。</p>
    <div class="candidate-rail-list">${candidates.map((candidate, index) => `<article class="candidate-rail-card" data-candidate-index="${Number(candidate.index)}">
      <div class="candidate-thumb" style="${candidateThumbnailStyle(candidate)}"><span>${formatTime(candidate.start)}</span></div>
      <span><strong>${index + 1}. ${escapeHtml(candidate.title || "未命名镜头")}</strong><small>${formatTime(candidate.start)} → ${formatTime(candidate.end)} · ${Number(candidate.duration || 0).toFixed(1)} 秒</small></span>
      <div><button type="button" data-preview-candidate-inline>预览</button><button type="button" data-open-candidate-detail>查看详情</button></div>
    </article>`).join("")}</div>
  </section>`;
  body.querySelector("[data-close-candidate-rail]")?.addEventListener("click", () => {
    setCandidateRailOpen(false);
    renderReviewRail(currentJob);
    syncReviewWorkbench();
  });
  body.querySelectorAll("[data-candidate-index]").forEach((row) => {
    const candidate = candidates.find((value) => Number(value.index) === Number(row.dataset.candidateIndex));
    row.querySelector("[data-preview-candidate-inline]")?.addEventListener("click", () => previewCandidate(Number(candidate?.index)));
    row.querySelector("[data-open-candidate-detail]")?.addEventListener("click", () => {
      currentCandidate = candidate || null;
      const search = $("#candidateDrawerSearch");
      if (search) search.value = String(candidate?.title || "");
      renderCandidateDrawer(currentJob);
      showCandidateDrawerSurface(row.querySelector("[data-open-candidate-detail]"));
    });
  });
  $("#railOutput")?.classList.add("hidden");
}

function openCandidateDrawer() {
  const contentMode = taskModePresentation(currentJob).key === "content_extract";
  const candidates = contentMode ? currentJob?.contentSearch?.candidates : currentJob?.candidates;
  if (!candidates?.length) return;
  if (!contentMode) {
    setCandidateRailOpen(!candidateRailOpen);
    closeCandidateDrawer({ restoreFocus: false });
    if (candidateRailOpen) renderCandidateRail(currentJob);
    else renderReviewRail(currentJob);
    syncReviewWorkbench();
    return;
  }
  if ($("#candidateDrawer")?.classList.contains("open")) return void closeCandidateDrawer({ restoreFocus: true });
  if (!contentMode && reviewPanelDeckAvailable(Boolean(currentJob))) {
    closePersonProfiles();
    closeVoiceProfiles();
    setReviewLowerPanelMode("collapsed");
  }
  renderCandidateDrawer(currentJob);
  showCandidateDrawerSurface();
  syncReviewWorkbench();
}

async function addCandidateToEvent(candidate) {
  const groups = currentJob?.eventGroups || [];
  if (!groups.length) return;
  const groupId = await requestChoice({
    kicker: "补充镜头",
    title: "选择要加入的事件",
    summary: `“${candidate.title || "当前候选"}”会追加到所选事件的末尾，之后仍可调整顺序。`,
    selectLabel: "目标事件",
    options: groups.map((group) => ({ value: group.id, label: group.title })),
    confirmLabel: "加入事件",
  });
  if (!groupId) return;
  const group = groups.find((item) => String(item.id) === String(groupId));
  if (!group) return void showToast("目标事件已变化，请刷新后重试");
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups/${encodeURIComponent(group.id)}/segments`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { start: candidate.start, end: candidate.end, role: candidate.title || "候选补充镜头" },
    });
    if (!jobActionStillCurrent(actionToken)) return;
    currentEventGroup = job.eventGroups.find((item) => item.id === group.id) || null;
    closeCandidateDrawer();
    setDirectorStage("events");
    commitJobAction(job, actionToken);
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
}

async function createEventFromCandidateSelection() {
  if (!currentJob || currentJob.status !== "awaiting_confirmation" || actionBusy) return;
  const indices = [...$("#candidateDrawerList")?.querySelectorAll(".drawer-candidate-check:checked") || []].map((input) => Number(input.value));
  if (!indices.length) return void showToast("请至少选择一个镜头候选");
  const title = await requestTextInput({
    kicker: "建立事件",
    title: "为所选镜头命名",
    summary: `已选择 ${indices.length} 个镜头；会按当前顺序建立一个可继续调整的事件。`,
    label: "事件名称",
    value: "重新编排高光",
    confirmLabel: "建立事件",
  });
  if (title === null) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const { job, groupId } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups/from-candidates`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { indices, title: title.trim() || "重新编排高光" },
    });
    if (!jobActionStillCurrent(actionToken)) return;
    currentEventGroup = job.eventGroups.find((group) => group.id === groupId) || null;
    currentEventSegment = null;
    closeCandidateDrawer();
    setDirectorStage("events");
    commitJobAction(job, actionToken);
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function selectedRailGroupIds() {
  const checked = new Set([...$("#railBody")?.querySelectorAll(".rail-event-check:checked") || []].map((input) => input.value));
  const ordered = eventGroupSelectionOrder.filter((id) => checked.has(String(id)));
  const remaining = [...checked].filter((id) => !ordered.includes(id));
  return [...ordered, ...remaining];
}

function recordEventGroupSelection(id, checked) {
  const value = String(id);
  eventGroupSelectionOrder = eventGroupSelectionOrder.filter((item) => item !== value);
  if (checked) eventGroupSelectionOrder.push(value);
}

function selectedSegmentIdsForGroup(group) {
  if (!group) return [];
  const saved = pendingSegmentSelections.get(String(group.id));
  return saved ? [...saved] : (group.segments || []).map((segment) => String(segment.id));
}

function selectedGroupsWithSegments(job) {
  return (job.eventGroups || []).filter((group) => selectedRailGroupIds().includes(String(group.id))).map((group) => ({
    ...group,
    segments: (group.segments || []).filter((segment) => selectedSegmentIdsForGroup(group).includes(String(segment.id))),
  })).filter((group) => group.segments.length);
}

function highlightSelectionHasSpeech(job, selectedGroups) {
  if (job?.videoInfo?.has_audio === false) return false;
  const ranges = selectedGroups.flatMap((group) => (group.segments || []).map((segment) => ({
    start: Number(segment.start) || 0,
    end: Number(segment.end) || Number(segment.start) || 0,
  })));
  const transcript = [
    ...(Array.isArray(job?.speechAnalysis?.segments) ? job.speechAnalysis.segments : []),
    ...(Array.isArray(job?.transcript) ? job.transcript : []),
  ];
  return transcript.some((segment) => {
    const text = String(segment?.text || "").trim();
    const start = Number(segment?.start);
    const end = Number(segment?.end);
    return /[\p{L}\p{N}]/u.test(text) && Number.isFinite(start) && Number.isFinite(end)
      && ranges.some((range) => Math.min(range.end, end) - Math.max(range.start, start) >= .08);
  });
}

async function saveSegmentTechnique(group, segment, values) {
  if (!currentJob || actionBusy) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/event-groups/${encodeURIComponent(group.id)}/segments/${encodeURIComponent(segment.id)}/technique`, {
      method: "PATCH", headers: { "Content-Type": "application/json" }, body: values,
    });
    if (!commitJobAction(job, actionToken)) return;
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function bindRailEventActions(job) {
  const railBody = $("#railBody");
  if (!railBody) return;
  normalizeSegmentActionLabels(railBody);
  railBody.querySelectorAll(".event-group-row").forEach((row) => {
    const group = job.eventGroups.find((item) => item.id === row.dataset.eventGroup);
    if (!group) return;
    row.querySelector(".preview-event")?.addEventListener("click", () => previewEventGroup(group));
    row.querySelector(".rename-event")?.addEventListener("click", () => renameEventGroup(group));
    row.querySelector(".add-selection-event")?.addEventListener("click", () => addSelectionToEventGroup(group));
    row.querySelectorAll(".event-segment").forEach((segmentRow, segmentIndex) => {
      const segment = group.segments.find((item) => item.id === segmentRow.dataset.segmentId);
      const techniqueControls = segmentRow.querySelector(".segment-technique-controls");
      if (techniqueControls && !segmentRow.querySelector(".segment-technique-toggle")) {
        techniqueControls.classList.add("hidden");
        const toggle = document.createElement("button");
        toggle.type = "button";
        toggle.className = "segment-technique-toggle";
        toggle.textContent = "精修";
        toggle.setAttribute("aria-expanded", "false");
        const actionBar = segmentRow.querySelector(".event-segment-actions");
        const previewButton = actionBar?.querySelector(".preview-segment");
        if (previewButton) previewButton.after(toggle);
        else if (actionBar) actionBar.prepend(toggle);
        else techniqueControls.before(toggle);
        toggle.addEventListener("click", () => {
          const expanded = toggle.getAttribute("aria-expanded") !== "true";
          toggle.setAttribute("aria-expanded", String(expanded));
          toggle.textContent = expanded ? "收起精修" : "精修";
          techniqueControls.classList.toggle("hidden", !expanded);
        });
      }
      segmentRow.querySelector(".preview-segment")?.addEventListener("click", () => previewEventSegment(group, segment));
      segmentRow.querySelector(".delete-segment")?.addEventListener("click", () => deleteEventSegment(group, segment));
      segmentRow.querySelector(".move-segment-up")?.addEventListener("click", () => reorderEventSegment(group, segmentIndex, segmentIndex - 1));
      segmentRow.querySelector(".move-segment-down")?.addEventListener("click", () => reorderEventSegment(group, segmentIndex, segmentIndex + 1));
      segmentRow.querySelector(".move-segment-group")?.addEventListener("click", () => moveEventSegment(group, segment));
      segmentRow.querySelector(".segment-speed")?.addEventListener("change", (event) => saveSegmentTechnique(group, segment, { playbackRate: Number(event.target.value), speedLocked: true }));
      segmentRow.querySelector(".segment-transition")?.addEventListener("change", (event) => saveSegmentTechnique(group, segment, { transitionType: event.target.value, transitionLocked: true }));
      segmentRow.querySelector(".segment-bridge")?.addEventListener("change", (event) => saveSegmentTechnique(group, segment, { audioBridgeType: event.target.value, audioBridgeLocked: true }));
    });
  });
  const refreshEventReviewCta = () => {
    const footer = railBody.querySelector(".event-review-next");
    if (!footer) return;
    const count = railBody.querySelectorAll(".rail-event-check:checked").length;
    const countNode = footer.querySelector("[data-selected-event-count]");
    const button = footer.querySelector(".open-compose-stage");
    if (countNode) countNode.textContent = String(count);
    if (button) button.disabled = count === 0;
    syncReviewActionDock();
  };
  railBody.querySelectorAll(".rail-event-check").forEach((input) => input.addEventListener("change", () => {
    recordEventGroupSelection(input.value, input.checked);
    const group = job.eventGroups.find((item) => String(item.id) === String(input.value));
    if (group) pendingSegmentSelections.set(String(group.id), input.checked ? new Set((group.segments || []).map((segment) => String(segment.id))) : new Set());
    renderRailOutput(job);
    refreshEventReviewCta();
  }));
  railBody.querySelectorAll(".rail-segment-check").forEach((input) => input.addEventListener("change", () => {
    const group = job.eventGroups.find((item) => String(item.id) === String(input.dataset.groupId));
    if (!group) return;
    const selected = new Set(selectedSegmentIdsForGroup(group));
    if (input.checked) selected.add(String(input.value)); else selected.delete(String(input.value));
    pendingSegmentSelections.set(String(group.id), selected);
    const eventCheck = railBody.querySelector(`.rail-event-check[value="${CSS.escape(String(group.id))}"]`);
    if (eventCheck) eventCheck.checked = selected.size > 0;
    renderRailOutput(job);
    refreshEventReviewCta();
  }));
  refreshEventReviewCta();
}

function renderRailOutput(job) {
  const output = $("#railOutput");
  if (!output) return;
  if (job.status !== "awaiting_confirmation" || !job.eventGroups?.length) {
    output.classList.add("hidden");
    return;
  }
  const selected = selectedGroupsWithSegments(job);
  const total = selected.reduce((sum, group) => sum + group.segments.reduce((inner, segment) => inner + Number(segment.duration || (Number(segment.end) - Number(segment.start)) || 0), 0), 0);
  const segmentCount = selected.reduce((sum, group) => sum + group.segments.length, 0);
  const info = job.videoInfo || {};
  let singleReel = outputAssemblyMode === "single_reel";
  const savedSubtitleMode = String(job.brief?.subtitlePreference || job.request?.subtitleMode || "none");
  const briefSubtitle = savedSubtitleMode === "burn" ? "burn" : "none";
  const savedSubtitleStyle = String(job.brief?.subtitleStyle || job.request?.subtitleStyle || "clean");
  const briefSubtitleStyle = ["clean", "bold", "social"].includes(savedSubtitleStyle) ? savedSubtitleStyle : "clean";
  const multipleEvents = selected.length > 1;
  const subtitleAvailable = highlightSelectionHasSpeech(job, selected);
  if (!multipleEvents) {
    outputAssemblyMode = "single_reel";
    singleReel = true;
  }
  output.classList.remove("hidden");
  output.innerHTML = `<section class="composition-workbench">
      <header class="composition-header"><div><small>最终合成</small><strong>生成高光成片</strong></div><span>${selected.length} 个事件 · ${segmentCount} 个镜头</span></header>
      <div class="composition-selection"><div><b>当前选择</b><span>${total.toFixed(1)} 秒${job.totalTargetSeconds ? ` · 单条目标 ${Number(job.totalTargetSeconds).toFixed(1)} 秒` : " · 当前推荐总时长"}</span></div><button type="button" class="back-to-events">返回事件审核</button></div>
      ${multipleEvents ? `<section class="composition-step"><div class="composition-step-title"><div><strong>成片形式</strong><small>将已选事件合成一条视频，或分别导出</small></div></div><div class="output-mode-switch" role="group" aria-label="成片形式"><button type="button" data-output-mode="single_reel" class="${singleReel ? "active" : ""}">已选事件合成 1 条</button><button type="button" data-output-mode="separate_events" class="${singleReel ? "" : "active"}">分别导出已选事件</button></div></section>` : ""}
      <section class="composition-step"><div class="composition-step-title"><div><strong>按当前选择生成</strong><small>保持事件审核中的镜头顺序</small></div></div><div class="manual-mode-note"><strong>${singleReel ? "生成一条成片" : "分别导出已选事件"}</strong><span>${singleReel ? "不改变镜头顺序，直接合成。" : "每个已选事件导出为一条视频。"}</span><button type="button" class="generate-events" ${selected.length ? "" : "disabled"}>${singleReel ? "生成成片" : `导出 ${selected.length} 个事件`}</button></div></section>
      <div class="output-specs output-specs-summary"><div><span>格式</span><b>MP4 · H.264</b></div><div><span>分辨率</span><b>${info.width && info.height ? `${info.width}×${info.height}` : "保持源画面"}</b></div><div><span>码率</span><b>自动设置</b></div></div>
      ${subtitleAvailable ? `<details class="composition-step advanced-output"><summary><span class="composition-step-title"><span><strong>字幕设置</strong><small>所选镜头包含可转写对白</small></span></span></summary><div class="output-specs"><label class="subtitle-output-option"><span>字幕</span><select id="subtitleMode"><option value="none" ${briefSubtitle === "none" ? "selected" : ""}>不添加字幕</option><option value="burn" ${briefSubtitle === "burn" ? "selected" : ""}>添加 AI 字幕</option></select></label><label class="subtitle-output-option subtitle-style-option ${briefSubtitle === "burn" ? "" : "hidden"}"><span>样式</span><select id="subtitleStyle"><option value="clean" ${briefSubtitleStyle === "clean" ? "selected" : ""}>简洁 · 白字描边</option><option value="bold" ${briefSubtitleStyle === "bold" ? "selected" : ""}>醒目 · 加粗亮色</option><option value="social" ${briefSubtitleStyle === "social" ? "selected" : ""}>短视频 · 大字底框</option></select></label><small class="subtitle-warning ${briefSubtitle === "burn" ? "" : "hidden"}">源视频已有画面字幕时请保持“不添加字幕”，否则可能出现双字幕。</small></div></details>` : ""}
      ${job.reediting && job.outputs?.length ? '<button type="button" class="cancel-reedit">返回上一次结果</button>' : ""}
    </section>`;
  output?.querySelectorAll("[data-output-mode]").forEach((button) => button.addEventListener("click", () => {
    outputAssemblyMode = button.dataset.outputMode;
    renderRailOutput(job);
  }));
  output.querySelector("#subtitleMode")?.addEventListener("change", (event) => {
    const enabled = event.target.value === "burn";
    output.querySelector(".subtitle-style-option")?.classList.toggle("hidden", !enabled);
    output.querySelector(".subtitle-warning")?.classList.toggle("hidden", !enabled);
  });
  output.querySelector(".back-to-events")?.addEventListener("click", () => setDirectorStage("events"));
  output.querySelector(".generate-events")?.addEventListener("click", () => {
    const subtitleMode = output.querySelector("#subtitleMode")?.value || "none";
    const subtitleStyle = output.querySelector("#subtitleStyle")?.value || "clean";
    confirmEventGroups(selectedRailGroupIds(), outputAssemblyMode, Object.fromEntries([...pendingSegmentSelections].map(([id, values]) => [id, [...values]])), subtitleMode, subtitleStyle);
  });
  output.querySelector(".cancel-reedit")?.addEventListener("click", cancelCurrentJobReediting);
}

async function requestAutoPlans(job, scope = "selected_only", variantCount = 1, groupIdsOverride = null, skipConfirmation = false) {
  if (!job || actionBusy) return;
  const editMode = String(job.brief?.editMode || job.request?.editMode || "ai_plan");
  if (!skipConfirmation && editMode === "manual") return void showToast("当前为手动剪辑，请直接在时间轴和事件审核中选择镜头。");
  if (!skipConfirmation && editMode === "recommend_review") return void showToast("当前为 AI 推荐后审核，请先审核候选事件，再直接合成。");
  const groupIds = groupIdsOverride?.length ? [...groupIdsOverride] : selectedRailGroupIds();
  const segmentIds = Object.fromEntries([...pendingSegmentSelections].map(([id, values]) => [id, [...values]]));
  if (!groupIds.length) return void showToast("请至少选择一个事件高光");
  if (!skipConfirmation && !await requestActionConfirmation({ title: "AI 智能规划剪辑方案", summary: `将生成 ${variantCount} 个不同的局部镜头与叙事编排方案。`, details: [scope === "all_pool" ? "当前选择优先，必要时从全候选池补充" : "只在当前选择内重新编排", "每个候选只保留必要的小段", "先审核方案，确认后才渲染"] })) return;
  const actionToken = captureJobAction(job);
  if (!jobActionStillCurrent(actionToken)) return;
  setDirectorStage("compose");
  actionBusy = true;
  try {
    const { job: updated } = await apiJson(`/api/jobs/${job.id}/auto-plans`, { method: "POST", headers: { "Content-Type": "application/json" }, body: { scope, groupIds, segmentIds, targetSeconds: Number(job.totalTargetSeconds || job.request?.totalTargetSeconds || 0) || null, structure: job.brief?.structure || job.request?.structure || "auto", variantCount: Math.max(1, Math.min(5, Number(variantCount) || 1)), techniquePolicy: job.brief?.techniquePolicy || job.request?.techniquePolicy || { preset: "auto" } } });
    if (!jobActionStillCurrent(actionToken)) return;
    currentEventGroup = null;
    currentEventSegment = null;
    commitJobAction(updated, actionToken);
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) actionBusy = false; }
}

async function requestAlternativeCut(job) {
  if (!job || actionBusy) return;
  const actionToken = captureJobAction(job);
  if (!jobActionStillCurrent(actionToken)) return;
  actionBusy = true;
  setReviewLowerPanelMode("collapsed");
  try {
    const { job: updated } = await api(`/api/jobs/${job.id}/alternative`, { method: "POST" });
    if (!commitJobAction(updated, actionToken)) return;
    showToast("已开始在后台生成另一种剪法，当前版本会保留");
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function requestLlmOrder(job, groupIds, segmentIds = null) {
  if (!job || actionBusy) return;
  const actionToken = captureJobAction(job);
  if (!jobActionStillCurrent(actionToken)) return;
  actionBusy = true;
  setDirectorStage("conversation");
  try {
    const { job: updated } = await apiJson(`/api/jobs/${job.id}/llm-order`, { method: "POST", headers: { "Content-Type": "application/json" }, body: { groupIds, segmentIds } });
    if (!commitJobAction(updated, actionToken)) return;
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) actionBusy = false; }
}

function renderReviewRail(job) {
  const body = $("#railBody");
  const openCandidates = $("#openCandidateDrawer");
  if (!body) return;
  const railTitle = $("#railTitle");
  const contentMode = taskModePresentation(job).key === "content_extract";
  const vocabulary = workflowVocabulary(job);
  const execution = executionForJob(job);
  const generationFailed = contentGenerationFailed(job);
  const setRailTitle = (value) => { if (railTitle) railTitle.textContent = value; };
  const contentCandidates = Array.isArray(job.contentSearch?.candidates) ? job.contentSearch.candidates : [];
  const agentReviewPreviews = agentReviewPreviewsForJob(job);
  if (openCandidates) openCandidates.disabled = contentMode || !(job.candidates?.length);
  const hasGeneratedOutputs = Boolean(
    (job.outputs || []).length
    || (job.outputVersions || []).some((version) => (version.outputs || []).length),
  );
  if (candidateRailOpen && !contentMode && job.candidates?.length) {
    renderCandidateRail(job);
    return;
  }
  const unifiedPresentation = window.ClipTalkWorkspaceState?.derivePresentation?.(job);
  if (unifiedPresentation?.key === "waiting_instruction") {
    setRailTitle(unifiedPresentation.railTitle);
    body.innerHTML = `<div class="rail-empty rail-state-action"><strong>${escapeHtml(unifiedPresentation.headline)}</strong><p>${escapeHtml(unifiedPresentation.detail)}</p><small class="rail-summary">请在左侧对话框输入剪辑要求，或选择一种快速剪辑方式。</small></div>`;
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (unifiedPresentation?.key === "no_result") {
    setRailTitle(unifiedPresentation.railTitle);
    body.innerHTML = `<div class="rail-empty rail-state-action"><strong>${escapeHtml(unifiedPresentation.headline)}</strong><p>${escapeHtml(unifiedPresentation.detail)}</p><button type="button" class="primary" data-revise-no-result>修改剪辑要求</button><small class="rail-summary">不会重复上传素材；新要求会基于当前源视频继续。</small></div>`;
    body.querySelector("[data-revise-no-result]")?.addEventListener("click", () => {
      if (chatInput) {
        chatInput.value = "换一种查找方式：";
        chatInput.focus();
        chatInput.setSelectionRange(chatInput.value.length, chatInput.value.length);
      }
    });
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (["failed", "cancelled"].includes(String(unifiedPresentation?.key || ""))
    && !generationFailed
    && !(job.status === "failed" && job.stage === "interrupted")) {
    setRailTitle(unifiedPresentation.railTitle);
    body.innerHTML = `<div class="rail-empty rail-state-action"><strong>${escapeHtml(unifiedPresentation.headline)}</strong><p>${escapeHtml(unifiedPresentation.detail)}</p><button type="button" class="primary" data-open-agent-activity>查看失败步骤</button><button type="button" data-revise-failed>修改要求后重试</button></div>`;
    body.querySelector("[data-open-agent-activity]")?.addEventListener("click", () => {
      $("#agentPlanDock")?.scrollIntoView({ behavior: "smooth", block: "center" });
      $("#agentPlanDock button")?.focus();
    });
    body.querySelector("[data-revise-failed]")?.addEventListener("click", () => chatInput?.focus());
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (job.status === "briefing") {
    setRailTitle("理解需求");
    body.innerHTML = `<div class="rail-empty"><span class="empty-thinking-orb" data-thinking-orb data-orb-state="composing" data-orb-size="64" data-orb-theme="light" data-orb-label="正在理解剪辑需求"></span><strong>正在理解剪辑需求</strong><p>${escapeHtml(job.detail || "正在整理目标、重点和限制")}</p></div>`;
    syncThinkingOrbs(body);
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (job.status === "brief_confirmation") {
    const brief = job.brief || {};
    setRailTitle("需求确认");
    body.innerHTML = `<div class="rail-section-title"><strong>智能高光需求简报</strong><b>等待确认</b></div><p class="rail-summary">检查并修改剪辑需求，确认后开始分析。</p><section class="brief-review-card"><dl><div><dt>剪辑目标</dt><dd>${escapeHtml(brief.objective || "事件高光合集")}</dd></div><div><dt>单条成片目标</dt><dd>${brief.targetDurationSeconds ? `${Number(brief.targetDurationSeconds).toFixed(1)} 秒` : "智能推荐"}</dd></div><div><dt>关注重点</dt><dd>${escapeHtml((brief.focus || ["综合判断"]).join("、"))}</dd></div><div><dt>风格节奏</dt><dd>${escapeHtml([brief.style?.pace, brief.style?.tone].filter(Boolean).join(" · ") || "纪实自然")}</dd></div></dl><button type="button" class="confirm-brief-button" data-focus-brief>编辑并确认简报</button></section>`;
    body.querySelector("[data-focus-brief]")?.addEventListener("click", () => $("#chatMessages .brief-editor")?.scrollIntoView({ behavior: "smooth", block: "center" }));
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (job.status === "awaiting_model_decision") {
    setRailTitle("等待处理");
    const candidates = job.candidates || [];
    body.innerHTML = `<div class="rail-section-title"><strong>${candidates.length ? `已精修候选（${candidates.length}）` : "候选审核区"}</strong><b>检查点已保存</b></div><p class="rail-summary">请在分析进度卡中选择重试、降级继续或取消。${candidates.length ? "这些精修结果不会因重试而丢失。" : "完成当前阶段后会继续发现候选。"}</p>${candidates.length
      ? `<div class="paused-candidate-list">${candidates.map((candidate, index) => `<article><span><b>${index + 1}. ${escapeHtml(candidate.title)}</b><small>${formatTime(candidate.start)} → ${formatTime(candidate.end)} · ${Number(candidate.duration).toFixed(1)} 秒</small></span><em>${Math.round(candidate.score)}</em></article>`).join("")}</div>`
      : `<div class="rail-skeleton-list" aria-hidden="true">${Array.from({ length: 3 }, () => `<i><span></span><b></b><em></em></i>`).join("")}</div>`}`;
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (job.status === "awaiting_agent_plan") {
    setRailTitle(unifiedPresentation?.railTitle || "任务状态");
    const agent = job.agent || {};
    const planStatus = String(agent.status || "planning");
    const completed = Math.max(0, Number(agent.completedSteps) || 0);
    const total = Math.max(0, Number(agent.totalSteps) || 0);
    const step = agent.currentStepTitle || "下一步";
    const copy = planStatus === "awaiting_confirmation"
      ? { title: "计划已生成，等待确认", detail: "请在对话区核对目标、当前阶段和下一步；确认后才会开始分析或渲染。" }
      : ["approved", "running"].includes(planStatus)
        ? { title: `正在执行：${step}`, detail: total ? `已完成 ${completed}/${total} 步；结果会写入当前任务。` : "正在启动第一个计划步骤。" }
        : planStatus === "action_required"
          ? { title: `需要确认：${step}`, detail: "请在对话区完成当前选择，再继续执行。" }
          : planStatus === "preview_ready"
            ? { title: "审核结果已生成", detail: "可以查看样片、封面或继续输入新的精剪要求。" }
            : planStatus === "failed"
              ? { title: "任务没有完成", detail: "请查看失败原因，调整目标或重新执行失败链路。" }
              : planStatus === "cancelled"
                ? { title: "计划已停止", detail: "可以继续输入新的剪辑目标，重新生成计划。" }
                : { title: "正在生成剪辑计划", detail: "素材工作区已保存。当前只整理目标、阶段和确认点；此时不会开始分析或渲染。" };
    const previewMarkup = agentReviewPreviews.length
      ? `<section class="agent-review-preview-panel"><div class="rail-section-title"><strong>审核样片已生成</strong><b>${agentReviewPreviews.length} 个样片</b></div><p class="rail-summary">选择样片预览，确认效果后生成成片。</p>${agentReviewPreviewMarkup(agentReviewPreviews)}</section>`
      : "";
    const planAction = planStatus === "awaiting_confirmation"
      ? '<button type="button" class="primary" data-focus-agent-plan>核对并确认计划</button>'
      : planStatus === "action_required"
        ? '<button type="button" class="primary" data-focus-agent-plan>处理当前确认</button>'
        : "";
    body.innerHTML = previewMarkup || `<div class="rail-empty rail-state-action">${planStatus === "planning" ? '<span class="empty-thinking-orb" data-thinking-orb data-orb-state="composing" data-orb-size="64" data-orb-theme="light" data-orb-label="正在生成 Agent 计划"></span>' : ""}<strong>${escapeHtml(copy.title)}</strong><p>${escapeHtml(copy.detail)}</p>${planAction}</div>`;
    body.querySelector("[data-focus-agent-plan]")?.addEventListener("click", () => {
      $("#agentPlanDock")?.scrollIntoView({ behavior: "smooth", block: "center" });
      $("#agentPlanDock [data-agent-plan-confirm], #agentPlanDock button")?.focus();
    });
    bindAgentReviewPreviewActions(body, agentReviewPreviews);
    if (planStatus === "planning") syncThinkingOrbs(body);
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (execution.active && !["auto_composition", "quality_review"].includes(execution.operation)) {
    const rendering = execution.operation === "render" || ["rendering", "render"].includes(String(job.stage || ""));
    setRailTitle(rendering ? `合成${vocabulary.output}` : contentMode ? "内容检索" : "实时分析");
    body.innerHTML = `<div class="rail-section-title"><strong>${rendering ? `正在合成${vocabulary.output}` : contentMode ? "正在检索内容" : "候选审核区"}</strong><b>${rendering ? "按已确认片段生成" : contentMode ? "完成后确认" : "分析后显示"}</b></div>
      <p class="rail-summary">${rendering ? "正在按最终确认的时间范围合成并检查视频文件。完成后直接进入播放器，不需要再次确认。" : contentMode ? `新的匹配片段会在检索完成后显示。${hasGeneratedOutputs ? "已有内容视频仍可在对话框中点击播放。" : ""}` : "候选镜头与高光事件整理完成后将在这里出现。"}</p>
      <div class="rail-skeleton-list" aria-hidden="true">${Array.from({ length: 4 }, () => `<i><span></span><b></b><em></em></i>`).join("")}</div>`;
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (contentMode && (job.status === "awaiting_content_confirmation" || generationFailed)) {
    const warning = job.workflow?.actionRequired;
    const selectedCount = Number(job.contentSearch?.defaultSelectedIds?.length || 0);
    setRailTitle(workflowKindForJob(job) === "person_edit" ? "核对人物出镜" : "片段确认");
    const searchId = String(job.contentSearch?.id || "");
    const reviewSection = [...document.querySelectorAll("#chatMessages .content-search-review")]
      .find((node) => String(node.dataset.contentSearchId || "") === searchId);
    const reviewMessage = reviewSection?.closest(".content-search-message");
    body.replaceChildren();
    if (generationFailed) {
      const notice = document.createElement("section");
      notice.className = "content-generation-retry-notice";
      notice.innerHTML = `<strong>上次合成没有完成</strong><p>${escapeHtml(job.error || "已保留全部确认片段，可直接重新合成，不需要重新识别人物。")}</p><small>人物选择和出镜边界均未丢失</small>`;
      body.append(notice);
    }
    if (duplicateContentCompositionNotice
      && String(duplicateContentCompositionNotice.jobId || "") === String(job.id || "")
      && String(duplicateContentCompositionNotice.searchId || "") === searchId) {
      const notice = document.createElement("section");
      notice.className = "duplicate-content-notice";
      const versionNumber = Number(duplicateContentCompositionNotice.existingVersionNumber || 0);
      notice.innerHTML = `<div><strong>没有重复生成</strong><p>${escapeHtml(duplicateContentCompositionNotice.message || "当前片段、顺序和字幕与已有版本完全一致。")}</p></div>${duplicateContentCompositionNotice.filename ? `<button type="button" data-preview-duplicate-content>预览 V${versionNumber || "已有版本"}</button>` : ""}`;
      notice.querySelector("[data-preview-duplicate-content]")?.addEventListener("click", () => selectOutput(duplicateContentCompositionNotice.filename, true));
      body.append(notice);
    }
    if (reviewMessage) {
      const template = document.createElement("template");
      template.innerHTML = contentSearchHistorySummaryMarkup(job.contentSearch, { current: true, expandable: false, job }).trim();
      const summary = template.content.firstElementChild;
      reviewMessage.replaceWith(summary);
      summary?.querySelector("[data-content-review-focus]")?.addEventListener("click", () => {
        setDirectorStage("events");
        body.scrollTo({ top: 0, behavior: "smooth" });
      });
      reviewMessage.classList.add("rail-content-search-message");
      body.append(reviewMessage);
    } else {
      body.insertAdjacentHTML("beforeend", `<div class="rail-section-title"><strong>${contentCandidates.length ? "检索结果 · 选择片段" : escapeHtml(warning?.title || "需要补充确认")}</strong><b>${contentCandidates.length} 个匹配片段</b></div><p class="rail-summary">${contentCandidates.length ? `当前默认选中 ${selectedCount} 段。正在准备片段核对区。` : escapeHtml(warning?.message || "请确认人物、Speaker 或查找依据后继续。")} ${hasGeneratedOutputs ? "以前生成的内容视频仍可预览和下载。" : ""}</p>`);
    }
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  // A rendered version is the primary result, even when the backend keeps the
  // job in awaiting_confirmation so the source event pool remains reusable.
  // Do not show an empty compose form (0 events / 0 clips) underneath it.
  if (hasGeneratedOutputs && job.status === "awaiting_confirmation") {
    setRailTitle("版本与交付");
    const autoVersions = jobOutputVersions(job).filter((version) => version.generationBatchId || version.previewOnly);
    const sampleCount = autoVersions.filter((version) => version.previewOnly && String(version.variantKind || "independent") === "independent").length;
    const formalCount = autoVersions.filter((version) => !version.previewOnly || version.variantKind === "formal_export").length;
    const versionSummary = `${sampleCount} 个独立样片${formalCount ? ` · ${formalCount} 个成片版本` : ""}`;
    body.innerHTML = contentMode
      ? `<div class="rail-section-title"><strong>内容视频已生成</strong><b>可播放或下载</b></div><p class="rail-summary">成片 MP4 已单独归档到“成片版本”。源视频保持不变；需要调整时返回片段确认。</p><button type="button" class="reedit-job-button">返回片段确认</button>`
      : `<div class="rail-section-title"><strong>高光成片已生成</strong><b>${job.autoComposition?.status === "completed" ? versionSummary : "可播放或下载"}</b></div><p class="rail-summary">成片 MP4 已单独归档到“成片版本”，源视频保持不变。${job.autoComposition?.status === "completed" ? `当前有 ${versionSummary}，可在版本列表中播放比较。` : "需要更换镜头时，可返回事件审核继续调整。"}</p>${job.autoComposition?.status === "completed" ? "" : '<button type="button" class="reedit-job-button">返回事件审核</button>'}`;
    body.querySelector(".reedit-job-button")?.addEventListener("click", reopenCurrentJobForEditing);
    if (jobOutputCount(job)) renderOutputs(job);
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  if (job.status === "awaiting_confirmation" && job.eventGroups?.length) {
    const recommended = new Set(job.recommendedGroupIds || []);
    const target = Number(job.totalTargetSeconds || job.request?.totalTargetSeconds || 0);
    const allocated = Number(job.allocatedTotalSeconds || 0);
    const tolerance = Number(job.durationTolerance || .1);
    const over = target && Math.abs(allocated - target) > target * tolerance;
    const theme = String(job.request?.theme || "综合判断").split(/[，,、\s]+/).filter(Boolean).slice(0, 4);
    const profile = job.contentProfile || {};
    setRailTitle(`事件高光（${job.eventGroups.length}）`);
    const qualityNotice = execution.outcome === "no_acceptable_output"
      ? `<div class="rail-quality-notice"><strong>自动样片需要人工复核</strong><span>已渲染版本仍可预览比较，也可以调整事件后重新生成。</span></div>`
      : "";
    const durationBudget = target
      ? `<div class="duration-budget${over ? " over" : ""}"><span><b>${allocated.toFixed(1)} 秒</b><em>目标 ${target.toFixed(1)} 秒</em></span><i><b style="width:${Math.min(100, allocated / target * 100)}%"></b></i></div>`
      : "";
    body.innerHTML = `${qualityNotice}<div class="rail-section-title"><strong>高光事件</strong><b>${target ? `推荐 ${recommended.size} 个事件` : `推荐 ${recommended.size} 个 · ${allocated.toFixed(1)} 秒`}</b></div>
      <p class="rail-summary">每个事件包含一组相关镜头；可合成一条视频，也可分别导出。</p>
      ${durationBudget}
      <div class="event-group-list">${job.eventGroups.map((group, groupIndex) => `<article class="event-group-row${recommended.has(group.id) ? " recommended" : ""}${currentEventGroup?.id === group.id ? " active" : ""}" data-event-group="${escapeHtml(group.id)}">
        <header><input class="rail-event-check" type="checkbox" value="${escapeHtml(group.id)}" ${recommended.has(group.id) ? "checked" : ""}><span><strong>${escapeHtml(group.title)}</strong><small>${group.segments.length} 个镜头 · ${Number(group.actualDuration).toFixed(1)} 秒</small></span><b class="event-score" aria-label="推荐评分 ${Math.round(group.score)} 分"><small>评分</small>${Math.round(group.score)}</b></header>
        <p>${escapeHtml(group.summary)}</p><div class="event-group-actions"><button class="preview-event" type="button">组合预览</button><button class="rename-event" type="button">命名</button><button class="add-selection-event" type="button" ${job.manualSelection ? "" : "disabled"}>加入选区</button></div>
        <details ${currentEventGroup?.id === group.id || groupIndex === 0 ? "open" : ""}><summary>事件镜头 · ${selectedSegmentIdsForGroup(group).length}/${group.segments.length} 个已选</summary><div class="event-segments">${group.segments.map((segment, segmentIndex) => `<div class="event-segment${currentEventSegment?.id === segment.id ? " active" : ""}" data-segment-id="${escapeHtml(segment.id)}"><input class="rail-segment-check" data-group-id="${escapeHtml(group.id)}" type="checkbox" value="${escapeHtml(segment.id)}" ${selectedSegmentIdsForGroup(group).includes(String(segment.id)) ? "checked" : ""} aria-label="选择${escapeHtml(segment.role || "镜头")}"><span><b>${segmentIndex + 1}. ${escapeHtml(segment.role)}</b><small>${formatTime(segment.start)} → ${formatTime(segment.end)} · 输出约 ${Number(segment.effectiveDuration || segment.duration || (Number(segment.end) - Number(segment.start))).toFixed(1)} 秒</small>${audioEvidenceMarkup(segment.audioEvidence)}<span class="segment-technique-controls"><label>速度<select class="segment-speed"><option value="1" ${Number(segment.playbackRate || 1) === 1 ? "selected" : ""}>1×</option><option value="1.1" ${Number(segment.playbackRate) === 1.1 ? "selected" : ""}>1.1×</option><option value="1.25" ${Number(segment.playbackRate) === 1.25 ? "selected" : ""}>1.25×</option><option value="1.5" ${Number(segment.playbackRate) === 1.5 ? "selected" : ""}>1.5×</option></select></label><label>转场<select class="segment-transition"><option value="cut" ${(segment.transitionIn?.type || "cut") === "cut" ? "selected" : ""}>硬切</option><option value="dissolve" ${segment.transitionIn?.type === "dissolve" ? "selected" : ""}>短叠化</option><option value="fade_black" ${segment.transitionIn?.type === "fade_black" ? "selected" : ""}>淡黑</option></select></label><label>声音<select class="segment-bridge"><option value="none" ${(segment.audioBridge?.type || "none") === "none" ? "selected" : ""}>同步</option><option value="j_cut" ${segment.audioBridge?.type === "j_cut" ? "selected" : ""}>J-cut</option><option value="l_cut" ${segment.audioBridge?.type === "l_cut" ? "selected" : ""}>L-cut</option></select></label></span></span><div class="event-segment-actions"><button class="preview-segment" type="button">预览</button><button class="move-segment-up" type="button" aria-label="上移镜头" ${segmentIndex === 0 ? "disabled" : ""}>↑</button><button class="move-segment-down" type="button" aria-label="下移镜头" ${segmentIndex === group.segments.length - 1 ? "disabled" : ""}>↓</button><button class="move-segment-group" type="button">移动</button><button class="delete-segment" type="button">删除</button></div></div>`).join("")}</div></details>
      </article>`).join("")}</div>
      <details class="analysis-tags"><summary>本次分析依据${job.directorDegraded ? " · 事件归组已降级" : ""}${job.speechAnalysis?.degraded ? " · 语音已降级" : ""}<small>按需查看</small></summary><div><span>${job.request?.analysisMode === "audiovisual" ? "视听综合" : "纯视觉"}</span>${job.speechAnalysis?.status === "ready" ? `<span>SenseVoice · ${Number(job.speechAnalysis.segments || 0)} 段</span>` : ""}${job.speechAnalysis?.diarization ? `<span>说话人分段</span>` : ""}${profile.primaryType ? `<span>${escapeHtml(profile.primaryType)}</span>` : ""}${profile.narrativeMode ? `<span>${escapeHtml(profile.narrativeMode)}</span>` : ""}${(theme.length ? theme : ["综合判断"]).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}</div></details>
      <footer class="event-review-next"><span><b data-selected-event-count>${recommended.size}</b> 个事件已选 · 可继续调整镜头</span><button type="button" class="open-compose-stage" ${recommended.size ? "" : "disabled"}>继续生成成片</button></footer>`;
    bindRailEventActions(job);
    body.querySelector(".open-compose-stage")?.addEventListener("click", () => setDirectorStage("compose"));
    renderRailOutput(job);
    return;
  }
  if (job.status === "awaiting_confirmation" && job.candidates?.length) {
    const recommended = new Set(job.recommendedIndices || []);
    setRailTitle(`历史候选（${job.candidates.length}）`);
    body.innerHTML = `<div class="rail-section-title"><strong>单镜头候选</strong><b>兼容旧任务</b></div>
      <p class="rail-summary">该任务创建于事件编排功能上线前，可继续按原候选生成；新任务会自动使用多镜头事件成片。</p>
      <div class="legacy-candidate-list">${job.candidates.map((candidate, index) => `<label class="legacy-candidate-row"><input type="checkbox" value="${candidate.index}" ${recommended.has(candidate.index) ? "checked" : ""}><span><strong>${index + 1}. ${escapeHtml(candidate.title)}</strong><small>${formatTime(candidate.start)} → ${formatTime(candidate.end)} · ${Number(candidate.duration).toFixed(1)} 秒</small></span><b>${Math.round(candidate.score)}</b><button type="button" data-legacy-preview="${candidate.index}">预览</button></label>`).join("")}</div>`;
  body?.querySelectorAll("[data-legacy-preview]").forEach((button) => button.addEventListener("click", (event) => {
      event.preventDefault();
      previewCandidate(Number(button.dataset.legacyPreview));
    }));
    const output = $("#railOutput");
    if (!output) return;
    output.classList.remove("hidden");
    const updateLegacyOutput = () => {
      const indices = [...(body?.querySelectorAll(".legacy-candidate-row input:checked") || [])].map((input) => Number(input.value));
      const selected = job.candidates.filter((candidate) => indices.includes(Number(candidate.index)));
      output.innerHTML = `<div class="output-title"><strong>输出设置</strong><span>兼容旧任务</span></div><div class="output-specs"><div><span>格式</span><b>MP4 · H.264</b></div><div><span>分辨率</span><b>保持源画面</b></div><div><span>输出</span><b>1 条成片 · ${selected.length} 个镜头</b></div></div><button type="button" class="generate-events" ${indices.length ? "" : "disabled"}>✦ 合成 1 条高光成片</button>`;
      output.querySelector(".generate-events")?.addEventListener("click", () => confirmCandidates(indices, "single_reel"));
    };
    body?.querySelectorAll(".legacy-candidate-row input").forEach((input) => input.addEventListener("change", updateLegacyOutput));
    updateLegacyOutput();
    return;
  }
  if (job.status === "completed") {
    if (!hasGeneratedOutputs && agentReviewPreviews.length) {
      setRailTitle(`审核样片（${agentReviewPreviews.length}）`);
      body.innerHTML = `<section class="agent-review-preview-panel"><div class="rail-section-title"><strong>审核样片已生成</strong><b>等待确认</b></div><p class="rail-summary">选择样片预览，确认时间线和封面后生成成片。</p>${agentReviewPreviewMarkup(agentReviewPreviews)}</section>`;
      bindAgentReviewPreviewActions(body, agentReviewPreviews);
      renderOutputPreviewSelector(job);
      $("#railOutput")?.classList.add("hidden");
      return;
    }
    setRailTitle("版本与交付");
    const autoDone = job.autoComposition?.status === "completed";
    const autoVersions = jobOutputVersions(job).filter((version) => version.generationBatchId || version.previewOnly);
    const sampleCount = autoVersions.filter((version) => version.previewOnly && String(version.variantKind || "independent") === "independent").length;
    const formalCount = autoVersions.filter((version) => !version.previewOnly || version.variantKind === "formal_export").length;
    const versionSummary = `${sampleCount} 个独立样片${formalCount ? ` · ${formalCount} 个成片版本` : ""}`;
    body.innerHTML = `<div class="rail-section-title"><strong>${contentMode ? "内容视频已生成" : "视频已生成"}</strong><b>${autoDone ? versionSummary : "已完成"}</b></div><p class="rail-summary">${autoDone ? `已生成 ${versionSummary}，可在成片版本中查看。` : contentMode ? "可以继续预览，或返回片段确认进行调整。" : "可以继续预览，或返回事件审核进行调整。"}</p>${autoDone ? "" : `<button type="button" class="reedit-job-button">${contentMode ? "返回片段确认" : "返回事件审核"}</button>`}`;
    body.querySelector(".reedit-job-button")?.addEventListener("click", reopenCurrentJobForEditing);
    if (jobOutputCount(job)) renderOutputs(job);
    $("#railOutput")?.classList.add("hidden");
    return;
  }
  setRailTitle(job.status === "failed" ? (generationFailed ? `${vocabulary.output}生成失败` : "分析失败") : "任务已停止");
  const interrupted = job.status === "failed" && job.stage === "interrupted";
  const resumeLabel = job.resumeAvailable ? "↻ 从检查点恢复分析" : "↻ 使用原素材重新分析";
  const resumeHint = job.resumeAvailable ? "已保存的媒体、波形和阶段检查点会优先复用。" : "未找到阶段检查点，将复用已上传的原素材。";
  body.innerHTML = `<div class="rail-empty"><span class="empty-thinking-orb" data-thinking-orb data-orb-state="shaping" data-orb-size="64" data-orb-theme="light" data-orb-label="任务未完成"></span><strong>${escapeHtml(job.detail || "任务未完成")}</strong><p>${escapeHtml(job.error || "返回全部任务后，可新建任务并重新选择素材。")}</p>${interrupted ? `<button type="button" class="primary resume-interrupted-job">${resumeLabel}</button><small class="rail-summary">${resumeHint}</small>` : '<button type="button" class="primary return-home-from-failure">返回全部任务</button>'}</div>`;
  body.querySelector(".resume-interrupted-job")?.addEventListener("click", () => resolveModelDecision("retry"));
  body.querySelector(".return-home-from-failure")?.addEventListener("click", () => resetWorkspace());
  $("#railOutput")?.classList.add("hidden");
}

function syncReviewSelectionClasses() {
  document.querySelectorAll(".event-group-row").forEach((row) => row.classList.toggle("active", row.dataset.eventGroup === currentEventGroup?.id));
  document.querySelectorAll(".event-segment").forEach((row) => row.classList.toggle("active", row.dataset.segmentId === currentEventSegment?.id));
  document.querySelectorAll(".drawer-candidate").forEach((row) => row.classList.toggle("active", Number(row.dataset.drawerCandidate) === Number(currentCandidate?.index)));
}

function renderOutputs(job) {
  const entries = orderedJobOutputs(job);
  const outputs = entries.map(({ item }) => item);
  const section = $("#clipSection");
  const strip = $("#clipStrip");
  const picker = $("#clipVersionPicker");
  const summary = $("#clipSummary");
  const reeditButton = $("#outputReeditButton");
  section?.classList.add("hidden");
  if (section) section.style.display = "none";
  if (!outputs.length) {
    if (strip) strip.innerHTML = "";
    if (picker) picker.innerHTML = "";
    renderOutputPreviewSelector(job);
    showSource();
    return;
  }
  const contentMode = taskModePresentation(job).key === "content_extract";
  if (picker) {
    const sourceSelected = viewerMediaKind !== "output";
    const sourceButton = `<button type="button" class="clip-picker-button${sourceSelected ? " active" : ""}" data-clip-picker-source role="tab" aria-selected="${sourceSelected ? "true" : "false"}">源视频</button>`;
    const outputButtons = entries.map(({ item: output, version }) => {
      const selected = currentOutput?.filename === output.filename;
      const identity = outputVersionIdentity(version, output);
      const label = contentMode && !currentOutputIsReviewSample(output, version)
        ? `${identity} · 内容视频`
        : identity;
      return `<button type="button" class="clip-picker-button${selected ? " active" : ""}" data-clip-picker-output="${escapeHtml(output.filename || "")}" role="tab" aria-selected="${selected ? "true" : "false"}">${escapeHtml(label)}</button>`;
    }).join("");
    picker.innerHTML = sourceButton + outputButtons;
    picker.querySelector("[data-clip-picker-source]")?.addEventListener("click", () => showSource({ preserveMappedTime: true }));
    picker.querySelectorAll("[data-clip-picker-output]").forEach((button) => button.addEventListener("click", () => selectOutput(button.dataset.clipPickerOutput, true)));
  }
  if (summary) {
    const metrics = jobOutputMetrics(job);
    summary.textContent = [
      metrics.formalVersionCount ? `${metrics.formalVersionCount} 个正式版本` : "",
      metrics.reviewSampleCount ? `${metrics.reviewSampleCount} 个审核样片` : "",
      metrics.formalVideoCount > metrics.formalVersionCount ? `${metrics.formalVideoCount} 条成片` : "",
    ].filter(Boolean).join(" · ");
  }
  if (strip) {
    strip.innerHTML = entries.map(({ item: output, version }) => {
      const identity = outputVersionIdentity(version, output);
      const presentation = autoVersionPresentation(job, version);
      const outputTitle = cleanDisplayText(output.displayTitle)
        || cleanDisplayText(output.title)
        || cleanDisplayText(presentation.displayName, "成片");
      const label = contentMode
        ? `${identity} · 内容视频`
        : `${identity} · ${outputTitle}`;
      const selected = currentOutput?.filename === output.filename;
      const qualityLabel = outputVersionQualityLabel(version, output);
      const quality = outputVersionQuality(version, output);
      const variantKind = String(version.variantKind || (version.previewOnly || output.previewOnly ? "independent" : "formal_export"));
      const kindClass = variantKind === "formal_export" ? "formal" : variantKind === "repair" ? "repair" : "sample";
      const score = Number(version.reviewReport?.overallScore ?? output.reviewReport?.overallScore ?? output.qualityReport?.score);
      const editableSegments = (output.segments || []).filter((segment) => Number(segment?.sourceEnd ?? segment?.end) > Number(segment?.sourceStart ?? segment?.start));
      const canFineEdit = output.capabilities?.canEdit !== false && Boolean(output.filename && (version.id === "agent-review-previews" ? sourceEditSessionForOutput(output)?.clips?.length : version.id && editableSegments.length));
      const editLabel = canFineEdit ? "编辑此版本" : "无可编辑时间线";
      const editReason = canFineEdit ? "在版本编辑时间线中继续调整" : "这个旧版本没有保存可编辑片段信息，可返回片段确认后生成新版本";
      const versionBadge = identity;
      const coverThumb = output.coverUrl
        ? `<i class="clip-version-index has-cover"><img src="${escapeHtml(output.coverUrl)}" alt="${escapeHtml(label)}封面" loading="lazy"><b>${escapeHtml(versionBadge)}</b></i>`
        : `<i class="clip-version-index">${escapeHtml(versionBadge)}</i>`;
      return `<div class="clip-version-entry quality-${escapeHtml(quality)} kind-${kindClass}"><button type="button" class="auto-version-button clip-version-button${selected ? " active" : ""}${version.recommended ? " recommended" : ""}" data-auto-output="${escapeHtml(output.filename || "")}" data-auto-version="${escapeHtml(version.id || identity)}" aria-pressed="${selected ? "true" : "false"}">${coverThumb}<span><strong>${escapeHtml(label.replace(`${identity} · `, ""))}</strong><em>${escapeHtml(qualityLabel)}</em></span><small>${Number(output.segmentCount || output.segments?.length || 0)} 个片段 · ${Number(output.duration || 0).toFixed(1)} 秒${Number.isFinite(score) ? ` · ${Math.round(score)} 分` : ""}</small></button><div class="clip-version-actions">${outputVersionDownloadLinkMarkup(version, output, { className: "clip-version-download" })}<button type="button" class="clip-version-edit" data-secondary-edit-version="${escapeHtml(version.id || "")}" data-secondary-edit-output="${escapeHtml(output.filename || "")}" ${canFineEdit ? "" : "disabled aria-disabled=\"true\""} title="${escapeHtml(editReason)}">${editLabel}</button></div></div>`;
    }).join("");
    strip.querySelectorAll("[data-auto-output]").forEach((button) => button.addEventListener("click", () => {
      strip.querySelectorAll(".auto-version-button").forEach((item) => item.classList.toggle("active", item === button));
      selectOutput(button.dataset.autoOutput, true);
    }));
    strip.querySelectorAll("[data-secondary-edit-version]:not(:disabled)").forEach((button) => button.addEventListener("click", () => {
      window.ClipTalkVersionAction(button.dataset.secondaryEditOutput, "edit").catch((error) => showToast(error.message));
    }));
    strip.querySelectorAll(".clip-version-actions").forEach((actions, index) => {
      const { item, version } = entries[index];
      const preview = currentOutputIsReviewSample(item, version);
      const button = document.createElement("button");
      button.type = "button";
      button.textContent = preview ? "生成成片" : item.kept ? "已长期保留" : "长期保留";
      button.disabled = preview ? item.capabilities?.canExport === false : Boolean(item.kept) || item.capabilities?.canKeep === false;
      button.addEventListener("click", async () => {
        button.disabled = true;
        try { await window.ClipTalkVersionAction(item.filename, preview ? "export" : "keep"); }
        catch (error) { showToast(error.message); }
        finally { if (button.isConnected) button.disabled = Boolean(!preview && item.kept); }
      });
      actions.append(button);
    });
  }
  if (reeditButton) {
    reeditButton.textContent = contentMode ? "返回片段确认" : "返回事件审核";
    reeditButton.classList.toggle("hidden", !["completed", "awaiting_confirmation"].includes(String(job.status || "")));
    reeditButton.onclick = reopenCurrentJobForEditing;
    const actions = reeditButton.parentElement;
    actions?.querySelector(".output-edit-query-button")?.remove();
    if (contentMode && job.status === "completed" && actions) {
      const editQueryButton = document.createElement("button");
      editQueryButton.type = "button";
      editQueryButton.className = "output-edit-query-button";
      editQueryButton.textContent = "修改查找条件";
      editQueryButton.addEventListener("click", () => {
        setDirectorStage("conversation");
        chatInput.value = "";
        chatInput.placeholder = "输入新的查找条件，例如：只保留说话人 B 回答核心问题的片段";
        chatInput.focus();
      });
      actions.append(editQueryButton);
    }
  }
  renderOutputPreviewSelector(job);
  const firstSelection = initialOutputJobId !== job.id;
  initialOutputJobId = job.id;
  if (!firstSelection && !currentOutput) return; // Keep an intentional source/candidate inspection.
  if (!firstSelection && outputs.some(item => item.filename === currentOutput?.filename)) {
    currentOutput = outputs.find(item => item.filename === currentOutput.filename);
    syncCurrentOutputEditAction(job);
    syncLibrarySaveAction(job, currentOutput);
    return; // Metadata updates must not reset playback, zoom, or the playhead.
  }
  const route = new URLSearchParams(location.hash.replace(/^#/, ""));
  const explicit = route.get("job") === job.id ? outputs.find(item => item.filename === route.get("output")) : null;
  const sample = entries.filter(({ item, version }) => currentOutputIsReviewSample(item, version)
    && (!item.planId || item.planId === job.agent?.planId)).sort((a, b) =>
    Number(Boolean(b.item.planId && b.item.planId === job.agent?.planId)) - Number(Boolean(a.item.planId && a.item.planId === job.agent?.planId))
    || (window.ClipTalkAgentWorkspace?.previewPriority?.(b.item) || 0) - (window.ClipTalkAgentWorkspace?.previewPriority?.(a.item) || 0)
    || String(b.item.createdAt || "").localeCompare(String(a.item.createdAt || ""))
    || Number(b.item.revision || 0) - Number(a.item.revision || 0)
    || Number(Boolean(b.item.socialReframe)) - Number(Boolean(a.item.socialReframe)))[0]?.item;
  const defaultOutput = job.presentation?.key === "preview_review" ? sample : null;
  const preferred = !firstSelection && currentOutput && outputs.some((item) => item.filename === currentOutput.filename)
    ? currentOutput.filename
    : explicit?.filename || defaultOutput?.filename || selectedJobOutput(job)?.item.filename || outputs.at(-1).filename;
  const preferredOutput = outputs.find((item) => item.filename === preferred) || outputs[0];
  const suppressLegacySection = document.body.classList.contains("ct-workbench-v4")
    && currentOutputIsReviewSample(preferredOutput);
  if (suppressLegacySection) {
    section?.classList.add("hidden", "ct-v4-preview-suppressed");
    if (section) section.style.display = "none";
  } else {
    section?.classList.remove("hidden", "ct-v4-preview-suppressed");
    if (section) section.style.removeProperty("display");
  }
  selectOutput(preferred);
  syncLegacyOutputPanelsForV4();
}

function jobRenderRevision(job) {
  // The server revision advances for every progress heartbeat so polling can
  // be cheap and race-safe. DOM reconstruction is intentionally keyed only
  // to structural changes; same-stage percent updates use the live progress
  // updater below and no longer replace the conversation/timeline tree.
  const outputs = orderedJobOutputs(job).map(({ item }) => item);
  return JSON.stringify({
    id: job?.id,
    status: job?.status,
    presentation: [job?.presentation?.key, job?.presentation?.primaryAction],
    stage: job?.stage,
    error: job?.error,
    pendingDecision: job?.pendingDecision?.stage,
    previewReady: job?.previewReady,
    previewPreparing: job?.previewPreparing,
    candidateCount: job?.candidates?.length || 0,
    eventGroupCount: job?.eventGroups?.length || 0,
    pendingSelectionGroupIds: job?.pendingSelectionGroupIds || [],
    editProposal: [job?.pendingEditProposal?.id || "", job?.pendingEditProposal?.status || ""],
    outputVersionId: job?.currentOutputVersionId,
    autoComposition: [job?.autoComposition?.status, job?.autoComposition?.phase, job?.autoComposition?.versions?.length || 0],
    voiceDiscovery: [job?.voiceDiscovery?.status, job?.voiceDiscovery?.speakerCount],
    personDiscovery: [
      workflowKindForJob(job),
      job?.contentIndex?.persons?.length || 0,
      (job?.contentSearchPersonTarget?.personIds || job?.request?.contentSearchPersonTarget?.personIds || []).join(","),
    ],
    execution: [job?.execution?.status, job?.execution?.operation, job?.execution?.phase, job?.execution?.outcome, job?.execution?.actionRequired],
    agent: [job?.agent?.workspaceId, job?.agent?.planId, job?.agent?.status, job?.agent?.completedSteps, job?.agent?.totalSteps, job?.agent?.currentStepId],
    handoff: [job?.handoff?.fromJobId, job?.activeChildJobId],
    outputs: outputs.map((item) => [item.filename, item.previewReady, item.kept, item.previewOnly, item.outputKind]),
    messageCount: job?.messages?.length || 0,
    contentUiRevision: job?.contentUiRevision || "",
  });
}

function renderJob(job) {
  // A home "new task" action owns the workspace until the next upload is
  // submitted. Ignore any late history/poll response from the previous task;
  // otherwise its conversation can be painted back over the empty composer.
  if (!job?.id) return;
  if (homeNavigationRequested && !restoringHistory) return;
  // A response for another task is stale even when the user is no longer on
  // the home screen (for example, task A finishes after task B was opened).
  if (!restoringHistory && currentJob && String(currentJob.id) !== String(job.id)) return;
  observeFormalDelivery(job);
  window.ClipTalkAppShell?.syncCurrentJob(job);
  rememberCurrentJob(job);
  const previousId = currentJob?.id;
  const previousStatus = String(currentJob?.status || "");
  const previousPersonSearchId = String(currentJob?.contentSearch?.id || "");
  const previousPersonCandidateCount = Number(currentJob?.contentSearch?.candidates?.length || 0);
  const personWorkspaceWasOpen = document.body.classList.contains("person-workspace-open");
  const previousVoiceDiscoveryStatus = String(currentJob?.voiceDiscovery?.status || "not_started");
  setDirectorWorkspaceEmpty(false);
  const revision = jobRenderRevision(job);
  if (previousId === job?.id && currentJobRevision === revision) {
    currentJob = job;
    renderJobError(job.error || "", "job");
    void window.ClipTalkAgentWorkspace?.resumeForJob?.(job);
    const nextVoiceDiscoveryStatus = String(job?.voiceDiscovery?.status || "not_started");
    if (nextVoiceDiscoveryStatus !== previousVoiceDiscoveryStatus
      && ["ready", "failed"].includes(nextVoiceDiscoveryStatus)
      && (job?.narratorSelectionPending?.active || !$("#voiceProfilePanel")?.classList.contains("hidden"))) {
      window.setTimeout(loadCurrentVoices, 0);
    }
    syncWorkspaceState({ home: false });
    syncOneOffFinalizeAction(job);
    renderReviewStatus(job);
    renderDirectorTaskSummary(job);
    if (!$("#personProfilePanel")?.classList.contains("hidden")) renderCurrentPersons();
    // Progress updates can arrive without a DOM revision (for example while
    // the worker is reporting the same stage). Keep the live console visible
    // and refresh its text/percent on every poll instead of waiting for a
    // browser reload to rebuild the page.
    const execution = executionForJob(job);
    const live = execution.active;
    const progressVisible = analysisConsoleVisible(job);
    const cancellable = Boolean(execution.capabilities?.canCancel);
    updateDirectorState(job);
    updateDirectorThinkingOrb(job);
    updateDirectorFlow(job);
    if (live && directorStage === "conversation") setDirectorStage("analysis");
    placeAnalysisConsole(progressVisible);
    const cancelButton = $("#cancelButton");
    cancelButton?.classList.toggle("hidden", !cancellable);
    if (cancelButton) cancelButton.textContent = executionCancelLabel(job);
    if ($("#jobDetail")) $("#jobDetail").textContent = job.detail || job.stage || "准备中";
    if ($("#jobPercent")) $("#jobPercent").textContent = `${Math.round((Number(job.progress) || 0) * 100)}%`;
    $("#jobProgress")?.style.setProperty("width", `${Math.round((Number(job.progress) || 0) * 100)}%`);
    updateAnalysisConsole(job);
    updateInlineAnalysisProgress(job);
    updateAutoCompositionProgress(job);
    syncComposerSuggestion(job);
    // Resource endpoints can become ready without changing the public job
    // revision. Keep their lightweight retry loops alive even when the DOM
    // itself does not need to be rebuilt.
    loadWaveform(job);
    loadTimelineAssets(job);
    const reviewReady = ["brief_confirmation", "awaiting_confirmation", "awaiting_content_confirmation", "awaiting_model_decision", "completed"].includes(job.status);
    const transcriptPendingStages = new Set(["queued", "starting", "probing", "audio_analysis", "speech_recognition"]);
    const transcriptMayBeReady = reviewReady || !transcriptPendingStages.has(String(job.stage || ""));
    if (transcriptMayBeReady && waveformData?.hasAudio !== false) loadTimelineTranscript(job);
    return;
  }
  currentJobRevision = revision;
  const previousPreviewReady = Boolean(currentJob?.previewReady);
  const activeGroupId = currentEventGroup?.id;
  const activeSegmentId = currentEventSegment?.id;
  const activeCandidateIndex = currentCandidate?.index;
  const activeContentCandidateId = String(currentCandidate?.id || "");
  const activeContentSearchId = String(currentContentPreviewSearchId || currentJob?.contentSearchSession?.activeSearchId || currentJob?.contentSearch?.id || "");
  currentJob = job;
  void window.ClipTalkAgentWorkspace?.resumeForJob?.(job);
  const nextPersonSearchId = String(job?.contentSearch?.id || "");
  const nextPersonCandidateCount = Number(job?.contentSearch?.candidates?.length || 0);
  const personReviewJustArrived = previousId === job.id
    && personWorkspaceWasOpen
    && workflowKindForJob(job) === "person_edit"
    && job.status === "awaiting_content_confirmation"
    && nextPersonCandidateCount > 0
    && (previousPersonCandidateCount === 0 || previousPersonSearchId !== nextPersonSearchId);
  const nextVoiceDiscoveryStatus = String(job?.voiceDiscovery?.status || "not_started");
  if (previousId === job.id
    && nextVoiceDiscoveryStatus !== previousVoiceDiscoveryStatus
    && ["ready", "failed"].includes(nextVoiceDiscoveryStatus)
    && (job?.narratorSelectionPending?.active || !$("#voiceProfilePanel")?.classList.contains("hidden"))) {
    window.setTimeout(loadCurrentVoices, 0);
  }
  syncWorkspaceState({ home: false });
  syncOneOffFinalizeAction(job);
  updatePlayerChrome();
  renderReviewStatus(job);
  renderDirectorTaskSummary(job);
  updateDirectorFlow(job);
  // Polling a job must not replace the active output's aspect with the source
  // dimensions. Output selection and loadedmetadata own that media's aspect.
  if (viewerMediaKind !== "output" || !currentOutput) {
    applyMediaAspect(viewerShell, job.videoInfo?.width, job.videoInfo?.height);
  }
  currentEventGroup = activeGroupId ? job.eventGroups?.find((item) => item.id === activeGroupId) || null : null;
  currentEventSegment = activeSegmentId && currentEventGroup
    ? currentEventGroup.segments.find((item) => item.id === activeSegmentId) || null
    : null;
  if (String(job?.taskMode || "") === "content_extract" && activeContentCandidateId && previousId === job.id) {
    const search = contentSearchRecordsForJob(job).find((item) => String(item?.id || "") === activeContentSearchId);
    const candidates = Array.isArray(search?.candidates) && search.candidates.length
      ? search.candidates
      : Array.isArray(search?.timelineCandidates) ? search.timelineCandidates : [];
    currentCandidate = candidates.find((item) => String(item?.id || "") === activeContentCandidateId) || null;
    if (!currentCandidate) {
      currentContentPreviewSearchId = "";
      if (contentBoundaryTimelineEdit) closeContentBoundaryTimelineEdit({ restorePreview: false });
    }
  } else {
    currentCandidate = activeCandidateIndex === undefined
      ? currentCandidate
      : (job.candidates || []).find((item) => Number(item.index) === Number(activeCandidateIndex)) || null;
  }
  if (previousId !== job.id) {
    clearToast();
    resetCandidateDrawerContext();
    setCandidateRailOpen(false);
    closeSubtitleReview(null);
    dismissActionConfirmation();
    setReviewLowerPanelMode("collapsed");
    resetCurrentVoiceState(job.id);
    resetCurrentPersonState();
    if (["ready", "running"].includes(String(job.voiceDiscovery?.status || ""))) {
      window.setTimeout(() => {
        if (String(currentJob?.id || "") === String(job.id)) loadCurrentVoices();
      }, 0);
    }
    expandedContentSearchIds.clear();
    contentSearchDetailCache.clear();
    contentSearchFilterState.clear();
    cancelTimelinePan(null);
    cancelTimelineOverview(null);
    currentJobRevision = revision;
    currentOutput = null;
    viewerMediaKind = "source";
    syncOutputPreviewMode();
    currentCandidate = null;
    currentEventGroup = null;
    currentEventSegment = null;
    timelineFrameSelectionTime = null;
    pendingTimelineSelection = null;
    pendingTimelineOriginal = null;
    timelineManualSelectMode = false;
    locallyExcludedCandidates = new Set((job.reviewExcludedCandidates || []).map((index) => Number(index)));
    outputAssemblyMode = "single_reel";
    pendingSegmentSelections = new Map();
    timelineChatSelections = [];
    eventGroupSelectionOrder = [];
    ignoredChatContextKeys = new Set();
    activeProposalSourceRange = null;
    timelineViewStart = 0;
    timelineViewEnd = Number(job.videoInfo?.duration || 0);
    timelineCoordinateSpace = "source";
    timelineSelectedOutputSegmentIndex = null;
    timelineReviewFollow = false;
    timelineAssetsJobId = null;
    timelineAssetsLoadingJobId = null;
    timelineAssets = null;
    timelineAssetsRetryAt = 0;
    timelineTranscript = [];
    transcriptLoadingJobId = null;
    timelineTranscriptJobId = null;
    transcriptRetryAt = 0;
    timelineMediaRenderKey = "";
    waveformRenderKey = "";
    waveformLoadingJobId = null;
    waveformRetryAt = 0;
  }
  if (previousId === job.id && job.previewReady && !previousPreviewReady && ["source", "candidate", "segment"].includes(viewerMediaKind)) {
    const resumeTime = Number(mainVideo.currentTime || 0);
    const wasPlaying = !mainVideo.paused;
    clearPlayerNotice();
    mainVideo.addEventListener("loadedmetadata", () => {
      mainVideo.currentTime = Math.min(Math.max(0, mainVideo.duration - .05), resumeTime);
      if (wasPlaying) safePlay();
    }, { once: true });
    setMainVideoSource(sourcePreviewUrl(job));
  }
  const reviewReady = ["brief_confirmation", "awaiting_confirmation", "awaiting_content_confirmation", "awaiting_model_decision", "completed"].includes(job.status);
  // Waveform and frame thumbnails describe the source, so they are useful while
  // analysis is still running. Candidate overlays remain unavailable until the
  // model has produced them, but the base timeline should never stay blank.
  loadWaveform(job);
  loadTimelineAssets(job);
  const transcriptPendingStages = new Set(["queued", "starting", "probing", "audio_analysis", "speech_recognition"]);
  const transcriptMayBeReady = reviewReady || !transcriptPendingStages.has(String(job.stage || ""));
  if (transcriptMayBeReady && waveformData?.hasAudio !== false) loadTimelineTranscript(job);
  $("#uploadView")?.classList.add("hidden");
  $("#reviewView")?.classList.remove("hidden");
  // ResizeObserver does not fire reliably when a whole grid changes from
  // display:none in every Chromium build. Fit explicitly on workspace entry
  // so the media canvas can never remain at its empty intrinsic size (0×0).
  scheduleMediaFrameFit(true);
  chatInput.disabled = false;
  const interactionKind = String(job.contentSearch?.interactionState?.kind || "");
  const workflowKey = workflowKindForJob(job);
  const canChooseLegacyWorkflow = isAgentInstructionDraft(job);
  // A restored upload draft must look actionable immediately.  The source
  // summary is already persisted in conversation history; reopen the
  // capability chooser beside it instead of making the user rediscover a tiny
  // composer toggle on every visit.
  const hasStagedCapability = pendingCapabilityCategory !== "auto"
    || Boolean(pendingCapabilityOption)
    || Boolean(pendingWorkflowSwitch);
  if (canChooseLegacyWorkflow && previousId !== job.id && !hasStagedCapability) {
    pendingCapabilityCategory = "auto";
    pendingCapabilityOption = "";
    const skillSelect = $("#agentSkillSelect");
    if (skillSelect?.value) {
      skillSelect.value = "";
      skillSelect.dispatchEvent(new Event("change", { bubbles: true }));
    }
    legacyWorkflowPickerOpen = false;
  }
  if (!canChooseLegacyWorkflow) {
    legacyWorkflowPickerOpen = false;
    pendingWorkflowSwitch = "";
    quickWorkflowValidation = "";
  }
  chatInput.placeholder = pendingWorkflowSwitch
    ? quickWorkflowPlaceholder(pendingWorkflowSwitch)
    : isAgentInstructionDraft(job)
      ? agentDraftPlaceholder()
      : taskModePresentation(job).key === "content_extract"
    ? workflowKey === "speaker_edit" ? "筛选发言内容，或继续调整说话人选择……"
      : workflowKey === "person_edit" ? "补充人物出镜条件，或继续选择人物……"
      : interactionKind === "person_target" ? "回复人物 A、人物 B，或输入新的查找条件"
      : interactionKind === "speaker_link" ? "回复 Speaker 编号，或点击上方选项确认"
        : interactionKind === "capability_confirmation" ? "回复“确认”启用新增分析，或修改查找条件"
          : "描述要找的内容，或和 AI 讨论如何剪辑……"
    : "请输入指令或补充需求……";
  $("#sendButton").disabled = false;
  $("#chatLegacyModeButton")?.classList.toggle("hidden", !canChooseLegacyWorkflow);
  renderQuickWorkflowPicker(job);
  syncComposerSuggestion(job);
  updateComposerBeam();
  const execution = executionForJob(job);
  const running = execution.active;
  const briefing = job.status === "briefing";
  const awaitingDecision = job.status === "awaiting_model_decision";
  updateDirectorState(job);
  updateDirectorThinkingOrb(job);
  // renderConversation replaces its message list, so move the console after
  // the conversation has been rendered and keep it in the same visual flow.
  renderConversation(job);
  if (!$("#personProfilePanel")?.classList.contains("hidden")) renderCurrentPersons();
  // Failed/interrupted jobs must keep the review rail visible so the user can
  // access recovery actions such as “从中断处恢复分析”. Only active pipeline
  // work and explicit model decisions take over the rail.
  const cancellable = Boolean(execution.capabilities?.canCancel);
  const pipelineVisible = running || awaitingDecision;
  const progressVisible = analysisConsoleVisible(job);
  const compositionRunning = running && ["render", "auto_composition", "quality_review"].includes(execution.operation);
  if (isAgentInstructionDraft(job)) setDirectorStage("conversation");
  else if (briefing) setDirectorStage("conversation");
  else if (awaitingDecision) setDirectorStage("analysis");
  else if (compositionRunning) setDirectorStage("compose");
  else if (running) setDirectorStage("analysis");
  else if (job.status === "awaiting_content_confirmation") setDirectorStage("events");
  else if (job.status === "awaiting_confirmation") setDirectorStage(jobOutputCount(job) ? "compose" : "events");
  else if (job.status === "completed") setDirectorStage("compose");
  else if (["cancelled", "failed"].includes(job.status)) setDirectorStage("conversation");
  if (personReviewJustArrived) {
    closePersonProfiles();
    setDirectorStage("events");
    setReviewLowerPanelMode("review");
  }
  placeAnalysisConsole(progressVisible);
  $(".review-rail")?.classList.toggle("pipeline-mode", pipelineVisible);
  if ($("#jobDetail")) $("#jobDetail").textContent = job.detail || job.stage;
  if ($("#jobPercent")) $("#jobPercent").textContent = `${Math.round((job.progress || 0) * 100)}%`;
  $("#jobProgress")?.style.setProperty("width", `${Math.round((job.progress || 0) * 100)}%`);
  updateAnalysisConsole(job);
  updateInlineAnalysisProgress(job);
  renderAnalysisDecision(job);
  const cancelButton = $("#cancelButton");
  cancelButton?.classList.toggle("hidden", !cancellable);
  if (cancelButton) cancelButton.textContent = executionCancelLabel(job);
  renderJobError(job.error || "", "job");
  if ($("#assetDuration")) $("#assetDuration").textContent = `时长 ${formatClock(job.videoInfo?.duration || 0)}`;
  if ($("#assetResolution")) $("#assetResolution").textContent = job.videoInfo?.width && job.videoInfo?.height
    ? `分辨率 ${job.videoInfo.width}×${job.videoInfo.height}` : "分辨率 --";
  renderReviewRail(job);
  syncThinkingOrbs($("#railBody"));
  if (jobOutputCount(job)) {
    renderOutputs(job);
  }
  else {
    $("#clipSection")?.classList.add("hidden");
    if ($("#clipSection")) $("#clipSection").style.display = "none";
    if (previousId !== job.id || !mainVideo.src) showSource();
  }
  updateTimeline();
  const enteredCompletedWorkspace = job.status === "completed"
    && jobOutputCount(job) > 0
    && (previousId !== job.id || previousStatus !== "completed");
  if (enteredCompletedWorkspace) {
    // Preview first. Detailed source inspection is an explicit user action.
    setReviewLowerPanelMode("collapsed");
  }
  syncReviewWorkbench();
  syncComposerSuggestion(job);
  if (!jobNeedsPolling(job) && !briefing && !restoringHistory) {
    clearTimeout(pollTimer);
    pollTimer = null;
  }
}

async function pollJob() {
  if (!currentJob) return;
  const polledJobId = String(currentJob.id);
  clearTimeout(pollTimer);
  pollTimer = null;
  if (document.hidden) {
    pollTimer = window.setTimeout(pollJob, 6000);
    return;
  }
  try {
    const knownRevision = Number(currentJob?.revision || 0);
    const response = await api(`/api/jobs/${encodeURIComponent(polledJobId)}/status?revision=${knownRevision}`);
    if (!currentJob || String(currentJob.id) !== polledJobId) return;
    pollFailureDelay = 2500;
    clearRecoveredPollError();
    if (!response.changed) {
      if (jobNeedsPolling(currentJob)) pollTimer = setTimeout(pollJob, jobPollDelay(currentJob));
      return;
    }
    const snapshot = response.job || {};
    const needsFullRefresh = Number(snapshot.candidateCount || 0) !== Number(currentJob.candidates?.length || 0)
      || Number(snapshot.eventGroupCount || 0) !== Number(currentJob.eventGroups?.length || 0)
      || Number(snapshot.outputVersionCount || 0) !== Number(currentJob.outputVersions?.length || 0)
      || String(snapshot.contentUiRevision || "") !== String(currentJob.contentUiRevision || "")
      || String(snapshot.execution?.outcome || "") !== String(currentJob.execution?.outcome || "")
      || String(snapshot.execution?.actionRequired || "") !== String(currentJob.execution?.actionRequired || "")
      || String(snapshot.activeChildJobId || "") !== String(currentJob.activeChildJobId || "")
      || String(snapshot.voiceDiscovery?.status || "") !== String(currentJob.voiceDiscovery?.status || "")
      || Boolean(snapshot.pendingDecision) !== Boolean(currentJob.pendingDecision)
      || (!["briefing", "queued", "running", "cancelling"].includes(String(snapshot.status || ""))
        && String(snapshot.status || "") !== String(currentJob.status || ""));
    let job;
    if (needsFullRefresh) {
      ({ job } = await api(`/api/jobs/${encodeURIComponent(polledJobId)}`));
      if (!currentJob || String(currentJob.id) !== polledJobId) return;
    } else {
      job = { ...currentJob, ...snapshot, autoComposition: { ...(currentJob.autoComposition || {}), ...(snapshot.autoComposition || {}) } };
    }
    renderJob(job);
    if (jobNeedsPolling(job)) pollTimer = setTimeout(pollJob, job.status === "briefing" ? 1400 : jobPollDelay(job));
  } catch (error) {
    const reconnecting = error?.code === "network_error" || Number(error?.status) === 0;
    if (!currentJob?.error) renderJobError(reconnecting ? "连接短暂中断，正在自动重连…" : error.message, "poll");
    pollTimer = setTimeout(pollJob, pollFailureDelay);
    pollFailureDelay = Math.min(10000, Math.round(pollFailureDelay * 1.5));
  }
}

async function confirmCandidates(indices, outputMode = "single_reel") {
  if (!currentJob || currentJob.status !== "awaiting_confirmation" || actionBusy) return;
  if (!indices.length) return void showToast("请至少选择一个高光候选");
  const selected = indices.map((index) => currentJob.candidates?.find((candidate) => Number(candidate.index) === Number(index))).filter(Boolean);
  const total = selected.reduce((sum, item) => sum + Number(item.duration || (Number(item.end) - Number(item.start)) || 0), 0);
  const confirmation = await requestActionConfirmation({
    title: "合成候选高光",
    summary: `将把 ${selected.length} 个候选镜头按当前顺序合成为 1 条高光视频，预计 ${total.toFixed(1)} 秒。`,
    details: [`来源：${currentJob.filename || "当前视频"}`],
    orderMode: "selection",
    orderItems: selected.map((item) => ({ id: String(item.index), label: item.title || `候选 ${Number(item.index) + 1}`, meta: `${formatTime(item.start)}→${formatTime(item.end)}` })),
  });
  if (!confirmation) return;
  const orderMode = typeof confirmation === "object" ? confirmation.orderMode : "selection";
  if (typeof confirmation === "object" && confirmation.orderedItems?.length) indices = confirmation.orderedItems.map((item) => Number(item.id));
  if (orderMode === "llm_recommend") {
    return void showToast("剪辑规划模型需要高光事件数据才能推荐顺序；当前候选会按你调整后的顺序合成。可先进入事件审核，再使用顺序推荐。");
  }
  if (orderMode === "ai_plan") {
    setDirectorStage("compose");
    return;
  }
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  setDirectorStage("compose");
  actionBusy = true;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/confirm`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: { indices, outputMode, orderMode },
    });
    if (!commitJobAction(job, actionToken)) return;
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function recognitionEvidenceKind(item = {}) {
  const value = String(item.modality || item.evidenceKind || "visual").toLowerCase();
  const labels = {
    speech: "对白", speechunits: "对白", visual: "画面", visualunits: "画面",
    ocr: "屏幕文字", ocrunits: "屏幕文字", audio: "声音", audiounits: "声音",
    person: "匿名人物", persons: "匿名人物", track: "人物轨迹", persontracks: "人物轨迹",
    shot: "镜头", shots: "镜头",
  };
  return labels[value] || "内容证据";
}

function recognitionEvidenceCopy(item = {}) {
  const values = [
    item.text, item.summary, item.title, item.label,
    ...(item.visibleText || []), ...(item.entities || []), ...(item.actions || []),
    ...(item.labels || []), ...(item.personLabels || []), ...(item.audioEvents || []),
  ].map((value) => String(value || "").trim()).filter(Boolean);
  return [...new Set(values)].join(" · ") || `${recognitionEvidenceKind(item)}已在该时间范围内检出`;
}

function setContentDetailPanelMode(mode = "evidence", { focus = false } = {}) {
  const panel = $("#evidencePanel");
  const switcher = $("#contentDetailModeSwitch");
  const inspector = $("#contentBoundaryInspector");
  if (!panel || !switcher || !inspector) return false;
  const editReady = contentBoundaryTimelineEdit?.surface === "evidence-panel"
    && contentBoundaryTimelineEdit.editor?.isConnected;
  const next = mode === "edit" && editReady ? "edit" : "evidence";
  panel.classList.toggle("content-boundary-view", next === "edit");
  inspector.classList.toggle("hidden", next !== "edit");
  switcher.querySelectorAll("[data-content-detail-mode]").forEach((button) => {
    const active = button.dataset.contentDetailMode === next;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
  });
  if (focus) switcher.querySelector(`[data-content-detail-mode="${next}"]`)?.focus();
  return next === mode;
}

function contentBoundaryEntryState(match = currentCandidate) {
  const activeSearchId = String(currentJob?.contentSearchSession?.activeSearchId || currentJob?.contentSearch?.id || "");
  const previewSearchId = String(currentContentPreviewSearchId || activeSearchId);
  const search = contentSearchRecordsForJob(currentJob).find((item) => String(item?.id || "") === previewSearchId)
    || (previewSearchId === String(currentJob?.contentSearch?.id || "") ? currentJob?.contentSearch : null);
  const sourceMatch = (search?.candidates || []).find((item) => String(item?.id || "") === String(match?.id || "")) || match;
  const reviewStatus = String(sourceMatch?.reviewStatus || (sourceMatch?.requiresReview ? "pending" : "confirmed"));
  const historical = Boolean(previewSearchId && activeSearchId && previewSearchId !== activeSearchId);
  const capabilities = interactionCapabilitiesForJob(currentJob);
  if (!match?.id || String(currentJob?.taskMode || "") !== "content_extract") {
    return { kind: "unavailable", label: "不可调整边界", reason: "当前没有可编辑的内容片段", disabled: true, searchId: previewSearchId };
  }
  if (reviewStatus === "rejected") {
    return { kind: "unavailable", label: "已排除，无法调整", reason: "请先在结果列表中恢复并保留这个片段", disabled: true, searchId: previewSearchId };
  }
  if (capabilities.active) {
    return { kind: "unavailable", label: "生成中，暂不可调整", reason: capabilities.reason, disabled: true, searchId: previewSearchId };
  }
  if (historical) {
    return { kind: "restore", label: "恢复此检索后调整", reason: "先把这次历史检索恢复为当前结果，再调整片段入点和出点", disabled: false, searchId: previewSearchId };
  }
  if (capabilities.canEditContentBoundary) {
    return { kind: "edit", label: "调整边界", reason: "调整当前片段的入点和出点", disabled: false, searchId: activeSearchId };
  }
  const hasOutputs = Boolean(
    (currentJob?.outputs || []).length
    || (currentJob?.outputVersions || []).some((version) => (version.outputs || []).length),
  );
  if (String(currentJob?.status || "") === "completed" && hasOutputs) {
    return { kind: "reedit", label: "返回确认后调整", reason: "返回片段确认并调整边界", disabled: false, searchId: activeSearchId };
  }
  const processing = capabilities.active;
  return {
    kind: "unavailable",
    label: processing ? "生成中，暂不可调整" : "当前状态不可调整",
    reason: processing ? "生成完成后可返回片段确认再调整边界" : "请先进入当前检索的片段确认阶段",
    disabled: true,
    searchId: activeSearchId,
  };
}

function syncContentBoundaryEntry(match = currentCandidate) {
  const button = $("#contentBoundaryEntryButton");
  if (!button) return null;
  const state = contentBoundaryEntryState(match);
  button.dataset.boundaryEntryKind = state.kind;
  button.dataset.boundarySearchId = state.searchId || "";
  button.textContent = state.label;
  button.title = state.reason;
  button.setAttribute("aria-label", `${state.label}：${state.reason}`);
  button.disabled = state.disabled;
  return state;
}

function showContentDetailModeSwitch() {
  $("#contentDetailModeSwitch")?.classList.remove("hidden");
  syncContentBoundaryEntry();
}

function hideContentDetailModeSwitch() {
  $("#contentDetailModeSwitch")?.classList.add("hidden");
  $("#evidencePanel")?.classList.remove("content-boundary-view");
  $("#contentBoundaryInspector")?.classList.add("hidden");
  const button = $("#contentBoundaryEntryButton");
  if (button) {
    button.disabled = false;
    button.dataset.boundaryEntryKind = "edit";
    button.dataset.boundarySearchId = "";
    button.textContent = "调整边界";
    button.title = "调整当前片段的入点和出点";
  }
}

function contentMatchEvidenceShell(match, { deferred = false } = {}) {
  $("#evidencePanel")?.classList.remove("hidden", "evidence-placeholder", "output-mode", "montage-mode");
  $("#evidencePanel")?.classList.add("candidate-mode");
  $("#clipTime").textContent = `${formatTime(match.start)} → ${formatTime(match.end)} · ${Number(match.duration || 0).toFixed(1)} 秒`;
  $("#clipTitle").textContent = match.title || "匹配内容";
  $("#clipScore").textContent = `${Math.round(Number(match.score || 0))}/100`;
  $("#clipReason").textContent = match.reason || match.matchedEvidence || "内容索引匹配";
  $("#outputExplanation")?.classList.add("hidden");
  const transcript = $("#clipTranscript");
  const excerpt = String(match.transcriptExcerpt || "").trim();
  const transcriptIsContext = (match.speechUnits || []).some((unit) => Number(unit.start) < Number(match.start) - .5 || Number(unit.end) > Number(match.end) + .5);
  if (transcript) {
    transcript.innerHTML = excerpt ? `<small>${transcriptIsContext ? "相邻对白上下文" : "对白证据"}</small><p>${escapeHtml(excerpt)}</p>` : "";
    transcript.classList.toggle("hidden", !excerpt);
  }
  const modalities = [...new Set((match.matchedModalities || [match.evidenceType]).filter(Boolean))];
  if ($("#clipEvidenceMeta")) $("#clipEvidenceMeta").innerHTML = [
    `<span>类型 ${escapeHtml(contentMatchTypeLabel(match))}</span>`,
    ...modalities.map((value) => `<span>${escapeHtml(({ speech: "对白", visual: "画面", ocr: "屏幕文字", audio: "声音", person: "匿名人物" })[value] || value)}</span>`),
  ].join("");
  if ($("#clipEvidence")) $("#clipEvidence").innerHTML = deferred
    ? `<li class="content-evidence-deferred"><span>点击“查看判断依据”后加载识别明细</span></li>`
    : `<li class="content-evidence-loading"><span>正在读取该片段的识别证据…</span></li>`;
  $("#addToChatButton")?.classList.add("hidden");
  $("#keepButton")?.classList.add("hidden");
  $("#replaceButton")?.classList.add("hidden");
  showContentDetailModeSwitch();
  const editingSameMatch = contentBoundaryTimelineEdit?.surface === "evidence-panel"
    && String(contentBoundaryTimelineEdit.matchId || "") === String(match.id || "");
  setContentDetailPanelMode(editingSameMatch ? "edit" : "evidence");
}

async function loadContentMatchEvidence(match) {
  if (!currentJob || !match) return;
  const requestToken = ++contentEvidenceRequestToken;
  const jobId = String(currentJob.id);
  const params = new URLSearchParams({
    start: String(Math.max(0, Number(match.start) || 0)),
    end: String(Math.max(Number(match.start) || 0, Number(match.end) || 0)),
    limit: "200",
  });
  try {
    const payload = await api(`/api/jobs/${encodeURIComponent(jobId)}/recognition?${params}`);
    if (requestToken !== contentEvidenceRequestToken || String(currentJob?.id || "") !== jobId) return;
    const allEvidence = Array.isArray(payload.evidence) ? payload.evidence : [];
    const referenced = new Set((match.evidenceRefs || []).map((item) => String(item?.id || item)).filter(Boolean));
    const evidence = (referenced.size
      ? allEvidence.filter((item) => referenced.has(String(item.id)))
      : allEvidence).slice(0, referenced.size ? Math.max(1, referenced.size) : 40);
    const kindCounts = evidence.reduce((counts, item) => {
      const label = recognitionEvidenceKind(item);
      counts[label] = Number(counts[label] || 0) + 1;
      return counts;
    }, {});
    const meta = $("#clipEvidenceMeta");
    if (meta) meta.innerHTML = [
      `<span>检索类型 ${escapeHtml(contentMatchTypeLabel(match))}</span>`,
      referenced.size ? `<span>候选引用 ${referenced.size} 条</span>` : "",
      ...Object.entries(kindCounts).map(([label, count]) => `<span>${escapeHtml(label)} ${count}</span>`),
      payload.truncated ? `<span>仅显示前 ${evidence.length} 条</span>` : "",
    ].join("");
    const list = $("#clipEvidence");
    if (!list) return;
    list.innerHTML = evidence.length ? evidence.map((item, index) => {
      const start = Number(item.start ?? item.time ?? match.start) || 0;
      const end = Number(item.end ?? item.time ?? start) || start;
      const range = end > start + .01 ? `${formatTime(start)} → ${formatTime(end)}` : formatTime(start);
      return `<li><button type="button" class="evidence-main-button" data-content-evidence-time="${start}"><small class="evidence-source-label">${escapeHtml(recognitionEvidenceKind(item))} ${String(index + 1).padStart(2, "0")} · ${range}</small><span class="evidence-item-copy">${escapeHtml(recognitionEvidenceCopy(item))}</span></button></li>`;
    }).join("") : `<li class="content-evidence-empty"><span>这个片段有检索匹配，但当前识别范围内没有更多可展开的明细。</span></li>`;
    list.querySelectorAll("[data-content-evidence-time]").forEach((button) => button.addEventListener("click", () => seekSourceTime(Number(button.dataset.contentEvidenceTime))));
  } catch (error) {
    if (requestToken !== contentEvidenceRequestToken || String(currentJob?.id || "") !== jobId) return;
    if ($("#clipEvidence")) $("#clipEvidence").innerHTML = `<li class="content-evidence-empty"><span>${escapeHtml(error.message || "详细证据读取失败")}</span></li>`;
  }
}

window.ClipTalkLoadSelectedEvidence = async () => {
  if (!currentJob) return false;
  if (String(currentJob.taskMode || "") === "content_extract" && currentCandidate) {
    const list = $("#clipEvidence");
    if (list) list.innerHTML = `<li class="content-evidence-loading"><span>正在读取该片段的识别证据…</span></li>`;
    await loadContentMatchEvidence(currentCandidate);
    return true;
  }
  return Boolean(currentCandidate || currentEventGroup || currentEventSegment || currentOutput);
};

function previewContentMatchesSequentially(matches) {
  const playable = (matches || []).filter((item) => Number(item?.end) > Number(item?.start));
  if (!currentJob || !playable.length) return void showToast("当前没有可连续播放的片段", "error");
  contentPreviewSequence = {
    jobId: String(currentJob.id || ""),
    searchId: String(currentJob.contentSearch?.id || ""),
    matches: playable,
    position: 0,
  };
  previewContentMatch(playable[0], { preserveSequence: true });
  showToast(`开始连续播放 ${playable.length} 个片段`, "neutral");
}

function playMainVideoWhenPositioned(start, isCurrent) {
  let started = false;
  const cleanup = () => {
    mainVideo.removeEventListener("loadedmetadata", tryStart);
    mainVideo.removeEventListener("seeked", tryStart);
  };
  const tryStart = () => {
    if (started) return;
    if (!isCurrent()) {
      cleanup();
      return;
    }
    if (mainVideo.readyState < 1) return;
    const aligned = Math.abs(Number(mainVideo.currentTime || 0) - start) <= .05;
    if (!aligned) {
      mainVideo.currentTime = start;
      return;
    }
    if (mainVideo.seeking) return;
    started = true;
    cleanup();
    window.requestAnimationFrame(() => {
      if (isCurrent()) safePlay();
    });
  };
  mainVideo.addEventListener("loadedmetadata", tryStart);
  mainVideo.addEventListener("seeked", tryStart);
  tryStart();
}

function previewContentMatch(match, { autoplay = true, loadEvidence = false, preserveSequence = false, searchId = "" } = {}) {
  if (!currentJob || !match) return;
  if (!preserveSequence) contentPreviewSequence = null;
  currentContentPreviewSearchId = String(searchId || currentJob?.contentSearchSession?.activeSearchId || currentJob?.contentSearch?.id || "");
  const start = Math.max(0, Number(match.start) || 0);
  const end = Math.max(start, Number(match.end) || start);
  showSource({ autoplay: false, seekTime: start });
  const previewToken = candidatePreviewToken;
  const previewJobId = String(currentJob.id || "");
  const previewMatchId = String(match.id || "");
  currentOutput = null;
  currentEventGroup = null;
  currentEventSegment = null;
  currentCandidate = match;
  candidatePreviewEnd = Math.max(start, end - 0.008);
  viewerMediaKind = "segment";
  syncOutputPreviewMode();
  $("#viewerBadge").textContent = "内容匹配预览";
  $("#reviewKicker").textContent = "内容匹配预览";
  $("#reviewTitle").textContent = match.title || "匹配内容";
  contentMatchEvidenceShell(match, { deferred: !loadEvidence });
  if (loadEvidence) loadContentMatchEvidence(match);
  seekSourceTime(start);
  if (autoplay) {
    playMainVideoWhenPositioned(start, () => (
      previewToken === candidatePreviewToken
      && String(currentJob?.id || "") === previewJobId
      && String(currentCandidate?.id || "") === previewMatchId
      && viewerMediaKind === "segment"
    ));
  } else {
    mainVideo.pause();
  }
  refreshTimelineAfterReviewSelection();
  syncReviewSelectionClasses();
  document.querySelectorAll("[data-drawer-content-candidate]").forEach((row) => {
    row.classList.toggle("active", String(row.dataset.drawerContentCandidate || "") === String(match.id || ""));
  });
  document.querySelectorAll("[data-content-match-row]").forEach((row) => {
    const focused = String(row.dataset.contentMatchRow || "") === String(match.id || "");
    row.classList.toggle("focused", focused);
    if (focused) row.setAttribute("aria-current", "true");
    else row.removeAttribute("aria-current");
  });
  syncUnifiedWorkspaceView();
}

function previewContentEvidence(match, { autoplay = false, searchId = "" } = {}) {
  if (!match) return;
  const editingDifferentMatch = contentBoundaryTimelineEdit?.surface === "evidence-panel"
    && String(contentBoundaryTimelineEdit.matchId || "") !== String(match.id || "");
  if (editingDifferentMatch) closeContentBoundaryTimelineEdit({ restorePreview: false });
  previewContentMatch(match, { autoplay, loadEvidence: true, searchId });
  setContentDetailPanelMode("evidence");
  window.ClipTalkWorkspaceController?.openEvidence?.({ load: false });
}

function contentBoundaryFrameRate(job = currentJob) {
  const value = Number(job?.videoInfo?.frame_rate || job?.videoInfo?.frameRate || 0);
  return Number.isFinite(value) && value >= 1 && value <= 240 ? value : 30;
}

function contentBoundaryDraft(editor, match) {
  const fallbackStart = Number(match?.start) || 0;
  const fallbackEnd = Number(match?.end) || fallbackStart;
  return {
    ...match,
    start: Number.isFinite(Number(editor?.dataset.boundaryStart)) ? Number(editor.dataset.boundaryStart) : fallbackStart,
    end: Number.isFinite(Number(editor?.dataset.boundaryEnd)) ? Number(editor.dataset.boundaryEnd) : fallbackEnd,
  };
}

function syncContentBoundaryEditor(editor, scopedJob, match, { seekEdge = "", preview = true } = {}) {
  if (!editor || !match) return;
  const fps = contentBoundaryFrameRate(scopedJob);
  const frame = 1 / fps;
  const duration = Number(scopedJob?.videoInfo?.duration || currentJob?.videoInfo?.duration || 0);
  let start = Math.max(0, Number(editor.dataset.boundaryStart) || 0);
  let end = Math.max(start + frame, Number(editor.dataset.boundaryEnd) || start + frame);
  if (duration > 0) {
    end = Math.min(duration, end);
    start = Math.min(start, Math.max(0, end - frame));
  }
  start = Math.round(start * 1e6) / 1e6;
  end = Math.round(end * 1e6) / 1e6;
  editor.dataset.boundaryStart = String(start);
  editor.dataset.boundaryEnd = String(end);
  editor.querySelector("[data-boundary-start-label]").textContent = formatTime(start);
  editor.querySelector("[data-boundary-end-label]").textContent = formatTime(end);
  editor.querySelector("[data-boundary-summary]").textContent = `${formatTime(start)} → ${formatTime(end)} · ${(end - start).toFixed(2)} 秒`;
  editor.querySelector("[data-boundary-frame-rate]").textContent = `${fps.toFixed(Math.abs(fps - Math.round(fps)) < .001 ? 0 : 2)} fps · 1 帧约 ${(frame * 1000).toFixed(1)} ms`;
  const originalStart = Number(editor.dataset.boundaryOriginalStart);
  const originalEnd = Number(editor.dataset.boundaryOriginalEnd);
  const startDelta = Number.isFinite(originalStart) ? start - originalStart : 0;
  const endDelta = Number.isFinite(originalEnd) ? end - originalEnd : 0;
  const changed = Math.abs(startDelta) >= frame / 2 || Math.abs(endDelta) >= frame / 2;
  const signedDelta = (value) => `${value >= 0 ? "+" : "−"}${Math.abs(value).toFixed(2)}s`;
  const changeStatus = editor.querySelector("[data-boundary-change]");
  if (changeStatus) changeStatus.textContent = changed
    ? `已修改 · 入点 ${signedDelta(startDelta)} · 出点 ${signedDelta(endDelta)}`
    : "尚未修改";
  editor.dataset.dirty = String(changed);
  const saveButton = editor.querySelector("[data-boundary-save]");
  if (saveButton) saveButton.disabled = !changed;
  editor.querySelectorAll("[data-boundary-value]").forEach((input) => {
    const edge = String(input.dataset.boundaryValue || "");
    input.step = String(frame);
    if (document.activeElement !== input) input.value = String(edge === "start" ? start : end);
  });
  if (contentBoundaryTimelineEdit?.editor === editor) {
    contentBoundaryTimelineEdit.start = start;
    contentBoundaryTimelineEdit.end = end;
    updateTimelineSelection();
  }
  if (!preview) return;
  const draft = { ...match, start, end, duration: end - start, title: `${match.title || "匹配内容"} · 边界预览` };
  previewContentMatch(draft, {
    autoplay: false,
    loadEvidence: false,
    searchId: contentBoundaryTimelineEdit?.searchId || currentContentPreviewSearchId,
  });
  if (seekEdge === "end") seekSourceTime(Math.max(start, end - frame));
}

function closeContentBoundaryTimelineEdit({ restorePreview = true } = {}) {
  const edit = contentBoundaryTimelineEdit;
  if (!edit) return;
  contentBoundaryTimelineEdit = null;
  edit.row?.classList.remove("boundary-open");
  edit.row?.removeAttribute("aria-current");
  if (edit.surface === "evidence-panel") {
    const origin = edit.origin;
    if (origin?.parent?.isConnected) {
      if (origin.next?.parentElement === origin.parent) origin.parent.insertBefore(edit.editor, origin.next);
      else origin.parent.append(edit.editor);
    } else {
      edit.editor?.remove();
    }
    setContentDetailPanelMode("evidence");
  }
  edit.editor?.classList.add("hidden");
  edit.editor?.closest("[data-content-match-row]")?.classList.remove("boundary-open");
  if (restorePreview && edit.match) previewContentMatch(edit.match, { autoplay: false, searchId: edit.searchId });
  else updateTimeline();
}

function openContentBoundaryInspector(match, { root: preferredRoot = null, focus = false } = {}) {
  if (!match?.id || String(currentJob?.taskMode || "") !== "content_extract") return false;
  const searchId = String(currentJob?.contentSearchSession?.activeSearchId || currentJob?.contentSearch?.id || "");
  const root = activeContentReviewRoot(currentJob, { searchId, preferredRoot });
  const editor = root?.querySelector(`[data-content-boundary-editor="${CSS.escape(String(match.id))}"]`);
  const inspector = $("#contentBoundaryInspector");
  const panel = $("#evidencePanel");
  if (!root || !editor || !inspector || !panel) return false;
  if (contentBoundaryTimelineEdit && contentBoundaryTimelineEdit.editor !== editor) {
    closeContentBoundaryTimelineEdit({ restorePreview: false });
  }
  const scopedJob = contentSearchJobForRoot(root, currentJob);
  editor.dataset.boundaryStart = String(Number(match.start) || 0);
  editor.dataset.boundaryEnd = String(Number(match.end) || Number(match.start) || 0);
  const row = editor.closest("[data-content-match-row]");
  contentBoundaryTimelineEdit = {
    jobId: String(currentJob.id || ""), searchId, matchId: String(match.id),
    match, scopedJob, editor, root, surface: "evidence-panel",
    origin: { parent: editor.parentElement, next: editor.nextElementSibling },
    row,
    start: Number(match.start) || 0,
    end: Number(match.end) || Number(match.start) || 0,
  };
  syncContentBoundaryEditor(editor, scopedJob, match, { preview: false });
  const span = Math.max(.5, Number(match.end) - Number(match.start));
  const padding = Math.max(1.5, Math.min(6, span * .35));
  setTimelineView(
    Math.max(0, Number(match.start) - padding),
    Math.min(timelineDurationValue(), Number(match.end) + padding),
  );
  inspector.replaceChildren(editor);
  editor.classList.remove("hidden");
  row?.classList.add("boundary-open");
  row?.setAttribute("aria-current", "true");
  panel.classList.remove("hidden", "evidence-placeholder", "output-mode", "montage-mode");
  panel.classList.add("candidate-mode");
  showContentDetailModeSwitch();
  setContentDetailPanelMode("edit", { focus });
  window.ClipTalkWorkspaceController?.openEvidence?.({ load: false });
  syncEvidencePlacement();
  scheduleMediaFrameFit(true);
  if (focus) window.requestAnimationFrame(() => {
    editor.querySelector('[data-boundary-value="start"]')?.focus({ preventScroll: true });
    inspector.scrollIntoView({ behavior: "smooth", block: "nearest" });
  });
  $("#timelineHint").textContent = "右侧详情已切换到边界调整；可随时切回片段证据";
  return true;
}

function openContentBoundaryMatchAfterTransition(matchId) {
  const searchId = String(currentJob?.contentSearchSession?.activeSearchId || currentJob?.contentSearch?.id || "");
  const match = (currentJob?.contentSearch?.candidates || []).find((item) => String(item?.id || "") === String(matchId || ""));
  if (!match) {
    showToast("已进入片段确认，但原片段已不在当前结果中", "error");
    return false;
  }
  setDirectorStage("events");
  previewContentMatch(match, { autoplay: false, searchId });
  if (openContentBoundaryInspector(match, { focus: true })) return true;
  // A status transition can rebuild the review rail in the same frame. Retry
  // once after rendering instead of exposing a clickable control with no body.
  renderConversation(currentJob);
  window.requestAnimationFrame(() => {
    const refreshed = (currentJob?.contentSearch?.candidates || []).find((item) => String(item?.id || "") === String(matchId || ""));
    if (!refreshed || !openContentBoundaryInspector(refreshed, { focus: true })) {
      showToast("片段确认已打开，但边界编辑器加载失败，请重新选择该片段", "error");
    }
  });
  return false;
}

async function activateContentBoundaryEntry() {
  const state = syncContentBoundaryEntry();
  if (!state || state.disabled || actionBusy) return;
  const matchId = String(currentCandidate?.id || contentBoundaryTimelineEdit?.matchId || "");
  if (!matchId) return void showToast("请先选择一个检索结果片段", "error");
  if (state.kind === "restore") {
    await restoreContentSearch(state.searchId, { openBoundaryMatchId: matchId });
    return;
  }
  if (state.kind === "reedit") {
    await reopenCurrentJobForEditing({ openBoundaryMatchId: matchId });
    return;
  }
  const match = (currentJob?.contentSearch?.candidates || []).find((item) => String(item?.id || "") === matchId);
  if (!match) return void showToast("当前检索中找不到这个片段，请重新选择", "error");
  if (contentBoundaryTimelineEdit?.surface === "evidence-panel"
      && String(contentBoundaryTimelineEdit.matchId || "") === matchId) {
    setContentDetailPanelMode("edit");
    return;
  }
  if (!openContentBoundaryInspector(match, { focus: true })) {
    renderConversation(currentJob);
    window.requestAnimationFrame(() => {
      if (!openContentBoundaryInspector(match, { focus: true })) {
        showToast("边界编辑器尚未加载，请重新选择当前检索中的片段", "error");
      }
    });
  }
}

function contentTimelineReviewContext(match = currentCandidate) {
  if (!match?.id || String(currentJob?.taskMode || "") !== "content_extract") return null;
  const searchId = String(currentJob?.contentSearchSession?.activeSearchId || currentJob?.contentSearch?.id || "");
  const root = activeContentReviewRoot(currentJob, {
    searchId,
    preferredRoot: contentBoundaryTimelineEdit?.root,
  });
  const input = root?.querySelector(`[data-content-match][value="${CSS.escape(String(match.id))}"]`);
  if (!root || !input) return null;
  const search = contentSearchForRoot(root, currentJob);
  const selectedIds = [...root.querySelectorAll("[data-content-match]:checked:not(:disabled)")].map((item) => String(item.value));
  const savedOrder = (search.reviewDraft?.orderedMatchIds || []).map(String).filter((id) => selectedIds.includes(id));
  const orderedIds = [...new Set([...savedOrder, ...selectedIds])];
  return { root, input, search, selectedIds, orderedIds };
}

function syncContentTimelineEditActions(match = contentBoundaryTimelineEdit?.match) {
  const toggle = $("#timelineContentToggleSelected");
  const earlier = $("#timelineContentMoveEarlier");
  const later = $("#timelineContentMoveLater");
  const context = contentTimelineReviewContext(match);
  [toggle, earlier, later].forEach((button) => button?.classList.toggle("hidden", !context));
  if (!context) return;
  const selected = context.input.checked && !context.input.disabled;
  const index = context.orderedIds.indexOf(String(match.id));
  if (toggle) {
    toggle.disabled = context.input.disabled || actionBusy;
    toggle.textContent = selected ? "移出结果" : "加入结果";
    toggle.title = selected ? "从本次探索结果中移除，源素材不会删除" : "重新加入本次探索结果";
  }
  if (earlier) earlier.disabled = !selected || index <= 0 || actionBusy;
  if (later) later.disabled = !selected || index < 0 || index >= context.orderedIds.length - 1 || actionBusy;
}

async function toggleContentTimelineSelection() {
  const match = contentBoundaryTimelineEdit?.match;
  const context = contentTimelineReviewContext(match);
  if (!context || context.input.disabled || actionBusy) return;
  context.input.checked = !context.input.checked;
  syncContentSearchSubtitleControls(context.root, currentJob);
  syncContentSearchOutputControls(context.root, currentJob);
  syncContentSearchSelectionSummary(context.root, currentJob);
  syncContentBasketAddButton(context.root, currentJob, context.search.id);
  renderChatContextBar();
  await saveContentReviewDraft(context.root, currentJob);
  syncContentTimelineEditActions(match);
  updateTimeline();
  showToast(context.input.checked ? "片段已加入当前探索结果" : "片段已从当前探索结果移出", "success");
}

async function moveContentTimelineSelection(direction) {
  const match = contentBoundaryTimelineEdit?.match;
  const context = contentTimelineReviewContext(match);
  if (!context || !context.input.checked || actionBusy) return;
  const index = context.orderedIds.indexOf(String(match.id));
  const target = index + direction;
  if (index < 0 || target < 0 || target >= context.orderedIds.length) return;
  [context.orderedIds[index], context.orderedIds[target]] = [context.orderedIds[target], context.orderedIds[index]];
  const orderSelect = context.root.querySelector("[data-content-order-mode]");
  if (orderSelect) orderSelect.value = "selection";
  syncContentSearchOutputControls(context.root, currentJob);
  await saveContentReviewDraft(context.root, currentJob, {
    orderMode: "selection", orderedMatchIds: context.orderedIds,
  });
  syncContentTimelineEditActions(match);
  updateTimeline();
  $("#timelineHint").textContent = "P 序号已更新；片段仍显示在源片时间位置，生成时按 P 序号合成";
}

async function saveContentBoundary(scopedJob, match, editor, operation = "save") {
  if (!currentJob || !scopedJob?.contentSearch?.id || actionBusy) return;
  const draft = contentBoundaryDraft(editor, match);
  const actionToken = captureJobAction();
  actionBusy = true;
  editor.querySelectorAll("button").forEach((button) => { button.disabled = true; });
  try {
    const { job } = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/content-search/boundary`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: {
        searchId: scopedJob.contentSearch.id,
        matchId: match.id,
        operation,
        ...(operation === "save" ? { start: draft.start, end: draft.end } : {}),
      },
    });
    const activeEdit = contentBoundaryTimelineEdit?.editor === editor ? contentBoundaryTimelineEdit : null;
    if (activeEdit?.surface === "evidence-panel") {
      activeEdit.row?.classList.remove("boundary-open");
      activeEdit.row?.removeAttribute("aria-current");
      const origin = activeEdit.origin;
      if (origin?.parent?.isConnected) {
        if (origin.next?.parentElement === origin.parent) origin.parent.insertBefore(editor, origin.next);
        else origin.parent.append(editor);
      }
      editor.classList.add("hidden");
      setContentDetailPanelMode("evidence");
    }
    if (activeEdit) contentBoundaryTimelineEdit = null;
    if (!commitJobAction(job, actionToken)) return;
    const updatedMatch = (currentJob?.contentSearch?.candidates || []).find((item) => String(item.id) === String(match.id));
    if (updatedMatch) previewContentMatch(updatedMatch, { autoplay: false, searchId: scopedJob.contentSearch.id });
    showToast(operation === "reset" ? "已恢复系统识别的原始边界" : "片段边界已保存", "success");
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(`边界保存失败：${error.message}`);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function wireContentBoundaryEditors(root, scopedJob) {
  root.querySelectorAll("[data-content-boundary-open]").forEach((button) => button.addEventListener("click", (event) => {
    event.preventDefault();
    event.stopPropagation();
    const matchId = String(button.dataset.contentBoundaryOpen || "");
    const match = (scopedJob.contentSearch?.candidates || []).find((item) => String(item.id) === matchId);
    if (!match) return;
    previewContentMatch(match, { autoplay: false, searchId: scopedJob.contentSearch?.id });
    if (!openContentBoundaryInspector(match, { root, focus: true })) {
      showToast("边界编辑器尚未加载，请重新选择当前检索中的片段", "error");
    }
  }));
  root.querySelectorAll("[data-content-card-preview]").forEach((copy) => {
    const activate = (event) => {
      if (event.type === "keydown" && !["Enter", " "].includes(event.key)) return;
      if (event.target.closest("button, input, label, a, select, textarea, details")) return;
      event.preventDefault();
      const matchId = String(copy.dataset.contentCardPreview || "");
      const match = (scopedJob.contentSearch?.candidates || []).find((item) => String(item.id) === matchId);
      if (match) previewContentMatch(match, { searchId: scopedJob.contentSearch?.id });
    };
    copy.addEventListener("click", activate);
    copy.addEventListener("keydown", activate);
  });
  root.querySelectorAll("[data-content-boundary-editor]").forEach((editor) => {
    const matchId = String(editor.dataset.contentBoundaryEditor || "");
    const match = (scopedJob.contentSearch?.candidates || []).find((item) => String(item.id) === matchId);
    if (!match) return;
    editor.querySelectorAll("[data-boundary-adjust]").forEach((button) => button.addEventListener("click", () => {
      const [edge, amountText] = String(button.dataset.boundaryAdjust || "").split(":");
      const frame = 1 / contentBoundaryFrameRate(scopedJob);
      const amount = amountText === "frame" ? frame : amountText === "-frame" ? -frame : Number(amountText);
      if (!Number.isFinite(amount) || !["start", "end"].includes(edge)) return;
      editor.dataset[edge === "start" ? "boundaryStart" : "boundaryEnd"] = String(
        Number(editor.dataset[edge === "start" ? "boundaryStart" : "boundaryEnd"]) + amount,
      );
      syncContentBoundaryEditor(editor, scopedJob, match, { seekEdge: edge });
    }));
    editor.querySelectorAll("[data-boundary-playhead]").forEach((button) => button.addEventListener("click", () => {
      const edge = String(button.dataset.boundaryPlayhead || "");
      if (!["start", "end"].includes(edge)) return;
      editor.dataset[edge === "start" ? "boundaryStart" : "boundaryEnd"] = String(timelineAbsoluteTime());
      syncContentBoundaryEditor(editor, scopedJob, match, { seekEdge: edge });
    }));
    editor.querySelectorAll("[data-boundary-value]").forEach((input) => input.addEventListener("change", () => {
      const edge = String(input.dataset.boundaryValue || "");
      const value = Number(input.value);
      if (!Number.isFinite(value) || !["start", "end"].includes(edge)) return;
      editor.dataset[edge === "start" ? "boundaryStart" : "boundaryEnd"] = String(value);
      syncContentBoundaryEditor(editor, scopedJob, match, { seekEdge: edge });
    }));
    editor.querySelector("[data-boundary-preview]")?.addEventListener("click", () => {
      const draft = contentBoundaryDraft(editor, match);
      previewContentMatch(
        { ...draft, duration: draft.end - draft.start },
        { autoplay: true, searchId: scopedJob.contentSearch?.id },
      );
    });
    editor.querySelector("[data-boundary-cancel]")?.addEventListener("click", () => {
      if (contentBoundaryTimelineEdit?.editor === editor) closeContentBoundaryTimelineEdit();
      else {
        editor.classList.add("hidden");
        editor.closest("[data-content-match-row]")?.classList.remove("boundary-open");
        previewContentMatch(match, { autoplay: false, searchId: scopedJob.contentSearch?.id });
      }
    });
    editor.querySelector("[data-boundary-save]")?.addEventListener("click", () => saveContentBoundary(scopedJob, match, editor));
    editor.querySelector("[data-boundary-reset]")?.addEventListener("click", () => saveContentBoundary(scopedJob, match, editor, "reset"));
    editor.querySelector("[data-boundary-auto]")?.addEventListener("click", () => {
      sendContentSearchFeedback("boundary_incorrect", match.id, match, { searchId: scopedJob.contentSearch.id });
    });
  });
  const resumable = contentBoundaryTimelineEdit;
  if (resumable
      && String(resumable.jobId || "") === String(currentJob?.id || "")
      && String(resumable.searchId || "") === String(scopedJob.contentSearch?.id || "")) {
    const match = (scopedJob.contentSearch?.candidates || []).find((item) => String(item.id) === String(resumable.matchId));
    const editor = root.querySelector(`[data-content-boundary-editor="${CSS.escape(String(resumable.matchId))}"]`);
    if (match && editor && editor !== resumable.editor && !resumable.editor?.isConnected) {
      editor.dataset.boundaryStart = String(resumable.start);
      editor.dataset.boundaryEnd = String(resumable.end);
      if (resumable.surface === "evidence-panel") {
        const inspector = $("#contentBoundaryInspector");
        resumable.origin = { parent: editor.parentElement, next: editor.nextElementSibling };
        resumable.row = editor.closest("[data-content-match-row]");
        resumable.row?.classList.add("boundary-open");
        resumable.row?.setAttribute("aria-current", "true");
        inspector?.replaceChildren(editor);
        editor.classList.remove("hidden");
        setContentDetailPanelMode("edit");
      } else {
        editor.classList.toggle("hidden", resumable.surface !== "card");
      }
      editor.closest("[data-content-match-row]")?.classList.toggle("boundary-open", resumable.surface === "card");
      Object.assign(resumable, { match, scopedJob, editor, root });
      syncContentBoundaryEditor(editor, scopedJob, match, { preview: false });
    }
  }
}

async function sendContentSearchFeedback(verdict, matchId = null, match = null, { skipConfirmation = false, searchId = null } = {}) {
  if (!currentJob || actionBusy) return false;
  const labels = {
    not_relevant: "确认将这条标记为不相关，并从当前候选移除？",
    boundary_incorrect: "确认让系统自动重新识别这个片段的边界？这可能需要较长时间；如果只需前后移动几帧，请使用“调整边界”。",
    missed_content: "确认提高画面采样密度并检查剩余相关范围？这会重新运行局部视觉分析。",
    review_keep: "确认保留这个不确定候选？它会计入最终结果。",
    review_reject: "确认排除这个不确定候选？它会保留在完整性记录中，但不会生成。",
  };
  if (!skipConfirmation && !await requestActionConfirmation({ title: "内容检索反馈", summary: labels[verdict] || "提交反馈？", details: [] })) return false;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/content-search/feedback`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: {
        searchId, verdict, matchId,
        evidenceIds: (match?.evidenceRefs || []).map((item) => item.id).filter(Boolean),
      },
    });
    if (!commitJobAction(job, actionToken)) return;
    if (jobNeedsPolling(job)) pollJob();
    return true;
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
  return false;
}

async function restoreContentSearch(searchId, { openBoundaryMatchId = "" } = {}) {
  openBoundaryMatchId = String(openBoundaryMatchId || "");
  if (!currentJob || !searchId) return false;
  if (!interactionCapabilitiesForJob(currentJob).canRestoreContentSearch) {
    showToast(unavailableInteractionMessage(currentJob, "当前不能恢复历史检索"), "error");
    return false;
  }
  if (actionBusy) {
    showToast("另一项操作正在提交，请稍候", "neutral");
    return false;
  }
  const actionToken = captureJobAction();
  actionBusy = true;
  const entryButton = $("#contentBoundaryEntryButton");
  if (openBoundaryMatchId && entryButton) {
    entryButton.disabled = true;
    entryButton.textContent = "正在恢复检索…";
  }
  try {
    const { job } = await api(`/api/jobs/${actionToken.jobId}/content-search/history/${encodeURIComponent(searchId)}/restore`, { method: "POST" });
    if (!commitJobAction(job, actionToken)) return false;
    if (openBoundaryMatchId) openContentBoundaryMatchAfterTransition(openBoundaryMatchId);
    return true;
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
    return false;
  } finally {
    if (jobActionStillCurrent(actionToken)) {
      actionBusy = false;
      syncContentBoundaryEntry();
    }
  }
}

async function updateContentPersonLabel(personId, currentLabel = "") {
  if (!currentJob || !personId) return;
  if (!interactionCapabilitiesForJob(currentJob).canCorrectPerson) {
    showToast(unavailableInteractionMessage(currentJob, "当前不能修改人物标签"), "error");
    return;
  }
  if (actionBusy) return void showToast("另一项人物操作正在提交，请稍候", "neutral");
  const label = await requestTextInput({
    kicker: "人物校正",
    title: currentLabel ? "修改人物标签" : "添加人物标签",
    summary: "标签只在当前项目内使用，不会推断或保存真实身份。",
    label: "项目内标签",
    value: currentLabel,
    placeholder: "例如：主持人、女嘉宾、黑衣讲解员",
    required: true,
    confirmLabel: "保存标签",
  });
  if (label === null) return;
  const normalized = String(label).trim().replace(/\s+/g, " ");
  if (!normalized) return void showToast("人物标签不能为空");
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const response = await apiJson(
      `/api/jobs/${actionToken.jobId}/content-search/persons/${encodeURIComponent(personId)}`,
      {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: { label: normalized },
      },
    );
    if (!commitJobAction(response.job, actionToken)) return;
    showToast(`已保存人物标签“${normalized}”`, "success");
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function mergeSelectedCurrentPersons() {
  const personIds = [...selectedPersonCorrectionPersons];
  if (!currentJob?.id || personIds.length < 2) return;
  if (!interactionCapabilitiesForJob(currentJob).canCorrectPerson) return void showToast(unavailableInteractionMessage(currentJob), "error");
  if (actionBusy) return void showToast("另一项人物操作正在提交，请稍候", "neutral");
  const persons = currentPersonCatalog(currentJob);
  const selected = persons.filter((person) => personIds.includes(String(person.id)));
  const target = selected[0];
  const labels = selected.map((person) => String(person.label || person.defaultLabel || person.id));
  const confirmed = await requestActionConfirmation({
    title: "合并重复人物卡",
    summary: `确认 ${labels.join("、")} 都是同一个人？`,
    details: [
      `界面将保留“${target?.label || personIds[0]}”，并汇总全部不连续出镜片段`,
      "后续人物检索会同时使用这些卡片的原始轨迹",
      "原始识别索引不会被覆盖，可以随时撤销",
    ],
    confirmLabel: "确认合并",
  });
  if (!confirmed) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const response = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/content-search/persons/merge`, {
      method: "POST",
      body: {
        personIds,
        targetPersonId: personIds[0],
        revision: Number(currentJob.personMergeRevision || 0),
      },
    });
    if (!commitJobAction(response.job, actionToken)) return;
    selectedPersonCorrectionPersons.clear();
    focusedCurrentPersonRef = personIds[0];
    renderCurrentPersons();
    showToast(`已合并 ${personIds.length} 张人物卡，全部原始出镜轨迹已汇总`, "success");
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function undoCurrentPersonMerge() {
  if (!currentJob?.id || !currentJob.personMergeCanUndo) return;
  if (!interactionCapabilitiesForJob(currentJob).canCorrectPerson) return void showToast(unavailableInteractionMessage(currentJob), "error");
  if (actionBusy) return void showToast("另一项人物操作正在提交，请稍候", "neutral");
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const response = await api(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/content-search/persons/merge/undo`, { method: "POST" });
    if (!commitJobAction(response.job, actionToken)) return;
    selectedCurrentPersons.clear();
    selectedPersonCorrectionPersons.clear();
    selectedCurrentPersonRanges.clear();
    selectedCurrentPersonRangeOwner = "";
    focusedCurrentPersonRef = "";
    renderCurrentPersons();
    showToast("已撤销上次人物校正", "success");
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function splitSelectedCurrentPersonRanges() {
  const rangeIds = [...selectedCurrentPersonRanges];
  if (!currentJob?.id || !selectedCurrentPersonRangeOwner || !rangeIds.length) return;
  if (!interactionCapabilitiesForJob(currentJob).canCorrectPerson) return void showToast(unavailableInteractionMessage(currentJob), "error");
  if (actionBusy) return void showToast("另一项人物操作正在提交，请稍候", "neutral");
  const person = currentPersonCatalog(currentJob).find((item) => String(item.id) === selectedCurrentPersonRangeOwner);
  if (!person) return void showToast("人物列表已变化，请刷新后重试");
  const label = await requestTextInput({
    kicker: "人物校正",
    title: "为拆分出的人物命名",
    summary: "可留空并稍后添加标签；拆分后的出镜片段会组成一张新人物卡。",
    label: "新人物标签（可选）",
    placeholder: "例如：另一位嘉宾",
    confirmLabel: "继续拆分",
  });
  if (label === null) return;
  const confirmed = await requestActionConfirmation({
    title: "将错误片段拆为新人物",
    summary: `从“${person.label || person.defaultLabel || person.id}”移出 ${rangeIds.length} 个出镜片段？`,
    details: [
      "这些片段会组成一张新的匿名人物卡",
      "后续人物检索和生成成片会直接使用校正后的分组",
      "原始识别轨迹不会删除，可以撤销此次校正",
    ],
    confirmLabel: "确认拆分",
  });
  if (!confirmed) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const response = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/content-search/persons/ranges/reassign`, {
      method: "POST",
      body: {
        sourcePersonId: selectedCurrentPersonRangeOwner,
        rangeIds,
        label: String(label || "").trim(),
        revision: Number(currentJob.personMergeRevision || 0),
      },
    });
    if (!commitJobAction(response.job, actionToken)) return;
    selectedCurrentPersonRanges.clear();
    selectedCurrentPersonRangeOwner = "";
    selectedCurrentPersons = new Set([String(response.targetPersonId || "")].filter(Boolean));
    focusedCurrentPersonRef = String(response.targetPersonId || "");
    renderCurrentPersons();
    showToast(`已将 ${rangeIds.length} 个片段拆为新人物，后续检索将沿用此次校正`, "success");
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function moveSelectedCurrentPersonRanges(targetPersonId) {
  const rangeIds = [...selectedCurrentPersonRanges];
  const persons = currentPersonCatalog(currentJob);
  const source = persons.find((item) => String(item.id) === selectedCurrentPersonRangeOwner);
  const target = persons.find((item) => String(item.id) === String(targetPersonId || ""));
  if (!currentJob?.id || !source || !target || !rangeIds.length) return;
  if (!interactionCapabilitiesForJob(currentJob).canCorrectPerson) return void showToast(unavailableInteractionMessage(currentJob), "error");
  if (actionBusy) return void showToast("另一项人物操作正在提交，请稍候", "neutral");
  const confirmed = await requestActionConfirmation({
    title: "重新归属人物片段",
    summary: `把 ${rangeIds.length} 个片段从“${source.label || source.id}”移动到“${target.label || target.id}”？`,
    details: ["人物时间线、后续检索和成片生成都会使用新的归属", "原始轨迹不会删除，可以撤销此次校正"],
    confirmLabel: "确认移动",
  });
  if (!confirmed) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const response = await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/content-search/persons/ranges/reassign`, {
      method: "POST",
      body: {
        sourcePersonId: selectedCurrentPersonRangeOwner,
        rangeIds,
        targetPersonId: String(targetPersonId),
        revision: Number(currentJob.personMergeRevision || 0),
      },
    });
    if (!commitJobAction(response.job, actionToken)) return;
    selectedCurrentPersonRanges.clear();
    selectedCurrentPersonRangeOwner = "";
    selectedCurrentPersons = new Set([String(targetPersonId)]);
    focusedCurrentPersonRef = String(targetPersonId);
    renderCurrentPersons();
    showToast(`已将 ${rangeIds.length} 个片段移动到“${target.label || target.id}”`, "success");
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function syncContentPersonTargetControls(panel, { initial = false } = {}) {
  if (!panel) return;
  const controls = panel.querySelector("[data-person-target-controls]");
  if (!controls) return;
  const selected = [...panel.querySelectorAll("[data-person-target]:checked")];
  const previousCount = Number(controls.dataset.selectionCount || 0);
  const modes = [...panel.querySelectorAll("[data-person-match-mode]")];
  const modeGroup = panel.querySelector(".content-person-match-modes");
  if (selected.length === 1) {
    const any = modes.find((input) => input.value === "any");
    if (any && !modes.some((input) => input.checked)) {
      any.checked = true;
      controls.dataset.autoSingleMode = "true";
    }
  } else if (!initial && selected.length > 1 && previousCount < 2 && controls.dataset.autoSingleMode === "true") {
    modes.forEach((input) => { input.checked = false; });
    delete controls.dataset.autoSingleMode;
  }
  controls.dataset.selectionCount = String(selected.length);
  modeGroup?.classList.toggle("hidden", selected.length <= 1);
  panel.querySelectorAll(".content-person-card").forEach((card) => {
    const input = card.querySelector("[data-person-target]");
    card.classList.toggle("target", Boolean(input?.checked));
    const choiceText = card.querySelector(".content-person-choice span");
    if (choiceText) choiceText.textContent = input?.checked ? "已选择" : "选择人物";
  });
  const summary = controls.querySelector("[data-person-selection-summary]");
  if (summary) summary.textContent = `已选 ${selected.length} 人`;
  const confirm = controls.querySelector("[data-person-target-confirm]");
  const hasMode = selected.length === 1 || modes.some((input) => input.checked);
  if (confirm) confirm.disabled = !selected.length || !hasMode || actionBusy;
}

async function selectContentPersonTarget(personIds, matchMode = "any", triggerButton = null, activity = "") {
  const normalizedIds = Array.isArray(personIds) ? personIds.map(String).filter(Boolean) : [String(personIds || "")].filter(Boolean);
  if (!currentJob || !normalizedIds.length || actionBusy) return;
  const actionToken = captureJobAction();
  const messagesEl = ensureChatMessages();
  const targetButtons = [...new Set([
    ...messagesEl.querySelectorAll("[data-person-target], [data-person-target-confirm]"),
    ...document.querySelectorAll("[data-current-person-select], #searchSelectedPersons"),
  ])];
  const previousLabels = new Map(targetButtons.map((button) => [button, button.textContent]));
  actionBusy = true;
  targetButtons.forEach((button) => { button.disabled = true; });
  if (triggerButton) {
    triggerButton.setAttribute("aria-busy", "true");
    triggerButton.textContent = "正在确认…";
  }
  try {
    const response = await apiJson(`/api/jobs/${actionToken.jobId}/content-search/target-person`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: { personIds: normalizedIds, matchMode, ...(activity ? { activity } : {}) },
    });
    if (!commitJobAction(response.job, actionToken)) return;
    currentPersonJobId = String(response.job.id || actionToken.jobId);
    selectedCurrentPersons = new Set(normalizedIds);
    currentPersonMatchMode = matchMode || "any";
    currentPersonActivity = activity === "speaking" ? "speaking" : "appearance";
    if (!$("#personProfilePanel")?.classList.contains("hidden")) renderCurrentPersons();
    const presentation = personTargetToastPresentation(response, normalizedIds.length);
    showToast(presentation.message, presentation.tone);
    if (jobNeedsPolling(response.job)) pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    targetButtons.forEach((button) => {
      if (!button.isConnected) return;
      button.disabled = false;
      button.removeAttribute("aria-busy");
      button.textContent = previousLabels.get(button) || button.textContent;
    });
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

function knownCandidateCount(search) {
  const explicit = search?.candidateCount;
  if (explicit !== null && explicit !== undefined && explicit !== "") {
    const count = Number(explicit);
    if (Number.isFinite(count) && count >= 0) return Math.round(count);
  }
  return Array.isArray(search?.candidates) ? search.candidates.length : null;
}

function personTargetToastPresentation(response, selectedCount = 0) {
  if (!response?.reused) {
    return {
      message: `已确认 ${Math.max(0, Number(selectedCount) || 0)} 个目标人物，正在补充必要分析`,
      tone: "neutral",
    };
  }
  const count = knownCandidateCount(response.job?.contentSearch);
  if (count === null) return { message: "已复用人物轨迹，检索结果已更新", tone: "neutral" };
  if (count === 0) return { message: "已检查所选人物轨迹，没有找到可用出镜片段", tone: "neutral" };
  return { message: `已复用人物轨迹，整理出 ${count} 个出镜片段`, tone: "success" };
}

async function confirmContentPersonSpeaker(personId, speakerRef, triggerButton = null) {
  if (!currentJob || !personId || !speakerRef) {
    showToast("缺少人物或 Speaker 信息，请刷新后重试");
    return;
  }
  if (speakerConfirmationBusy) {
    showToast("正在处理上一次 Speaker 确认，请稍候");
    return;
  }
  const actionToken = captureJobAction();
  const messagesEl = ensureChatMessages();
  speakerConfirmationBusy = true;
  const confirmationButtons = [...messagesEl.querySelectorAll("[data-content-speaker-confirm]")];
  const previousLabels = new Map(confirmationButtons.map((button) => [button, button.textContent]));
  confirmationButtons.forEach((button) => { button.disabled = true; });
  if (triggerButton) {
    triggerButton.setAttribute("aria-busy", "true");
    triggerButton.textContent = `正在确认 ${speakerRef}…`;
  }
  try {
    showToast(`正在提交人物与 ${speakerRef} 的关联…`, "neutral");
    const response = await apiJson(`/api/jobs/${actionToken.jobId}/content-search/confirm-speaker`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: { personId, speakerRef },
    });
    if (!commitJobAction(response.job, actionToken)) {
      if (jobActionStillCurrent(actionToken)) showToast("任务状态已经更新，请在最新结果中重新确认", "neutral");
      return;
    }
    showToast(`已关联 ${speakerRef}，正在按完整对白时间轴检索`, "success");
    if (jobNeedsPolling(response.job)) pollJob();
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(`Speaker 确认失败：${error.message}`);
  } finally {
    speakerConfirmationBusy = false;
    confirmationButtons.forEach((button) => {
      if (!button.isConnected) return;
      button.disabled = false;
      button.removeAttribute("aria-busy");
      button.textContent = previousLabels.get(button) || button.textContent;
    });
  }
}

async function confirmContentSearch(root, reviewJob = currentJob) {
  if (!currentJob || ["running", "rendering", "cancelling"].includes(String(currentJob.status || "")) || actionBusy) return;
  const search = reviewJob?.contentSearch || {};
  const personWorkflow = workflowKindForJob(reviewJob) === "person_edit";
  const completeness = effectiveContentSearchCompleteness(search);
  const exhaustiveIncomplete = String(search.resultMode || "") === "exhaustive"
    && completeness.status !== "complete";
  const pending = Number(completeness.pendingCount || 0);
  if (exhaustiveIncomplete && pending) return void showToast(
    `还有 ${pending} 个不确定候选需要逐项“保留”或“排除”。处理完后即可生成。`, "error",
  );
  const matchIds = [...root.querySelectorAll("[data-content-match]:checked")].map((input) => input.value);
  if (!matchIds.length) return void showToast("请至少选择一个匹配片段", "error");
  const outputMode = root.querySelector("[data-content-output-mode]")?.value || "single_reel";
  const requestedOrderMode = outputMode === "single_reel"
    ? root.querySelector("[data-content-order-mode]")?.value || "source"
    : "source";
  const selectedLookup = new Map(
    (search.candidates || [])
      .filter((item) => matchIds.includes(String(item.id)))
      .map((item) => [String(item.id), item]),
  );
  const savedOrder = (search.reviewDraft?.orderedMatchIds || []).map(String);
  const initialOrder = [...savedOrder.filter((id) => selectedLookup.has(id)), ...matchIds.filter((id) => !savedOrder.includes(id))];
  const matches = initialOrder.map((id) => selectedLookup.get(id)).filter(Boolean);
  const needsContentReview = matches.some((match) => match.evidenceType !== "source_scope"
    && (!["verified", "human_confirmed"].includes(String(match.boundaryVerification?.status || ""))
      || Number(match.boundaryVerification?.verifiedRange?.[0]) !== Number(match.start)
      || Number(match.boundaryVerification?.verifiedRange?.[1]) !== Number(match.end)));
  const total = matches.reduce((sum, item) => sum + Number(item.duration || 0), 0);
  const orderItems = matches.map((item, index) => ({
    id: String(item.id),
    label: item.title || `匹配片段 ${index + 1}`,
    meta: `${formatTime(item.start)}→${formatTime(item.end)} · ${Number(item.duration || 0).toFixed(1)} 秒`,
  }));
  const orderLabels = {
    source: "按源视频时间排列",
    selection: "由你自定义排列",
    llm_recommend: "智能排序后由你确认",
  };
  const confirmation = await requestActionConfirmation({
    title: personWorkflow
      ? (outputMode === "single_reel" ? "第 3 步 · 合成人物出镜视频" : "第 3 步 · 分别导出出镜片段")
      : outputMode === "single_reel" ? "确认生成内容合集" : "确认分别导出内容",
    summary: outputMode === "single_reel"
      ? `将把 ${matches.length} 个已选片段合成一条，约 ${total.toFixed(1)} 秒；${orderLabels[requestedOrderMode] || orderLabels.source}。合成阶段不会再增删片段。`
      : `将把 ${matches.length} 个已选片段分别导出；不会再改变片段范围。`,
    details: matches.map((item, index) => `${index + 1}. ${formatTime(item.start)}→${formatTime(item.end)} · ${item.title || "匹配内容"}`),
    orderMode: outputMode === "single_reel" ? requestedOrderMode : null,
    orderItems: outputMode === "single_reel" ? orderItems : [],
    showOrderOptions: outputMode === "single_reel",
    warning: [needsContentReview ? "部分片段的内容或边界尚未核验，可能混入无关画面。建议先逐项预览和修剪；继续仅代表接受当前范围，不代表内容质检通过。" : "",
      exhaustiveIncomplete ? "完整性检查尚未通过，当前选择可能遗漏其他符合条件的片段；继续不会标记为“已找全”。" : ""].filter(Boolean).join(" "),
    confirmLabel: exhaustiveIncomplete ? "接受可能遗漏并开始合成" : (personWorkflow ? "开始合成视频" : "确认并生成"),
    onDraftChange: ({ orderMode: nextOrderMode, orderedItems }) => scheduleContentReviewDraftSave(root, reviewJob, {
      orderMode: nextOrderMode,
      orderedMatchIds: orderedItems.map((item) => String(item.id)),
    }),
  });
  if (!confirmation) return;
  let orderMode = outputMode === "single_reel" && typeof confirmation === "object"
    ? confirmation.orderMode
    : "source";
  let orderedMatchIds = typeof confirmation === "object" && confirmation.orderedItems?.length
    ? confirmation.orderedItems.map((item) => String(item.id))
    : matchIds;
  let orderReason = "";
  await saveContentReviewDraft(root, reviewJob, { orderMode, orderedMatchIds });

  if (orderMode === "llm_recommend") {
    const recommendationToken = captureJobAction();
    const confirmButton = root.querySelector("[data-confirm-content]");
    const previousLabel = confirmButton?.textContent || (personWorkflow ? "核对完成，合成视频" : "确认所选并生成");
    actionBusy = true;
    if (confirmButton) {
      confirmButton.disabled = true;
      confirmButton.textContent = "正在智能排序…";
    }
    try {
      const recommendation = await apiJson(`/api/jobs/${recommendationToken.jobId}/content-search/order-recommendation`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: {
          searchId: search.id,
          matchIds: orderedMatchIds,
        },
      });
      if (!jobActionStillCurrent(recommendationToken)) return;
      orderedMatchIds = Array.isArray(recommendation.orderedMatchIds)
        ? recommendation.orderedMatchIds.map(String)
        : orderedMatchIds;
      orderReason = String(recommendation.reason || "");
      const matchLookup = new Map(matches.map((item) => [String(item.id), item]));
      const recommendationConfirmed = await requestActionConfirmation({
        title: "确认智能排序",
        summary: "已根据内容连贯性调整所选片段顺序。",
        details: orderedMatchIds.map((id, index) => {
          const item = matchLookup.get(id);
          return `${index + 1}. ${item?.title || "匹配内容"} · ${formatTime(item?.start)}→${formatTime(item?.end)}`;
        }),
        warning: orderReason ? `推荐理由：${orderReason}` : "",
        confirmLabel: "按此顺序生成",
      });
      if (!recommendationConfirmed) return;
      orderMode = "ai_plan";
    } catch (error) {
      if (jobActionStillCurrent(recommendationToken)) showToast(error.message);
      return;
    } finally {
      actionBusy = false;
      if (confirmButton && jobActionStillCurrent(recommendationToken)) {
        confirmButton.disabled = false;
        confirmButton.textContent = previousLabel;
      }
    }
  }

  const subtitleMode = root.querySelector("[data-content-subtitle]")?.checked ? "burn" : "none";
  let subtitleDraftId = null;
  if (subtitleMode === "burn") {
    const matchLookup = new Map(matches.map((item) => [String(item.id), item]));
    const orderedMatches = orderedMatchIds.map((id) => matchLookup.get(String(id))).filter(Boolean);
    const subtitleSegments = orderedMatches.map((item) => ({ start: Number(item.start), end: Number(item.end), playbackRate: 1, transitionIn: { type: "cut", duration: 0 } }));
    const outputs = outputMode === "single_reel"
      ? [{ segments: subtitleSegments }]
      : subtitleSegments.map((segment) => ({ segments: [segment] }));
    const draft = await reviewSubtitlesBeforeRender(outputs, "clean");
    if (!draft) return;
    subtitleDraftId = draft.id;
  }

  const actionToken = captureJobAction();
  actionBusy = true;
  duplicateContentCompositionNotice = null;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/content-search/confirm`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: {
        searchId: search.id,
        matchIds: orderedMatchIds,
        outputMode,
        orderMode,
        orderReason,
        subtitleMode,
        subtitleStyle: "clean",
        subtitleDraftId,
        acknowledgeIncomplete: exhaustiveIncomplete,
        acknowledgeUnverified: needsContentReview,
      },
    });
    if (!commitJobAction(job, actionToken)) return;
    duplicateContentCompositionNotice = null;
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (!jobActionStillCurrent(actionToken)) return;
    if (error.code === "duplicate_content_composition" && error.detail && typeof error.detail === "object") {
      duplicateContentCompositionNotice = {
        ...error.detail,
        jobId: actionToken.jobId,
        searchId: search.id,
      };
      renderReviewRail(currentJob);
      setDirectorStage("events");
      showToast(error.message || "当前选择与已有版本完全一致，未重复生成。", "success");
    } else {
      showToast(error.message || "生成失败，请检查所选片段后重试。", "error");
    }
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function confirmContentSelectionBasket(job = currentJob, acknowledgements = {}) {
  const items = contentBasketItems(job);
  if (!job?.id || !items.length) return;
  if (!interactionCapabilitiesForJob(job).canMutateContentBasket) {
    showToast(unavailableInteractionMessage(job, "当前不能生成成片清单"), "error");
    return;
  }
  if (actionBusy) return void showToast("另一项操作正在提交，请稍候", "neutral");
  const timing = contentBasketTimingSummary(items);
  const needsContentReview = items.some((item) => item.contentVerified !== true);
  const confirmed = await requestActionConfirmation({
    title: "生成清单内容",
    summary: `将成片清单中的 ${items.length} 段按源视频时间合成一条视频，去除重复时间后约 ${timing.uniqueDuration.toFixed(1)} 秒。生成成功后会清空；如果生成失败，选择会继续保留。`,
    details: items.map((item, index) => `${index + 1}. ${item.sourceQuery || "检索"} · ${formatTime(item.start)}→${formatTime(item.end)} · ${item.title || "匹配片段"}`),
    warning: [timing.overlapCount ? `存在 ${timing.overlapCount} 处时间重叠；生成时会保留你选择的片段顺序，但实际时长按时间并集计算。` : "",
      needsContentReview ? "清单包含内容或边界尚未核验的片段；继续表示接受当前范围，不代表内容质检通过。" : ""].filter(Boolean).join(" "),
    confirmLabel: "确认生成",
  });
  if (!confirmed) return;
  const actionToken = captureJobAction(job);
  actionBusy = true;
  try {
    const { job: updated } = await apiJson(`/api/jobs/${encodeURIComponent(job.id)}/content-search/basket/confirm`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: {
        outputMode: job.contentSelectionBasket?.outputMode || "single_reel",
        orderMode: job.contentSelectionBasket?.orderMode || "source",
        subtitleMode: job.contentSelectionBasket?.subtitleMode || "none",
        subtitleStyle: job.contentSelectionBasket?.subtitleStyle || "clean",
        targetSeconds: Number(job.contentSelectionBasket?.targetSeconds) > 0 ? Number(job.contentSelectionBasket.targetSeconds) : null,
        acknowledgeOverlap: timing.overlapCount > 0, acknowledgeUnverified: needsContentReview, ...acknowledgements,
      },
    });
    if (!commitJobAction(updated, actionToken)) return;
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) {
    if (!jobActionStillCurrent(actionToken)) return;
    const message = String(error.message || "");
    if (/时间重叠/.test(message) && !acknowledgements.acknowledgeOverlap) {
      const allow = await requestActionConfirmation({ title: "片段时间存在重叠", summary: message, details: ["系统不会静默合并或删除任何已选片段。"], confirmLabel: "保留重叠并生成" });
      if (allow) { actionBusy = false; return confirmContentSelectionBasket(job, { ...acknowledgements, acknowledgeOverlap: true }); }
    }
    if (/尚未证明找全|可能遗漏/.test(message) && !acknowledgements.acknowledgeIncomplete) {
      const allow = await requestActionConfirmation({ title: "检索完整性未确认", summary: message, details: ["只生成已经明确加入待合并片段的内容。"], confirmLabel: "接受可能遗漏并生成" });
      if (allow) { actionBusy = false; return confirmContentSelectionBasket(job, { ...acknowledgements, acknowledgeIncomplete: true }); }
    }
    showToast(message || "生成清单内容失败，请检查所选片段后重试。", "error");
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function reopenCurrentJobForEditing(options = {}) {
  const openBoundaryMatchId = String(
    options && typeof options === "object" && "openBoundaryMatchId" in options
      ? options.openBoundaryMatchId || ""
      : "",
  );
  const hasOutputs = Boolean(
    (currentJob?.outputs || []).length
    || (currentJob?.outputVersions || []).some((version) => (version.outputs || []).length),
  );
  const canReedit = currentJob && (currentJob.status === "completed" || (currentJob.status === "awaiting_confirmation" && hasOutputs));
  if (!canReedit || actionBusy) return false;
  const actionToken = captureJobAction();
  actionBusy = true;
  const entryButton = $("#contentBoundaryEntryButton");
  if (openBoundaryMatchId && entryButton) {
    entryButton.disabled = true;
    entryButton.textContent = "正在返回片段确认…";
  }
  try {
    const { job } = await api(`/api/jobs/${actionToken.jobId}/reedit`, { method: "POST" });
    if (!jobActionStillCurrent(actionToken)) return false;
    currentOutput = null;
    currentEventGroup = null;
    currentEventSegment = null;
    setDirectorStage("events");
    if (!commitJobAction(job, actionToken)) return false;
    if (openBoundaryMatchId) openContentBoundaryMatchAfterTransition(openBoundaryMatchId);
    return true;
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
    return false;
  } finally {
    if (jobActionStillCurrent(actionToken)) {
      actionBusy = false;
      syncContentBoundaryEntry();
    }
  }
}

async function cancelCurrentJobReediting() {
  if (!currentJob?.reediting || actionBusy) return;
  const actionToken = captureJobAction();
  actionBusy = true;
  try {
    const { job } = await api(`/api/jobs/${actionToken.jobId}/reedit/cancel`, { method: "POST" });
    if (!jobActionStillCurrent(actionToken)) return;
    closeCandidateDrawer();
    commitJobAction(job, actionToken);
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    if (jobActionStillCurrent(actionToken)) actionBusy = false;
  }
}

async function confirmEventGroups(groupIds, outputMode = "single_reel", segmentIds = null, subtitleMode = "none", subtitleStyle = "clean") {
  if (!currentJob || currentJob.status !== "awaiting_confirmation" || actionBusy) return;
  if (!groupIds.length) return void showToast("请至少选择一个事件高光");
  const selected = currentJob.eventGroups.filter((group) => groupIds.includes(group.id)).map((group) => ({
    ...group,
    segments: (group.segments || []).filter((segment) => !segmentIds?.[group.id] || segmentIds[group.id].includes(String(segment.id))),
  })).filter((group) => group.segments.length);
  if (selected.length !== groupIds.length) return void showToast("每个已选事件至少需要保留一个镜头");
  const total = selected.reduce((sum, group) => sum + group.segments.reduce((inner, segment) => inner + Number(segment.duration || (Number(segment.end) - Number(segment.start)) || 0), 0), 0);
  const target = Number(currentJob.totalTargetSeconds || currentJob.request?.totalTargetSeconds || 0);
  const techniquePolicy = currentJob.brief?.techniquePolicy || currentJob.request?.techniquePolicy || { preset: "auto" };
  let techniquePlan = null;
  try {
    const response = await apiJson(`/api/jobs/${currentJob.id}/technique-plan`, {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: { groupIds, segmentIds, targetSeconds: target || null, orderMode: "selection", techniquePolicy, manualSelection: true },
    });
    techniquePlan = response.plan;
  } catch (error) {
    return void showToast(`剪辑手法预检失败：${error.message}`);
  }
  const effectiveTotal = Number(techniquePlan?.effectiveDuration ?? total);
  const outside = techniquePlan?.durationStatus === "over_target" || (target && Math.abs(effectiveTotal - target) > Math.max(4, target * Number(currentJob.durationTolerance || .1)));
  const warning = outside ? `安全精剪后仍为 ${effectiveTotal.toFixed(1)} 秒，超过目标 ${target.toFixed(1)} 秒。系统不会擅自删除你选中的镜头；继续即表示接受本次超时。` : "";
  const segmentCount = selected.reduce((sum, group) => sum + group.segments.length, 0);
  const description = outputMode === "single_reel"
    ? `将把 ${selected.length} 个高光事件、${segmentCount} 个镜头按当前顺序合成为 1 条视频：源片段 ${total.toFixed(1)} 秒，精剪后约 ${effectiveTotal.toFixed(1)} 秒。`
    : `将把 ${selected.length} 个已选事件分别导出为 ${selected.length} 条视频，共 ${segmentCount} 个镜头。`;
  const orderPreview = selected.flatMap((group) => (group.segments || []).map((segment) => `${formatTime(segment.start)}→${formatTime(segment.end)}`)).join("、");
  const orderItems = selected.flatMap((group) => group.segments.map((segment, index) => ({ id: `${group.id}::${segment.id}`, label: `${group.title || "未命名事件"} · 镜头 ${index + 1}`, meta: `${formatTime(segment.start)}→${formatTime(segment.end)}` })));
  const techniqueFacts = (techniquePlan?.segments || []).map((segment, index) => {
    const rate = Number(segment.playbackRate || 1);
    const transition = segment.transitionIn?.type === "fade_black" ? "淡黑" : segment.transitionIn?.type === "dissolve" ? "叠化" : "硬切";
    const bridge = segment.audioBridge?.type === "j_cut" ? "J-cut" : segment.audioBridge?.type === "l_cut" ? "L-cut" : "同步音画";
    return `镜头 ${index + 1} · ${rate.toFixed(rate === 1 ? 0 : 2)}× · ${transition} · ${bridge}`;
  });
  const confirmation = await requestActionConfirmation({ title: outputMode === "single_reel" ? "确认精剪并合成" : "分别导出事件片段", summary: description, details: [`来源：${currentJob.filename || "当前视频"}`, `剪辑策略：${({ auto: "AI 自适应", natural: "自然连贯", tight: "紧凑高光", attraction: "吸引力优先" })[techniquePlan?.techniquePolicy?.preset] || "AI 自适应"}`, `当前排列：${orderPreview || "暂无镜头"}`, ...techniqueFacts.slice(0, 8), ...(techniquePlan?.cutaways?.length ? [`插入镜头：${techniquePlan.cutaways.length} 个（保留主音轨）`] : [])], warning: warning.trim(), orderMode: outputMode === "single_reel" ? "selection" : null, orderItems });
  if (!confirmation) return;
  const orderMode = typeof confirmation === "object" ? confirmation.orderMode : "selection";
  if (typeof confirmation === "object" && confirmation.orderedItems?.length) {
    const orderedGroups = [];
    const orderedSegments = {};
    confirmation.orderedItems.forEach((item) => {
      const [groupId, segmentId] = String(item.id).split("::");
      if (!groupId || !segmentId) return;
      if (!orderedSegments[groupId]) { orderedSegments[groupId] = []; orderedGroups.push(groupId); }
      orderedSegments[groupId].push(segmentId);
    });
    if (orderedGroups.length) { groupIds = orderedGroups; segmentIds = { ...(segmentIds || {}), ...orderedSegments }; }
  }
  if (orderMode === "llm_recommend") {
    requestLlmOrder(currentJob, groupIds, segmentIds);
    return;
  }
  if (orderMode === "ai_plan") {
    setDirectorStage("compose");
    return;
  }
  let subtitleDraftId = null;
  if (subtitleMode === "burn") {
    try {
      let outputs = [];
      if (outputMode === "single_reel") {
        outputs = [{ segments: techniquePlan?.segments || [] }];
      } else {
        for (const groupId of groupIds) {
          const response = await apiJson(`/api/jobs/${currentJob.id}/technique-plan`, {
            method: "POST", headers: { "Content-Type": "application/json" },
            body: {
              groupIds: [groupId],
              segmentIds: segmentIds?.[groupId] ? { [groupId]: segmentIds[groupId] } : null,
              targetSeconds: target || null,
              orderMode: "selection", techniquePolicy, manualSelection: true,
            },
          });
          outputs.push({ segments: response.plan?.segments || [] });
        }
      }
      const draft = await reviewSubtitlesBeforeRender(outputs, subtitleStyle);
      if (!draft) return;
      subtitleDraftId = draft.id;
    } catch (error) {
      return void showToast(error.message);
    }
  }
  const actionToken = captureJobAction();
  if (!jobActionStillCurrent(actionToken)) return;
  // Composition is conversational: keep the left AI director visible while
  // rendering and report progress there instead of opening a second form.
  setDirectorStage("conversation");
  actionBusy = true;
  try {
    const { job } = await apiJson(`/api/jobs/${actionToken.jobId}/confirm`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: { groupIds, segmentIds, outputMode, subtitleMode, subtitleStyle, subtitleDraftId, orderMode, techniquePolicy, acceptOvertime: Boolean(outside) },
    });
    currentEventGroup = null;
    currentEventSegment = null;
    if (!commitJobAction(job, actionToken)) return;
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) actionBusy = false; }
}

async function sendChat(text, contentOptions = null) {
  const value = String(text || chatInput.value).trim();
  if (!currentJob || !value || actionBusy) return;
  const actionToken = captureJobAction();
  if (pendingTimelineSelection && !await confirmPendingTimelineSelection()) return;
  if (!jobActionStillCurrent(actionToken)) return;
  const timelineCompose = chatInput.dataset.timelineCompose === "true";
  const subtitleMode = timelineCompose ? ($("#chatComposeSubtitleMode")?.value || "none") : null;
  const selectedTimelineSelections = timelineChatSelections.map((entry) => ({ ...entry }));
  const uiContext = collectChatUiContext();
  actionBusy = true;
  setComposerSending(true);
  chatInput.value = "";
  timelineChatSelections = [];
  delete chatInput.dataset.timelineCompose;
  ignoredChatContextKeys = new Set();
  renderChatContextBar();
  $("#sendButton").disabled = true;
  activeChatController?.abort();
  const chatController = new AbortController();
  activeChatController = chatController;
  let transientNodes = [];
  let streamNode = null;
  let streamMessage = null;
  let streamText = "";
  const thinkingTimer = window.setTimeout(() => { transientNodes = appendTransientThinking(value); }, 180);
  try {
    const response = await fetch(`/api/jobs/${currentJob.id}/messages/stream`, {
      method: "POST", headers: { "Content-Type": "application/json", Accept: "text/event-stream" },
      signal: chatController.signal,
      body: JSON.stringify({
        text: value,
        uiContext,
        ...(subtitleMode ? { subtitleMode } : {}),
        ...(timelineCompose && selectedTimelineSelections.length ? { selections: selectedTimelineSelections } : {}),
        ...(timelineCompose && selectedTimelineSelections.length ? { orderMode: "selection" } : {}),
        ...(contentOptions || {}),
      }),
    });
    if (!response.ok) {
      const body = await response.json().catch(() => ({}));
      if (window.ClipTalkApi?.createResponseError) {
        throw window.ClipTalkApi.createResponseError(
          body,
          response.status,
          response.headers.get("X-Request-ID") || "",
        );
      }
      throw new Error(
        (typeof body.detail === "object" ? body.detail?.message : body.detail)
        || `请求失败（${response.status}）`,
      );
    }
    if (!response.body) throw new Error("浏览器不支持流式响应");
    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    let buffer = "";
    let finalResult = null;
    const consume = (block) => {
      const lines = block.split(/\r?\n/);
      const event = lines.find((line) => line.startsWith("event:"))?.slice(6).trim();
      const dataLine = lines.find((line) => line.startsWith("data:"));
      if (!dataLine) return;
      const data = JSON.parse(dataLine.slice(5).trim() || "{}");
      if (!jobActionStillCurrent(actionToken)) return;
      if (event === "delta") {
        if (!streamNode) {
          transientNodes.forEach((node) => node.remove());
          transientNodes = [];
          const streamingRole = workflowAssistantLabel(currentJob);
          $("#chatMessages").insertAdjacentHTML("beforeend", `<article class="chat-message assistant streaming-answer"><span class="avatar">AI</span><div class="bubble"><small>${streamingRole}</small><p><span class="stream-text-loader" data-generative-loader="text" data-loader-variant="cascade" data-loader-speed="1.15" data-loader-label="AI 正在输出回答"></span></p></div></article>`);
          streamMessage = $("#chatMessages .streaming-answer:last-child");
          streamNode = streamMessage?.querySelector(".stream-text-loader");
        }
        streamText += String(data.text || "");
        if (window.GenerativeLoadersBridge) {
          const rendered = updateGenerativeLoader(streamNode, {
            kind: "text", variant: "cascade", speed: 1.15, text: streamText,
            label: "AI 正在输出回答",
          });
          if (!rendered && streamNode) {
            clearGenerativeLoader(streamNode);
            streamNode.removeAttribute("data-generative-loader");
            streamNode.textContent = streamText;
          }
        } else if (streamNode) streamNode.textContent = streamText;
        $("#chatMessages").scrollTop = $("#chatMessages").scrollHeight;
      } else if (event === "done") {
        finalResult = data;
      }
    };
    while (true) {
      const { value: chunk, done } = await reader.read();
      buffer += decoder.decode(chunk || new Uint8Array(), { stream: !done });
      const blocks = buffer.split(/\r?\n\r?\n/);
      buffer = blocks.pop() || "";
      blocks.forEach(consume);
      if (done) break;
    }
    if (buffer.trim()) consume(buffer);
    if (!finalResult?.job) throw new Error("流式回答未返回完整任务状态");
    if (!jobActionStillCurrent(actionToken)) return;
    if (streamNode) {
      streamNode.dataset.loaderText = streamText;
      clearGenerativeLoader(streamNode, { preserveText: true });
      if (!window.GenerativeLoadersBridge) streamNode.textContent = streamText;
      streamNode.removeAttribute("data-generative-loader");
      streamMessage?.classList.remove("streaming-answer");
    }
    const { action, job } = finalResult;
    const routingConfirmation = action === "workflow-confirmation" ? finalResult.routingConfirmation : null;
    if (action === "selection-ready-event" && finalResult.groupIds?.length) {
      // Limit the next confirmation card to the clips just selected on the
      // timeline; the complete event pool remains available for later edits.
      job.pendingSelectionGroupIds = finalResult.groupIds.map(String);
    }
    const derivedJob = action === "derived" || action === "job-handoff";
    if (derivedJob) currentOutput = null;
    if (!commitJobAction(job, actionToken, { allowJobSwitch: derivedJob })) return;
    // The action token correctly belongs to the parent task and becomes stale
    // after the intentional switch. Restore the composer here instead of
    // relying on the finally block's same-job cleanup.
    if (derivedJob) {
      actionBusy = false;
      $("#sendButton").disabled = false;
      setComposerSending(false);
    }
    if (action === "workflow-confirmation" && routingConfirmation) {
      const recommendation = routingConfirmation.recommendation || {};
      const recommendedKind = String(recommendation.workflowKind || "");
      const currentKind = workflowKindForJob(currentJob);
      const card = document.createElement("article");
      card.className = "chat-message assistant workflow-routing-confirmation";
      card.innerHTML = `<span class="avatar">AI</span><div class="bubble"><small>剪辑方式确认</small><p>${escapeHtml(routingConfirmation.message || "请选择要使用的剪辑方式。")}</p>${recommendedKind ? `<p class="workflow-routing-reason">AI 倾向于 ${escapeHtml(workflowDisplayName(recommendedKind))} · ${Math.round(Number(recommendation.confidence || 0) * 100)}%<br>${escapeHtml(recommendation.reason || "")}</p>` : ""}<div class="workflow-routing-options">${(routingConfirmation.options || []).map((option) => { const kind = String(option.id || ""); return `<button type="button" data-confirmed-workflow="${escapeHtml(kind)}" class="${kind === recommendedKind ? "recommended" : ""}">${kind === currentKind ? `继续${escapeHtml(workflowDisplayName(kind))}` : escapeHtml(option.label || workflowDisplayName(kind))}${kind === recommendedKind ? " · AI 推荐" : ""}</button>`; }).join("")}</div></div>`;
      $("#chatMessages")?.appendChild(card);
      card.querySelectorAll("[data-confirmed-workflow]").forEach((button) => button.addEventListener("click", () => {
        card.remove();
        sendChat(String(routingConfirmation.text || value), { confirmedWorkflowKind: button.dataset.confirmedWorkflow });
      }, { once: true }));
      $("#chatMessages").scrollTop = $("#chatMessages").scrollHeight;
      return;
    }
    clearTimeout(pollTimer);
    if (action === "selection-ready" && Number.isInteger(Number(finalResult.position))) {
      // The user asked to compose a manually selected range. Pause here so
      // the confirmation card can expose manual ordering and LLM ordering;
      // do not silently start rendering with an implicit order.
      actionBusy = false;
      await confirmCandidates([Number(finalResult.position)], "single_reel");
      return;
    }
    if (action === "selection-ready-event" && finalResult.groupIds?.length) {
      actionBusy = false;
      return;
    }
    if (jobNeedsPolling(job)) pollJob();
  } catch (error) {
    if (streamNode && streamText) {
      streamNode.dataset.loaderText = streamText;
      clearGenerativeLoader(streamNode, { preserveText: true });
      if (!window.GenerativeLoadersBridge) streamNode.textContent = streamText;
      streamNode.removeAttribute("data-generative-loader");
      streamMessage?.classList.remove("streaming-answer");
    }
    if (jobActionStillCurrent(actionToken) && error.name !== "AbortError") showToast(error.message);
  } finally {
    window.clearTimeout(thinkingTimer);
    if (activeChatController === chatController) activeChatController = null;
    transientNodes.forEach((node) => node.remove());
    if (jobActionStillCurrent(actionToken)) {
      actionBusy = false;
      $("#sendButton").disabled = false;
      setComposerSending(false);
      chatInput.focus();
    }
  }
}

async function confirmBrief(job) {
  if (!job || job.status !== "brief_confirmation" || actionBusy) return;
  const brief = collectBriefFromEditor($("#chatMessages"), job.brief || {});
  if (!brief.objective?.trim()) { showToast("请先填写剪辑目标"); return; }
  const actionToken = captureJobAction(job);
  actionBusy = true;
  try {
    const { job: updated } = await apiJson(`/api/jobs/${actionToken.jobId}/brief/confirm`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: { brief, confirmed: true },
    });
    if (!commitJobAction(updated, actionToken)) return;
    pollJob();
  } catch (error) { if (jobActionStillCurrent(actionToken)) showToast(error.message); }
  finally { if (jobActionStillCurrent(actionToken)) actionBusy = false; }
}

function briefEditorMarkup(brief = {}, context = "chat") {
  const style = brief.style || {};
  const list = (value) => Array.isArray(value) ? value.join("、") : String(value || "");
  const field = (name) => `${context}-${name}`;
  return `<section class="brief-editor" data-brief-editor>
    <header><div><small>AI 生成 · 可编辑草稿</small><strong>请确认这是不是你真正想剪的内容</strong></div><span>未确认</span></header>
    <div class="brief-editor-grid">
      <label><span>剪辑目标</span><input data-brief-field="objective" id="${field("objective")}" value="${escapeHtml(brief.objective || "事件高光合集")}" placeholder="例如：把人物反应剪成一条高光成片"></label>
      <label><span>目标总时长（秒）</span><input data-brief-field="targetDurationSeconds" id="${field("duration")}" type="number" min="0" step="1" value="${brief.targetDurationSeconds ? Number(brief.targetDurationSeconds) : ""}" placeholder="留空由 AI 推荐"></label>
      <label class="brief-editor-wide"><span>叙事目标</span><textarea data-brief-field="narrativeGoal" id="${field("narrative")}" rows="2" placeholder="希望观众看完后理解什么">${escapeHtml(brief.narrativeGoal || "")}</textarea></label>
      <label><span>重点关注</span><input data-brief-field="focus" id="${field("focus")}" value="${escapeHtml(list(brief.focus))}" placeholder="人物反应、情绪变化"></label>
      <label><span>希望保留</span><input data-brief-field="includeRules" id="${field("include")}" value="${escapeHtml(list(brief.includeRules))}" placeholder="关键事件、完整表达"></label>
      <label><span>排除内容</span><input data-brief-field="excludeRules" id="${field("exclude")}" value="${escapeHtml(list(brief.excludeRules))}" placeholder="重复镜头、片头广告"></label>
      <label><span>剪辑节奏</span><select data-brief-field="pace" id="${field("pace")}"><option value="natural" ${style.pace === "natural" ? "selected" : ""}>自然纪实</option><option value="tight" ${style.pace === "tight" ? "selected" : ""}>紧凑</option><option value="fast" ${style.pace === "fast" ? "selected" : ""}>快节奏</option></select></label>
      <label><span>视觉风格</span><select data-brief-field="tone" id="${field("tone")}"><option value="documentary" ${style.tone === "documentary" ? "selected" : ""}>纪实自然</option><option value="cinematic" ${style.tone === "cinematic" ? "selected" : ""}>电影感</option><option value="emotional" ${style.tone === "emotional" ? "selected" : ""}>情绪优先</option></select></label>
      <label class="brief-editor-wide"><span>剪辑策略</span><select data-brief-field="techniquePreset" id="${field("technique")}"><option value="auto" ${!brief.techniquePolicy?.preset || brief.techniquePolicy?.preset === "auto" ? "selected" : ""}>AI 自适应</option><option value="natural" ${brief.techniquePolicy?.preset === "natural" ? "selected" : ""}>自然连贯</option><option value="tight" ${brief.techniquePolicy?.preset === "tight" ? "selected" : ""}>紧凑高光</option><option value="attraction" ${brief.techniquePolicy?.preset === "attraction" ? "selected" : ""}>吸引力优先</option></select><small>只调整节奏和衔接；不会擅自删除你手动选择的镜头。</small></label>
      <label class="brief-advanced-hidden"><span>字幕策略</span><select data-brief-field="subtitlePreference" id="${field("subtitle")}"><option value="none" ${!brief.subtitlePreference || brief.subtitlePreference === "none" ? "selected" : ""}>不添加字幕</option><option value="ask" ${brief.subtitlePreference === "ask" ? "selected" : ""}>稍后确认</option><option value="burn" ${brief.subtitlePreference === "burn" ? "selected" : ""}>添加字幕</option><option value="custom" ${brief.subtitlePreference && !["ask", "burn", "none"].includes(brief.subtitlePreference) ? "selected" : ""}>自定义</option></select><input class="brief-custom-input ${brief.subtitlePreference && !["ask", "burn", "none"].includes(brief.subtitlePreference) ? "" : "hidden"}" data-brief-custom="subtitlePreference" value="${brief.subtitlePreference && !["ask", "burn", "none"].includes(brief.subtitlePreference) ? escapeHtml(brief.subtitlePreference) : ""}" placeholder="例如：只添加重点对白"></label>
      <label class="brief-advanced-hidden"><span>剪辑方式</span><select data-brief-field="editMode" id="${field("edit")}"><option value="ai_plan" ${!brief.editMode || brief.editMode === "ai_plan" ? "selected" : ""}>AI 智能规划</option><option value="recommend_review" ${brief.editMode === "recommend_review" ? "selected" : ""}>AI 推荐后我审核</option><option value="manual" ${brief.editMode === "manual" ? "selected" : ""}>我手动选择镜头</option><option value="custom" ${brief.editMode && !["ai_plan", "recommend_review", "manual"].includes(brief.editMode) ? "selected" : ""}>自定义</option></select><input class="brief-custom-input ${brief.editMode && !["ai_plan", "recommend_review", "manual"].includes(brief.editMode) ? "" : "hidden"}" data-brief-custom="editMode" value="${brief.editMode && !["ai_plan", "recommend_review", "manual"].includes(brief.editMode) ? escapeHtml(brief.editMode) : ""}" placeholder="例如：先生成 3 个版本"></label>
      <label class="brief-editor-wide brief-advanced-hidden"><span>成片结构</span><select data-brief-field="structure" id="${field("structure")}"><option value="auto" ${!brief.structure || brief.structure === "auto" ? "selected" : ""}>由 AI 根据事件完整性决定</option><option value="hook_story_result" ${brief.structure === "hook_story_result" ? "selected" : ""}>开场 → 发展 → 高潮 → 结尾</option><option value="montage" ${brief.structure === "montage" ? "selected" : ""}>节奏蒙太奇，优先连续精彩瞬间</option><option value="custom" ${brief.structure && !["auto", "hook_story_result", "montage"].includes(brief.structure) ? "selected" : ""}>自定义</option></select><input class="brief-custom-input ${brief.structure && !["auto", "hook_story_result", "montage"].includes(brief.structure) ? "" : "hidden"}" data-brief-custom="structure" value="${brief.structure && !["auto", "hook_story_result", "montage"].includes(brief.structure) ? escapeHtml(brief.structure) : ""}" placeholder="例如：先冲突后解释，最后用人物反应结束"></label>
    </div>
    <p class="brief-editor-hint">用逗号或顿号分隔多个重点。保存只更新草稿，不会开始分析；点击“确认简报并开始分析”后，这些要求会用于高光发现和成片编排。目标时长按单个成片版本计算；素材不足时会明确提示，不会用重复或低价值画面硬凑。</p>
    <div class="brief-editor-actions"><button type="button" class="brief-reset-button" data-brief-reset>恢复 AI 建议</button><button type="button" class="brief-save-button" data-brief-save>保存修改</button><button type="button" class="confirm-brief-button" data-brief-confirm>确认简报并开始分析</button></div>
  </section>`;
}

function collectBriefFromEditor(root, fallback = {}) {
  const value = (name, defaultValue = "") => root?.querySelector(`[data-brief-field="${name}"]`)?.value ?? defaultValue;
  const split = (name) => value(name).split(/[，,、\n]/).map((item) => item.trim()).filter(Boolean);
  const customValue = (name, selected) => selected === "custom" ? (root?.querySelector(`[data-brief-custom="${name}"]`)?.value?.trim() || "ask") : selected;
  const subtitlePreference = customValue("subtitlePreference", value("subtitlePreference", fallback.subtitlePreference || "none"));
  const editMode = customValue("editMode", value("editMode", fallback.editMode || "ai_plan"));
  const structure = customValue("structure", value("structure", fallback.structure || "auto"));
  return { ...fallback, objective: value("objective", fallback.objective || "事件高光合集").trim(), narrativeGoal: value("narrativeGoal", fallback.narrativeGoal || "").trim(), targetDurationSeconds: Number(value("targetDurationSeconds")) || null, focus: split("focus"), includeRules: split("includeRules"), excludeRules: split("excludeRules"), subtitlePreference, editMode, structure, techniquePolicy: { ...(fallback.techniquePolicy || {}), preset: value("techniquePreset", fallback.techniquePolicy?.preset || "auto") }, style: { ...(fallback.style || {}), pace: value("pace", fallback.style?.pace || "natural"), tone: value("tone", fallback.style?.tone || "documentary") } };
}

function bindBriefEditor(job, root) {
  const editor = root?.querySelector("[data-brief-editor]");
  if (!editor) return;
  editor?.querySelectorAll("[data-brief-field]").forEach((select) => select.addEventListener("change", () => {
    const custom = editor.querySelector(`[data-brief-custom="${select.dataset.briefField}"]`);
    if (custom) custom.classList.toggle("hidden", select.value !== "custom");
  }));
  editor.querySelector("[data-brief-confirm]")?.addEventListener("click", () => confirmBrief(job));
  editor.querySelector("[data-brief-save]")?.addEventListener("click", async () => {
    const brief = collectBriefFromEditor(root, job.brief || {});
    if (!brief.objective) { showToast("请先填写剪辑目标"); return; }
    // Keep the draft local while the current server process is running. This
    // deliberately does not call the confirm endpoint, so saving can never
    // accidentally start VLM analysis.
    renderJob({ ...job, brief, briefStatus: "draft", detail: "需求简报已保存，等待确认" });
  });
  editor.querySelector("[data-brief-reset]")?.addEventListener("click", () => { renderJob({ ...job, brief: job.brief || {} }); });
}

async function cancelCurrentJob() {
  if (!currentJob || !executionForJob(currentJob).capabilities?.canCancel) return;
  const actionToken = captureJobAction();
  const buttons = [...document.querySelectorAll("[data-inline-cancel], #cancelButton")];
  buttons.forEach((button) => { button.disabled = true; button.textContent = "正在停止…"; });
  const progressPanel = $("#inlineAnalysisProgress");
  const progressDetail = progressPanel?.querySelector("[data-inline-detail]");
  const progressEta = progressPanel?.querySelector("[data-inline-eta]");
  if (progressPanel) {
    progressPanel.dataset.cancelRequested = "true";
    progressPanel.setAttribute("aria-label", "正在停止当前处理");
  }
  if (progressDetail) progressDetail.textContent = "停止请求已发送，正在终止当前处理";
  if (progressEta) progressEta.textContent = "复杂模型调用最长约 15 秒";
  showToast("已收到停止请求，正在终止当前处理；复杂模型调用最长约 15 秒。", "success");
  try {
    const activeSearchId = String(currentJob?.contentSearchSession?.activeSearchId || "");
    const pendingSearchId = String(currentJob?.pendingContentSearch?.id || "");
    const queryOnly = activeSearchId && pendingSearchId && activeSearchId === pendingSearchId && currentJob?.contentSearch?.id;
    const endpoint = queryOnly
      ? `/api/jobs/${actionToken.jobId}/content-search/${encodeURIComponent(activeSearchId)}/cancel`
      : `/api/jobs/${actionToken.jobId}/cancel`;
    const { job } = await api(endpoint, { method: "POST" });
    if (!commitJobAction(job, actionToken)) return;
    if (jobNeedsPolling(job)) {
      clearTimeout(pollTimer);
      pollTimer = window.setTimeout(pollJob, 120);
    }
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) {
      buttons.forEach((button) => { button.disabled = false; button.textContent = executionCancelLabel(currentJob); });
      if (progressPanel) {
        delete progressPanel.dataset.cancelRequested;
        progressPanel.setAttribute("aria-label", "AI 分析进度");
      }
      updateInlineAnalysisProgress(currentJob);
      showToast(`停止失败：${error.message || error}`, "error");
    }
  }
}

async function deleteHistoryJob(jobId, relatedJobIds = []) {
  const ids = [...new Set([...relatedJobIds, jobId].map(String).filter(Boolean))];
  const records = (await Promise.all(ids.map((id) => api(`/api/jobs/${encodeURIComponent(id)}`).catch(() => null))))
    .filter((entry) => entry?.job)
    .map((entry) => entry.job);
  const job = records.find((entry) => String(entry.id) === String(jobId));
  if (!job) return void showToast("任务已不存在，任务列表将自动刷新。", "neutral");
  if (records.some((entry) => isActiveJobStatus(entry.status))) return void showToast("任务仍在运行，请先停止后再删除。");
  const historyCount = Math.max(0, records.length - 1);
  if (!await requestActionConfirmation({
    title: "删除剪辑任务",
    summary: `将删除“${taskModePresentation(job).label}”任务${historyCount ? `及其 ${historyCount} 条历史记录` : ""}。`,
    details: [
      `${records.length} 个任务记录中的分析数据和任务内成片将被删除`,
      "若同源的其他任务仍存在，共享源视频与缓存会继续保留",
      "删除最后一个同源任务时，共享源视频、分析证据与缓存会一并清理",
      "此前另存的独立 MP4 副本不会被删除",
      "删除后无法恢复",
    ],
    confirmLabel: "确认删除",
    tone: "danger",
  })) return;
  let deleted = 0;
  const deleteOrder = [...records].sort((left, right) => String(left.id) === String(jobId) ? 1 : String(right.id) === String(jobId) ? -1 : 0);
  try {
    for (const record of deleteOrder) {
      const id = String(record.id);
      const intent = await api(`/api/jobs/${encodeURIComponent(id)}/delete-intent`, { method: "POST" });
      await apiJson(`/api/jobs/${encodeURIComponent(id)}`, {
        method: "DELETE",
        headers: { "Content-Type": "application/json" },
        body: { revision: intent.revision, deleteIntent: intent.deleteIntent },
      });
      deleted += 1;
    }
    if (ids.includes(String(currentJob?.id || ""))) resetWorkspace();
    window.ClipTalkAppShell?.refreshCatalog();
  } catch (error) {
    showToast(deleted
      ? `已删除 ${deleted}/${records.length} 条记录；其余记录仍保留。${error.message}`
      : error.message);
  }
}
window.deleteHistoryJob = deleteHistoryJob;

// Task restoration remains available after removing the history/keep UI. It
// is deliberately private to startup and never renders a task list.
async function restoreCurrentJob() {
  if (currentJob || homeNavigationRequested) return;
  const generation = workspaceGeneration;
  const response = await api("/api/jobs").catch(() => ({ jobs: [] }));
  if (generation !== workspaceGeneration || currentJob || homeNavigationRequested) return;
  const jobs = response.jobs || [];
  const savedJobId = storedCurrentJobId();
  const requestedJobId = routeJobId() || savedJobId;
  // Only restore a task when this tab explicitly remembered one.  Falling
  // back to the first unfinished task makes a newly-created workspace jump
  // into an unrelated task after a refresh or a late API response.
  let activeJob = requestedJobId
    ? jobs.find((item) => String(item.id) === String(requestedJobId))
    : null;
  if (requestedJobId && !activeJob) {
    const direct = await api(`/api/jobs/${encodeURIComponent(requestedJobId)}`).catch(() => null);
    activeJob = direct?.job || null;
  }
  if (requestedJobId && !activeJob) {
    // Do not retry a deleted/invalid task id on every refresh.
    forgetCurrentJob();
  }
  if (!activeJob || generation !== workspaceGeneration || homeNavigationRequested) return;
  const opened = await api(`/api/jobs/${encodeURIComponent(activeJob.id)}`).catch(() => ({ job: activeJob }));
  if (currentJob || generation !== workspaceGeneration || homeNavigationRequested) return;
  restoringHistory = true;
  try { renderJob(opened.job); } finally { restoringHistory = false; }
  if (jobNeedsPolling(opened.job)) pollJob();
  studio?.classList.remove("home-mode");
  $("#homeView")?.classList.add("hidden");
}

function jumpToAdjacentMoment(direction) {
  const candidates = [...(currentJob?.candidates || [])].sort((left, right) => Number(left.start) - Number(right.start));
  if (!candidates.length) return;
  const now = timelineAbsoluteTime();
  let candidate;
  if (direction > 0) candidate = candidates.find((item) => Number(item.start) > now + .35) || candidates[0];
  else candidate = [...candidates].reverse().find((item) => Number(item.start) < now - .35) || candidates.at(-1);
  previewCandidate(Number(candidate.index), { showEvidence: false });
}

function adjacentCandidate(direction) {
  const candidates = [...(currentJob?.candidates || [])].sort((left, right) => Number(left.start) - Number(right.start));
  if (!candidates.length) return null;
  const currentIndex = currentCandidate ? candidates.findIndex((item) => Number(item.index) === Number(currentCandidate.index)) : -1;
  const nextIndex = currentIndex < 0 ? (direction > 0 ? 0 : candidates.length - 1) : currentIndex + direction;
  return nextIndex >= 0 && nextIndex < candidates.length ? candidates[nextIndex] : null;
}

function setCandidateChecked(index, checked) {
  const input = document.querySelector(`.candidate-list input[type="checkbox"][value="${CSS.escape(String(index))}"]`)
    || document.querySelector(`.legacy-candidate-row input[type="checkbox"][value="${CSS.escape(String(index))}"]`);
  if (!input) return false;
  input.checked = checked;
  input.closest(".candidate-row,.legacy-candidate-row")?.classList.toggle("excluded", !checked);
  return true;
}

function reviewSelectionKeys() {
  if (!currentJob) return { type: "none", values: [] };
  if (currentJob.eventGroups?.length) {
    return { type: "groups", values: [...document.querySelectorAll(".event-group-check:checked,.rail-event-check:checked")].map((input) => input.value) };
  }
  return { type: "candidates", values: [...document.querySelectorAll(".candidate-list input:checked,.legacy-candidate-row input:checked")].map((input) => Number(input.value)) };
}

function updateReviewSelectionSummary(root = document) {
  const summary = root.querySelector?.("[data-selection-summary]");
  if (!summary) return;
  const groupInputs = [...root.querySelectorAll(".event-group-check:checked,.rail-event-check:checked")];
  const candidateInputs = [...root.querySelectorAll(".candidate-list input:checked,.legacy-candidate-row input:checked")];
  if (groupInputs.length) {
    const selectedGroups = groupInputs.map((input) => currentJob?.eventGroups?.find((group) => String(group.id) === String(input.value))).filter(Boolean);
    const duration = selectedGroups.reduce((sum, group) => sum + Number(group.actualDuration || 0), 0);
    summary.innerHTML = `已选 <b>${selectedGroups.length}</b> 个事件 · 预计 <b>${duration.toFixed(1)} 秒</b>`;
  } else if (candidateInputs.length || root.querySelector(".candidate-list")) {
    const selected = candidateInputs.map((input) => currentJob?.candidates?.find((candidate) => Number(candidate.index) === Number(input.value))).filter(Boolean);
    const duration = selected.reduce((sum, candidate) => sum + Number(candidate.duration || 0), 0);
    summary.innerHTML = `已选 <b>${selected.length}</b> 个镜头 · 预计 <b>${duration.toFixed(1)} 秒</b>`;
  }
  syncReviewActionDock();
}

function normalizeSegmentActionLabels(root = document) {
  root.querySelectorAll?.(".preview-segment").forEach((button) => { button.textContent = "预览"; button.title = "预览镜头"; button.setAttribute("aria-label", "预览镜头"); });
  root.querySelectorAll?.(".move-segment-up").forEach((button) => { button.textContent = "↑"; button.title = "上移镜头"; button.setAttribute("aria-label", "上移镜头"); });
  root.querySelectorAll?.(".move-segment-down").forEach((button) => { button.textContent = "↓"; button.title = "下移镜头"; button.setAttribute("aria-label", "下移镜头"); });
  root.querySelectorAll?.(".move-segment-group").forEach((button) => { button.textContent = "移动"; button.title = "移动到其他事件"; button.setAttribute("aria-label", "移动到其他事件"); });
  root.querySelectorAll?.(".delete-segment").forEach((button) => { button.textContent = "删除"; button.title = "删除镜头"; button.setAttribute("aria-label", "删除镜头"); });
}

let reviewExclusionsPersistTimer = null;
function persistReviewExclusions() {
  if (!currentJob?.id || !["awaiting_confirmation", "completed"].includes(currentJob.status)) return;
  const actionToken = captureJobAction();
  const jobId = actionToken.jobId;
  const indices = [...locallyExcludedCandidates];
  clearTimeout(reviewExclusionsPersistTimer);
  const confirmedIndices = new Set((currentJob.reviewExcludedCandidates || []).map((index) => Number(index)));
  reviewExclusionsPersistTimer = setTimeout(async () => {
    if (!jobActionStillCurrent(actionToken)) return;
    try {
      const { job } = await window.ClipTalkReviewActions.persistExclusions({ jobId, indices });
      if (!jobActionStillCurrent(actionToken)) return;
      locallyExcludedCandidates = new Set((job.reviewExcludedCandidates || []).map((index) => Number(index)));
      commitJobAction(job, actionToken);
    } catch (error) {
      if (!jobActionStillCurrent(actionToken)) return;
      locallyExcludedCandidates = confirmedIndices;
      (currentJob.candidates || []).forEach((candidate) => {
        const index = Number(candidate.index);
        setCandidateChecked(index, !confirmedIndices.has(index));
      });
      updateReviewSelectionSummary(document);
      showToast(`排除状态保存失败：${error.message}`);
    }
  }, 180);
}

function confirmCurrentReviewSelection() {
  const selection = reviewSelectionKeys();
  if (selection.type === "groups") return confirmEventGroups([...new Set(selection.values)], "single_reel");
  if (selection.type === "candidates") return confirmCandidates([...new Set(selection.values)]);
  return null;
}

function excludeCurrentCandidate() {
  if (!currentCandidate || currentJob?.status !== "awaiting_confirmation") return;
  if (setCandidateChecked(currentCandidate.index, false)) {
    locallyExcludedCandidates.add(Number(currentCandidate.index));
    persistReviewExclusions();
    document.querySelector(`[data-candidate-row="${CSS.escape(String(currentCandidate.index))}"]`)?.classList.add("excluded");
    $("#timelineHint").textContent = `已排除候选 ${Number(currentCandidate.index) + 1}；按 N 继续查看下一条`;
  }
}

function renderHomeCreateCard() {
  return `<article class="home-create-card"><button type="button" data-home-create><span class="home-create-icon" data-ui-icon="film-slate" aria-hidden="true"></span><span class="home-create-copy"><strong>创建新任务</strong><small>进入后选择视频，再用一句话描述你想得到的结果。</small></span><span class="home-create-arrow" data-ui-icon="arrow-right" aria-hidden="true"></span></button><p>AI 会先理解需求，再开始分析素材。</p></article>`;
}

function renderHomeLoadingCard() {
  return `<div class="home-loading-card" role="status" aria-label="正在加载最近任务"><span class="home-loading-cover" aria-hidden="true"></span><span class="home-loading-line home-loading-line-title" aria-hidden="true"></span><span class="home-loading-line" aria-hidden="true"></span><strong class="sr-only">正在加载最近任务</strong></div>`;
}

function setHomeViewState(state) {
  const homeView = $("#homeView");
  const summary = homeView?.querySelector(".home-summary");
  if (homeView) {
    homeView.dataset.homeState = state;
    homeView.setAttribute("aria-busy", String(state === "loading"));
  }
  summary?.classList.toggle("hidden", state !== "ready");
}

function renderHomeProjectCard(projectJobs) {
  const ordered = [...projectJobs].sort((left, right) => String(right.updatedAt || "").localeCompare(String(left.updatedAt || "")));
  const primary = ordered[0];
  const width = Number(primary.videoInfo?.width || 0);
  const height = Number(primary.videoInfo?.height || 0);
  const orientation = width > 0 && height > 0 ? width / height : 16 / 9;
  const orientationClass = orientation > 1.25 ? " landscape" : orientation < .82 ? " portrait" : " square";
  const thumbnailState = String(primary.thumbnailStatus || (primary.thumbnailReady ? "ready" : "pending"));
  const thumbnailUrl = primary.thumbnailUrl || `/api/jobs/${encodeURIComponent(primary.id)}/thumbnail`;
  const totalOutputs = ordered.reduce((sum, job) => sum + jobOutputCount(job), 0);
  const rows = ordered.map((job, index) => {
    const workflow = taskModePresentation(job);
    const vocabulary = workflowVocabulary(job);
    const display = displayStatusForJob(job);
    const count = Number(job.candidateCount || job.eventGroupCount || 0);
    return `<div class="home-project-task-row${index >= 4 ? " home-project-task-extra" : ""}"><button type="button" class="home-project-task" data-home-task="${escapeHtml(job.id)}"><span class="home-workflow-label ${escapeHtml(workflow.workflowKey)}">${escapeHtml(workflow.label)}</span><span><strong>${escapeHtml(display.text)}</strong><small>${count} 个${escapeHtml(vocabulary.candidatePlural)} / ${jobOutputCount(job)} 个${escapeHtml(vocabulary.outputPlural)}</small></span><span class="home-task-arrow" data-ui-icon="arrow-right" aria-hidden="true"></span></button><button type="button" class="home-project-task-delete" data-home-delete="${escapeHtml(job.id)}" aria-label="删除${escapeHtml(workflow.label)}任务" title="删除该剪辑任务">删除</button></div>`;
  }).join("");
  const more = ordered.length > 4
    ? `<button type="button" class="home-project-more" data-home-project-more aria-expanded="false"><span>查看其余 ${ordered.length - 4} 个任务</span><b>⌄</b></button>` : "";
  return `<article class="home-project-card" data-home-project="${escapeHtml(primary.sourceProjectId || primary.id)}"><div class="home-project-cover${orientationClass}" data-home-task="${escapeHtml(primary.id)}" data-thumbnail-state="${escapeHtml(thumbnailState)}"><span class="home-cover-loader" data-generative-loader="image" data-loader-variant="resolution" data-loader-size="100%" data-loader-radius="10" data-loader-label="正在准备视频封面"></span><img data-task-thumbnail data-thumbnail-url="${escapeHtml(thumbnailUrl)}" data-thumbnail-status="${escapeHtml(thumbnailState)}" alt="${escapeHtml(primary.filename || "视频封面")}" loading="lazy"></div><header><div><small>视频素材</small><strong title="${escapeHtml(primary.filename || "未命名视频")}">${escapeHtml(primary.filename || "未命名视频")}</strong></div><span>${ordered.length} 个任务 / ${totalOutputs} 条成片</span></header><div class="home-project-tasks">${rows}</div>${more}</article>`;
}

function openNewTaskFromHome() {
  // Switch immediately so a stale panel state can never make the click look
  // inert. Workspace cleanup is best-effort and must not block the upload UI.
  // Keep restoration blocked until the new upload has been submitted. An
  // older /api/jobs request may still resolve after this click; it must not
  // render the previous task over the empty new-task workspace.
  homeNavigationRequested = true;
  studio?.classList.remove("home-mode");
  $("#homeView")?.classList.add("hidden");
  $("#uploadView")?.classList.remove("hidden");
  try {
    resetWorkspace(false, true);
    beginTaskCreationSession();
    // Keep this explicit even if a legacy optional panel throws during
    // cleanup; the new-task screen must never retain the previous dialogue.
    initialConversation();
    homeNavigationRequested = true;
  } catch (error) {
    console.warn("Failed to fully reset the workspace before a new task", error);
    beginTaskCreationSession();
    initialConversation();
    homeNavigationRequested = true;
    $("#homeView")?.classList.add("hidden");
    $("#uploadView")?.classList.remove("hidden");
  }
}

async function openHomeTask(jobId) {
  if (!jobId || openingHomeTaskId) return;
  invalidateWorkspaceRequests();
  const generation = workspaceGeneration;
  homeNavigationRequested = false;
  openingHomeTaskId = String(jobId);
  try {
    const { job } = await api(`/api/jobs/${encodeURIComponent(jobId)}`);
    if (generation !== workspaceGeneration) return;
    switchWorkspaceJob(job, { invalidate: false });
    studio?.classList.remove("home-mode");
    $("#homeView")?.classList.add("hidden");
  } catch (error) {
    if (generation === workspaceGeneration && !homeNavigationRequested) {
      showToast(`无法打开任务：${error.message || "服务暂时不可用"}`);
    }
  } finally {
    openingHomeTaskId = null;
  }
}

// Use delegation so the initial placeholder card works before /api/jobs
// finishes, and the refreshed card after task loading works identically.
document.addEventListener("click", async (event) => {
  const target = event.target instanceof Element ? event.target : null;
  if (target?.closest("#capabilityDrawerClose, #capabilityDrawerScrim")) {
    event.preventDefault();
    closeCapabilityDrawer();
    return;
  }
  if (target?.closest("[data-open-capability-drawer]")) {
    event.preventDefault();
    openCapabilityDrawer(target.closest("[data-open-capability-drawer]"));
    return;
  }
  if (target?.closest("[data-home-retry]")) {
    event.preventDefault();
    event.stopPropagation();
    loadHomeTasks();
    return;
  }
	  if (target?.closest("[data-home-create]")) {
	    event.preventDefault();
	    event.stopPropagation();
	    openNewTaskFromHome();
	    return;
	  }
	  const card = target?.closest("[data-home-task]");
  if (card && !target.closest("[data-home-delete]")) {
    event.preventDefault();
    event.stopPropagation();
    openHomeTask(card.dataset.homeTask);
  }
});

let staleHomeTasksRefreshTimer = null;

function setHomeThumbnailState(image, state, message = "", retryable = false) {
  const cover = image?.parentElement;
  if (!cover) return;
  const loader = cover.querySelector(".home-cover-loader");
  const retry = cover.querySelector("[data-thumbnail-retry]");
  const loading = state === "pending";
  cover.dataset.thumbnailState = state;
  cover.dataset.thumbnailMessage = message;
  cover.classList.toggle("thumbnail-unavailable", ["failed", "source_missing", "auth_failed", "network_failed"].includes(state));
  image.classList.toggle("ready", state === "ready");
  if (loader) {
    loader.dataset.loaderActive = String(loading);
    loader.classList.toggle("hidden", !loading);
    if (!loading) clearGenerativeLoader(loader);
  }
  if (retry) {
    retry.classList.toggle("hidden", !retryable);
    retry.textContent = state === "failed" ? "重新生成" : "重新检查";
  }
  if (message) cover.setAttribute("title", message);
  else cover.removeAttribute("title");
}

function refreshAfterStaleHomeTask() {
  window.clearTimeout(staleHomeTasksRefreshTimer);
  staleHomeTasksRefreshTimer = window.setTimeout(() => loadHomeTasks(), 300);
}

async function loadHomeTaskThumbnail(image, { forceRetry = false } = {}) {
  const card = image?.closest("[data-home-task]");
  const jobId = card?.dataset.homeTask;
  const url = image?.dataset.thumbnailUrl;
  if (!jobId || !url) return;
  if (image.dataset.thumbnailLoading === "true") return;
  image.dataset.thumbnailLoading = "true";
  const retry = image.parentElement?.querySelector("[data-thumbnail-retry]");
  if (retry) retry.onclick = async (event) => {
    event.preventDefault();
    event.stopPropagation();
    image.dataset.thumbnailLoading = "false";
    try {
      setHomeThumbnailState(image, "pending", "正在重新生成视频封面");
      await api(`/api/jobs/${encodeURIComponent(jobId)}/thumbnail/retry`, { method: "POST" });
      await loadHomeTaskThumbnail(image, { forceRetry: true });
    } catch (error) {
      setHomeThumbnailState(image, error?.code === "thumbnail_source_missing" ? "source_missing" : "failed", error?.message || "封面重新生成失败", error?.code !== "thumbnail_source_missing");
    }
  };

  const initialStatus = forceRetry ? "pending" : String(image.dataset.thumbnailStatus || "pending");
  if (initialStatus === "source_missing") {
    setHomeThumbnailState(image, "source_missing", "源视频已丢失，无法生成封面");
    image.dataset.thumbnailLoading = "false";
    return;
  }
  if (initialStatus === "failed") {
    setHomeThumbnailState(image, "failed", "视频开头没有可解码的非黑画面", true);
    image.dataset.thumbnailLoading = "false";
    return;
  }

  setHomeThumbnailState(image, "pending", "正在查找首个可用画面");
  const delays = [0, 1000, 3000, 10000];
  let lastError = null;
  for (const delay of delays) {
    if (!image.isConnected) break;
    if (delay) await new Promise((resolve) => window.setTimeout(resolve, delay));
    try {
      const blob = await apiBlob(url);
      if (!blob.size || !String(blob.type || "").startsWith("image/")) {
        const invalid = new Error("服务未返回有效的封面图像");
        invalid.code = "thumbnail_invalid_response";
        throw invalid;
      }
      const objectUrl = URL.createObjectURL(blob);
      await new Promise((resolve, reject) => {
        image.onload = resolve;
        image.onerror = () => reject(new Error("封面图像无法显示"));
        image.src = objectUrl;
      });
      URL.revokeObjectURL(objectUrl);
      setHomeThumbnailState(image, "ready");
      image.dataset.thumbnailLoading = "false";
      return;
    } catch (error) {
      lastError = error;
      if (error?.code === "job_not_found") {
        card.remove();
        refreshAfterStaleHomeTask();
        image.dataset.thumbnailLoading = "false";
        return;
      }
      if (!["thumbnail_pending", "thumbnail_temporary_failure", "network_error"].includes(error?.code)) break;
    }
  }

  const code = lastError?.code || "thumbnail_temporary_failure";
  if (code === "thumbnail_source_missing") {
    setHomeThumbnailState(image, "source_missing", "源视频已丢失，无法生成封面");
  } else if (code === "thumbnail_decode_failed") {
    setHomeThumbnailState(image, "failed", lastError?.message || "视频开头没有可用画面", true);
  } else if (lastError?.status === 401) {
    setHomeThumbnailState(image, "auth_failed", "访问令牌无效，无法读取封面", true);
  } else if (code === "network_error") {
    setHomeThumbnailState(image, "network_failed", "网络暂不可用，封面会在重试后恢复", true);
  } else {
    setHomeThumbnailState(image, "failed", lastError?.message || "封面仍在生成，可稍后重新检查", true);
  }
  image.dataset.thumbnailLoading = "false";
}

async function loadHomeTasks() {
  if (window.ClipTalkAppShell?.loadHomeTasks) {
    return window.ClipTalkAppShell.loadHomeTasks();
  }
  const grid = $("#homeTaskGrid");
  if (!grid) return;
  setHomeViewState("loading");
  grid.innerHTML = `${renderHomeCreateCard()}${renderHomeLoadingCard()}`;
  window.UIIconsBridge?.sync(grid);
  try {
    const response = await Promise.race([
      api("/api/jobs"),
      new Promise((_, reject) => window.setTimeout(() => reject(new Error("读取任务列表超时")), 8000)),
    ]);
    const jobs = Array.isArray(response?.jobs) ? response.jobs : [];
    const taskCount = $("#homeTaskCount");
    const assetCount = $("#homeAssetCount");
    const outputCount = $("#homeOutputCount");
    if (taskCount) taskCount.textContent = String(jobs.length);
    const groupedProjects = new Map();
    jobs.forEach((job) => {
      const projectId = String(job.sourceProjectId || `legacy_${job.id}`);
      groupedProjects.set(projectId, [...(groupedProjects.get(projectId) || []), job]);
    });
    if (assetCount) assetCount.textContent = String(groupedProjects.size);
    if (outputCount) outputCount.textContent = String(jobs.reduce((sum, job) => sum + jobOutputCount(job), 0));
    const cards = [...groupedProjects.values()].map((projectJobs) => {
      try { return renderHomeProjectCard(projectJobs); } catch (error) { console.error("源视频项目卡渲染失败", projectJobs?.[0]?.id, error); return ""; }
    }).join("");
    const state = jobs.length ? "ready" : "empty";
    setHomeViewState(state);
    grid.innerHTML = `${renderHomeCreateCard()}${cards}`;
    window.UIIconsBridge?.sync(grid);
    grid.querySelectorAll("[data-task-thumbnail]").forEach((image) => loadHomeTaskThumbnail(image));
    syncGenerativeLoaders(grid);
    grid.querySelectorAll("[data-home-task]").forEach((card) => {
      const open = async () => {
        await openHomeTask(card.dataset.homeTask);
      };
      card.addEventListener("keydown", (event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); open(); } });
    });
    grid.querySelectorAll("[data-home-delete]").forEach((button) => button.addEventListener("click", async (event) => {
      event.preventDefault();
      event.stopPropagation();
      await deleteHistoryJob(button.dataset.homeDelete);
      loadHomeTasks();
    }));
    grid.querySelectorAll("[data-home-project-more]").forEach((button) => button.addEventListener("click", () => {
      const card = button.closest("[data-home-project]");
      const expanded = button.getAttribute("aria-expanded") !== "true";
      button.setAttribute("aria-expanded", String(expanded));
      card?.classList.toggle("tasks-expanded", expanded);
      const label = button.querySelector("span");
      if (label) label.textContent = expanded ? "收起任务" : `查看其余 ${card?.querySelectorAll(".home-project-task-extra").length || 0} 个任务`;
    }));
  } catch (error) {
    const homeGrid = $("#homeTaskGrid");
    setHomeViewState("error");
    if (homeGrid) {
      homeGrid.innerHTML = `${renderHomeCreateCard()}<section class="home-error-card" role="status"><strong>最近任务暂时无法加载</strong><p>你仍然可以创建新任务。</p><small>${escapeHtml(error.message || "服务暂时不可用")}</small><button type="button" data-home-retry>重新加载</button></section>`;
      window.UIIconsBridge?.sync(homeGrid);
    }
  }
}

function resetWorkspace(showHome = true, clearSavedJob = showHome) {
  // Switch the top-level view before any cleanup work.  Cleanup is best-effort
  // because legacy tasks may lack optional DOM/resource data; a missing node
  // must never make the Home button appear inert.
  invalidateWorkspaceRequests();
  currentCreationSessionId = "";
  pendingWorkflowSwitch = "";
  pendingCapabilityCategory = "auto";
  pendingCapabilityOption = "";
  legacyWorkflowPickerOpen = false;
  renderQuickWorkflowPicker(null);
  pendingWorkflowOptions = { scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3 };
  quickWorkflowValidation = "";
  window.ClipTalkAgentWorkspace?.reset?.();
  closeVoiceProfiles();
  closePersonProfiles();
  homeNavigationRequested = Boolean(showHome);
  if (clearSavedJob) forgetCurrentJob();
  if (showHome) {
    studio?.classList.add("home-mode");
    $("#homeView")?.classList.remove("hidden");
    window.ClipTalkAppShell?.showView("home", { route: false });
  } else {
    studio?.classList.remove("home-mode");
    $("#homeView")?.classList.add("hidden");
    window.ClipTalkAppShell?.showView("workspace", { route: false });
  }
  // Clear the conversation before optional media/timeline cleanup. This
  // makes a new task visually empty even when an older task lacks a legacy
  // DOM node and a later cleanup step has to be skipped.
  initialConversation();
  stopSourcePreviewPolling();
  currentJob = null;
  initialOutputJobId = null;
  window.ClipTalkAppShell?.syncCurrentJob?.(null);
  timelineChatSelections = [];
  eventGroupSelectionOrder = [];
  ignoredChatContextKeys = new Set();
  const contextBar = $("#chatContextBar");
  if (contextBar) {
    contextBar.replaceChildren();
    contextBar.classList.add("hidden");
  }
  const selectionBasket = $("#contentSelectionBasket");
  if (selectionBasket) {
    selectionBasket.replaceChildren();
    selectionBasket.classList.add("hidden");
  }
  resetCurrentVoiceState();
  resetCurrentPersonState();
  syncOneOffFinalizeAction(null);
  syncLibrarySaveAction(null, null);
  currentJobRevision = "";
  renderReviewStatus(null);
  renderDirectorTaskSummary(null);
  updateDirectorFlow(null);
  currentOutput = null;
  reviewLayoutMediaIdentity = "";
  currentCandidate = null;
  currentEventGroup = null;
  currentEventSegment = null;
  outputAssemblyMode = "single_reel";
  candidatePreviewEnd = null;
  viewerMediaKind = "source";
  syncOutputPreviewMode();
  waveformJobId = null;
  waveformLoadingJobId = null;
  waveformData = null;
  waveformRetryAt = 0;
  waveformRequestToken += 1;
  timelineAssetsJobId = null;
  timelineAssetsLoadingJobId = null;
  timelineAssets = null;
  timelineAssetsRetryAt = 0;
  timelineTranscript = [];
  transcriptLoadingJobId = null;
  timelineTranscriptJobId = null;
  transcriptRetryAt = 0;
  timelineMediaRenderKey = "";
  waveformRenderKey = "";
  timelineFrameSelectionTime = null;
  browserFallbackAttempts = new Set();
  timelineViewStart = 0;
  timelineViewEnd = 0;
  timelineCoordinateSpace = "source";
  timelineSelectedOutputSegmentIndex = null;
  timelineReviewFollow = false;
  boundaryDrag = null;
  timelineRangeDrag = null;
  cancelTimelinePan(null);
  cancelTimelineOverview(null);
  timelineSpaceHeld = false;
  timelineSpaceDidPan = false;
  timelineViewport?.classList.remove("pan-ready", "panning", "manual-select-mode");
  $("#timelineOverview")?.classList.remove("dragging");
  document.body.classList.remove("timeline-panning");
  pendingTimelineSelection = null;
  pendingTimelineOriginal = null;
  timelineManualSelectMode = false;
  document.removeEventListener("pointermove", moveBoundary);
  document.removeEventListener("pointerup", finishBoundaryDrag);
  document.removeEventListener("pointermove", moveTimelineRange);
  document.removeEventListener("pointerup", finishTimelineRange);
  fragmentDownloadBusy = false;
  clearTimeout(reviewExclusionsPersistTimer);
  reviewExclusionsPersistTimer = null;
  uploadForm?.reset();
  const fileLabel = $("#fileLabel");
  if (fileLabel) fileLabel.textContent = "拖入视频，或点击选择";
  const uploadProgress = $("#uploadProgress");
  if (uploadProgress) {
    uploadProgress.hidden = true;
    uploadProgress.removeAttribute("value");
  }
  $("#dropZone")?.removeAttribute("data-upload-state");
  if (videoInput) videoInput.disabled = false;
  $("#uploadView")?.classList.remove("has-source");
  $("#dropZone")?.classList.remove("has-file");
  $("#localPreviewPanel")?.classList.add("hidden");
  localPreviewPanel?.classList.remove("portrait", "square");
  localPreviewPanel?.style.removeProperty("--media-aspect");
  const localPreviewVideo = $("#localPreviewVideo");
  localPreviewVideo?.removeAttribute("src");
  localPreviewVideo?.load();
  if (localPreviewUrl) URL.revokeObjectURL(localPreviewUrl);
  localPreviewUrl = null;
  $("#uploadView")?.classList.remove("hidden");
  $("#reviewView")?.classList.add("hidden");
  $("#reviewView")?.removeAttribute("data-review-layout");
  setDirectorWorkspaceEmpty(true);
  $("#keepButton")?.classList.add("hidden");
  pendingNewTaskInstruction = "";
  pendingNewTaskInstructionCommitted = false;
  if (chatInput) {
    chatInput.value = "";
    chatInput.disabled = Boolean(showHome);
    chatInput.placeholder = showHome
      ? "请输入指令或补充需求……"
      : "描述想保留的内容，例如：保留产品特写，剪成一分钟竖屏短片……";
  }
  const sendButton = $("#sendButton");
  if (sendButton) sendButton.disabled = true;
  setDirectorState("等待素材");
  updateDirectorThinkingOrb(null);
  mainVideo?.removeAttribute("src");
  mainVideoAutoplayToken += 1;
  mainVideo?.load();
  setPlayerSurfaceState("empty");
  viewerShell?.classList.remove("portrait", "square");
  viewerShell?.style.setProperty("--media-aspect", "1.90476");
  if (viewerShell) viewerShell.dataset.mediaAspect = "16 / 9";
  if (mediaFrame) {
    delete mediaFrame.dataset.fitKey;
    mediaFrame.style.removeProperty("--decoded-media-aspect");
  }
  scheduleMediaFrameFit(true);
  clearPlayerNotice();
  timelinePanel?.classList.add("hidden");
  setReviewLowerPanelMode("collapsed");
  $("#reviewWorkbench")?.classList.add("hidden");
  $("#reviewView")?.classList.remove("workbench-visible");
  $("#timelineManualSelectToggle")?.classList.remove("active");
  $("#timelineManualSelectToggle")?.setAttribute("aria-pressed", "false");
  $("#jobStatus")?.classList.add("hidden");
  $("#autoCompositionDock")?.classList.add("hidden");
  autoCompositionDockJobId = "";
  $(".review-rail")?.classList.remove("pipeline-mode");
  $("#clipSection")?.classList.add("hidden");
  $("#railOutput")?.classList.add("hidden");
  const railTitle = $("#railTitle");
  if (railTitle) railTitle.textContent = "审核结果";
  const railBody = $("#railBody");
  if (railBody) {
    railBody.innerHTML = '<div class="rail-empty"><span class="empty-thinking-orb" data-thinking-orb data-orb-state="shaping" data-orb-size="64" data-orb-theme="light" data-orb-label="等待分析任务"></span><strong>等待分析任务</strong><p>上传素材后，这里会显示事件高光、候选镜头与生成进度。</p></div>';
    syncThinkingOrbs(railBody);
  }
  $("#openCandidateDrawer")?.setAttribute("disabled", "true");
  closeCandidateDrawer();
  updatePlayerChrome();
  initialConversation();
  setDirectorStage("conversation");
  if (showHome) {
    studio?.classList.remove("new-task-workbench");
    $("#uploadView")?.classList.remove("new-task-upload");
    studio?.classList.add("home-mode");
    $("#homeView")?.classList.remove("hidden");
    loadHomeTasks();
  }
  syncWorkspaceState({ home: Boolean(showHome) });
  // Keep the full capability catalogue behind the compact action dock. The
  // selected mode still survives upload, but the conversation stays readable.
  legacyWorkflowPickerOpen = false;
  renderQuickWorkflowPicker(null);
  syncComposerSuggestion(null);
  updateComposerBeam();
}

async function bootstrapAgentDraftFromUpload(file) {
  if (!file || draftBootstrapBusy) return;
  const creationToken = activeCreationToken();
  const stagedInstruction = String(chatInput?.value || pendingNewTaskInstruction || "").trim();
  const stagedInstructionCommitted = Boolean(stagedInstruction && pendingNewTaskInstructionCommitted);
  draftBootstrapBusy = true;
  actionBusy = true;
  $("#uploadView")?.classList.add("is-uploading");
  syncWorkspaceState({ uploading: true, home: false });
  const sessionId = createClientSessionId("draft");
  let acceptedCreation = false;
  const label = $("#fileLabel");
  const uploadMeta = $("#dropZone")?.querySelector("small");
  const uploadProgress = $("#uploadProgress");
  const dropZone = $("#dropZone");
  if (videoInput) videoInput.disabled = true;
  if (dropZone) dropZone.dataset.uploadState = "uploading";
  if (uploadProgress) {
    uploadProgress.hidden = false;
    uploadProgress.removeAttribute("value");
    uploadProgress.setAttribute("aria-valuetext", "正在上传并创建任务");
  }
  if (uploadMeta) {
    uploadMeta.textContent = "正在上传并创建任务，完成后自动进入工作区";
    delete uploadMeta.dataset.uploadError;
  }
  if (label) label.textContent = `${file.name} · 正在上传并建立工作区`;
  try {
    const resumable = file.size >= 16 * 1024 * 1024
      ? await prepareResumableUpload(file, (fraction) => {
          if (!creationTokenStillCurrent(creationToken)) return;
          const percent = Math.round(fraction * 100);
          if (label) label.textContent = `${file.name} · 上传 ${percent}%`;
          if (uploadProgress) {
            uploadProgress.value = percent;
            uploadProgress.setAttribute("aria-valuetext", `已上传 ${percent}%`);
          }
        }, { creationSessionId: creationToken.sessionId }) : null;
    if (!creationTokenStillCurrent(creationToken)) {
      if (resumable) localStorage.removeItem(resumable.storageKey);
      return;
    }
    const form = window.ClipTalkTaskCreation.buildForm({
      file, uploadSessionId: resumable?.id || "", instruction: "", taskMode: "highlight",
      sourceScope: { kind: "all" }, entryWorkflow: "agent", workflowKind: "highlight",
      agentDraft: true, draftSessionId: sessionId,
    });
    const { job } = await api("/api/jobs", { method: "POST", body: form });
    if (resumable) localStorage.removeItem(resumable.storageKey);
    if (!creationTokenStillCurrent(creationToken)) return;
    acceptedCreation = true;
    currentCreationSessionId = "";
    homeNavigationRequested = false;
    renderJob(job);
    if (stagedInstruction && chatInput) {
      pendingNewTaskInstruction = stagedInstruction;
      chatInput.value = stagedInstruction;
      chatInput.dispatchEvent(new Event("input", { bubbles: true }));
    }
    $("#uploadView")?.classList.add("hidden");
    if (stagedInstructionCommitted) {
      pendingNewTaskInstructionCommitted = false;
      window.setTimeout(() => {
        if (String(currentJob?.id || "") !== String(job?.id || "") || actionBusy || !chatInput) return;
        pendingNewTaskInstruction = "";
        submitComposer();
      }, 0);
    }
  } catch (error) {
    if (!creationTokenStillCurrent(creationToken)) return;
    const uploadError = error.message || "视频上传失败，请重试";
    showToast(uploadError, "error");
    if (label) label.textContent = `${file.name} · 上传失败，请重试`;
    if (uploadMeta) {
      uploadMeta.textContent = uploadError;
      uploadMeta.dataset.uploadError = "true";
    }
    if (dropZone) dropZone.dataset.uploadState = "error";
    if (uploadProgress) uploadProgress.hidden = true;
    setDirectorState("上传失败");
  } finally {
    if (acceptedCreation || creationTokenStillCurrent(creationToken) || String(currentJob?.draftSessionId || "") === sessionId) {
      $("#uploadView")?.classList.remove("is-uploading");
      draftBootstrapBusy = false;
      actionBusy = false;
      if (videoInput) videoInput.disabled = false;
      syncComposerSuggestion(currentJob);
      updateComposerBeam();
    }
  }
}

videoInput.addEventListener("change", () => {
  const file = videoInput.files[0];
  $("#fileLabel").textContent = file ? `${file.name} · ${(file.size / 1024 / 1024).toFixed(1)} MB` : "拖入视频，或点击选择";
  if (localPreviewUrl) URL.revokeObjectURL(localPreviewUrl);
  if (file) {
    // A new upload starts with the complete source by default. Do not carry a
    // stale custom range from the previous video into the next task.
    const scopeSelect = $("#briefSourceScope");
    if (scopeSelect) scopeSelect.value = "all";
    localPreviewUrl = URL.createObjectURL(file);
    const localVideo = $("#localPreviewVideo");
    localVideo.src = localPreviewUrl;
    // The browser can decode a local file before the upload is complete, but
    // showing that player here makes the unfinished source look server-ready.
    // Keep it hidden for metadata probing and use one honest upload surface.
    $("#localPreviewPanel")?.classList.add("hidden");
    $("#uploadView")?.classList.remove("has-source");
    $("#dropZone")?.classList.add("has-file");
    setDirectorState("上传中");
    bootstrapAgentDraftFromUpload(file);
    syncWorkspaceState({ home: false });
  } else {
    resetWorkspace();
  }
});

$("#localPreviewVideo").addEventListener("loadedmetadata", (event) => {
  applyMediaAspect(localPreviewPanel, event.currentTarget.videoWidth, event.currentTarget.videoHeight);
  syncBriefSettingsUi();
});
[
  [["dragenter", "dragover"], true],
  [["dragleave", "drop"], false],
].forEach(([names, active]) => names.forEach((name) => $("#dropZone")?.addEventListener(name, () => $("#dropZone")?.classList.toggle("dragging", active))));

async function submitComposer() {
  if (chatInput?.disabled || $("#sendButton")?.disabled) return;
  const draftText = String(chatInput?.value || "").trim();
  const agentWorkspace = Boolean(
    String(currentJob?.request?.entryWorkflow || "") === "agent" || currentJob?.agent?.workspaceId
  );
  if (currentJob && (agentWorkspace || (!pendingWorkflowSwitch && !chatInput?.dataset.timelineCompose)) && !await requireSetupCapability("agent")) return;
  if (!currentJob && videoInput.files.length && !await requireSetupCapability("create")) return;
  if (currentJob && pendingWorkflowSwitch) createSameSourceWorkflow(pendingWorkflowSwitch, chatInput?.value.trim() || "");
  else if (currentJob && agentWorkspace && window.ClipTalkAgentWorkspace?.submitGoal) {
    const selectedCapability = selectedDraftCapability(currentJob);
    window.ClipTalkAgentWorkspace.submitGoal(chatInput?.value.trim() || "", selectedCapability?.option?.skill
      ? { skillId: selectedCapability.option.skill }
      : {});
  }
  else if (currentJob && window.ClipTalkAgentWorkspace?.submitGoal && !chatInput?.dataset.timelineCompose) window.ClipTalkAgentWorkspace.submitGoal(draftText);
  else if (currentJob) sendChat();
  else if (videoInput.files.length) createJobFromBrief();
  else if (draftText) {
    pendingNewTaskInstruction = draftText;
    pendingNewTaskInstructionCommitted = true;
    showPendingNewTaskInstruction(draftText);
    showToast("剪辑要求已暂存，选择视频后会自动继续。", "success");
    videoInput?.click();
  }
}

async function createSameSourceWorkflow(workflowKind, instruction = "") {
  if (!currentJob?.id || actionBusy) return;
  const needsInstruction = workflowKind === "content_search";
  if (needsInstruction && !instruction) {
    quickWorkflowValidation = "请先在输入框描述要查找的内容。";
    renderQuickWorkflowPicker(currentJob);
    showToast("请描述要查找的内容");
    chatInput?.focus();
    return;
  }
  const effectiveInstruction = instruction || ({
    highlight: "自动分析整个源视频并生成高光视频",
    person_edit: "识别画面人物并提取所选人物的所有出镜片段",
    speaker_edit: "识别本视频中的说话人并按发言整理片段",
  })[workflowKind] || "";
  const actionToken = captureJobAction(currentJob);
  actionBusy = true;
  setComposerSending(true);
  if (chatInput) chatInput.disabled = true;
  try {
    const currentScope = currentJob?.request?.sourceScope || {};
    const useCurrentScope = pendingWorkflowOptions.scope === "current"
      && Number(currentScope.end) > Number(currentScope.start);
    const body = {
        workflowKind, instruction: effectiveInstruction,
        sourceScopeKind: useCurrentScope ? "custom" : "all",
        sourceScopeStart: useCurrentScope ? Number(currentScope.start) : null,
        sourceScopeEnd: useCurrentScope ? Number(currentScope.end) : null,
        ...(workflowKind === "highlight" && Number(pendingWorkflowOptions.targetSeconds) >= 4 ? { targetSeconds: Number(pendingWorkflowOptions.targetSeconds) } : {}),
        ...(workflowKind === "highlight" ? { variantCount: Number(pendingWorkflowOptions.variantCount) || 3 } : {}),
        ...(workflowKind === "speaker_edit" && Number(pendingWorkflowOptions.expectedSpeakerCount) > 0 ? { expectedSpeakerCount: Number(pendingWorkflowOptions.expectedSpeakerCount) } : {}),
      };
    const activatingDraft = isAgentInstructionDraft(currentJob);
    const response = activatingDraft
      ? await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/activate`, { method: "POST", body })
      : await apiJson(`/api/jobs/${encodeURIComponent(actionToken.jobId)}/tasks`, { method: "POST", body });
    if (!jobActionStillCurrent(actionToken) || !response.job) return;
    pendingWorkflowSwitch = "";
    pendingCapabilityCategory = "auto";
    pendingCapabilityOption = "";
    legacyWorkflowPickerOpen = false;
    pendingWorkflowOptions = { scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3 };
    quickWorkflowValidation = "";
    if (chatInput) chatInput.value = "";
    if (currentJob?.agentDraft && String(response.job.id || "") === String(actionToken.jobId || "")) {
      renderJob(response.job);
    } else {
      switchWorkspaceJob(response.job);
    }
    if (workflowKind === "speaker_edit") {
      setVoiceProfileStatus("同一视频的说话人任务已创建，可以在对话中开始识别。", "success");
    }
    // Draft activation always hands work to a background worker.  Pull at
    // least once even if an older server briefly returns the pre-queue state;
    // current servers also return execution.active=true immediately.
    if (activatingDraft || jobNeedsPolling(currentJob)) {
      pollJob();
    }
  } catch (error) {
    if (jobActionStillCurrent(actionToken)) showToast(error.message);
  } finally {
    actionBusy = false;
    setComposerSending(false);
    if (chatInput) chatInput.disabled = false;
    updateComposerBeam();
  }
}

document.addEventListener("click", async (event) => {
  const target = event.target instanceof Element ? event.target : null;
  if (target?.closest("[data-open-person-workspace]")) {
    event.preventDefault();
    if (document.body.classList.contains("person-workspace-open")) closePersonProfiles();
    else openPersonProfiles();
    return;
  }
  if (target?.closest("[data-open-speaker-workspace]")) {
    event.preventDefault();
    if (document.body.classList.contains("voice-workspace-open")) closeVoiceProfiles();
    else openVoiceProfiles();
    return;
  }
  if (target?.closest("[data-change-draft-capability]")) {
    event.preventDefault();
    capabilityDrawerReturnFocus = target.closest("[data-change-draft-capability]");
    pendingCapabilityOption = "";
    const skillSelect = $("#agentSkillSelect");
    if (skillSelect) {
      skillSelect.value = "";
      skillSelect.dispatchEvent(new Event("change", { bubbles: true }));
    }
    legacyWorkflowPickerOpen = pendingCapabilityCategory !== "auto";
    renderQuickWorkflowPicker(currentJob);
    renderDirectorTaskSummary(currentJob);
    if (chatInput) {
      chatInput.placeholder = `选择“${BRIEF_CAPABILITY_GROUPS[pendingCapabilityCategory]?.title || "具体功能"}”下的处理方式……`;
      chatInput.focus();
    }
    return;
  }
  if (target?.closest("[data-run-draft-capability]")) {
    event.preventDefault();
    const selected = selectedDraftCapability(currentJob);
    const value = String(chatInput?.value || "").trim();
    if (!selected || !value) {
      showToast(selected?.option.id === "cover" ? "请先描述封面重点或标题文字。" : "请先补充具体处理要求。", "error");
      chatInput?.focus();
      return;
    }
    submitComposer();
    return;
  }
  const categoryButton = target?.closest("[data-capability-category-switch]");
  if (categoryButton) {
    event.preventDefault();
    pendingCapabilityCategory = categoryButton.dataset.capabilityCategorySwitch || "auto";
    pendingCapabilityOption = "";
    pendingWorkflowSwitch = "";
    pendingWorkflowOptions = { scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3 };
    quickWorkflowValidation = "";
    legacyWorkflowPickerOpen = pendingCapabilityCategory !== "auto";
    const skillSelect = $("#agentSkillSelect");
    if (skillSelect) {
      skillSelect.value = "";
      skillSelect.dispatchEvent(new Event("change", { bubbles: true }));
    }
    renderQuickWorkflowPicker(currentJob);
    renderDirectorTaskSummary(currentJob);
    if (chatInput) {
      chatInput.placeholder = pendingCapabilityCategory === "auto"
        ? agentDraftPlaceholder()
        : `选择“${BRIEF_CAPABILITY_GROUPS[pendingCapabilityCategory]?.title || "具体功能"}”下的处理方式……`;
      if (pendingCapabilityCategory === "auto") chatInput.focus();
    }
    renderChatContextBar();
    return;
  }
  const skillButton = target?.closest("[data-draft-capability-skill]");
  if (skillButton) {
    event.preventDefault();
    if (skillButton.disabled) return;
    const skillId = skillButton.dataset.draftCapabilitySkill || "";
    const optionId = skillButton.dataset.draftCapabilityOption || "";
    await window.ClipTalkAgentWorkspace?.loadSkills?.();
    const skillSelect = $("#agentSkillSelect");
    if (!skillSelect || ![...skillSelect.options].some((option) => option.value === skillId)) {
      showToast("这项能力当前不可用，请在设置中检查 Skill 状态。", "error");
      return;
    }
    const option = BRIEF_CAPABILITY_GROUPS[pendingCapabilityCategory]?.options.find((item) => item.id === optionId);
    pendingCapabilityOption = optionId;
    pendingWorkflowSwitch = "";
    skillSelect.value = skillId;
    skillSelect.dispatchEvent(new Event("change", { bubbles: true }));
    legacyWorkflowPickerOpen = false;
    renderQuickWorkflowPicker(currentJob);
    renderDirectorTaskSummary(currentJob);
    const insertedRecommendation = applyDraftCapabilityRecommendation(option);
    if (chatInput) {
      chatInput.placeholder = option?.placeholder || `请补充“${option?.label || "所选功能"}”的具体要求……`;
      chatInput.focus();
    }
    syncComposerSuggestion(currentJob);
    showToast(`已选择“${option?.label || "具体功能"}”${insertedRecommendation ? "，可修改推荐要求后发送。" : "，可继续编辑或添加推荐要求。"}`, "success");
    return;
  }
  const button = target?.closest("[data-workflow-switch]");
  if (!button) return;
  event.preventDefault();
  const workflowKind = button.dataset.workflowSwitch;
  const workflowOption = BRIEF_CAPABILITY_GROUPS[pendingCapabilityCategory]?.options.find((item) => item.workflow === workflowKind);
  pendingCapabilityOption = workflowOption?.id || "";
  button.closest("details")?.removeAttribute("open");
  pendingWorkflowSwitch = workflowKind;
  document.querySelector(".agent-draft-modes")?.classList.add("workflow-choice-made");
  // Keep the two-level picker visible before upload so the selected second-
  // level option remains obvious. After upload, switch to its launch settings.
  legacyWorkflowPickerOpen = Boolean(currentJob);
  const suggestedInstruction = String(chatInput?.value || "").trim()
    || draftCapabilityRecommendation(workflowOption)
    || composerSuggestionText(currentJob);
  pendingWorkflowOptions = {
    scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3,
    instruction: suggestedInstruction,
  };
  quickWorkflowValidation = "";
  const insertedRecommendation = Boolean(!String(chatInput?.value || "").trim() && suggestedInstruction);
  if (chatInput && insertedRecommendation) {
    chatInput.value = suggestedInstruction;
    chatInput.dispatchEvent(new Event("input", { bubbles: true }));
  }
  renderQuickWorkflowPicker(currentJob);
  if (chatInput) {
    chatInput.placeholder = quickWorkflowPlaceholder(workflowKind);
    chatInput.focus();
  }
  renderChatContextBar();
  showToast(!currentJob
    ? `已选择“${workflowLaunchLabel(workflowKind)}”${insertedRecommendation ? "并填入推荐要求，" : "，"}添加视频后会继续使用这个设置。`
    : workflowKind === "content_search"
      ? `${insertedRecommendation ? "已填入推荐要求；修改后发送，" : "输入要求并发送，"}将基于同一视频创建独立任务`
      : `${insertedRecommendation ? "已填入推荐要求；" : ""}确认参数后点击开始，将基于同一视频创建独立任务`, "success");
});

$("#chatLegacyModeButton")?.addEventListener("click", () => {
  if (!isAgentInstructionDraft(currentJob)) return;
  if (pendingWorkflowSwitch) {
    pendingWorkflowSwitch = "";
    document.querySelector(".agent-draft-modes")?.classList.remove("workflow-choice-made");
    pendingWorkflowOptions = { scope: "all", targetSeconds: "", expectedSpeakerCount: 0, variantCount: 3 };
    quickWorkflowValidation = "";
    if (chatInput) chatInput.placeholder = agentDraftPlaceholder();
    legacyWorkflowPickerOpen = true;
    renderQuickWorkflowPicker(currentJob);
    renderChatContextBar();
    return;
  }
  openCapabilityDrawer($("#chatLegacyModeButton"));
  renderChatContextBar();
});

document.addEventListener("keydown", (event) => {
  if (event.key === "Escape" && legacyWorkflowPickerOpen) {
    event.preventDefault();
    closeCapabilityDrawer();
  }
});

function setChatInputDraft(text, { append = false } = {}) {
  if (!chatInput || chatInput.disabled) return;
  const value = String(text || "").trim();
  if (!value) return;
  const current = String(chatInput.value || "").trim();
  chatInput.value = append && current ? `${current}\n${value}` : value;
  chatInput.dispatchEvent(new Event("input", { bubbles: true }));
  chatInput.focus();
  chatInput.setSelectionRange(chatInput.value.length, chatInput.value.length);
}

$("#composerContextButton")?.addEventListener("click", () => {
  const time = Number(mainVideo?.currentTime || timelineAbsoluteTime?.() || 0);
  const kind = viewerMediaKind === "output" ? "当前成片画面" : "当前源视频画面";
  setChatInputDraft(`引用${kind} ${formatTime(time)}：`, { append: true });
});

$("#composerMagicButton")?.addEventListener("click", () => {
  const current = String(chatInput?.value || "").trim();
  const template = current
    ? `请按可执行剪辑任务整理这个需求：${current}`
    : "请根据当前视频自动规划一个可审核的剪辑方案，说明要查找的内容、成片比例、字幕和封面要求。";
  setChatInputDraft(template);
});

$("#composerSuggestion")?.addEventListener("click", () => {
  const text = composerSuggestionText(currentJob);
  if (!text) return;
  setChatInputDraft(text, { append: $("#composerSuggestion")?.dataset.suggestionMode === "append" });
  syncComposerSuggestion(currentJob);
});

$("#chatMessages")?.addEventListener("click", (event) => {
  const trigger = event.target?.closest?.("[data-empty-prompt]");
  if (!trigger) return;
  setChatInputDraft(trigger.dataset.emptyPrompt || trigger.textContent || "");
});

chatForm.addEventListener("submit", (event) => { event.preventDefault(); submitComposer(); });
chatInput.addEventListener("input", () => {
  if (!currentJob) {
    pendingNewTaskInstruction = chatInput.value;
    if (!pendingCreateWorkflowKind) pendingCreateIntentMode = "auto";
    $("#briefIntentClarification")?.classList.add("hidden");
    setBriefCreationError("");
  } else {
    if (pendingWorkflowSwitch) {
      pendingWorkflowOptions.instruction = chatInput.value;
      const launchInput = document.querySelector("[data-workflow-launch-instruction]");
      if (launchInput && launchInput.value !== chatInput.value) launchInput.value = chatInput.value;
    }
    if (pendingWorkflowSwitch === "content_search" && quickWorkflowValidation && chatInput.value.trim()) {
      quickWorkflowValidation = "";
      renderQuickWorkflowPicker(currentJob);
    }
    renderChatContextBar();
  }
  syncComposerSuggestion(currentJob);
  updateComposerBeam();
});
chatInput.addEventListener("focus", updateComposerBeam);
chatInput.addEventListener("blur", updateComposerBeam);
chatInput.addEventListener("keydown", (event) => {
  if (event.key === "Enter" && !event.shiftKey && !event.isComposing && event.keyCode !== 229) { event.preventDefault(); submitComposer(); }
});
mainVideo.addEventListener("timeupdate", () => {
  updateTimelinePlayhead();
  updatePlayerChrome();
  if (candidatePreviewEnd !== null && mainVideo.currentTime >= candidatePreviewEnd) {
    const stopAt = Math.max(0, candidatePreviewEnd);
    candidatePreviewEnd = null;
    mainVideo.pause();
    if (Math.abs(Number(mainVideo.currentTime || 0) - stopAt) > .01) mainVideo.currentTime = stopAt;
    if (autoAdvanceCandidates && currentCandidate && viewerMediaKind === "candidate") {
      const finishedIndex = Number(currentCandidate.index);
      const next = adjacentCandidate(1);
      if (next) window.setTimeout(() => {
        if (Number(currentCandidate?.index) === finishedIndex && viewerMediaKind === "candidate") previewCandidate(Number(next.index));
      }, 180);
    } else if (
      contentPreviewSequence
      && viewerMediaKind === "segment"
      && String(contentPreviewSequence.jobId || "") === String(currentJob?.id || "")
      && String(contentPreviewSequence.matches?.[contentPreviewSequence.position]?.id || "") === String(currentCandidate?.id || "")
    ) {
      const nextPosition = Number(contentPreviewSequence.position || 0) + 1;
      const next = contentPreviewSequence.matches?.[nextPosition];
      if (next) {
        contentPreviewSequence.position = nextPosition;
        window.setTimeout(() => {
          if (
            contentPreviewSequence
            && String(contentPreviewSequence.jobId || "") === String(currentJob?.id || "")
            && Number(contentPreviewSequence.position) === nextPosition
          ) previewContentMatch(next, { preserveSequence: true });
        }, 180);
      } else {
        const completed = contentPreviewSequence.matches.length;
        contentPreviewSequence = null;
        showToast(`已连续播放完 ${completed} 个片段`, "success");
      }
    }
  }
});
mainVideo.addEventListener("loadedmetadata", () => {
  setPlayerSurfaceState("ready");
  applyMediaAspect(viewerShell, mainVideo.videoWidth, mainVideo.videoHeight);
  mainVideo.style.setProperty("--decoded-media-aspect", `${mainVideo.videoWidth} / ${mainVideo.videoHeight}`);
  clearPlayerNotice();
  updateTimeline();
  updatePlayerChrome();
});
mainVideo.addEventListener("error", () => {
  const code = Number(mainVideo.error?.code || 0);
  const messages = {
    1: "视频加载被中止，请重新加载。",
    2: "读取视频数据失败，请检查网络后重试。",
    3: "浏览器暂时无法解码原始视频；兼容播放版本可能仍在生成。",
    4: "浏览器不支持当前视频编码或尺寸；请等待兼容播放代理生成完成。",
  };
  const browserFallback = [3, 4].includes(code) ? browserFallbackForCurrentMedia() : null;
  if (browserFallback && !browserFallbackAttempts.has(browserFallback.key)) {
    browserFallbackAttempts.add(browserFallback.key);
    showPlayerNotice(browserFallback.label, "正在准备兼容播放版本");
    setMainVideoSource(`${browserFallback.url}?v=${encodeURIComponent(currentJob?.updatedAt || "latest")}`, { force: true });
    mainVideo.load();
    return;
  }
  if (currentJob && ["source", "candidate", "segment"].includes(viewerMediaKind) && currentJob.previewReady && sourcePreviewRetryToken < 1) {
    sourcePreviewRetryToken += 1;
    const retryUrl = `${sourcePreviewUrl(currentJob)}&retry=${Date.now()}`;
    showPlayerNotice("正在切换兼容播放代理并重新加载…", "正在恢复视频预览");
    setMainVideoSource(retryUrl, { force: true });
    mainVideo.load();
    return;
  }
  if (currentJob && ["source", "candidate", "segment"].includes(viewerMediaKind) && !currentJob.previewReady) {
    beginSourcePreviewPolling(currentJob, true);
  }
  const proxyHint = currentJob?.previewPreparing ? " 当前正在后台生成兼容播放版本。" : "";
  showPlayerNotice(`${messages[code] || "浏览器返回了未知媒体错误。"}${proxyHint}`);
});
mainVideo.addEventListener("seeking", updateTimelinePlayhead);
mainVideo.addEventListener("play", () => {
  updatePlayerChrome();
  cancelAnimationFrame(timelineFrame);
  const tick = () => {
    updateTimelinePlayhead();
    if (!mainVideo.paused && !mainVideo.ended) timelineFrame = requestAnimationFrame(tick);
  };
  timelineFrame = requestAnimationFrame(tick);
});
mainVideo.addEventListener("pause", () => {
  playbackRequestToken += 1;
  cancelAnimationFrame(timelineFrame);
  updateTimelinePlayhead();
  updatePlayerChrome();
});
mainVideo.addEventListener("ended", updatePlayerChrome);
mainVideo.addEventListener("click", () => mainVideo.paused ? safePlay() : mainVideo.pause());
$("#playerPlay").addEventListener("click", () => mainVideo.paused ? safePlay() : mainVideo.pause());
$("#playerRetry").addEventListener("click", () => {
  const resumeTime = Number(mainVideo.currentTime || 0);
  const source = mainVideo.currentSrc || mainVideo.src || sourcePreviewUrl();
  clearPlayerNotice();
  mainVideo.addEventListener("loadedmetadata", () => {
    mainVideo.currentTime = Math.min(Math.max(0, mainVideo.duration - .05), resumeTime);
    safePlay();
  }, { once: true });
  setMainVideoSource(source, { force: true });
  mainVideo.load();
});
$("#playerPrevious").addEventListener("click", () => jumpToAdjacentMoment(-1));
$("#playerNext").addEventListener("click", () => jumpToAdjacentMoment(1));
$("#playerMute").addEventListener("click", () => { mainVideo.muted = !mainVideo.muted; updatePlayerChrome(); });
$("#playerVolume").addEventListener("input", (event) => {
  mainVideo.volume = Number(event.currentTarget.value);
  mainVideo.muted = mainVideo.volume === 0;
  updatePlayerChrome();
});
$("#playerSeek").addEventListener("input", (event) => {
  if (!Number.isFinite(mainVideo.duration)) return;
  candidatePreviewEnd = null;
  mainVideo.currentTime = Number(event.currentTarget.value) / 1000 * mainVideo.duration;
});
$("#playerRate").addEventListener("click", (event) => {
  const rates = [.75, 1, 1.25, 1.5, 2];
  const current = rates.indexOf(mainVideo.playbackRate);
  mainVideo.playbackRate = rates[(current + 1) % rates.length];
  event.currentTarget.textContent = `${mainVideo.playbackRate.toFixed(mainVideo.playbackRate % 1 ? 2 : 1)}×`;
});
$("#playerFullscreen").addEventListener("click", async () => {
  const viewerShell = $("#viewerShell");
  if (!viewerShell) return;
  try {
    if (document.fullscreenElement) {
      await document.exitFullscreen?.();
    } else {
      await viewerShell.requestFullscreen?.();
    }
  } catch (error) {
    console.warn("无法切换全屏状态", error);
  } finally {
    updatePlayerChrome();
  }
});
document.addEventListener("fullscreenchange", () => {
  updatePlayerChrome();
  scheduleMediaFrameFit(true);
});
document.addEventListener("keydown", (event) => {
  if (event.target.matches("input,textarea,select,button")) return;
  const key = event.key.toLowerCase();
  if (event.code === "Space" && timelinePointerInside) {
    event.preventDefault();
    if (!event.repeat) {
      timelineSpaceHeld = true;
      timelineSpaceDidPan = false;
      timelineViewport?.classList.add("pan-ready");
    }
    return;
  }
  if (event.code === "Space" || key === "k") { event.preventDefault(); $("#playerPlay").click(); return; }
  if (key === "j" || event.key === "ArrowLeft") {
    event.preventDefault();
    mainVideo.currentTime = Math.max(0, (mainVideo.currentTime || 0) - (key === "j" ? 1 : 5));
    updateTimelinePlayhead();
    return;
  }
  if (key === "l" || event.key === "ArrowRight") {
    event.preventDefault();
    mainVideo.currentTime = Math.min(mainVideo.duration || timelineDurationValue(), (mainVideo.currentTime || 0) + (key === "l" ? 1 : 5));
    updateTimelinePlayhead();
    return;
  }
  if (key === "n" || key === "p") {
    event.preventDefault();
    const next = adjacentCandidate(key === "n" ? 1 : -1);
    if (next) previewCandidate(Number(next.index));
    return;
  }
  if (key === "r") {
    event.preventDefault();
    excludeCurrentCandidate();
    return;
  }
  if (key === "a") {
    event.preventDefault();
    autoAdvanceCandidates = !autoAdvanceCandidates;
    $("#timelineHint").textContent = `自动跳转下一段：${autoAdvanceCandidates ? "开" : "关"} · N/P 切换高光 · R 排除 · Enter 确认`;
    return;
  }
  if (event.key === "Enter") {
    event.preventDefault();
    confirmCurrentReviewSelection();
  }
});
document.addEventListener("keyup", (event) => {
  if (event.code !== "Space" || !timelineSpaceHeld) return;
  event.preventDefault();
  const shouldTogglePlayback = !timelineSpaceDidPan && !timelinePanDrag;
  timelineSpaceHeld = false;
  timelineSpaceDidPan = false;
  timelineViewport?.classList.remove("pan-ready");
  if (shouldTogglePlayback) $("#playerPlay")?.click();
});
timelineViewport?.addEventListener("pointerdown", (event) => {
  const trackContent = timelineTrackContent || timelineViewport;
  if (trackContent && event.clientX < trackContent.getBoundingClientRect().left) return;
  const forcePan = event.button === 1 || (event.button === 0 && timelineSpaceHeld);
  if (forcePan) {
    if (timelineCanPan()) beginTimelinePan(event, { allowSeek: false, spaceMode: timelineSpaceHeld });
    return;
  }
  if (event.target.closest(".timeline-handle,.timeline-label,.timeline-clip,.timeline-shot-marker,.timeline-thumbnail,.timeline-selection")) return;
  if (!timelineManualSelectMode && event.button === 0) {
    if (timelineCanPan()) beginTimelinePan(event, { allowSeek: true });
    else beginTimelineRange(event);
    return;
  }
  if (event.button === 0) beginTimelineRange(event);
});
timelineViewport?.addEventListener("pointerenter", () => { timelinePointerInside = true; });
timelineViewport?.addEventListener("pointerleave", () => { timelinePointerInside = false; });
timelineViewport?.addEventListener("auxclick", (event) => { if (event.button === 1) event.preventDefault(); });
timelineViewport?.addEventListener("click", (event) => {
  if (Date.now() >= timelineSuppressClickUntil) return;
  event.preventDefault();
  event.stopImmediatePropagation();
}, true);
$("#timelineManualSelectToggle")?.addEventListener("click", async () => {
  if (!currentJob) return;
  if (timelineOutputAxisActive()) return void showToast("当前版本时间轴仅用于审看；修改请进入精剪");
  if (!["awaiting_confirmation", "awaiting_content_confirmation", "completed"].includes(currentJob.status)) {
    showToast("请等待视频分析完成后再选择时间段");
    return;
  }
  timelineManualSelectMode = !timelineManualSelectMode;
  if (timelineManualSelectMode) setTimelineExpanded(true);
  const button = $("#timelineManualSelectToggle");
  button?.classList.toggle("active", timelineManualSelectMode);
  button?.setAttribute("aria-pressed", String(timelineManualSelectMode));
  timelineViewport?.classList.toggle("manual-select-mode", timelineManualSelectMode);
  $("#timelineHint").textContent = timelineManualSelectMode
    ? "拖动生成选区，按住空格拖动可平移"
    : timelineCanPan() ? "拖动左右移动，单击定位" : "滚轮放大，单击定位";
  if (timelineManualSelectMode) timelineViewport?.scrollIntoView({ behavior: "smooth", block: "nearest" });
});
$("#timelineContentTrim")?.addEventListener("click", () => {
  const state = contentBoundaryEntryState(currentCandidate);
  if (state.disabled) return void showToast(state.reason, "error");
  setTimelineExpanded(true);
  void activateContentBoundaryEntry();
});
$("#timelineExpandToggle")?.addEventListener("click", () => {
  setTimelineExpanded(!timelineExpanded);
});
$("#portraitPrecisionToggle")?.addEventListener("click", () => {
  setTimelineExpanded(!timelineExpanded, { scroll: !timelineExpanded });
});
$("#portraitWorkbenchOpenProperties")?.addEventListener("click", () => {
  window.ClipTalkWorkspaceController?.setRailExpanded?.(true, { persist: true });
  document.querySelector('#ctV4RailTabs [data-ct-v4-rail="properties"]')?.click();
  requestAnimationFrame(() => $("#ctV4PropertiesPanel")?.focus?.({ preventScroll: true }));
});
document.querySelectorAll("[data-review-panel-mode]").forEach((button) => button.addEventListener("click", () => {
  const requested = button.dataset.reviewPanelMode;
  setCandidateRailOpen(false);
  closeCandidateDrawer();
  if (requested === "review") {
    const subjectOpen = document.body.classList.contains("person-workspace-open")
      || document.body.classList.contains("voice-workspace-open");
    if (subjectOpen) {
      closePersonProfiles();
      closeVoiceProfiles();
      setReviewLowerPanelMode("review");
      return;
    }
  }
  setReviewLowerPanelMode(reviewLowerPanelMode === requested ? "collapsed" : requested, { scroll: requested === "timeline" });
}));
$("#reviewPanelSubjectTab")?.addEventListener("click", () => {
  setCandidateRailOpen(false);
  closeCandidateDrawer();
  const kind = $("#reviewPanelSubjectTab")?.dataset.subjectKind;
  const subjectOpen = kind === "person"
    ? document.body.classList.contains("person-workspace-open")
    : kind === "voice" && document.body.classList.contains("voice-workspace-open");
  if (subjectOpen && reviewLowerPanelMode === "review") {
    if (kind === "person") closePersonProfiles();
    else if (kind === "voice") closeVoiceProfiles();
    setReviewLowerPanelMode("collapsed");
    return;
  }
  if (subjectOpen) {
    setReviewLowerPanelMode("review");
    return;
  }
  if (kind === "person") openPersonProfiles();
  else if (kind === "voice") openVoiceProfiles();
});
$("#timelinePrecisionDrawerClose")?.addEventListener("click", closeTimelinePrecisionDrawerFromUser);
document.addEventListener("keydown", (event) => {
  if (event.key === "Escape" && !$("#timelinePrecisionDrawer")?.classList.contains("hidden")) {
    event.preventDefault();
    closeTimelinePrecisionDrawerFromUser();
  }
});
$("#timelineSelection .timeline-handle.start")?.addEventListener("pointerdown", (event) => beginBoundaryDrag(event, "start"));
$("#timelineSelection .timeline-handle.end")?.addEventListener("pointerdown", (event) => beginBoundaryDrag(event, "end"));
$("#timelineSelectionPreview")?.addEventListener("click", () => {
  if (contentBoundaryTimelineEdit?.editor?.isConnected) {
    const edit = contentBoundaryTimelineEdit;
    previewContentMatch(
      { ...edit.match, start: edit.start, end: edit.end, duration: edit.end - edit.start },
      { autoplay: true, loadEvidence: false, searchId: edit.searchId },
    );
    return;
  }
  const item = currentJob?.manualSelection;
  if (!item) return;
  showSource();
  seekSourceTime(Number(item.start));
  candidatePreviewEnd = Number(item.end);
  safePlay();
});
$("#timelineSelectionConfirm")?.addEventListener("click", () => {
  if (contentBoundaryTimelineEdit?.editor?.isConnected) {
    const edit = contentBoundaryTimelineEdit;
    saveContentBoundary(edit.scopedJob, edit.match, edit.editor);
    return;
  }
  confirmPendingTimelineSelection();
});
$("#timelineSelectionCancel")?.addEventListener("click", () => {
  if (contentBoundaryTimelineEdit?.editor?.isConnected) return closeContentBoundaryTimelineEdit();
  cancelPendingTimelineSelection();
});
$("#timelineSelectionStartInput")?.addEventListener("change", (event) => {
  if (contentBoundaryTimelineEdit?.editor?.isConnected) {
    contentBoundaryTimelineEdit.editor.dataset.boundaryStart = String(event.currentTarget.value);
    syncContentBoundaryEditor(contentBoundaryTimelineEdit.editor, contentBoundaryTimelineEdit.scopedJob, contentBoundaryTimelineEdit.match, { seekEdge: "start" });
    return;
  }
  updatePendingTimelineBoundary("start", event.currentTarget.value);
});
$("#timelineSelectionEndInput")?.addEventListener("change", (event) => {
  if (contentBoundaryTimelineEdit?.editor?.isConnected) {
    contentBoundaryTimelineEdit.editor.dataset.boundaryEnd = String(event.currentTarget.value);
    syncContentBoundaryEditor(contentBoundaryTimelineEdit.editor, contentBoundaryTimelineEdit.scopedJob, contentBoundaryTimelineEdit.match, { seekEdge: "end" });
    return;
  }
  updatePendingTimelineBoundary("end", event.currentTarget.value);
});
$("#timelineSelectionCompose")?.addEventListener("click", () => {
  const item = currentJob?.manualSelection;
  if (!item) return;
  const startCompose = async () => {
    if (!await confirmPendingTimelineSelection()) return;
    if (chatInput?.dataset.timelineCompose !== "true") fillChatWithTimelineSelection(currentJob.manualSelection);
    sendChat();
  };
  startCompose();
});
$("#timelineContentToggleSelected")?.addEventListener("click", () => { void toggleContentTimelineSelection(); });
$("#timelineContentMoveEarlier")?.addEventListener("click", () => { void moveContentTimelineSelection(-1); });
$("#timelineContentMoveLater")?.addEventListener("click", () => { void moveContentTimelineSelection(1); });
function scheduleTimelineResizeRender(force = false) {
  clearTimeout(timelineResizeTimer);
  if (timelineResizeFrame !== null) cancelAnimationFrame(timelineResizeFrame);
  timelineResizeFrame = requestAnimationFrame(() => {
    timelineResizeFrame = null;
    scheduleMediaFrameFit();
    // updateTimeline measures horizontal layout from the track content, but
    // vertical layout from the viewport. Comparing both values against the
    // track bounds made their different heights look like a resize forever:
    // each render notified ResizeObserver and rebuilt the timeline again.
    const nextTrackBounds = (timelineTrackContent || timelineViewport)?.getBoundingClientRect();
    const nextViewportBounds = (timelineViewport || timelineTrackContent)?.getBoundingClientRect();
    const nextLayoutWidth = Math.round(nextTrackBounds?.width || 0);
    // Match updateTimeline's minimum logical layout height. Compact viewport
    // CSS may render below 190px; comparing that raw height with the clamped
    // layout baseline would also keep the observer loop alive.
    const nextLayoutHeight = Math.max(190, Math.round(nextViewportBounds?.height || 0));
    if (nextLayoutWidth && (
      Math.abs(nextLayoutWidth - timelineLabelLayoutWidth) > 2
      || Math.abs(nextLayoutHeight - timelineLabelLayoutHeight) > 2
    )) {
      timelineLabelLayoutWidth = nextLayoutWidth;
      timelineLabelLayoutHeight = nextLayoutHeight;
      updateTimeline();
      return;
    }
    drawWaveform(force);
    renderTimelineMediaAssets(force);
  });
}
window.addEventListener("cliptalk:themechange", (event) => {
  const theme = event.detail?.theme === "light" ? "light" : "dark";
  const progress = $("#inlineAnalysisProgress");
  if (progress) {
    progress.dataset.beamTheme = theme;
    updateBorderBeam(progress, { theme });
  }
  waveformRenderKey = "";
  requestAnimationFrame(() => { drawWaveform(true); drawSecondaryEditorWaveforms(); });
});
if (timelineViewport && window.ResizeObserver) {
  const resizeObserver = new ResizeObserver(() => scheduleTimelineResizeRender());
  resizeObserver.observe(timelineViewport);
  if (timelineTrackContent) resizeObserver.observe(timelineTrackContent);
}
$("#timelineZoomIn")?.addEventListener("click", () => zoomTimeline(.5));
$("#timelineZoomOut")?.addEventListener("click", () => zoomTimeline(2));
$("#timelineFit")?.addEventListener("click", () => setTimelineView(0, timelineDurationValue()));
$("#timelineFocusReview")?.addEventListener("click", focusCurrentTimelineReview);
$("#timelineOutputAxis")?.addEventListener("click", () => setTimelineCoordinateSpace("output"));
$("#timelineSourceAxis")?.addEventListener("click", () => setTimelineCoordinateSpace("source"));
$("#timelineLocatePlayhead")?.addEventListener("click", () => {
  const duration = timelineDurationValue();
  if (duration <= 0) return;
  const value = Math.max(0, Math.min(duration, timelineAbsoluteTime()));
  const view = timelineViewRange();
  const span = Math.min(duration, Math.max(30, view.duration));
  setTimelineView(value - span / 2, value + span / 2);
});
$("#timelineWaveMode")?.addEventListener("click", () => {
  timelineVisualMode = "waveform";
  timelineViewport.classList.add("waveform-mode");
  timelineViewport.classList.remove("frame-mode");
  $("#timelineWaveMode")?.classList.add("active");
  $("#timelineFrameMode")?.classList.remove("active");
  drawWaveform();
});
$("#timelineFrameMode")?.addEventListener("click", () => {
  timelineVisualMode = "frames";
  timelineViewport.classList.remove("waveform-mode");
  timelineViewport.classList.add("frame-mode");
  $("#timelineWaveMode")?.classList.remove("active");
  $("#timelineFrameMode")?.classList.add("active");
  drawWaveform(true);
  renderTimelineMediaAssets();
});
$("#timelineCutsToggle")?.addEventListener("click", () => {
  timelineCutsVisible = !timelineCutsVisible;
  saveTimelineLayerPreferences();
  updateTimelineLayerButtons();
  renderTimelineMediaAssets();
});
updateTimelineLayerButtons();
$("#speakerFilter")?.addEventListener("change", (event) => {
  timelineSpeakerFilter = event.target.value || "all";
  updateTimeline();
  if (currentJob) renderConversation(currentJob);
});
$("#timelineOverview")?.addEventListener("pointerdown", beginTimelineOverview);
timelineViewport?.addEventListener("wheel", (event) => {
  const horizontalGesture = Math.abs(event.deltaX) > Math.abs(event.deltaY);
  if (event.shiftKey || horizontalGesture) {
    if (timelineCanPan()) {
      event.preventDefault();
      const view = timelineViewRange();
      const delta = horizontalGesture ? event.deltaX : (event.deltaY || event.deltaX);
      const shift = view.duration * Math.sign(delta) * Math.min(.2, Math.max(.035, Math.abs(delta) / 700));
      setTimelineView(view.start + shift, view.end + shift);
    }
    return;
  }
  event.preventDefault();
  const center = timelineTimeFromPointer(event);
  zoomTimeline(event.deltaY > 0 ? 1.3 : .77, center);
}, { passive: false });
$("#cancelButton")?.addEventListener("click", cancelCurrentJob);
$("#videoViewSelect")?.addEventListener("change", (event) => {
  sourceInspectionContext = null;
  $("#returnInspectedVersion")?.classList.add("hidden");
  if (event.target.value === "source") return showSource({ preserveMappedTime: true });
  selectOutput(event.target.value, true);
});
$("#closeEvidenceButton")?.addEventListener("click", () => {
  if (contentBoundaryTimelineEdit) closeContentBoundaryTimelineEdit({ restorePreview: false });
  renderEvidencePlaceholder();
});
document.querySelectorAll("[data-content-detail-mode]").forEach((button) => button.addEventListener("click", () => {
  const mode = String(button.dataset.contentDetailMode || "evidence");
  if (mode === "evidence") {
    setContentDetailPanelMode("evidence");
    return;
  }
  activateContentBoundaryEntry();
}));
$("#addToChatButton")?.addEventListener("click", () => {
  const item = currentEventSegment || currentCandidate || currentJob?.manualSelection;
  if (item) selectTimelineItemForChat(item);
});
$("#openCandidateDrawer")?.addEventListener("click", openCandidateDrawer);
$("#closeCandidateDrawer")?.addEventListener("click", () => closeCandidateDrawer({ restoreFocus: true }));
$("#drawerBackdrop")?.addEventListener("click", () => closeCandidateDrawer({ restoreFocus: true }));
function setVisionConnectionStatus(message, tone = "neutral") {
  const node = $("#visionConnectionStatus");
  if (!node) return;
  setGenerativeInlineStatus(node, message, tone, "orbit");
}

function updateSettingsSaveState(role, dirty) {
  const vision = role !== "llm";
  if (vision) visionSettingsDirty = Boolean(dirty);
  else llmSettingsDirty = Boolean(dirty);
  const changed = vision ? visionSettingsDirty : llmSettingsDirty;
  const busy = vision ? visionSettingsBusy : llmSettingsBusy;
  const form = $(vision ? "#visionSettingsForm" : "#llmSettingsForm");
  const bar = form?.querySelector(".settings-save-bar");
  const state = $(vision ? "#visionDirtyState" : "#llmDirtyState");
  const discard = $(vision ? "#discardVisionSettings" : "#discardLlmSettings");
  const save = $(vision ? "#saveVisionSettings" : "#saveLlmSettings");
  if (form) form.dataset.dirty = String(changed);
  if (bar) bar.classList.toggle("hidden", !changed && !busy);
  if (state) {
    state.textContent = changed ? "存在未保存修改" : "所有修改已保存";
    state.dataset.tone = changed ? "dirty" : "saved";
  }
  if (discard) discard.disabled = busy || !changed;
  if (save) save.disabled = busy || !changed;
  window.ClipTalkAgentSettings?.renderEffectiveAgent();
}

function markSettingsDirty(role) {
  updateSettingsSaveState(role === "llm" ? "llm" : "vision", true);
}

function selectedVisionProviderRecord() {
  return visionSettingsState?.providers?.find((item) => item.id === selectedVisionProvider) || null;
}

function renderVisionModelOptions(preferredModel = "") {
  const select = $("#visionModelSelect");
  const count = $("#visionModelCount");
  if (!select) return;
  const record = selectedVisionProviderRecord();
  const models = visionDiscoveredModels.length ? visionDiscoveredModels : (record?.models || []);
  const unique = [];
  const seen = new Set();
  models.forEach((item) => {
    const id = String(item?.id || "").trim();
    if (!id || seen.has(id)) return;
    seen.add(id);
    unique.push({
      id,
      owner: String(item?.owner || ""),
      recommended: Boolean(item?.recommended),
      supportsVideo: item?.supportsVideo === true,
      supportsJson: item?.supportsJson,
      status: String(item?.status || ""),
    });
  });
  const currentModel = preferredModel || record?.model || "";
  if (currentModel && !seen.has(currentModel)) unique.unshift({ id: currentModel, owner: "", recommended: true, current: true });
  if (!unique.length) {
    select.innerHTML = '<option value="">验证连接后显示模型</option>';
    select.disabled = true;
    if (count) count.textContent = "等待验证";
    return;
  }
  const recommended = unique.filter((item) => item.recommended);
  const other = unique.filter((item) => !item.recommended);
  const options = (items) => items.map((item) => {
    const capability = item.supportsVideo ? "图像和视频" : "图像";
    const lifecycle = item.status === "Retiring" ? "，即将下线" : "";
    return `<option value="${escapeHtml(item.id)}">${escapeHtml(item.id)}（${capability}${lifecycle}${item.current ? "，当前" : ""}）</option>`;
  }).join("");
  select.innerHTML = `${recommended.length ? `<optgroup label="可用视觉模型">${options(recommended)}</optgroup>` : ""}${other.length ? `<optgroup label="需要注意">${options(other)}</optgroup>` : ""}`;
  select.disabled = false;
  select.value = unique.some((item) => item.id === currentModel) ? currentModel : (recommended[0]?.id || unique[0].id);
  if (count) count.textContent = `${unique.length} 个账号可见模型`;
}

function renderVisionProvider(providerId, { resetDiscovery = true } = {}) {
  const providers = visionSettingsState?.providers || [];
  const record = providers.find((item) => item.id === providerId) || providers[0];
  if (!record) return;
  selectedVisionProvider = record.id;
  if (resetDiscovery) {
    visionDiscoveredModels = [];
    visionVerifiedAt = null;
  }
  $("#visionProviderList")?.querySelectorAll("[data-vision-provider]").forEach((button) => {
    const active = button.dataset.visionProvider === record.id;
    button.classList.toggle("active", active);
    button.setAttribute("aria-selected", String(active));
  });
  const apiKey = $("#visionApiKey");
  if (apiKey) {
    apiKey.value = "";
    apiKey.placeholder = record.keyConfigured ? `已保存 ${record.keyHint}` : "粘贴 API Key";
  }
  const keyHint = $("#visionKeyHint");
  if (keyHint) keyHint.textContent = record.keyConfigured
    ? `已配置 ${record.keyHint}，留空表示继续使用。完整密钥不会回显。`
    : "密钥保存在当前服务实例中，界面不会回显完整内容。";
  const baseUrl = $("#visionBaseUrl");
  if (baseUrl) {
    baseUrl.value = record.baseUrl || "";
    baseUrl.readOnly = !record.baseUrlEditable;
  }
  $("#visionBaseUrlField")?.classList.toggle("fixed", !record.baseUrlEditable);
  const thinking = $("#visionThinkingType");
  if (thinking) {
    thinking.value = record.thinkingType || "";
    thinking.disabled = !record.thinkingSupported;
  }
  const responseFormat = $("#visionResponseFormat");
  if (responseFormat) responseFormat.value = record.responseFormat === "none" ? "none" : "json_object";
  $("#visionManualModelField")?.classList.add("hidden");
  $("#visionModelManual").value = "";
  renderVisionModelOptions(record.model || "");
  setVisionConnectionStatus(record.verifiedAt
    ? `连接已验证，上次验证于 ${new Date(record.verifiedAt).toLocaleString("zh-CN")}`
    : record.keyConfigured ? "密钥已配置，可重新验证并刷新模型列表。" : "填写密钥后验证连接。",
  record.verifiedAt ? "success" : "neutral");
  const summary = $("#visionActiveSummary");
  if (summary) summary.textContent = record.active && record.configured ? `${record.name} / ${record.model}` : "选择服务并完成连接";
}

function renderVisionSettings(state) {
  visionSettingsState = state;
  const list = $("#visionProviderList");
  if (!list) return;
  list.innerHTML = (state.providers || []).map((item) => `<button type="button" role="tab" data-vision-provider="${escapeHtml(item.id)}" data-configured="${String(Boolean(item.configured))}"><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(item.description)}</small><span>${item.configured ? "已配置" : "未配置"}</span></button>`).join("");
  list.querySelectorAll("[data-vision-provider]").forEach((button) => button.addEventListener("click", () => {
    renderVisionProvider(button.dataset.visionProvider);
    markSettingsDirty("vision");
  }));
  renderVisionProvider(state.activeProvider || state.providers?.[0]?.id);
  updateSettingsSaveState("vision", false);
}

async function loadVisionSettings() {
  setVisionConnectionStatus("正在读取模型配置。", "loading");
  try {
    renderVisionSettings(await api("/api/settings/vision"));
  } catch (error) {
    setVisionConnectionStatus(error.message, "error");
  }
}

function setVisionSettingsBusy(busy) {
  visionSettingsBusy = busy;
  const discover = $("#discoverVisionModels");
  if (discover) discover.disabled = busy;
  updateSettingsSaveState("vision", visionSettingsDirty);
}

function modelConnectionSignature(role) {
  return JSON.stringify([role === "llm" ? selectedLlmProvider : selectedVisionProvider,
    role === "llm" ? selectedLlmMode : "vision",
    $(`#${role}ApiKey`)?.value.trim(), $(`#${role}BaseUrl`)?.value.trim()]);
}

async function discoverAvailableVisionModels() {
  if (visionSettingsBusy) return;
  const record = selectedVisionProviderRecord();
  if (!record) return;
  const apiKey = $("#visionApiKey")?.value.trim() || "";
  const baseUrl = $("#visionBaseUrl")?.value.trim() || "";
  if (!apiKey && !record.keyConfigured) return setVisionConnectionStatus("请先填写 API Key。", "error");
  if (!baseUrl) return setVisionConnectionStatus("请先填写接口地址。", "error");
  const signature = modelConnectionSignature("vision");
  setVisionSettingsBusy(true);
  setVisionConnectionStatus("正在验证密钥并读取模型列表。", "loading");
  const button = $("#discoverVisionModels");
  if (button) button.textContent = "正在连接";
  try {
    const result = await apiJson("/api/settings/vision/discover", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: { provider: record.id, apiKey, baseUrl },
    });
    if (modelConnectionSignature("vision") !== signature) return setVisionConnectionStatus("连接配置已变化，请重新读取当前配置的模型列表。", "neutral");
    visionDiscoveredModels = result.models || [];
    visionVerifiedAt = result.verifiedAt || null;
    renderVisionModelOptions(record.model || "");
    markSettingsDirty("vision");
    const recommended = visionDiscoveredModels.filter((item) => item.recommended).length;
    setVisionConnectionStatus(`连接成功，读取到 ${visionDiscoveredModels.length} 个账号可见模型，其中 ${recommended} 个优先列为视觉候选；尚未测试实际视觉能力。`, "success");
  } catch (error) {
    if (modelConnectionSignature("vision") !== signature) return setVisionConnectionStatus("连接配置已变化，请重新读取当前配置的模型列表。", "neutral");
    visionDiscoveredModels = [];
    visionVerifiedAt = null;
    setVisionConnectionStatus(error.message, "error");
  } finally {
    setVisionSettingsBusy(false);
    if (button) button.textContent = "验证连接并读取列表";
  }
}

async function saveVisionConfiguration(event) {
  event.preventDefault();
  if (visionSettingsBusy) return;
  const record = selectedVisionProviderRecord();
  if (!record) return;
  const apiKey = $("#visionApiKey")?.value.trim() || "";
  const manualVisible = !$("#visionManualModelField")?.classList.contains("hidden");
  const model = manualVisible ? $("#visionModelManual")?.value.trim() : $("#visionModelSelect")?.value.trim();
  const baseUrl = $("#visionBaseUrl")?.value.trim() || "";
  if (apiKey && !visionVerifiedAt && !manualVisible) return setVisionConnectionStatus("新密钥需要先验证连接，或改为手动填写模型 ID。", "error");
  if (!apiKey && !record.keyConfigured) return setVisionConnectionStatus("请填写并验证 API Key。", "error");
  if (!model) return setVisionConnectionStatus("请选择或填写视觉模型。", "error");
  setVisionSettingsBusy(true);
  setVisionConnectionStatus("正在保存配置。", "loading");
  try {
    const state = await apiJson("/api/settings/vision", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: {
        provider: record.id,
        apiKey,
        model,
        baseUrl,
        thinkingType: $("#visionThinkingType")?.value || "",
        responseFormat: $("#visionResponseFormat")?.value || "json_object",
        models: visionDiscoveredModels.length ? visionDiscoveredModels : (record.models || []),
        verifiedAt: visionVerifiedAt || record.verifiedAt || null,
      },
    });
    renderVisionSettings(state);
    setVisionConnectionStatus("配置已保存，新建任务会使用这个视觉模型。", "success");
    showToast("视觉模型配置已保存", "success");
    loadHealth();
  } catch (error) {
    setVisionConnectionStatus(error.message, "error");
  } finally {
    setVisionSettingsBusy(false);
  }
}

function setLlmConnectionStatus(message, tone = "neutral") {
  const node = $("#llmConnectionStatus");
  if (!node) return;
  setGenerativeInlineStatus(node, message, tone, "orbit");
}

function selectedLlmProviderRecord() {
  return llmSettingsState?.providers?.find((item) => item.id === selectedLlmProvider) || null;
}

function renderLlmModelOptions(preferredModel = "") {
  const select = $("#llmModelSelect");
  const count = $("#llmModelCount");
  if (!select) return;
  const record = selectedLlmProviderRecord();
  const models = llmDiscoveredModels.length ? llmDiscoveredModels : (record?.models || []);
  const unique = [];
  const seen = new Set();
  models.forEach((item) => {
    const id = String(item?.id || "").trim();
    if (!id || seen.has(id)) return;
    seen.add(id);
    unique.push({
      id,
      owner: String(item?.owner || ""),
      recommended: Boolean(item?.recommended),
      supportsJson: item?.supportsJson,
      status: String(item?.status || ""),
    });
  });
  const currentModel = preferredModel || record?.model || "";
  if (currentModel && !seen.has(currentModel)) unique.unshift({ id: currentModel, owner: "", recommended: true, current: true });
  if (!unique.length) {
    select.innerHTML = '<option value="">验证连接后显示模型</option>';
    select.disabled = true;
    if (count) count.textContent = "等待验证";
    return;
  }
  const recommended = unique.filter((item) => item.recommended);
  const other = unique.filter((item) => !item.recommended);
  const options = (items) => items.map((item) => {
    const json = item.supportsJson === true ? "，支持 JSON" : "";
    const lifecycle = item.status === "Retiring" ? "，即将下线" : "";
    return `<option value="${escapeHtml(item.id)}">${escapeHtml(item.id)}（文本${json}${lifecycle}${item.current ? "，当前" : ""}）</option>`;
  }).join("");
  select.innerHTML = `${recommended.length ? `<optgroup label="推荐用于剪辑规划">${options(recommended)}</optgroup>` : ""}${other.length ? `<optgroup label="其他文本模型">${options(other)}</optgroup>` : ""}`;
  select.disabled = false;
  select.value = unique.some((item) => item.id === currentModel) ? currentModel : (recommended[0]?.id || unique[0].id);
  if (count) count.textContent = `${unique.length} 个账号可见模型`;
}

function renderLlmProvider(providerId, { resetDiscovery = true } = {}) {
  const providers = llmSettingsState?.providers || [];
  const record = providers.find((item) => item.id === providerId) || providers[0];
  if (!record) return;
  selectedLlmProvider = record.id;
  if (resetDiscovery) {
    llmDiscoveredModels = [];
    llmVerifiedAt = null;
  }
  $("#llmProviderList")?.querySelectorAll("[data-llm-provider]").forEach((button) => {
    const active = button.dataset.llmProvider === record.id;
    button.classList.toggle("active", active);
    button.setAttribute("aria-selected", String(active));
  });
  const apiKey = $("#llmApiKey");
  if (apiKey) {
    apiKey.value = "";
    apiKey.placeholder = record.keyConfigured ? `已保存 ${record.keyHint}` : "粘贴 API Key";
  }
  const keyHint = $("#llmKeyHint");
  if (keyHint) keyHint.textContent = record.keyConfigured
    ? `已配置 ${record.keyHint}，留空表示继续使用。完整密钥不会回显。`
    : "密钥保存在当前服务实例中，界面不会回显完整内容。";
  const baseUrl = $("#llmBaseUrl");
  if (baseUrl) {
    baseUrl.value = record.baseUrl || "";
    baseUrl.readOnly = !record.baseUrlEditable;
  }
  $("#llmBaseUrlField")?.classList.toggle("fixed", !record.baseUrlEditable);
  const help = $("#llmBaseUrlHelp");
  if (help) help.textContent = record.protocol === "anthropic" ? "兼容接口填写 Messages API 的服务根地址。" : "兼容接口填写 Chat Completions 的服务根地址。";
  const thinking = $("#llmThinkingType");
  if (thinking) {
    thinking.value = record.thinkingType || "";
    thinking.disabled = !record.thinkingSupported;
  }
  const responseFormat = $("#llmResponseFormat");
  if (responseFormat) {
    responseFormat.value = record.responseFormat === "none" ? "none" : "json_object";
    responseFormat.disabled = record.protocol === "anthropic";
  }
  $("#llmManualModelField")?.classList.add("hidden");
  const manual = $("#llmModelManual");
  if (manual) manual.value = "";
  const manualToggle = $("#toggleManualLlmModel");
  if (manualToggle) manualToggle.textContent = "找不到模型？手动填写模型 ID";
  renderLlmModelOptions(record.model || "");
  setLlmConnectionStatus(record.verifiedAt
    ? `连接已验证，上次验证于 ${new Date(record.verifiedAt).toLocaleString("zh-CN")}`
    : record.keyConfigured ? "密钥已配置，可重新验证并刷新模型列表。" : "填写密钥后验证连接。",
  record.verifiedAt ? "success" : "neutral");
}

function setLlmMode(mode) {
  selectedLlmMode = mode === "independent" ? "independent" : "reuse_vision";
  document.querySelectorAll("[data-llm-mode]").forEach((button) => {
    const active = button.dataset.llmMode === selectedLlmMode;
    button.classList.toggle("active", active);
    button.setAttribute("aria-checked", String(active));
  });
  $("#llmIndependentSettings")?.classList.toggle("hidden", selectedLlmMode !== "independent");
  $("#llmReuseNotice")?.classList.toggle("hidden", selectedLlmMode !== "reuse_vision");
  const summary = $("#llmActiveSummary");
  const record = selectedLlmProviderRecord();
  if (summary) summary.textContent = selectedLlmMode === "reuse_vision"
    ? "跟随视觉分析模型"
    : record?.configured ? `${record.name} / ${record.model}` : "选择服务并完成连接";
}

function renderLlmSettings(state) {
  llmSettingsState = state;
  const list = $("#llmProviderList");
  if (!list) return;
  list.innerHTML = (state.providers || []).map((item) => `<button type="button" role="tab" data-llm-provider="${escapeHtml(item.id)}" data-configured="${String(Boolean(item.configured))}"><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(item.description)}</small><span>${item.configured ? "已配置" : "未配置"}</span></button>`).join("");
  list.querySelectorAll("[data-llm-provider]").forEach((button) => button.addEventListener("click", () => {
    renderLlmProvider(button.dataset.llmProvider);
    markSettingsDirty("llm");
  }));
  renderLlmProvider(state.activeProvider || state.providers?.[0]?.id);
  setLlmMode(state.reuseVision ? "reuse_vision" : "independent");
  updateSettingsSaveState("llm", false);
}

async function loadLlmSettings() {
  setLlmConnectionStatus("正在读取模型配置。", "loading");
  try {
    renderLlmSettings(await api("/api/settings/llm"));
  } catch (error) {
    setLlmConnectionStatus(error.message, "error");
  }
}

function setLlmSettingsBusy(busy) {
  llmSettingsBusy = busy;
  const discover = $("#discoverLlmModels");
  if (discover) discover.disabled = busy;
  updateSettingsSaveState("llm", llmSettingsDirty);
}

async function discoverAvailableLlmModels() {
  if (llmSettingsBusy || selectedLlmMode !== "independent") return;
  const record = selectedLlmProviderRecord();
  if (!record) return;
  const apiKey = $("#llmApiKey")?.value.trim() || "";
  const baseUrl = $("#llmBaseUrl")?.value.trim() || "";
  if (!apiKey && !record.keyConfigured) return setLlmConnectionStatus("请先填写 API Key。", "error");
  if (!baseUrl) return setLlmConnectionStatus("请先填写接口地址。", "error");
  const signature = modelConnectionSignature("llm");
  setLlmSettingsBusy(true);
  setLlmConnectionStatus("正在验证密钥并读取文本模型。", "loading");
  const button = $("#discoverLlmModels");
  if (button) button.textContent = "正在连接";
  try {
    const result = await apiJson("/api/settings/llm/discover", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: { provider: record.id, apiKey, baseUrl },
    });
    if (modelConnectionSignature("llm") !== signature) return setLlmConnectionStatus("连接配置已变化，请重新读取当前配置的模型列表。", "neutral");
    llmDiscoveredModels = result.models || [];
    llmVerifiedAt = result.verifiedAt || null;
    renderLlmModelOptions(record.model || "");
    markSettingsDirty("llm");
    setLlmConnectionStatus(`连接成功，读取到 ${llmDiscoveredModels.length} 个账号可见文本模型；尚未测试实际规划能力。`, "success");
  } catch (error) {
    if (modelConnectionSignature("llm") !== signature) return setLlmConnectionStatus("连接配置已变化，请重新读取当前配置的模型列表。", "neutral");
    llmDiscoveredModels = [];
    llmVerifiedAt = null;
    setLlmConnectionStatus(error.message, "error");
  } finally {
    setLlmSettingsBusy(false);
    if (button) button.textContent = "验证连接并读取列表";
  }
}

async function saveLlmConfiguration(event) {
  event.preventDefault();
  if (llmSettingsBusy) return;
  if (selectedLlmMode === "reuse_vision") {
    setLlmSettingsBusy(true);
    try {
      renderLlmSettings(await apiJson("/api/settings/llm", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: { reuseVision: true },
      }));
      showToast("剪辑规划将复用视觉模型配置", "success");
      loadHealth();
    } catch (error) {
      showToast(error.message);
    } finally {
      setLlmSettingsBusy(false);
    }
    return;
  }
  const record = selectedLlmProviderRecord();
  if (!record) return;
  const apiKey = $("#llmApiKey")?.value.trim() || "";
  const manualVisible = !$("#llmManualModelField")?.classList.contains("hidden");
  const model = manualVisible ? $("#llmModelManual")?.value.trim() : $("#llmModelSelect")?.value.trim();
  const baseUrl = $("#llmBaseUrl")?.value.trim() || "";
  if (apiKey && !llmVerifiedAt && !manualVisible) return setLlmConnectionStatus("新密钥需要先验证连接，或改为手动填写模型 ID。", "error");
  if (!apiKey && !record.keyConfigured) return setLlmConnectionStatus("请填写并验证 API Key。", "error");
  if (!model) return setLlmConnectionStatus("请选择或填写剪辑规划模型。", "error");
  setLlmSettingsBusy(true);
  setLlmConnectionStatus("正在保存配置。", "loading");
  try {
    const state = await apiJson("/api/settings/llm", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: {
        reuseVision: false,
        provider: record.id,
        apiKey,
        model,
        baseUrl,
        thinkingType: $("#llmThinkingType")?.value || "",
        responseFormat: $("#llmResponseFormat")?.value || "none",
        models: llmDiscoveredModels.length ? llmDiscoveredModels : (record.models || []),
        verifiedAt: llmVerifiedAt || record.verifiedAt || null,
      },
    });
    renderLlmSettings(state);
    setLlmConnectionStatus("配置已保存，新建任务会使用这个剪辑规划模型。", "success");
    showToast("剪辑规划模型配置已保存", "success");
    loadHealth();
  } catch (error) {
    setLlmConnectionStatus(error.message, "error");
  } finally {
    setLlmSettingsBusy(false);
  }
}

const modelSettingsScrollPositions = new Map();
let activeModelSettingsRole = "vision";

function setModelSettingsRole(role) {
  const selected = ["vision", "llm", "agent", "system"].includes(role) ? role : "vision";
  const panel = $("#settingsPanel");
  const previous = activeModelSettingsRole;
  if (panel && previous !== selected) modelSettingsScrollPositions.set(previous, panel.scrollTop);
  document.querySelectorAll("[data-model-role]").forEach((button) => {
    const active = button.dataset.modelRole === selected;
    button.classList.toggle("active", active);
    button.setAttribute("aria-selected", String(active));
  });
  document.querySelectorAll("[data-model-view]").forEach((view) => view.classList.toggle("hidden", view.dataset.modelView !== selected));
  activeModelSettingsRole = selected;
  if (panel && previous !== selected) {
    requestAnimationFrame(() => {
      if (activeModelSettingsRole === selected) panel.scrollTop = modelSettingsScrollPositions.get(selected) || 0;
    });
  }
}

function discardVisionSettingsChanges() {
  if (!visionSettingsState) return;
  renderVisionSettings(visionSettingsState);
  showToast("已放弃视觉分析配置的未保存修改", "neutral");
}

function discardLlmSettingsChanges() {
  if (!llmSettingsState) return;
  renderLlmSettings(llmSettingsState);
  showToast("已放弃剪辑规划配置的未保存修改", "neutral");
}

function openSettings() {
  $("#settingsPanel")?.classList.remove("hidden");
  $("#settingsBackdrop")?.classList.add("hidden");
  document.body.classList.add("settings-open");
  if (window.ClipTalkAppShell?.showView) {
    window.ClipTalkAppShell.showView("settings", { route: false });
  } else {
    loadVisionSettings();
    loadLlmSettings();
  }
}

let setupReadinessState = null;
let setupReadinessRequest = null;

function renderSetupReadiness(state) {
  setupReadinessState = state;
  window.ClipTalkSetupStatus = state;
  const root = $("#setupReadiness");
  if (!root) return;
  const title = $("#setupReadinessTitle");
  const summary = $("#setupReadinessSummary");
  if (title) title.textContent = state.complete ? "剪辑能力已就绪" : "完成以下设置后开始剪辑";
  if (summary) summary.textContent = state.complete
    ? "运行环境和对话剪辑能力已验证。"
    : state.canCreateTask ? "基础剪辑已可用；完成对话模型验证后可使用 AI 助手。" : "仍有必需配置未完成。";
  const steps = $("#setupReadinessSteps");
  if (steps) steps.innerHTML = (state.steps || []).map((item) => `
    <li data-status="${escapeHtml(item.status || "required")}">
      <button type="button" data-setup-action="${escapeHtml(item.action || "runtime")}">
        <b>${escapeHtml(item.title || "待检查")}</b><small>${escapeHtml(item.detail || "")}</small>
      </button>
    </li>`).join("");
  window.dispatchEvent(new window.CustomEvent("cliptalk:setup-status"));
}

function loadSetupReadiness(options = {}) {
  if (!setupReadinessRequest) setupReadinessRequest = checkSetupReadiness(options).finally(() => { setupReadinessRequest = null; });
  return setupReadinessRequest;
}

async function checkSetupReadiness({ autoOpen = false } = {}) {
  try {
    const state = await api("/api/setup/status");
    if (!Array.isArray(state?.steps) || typeof state.canCreateTask !== "boolean" || typeof state.canUseAgent !== "boolean") {
      throw new Error("部署状态响应无效");
    }
    renderSetupReadiness(state);
    if (autoOpen && !state.complete && !sessionStorage.getItem("cliptalk-setup-readiness-shown")) {
      sessionStorage.setItem("cliptalk-setup-readiness-shown", "1");
      openSettings();
    }
    return state;
  } catch (error) {
    setupReadinessState = null;
    window.ClipTalkSetupStatus = null;
    window.ClipTalkAgentSettings?.renderEffectiveAgent();
    const title = $("#setupReadinessTitle");
    if (title) title.textContent = error.status === 404 ? "服务需更新" : "能力检查失败";
    if ($("#setupReadinessSummary")) $("#setupReadinessSummary").textContent = error.status === 404
      ? "当前后端不支持新版界面，请更新并重启服务。已有任务仍可查看。"
      : "请检查服务连接后重试。检查通过前不会开始新的 AI 操作。";
    $("#setupReadinessSteps")?.replaceChildren();
    return null;
  }
}

async function requireSetupCapability(capability) {
  const generation = workspaceGeneration;
  const jobId = currentJob?.id;
  if (!setupReadinessState) await loadSetupReadiness();
  if (generation !== workspaceGeneration || jobId !== currentJob?.id) return false;
  if (!setupReadinessState) {
    openSettings();
    showToast("暂时无法确认剪辑能力，请重新检查服务状态。", "error");
    return false;
  }
  const allowed = capability === "agent" ? setupReadinessState.canUseAgent : setupReadinessState.canCreateTask;
  if (allowed) return true;
  openSettings();
  const targetRole = capability === "agent" ? "agent" :
    (setupReadinessState.steps || []).find((item) => ["vision", "planner"].includes(item.id) && item.status !== "ready")?.action || "vision";
  if (["vision", "llm", "agent", "system"].includes(targetRole)) setModelSettingsRole(targetRole);
  showToast(capability === "agent" ? "请先验证 Agent 模型的 Tool Calling 能力" : "请先完成视觉分析和剪辑规划配置");
  return false;
}

window.refreshSetupReadiness = loadSetupReadiness;

function closeSettings() {
  $("#settingsPanel")?.classList.add("hidden");
  $("#settingsBackdrop")?.classList.add("hidden");
  document.body.classList.remove("settings-open");
  window.ClipTalkAppShell?.returnFromPage();
}

function currentPersonCatalog(job = currentJob) {
  return Array.isArray(job?.contentIndex?.persons) ? job.contentIndex.persons : [];
}

function currentPersonTargetState(job = currentJob) {
  const target = job?.contentSearchPersonTarget || job?.request?.contentSearchPersonTarget || {};
  const legacyPersonId = String(job?.contentSearchTargetPersonId || job?.request?.contentSearchTargetPersonId || "");
  return {
    personIds: Array.isArray(target.personIds) && target.personIds.length
      ? target.personIds.map(String).filter(Boolean)
      : legacyPersonId ? [legacyPersonId] : [],
    matchMode: ["any", "all"].includes(String(target.matchMode || "")) ? String(target.matchMode) : "any",
    activity: String(target.activity || "") === "speaking" ? "speaking" : "appearance",
  };
}

function resetCurrentPersonState(jobId = "") {
  speakerConfirmationBusy = false;
  currentPersonJobId = String(jobId || "");
  focusedCurrentPersonRef = "";
  selectedCurrentPersons = new Set();
  selectedPersonCorrectionPersons = new Set();
  selectedCurrentPersonRanges = new Set();
  selectedCurrentPersonRangeOwner = "";
  currentPersonMatchMode = "any";
  currentPersonActivity = "appearance";
  currentPersonRangeLimits.clear();
}

function ensureCurrentPersonState(job = currentJob) {
  const jobId = String(job?.id || "");
  if (currentPersonJobId === jobId) return;
  resetCurrentPersonState(jobId);
  const target = currentPersonTargetState(job);
  selectedCurrentPersons = new Set(target.personIds);
  currentPersonMatchMode = target.matchMode;
  currentPersonActivity = target.activity;
}

function setPersonProfileStatus(message = "", tone = "") {
  const node = $("#personProfileStatus");
  if (!node) return;
  node.textContent = message;
  node.dataset.tone = tone;
}

function personAppearanceSeconds(person) {
  return (person?.ranges || []).reduce((sum, range) => sum + Math.max(0, Number(range.end) - Number(range.start)), 0);
}

function validPersonRanges(person) {
  return (person?.ranges || [])
    .map((range) => ({ ...range, start: Number(range?.start), end: Number(range?.end) }))
    .filter((range) => Number.isFinite(range.start) && Number.isFinite(range.end) && range.end > range.start)
    .sort((left, right) => left.start - right.start);
}

function representativePersonRange(person) {
  const ranges = validPersonRanges(person);
  const representativeTime = Number(person?.representativeTime);
  if (Number.isFinite(representativeTime)) {
    const containingRange = ranges.find((range) => representativeTime >= range.start && representativeTime <= range.end);
    if (containingRange) return containingRange;
  }
  return ranges[0] || null;
}

function focusCurrentPerson(personId, { scroll = true } = {}) {
  focusedCurrentPersonRef = String(personId || "");
  document.querySelectorAll("[data-current-person], [data-timeline-person-label-ref], [data-timeline-person-ref]").forEach((node) => {
    const ref = node.dataset.currentPerson || node.dataset.timelinePersonLabelRef || node.dataset.timelinePersonRef;
    node.classList.toggle("focused", ref === focusedCurrentPersonRef);
  });
  if (scroll && !$("#personProfilePanel")?.classList.contains("hidden")) {
    document.querySelector(`[data-current-person="${CSS.escape(focusedCurrentPersonRef)}"]`)?.scrollIntoView({ behavior: "smooth", block: "nearest" });
  }
}

function previewCurrentPerson(person, start = null, end = null) {
  if (!person) return;
  const hasStart = start !== null && start !== undefined && String(start).trim() !== "" && Number.isFinite(Number(start));
  const hasEnd = end !== null && end !== undefined && String(end).trim() !== "" && Number.isFinite(Number(end));
  const defaultRange = representativePersonRange(person);
  const representativeTime = Number(person.representativeTime);
  const fallbackStart = Number.isFinite(representativeTime)
    ? representativeTime
    : defaultRange ? defaultRange.start : 0;
  const previewStart = Math.max(0, hasStart ? Number(start) : (defaultRange?.start ?? fallbackStart));
  const resolvedEnd = hasEnd ? Number(end) : defaultRange?.end;
  const previewEnd = Number.isFinite(resolvedEnd) && resolvedEnd > previewStart ? resolvedEnd : null;
  const label = String(person.label || person.defaultLabel || person.id || "人物");
  focusCurrentPerson(person.id);
  showSource({ autoplay: false });
  viewerMediaKind = "source";
  mainVideo.pause();
  candidatePreviewEnd = previewEnd;
  seekSourceTime(previewStart);
  if (previewEnd != null) {
    setPersonProfileStatus(`正在预览 ${label} 的出镜片段 · ${formatTime(previewStart)} → ${formatTime(previewEnd)}`, "ready");
    window.setTimeout(() => safePlay(), 80);
  } else {
    setPersonProfileStatus(`${label} 的代表画面 · ${formatTime(previewStart)} · 暂无可播放片段`, "ready");
  }
}

function syncCurrentPersonControls() {
  const count = selectedCurrentPersons.size;
  const correctionCount = selectedPersonCorrectionPersons.size;
  const capabilities = interactionCapabilitiesForJob(currentJob);
  const locked = !capabilities.canCorrectPerson;
  const summary = $("#currentPersonSelectionSummary");
  if (summary) summary.textContent = `已选 ${count} 人`;
  const modes = $("#currentPersonMatchModes");
  modes?.classList.toggle("hidden", count <= 1);
  modes?.querySelectorAll("input").forEach((input) => { input.checked = input.value === currentPersonMatchMode; });
  const speaking = $("#currentPersonSpeakingOnly");
  if (speaking) speaking.checked = currentPersonActivity === "speaking";
  const action = $("#searchSelectedPersons");
  if (action) {
    action.disabled = locked || !count || actionBusy;
    action.title = locked ? capabilities.reason : "";
    action.textContent = currentPersonActivity === "speaking"
      ? (count ? `识别并整理发言片段（${count}）` : "识别并整理发言片段")
      : (count ? `直接整理出镜片段（${count}）` : "直接整理出镜片段");
  }
  const merge = $("#mergeSelectedPersons");
  if (merge) {
    merge.disabled = locked || correctionCount < 2 || actionBusy;
    merge.title = locked ? capabilities.reason : "";
    merge.textContent = correctionCount >= 2 ? `合并所选人物（${correctionCount}）` : "合并所选人物";
  }
  const split = $("#splitSelectedPersonRanges");
  if (split) {
    split.disabled = locked || !selectedCurrentPersonRanges.size || actionBusy;
    split.title = locked ? capabilities.reason : "";
    split.textContent = selectedCurrentPersonRanges.size
      ? `所选 ${selectedCurrentPersonRanges.size} 段拆为新人物`
      : "所选片段拆为新人物";
  }
  const undo = $("#undoPersonMerge");
  if (undo) {
    undo.disabled = locked || !currentJob?.personMergeCanUndo || actionBusy;
    undo.title = locked ? capabilities.reason : "";
  }
  const toolbar = $("#personMergeToolbar");
  toolbar?.classList.toggle("has-selection", correctionCount > 0 || selectedCurrentPersonRanges.size > 0);
  syncUnifiedWorkspaceView();
  syncReviewActionDock();
}

function setPersonReviewMode(mode = "correct", { render = true } = {}) {
  personReviewMode = mode === "select" ? "select" : "correct";
  document.body.dataset.personReviewMode = personReviewMode;
  $("#personReviewModeSwitch")?.querySelectorAll("[data-person-review-mode]").forEach((button) => {
    const active = button.dataset.personReviewMode === personReviewMode;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
  });
  if (render && currentJob && workflowKindForJob(currentJob) === "person_edit") renderCurrentPersons();
  syncReviewWorkbench();
  syncUnifiedWorkspaceView();
}

function renderCurrentPersons() {
  const list = $("#currentPersonList");
  const actions = $("#currentPersonActions");
  const count = $("#currentPersonCount");
  if (!list || !actions || !count) return;
  if (!currentJob || workflowKindForJob(currentJob) !== "person_edit") {
    list.innerHTML = "";
    actions.classList.add("hidden");
    count.textContent = "尚未识别";
    $("#personReviewModeSwitch")?.classList.add("hidden");
    $("#personMergeToolbar")?.classList.add("hidden");
    return;
  }
  ensureCurrentPersonState(currentJob);
  const persons = currentPersonCatalog(currentJob);
  const running = workflowOperationActive(currentJob, {
    operations: ["person_discovery", "person_tracking"],
    agentTools: ["discover_people"],
  });
  const correctionLocked = !interactionCapabilitiesForJob(currentJob).canCorrectPerson;
  const correctionLockAttributes = correctionLocked
    ? `disabled aria-disabled="true" title="${escapeHtml(unavailableInteractionMessage(currentJob))}"`
    : "";
  if (!persons.length) {
    count.textContent = running ? "识别中" : "尚未识别";
    actions.classList.add("hidden");
    $("#personReviewModeSwitch")?.classList.add("hidden");
    $("#personMergeToolbar")?.classList.add("hidden");
    list.innerHTML = running
      ? '<div class="current-person-empty loading"><i></i><strong>正在发现画面人物</strong><small>人物卡会在连续轨迹建立后显示</small></div>'
      : '<div class="current-person-empty"><strong>还没有可选择的人物</strong><small>开始人物识别后，这里会显示匿名人物卡。</small></div>';
    setPersonProfileStatus(running ? (currentJob.detail || "正在扫描人物与连续出镜轨迹") : "当前任务尚未生成人物卡", running ? "loading" : "");
    renderTimelinePersonTrack();
    syncReviewActionDock();
    return;
  }
  $("#personReviewModeSwitch")?.classList.remove("hidden");
  $("#personMergeToolbar")?.classList.remove("hidden");
  setPersonProfileStatus(personReviewMode === "correct"
    ? "第 1 步：试听代表片段，合并重复人物或修正错误出镜段；完成后切换到“选择剪辑目标”。"
    : "第 2 步：选择最终要保留的人物。这里只决定生成目标，不会再修改人物分组。", "ready");
  const colorIndex = new Map(persons.map((person, index) => [String(person.id), index % 6]));
  const needsReview = (person) => Boolean(person.reviewRecommended)
    || Math.max(0, Math.min(1, Number(person.confidence || 0))) < .72;
  const reliablePersons = persons.filter((person) => !needsReview(person));
  const reviewPersons = persons.filter(needsReview);
  count.textContent = `${reliablePersons.length} 个主要人物 · ${reviewPersons.length} 个待复核`;
  const personCardMarkup = (person) => {
    const personId = String(person.id || "");
    const label = String(person.label || person.defaultLabel || personId || "匿名人物");
    const selected = (personReviewMode === "correct" ? selectedPersonCorrectionPersons : selectedCurrentPersons).has(personId);
    const focused = focusedCurrentPersonRef === personId;
    const review = needsReview(person);
    const color = `var(--person-${colorIndex.get(personId) ?? 0})`;
    const ranges = validPersonRanges(person);
    const duplicateLabels = (person.possibleDuplicatePersonIds || []).map((duplicateId) => {
      const duplicate = persons.find((item) => String(item.id) === String(duplicateId));
      return duplicate ? String(duplicate.label || duplicate.defaultLabel || duplicate.id) : "";
    }).filter(Boolean);
    const rangeLimit = Math.max(
      currentPersonRangePageSize,
      Number(currentPersonRangeLimits.get(personId) || currentPersonRangePageSize),
    );
    const visibleRanges = ranges.slice(0, rangeLimit);
    const rangeRows = visibleRanges.map((range, index) => {
      const rangeId = String(range.id || `${personId}_range_${String(index).padStart(4, "0")}`);
      return `<div class="current-person-range-row"><input type="checkbox" data-current-person-range-select="${escapeHtml(rangeId)}" data-current-person-range-owner="${escapeHtml(personId)}" ${selectedCurrentPersonRanges.has(rangeId) ? "checked" : ""} aria-label="选择片段 ${index + 1} 进行人物校正"><button type="button" data-current-person-range="${index}" data-current-person-ref="${escapeHtml(personId)}" data-person-start="${range.start}" data-person-end="${range.end}" aria-label="播放${escapeHtml(label)}的第 ${index + 1} 个出镜片段，${escapeHtml(formatTime(range.start))}到${escapeHtml(formatTime(range.end))}"><b><i aria-hidden="true">▶</i> 片段 ${index + 1}</b><span>${escapeHtml(formatTime(range.start))} → ${escapeHtml(formatTime(range.end))}</span><em>${(range.end - range.start).toFixed(1)}s</em></button></div>`;
    }).join("");
    const remainingRangeCount = Math.max(0, ranges.length - visibleRanges.length);
    const moreRanges = remainingRangeCount
      ? `<button type="button" class="current-person-range-more" data-current-person-ranges-more="${escapeHtml(personId)}">再显示 ${Math.min(currentPersonRangePageSize, remainingRangeCount)} 段 · 尚有 ${remainingRangeCount} 段</button>`
      : "";
    const rangeMarkup = ranges.length
      ? `<details class="current-person-ranges" aria-label="${escapeHtml(label)}的全部出镜片段"${ranges.length <= 8 || rangeLimit > currentPersonRangePageSize ? " open" : ""}><summary><strong>全部出镜片段</strong><small>${ranges.length} 段 · ${personAppearanceSeconds(person).toFixed(1)} 秒</small><em>展开</em></summary><div>${rangeRows}${moreRanges}</div></details>`
      : '<p class="current-person-ranges-empty">尚未形成可播放的连续出镜片段</p>';
    return `<article class="current-person-card${selected ? " selected" : ""}${focused ? " focused" : ""}${review ? " review" : ""}" data-current-person="${escapeHtml(personId)}" style="--person-color:${color}">
      <label class="current-person-choice"><input type="checkbox" data-current-person-select value="${escapeHtml(personId)}" ${selected ? "checked" : ""}><span class="current-person-thumbnail"><img src="${escapeHtml(person.thumbnailUrl || "")}" alt="${escapeHtml(label)}的代表画面" loading="lazy"><em>播放片段</em></span><span class="current-person-copy"><strong>${escapeHtml(label)}</strong><small>${personAppearanceSeconds(person).toFixed(1)} 秒出镜 · ${ranges.length} 段</small><small>${duplicateLabels.length ? `疑似与 ${duplicateLabels.join("、")} 为同一人 · 请复核` : review ? "短暂或不稳定人物簇 · 建议复核" : `${Number(person.trackCount || ranges.length)} 次画面观测 · 自动分组`}</small></span></label>
      ${rangeMarkup}
      <footer><button type="button" data-current-person-preview="${escapeHtml(personId)}">播放代表片段</button><button type="button" data-current-person-label="${escapeHtml(personId)}" data-current-person-current-label="${escapeHtml(person.userLabeled ? person.label || "" : "")}" ${correctionLockAttributes}>${person.userLabeled ? "修改标签" : "添加标签"}</button>${selectedCurrentPersonRanges.size && selectedCurrentPersonRangeOwner !== personId ? `<button type="button" data-current-person-range-target="${escapeHtml(personId)}" ${correctionLockAttributes}>移到此人物</button>` : ""}</footer>
    </article>`;
  };
  const activePersonSelection = personReviewMode === "correct" ? selectedPersonCorrectionPersons : selectedCurrentPersons;
  const reviewOpen = !reliablePersons.length || reviewPersons.some((person) => person.duplicateReviewRecommended || activePersonSelection.has(String(person.id)) || focusedCurrentPersonRef === String(person.id));
  list.innerHTML = `${reliablePersons.length ? `<section class="current-person-group"><header><span><strong>主要人物</strong><small>观测较充分，仍可通过代表画面人工确认</small></span><b>${reliablePersons.length}</b></header><div class="current-person-grid">${reliablePersons.map(personCardMarkup).join("")}</div></section>` : ""}${reviewPersons.length ? `<details class="current-person-group current-person-review-group"${reviewOpen ? " open" : ""}><summary><span><strong>待复核人物</strong><small>可能是短暂路人、分屏中的同一人物或不稳定轨迹</small></span><b>${reviewPersons.length}</b></summary><div class="current-person-grid">${reviewPersons.map(personCardMarkup).join("")}</div></details>` : ""}`;
  actions.classList.remove("hidden");
  syncCurrentPersonControls();
  list.querySelectorAll("[data-current-person-select]").forEach((input) => input.addEventListener("change", () => {
    const selection = personReviewMode === "correct" ? selectedPersonCorrectionPersons : selectedCurrentPersons;
    if (input.checked) selection.add(String(input.value));
    else selection.delete(String(input.value));
    if (personReviewMode === "select" && selectedCurrentPersons.size <= 1) currentPersonMatchMode = "any";
    renderCurrentPersons();
    if (personReviewMode === "select") renderTimelinePersonTrack();
  }));
  list.querySelectorAll("[data-current-person-preview]").forEach((button) => button.addEventListener("click", () => {
    previewCurrentPerson(persons.find((person) => String(person.id) === String(button.dataset.currentPersonPreview)));
  }));
  list.querySelectorAll("[data-current-person-range]").forEach((button) => button.addEventListener("click", () => {
    const person = persons.find((item) => String(item.id) === String(button.dataset.currentPersonRef));
    previewCurrentPerson(person, Number(button.dataset.personStart), Number(button.dataset.personEnd));
  }));
  list.querySelectorAll("[data-current-person-ranges-more]").forEach((button) => button.addEventListener("click", () => {
    const personId = String(button.dataset.currentPersonRangesMore || "");
    const currentLimit = Number(currentPersonRangeLimits.get(personId) || currentPersonRangePageSize);
    currentPersonRangeLimits.set(personId, currentLimit + currentPersonRangePageSize);
    renderCurrentPersons();
  }));
  list.querySelectorAll("[data-current-person-range-select]").forEach((input) => input.addEventListener("change", () => {
    const owner = String(input.dataset.currentPersonRangeOwner || "");
    const rangeId = String(input.dataset.currentPersonRangeSelect || "");
    if (input.checked && selectedCurrentPersonRangeOwner && selectedCurrentPersonRangeOwner !== owner) {
      selectedCurrentPersonRanges.clear();
      showToast("一次只能拆分同一张人物卡中的片段");
    }
    selectedCurrentPersonRangeOwner = input.checked ? owner : selectedCurrentPersonRangeOwner;
    if (input.checked) selectedCurrentPersonRanges.add(rangeId);
    else selectedCurrentPersonRanges.delete(rangeId);
    if (!selectedCurrentPersonRanges.size) selectedCurrentPersonRangeOwner = "";
    renderCurrentPersons();
  }));
  list.querySelectorAll("[data-current-person-label]").forEach((button) => button.addEventListener("click", () => {
    updateContentPersonLabel(button.dataset.currentPersonLabel, button.dataset.currentPersonCurrentLabel || "");
  }));
  list.querySelectorAll("[data-current-person-range-target]").forEach((button) => button.addEventListener("click", () => {
    moveSelectedCurrentPersonRanges(button.dataset.currentPersonRangeTarget);
  }));
  list.querySelectorAll(".current-person-thumbnail").forEach((thumbnail) => thumbnail.addEventListener("click", (event) => {
    event.preventDefault();
    const card = thumbnail.closest("[data-current-person]");
    previewCurrentPerson(persons.find((person) => String(person.id) === String(card?.dataset.currentPerson)));
  }));
  list.querySelectorAll(".current-person-thumbnail img").forEach((image) => image.addEventListener("error", () => {
    image.closest(".current-person-thumbnail")?.classList.add("thumbnail-unavailable");
    image.remove();
  }, { once: true }));
  renderTimelinePersonTrack();
}

function openPersonProfiles() {
  if (!currentJob || workflowKindForJob(currentJob) !== "person_edit") return;
  closeSettings();
  closeVoiceProfiles();
  const panel = $("#personProfilePanel");
  const workbench = $("#reviewWorkbench");
  const actionDock = $("#reviewActionDock");
  if (panel && workbench) {
    const actionAnchor = actionDock?.parentElement === workbench ? actionDock : null;
    if (panel.parentElement !== workbench || (actionAnchor && panel.nextElementSibling !== actionAnchor)) {
      workbench.insertBefore(panel, actionAnchor);
    }
  }
  const closeButton = $("#closePersonProfiles");
  const workbenchHeader = workbench?.querySelector(".review-workbench-header");
  if (closeButton && workbenchHeader && closeButton.parentElement !== workbenchHeader) workbenchHeader.appendChild(closeButton);
  closeButton?.classList.remove("hidden");
  panel?.classList.remove("hidden");
  document.body.classList.add("person-workspace-open");
  document.querySelectorAll("[data-open-person-workspace]").forEach((button) => {
    button.setAttribute("aria-expanded", "true");
  });
  setReviewLowerPanelMode("review");
  const target = currentPersonTargetState(currentJob);
  setPersonReviewMode(target.personIds.length || Number(currentJob?.contentSearch?.candidates?.length || 0) ? "select" : "correct", { render: false });
  renderCurrentPersons();
  updateTimeline();
  window.requestAnimationFrame(() => scheduleMediaFrameFit(true));
}

function closePersonProfiles() {
  $("#personProfilePanel")?.classList.add("hidden");
  $("#closePersonProfiles")?.classList.add("hidden");
  document.body.classList.remove("person-workspace-open");
  document.querySelectorAll("[data-open-person-workspace]").forEach((button) => {
    button.setAttribute("aria-expanded", "false");
  });
  syncReviewWorkbench();
  window.requestAnimationFrame(() => scheduleMediaFrameFit(true));
}

async function searchSelectedCurrentPersons() {
  if (!interactionCapabilitiesForJob(currentJob).canCorrectPerson) return void showToast(unavailableInteractionMessage(currentJob), "error");
  if (!selectedCurrentPersons.size) return void showToast("请至少选择一个人物");
  const button = $("#searchSelectedPersons");
  await selectContentPersonTarget(
    [...selectedCurrentPersons],
    selectedCurrentPersons.size > 1 ? currentPersonMatchMode : "any",
    button,
    currentPersonActivity,
  );
}

let voiceEnrollmentSource = "range";
let voiceProfiles = [];
let currentVoices = [];
let currentVoiceTimeline = [];
let currentVoiceRevision = 0;
let currentVoiceCanUndo = false;
let voiceCorrectionBusy = false;
let currentVoiceJobId = "";
let currentVoiceRequestGeneration = 0;
let currentVoiceDiscoveryStatus = "not_started";
const currentVoiceTurnPageSize = 80;
let currentVoiceTurnLimit = currentVoiceTurnPageSize;
let focusedCurrentVoiceRef = "";
let selectedCurrentVoices = new Set();
let selectedVoiceCorrectionVoices = new Set();
let temporaryVoiceSources = [];
let temporaryVoiceSession = null;
let voiceDiscoveryTimer = null;
let temporaryVoiceSessionTimer = null;
let temporaryVoiceSourceRequestGeneration = 0;

function setVoiceProfileStatus(message = "", tone = "") {
  const node = $("#voiceProfileStatus");
  if (!node) return;
  node.textContent = message;
  node.dataset.tone = tone;
}

function beginCurrentVoiceMutation(label = "声音校正") {
  if (!interactionCapabilitiesForJob(currentJob).canCorrectVoice) {
    setVoiceProfileStatus(unavailableInteractionMessage(currentJob, `当前不能执行${label}`), "error");
    return false;
  }
  if (voiceCorrectionBusy) {
    setVoiceProfileStatus("另一项声音操作正在保存，请稍候。", "error");
    return false;
  }
  voiceCorrectionBusy = true;
  syncCurrentVoiceControls();
  return true;
}

function finishCurrentVoiceMutation() {
  voiceCorrectionBusy = false;
  const locked = !interactionCapabilitiesForJob(currentJob).canCorrectVoice;
  document.querySelectorAll("[data-current-voice-role], [data-current-voice-label]").forEach((button) => {
    button.disabled = locked;
    button.title = locked ? unavailableInteractionMessage(currentJob) : "";
  });
  const discoveryBusy = ["running", "loading"].includes(currentVoiceDiscoveryStatus);
  const discoverButton = $("#discoverCurrentVoices");
  if (discoverButton) discoverButton.disabled = locked || discoveryBusy || !currentJob;
  const expectedCount = $("#expectedVoiceCount");
  if (expectedCount) expectedCount.disabled = locked || discoveryBusy;
  syncCurrentVoiceControls();
  renderInlineCurrentVoices({ status: currentVoiceDiscoveryStatus });
}

function resetCurrentVoiceState(jobId = "") {
  currentVoiceRequestGeneration += 1;
  temporaryVoiceSourceRequestGeneration += 1;
  currentVoiceJobId = String(jobId || "");
  clearTimeout(voiceDiscoveryTimer);
  clearTimeout(temporaryVoiceSessionTimer);
  currentVoices = [];
  currentVoiceTimeline = [];
  currentVoiceRevision = 0;
  currentVoiceCanUndo = false;
  voiceCorrectionBusy = false;
  currentVoiceDiscoveryStatus = "not_started";
  currentVoiceTurnLimit = currentVoiceTurnPageSize;
  focusedCurrentVoiceRef = "";
  selectedCurrentVoices.clear();
  selectedVoiceCorrectionVoices.clear();
  if ($("#currentVoiceMode")) $("#currentVoiceMode").value = "include";
  if ($("#voiceSearchQuery")) $("#voiceSearchQuery").value = "";
  temporaryVoiceSources = [];
  temporaryVoiceSession = null;
  renderTemporaryVoiceSources();
  renderTemporaryVoiceSession();
  renderCurrentVoices({ status: "not_started" });
}

function renderInlineCurrentVoices(discovery = {}) {
  const root = $("#inlineCurrentVoices");
  const narratorMode = Boolean(currentJob?.narratorSelectionPending?.active);
  if (!root || (workflowKindForJob(currentJob) !== "speaker_edit" && !narratorMode)) return;
  discovery = voiceDiscoveryPresentationForJob(currentJob, discovery);
  const interactionLocked = !interactionCapabilitiesForJob(currentJob).canCorrectVoice || voiceCorrectionBusy;
  const interactionLockAttributes = interactionLocked
    ? `disabled aria-disabled="true" title="${escapeHtml(voiceCorrectionBusy ? "正在保存声音校正" : unavailableInteractionMessage(currentJob))}"`
    : "";
  const status = String(discovery.status || currentVoiceDiscoveryStatus || "not_started");
  if (["running", "loading"].includes(status)) {
    root.innerHTML = `<header><div><small>${narratorMode ? "旁白识别" : "发言剪辑"}</small><strong>${narratorMode ? "正在生成疑似旁白候选" : "正在区分匿名说话人"}</strong></div><b>处理中</b></header><p>${escapeHtml(discovery.currentAction || discovery.detail || "正在通读全片语音并整理发言轮次")}</p><button type="button" data-inline-speaker-advanced>查看详细进度</button>`;
  } else if (status === "failed") {
    root.innerHTML = `<header><div><small>发言剪辑</small><strong>说话人识别未完成</strong></div></header><p>${escapeHtml(discovery.error || "可以重新尝试识别。")}</p><footer><button type="button" class="primary" data-inline-speaker-start ${interactionLockAttributes}>重新识别</button><button type="button" data-inline-speaker-advanced>打开高级面板</button></footer>`;
  } else if (status !== "ready" || !currentVoices.length) {
    root.innerHTML = `<header><div><small>${narratorMode ? "旁白识别" : "发言剪辑"}</small><strong>先区分视频中的匿名声音</strong></div></header><p>${narratorMode ? "系统只分析声音并给出疑似旁白，不会启动人物、人脸或 TalkNet 扫描。" : "不需要输入姓名。系统会提供代表片段供试听，再由你选择要保留的说话人。"}</p><label>预计说话人数 <input type="number" min="0" max="32" step="1" placeholder="自动判断" value="${Number(discovery.expectedSpeakerCount) || ""}" data-inline-speaker-count ${interactionLockAttributes}></label><footer><button type="button" class="primary" data-inline-speaker-start ${interactionLockAttributes}>开始识别说话人</button><button type="button" data-inline-speaker-advanced>高级设置</button></footer>`;
  } else {
    const orderedVoices = narratorMode ? [...currentVoices].sort((left, right) => {
      const order = { confirmed: 0, candidate: 1, unlikely: 2, rejected: 3 };
      return (order[left.narration?.status] ?? 4) - (order[right.narration?.status] ?? 4)
        || Number(right.narration?.score || 0) - Number(left.narration?.score || 0);
    }) : currentVoices;
    root.innerHTML = `<header><div><small>${narratorMode ? "旁白识别" : "发言剪辑"}</small><strong>${narratorMode ? "试听并确认旁白声音" : "选择要保留发言的说话人"}</strong></div><b>${currentVoices.length} 个声音</b></header><p>${narratorMode ? "“疑似旁白”只是根据发言时间线给出的建议；试听确认后才会绑定，后续检索会直接复用。" : "先试听代表片段。名称只是当前项目内的匿名标签，可以稍后修改。"}</p><div class="inline-voice-grid">${orderedVoices.map((voice) => {
      const selected = selectedCurrentVoices.has(String(voice.speakerRef));
      const sample = (voice.representativeSegments || [])[0];
      const narration = voice.narration || {};
      const quality = narratorMode && narration.status === "confirmed" ? "已确认旁白" : narratorMode && narration.status === "candidate" ? "疑似旁白" : voice.requiresReview ? "建议复核" : "声音稳定";
      const reason = narratorMode && (narration.reasons || []).length ? `<em>${escapeHtml(narration.reasons.join(" · "))}</em>` : "";
      return `<article class="inline-voice-card${selected ? " selected" : ""}${narration.status === "candidate" ? " narrator-candidate" : ""}"><label><input type="checkbox" data-inline-voice-select value="${escapeHtml(voice.speakerRef)}" ${selected ? "checked" : ""}><span><strong>${escapeHtml(voice.label || voice.speakerRef)}</strong><small>${Number(voice.speechSeconds || 0).toFixed(1)} 秒 · ${Number(voice.segmentCount || 0)} 段 · ${quality}</small>${reason}</span></label>${sample ? `<button type="button" data-inline-voice-preview data-start="${Number(sample.start) || 0}" data-end="${Number(sample.end) || 0}">▶ 试听 ${formatTime(sample.start)}</button>` : ""}${narratorMode && narration.status === "candidate" ? `<button type="button" data-inline-not-narrator="${escapeHtml(voice.speakerRef)}" ${interactionLockAttributes}>不是旁白</button>` : ""}</article>`;
    }).join("")}</div><footer><button type="button" class="primary" ${narratorMode ? "data-inline-narrator-confirm" : "data-inline-voice-confirm"} ${selectedCurrentVoices.size && !interactionLocked ? "" : "disabled"} ${interactionLocked ? `aria-disabled="true" title="${escapeHtml(unavailableInteractionMessage(currentJob))}"` : ""}>${interactionLocked ? "任务完成后继续" : narratorMode ? `确认旁白并继续检索（${selectedCurrentVoices.size}）` : `整理所选发言（${selectedCurrentVoices.size}）`}</button><button type="button" data-inline-speaker-advanced>更多校正工具</button></footer>`;
  }
  root.querySelector("[data-inline-speaker-start]")?.addEventListener("click", () => {
    const expected = Number(root.querySelector("[data-inline-speaker-count]")?.value || 0);
    const panelInput = $("#expectedVoiceCount");
    if (panelInput) panelInput.value = String(expected || 0);
    discoverCurrentVoices();
  });
  root.querySelectorAll("[data-inline-voice-preview]").forEach((button) => button.addEventListener("click", () => previewCurrentVoice(Number(button.dataset.start), Number(button.dataset.end))));
  root.querySelectorAll("[data-inline-voice-select]").forEach((input) => input.addEventListener("change", () => {
    if (input.checked) selectedCurrentVoices.add(input.value); else selectedCurrentVoices.delete(input.value);
    renderInlineCurrentVoices({ status: "ready" });
    renderCurrentVoices({ status: "ready" });
  }));
  root.querySelector("[data-inline-voice-confirm]")?.addEventListener("click", () => searchSelectedCurrentVoices("include"));
  root.querySelector("[data-inline-narrator-confirm]")?.addEventListener("click", confirmNarratorSelection);
  root.querySelectorAll("[data-inline-not-narrator]").forEach((button) => button.addEventListener("click", () => setCurrentVoiceRole(button.dataset.inlineNotNarrator, "regular")));
  root.querySelectorAll("[data-inline-speaker-advanced]").forEach((button) => button.addEventListener("click", openVoiceProfiles));
}

function renderCurrentVoices(discovery = {}) {
  const list = $("#currentVoiceList");
  const button = $("#discoverCurrentVoices");
  if (!list || !button) return;
  discovery = voiceDiscoveryPresentationForJob(currentJob, discovery);
  const status = String(discovery.status || "not_started");
  const voiceLocked = !interactionCapabilitiesForJob(currentJob).canCorrectVoice || voiceCorrectionBusy;
  const voiceLockAttributes = voiceLocked
    ? `disabled aria-disabled="true" title="${escapeHtml(voiceCorrectionBusy ? "正在保存声音校正" : unavailableInteractionMessage(currentJob))}"`
    : "";
  currentVoiceDiscoveryStatus = status;
  renderInlineCurrentVoices(discovery);
  renderTimelineSpeakerTrack();
  const expectedCount = $("#expectedVoiceCount");
  if (expectedCount && document.activeElement !== expectedCount && discovery.expectedSpeakerCount != null) {
    expectedCount.value = String(Number(discovery.expectedSpeakerCount) || 0);
  }
  if (expectedCount && discovery.expectedSpeakerCount != null) {
    expectedCount.dataset.indexedCount = String(Number(discovery.expectedSpeakerCount) || 0);
  }
  if (expectedCount) expectedCount.disabled = voiceLocked || ["running", "loading"].includes(status);
  if (status !== "failed") {
    const countChanged = Number(expectedCount?.value || 0) !== Number(expectedCount?.dataset.indexedCount || 0);
    button.textContent = status === "ready" && currentVoices.length
      ? (countChanged ? "按新人数重新识别" : "重新识别说话人")
      : "开始识别说话人";
  }
  $("#currentVoiceCount").textContent = status === "ready" ? `${currentVoices.length} 个声音` : status === "running" ? "识别中" : status === "loading" ? "读取中" : "尚未识别";
  syncReviewWorkbench();
  const unchangedReadyCount = status === "ready" && currentVoices.length
    && Number(expectedCount?.value || 0) === Number(expectedCount?.dataset.indexedCount || 0);
  button.disabled = voiceLocked || ["running", "loading"].includes(status) || !currentJob;
  button.title = voiceLocked ? (voiceCorrectionBusy ? "正在保存声音校正" : unavailableInteractionMessage(currentJob)) : "";
  button.closest(".current-voice-discovery-controls")?.classList.toggle("discovery-ready", Boolean(unchangedReadyCount));
  button.classList.remove("hidden");
  syncReviewActionDock();
  if (!currentJob) {
    $("#voiceReviewModeSwitch")?.classList.add("hidden");
    list.innerHTML = '<div class="voice-profile-empty">请先打开一个视频任务。</div>';
    return;
  }
  if (status === "loading") {
    list.innerHTML = '<div class="voice-profile-empty">正在读取当前视频的说话人信息…</div>';
    setVoiceProfileStatus("正在切换到当前视频，不会沿用其他视频的声音结果。");
    syncCurrentVoiceControls();
    return;
  }
  if (status === "running") {
    const completed = Number(discovery.stageCompleted);
    const total = Number(discovery.stageTotal);
    const stageProgress = Number(discovery.stageProgress);
    const determinate = Number.isFinite(completed) && Number.isFinite(total) && total > 0
      && Number.isFinite(stageProgress);
    const percent = determinate ? Math.round(Math.max(0, Math.min(1, stageProgress)) * 100) : 0;
    const startedAt = Date.parse(String(discovery.startedAt || ""));
    const elapsed = Number.isFinite(startedAt) ? Math.max(0, (Date.now() - startedAt) / 1000) : null;
    const progressFact = determinate
      ? `已完成 ${Math.min(completed, total)}/${total} ${escapeHtml(discovery.stageUnit || "个声音")}`
      : `正在通读全片语音，暂时无法可靠估算百分比${elapsed == null ? "" : ` · 已处理 ${formatClock(elapsed)}`}`;
    const detail = discovery.currentAction || discovery.detail || "正在分析语音并区分不同声音";
    list.innerHTML = `<section class="voice-discovery-progress${determinate ? " determinate" : " indeterminate"}" role="progressbar" aria-label="说话人识别进度" aria-busy="true" aria-valuemin="0" aria-valuemax="100"${determinate ? ` aria-valuenow="${percent}"` : ""}><header><span><i></i><b>说话人识别进行中</b></span><strong>${determinate ? `${percent}%` : "处理中"}</strong></header><p>${escapeHtml(detail)}</p><div class="voice-discovery-progress-track"><i style="width:${percent}%"></i></div><small>${progressFact}</small></section>`;
    setVoiceProfileStatus("识别正在后台进行；可以继续操作右侧视频和时间轴。");
    return;
  }
  if (status === "failed") {
    list.innerHTML = `<div class="voice-profile-empty">${escapeHtml(discovery.error || "说话人识别失败")}</div>`;
    setVoiceProfileStatus("可以稍后重新识别。", "error");
    button.classList.remove("hidden");
    button.textContent = "重新识别本视频说话人";
    return;
  }
  if (!currentVoices.length) {
    $("#voiceReviewModeSwitch")?.classList.add("hidden");
    list.innerHTML = '<div class="voice-profile-empty">先选择预计说话人数，再点击上方按钮。系统会识别多个匿名声音，不需要输入人物名称。</div>';
    setVoiceProfileStatus("视频已就绪；“自动判断”适合人数未知，知道人数时可直接指定。", "success");
    return;
  }
  $("#voiceReviewModeSwitch")?.classList.remove("hidden");
  selectedCurrentVoices = new Set([...selectedCurrentVoices].filter((value) => currentVoices.some((voice) => voice.speakerRef === value)));
  selectedVoiceCorrectionVoices = new Set([...selectedVoiceCorrectionVoices].filter((value) => currentVoices.some((voice) => voice.speakerRef === value)));
  const legacyClustering = discovery.clustering !== "camplus_conservative_v2";
  const mixedClusters = currentVoices.filter((voice) => voice.quality?.suspectedMixed);
  const reviewCount = currentVoices.filter((voice) => voice.requiresReview).length;
  const expectedSpeakerCount = Number(discovery.expectedSpeakerCount || 0);
  const hasMixedCluster = mixedClusters.length > 0;
  const qualityStatus = legacyClustering
    ? "当前结果来自旧版聚类。建议选择预计人数后重新识别；不确定人数可继续使用自动判断。"
    : hasMixedCluster
      ? `${mixedClusters.length} 个声音的多个样本差异显著。请先试听确认；${expectedSpeakerCount > 0 ? "如确实混入不同人物，可调整人数后重识别。" : "如确实混入不同人物，可指定人数后重识别。"}`
      : reviewCount
        ? `已识别 ${currentVoices.length} 个声音，其中 ${reviewCount} 个建议试听确认；单个低分样本不再判定为混声。`
        : "声音分组已建立。";
  setVoiceProfileStatus(voiceReviewMode === "correct"
    ? `第 1 步：${qualityStatus} 试听、合并声音或在发言时间轴中修正归属。`
    : "第 2 步：选择最终要保留或排除的说话人，再查看其发言片段。", legacyClustering || hasMixedCluster ? "error" : "success");
  list.innerHTML = currentVoices.map((voice, index) => {
    const quality = voice.quality || {};
    const narration = voice.narration || {};
    const score = voice.userCorrected ? "人工校正" : quality.clusterMinimumSimilarity == null ? "未评分" : `一致度 ${Number(quality.clusterMinimumSimilarity).toFixed(2)}`;
    const samples = (voice.representativeSegments || []).map((sample) => `<button type="button" class="current-voice-sample" data-current-voice-preview="${escapeHtml(voice.speakerRef)}" data-voice-start="${Number(sample.start || 0)}" data-voice-end="${Number(sample.end || 0)}"><span>▶ ${formatTime(sample.start)}</span><small>${escapeHtml(sample.text || "试听代表声音")}</small></button>`).join("");
    const selected = (voiceReviewMode === "correct" ? selectedVoiceCorrectionVoices : selectedCurrentVoices).has(voice.speakerRef);
    const suspectedMixed = Boolean(quality.suspectedMixed);
    const badge = narration.status === "confirmed" ? "已确认旁白" : narration.status === "candidate" ? "疑似旁白" : suspectedMixed ? "疑似混声" : voice.requiresReview ? "建议复核" : voice.userCorrected ? "人工确认" : "声音稳定";
    const narrationHint = ["confirmed", "candidate"].includes(narration.status)
      ? `<section class="current-voice-narration ${escapeHtml(narration.status)}"><strong>${narration.status === "confirmed" ? "本视频旁白" : "系统建议试听确认是否为旁白"}</strong><small>${escapeHtml((narration.reasons || []).join(" · ") || "仅依据声音时间线判断，不代表人物身份")}</small></section>` : "";
    const narratorButton = narration.status === "confirmed" ? "取消旁白标记" : narration.status === "candidate" ? "确认是旁白" : "标记为旁白";
    return `<article class="current-voice-card${selected ? " selected" : ""}${focusedCurrentVoiceRef === voice.speakerRef ? " focused" : ""}${narration.status === "candidate" ? " narrator-candidate" : ""}" data-current-voice="${escapeHtml(voice.speakerRef)}" style="--voice-color:var(--voice-${index % 6})"><header><label class="current-voice-choice"><input type="checkbox" data-current-voice-select value="${escapeHtml(voice.speakerRef)}" ${selected ? "checked" : ""}><span><strong>${escapeHtml(voice.label || voice.speakerRef)}</strong><small>${Number(voice.speechSeconds || 0).toFixed(1)} 秒发言 · ${Number(voice.segmentCount || 0)} 个片段 · ${score}</small></span></label><b class="${voice.requiresReview || narration.status === "candidate" ? "review" : ""}" title="${escapeHtml(quality.warning || "")}">${badge}</b></header>${narrationHint}<div class="current-voice-samples">${samples || '<small>没有足够长的试听片段</small>'}</div><footer><button type="button" data-current-voice-role="${escapeHtml(voice.speakerRef)}" data-role="${narration.status === "confirmed" ? "auto" : "narrator"}" ${voiceLockAttributes}>${narratorButton}</button><button type="button" data-current-voice-label="${escapeHtml(voice.speakerRef)}" ${voiceLockAttributes}>${voice.userLabeled && narration.status !== "confirmed" ? "改名" : "命名"}</button></footer></article>`;
  }).join("");
  list.querySelectorAll("[data-current-voice-preview]").forEach((button) => button.addEventListener("click", () => {
    focusCurrentVoice(button.dataset.currentVoicePreview, { scroll: false });
    previewCurrentVoice(Number(button.dataset.voiceStart), Number(button.dataset.voiceEnd));
  }));
  list.querySelectorAll("[data-current-voice-role]").forEach((button) => button.addEventListener("click", () => setCurrentVoiceRole(button.dataset.currentVoiceRole, button.dataset.role)));
  list.querySelectorAll("[data-current-voice-label]").forEach((button) => button.addEventListener("click", () => labelCurrentVoice(button.dataset.currentVoiceLabel)));
  list.querySelectorAll("[data-current-voice-select]").forEach((input) => input.addEventListener("change", () => {
    const selection = voiceReviewMode === "correct" ? selectedVoiceCorrectionVoices : selectedCurrentVoices;
    if (input.checked) selection.add(input.value); else selection.delete(input.value);
    input.closest(".current-voice-card")?.classList.toggle("selected", input.checked);
    if (voiceReviewMode === "select") renderTimelineSpeakerTrack();
    syncCurrentVoiceControls();
  }));
  renderCurrentVoiceTimeline();
  syncCurrentVoiceControls();
}

function syncCurrentVoiceControls() {
  const actions = $("#currentVoiceActions");
  const timeline = $("#currentVoiceTimelineSection");
  actions?.classList.toggle("hidden", !currentVoices.length);
  timeline?.classList.toggle("hidden", !currentVoiceTimeline.length);
  const count = selectedCurrentVoices.size;
  const correctionCount = selectedVoiceCorrectionVoices.size;
  const mode = $("#currentVoiceMode")?.value || "include";
  const capabilities = interactionCapabilitiesForJob(currentJob);
  const locked = !capabilities.canCorrectVoice || voiceCorrectionBusy;
  const lockedReason = voiceCorrectionBusy ? "正在保存声音校正" : capabilities.reason;
  const search = $("#searchSelectedVoices");
  if (search) { search.disabled = locked || !count || (mode === "qa_pair" && count !== 1); search.title = locked ? lockedReason : ""; search.textContent = count ? `查看发言片段（${count}）` : "查看发言片段"; }
  const merge = $("#mergeSelectedVoices");
  if (merge) {
    merge.disabled = locked || correctionCount < 2;
    merge.title = locked ? lockedReason : "";
    merge.textContent = correctionCount >= 2 ? `合并所选声音（${correctionCount}）` : "合并所选声音";
  }
  const undo = $("#undoVoiceCorrection"); if (undo) { undo.disabled = locked || !currentVoiceCanUndo; undo.title = locked ? lockedReason : ""; }
  const target = $("#voiceReassignTarget");
  if (target) {
    const previous = target.value;
    target.innerHTML = `<option value="new">新建声音</option>${currentVoices.map((voice) => `<option value="${escapeHtml(voice.speakerRef)}">${escapeHtml(voice.label || voice.speakerRef)}</option>`).join("")}`;
    if ([...target.options].some((option) => option.value === previous)) target.value = previous;
    target.disabled = locked;
    target.title = locked ? lockedReason : "";
  }
  const selectedTurnIds = new Set(
    [...document.querySelectorAll("[data-voice-turn-select]:checked")].map((input) => String(input.value)),
  );
  const selectedTurns = currentVoiceTimeline.filter((turn) => selectedTurnIds.has(String(turn.turnId)));
  const selectedDuration = selectedTurns.reduce((sum, turn) => sum + Math.max(0, Number(turn.end) - Number(turn.start)), 0);
  const turnSummary = $("#voiceTurnSelectionSummary");
  if (turnSummary) turnSummary.textContent = selectedTurns.length
    ? `已选 ${selectedTurns.length} 段 · ${selectedDuration.toFixed(1)} 秒`
    : "先勾选归属错误的发言";
  const newLabelWrap = $("#voiceReassignLabelWrap");
  newLabelWrap?.classList.toggle("hidden", target?.value !== "new");
  const ineffectiveMove = Boolean(
    selectedTurns.length && target?.value !== "new"
    && selectedTurns.every((turn) => String(turn.speakerRef) === String(target.value)),
  );
  const reassign = $("#reassignVoiceTurns");
  if (reassign) {
    reassign.disabled = locked || !selectedTurns.length || ineffectiveMove;
    reassign.title = locked ? lockedReason : ineffectiveMove ? "所选发言已经属于该说话人" : selectedTurns.length ? "保存校正并立即更新后续提取时间轴" : "请先勾选发言";
  }
  const split = $("#splitVoiceTurn");
  if (split) {
    split.disabled = locked || selectedTurns.length !== 1;
    split.title = locked ? lockedReason : selectedTurns.length === 1 ? "把所选发言在播放器当前时间拆成两段" : "请只勾选一段发言";
  }
  syncUnifiedWorkspaceView();
  syncReviewActionDock();
}

function setVoiceReviewMode(mode = "correct", { render = true } = {}) {
  voiceReviewMode = mode === "select" ? "select" : "correct";
  document.body.dataset.voiceReviewMode = voiceReviewMode;
  $("#voiceReviewModeSwitch")?.querySelectorAll("[data-voice-review-mode]").forEach((button) => {
    const active = button.dataset.voiceReviewMode === voiceReviewMode;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
  });
  if (render && currentVoices.length) renderCurrentVoices({ status: "ready" });
  syncReviewWorkbench();
  syncUnifiedWorkspaceView();
}

function renderCurrentVoiceTimeline() {
  const rows = $("#currentVoiceTurns");
  if (!rows) return;
  const colorIndex = new Map(currentVoices.map((voice, index) => [voice.speakerRef, index % 6]));
  const selectedTurnIds = new Set(
    [...rows.querySelectorAll("[data-voice-turn-select]:checked")].map((input) => String(input.value)),
  );
  const visibleTurns = currentVoiceTimeline.slice(0, currentVoiceTurnLimit);
  const remainingTurnCount = Math.max(0, currentVoiceTimeline.length - visibleTurns.length);
  rows.innerHTML = visibleTurns.map((turn) => `<article class="current-voice-turn${turn.requiresReview ? " review" : ""}${selectedTurnIds.has(String(turn.turnId)) ? " selected" : ""}" style="--voice-color:var(--voice-${colorIndex.get(turn.speakerRef) || 0})"><label><input type="checkbox" data-voice-turn-select value="${escapeHtml(turn.turnId)}" ${selectedTurnIds.has(String(turn.turnId)) ? "checked" : ""}><span><b>${escapeHtml(turn.label || turn.speakerRef)}</b><small>${formatTime(turn.start)} → ${formatTime(turn.end)}${turn.overlapSeconds >= .15 ? " · 有重叠声音" : ""}</small></span></label><button type="button" data-voice-turn-preview="${escapeHtml(turn.turnId)}">${escapeHtml(turn.text || "试听这段发言")}</button></article>`).join("")
    + (remainingTurnCount ? `<button type="button" class="current-voice-turn-more" data-current-voice-turn-more>再显示 ${Math.min(currentVoiceTurnPageSize, remainingTurnCount)} 段 · 尚有 ${remainingTurnCount} 段</button>` : "");
  $("#currentVoiceTurnCount").textContent = `${currentVoiceTimeline.length} 段${currentVoiceRevision ? ` · 已校正 r${currentVoiceRevision}` : " · 自动分组"}`;
  rows.querySelectorAll("[data-voice-turn-select]").forEach((input) => input.addEventListener("change", () => {
    input.closest(".current-voice-turn")?.classList.toggle("selected", input.checked);
    syncCurrentVoiceControls();
  }));
  rows.querySelectorAll("[data-voice-turn-preview]").forEach((button) => button.addEventListener("click", () => {
    const turn = currentVoiceTimeline.find((item) => item.turnId === button.dataset.voiceTurnPreview);
    if (turn) {
      focusCurrentVoice(turn.speakerRef, { scroll: false });
      previewCurrentVoice(Number(turn.start), Number(turn.end));
    }
  }));
  rows.querySelector("[data-current-voice-turn-more]")?.addEventListener("click", () => {
    currentVoiceTurnLimit += currentVoiceTurnPageSize;
    renderCurrentVoiceTimeline();
    syncCurrentVoiceControls();
  });
  renderTimelineSpeakerTrack();
}

function focusCurrentVoice(speakerRef, { scroll = true } = {}) {
  focusedCurrentVoiceRef = String(speakerRef || "");
  document.querySelectorAll("[data-current-voice]").forEach((card) => {
    card.classList.toggle("focused", card.dataset.currentVoice === focusedCurrentVoiceRef);
  });
  document.querySelectorAll("[data-timeline-speaker-ref], [data-timeline-speaker-label-ref]").forEach((segment) => {
    const speakerRef = segment.dataset.timelineSpeakerRef || segment.dataset.timelineSpeakerLabelRef;
    segment.classList.toggle("focused", speakerRef === focusedCurrentVoiceRef);
  });
  if (scroll && !$("#voiceProfilePanel")?.classList.contains("hidden")) {
    document.querySelector(`[data-current-voice="${CSS.escape(focusedCurrentVoiceRef)}"]`)?.scrollIntoView({ behavior: "smooth", block: "nearest" });
  }
}

function renderTimelinePersonTrack() {
  const root = $("#timelinePersonTrack");
  const labelsRoot = $("#timelinePersonTrackLabels");
  const segmentsRoot = $("#timelinePersonSegments");
  const count = $("#timelinePersonTrackCount");
  if (!root || !labelsRoot || !segmentsRoot || !count) return;
  const personMode = currentJob && workflowKindForJob(currentJob) === "person_edit";
  const persons = personMode ? currentPersonCatalog(currentJob) : [];
  const running = personMode && workflowOperationActive(currentJob, {
    operations: ["person_discovery", "person_tracking"],
    agentTools: ["discover_people"],
  });
  const visible = !timelineOutputAxisActive() && personMode && (persons.length > 0 || running);
  root.classList.toggle("hidden", !visible);
  if (!visible) {
    labelsRoot.innerHTML = "";
    segmentsRoot.innerHTML = "";
    count.textContent = "0 人 · 0 段";
    return;
  }
  ensureCurrentPersonState(currentJob);
  if (!persons.length) {
    root.style.setProperty("--person-rows", "1");
    count.textContent = "识别中";
    labelsRoot.innerHTML = '<span class="timeline-person-label-state">分析中</span>';
    segmentsRoot.innerHTML = '<span class="timeline-person-state loading"><i></i>正在建立人物卡与连续出镜轨迹</span>';
    updateTimelinePersonPlayhead();
    return;
  }
  const persistedTargetIds = new Set(currentPersonTargetState(currentJob).personIds);
  const selectionWorkspaceOpen = document.body.classList.contains("person-workspace-open");
  const displayedPersons = persistedTargetIds.size && !selectionWorkspaceOpen
    ? persons.filter((person) => persistedTargetIds.has(String(person.id)))
    : persons;
  const rows = displayedPersons.length ? displayedPersons : persons;
  const view = timelineViewRange();
  const colorIndex = new Map(persons.map((person, index) => [String(person.id), index % 6]));
  const totalRanges = rows.reduce((sum, person) => sum + (person.ranges || []).length, 0);
  root.style.setProperty("--person-rows", String(Math.max(1, rows.length)));
  count.textContent = `${rows.length} 人 · ${totalRanges} 段`;
  labelsRoot.innerHTML = rows.map((person) => {
    const personId = String(person.id || "");
    const label = String(person.label || person.defaultLabel || personId || "匿名人物");
    const selected = selectedCurrentPersons.has(personId);
    const focused = focusedCurrentPersonRef === personId;
    const muted = (selectedCurrentPersons.size > 0 || focusedCurrentPersonRef) && !selected && !focused;
    const color = `var(--person-${colorIndex.get(personId) ?? 0})`;
    const thumbnail = person.thumbnailUrl
      ? `<img src="${escapeHtml(person.thumbnailUrl)}" alt="${escapeHtml(label)}的代表画面" loading="lazy">`
      : `<span>${escapeHtml(label.slice(0, 1) || "人")}</span>`;
    return `<div class="timeline-person-row-label${selected ? " selected" : ""}${focused ? " focused" : ""}${muted ? " muted" : ""}" data-timeline-person-label-ref="${escapeHtml(personId)}" style="--person-color:${color}"><input type="checkbox" data-timeline-person-select value="${escapeHtml(personId)}" ${selected ? "checked" : ""} aria-label="选择${escapeHtml(label)}"><button type="button" data-timeline-person-focus title="聚焦 ${escapeHtml(label)}"><em class="timeline-person-avatar">${thumbnail}</em><i></i><span><strong>${escapeHtml(label)}</strong><small>${personAppearanceSeconds(person).toFixed(1)} 秒 · ${(person.ranges || []).length} 段</small></span></button></div>`;
  }).join("");
  segmentsRoot.innerHTML = rows.map((person, rowIndex) => {
    const personId = String(person.id || "");
    const label = String(person.label || person.defaultLabel || personId || "匿名人物");
    const selected = selectedCurrentPersons.has(personId);
    const focused = focusedCurrentPersonRef === personId;
    const muted = (selectedCurrentPersons.size > 0 || focusedCurrentPersonRef) && !selected && !focused;
    const review = Boolean(person.reviewRecommended)
      || (Number(person.confidence || 0) > 0 && Number(person.confidence) < .72);
    const color = `var(--person-${colorIndex.get(personId) ?? 0})`;
    const ranges = (person.ranges || [])
      .filter((range) => Number(range.end) > view.start && Number(range.start) < view.end)
      .sort((left, right) => Number(left.start) - Number(right.start));
    const segments = ranges.map((range, rangeIndex) => {
      const start = Math.max(view.start, Number(range.start) || 0);
      const end = Math.min(view.end, Number(range.end) || start);
      const title = [label, `${formatTime(range.start)} → ${formatTime(range.end)}`, review ? "人物匹配建议复核" : "可靠出镜轨迹"].join(" · ");
      return `<button type="button" class="timeline-person-segment${selected ? " selected" : ""}${focused ? " focused" : ""}${muted ? " muted" : ""}${review ? " review" : ""}" data-timeline-person-range="${rangeIndex}" data-timeline-person-ref="${escapeHtml(personId)}" data-person-start="${Number(range.start) || 0}" data-person-end="${Number(range.end) || 0}" style="left:${timelinePercentInView(start)}%;width:${Math.max(.3, (end - start) / view.duration * 100)}%;--person-color:${color}" title="${escapeHtml(title)}" aria-label="${escapeHtml(title)}"><b>${escapeHtml(label)}</b><span>${review ? "需复核" : "出镜"}</span></button>`;
    }).join("");
    return `<div class="timeline-person-row${focused ? " focused" : ""}${muted ? " muted" : ""}" data-timeline-person-row="${escapeHtml(personId)}" style="--person-row:${rowIndex};--person-color:${color}">${segments || '<span class="timeline-person-row-empty">当前范围无出镜</span>'}</div>`;
  }).join("");
  labelsRoot.querySelectorAll("[data-timeline-person-focus]").forEach((button) => button.addEventListener("click", () => {
    const personId = button.closest("[data-timeline-person-label-ref]")?.dataset.timelinePersonLabelRef;
    previewCurrentPerson(persons.find((person) => String(person.id) === String(personId)));
  }));
  labelsRoot.querySelectorAll("[data-timeline-person-select]").forEach((input) => input.addEventListener("change", () => {
    if (input.checked) selectedCurrentPersons.add(String(input.value));
    else selectedCurrentPersons.delete(String(input.value));
    if (selectedCurrentPersons.size <= 1) currentPersonMatchMode = "any";
    renderCurrentPersons();
  }));
  segmentsRoot.querySelectorAll("[data-timeline-person-range]").forEach((button) => button.addEventListener("click", () => {
    const person = persons.find((item) => String(item.id) === String(button.dataset.timelinePersonRef));
    previewCurrentPerson(person, Number(button.dataset.personStart), Number(button.dataset.personEnd));
  }));
  updateTimelinePersonPlayhead();
}

function updateTimelinePersonPlayhead() {
  const root = $("#timelinePersonTrack");
  const playhead = $("#timelinePersonPlayhead");
  if (!root || root.classList.contains("hidden") || !playhead) return;
  const duration = timelineDurationValue();
  const value = Math.max(0, Math.min(duration || 0, timelineAbsoluteTime()));
  const view = timelineViewRange();
  const outsideView = duration <= 0 || value < view.start || value > view.end;
  playhead.style.left = duration > 0 ? `${timelinePercentInView(value)}%` : "0%";
  playhead.classList.toggle("hidden", outsideView);
  root.querySelectorAll("[data-timeline-person-range]").forEach((segment) => {
    const active = !outsideView && value >= Number(segment.dataset.personStart) && value <= Number(segment.dataset.personEnd);
    segment.classList.toggle("playback-active", active);
  });
}

function renderTimelineSpeakerTrack() {
  const root = $("#timelineSpeakerTrack");
  const segmentsRoot = $("#timelineSpeakerSegments");
  const labelsRoot = $("#timelineSpeakerTrackLabels");
  const count = $("#timelineSpeakerTrackCount");
  if (!root || !segmentsRoot || !labelsRoot || !count) return;
  const sameJob = Boolean(currentJob?.id) && currentVoiceJobId === String(currentJob.id);
  const status = sameJob ? currentVoiceDiscoveryStatus : "not_started";
  const visible = !timelineOutputAxisActive() && ["loading", "running", "ready", "failed"].includes(status);
  root.classList.toggle("hidden", !visible);
  root.dataset.status = status;
  if (!visible) {
    segmentsRoot.innerHTML = "";
    labelsRoot.innerHTML = "";
    count.textContent = "0 人 · 0 段";
    return;
  }
  if (status === "loading" || status === "running") {
    count.textContent = status === "running" ? "识别中" : "读取中";
    root.style.setProperty("--speaker-rows", "1");
    labelsRoot.innerHTML = '<span class="timeline-speaker-label-state">分析中</span>';
    segmentsRoot.innerHTML = `<span class="timeline-speaker-state loading"><i></i>${status === "running" ? "正在区分不同声音并整理发言区间" : "正在读取当前视频的说话人结果"}</span>`;
    updateTimelineSpeakerPlayhead();
    return;
  }
  if (status === "failed") {
    count.textContent = "识别失败";
    root.style.setProperty("--speaker-rows", "1");
    labelsRoot.innerHTML = '<span class="timeline-speaker-label-state">不可用</span>';
    segmentsRoot.innerHTML = '<span class="timeline-speaker-state error">说话人轨道暂不可用，可在左侧重新识别</span>';
    updateTimelineSpeakerPlayhead();
    return;
  }
  const view = timelineViewRange();
  const voicesByRef = new Map(currentVoices.map((voice) => [String(voice.speakerRef), voice]));
  const speakerRefs = [...new Set([
    ...currentVoices.map((voice) => String(voice.speakerRef || "")),
    ...currentVoiceTimeline.map((turn) => String(turn.speakerRef || "")),
  ].filter(Boolean))];
  const colorIndex = new Map(speakerRefs.map((speakerRef, index) => [speakerRef, index % 6]));
  const speakerRows = speakerRefs.map((speakerRef) => {
    const voice = voicesByRef.get(speakerRef);
    const turns = currentVoiceTimeline.filter((turn) => String(turn.speakerRef || "") === speakerRef);
    const visibleTurns = turns
      .filter((turn) => Number(turn.end) > view.start && Number(turn.start) < view.end)
      .sort((left, right) => Number(left.start) - Number(right.start) || Number(left.end) - Number(right.end));
    return {
      speakerRef,
      label: String(voice?.label || turns[0]?.label || speakerRef || "未知声音"),
      turns,
      visibleTurns,
      color: `var(--voice-${colorIndex.get(speakerRef) ?? 0})`,
    };
  });
  const rowCount = Math.max(1, speakerRows.length);
  const activeVoiceSelection = voiceReviewMode === "correct"
    ? selectedVoiceCorrectionVoices
    : selectedCurrentVoices;
  root.style.setProperty("--speaker-rows", String(rowCount));
  count.textContent = `${speakerRows.length} 人 · ${currentVoiceTimeline.length} 段`;
  if (!speakerRows.length) {
    labelsRoot.innerHTML = '<span class="timeline-speaker-label-state">暂无</span>';
    segmentsRoot.innerHTML = '<span class="timeline-speaker-state">当前视频没有检测到可用发言</span>';
  } else {
    labelsRoot.innerHTML = speakerRows.map(({ speakerRef, label, turns, color }) => {
      const selected = activeVoiceSelection.has(speakerRef);
      const focused = focusedCurrentVoiceRef === speakerRef;
      const muted = (activeVoiceSelection.size > 0 || focusedCurrentVoiceRef) && !selected && !focused;
      const voice = voicesByRef.get(speakerRef);
      const selectionVerb = voiceReviewMode === "correct" ? "选择用于合并" : "选择用于成片";
      return `<div class="timeline-speaker-row-label${selected ? " selected" : ""}${focused ? " focused" : ""}${muted ? " muted" : ""}" data-timeline-speaker-label-ref="${escapeHtml(speakerRef)}" style="--voice-color:${color}"><input type="checkbox" data-timeline-speaker-select value="${escapeHtml(speakerRef)}" ${selected ? "checked" : ""} aria-label="${selectionVerb}${escapeHtml(label)}"><button type="button" data-timeline-speaker-focus title="聚焦 ${escapeHtml(label)}"><i></i><span><strong>${escapeHtml(label)}</strong><small>${Number(voice?.speechSeconds || 0).toFixed(1)} 秒 · ${turns.length} 段</small></span></button></div>`;
    }).join("");
    segmentsRoot.innerHTML = speakerRows.map(({ speakerRef, label, visibleTurns, color }, rowIndex) => {
      const selected = activeVoiceSelection.has(speakerRef);
      const focused = focusedCurrentVoiceRef === speakerRef;
      const muted = (activeVoiceSelection.size > 0 || focusedCurrentVoiceRef) && !selected && !focused;
      const segments = visibleTurns.map((turn) => {
        const start = Math.max(view.start, Number(turn.start) || 0);
        const end = Math.min(view.end, Number(turn.end) || start);
        const title = [label, `${formatTime(turn.start)} → ${formatTime(turn.end)}`, turn.overlapSeconds >= .15 ? "多人同时说话" : "", turn.requiresReview ? "建议人工复核" : "", turn.text || "无文字"].filter(Boolean).join(" · ");
        return `<button type="button" class="timeline-speaker-segment${selected ? " selected" : ""}${focused ? " focused" : ""}${muted ? " muted" : ""}${turn.requiresReview ? " review" : ""}${Number(turn.overlapSeconds || 0) >= .15 ? " overlap" : ""}" data-timeline-speaker-turn="${escapeHtml(turn.turnId)}" data-timeline-speaker-ref="${escapeHtml(speakerRef)}" data-speaker-start="${Number(turn.start) || 0}" data-speaker-end="${Number(turn.end) || 0}" style="left:${timelinePercentInView(start)}%;width:${Math.max(.3, (end - start) / view.duration * 100)}%;--voice-color:${color}" title="${escapeHtml(title)}" aria-label="${escapeHtml(title)}"><b>${escapeHtml(label)}</b><span>${escapeHtml(turn.text || "发言片段")}</span></button>`;
      }).join("");
      return `<div class="timeline-speaker-row${focused ? " focused" : ""}${muted ? " muted" : ""}" data-timeline-speaker-row="${escapeHtml(speakerRef)}" style="--speaker-row:${rowIndex};--voice-color:${color}">${segments || '<span class="timeline-speaker-row-empty">当前范围无发言</span>'}</div>`;
    }).join("");
  }
  labelsRoot.querySelectorAll("[data-timeline-speaker-focus]").forEach((button) => button.addEventListener("click", () => {
    focusCurrentVoice(button.closest("[data-timeline-speaker-label-ref]")?.dataset.timelineSpeakerLabelRef);
  }));
  labelsRoot.querySelectorAll("[data-timeline-speaker-select]").forEach((input) => input.addEventListener("change", () => {
    const selection = voiceReviewMode === "correct" ? selectedVoiceCorrectionVoices : selectedCurrentVoices;
    if (input.checked) selection.add(String(input.value));
    else selection.delete(String(input.value));
    const cardInput = document.querySelector(`[data-current-voice="${CSS.escape(String(input.value))}"] [data-current-voice-select]`);
    if (cardInput) cardInput.checked = input.checked;
    cardInput?.closest(".current-voice-card")?.classList.toggle("selected", input.checked);
    renderTimelineSpeakerTrack();
    syncCurrentVoiceControls();
  }));
  segmentsRoot.querySelectorAll("[data-timeline-speaker-turn]").forEach((button) => button.addEventListener("click", () => {
    const turn = currentVoiceTimeline.find((item) => String(item.turnId) === String(button.dataset.timelineSpeakerTurn));
    if (!turn) return;
    focusCurrentVoice(turn.speakerRef);
    previewCurrentVoice(Number(turn.start), Number(turn.end));
  }));
  updateTimelineSpeakerPlayhead();
}

function updateTimelineSpeakerPlayhead() {
  const root = $("#timelineSpeakerTrack");
  const playhead = $("#timelineSpeakerPlayhead");
  if (!root || root.classList.contains("hidden") || !playhead) return;
  const duration = timelineDurationValue();
  const value = Math.max(0, Math.min(duration || 0, timelineAbsoluteTime()));
  const view = timelineViewRange();
  const outsideView = duration <= 0 || value < view.start || value > view.end;
  playhead.style.left = duration > 0 ? `${timelinePercentInView(value)}%` : "0%";
  playhead.classList.toggle("hidden", outsideView);
  root.querySelectorAll("[data-timeline-speaker-turn]").forEach((segment) => {
    const active = !outsideView && value >= Number(segment.dataset.speakerStart) && value <= Number(segment.dataset.speakerEnd);
    segment.classList.toggle("playback-active", active);
  });
}

async function loadCurrentVoices() {
  clearTimeout(voiceDiscoveryTimer);
  if (!currentJob?.id) {
    resetCurrentVoiceState();
    return;
  }
  const jobId = String(currentJob.id);
  if (currentVoiceJobId !== jobId) resetCurrentVoiceState(jobId);
  if (!currentVoices.length && currentVoiceDiscoveryStatus !== "running") {
    renderCurrentVoices({ status: "loading" });
  }
  const requestGeneration = ++currentVoiceRequestGeneration;
  try {
    const result = await api(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices`);
    if (requestGeneration !== currentVoiceRequestGeneration || String(currentJob?.id || "") !== jobId) return;
    currentVoices = Array.isArray(result.voices) ? result.voices : [];
    if (currentJob?.narratorSelectionPending?.active && !selectedCurrentVoices.size) {
      selectedCurrentVoices = new Set(currentVoices
        .filter((voice) => ["confirmed", "candidate"].includes(String(voice.narration?.status || "")))
        .map((voice) => String(voice.speakerRef || ""))
        .filter(Boolean));
    }
    currentVoiceTimeline = Array.isArray(result.timeline) ? result.timeline : [];
    currentVoiceRevision = Number(result.revision || 0);
    currentVoiceCanUndo = Boolean(result.canUndo);
    const presentedStatus = voiceDiscoveryPresentationForJob(currentJob, result.status || {});
    renderCurrentVoices(result.status || {});
    if (secondaryEditorOpen() && workflowKindForJob(currentJob) === "speaker_edit") renderSecondaryEditorLibrary();
    if (presentedStatus.status === "running" && !$("#voiceProfilePanel")?.classList.contains("hidden")) {
      voiceDiscoveryTimer = setTimeout(loadCurrentVoices, 1500);
    }
  } catch (error) {
    if (requestGeneration !== currentVoiceRequestGeneration || String(currentJob?.id || "") !== jobId) return;
    currentVoices = [];
    currentVoiceTimeline = [];
    renderCurrentVoices({ status: "failed", error: error.message });
  }
}

async function discoverCurrentVoices() {
  if (!currentJob?.id) return setVoiceProfileStatus("请先打开一个视频任务。", "error");
  if (!interactionCapabilitiesForJob(currentJob).canCorrectVoice || voiceCorrectionBusy) {
    return setVoiceProfileStatus(unavailableInteractionMessage(currentJob, "当前已有声音操作正在处理"), "error");
  }
  const jobId = String(currentJob.id);
  const button = $("#discoverCurrentVoices");
  if (button) button.disabled = true;
  setVoiceProfileStatus("正在识别视频中的不同说话人…");
  try {
    const expectedSpeakerCount = Number($("#expectedVoiceCount")?.value || 0);
    const indexedSpeakerCount = Number($("#expectedVoiceCount")?.dataset.indexedCount || 0);
    if (currentVoices.length > 0 && expectedSpeakerCount === indexedSpeakerCount) {
      setVoiceProfileStatus("当前人数设置已经完成识别，无需重复处理。", "ready");
      if (button) button.disabled = true;
      return;
    }
    const result = await api(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices/discover`, {
      method: "POST",
      body: JSON.stringify({
        expectedSpeakerCount,
        force: currentVoices.length > 0 && expectedSpeakerCount !== indexedSpeakerCount,
      }),
    });
    if (String(currentJob?.id || "") !== jobId) return;
    if (result.job) renderJob(result.job);
    // This task was waiting for user input, so its previous polling loop has
    // stopped. Speaker discovery starts a new execution and must explicitly
    // restart the shared live-progress stream.
    void pollJob();
    await loadCurrentVoices();
  } catch (error) {
    if (String(currentJob?.id || "") !== jobId) return;
    setVoiceProfileStatus(error.message, "error");
    if (button) button.disabled = false;
  }
}

function previewCurrentVoice(start, end) {
  seekSourceTime(start);
  // Switching away from an output/event preview clears preview bounds. Apply
  // the voice-turn end after the source switch so playback still stops at the
  // end of the selected utterance.
  candidatePreviewEnd = Number.isFinite(end) ? end : null;
  window.setTimeout(() => safePlay(), 80);
}

async function labelCurrentVoice(speakerRef) {
  const voice = currentVoices.find((item) => item.speakerRef === speakerRef);
  const label = (await requestTextInput({
    kicker: "声音校正",
    title: "给这个声音命名",
    summary: "名称只在当前项目内用于区分说话人。",
    label: "说话人名称",
    value: voice?.label || "",
    required: true,
    confirmLabel: "保存名称",
  }))?.trim();
  if (!label || label === voice?.label) return;
  if (!beginCurrentVoiceMutation("声音命名")) return;
  const jobId = String(currentJob?.id || "");
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices/label`, {
      method: "PATCH", body: { speakerRef, label },
    });
    if (String(currentJob?.id || "") !== jobId) return;
    currentVoices = Array.isArray(result.voices) ? result.voices : currentVoices;
    renderCurrentVoices({ status: "ready" });
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
  finally { finishCurrentVoiceMutation(); }
}

async function setCurrentVoiceRole(speakerRef, role, { continuePendingSearch = true } = {}) {
  const jobId = String(currentJob?.id || "");
  if (!jobId || !speakerRef) return false;
  if (!beginCurrentVoiceMutation("声音角色校正")) return false;
  let continueSearch = false;
  let succeeded = false;
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices/role`, {
      method: "PATCH", body: { speakerRef, role },
    });
    if (String(currentJob?.id || "") !== jobId) return false;
    currentVoices = Array.isArray(result.voices) ? result.voices : currentVoices;
    if (role === "regular") selectedCurrentVoices.delete(speakerRef);
    renderCurrentVoices({ status: "ready" });
    if (role === "narrator") {
      setVoiceProfileStatus("已确认为本视频旁白；后续检索会直接复用这个 Speaker 的时间线。", "success");
      if (continuePendingSearch && currentJob?.narratorSelectionPending?.active) {
        selectedCurrentVoices = new Set([speakerRef]);
        continueSearch = true;
      }
    } else if (role === "regular") {
      setVoiceProfileStatus("已标记为普通说话人，不再作为疑似旁白推荐。", "success");
    } else {
      setVoiceProfileStatus("已取消旁白绑定，恢复为自动判断。", "success");
    }
    succeeded = true;
  } catch (error) {
    setVoiceProfileStatus(error.message, "error");
    return false;
  } finally {
    finishCurrentVoiceMutation();
  }
  if (continueSearch) await searchSelectedCurrentVoices("include");
  return succeeded;
}

async function confirmNarratorSelection() {
  if (!currentJob?.id || !selectedCurrentVoices.size) return;
  const selected = [...selectedCurrentVoices];
  for (const speakerRef of selected) {
    const confirmed = await setCurrentVoiceRole(speakerRef, "narrator", { continuePendingSearch: false });
    if (!confirmed) return;
  }
  await searchSelectedCurrentVoices("include");
}

async function searchSelectedCurrentVoices(forcedMode = "") {
  if (!currentJob?.id || !selectedCurrentVoices.size) return;
  const speakerRefs = [...selectedCurrentVoices];
  const mode = forcedMode || $("#currentVoiceMode")?.value || "include";
  if (mode === "qa_pair" && speakerRefs.length !== 1) return setVoiceProfileStatus("完整问答只能选择一个目标声音。", "error");
  if (!beginCurrentVoiceMutation("发言片段整理")) return;
  const actionToken = captureJobAction(currentJob);
  setVoiceProfileStatus(`正在按${currentVoiceRevision ? `人工校正后的时间轴 r${currentVoiceRevision}` : "当前声音时间轴"}整理发言…`);
  try {
    const pendingNarratorQuery = currentJob?.narratorSelectionPending?.active
      ? String(currentJob.narratorSelectionPending.query || "") : "";
    const result = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/content-search/target-speakers`, {
      method: "POST", body: { speakerRefs, mode, query: pendingNarratorQuery || $("#voiceSearchQuery")?.value.trim() || "" },
    });
    closeVoiceProfiles();
    const switched = result.action === "job-handoff";
    if (result.job && !commitJobAction(result.job, actionToken, { allowJobSwitch: switched })) return;
    clearTimeout(pollTimer);
    if (jobNeedsPolling(currentJob)) pollJob();
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
  finally { finishCurrentVoiceMutation(); }
}

async function mergeSelectedCurrentVoices() {
  const speakerRefs = [...selectedVoiceCorrectionVoices];
  if (!currentJob?.id || speakerRefs.length < 2) return;
  const target = currentVoices.find((item) => item.speakerRef === speakerRefs[0]);
  const selectedLabels = currentVoices
    .filter((item) => speakerRefs.includes(String(item.speakerRef)))
    .map((item) => String(item.label || item.speakerRef));
  const confirmed = await requestActionConfirmation({
    title: "合并说话人声音",
    summary: `确认 ${selectedLabels.join("、")} 属于同一个人？`,
    details: [
      `所有发言将归到“${target?.label || speakerRefs[0]}”`,
      "后续按说话人提取会立即使用合并后的时间轴",
      "可以使用“撤销上次校正”恢复",
    ],
    confirmLabel: "确认合并",
  });
  if (!confirmed) return;
  if (!beginCurrentVoiceMutation("合并说话人")) return;
  const jobId = String(currentJob.id);
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices/timeline`, {
      method: "PATCH", body: { operation: "merge", speakerRefs, targetSpeakerRef: speakerRefs[0], revision: currentVoiceRevision },
    });
    if (String(currentJob?.id || "") !== jobId) return;
    currentVoices = result.voices || []; currentVoiceTimeline = result.timeline || [];
    currentVoiceRevision = Number(result.revision || 0); currentVoiceCanUndo = Boolean(result.canUndo);
    selectedVoiceCorrectionVoices.clear(); renderCurrentVoices({ status: "ready" });
    setVoiceProfileStatus(`已合并 ${speakerRefs.length} 个声音 · 时间轴 r${currentVoiceRevision}；后续提取立即生效。`, "success");
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
  finally { finishCurrentVoiceMutation(); }
}

async function reassignSelectedVoiceTurns() {
  const turnIds = [...document.querySelectorAll("[data-voice-turn-select]:checked")].map((input) => String(input.value));
  if (!currentJob?.id || !turnIds.length) return setVoiceProfileStatus("请先勾选需要移动的发言。", "error");
  if (!beginCurrentVoiceMutation("移动发言归属")) return;
  const targetSpeakerRef = $("#voiceReassignTarget")?.value || "new";
  const label = targetSpeakerRef === "new" ? $("#voiceReassignLabel")?.value.trim() || "" : "";
  const selectedTurns = currentVoiceTimeline.filter((turn) => turnIds.includes(String(turn.turnId)));
  const sourceLabels = [...new Set(selectedTurns.map((turn) => String(turn.label || turn.speakerRef)))];
  const jobId = String(currentJob.id);
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices/timeline`, {
      method: "PATCH", body: { operation: "reassign", turnIds, targetSpeakerRef, label, revision: currentVoiceRevision },
    });
    if (String(currentJob?.id || "") !== jobId) return;
    currentVoices = result.voices || []; currentVoiceTimeline = result.timeline || [];
    currentVoiceRevision = Number(result.revision || 0); currentVoiceCanUndo = Boolean(result.canUndo);
    const movedTurns = currentVoiceTimeline.filter((turn) => turnIds.includes(String(turn.turnId)));
    const targetLabel = String(movedTurns[0]?.label || label || "新说话人");
    selectedCurrentVoices.clear();
    selectedVoiceCorrectionVoices.clear();
    renderCurrentVoices({ status: "ready" });
    if ($("#voiceReassignLabel")) $("#voiceReassignLabel").value = "";
    setVoiceProfileStatus(`已将 ${turnIds.length} 段发言从${sourceLabels.join("、")}移到“${targetLabel}” · 时间轴 r${currentVoiceRevision}；后续提取立即生效。`, "success");
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
  finally { finishCurrentVoiceMutation(); }
}

async function splitSelectedVoiceTurn() {
  const turnIds = [...document.querySelectorAll("[data-voice-turn-select]:checked")].map((input) => String(input.value));
  if (!currentJob?.id || turnIds.length !== 1) return setVoiceProfileStatus("请只勾选一段需要拆分的发言。", "error");
  const turn = currentVoiceTimeline.find((item) => String(item.turnId) === turnIds[0]);
  const splitTime = Number(timelineAbsoluteTime());
  if (!turn || splitTime <= Number(turn.start) + .1 || splitTime >= Number(turn.end) - .1) {
    return setVoiceProfileStatus("请先播放或拖动时间线，把播放点放在所选发言内部。", "error");
  }
  if (!beginCurrentVoiceMutation("拆分发言")) return;
  const targetSpeakerRef = $("#voiceReassignTarget")?.value || "new";
  const label = targetSpeakerRef === "new" ? $("#voiceReassignLabel")?.value.trim() || "" : "";
  const jobId = String(currentJob.id);
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices/timeline`, {
      method: "PATCH",
      body: { operation: "split", turnIds, splitTime, targetSpeakerRef, label, revision: currentVoiceRevision },
    });
    if (String(currentJob?.id || "") !== jobId) return;
    currentVoices = result.voices || [];
    currentVoiceTimeline = result.timeline || [];
    currentVoiceRevision = Number(result.revision || 0);
    currentVoiceCanUndo = Boolean(result.canUndo);
    renderCurrentVoices({ status: "ready" });
    setVoiceProfileStatus(`已在 ${formatTime(splitTime)} 拆分发言，后半段已移动到所选声音。`, "success");
  } catch (error) {
    setVoiceProfileStatus(error.message, "error");
  } finally {
    finishCurrentVoiceMutation();
  }
}

async function undoCurrentVoiceCorrection() {
  if (!currentJob?.id || !currentVoiceCanUndo) return;
  if (!beginCurrentVoiceMutation("撤销声音校正")) return;
  const jobId = String(currentJob.id);
  try {
    const result = await api(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voices/timeline/undo`, { method: "POST" });
    if (String(currentJob?.id || "") !== jobId) return;
    currentVoices = result.voices || []; currentVoiceTimeline = result.timeline || [];
    currentVoiceRevision = Number(result.revision || 0); currentVoiceCanUndo = Boolean(result.canUndo);
    selectedCurrentVoices.clear();
    selectedVoiceCorrectionVoices.clear();
    renderCurrentVoices({ status: "ready" });
    setVoiceProfileStatus(`已撤销最近一次校正；当前时间轴 r${currentVoiceRevision}。`, "success");
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
  finally { finishCurrentVoiceMutation(); }
}

function renderTemporaryVoiceSources() {
  const root = $("#temporaryVoiceSources");
  if (!root) return;
  root.innerHTML = temporaryVoiceSources.length ? temporaryVoiceSources.map((item) => `<label class="temporary-voice-source"><input type="checkbox" data-temporary-voice-source value="${escapeHtml(item.jobId)}"><span><strong>${escapeHtml(item.filename || "视频素材")}</strong><small>${formatTime(item.duration || 0)} · 独立生成检索结果</small></span></label>`).join("") : '<div class="voice-profile-empty">没有其他可用且包含音频的视频任务。</div>';
}

async function loadTemporaryVoiceSources() {
  if (!currentJob?.id) return;
  const jobId = String(currentJob.id);
  const requestGeneration = ++temporaryVoiceSourceRequestGeneration;
  try {
    const result = await api(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voice-sessions/sources`);
    if (requestGeneration !== temporaryVoiceSourceRequestGeneration || String(currentJob?.id || "") !== jobId) return;
    temporaryVoiceSources = result.sources || []; renderTemporaryVoiceSources();
  } catch (error) {
    if (requestGeneration !== temporaryVoiceSourceRequestGeneration || String(currentJob?.id || "") !== jobId) return;
    $("#temporaryVoiceSources").innerHTML = `<div class="voice-profile-empty">${escapeHtml(error.message)}</div>`;
  }
}

function renderTemporaryVoiceSession() {
  const root = $("#temporaryVoiceSession"); if (!root) return;
  if (!temporaryVoiceSession) { root.innerHTML = ""; return; }
  const active = ["queued", "running", "cancelling"].includes(temporaryVoiceSession.status);
  $("#cancelTemporaryVoiceSearch")?.classList.toggle("hidden", !active);
  root.innerHTML = `<section class="temporary-session-progress"><strong>${escapeHtml(temporaryVoiceSession.detail || "正在处理")}</strong><progress max="1" value="${Number(temporaryVoiceSession.progress || 0)}"></progress></section>${(temporaryVoiceSession.results || []).map((item) => `<article class="temporary-session-result ${escapeHtml(item.status || "running")}"><span><strong>${escapeHtml(item.filename || temporaryVoiceSources.find((source) => source.jobId === item.sourceJobId)?.filename || "目标视频")}</strong><small>${item.status === "ready" ? `找到 ${Number(item.matchedSegmentCount || 0)} 段${item.pendingReviewCount ? ` · ${item.pendingReviewCount} 段待复核` : ""}` : escapeHtml(item.detail || "正在检查")}</small></span>${item.resultJobId ? `<button type="button" data-open-temporary-result="${escapeHtml(item.resultJobId)}">打开结果</button>` : ""}</article>`).join("")}`;
  root.querySelectorAll("[data-open-temporary-result]").forEach((button) => button.addEventListener("click", async () => {
    const response = await api(`/api/jobs/${encodeURIComponent(button.dataset.openTemporaryResult)}`);
    closeVoiceProfiles(); switchWorkspaceJob(response.job); if (jobNeedsPolling(response.job)) pollJob();
  }));
}

async function pollTemporaryVoiceSession() {
  clearTimeout(temporaryVoiceSessionTimer);
  if (!currentJob?.id || !temporaryVoiceSession?.id) return;
  const jobId = String(currentJob.id);
  const sessionId = String(temporaryVoiceSession.id);
  try {
    const result = await api(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voice-sessions/${encodeURIComponent(sessionId)}`);
    if (String(currentJob?.id || "") !== jobId || String(temporaryVoiceSession?.id || "") !== sessionId) return;
    temporaryVoiceSession = result.session; renderTemporaryVoiceSession();
    if (["queued", "running", "cancelling"].includes(temporaryVoiceSession.status)) temporaryVoiceSessionTimer = setTimeout(pollTemporaryVoiceSession, 1500);
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
}

async function startTemporaryVoiceSearch() {
  const targets = [...document.querySelectorAll("[data-temporary-voice-source]:checked")].map((input) => input.value);
  const references = [...(voiceReviewMode === "correct" ? selectedVoiceCorrectionVoices : selectedCurrentVoices)];
  if (references.length !== 1) return setVoiceProfileStatus("临时跨视频查找需要在上方只选择一个参考声音。", "error");
  if (!targets.length) return setVoiceProfileStatus("请至少选择一个目标视频。", "error");
  const jobId = String(currentJob.id);
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voice-sessions`, { method: "POST", body: { referenceSpeakerRef: references[0], targetJobIds: targets, query: $("#voiceSearchQuery")?.value.trim() || "" } });
    if (String(currentJob?.id || "") !== jobId) return;
    temporaryVoiceSession = result.session; renderTemporaryVoiceSession(); pollTemporaryVoiceSession();
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
}

async function cancelTemporaryVoiceSearch() {
  if (!currentJob?.id || !temporaryVoiceSession?.id) return;
  const jobId = String(currentJob.id);
  const sessionId = String(temporaryVoiceSession.id);
  try {
    const result = await api(`/api/jobs/${encodeURIComponent(jobId)}/content-search/voice-sessions/${encodeURIComponent(sessionId)}/cancel`, { method: "POST" });
    if (String(currentJob?.id || "") !== jobId || String(temporaryVoiceSession?.id || "") !== sessionId) return;
    temporaryVoiceSession = result.session; renderTemporaryVoiceSession();
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
}

function renderVoiceProfiles() {
  const list = $("#voiceProfileList");
  if (!list) return;
  $("#voiceProfileCount").textContent = `${voiceProfiles.length} 个`;
  if (!voiceProfiles.length) {
    list.innerHTML = '<div class="voice-profile-empty">还没有声纹人物。请用当前视频的干净说话片段，或上传参考音频注册。</div>';
    return;
  }
  list.innerHTML = voiceProfiles.map((profile) => `<article class="voice-profile-card" data-voice-profile="${escapeHtml(profile.id)}"><div><strong>${escapeHtml(profile.label)}</strong><small>${Number(profile.speechSeconds || 0).toFixed(1)} 秒参考声音 · ${Number(profile.sampleCount || 0)} 个样本 · <span class="voice-profile-score">一致度 ${Number(profile.quality?.meanSimilarity || 0).toFixed(2)}</span></small></div><div class="voice-profile-actions"><button type="button" data-voice-search="${escapeHtml(profile.id)}">提取发言</button><button type="button" data-voice-append="${escapeHtml(profile.id)}" title="用上方当前选区或上传文件补充参考样本">补样本</button><button type="button" data-voice-rename="${escapeHtml(profile.id)}">改名</button><button type="button" data-voice-delete="${escapeHtml(profile.id)}">删除</button></div></article>`).join("");
  list.querySelectorAll("[data-voice-search]").forEach((button) => button.addEventListener("click", () => searchTargetVoice(button.dataset.voiceSearch)));
  list.querySelectorAll("[data-voice-append]").forEach((button) => button.addEventListener("click", () => submitVoiceEnrollment(button.dataset.voiceAppend)));
  list.querySelectorAll("[data-voice-rename]").forEach((button) => button.addEventListener("click", () => renameVoiceProfile(button.dataset.voiceRename)));
  list.querySelectorAll("[data-voice-delete]").forEach((button) => button.addEventListener("click", () => deleteVoiceProfile(button.dataset.voiceDelete)));
}

async function loadVoiceProfiles() {
  try {
    const result = await api("/api/voice-profiles");
    voiceProfiles = Array.isArray(result.profiles) ? result.profiles : [];
    renderVoiceProfiles();
  } catch (error) {
    voiceProfiles = [];
    const list = $("#voiceProfileList");
    if (list) list.innerHTML = `<div class="voice-profile-empty">${escapeHtml(error.status === 503 ? "跨视频人物库未启用；不影响上方发言剪辑。" : error.message)}</div>`;
    $("#voiceProfileCount").textContent = "不可用";
  }
}

function openVoiceProfiles() {
  closeSettings();
  closePersonProfiles();
  const panel = $("#voiceProfilePanel");
  const workbench = $("#reviewWorkbench");
  const actionDock = $("#reviewActionDock");
  if (panel && workbench) {
    const actionAnchor = actionDock?.parentElement === workbench ? actionDock : null;
    if (panel.parentElement !== workbench || (actionAnchor && panel.nextElementSibling !== actionAnchor)) {
      workbench.insertBefore(panel, actionAnchor);
    }
  }
  const closeButton = $("#closeVoiceProfiles");
  const workbenchHeader = workbench?.querySelector(".review-workbench-header");
  if (closeButton && workbenchHeader && closeButton.parentElement !== workbenchHeader) workbenchHeader.appendChild(closeButton);
  closeButton?.classList.remove("hidden");
  panel?.classList.remove("hidden");
  document.body.classList.add("voice-workspace-open");
  document.querySelectorAll("[data-open-speaker-workspace]").forEach((button) => {
    button.setAttribute("aria-expanded", "true");
  });
  setReviewLowerPanelMode("review");
  setVoiceReviewMode(Number(currentJob?.contentSearch?.candidates?.length || 0) ? "select" : "correct", { render: false });
  window.requestAnimationFrame(() => scheduleMediaFrameFit(true));
  loadCurrentVoices();
  if ($("#crossVideoVoiceSection")?.open) loadTemporaryVoiceSources();
}

function closeVoiceProfiles() {
  $("#voiceProfilePanel")?.classList.add("hidden");
  $("#closeVoiceProfiles")?.classList.add("hidden");
  document.body.classList.remove("voice-workspace-open");
  document.querySelectorAll("[data-open-speaker-workspace]").forEach((button) => {
    button.setAttribute("aria-expanded", "false");
  });
  syncReviewWorkbench();
  window.requestAnimationFrame(() => scheduleMediaFrameFit(true));
  clearTimeout(voiceDiscoveryTimer);
  clearTimeout(temporaryVoiceSessionTimer);
}

async function submitVoiceEnrollment(profileId = "") {
  const button = $("#enrollVoiceProfile");
  const form = new FormData();
  const existing = voiceProfiles.find((item) => item.id === profileId);
  const label = profileId ? existing?.label || "" : $("#voiceProfileLabel")?.value.trim() || "";
  if (!profileId && !label) return setVoiceProfileStatus("请先填写人物名称。", "error");
  form.set("label", label);
  if (voiceEnrollmentSource === "upload") {
    const file = $("#voiceProfileAudio")?.files?.[0];
    if (!file) return setVoiceProfileStatus("请选择参考音频文件。", "error");
    form.set("audio", file, file.name);
  } else {
    if (!currentJob?.id) return setVoiceProfileStatus("当前没有视频任务，请先打开一个视频。", "error");
    const start = Number($("#voiceRangeStart")?.value);
    const end = Number($("#voiceRangeEnd")?.value);
    if (!Number.isFinite(start) || !Number.isFinite(end) || end - start < 2) return setVoiceProfileStatus("选区至少需要 2 秒。", "error");
    form.set("jobId", currentJob.id);
    form.set("ranges", JSON.stringify([{ start, end }]));
  }
  if (button) button.disabled = true;
  setVoiceProfileStatus(profileId ? "正在补充样本并重新计算加密声纹…" : "正在提取并加密声纹，参考音频随后会被删除…");
  try {
    await api(profileId ? `/api/voice-profiles/${encodeURIComponent(profileId)}/enroll` : "/api/voice-profiles/enroll", { method: "POST", body: form });
    if (!profileId) $("#voiceProfileLabel").value = "";
    if ($("#voiceProfileAudio")) $("#voiceProfileAudio").value = "";
    await loadVoiceProfiles();
    setVoiceProfileStatus(profileId ? "参考样本已补充，原始音频已删除。" : "声纹人物已注册，原始音频已删除。", "success");
  } catch (error) {
    setVoiceProfileStatus(error.message, "error");
  } finally {
    if (button) button.disabled = false;
  }
}

async function renameVoiceProfile(profileId) {
  const profile = voiceProfiles.find((item) => item.id === profileId);
  const label = (await requestTextInput({
    kicker: "声纹人物库",
    title: "修改声纹人物名称",
    summary: "只修改显示名称，不会重新计算已有声纹。",
    label: "人物名称",
    value: profile?.label || "",
    required: true,
    confirmLabel: "保存名称",
  }))?.trim();
  if (!label || label === profile?.label) return;
  try {
    await apiJson(`/api/voice-profiles/${encodeURIComponent(profileId)}`, { method: "PATCH", body: { label } });
    await loadVoiceProfiles();
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
}

async function deleteVoiceProfile(profileId) {
  const profile = voiceProfiles.find((item) => item.id === profileId);
  if (!await requestActionConfirmation({
    title: "删除声纹人物",
    summary: `确认删除“${profile?.label || "这个人物"}”的声纹？`,
    details: ["已有视频、检索结果和成片不会被删除。", "以后需要再次使用时，必须重新注册参考声音。"],
    confirmLabel: "删除声纹",
  })) return;
  try {
    await api(`/api/voice-profiles/${encodeURIComponent(profileId)}`, { method: "DELETE" });
    await loadVoiceProfiles();
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
}

async function searchTargetVoice(profileId) {
  const profile = voiceProfiles.find((item) => item.id === profileId);
  if (!currentJob?.id) return setVoiceProfileStatus("请先打开一个内容检索任务。", "error");
  if (String(currentJob.taskMode || "") !== "content_extract") return setVoiceProfileStatus("目标声音剪辑需要在“内容检索”任务中使用。", "error");
  if (["running", "queued", "rendering", "cancelling"].includes(String(currentJob.status || ""))) return setVoiceProfileStatus("当前任务正在处理，请稍后再试。", "error");
  setVoiceProfileStatus(`正在启动 ${profile?.label || "目标人物"} 的全片声纹检索…`);
  try {
    const result = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/content-search/target-voice`, {
      method: "POST", body: { profileId, query: $("#voiceSearchQuery")?.value.trim() || "" },
    });
    closeVoiceProfiles();
    if (result.job) renderJob(result.job);
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) { setVoiceProfileStatus(error.message, "error"); }
}

$("#settingsButton")?.addEventListener("click", openSettings);
$("#engineState")?.addEventListener("click", openSettings);
$("#closeSettings")?.addEventListener("click", closeSettings);
$("#settingsBackdrop")?.addEventListener("click", closeSettings);
$("#closeVoiceProfiles")?.addEventListener("click", closeVoiceProfiles);
$("#closePersonProfiles")?.addEventListener("click", closePersonProfiles);
$("#searchSelectedPersons")?.addEventListener("click", searchSelectedCurrentPersons);
$("#mergeSelectedPersons")?.addEventListener("click", mergeSelectedCurrentPersons);
$("#splitSelectedPersonRanges")?.addEventListener("click", splitSelectedCurrentPersonRanges);
$("#undoPersonMerge")?.addEventListener("click", undoCurrentPersonMerge);
$("#personReviewModeSwitch")?.querySelectorAll("[data-person-review-mode]").forEach((button) => {
  button.addEventListener("click", () => setPersonReviewMode(button.dataset.personReviewMode));
});
$("#currentPersonMatchModes")?.querySelectorAll("input").forEach((input) => input.addEventListener("change", () => {
  if (input.checked) currentPersonMatchMode = input.value;
  syncCurrentPersonControls();
}));
$("#currentPersonSpeakingOnly")?.addEventListener("change", (event) => {
  currentPersonActivity = event.currentTarget.checked ? "speaking" : "appearance";
  syncCurrentPersonControls();
});
$("#discoverCurrentVoices")?.addEventListener("click", discoverCurrentVoices);
$("#expectedVoiceCount")?.addEventListener("change", (event) => {
  const button = $("#discoverCurrentVoices");
  if (!button || currentVoiceDiscoveryStatus !== "ready" || !currentVoices.length) return;
  const changed = Number(event.currentTarget.value || 0)
    !== Number(event.currentTarget.dataset.indexedCount || 0);
  button.disabled = false;
  button.textContent = changed ? "按新人数重新识别" : "重新识别说话人";
  setVoiceProfileStatus(
    changed ? "人数设置已改变；重新识别会保留源视频，但会重建说话人分组。" : "当前人数设置已经完成识别。",
    changed ? "" : "ready",
  );
});
$("#searchSelectedVoices")?.addEventListener("click", () => searchSelectedCurrentVoices());
$("#currentVoiceMode")?.addEventListener("change", syncCurrentVoiceControls);
$("#mergeSelectedVoices")?.addEventListener("click", mergeSelectedCurrentVoices);
$("#voiceReassignTarget")?.addEventListener("change", syncCurrentVoiceControls);
$("#splitVoiceTurn")?.addEventListener("click", splitSelectedVoiceTurn);
$("#reassignVoiceTurns")?.addEventListener("click", reassignSelectedVoiceTurns);
$("#undoVoiceCorrection")?.addEventListener("click", undoCurrentVoiceCorrection);
$("#voiceReviewModeSwitch")?.querySelectorAll("[data-voice-review-mode]").forEach((button) => {
  button.addEventListener("click", () => setVoiceReviewMode(button.dataset.voiceReviewMode));
});
$("#startTemporaryVoiceSearch")?.addEventListener("click", startTemporaryVoiceSearch);
$("#cancelTemporaryVoiceSearch")?.addEventListener("click", cancelTemporaryVoiceSearch);
$("#crossVideoVoiceSection")?.addEventListener("toggle", (event) => {
  if (event.currentTarget.open) loadTemporaryVoiceSources();
});
$("#discoverVisionModels")?.addEventListener("click", discoverAvailableVisionModels);
$("#visionSettingsForm")?.addEventListener("submit", saveVisionConfiguration);
document.querySelectorAll("[data-model-role]").forEach((button) => button.addEventListener("click", () => setModelSettingsRole(button.dataset.modelRole)));
document.querySelectorAll("[data-llm-mode]").forEach((button) => button.addEventListener("click", () => {
  setLlmMode(button.dataset.llmMode);
  markSettingsDirty("llm");
}));
$("#discoverLlmModels")?.addEventListener("click", discoverAvailableLlmModels);
$("#llmSettingsForm")?.addEventListener("submit", saveLlmConfiguration);
[$("#visionSettingsForm"), $("#llmSettingsForm")].forEach((form) => {
  if (!form) return;
  const role = form.id === "llmSettingsForm" ? "llm" : "vision";
  form.addEventListener("input", () => markSettingsDirty(role));
  form.addEventListener("change", () => markSettingsDirty(role));
});
$("#discardVisionSettings")?.addEventListener("click", discardVisionSettingsChanges);
$("#discardLlmSettings")?.addEventListener("click", discardLlmSettingsChanges);
$("#toggleVisionKey")?.addEventListener("click", () => {
  const input = $("#visionApiKey");
  if (!input) return;
  input.type = input.type === "password" ? "text" : "password";
  $("#toggleVisionKey").textContent = input.type === "password" ? "显示" : "隐藏";
});
$("#toggleLlmKey")?.addEventListener("click", () => {
  const input = $("#llmApiKey");
  if (!input) return;
  input.type = input.type === "password" ? "text" : "password";
  $("#toggleLlmKey").textContent = input.type === "password" ? "显示" : "隐藏";
});
$("#toggleManualVisionModel")?.addEventListener("click", () => {
  const field = $("#visionManualModelField");
  if (!field) return;
  field.classList.toggle("hidden");
  $("#toggleManualVisionModel").textContent = field.classList.contains("hidden") ? "找不到模型？手动填写模型 ID" : "返回模型列表";
  if (!field.classList.contains("hidden")) $("#visionModelManual")?.focus();
  markSettingsDirty("vision");
});
$("#toggleManualLlmModel")?.addEventListener("click", () => {
  const field = $("#llmManualModelField");
  if (!field) return;
  field.classList.toggle("hidden");
  $("#toggleManualLlmModel").textContent = field.classList.contains("hidden") ? "找不到模型？手动填写模型 ID" : "返回模型列表";
  if (!field.classList.contains("hidden")) $("#llmModelManual")?.focus();
  markSettingsDirty("llm");
});
$("#visionApiKey")?.addEventListener("input", () => { visionVerifiedAt = null; setVisionConnectionStatus("密钥已修改，请重新验证连接。", "neutral"); });
$("#visionBaseUrl")?.addEventListener("input", () => { visionVerifiedAt = null; setVisionConnectionStatus("接口地址已修改，请重新验证连接。", "neutral"); });
$("#llmApiKey")?.addEventListener("input", () => { llmVerifiedAt = null; setLlmConnectionStatus("密钥已修改，请重新验证连接。", "neutral"); });
$("#llmBaseUrl")?.addEventListener("input", () => { llmVerifiedAt = null; setLlmConnectionStatus("接口地址已修改，请重新验证连接。", "neutral"); });
document.addEventListener("keydown", (event) => { if (event.key === "Escape" && !$("#settingsPanel")?.classList.contains("hidden")) closeSettings(); });

$("#replaceButton")?.addEventListener("click", () => {
  if (currentCandidate || currentEventSegment) return void deleteTimelineItem();
});

$("#keepButton")?.addEventListener("click", () => {
  if (currentCandidate || currentEventSegment || currentEventGroup) downloadCurrentFragment();
});

let localCapabilityChecked = false;
function renderLocalCapability(capability) {
  const status = $("#localCapabilityStatus");
  const detail = $("#localCapabilityDetail");
  if (!status || !detail) return;
  const labels = { available: "可用", not_installed: "未安装完整", unchecked: "待检查", unavailable: "不可用", disabled: "已关闭" };
  status.textContent = capability?.label || labels[capability?.status] || "状态未知";
  detail.textContent = capability?.detail || "请点击“检查能力”确认安装和运行状态。";
  $("#localCapabilities").dataset.status = capability?.status || "unknown";
}
$("#checkLocalCapabilities")?.addEventListener("click", async (event) => {
  const button = event.currentTarget;
  button.disabled = true;
  button.textContent = "检查中…";
  $("#localCapabilityStatus").textContent = "正在检查依赖和计算设备";
  try {
    const response = await api("/api/capabilities/local");
    if (!response?.talknet?.status) throw new Error("未收到有效的能力状态，请确认后端已更新并重启。");
    localCapabilityChecked = true;
    renderLocalCapability(response.talknet);
  } catch {
    localCapabilityChecked = false;
    renderLocalCapability({ status: "unknown", label: "检查未完成", detail: "无法完成能力检查，请确认服务已更新并重启，然后重试。" });
  } finally {
    button.disabled = false;
    button.textContent = "重新检查";
  }
});

$("#refreshSetupReadiness")?.addEventListener("click", () => loadSetupReadiness());
$("#setupReadinessSteps")?.addEventListener("click", (event) => {
  const action = event.target.closest("[data-setup-action]")?.dataset.setupAction;
  if (["vision", "llm", "agent", "system"].includes(action)) setModelSettingsRole(action);
  if (action === "runtime") {
    const runtime = $("#settingsPanel .runtime-settings-summary");
    setModelSettingsRole("system");
    if (runtime) requestAnimationFrame(() => { runtime.open = true; runtime.scrollIntoView({ block: "nearest" }); });
  }
});
$("#probeEffectiveAgent")?.addEventListener("click", () => window.ClipTalkAgentSettings?.probeEffectiveAgent());

async function loadHealth() {
  if (document.hidden) return;
  try {
    const health = await api("/api/health");
    if (!localCapabilityChecked) renderLocalCapability(health.localCapabilities?.talknet);
    const visionConfigured = health.visionConfigured ?? health.arkConfigured;
    const visionModel = health.visionModel || health.arkModel;
    const visionProvider = health.visionProviderLabel || "视觉模型接口";
    const llmModel = health.llmModel || "未配置";
    const llmProvider = health.llmUsesVision ? "复用视觉配置" : (health.llmProviderLabel || "剪辑规划接口");
    const settingsModel = $("#settingsModel");
    if (settingsModel) settingsModel.textContent = visionConfigured ? `${visionModel} · ${visionProvider}` : "未配置";
    const settingsBackend = $("#settingsBackend");
    if (settingsBackend) settingsBackend.textContent = visionConfigured ? visionProvider : "未配置";
    const settingsLlm = $("#settingsLlmModel");
    if (settingsLlm) settingsLlm.textContent = health.llmConfigured ? `${llmModel} · ${llmProvider}` : "未配置";
    const settingsFfmpeg = $("#settingsFfmpeg");
    if (settingsFfmpeg) settingsFfmpeg.textContent = health.ffmpeg && health.ffprobe ? "FFmpeg 可用" : "不可用";
    const speechStatus = { ready: "已就绪", preparing: "后台准备中", failed: "准备失败", not_started: "等待准备" }[health.speechModelStatus] || health.speechModelStatus;
    const settingsSpeech = $("#settingsSpeech");
    if (settingsSpeech) settingsSpeech.textContent = `${health.senseVoiceModel || health.speechEngine} · ${speechStatus}`;
    const settingsSpeechDevice = $("#settingsSpeechDevice");
    if (settingsSpeechDevice) settingsSpeechDevice.textContent = health.speechDevice || "自动选择";
    void loadSetupReadiness();
  } catch {
    ["#settingsModel", "#settingsLlmModel", "#settingsBackend", "#settingsFfmpeg", "#settingsSpeech", "#settingsSpeechDevice"].forEach((selector) => {
      const node = $(selector);
      if (node) node.textContent = selector === "#settingsFfmpeg" ? "未知" : selector === "#settingsSpeechDevice" ? "未知" : "服务连接失败";
    });
  }
}

let secondaryEditSession = null;
let secondaryEditSelectedClips = new Set();
let secondaryEditView = "sequence";
let secondaryEditSequenceIndex = 0;
let secondaryEditExactPreview = false;
let secondaryEditDraggedClipId = null;
let secondaryEditBusy = false;
let secondaryEditTimelinePixelsPerSecond = null;
let secondaryEditPlayheadTime = 0;
let secondaryEditSnapEnabled = true;
let secondaryEditTrimDrag = null;
let secondaryEditCueDrag = null;
let secondaryEditSubtitleCanvasDrag = null;
let secondaryEditSubtitleDraft = null;
let secondaryEditSubtitleLoadingId = null;
let secondaryEditSubtitleSaveTimer = null;
let secondaryEditSubtitleSaving = false;
let secondaryEditSubtitleNeedsReview = false;
let secondaryEditInspectorDraft = null;
let secondaryEditSelectionAnchorId = null;
let secondaryEditDropIndex = null;
let secondaryEditDraggedMaterial = null;
let secondaryEditSelectedMarkerId = null;
let secondaryEditSelectedCueId = null;
let secondaryEditSelectedTextLayerId = null;
let secondaryEditTextLayerDrag = null;
let secondaryEditTextTimeDrag = null;
let secondaryEditPreviewRequestToken = 0;
let secondaryEditPreviewPending = false;
let secondaryEditSourceTime = 0;
let secondaryEditInsertionIndexOverride = null;
let secondaryEditPreviewMaterialId = null;
let secondaryEditPreviewMaterialRange = null;

let secondaryEditActivationToken = 0;

function secondaryEditorOpen() {
  return !$("#secondaryEditor")?.classList.contains("hidden");
}

function secondaryEditClip(clipId = null) {
  const id = clipId || [...secondaryEditSelectedClips][0];
  return secondaryEditSession?.clips?.find((clip) => String(clip.id) === String(id)) || null;
}

function setSecondaryEditorBusy(busy, label = "", operation = "working") {
  secondaryEditBusy = Boolean(busy);
  $("#secondaryEditor")?.querySelectorAll("button,input,select,textarea").forEach((node) => {
    if (node.closest("video")) return;
    node.disabled = (
      (secondaryEditBusy && node.id !== "secondaryEditorClose")
      || node.dataset.secondaryAlwaysDisabled === "true"
    );
  });
  if (label) {
    const state = $("#secondaryEditorSaveState");
    if (state) {
      state.textContent = label;
      state.dataset.operation = busy ? operation : "idle";
      if (operation === "save") state.dataset.state = busy ? "saving" : "saved";
    }
  }
  syncSecondaryEditorControls();
}

function secondaryEditorWorkflowLabel(workflow) {
  return ({ highlight: "高光候选", content_search: "内容检索结果", person_edit: "人物出镜结果", speaker_edit: "说话人发言结果" })[workflow] || "可用片段";
}

function secondaryEditorWorkflowContext(workflow) {
  return ({
    highlight: "已继承高光模式，只显示事件候选、未采用镜头和当前时间线来源。",
    content_search: "已继承内容检索，当前结果、历史保留结果和低置信候选会分组显示。",
    person_edit: "已继承人物模式，只整理所选人物的出镜轨迹及需要复核的匹配。",
    speaker_edit: "已继承说话人模式，只整理目标声音的发言片段及需要复核的轮次。",
  })[workflow] || "这里只显示当前任务可追溯的源视频片段。";
}

function secondaryEditorMaterialClipIds(item, session = secondaryEditSession) {
  const direct = Array.isArray(item?.timelineClipIds) ? item.timelineClipIds.map(String) : [];
  const referenceKind = String(item?.sourceRef?.kind || "");
  const referenceId = String(item?.sourceRef?.id || "");
  const start = Number(item?.start);
  const end = Number(item?.end);
  const matches = (session?.clips || []).filter((clip) => {
    if (direct.includes(String(clip.id))) return true;
    const clipKind = String(clip.sourceRef?.kind || "");
    const clipId = String(clip.sourceRef?.id || "");
    const originIds = [clip.origin?.candidateId, clip.origin?.matchId].map(String).filter(Boolean);
    const referenceMatch = referenceId && (
      (clipKind === referenceKind && clipId === referenceId)
      || clipId === referenceId
      || originIds.includes(referenceId)
    );
    const rangeMatch = Number.isFinite(start) && Number.isFinite(end)
      && Math.abs(Number(clip.sourceStart) - start) <= .035
      && Math.abs(Number(clip.sourceEnd) - end) <= .035;
    return referenceMatch || rangeMatch;
  }).map((clip) => String(clip.id));
  return [...new Set([...direct, ...matches])];
}

function secondaryEditorMaterialGroups(job = currentJob, session = secondaryEditSession) {
  const workflow = String(session?.workflowKind || workflowKindForJob(job));
  const groupDescriptions = {
    timeline: "正在成片时间线上使用，可再次插入或从草稿移除",
    recommended: ({
      highlight: "当前模式发现但尚未采用的高质量事件与镜头",
      content_search: "当前检索中证据充分、尚未采用的结果",
      person_edit: "所选人物尚未采用的连续出镜轨迹",
      speaker_edit: "目标说话人尚未采用的可靠发言片段",
    })[workflow] || "当前模式建议补入的源片段",
    kept: workflow === "content_search"
      ? "历史检索结果和你此前选中的片段"
      : "已由你确认，但尚未放入当前时间线的片段",
    review: "低置信候选、已排除内容和从本草稿移除的片段",
  };
  const groups = [
    { id: "timeline", title: "当前成片素材", description: groupDescriptions.timeline, items: [], open: true },
    { id: "recommended", title: "推荐补充", description: groupDescriptions.recommended, items: [], open: true },
    { id: "kept", title: "历史已选素材", description: groupDescriptions.kept, items: [], open: false },
    { id: "review", title: "待确认与已移除", description: groupDescriptions.review, items: [], open: false },
  ];
  const groupLookup = new Map(groups.map((group) => [group.id, group]));
  const groupKeys = new Map(groups.map((group) => [group.id, new Set()]));
  const catalogRanges = new Set();
  const validRange = (start, end) => Number.isFinite(start) && Number.isFinite(end) && end > start;
  const rangeKey = (start, end) => `${Number(start).toFixed(3)}:${Number(end).toFixed(3)}`;
  const sourceIdFor = (item, start, end) => String(item?.id ?? item?.candidateId ?? item?.turnId ?? item?.index ?? rangeKey(start, end));
  const add = (groupId, raw, options = {}) => {
    const start = Number(raw?.sourceStart ?? raw?.start);
    const end = Number(raw?.sourceEnd ?? raw?.end);
    if (!validRange(start, end)) return null;
    const sourceKind = String(options.sourceKind || raw?.sourceRef?.kind || "source_range");
    const sourceId = String(options.sourceId || raw?.sourceRef?.id || sourceIdFor(raw, start, end));
    const key = `${sourceKind}:${sourceId}:${rangeKey(start, end)}`;
    const seen = groupKeys.get(groupId);
    if (!seen || seen.has(key)) {
      const existing = groupLookup.get(groupId)?.items.find((item) => item.id === `${groupId}:${key}`);
      if (existing && options.timelineClipIds?.length) {
        existing.timelineClipIds = [...new Set([...(existing.timelineClipIds || []), ...options.timelineClipIds.map(String)])];
      }
      return existing || null;
    }
    seen.add(key);
    const item = {
      id: `${groupId}:${key}`,
      title: String(options.title || raw?.title || raw?.role || raw?.shotTitle || "可用片段"),
      start, end,
      reason: String(options.reason || raw?.reason || raw?.summary || raw?.transcriptExcerpt || "来自当前任务可追溯的源视频范围"),
      sourceRef: { ...(raw?.sourceRef || {}), kind: sourceKind, id: sourceId },
      sourceLabel: String(options.sourceLabel || "源视频"),
      state: String(options.state || groupId),
      timelineClipIds: (options.timelineClipIds || []).map(String),
      confidenceTier: String(options.confidenceTier || raw?.confidenceTier || ""),
    };
    groupLookup.get(groupId).items.push(item);
    return item;
  };
  const addCatalog = (raw, options = {}) => {
    const start = Number(raw?.start);
    const end = Number(raw?.end);
    if (!validRange(start, end)) return;
    const exactRange = rangeKey(start, end);
    if (catalogRanges.has(exactRange) && !options.allowDuplicateRange) return;
    const status = String(raw?.reviewStatus || "");
    const tier = String(raw?.confidenceTier || options.confidenceTier || (raw?.requiresReview ? "possible" : "reliable"));
    const retained = Boolean(options.retained || raw?.selected || status === "kept");
    let groupId = "recommended";
    let state = options.historical ? "historical" : "recommended";
    if (status === "rejected") { groupId = "review"; state = "rejected"; }
    else if (status === "pending" || (tier === "possible" && status !== "kept")) { groupId = "review"; state = "pending"; }
    else if (retained || options.historical) { groupId = "kept"; state = options.historical ? "historical" : "kept"; }
    const probe = {
      start, end,
      sourceRef: { kind: options.sourceKind || "content_match", id: options.sourceId || sourceIdFor(raw, start, end) },
    };
    if (secondaryEditorMaterialClipIds(probe, session).length) return;
    catalogRanges.add(exactRange);
    add(groupId, raw, { ...options, state, confidenceTier: tier });
  };

  (session?.clips || []).forEach((clip, index) => add("timeline", clip, {
    sourceKind: clip.sourceRef?.kind || "timeline_clip",
    sourceId: clip.sourceRef?.id || clip.id,
    title: clip.title || `片段 ${index + 1}`,
    reason: clip.sourceRef?.kind === "manual_range"
      ? "从源视频手动加入的范围"
      : "来自当前成片草稿，可继续裁剪、复制或调整顺序",
    sourceLabel: clip.sourceRef?.kind === "manual_range" ? "手动范围" : "当前成片",
    state: "timeline",
    timelineClipIds: [clip.id],
  }));

  if (workflow === "highlight") {
    (job?.eventGroups || []).forEach((event) => (event.segments || []).forEach((segment, index) => addCatalog(segment, {
      sourceKind: "event_segment",
      sourceLabel: `高光事件 · ${event.title || "未命名事件"}`,
      title: segment.title || segment.role || `${event.title || "高光事件"} · 镜头 ${index + 1}`,
      reason: segment.reason || event.summary || "来自高光分析的候选镜头",
      retained: Boolean(segment.selected || event.selected),
    })));
    (job?.candidates || []).forEach((candidate) => addCatalog(candidate, {
      sourceKind: "candidate", sourceLabel: "高光候选", retained: Boolean(candidate.selected),
    }));
  } else {
    const records = contentSearchRecordsForJob(job);
    const activeWithCandidates = records.find((record) => (
      String(record?.id || "") === String(job?.contentSearchSession?.activeSearchId || "")
      && ((record?.candidates || []).length || (record?.timelineCandidates || []).length)
    ));
    const currentSearchId = String(activeWithCandidates?.id || job?.contentSearch?.id || records.at(-1)?.id || "");
    const basketRefs = new Set(((job?.contentSelectionBasket || {}).items || []).map((item) => `${item.searchId}:${item.matchId}`));
    const orderedRecords = [...records].sort((left, right) => (
      Number(String(right?.id || "") === currentSearchId) - Number(String(left?.id || "") === currentSearchId)
    ));
    orderedRecords.forEach((record, recordIndex) => {
      const candidates = (record?.candidates || []).length ? record.candidates : (record?.timelineCandidates || []);
      const historical = String(record?.id || "") !== currentSearchId;
      const selectedIds = new Set([
        ...(record?.reviewDraft?.selectedMatchIds || []),
        ...(record?.defaultSelectedIds || []),
        ...(record?.confirmedMatchIds || []),
      ].map(String));
      const query = String(record?.intent?.query || record?.instruction || `检索 ${recordIndex + 1}`);
      candidates.forEach((candidate) => addCatalog(candidate, {
        sourceKind: "content_match",
        sourceLabel: `${historical ? "历史检索" : "当前检索"} · ${query}`,
        reason: candidate.reason || candidate.summary || candidate.transcriptExcerpt || `来自${historical ? "已保留的历史" : "当前"}检索“${query}”`,
        historical,
        retained: selectedIds.has(String(candidate.id)) || basketRefs.has(`${record.id}:${candidate.id}`),
      }));
    });

    if (workflow === "person_edit") {
      const targetIds = new Set((currentPersonTargetState(job).personIds || []).map(String));
      if (!targetIds.size) {
        records.flatMap((record) => record?.candidates || record?.timelineCandidates || []).forEach((candidate) => {
          (candidate?.matchedPersonIds || []).forEach((id) => targetIds.add(String(id)));
        });
      }
      currentPersonCatalog(job).filter((person) => targetIds.has(String(person.id))).forEach((person) => {
        validPersonRanges(person).forEach((range, index) => addCatalog({
          ...range, id: `${person.id}:${index}`,
          title: `${person.label || person.defaultLabel || "所选人物"} 出镜 · 第 ${index + 1} 段`,
        }, {
          sourceKind: "person_range",
          sourceLabel: `人物轨迹 · ${person.label || person.defaultLabel || person.id}`,
          reason: "来自已选人物的连续出镜轨迹，可直接补回成片",
          confidenceTier: person.reviewRecommended || range.requiresReview ? "possible" : "reliable",
        }));
      });
    }

    if (workflow === "speaker_edit" && String(currentVoiceJobId || "") === String(job?.id || "") && currentVoiceTimeline.length) {
      const selectedRefs = new Set([
        ...((job?.contentSearch?.intent || {}).speakerRefs || []),
        ...(job?.contentSearch?.selectedSpeakerRefs || []),
      ].map(String));
      currentVoiceTimeline.filter((turn) => !selectedRefs.size || selectedRefs.has(String(turn.speakerRef))).forEach((turn) => addCatalog({
        ...turn, id: turn.turnId,
        title: `${turn.label || turn.speakerRef || "目标说话人"} 发言`,
        confidenceTier: turn.requiresReview ? "possible" : "reliable",
        reviewStatus: turn.requiresReview ? "pending" : "confirmed",
      }, {
        sourceKind: "speaker_turn",
        sourceLabel: `说话人时间线 · ${turn.label || turn.speakerRef || "匿名声音"}`,
        reason: turn.text || "来自当前视频已校正的说话人发言时间线",
      }));
    }
  }

  const baseVersion = (job?.outputVersions || []).find((version) => String(version.id) === String(session?.baseVersionId));
  const baseOutput = (baseVersion?.outputs || []).find((output) => (
    !session?.baseOutputFilename || String(output.filename) === String(session.baseOutputFilename)
  ));
  (baseOutput?.segments || []).forEach((segment, index) => {
    const start = Number(segment.start);
    const end = Number(segment.end);
    const sourceId = sourceIdFor(segment, start, end);
    const probe = { start, end, sourceRef: { kind: "output_segment", id: sourceId } };
    if (secondaryEditorMaterialClipIds(probe, session).length) return;
    add("review", segment, {
      sourceKind: "output_segment", sourceId,
      sourceLabel: "已从草稿移除",
      title: segment.title || segment.role || `原成片片段 ${index + 1}`,
      reason: "该源视频范围来自初始成片，已从当前草稿移除，可重新插入恢复",
      state: "removed",
    });
  });

  groups.forEach((group) => group.items.sort((left, right) => left.start - right.start || left.end - right.end));
  return groups;
}

function renderSecondaryEditorLibrary() {
  const body = $("#secondaryEditorLibraryBody");
  if (!body || !currentJob) return;
  const workflow = String(secondaryEditSession?.workflowKind || workflowKindForJob(currentJob));
  const groups = secondaryEditorMaterialGroups(currentJob, secondaryEditSession);
  const materials = groups.flatMap((group) => group.items);
  const groupLookup = new Map(groups.map((group) => [group.id, group]));
  if (!groupLookup.has(secondaryEditorMaterialFilter)) {
    secondaryEditorMaterialFilter = groupLookup.get("recommended")?.items.length ? "recommended" : "timeline";
  }
  const query = secondaryEditorMaterialSearch.trim().toLocaleLowerCase("zh-CN");
  const matchesSearch = (item) => !query || [item.title, item.reason, item.sourceLabel, formatPreciseTimecode(item.start), formatPreciseTimecode(item.end)]
    .some((value) => String(value || "").toLocaleLowerCase("zh-CN").includes(query));
  $("#secondaryEditorLibraryTitle").textContent = secondaryEditorWorkflowLabel(workflow);
  $("#secondaryEditorLibraryCount").textContent = `${materials.length} 段`;
  $("#secondaryEditorLibraryFilters")?.querySelectorAll("[data-secondary-material-filter]").forEach((button) => {
    const group = groupLookup.get(button.dataset.secondaryMaterialFilter);
    const active = group?.id === secondaryEditorMaterialFilter;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
    const count = button.querySelector("b");
    if (count) count.textContent = String(group?.items.length || 0);
  });
  const context = $("#secondaryEditorLibraryContext");
  const sourceRangeActive = $("#secondaryEditor [data-secondary-library='source']")?.classList.contains("active");
  if (context) context.textContent = sourceRangeActive
    ? "输入源视频的真实入点与出点，片段只会作为虚拟范围加入时间线。"
    : secondaryEditorWorkflowContext(workflow);
  const itemMarkup = (item) => {
    const previewing = String(item.id) === String(secondaryEditPreviewMaterialId);
    const usedClipIds = secondaryEditorMaterialClipIds(item);
    const stateLabel = ({
      timeline: "成片中", recommended: "建议补充", kept: "历史已选", historical: "历史结果",
      pending: "待确认", rejected: "已排除", removed: "已移除",
    })[item.state] || "源素材";
    const insertLabel = item.state === "removed" || item.state === "rejected"
      ? "恢复到时间线" : usedClipIds.length ? "再次插入" : "插入";
    return `<article draggable="true" tabindex="0" class="secondary-library-item${previewing ? " previewing" : ""}" data-secondary-material="${escapeHtml(item.id)}" aria-label="${escapeHtml(`${item.title}，源片 ${formatPreciseTimecode(item.start)} 到 ${formatPreciseTimecode(item.end)}`)}">
      <button class="secondary-library-thumb" data-secondary-material-preview type="button" aria-label="预览${escapeHtml(item.title)}" aria-pressed="${previewing}"><span class="secondary-clip-media" aria-hidden="true">${secondaryEditorClipMediaMarkup({ sourceStart: item.start, sourceEnd: item.end })}</span><i aria-hidden="true"></i><em>${previewing ? "正在预览" : "预览"}</em></button>
      <div class="secondary-library-copy"><strong title="${escapeHtml(item.title)}">${escapeHtml(item.title)}</strong><small>源片 ${formatPreciseTimecode(item.start)} → ${formatPreciseTimecode(item.end)}</small><span><b>${(item.end - item.start).toFixed(2)} 秒</b><b class="source">${escapeHtml(item.sourceLabel)}</b><b class="state-${escapeHtml(item.state)}">${stateLabel}</b>${usedClipIds.length ? `<b class="used">时间线 ${usedClipIds.length} 处</b>` : ""}</span></div>
      <details class="secondary-library-reason"><summary>查看依据</summary><p>${escapeHtml(item.reason)}</p></details>
      <footer class="${usedClipIds.length ? "has-used-clips" : ""}"><button class="primary" data-secondary-material-insert type="button">${insertLabel}</button>${usedClipIds.length ? `<button class="secondary-library-remove" data-secondary-material-remove type="button">移出时间线${usedClipIds.length > 1 ? `（${usedClipIds.length}）` : ""}</button>` : ""}</footer>
    </article>`;
  };
  body.innerHTML = groups.map((group) => {
    const visibleItems = group.items.filter(matchesSearch);
    const emptyText = query
      ? "没有匹配当前搜索的素材"
      : group.id === "timeline" ? "当前时间线还没有片段" : group.id === "recommended" ? "暂时没有新的建议素材" : group.id === "kept" ? "还没有单独保留的素材" : "没有待确认或已移除的素材";
    return `<details class="secondary-library-group${group.id === secondaryEditorMaterialFilter ? " active" : ""}" data-secondary-material-group="${group.id}" ${group.id === secondaryEditorMaterialFilter ? "open" : ""}>
    <summary><span><strong>${group.title}</strong><small>${escapeHtml(group.description)}</small></span><b>${query ? `${visibleItems.length}/${group.items.length}` : group.items.length}</b></summary>
    <div class="secondary-library-group-items">${visibleItems.length ? visibleItems.map(itemMarkup).join("") : `<p class="secondary-library-group-empty">${emptyText}</p>`}</div>
  </details>`;
  }).join("");
  body.querySelectorAll("[data-secondary-material]").forEach((node) => {
    const item = materials.find((value) => value.id === node.dataset.secondaryMaterial);
    node.querySelectorAll("[data-secondary-material-preview]").forEach((button) => button.addEventListener("click", (event) => {
      event.stopPropagation();
      secondaryEditorPreviewMaterial(item);
    }));
    node.querySelector("[data-secondary-material-insert]")?.addEventListener("click", (event) => {
      event.stopPropagation();
      void secondaryEditorOperation({ type: "insert_clip", sourceStart: item.start, sourceEnd: item.end, title: item.title, sourceRef: item.sourceRef, targetIndex: secondaryEditorInsertionIndex() });
    });
    node.querySelector("[data-secondary-material-remove]")?.addEventListener("click", (event) => {
      event.stopPropagation();
      const clipIds = secondaryEditorMaterialClipIds(item);
      if (clipIds.length) void secondaryEditorOperation({ type: "delete_clips", clipIds });
    });
    node.addEventListener("click", (event) => { if (!event.target.closest("button, details, summary")) secondaryEditorPreviewMaterial(item); });
    node.addEventListener("keydown", (event) => {
      if (event.target !== node || (event.key !== "Enter" && event.key !== " ")) return;
      event.preventDefault();
      secondaryEditorPreviewMaterial(item);
    });
    node.addEventListener("dragstart", (event) => {
      secondaryEditDraggedMaterial = item;
      event.dataTransfer?.setData("application/x-videopilot-material", item.id);
      if (event.dataTransfer) event.dataTransfer.effectAllowed = "copy";
    });
    node.addEventListener("dragend", () => {
      secondaryEditDraggedMaterial = null;
      secondaryEditDropIndex = null;
      $("#secondaryEditorDropGuide")?.classList.add("hidden");
    });
  });
  syncSecondaryEditorInsertionHint();
}

function secondaryEditorAutomaticInsertionIndex() {
  const clips = secondaryEditSession?.clips || [];
  if (!clips.length) return 0;
  const selectedIndices = clips
    .map((clip, index) => secondaryEditSelectedClips.has(String(clip.id)) ? index : -1)
    .filter((index) => index >= 0);
  if (selectedIndices.length) return Math.max(...selectedIndices) + 1;
  if (Number.isInteger(secondaryEditSequenceIndex) && secondaryEditSequenceIndex >= 0 && secondaryEditSequenceIndex < clips.length) {
    return secondaryEditSequenceIndex + 1;
  }
  return clips.length;
}

function secondaryEditorInsertionIndex() {
  const clips = secondaryEditSession?.clips || [];
  if (Number.isInteger(secondaryEditInsertionIndexOverride)) {
    return Math.max(0, Math.min(clips.length, secondaryEditInsertionIndexOverride));
  }
  return secondaryEditorAutomaticInsertionIndex();
}

function secondaryEditorInsertionLocation(index = secondaryEditorInsertionIndex()) {
  const clips = secondaryEditSession?.clips || [];
  if (!clips.length || index <= 0) return "时间线开头";
  const clip = clips[Math.min(index, clips.length) - 1];
  const title = String(clip?.title || `片段 ${index}`);
  return `第 ${index} 段「${title}」之后${index === clips.length ? "（末尾）" : ""}`;
}

function syncSecondaryEditorInsertionHint() {
  const clips = secondaryEditSession?.clips || [];
  const index = secondaryEditorInsertionIndex();
  const location = secondaryEditorInsertionLocation(index);
  const target = $("#secondaryEditorInsertTarget");
  if (target) {
    const signature = clips.map((clip) => `${clip.id}:${clip.title || ""}`).join("|");
    if (target.dataset.signature !== signature) {
      target.innerHTML = [`<option value="0">时间线开头</option>`, ...clips.map((clip, clipIndex) => {
        const position = clipIndex + 1;
        const suffix = position === clips.length ? " · 末尾" : "";
        return `<option value="${position}">第 ${position} 段后 · ${escapeHtml(clip.title || `片段 ${position}`)}${suffix}</option>`;
      })].join("");
      target.dataset.signature = signature;
    }
    target.value = String(index);
    target.disabled = secondaryEditBusy;
  }
  const follow = $("#secondaryEditorInsertFollow");
  if (follow) {
    follow.disabled = secondaryEditBusy || secondaryEditInsertionIndexOverride === null;
    follow.classList.toggle("active", secondaryEditInsertionIndexOverride === null);
  }
  const hint = $("#secondaryEditorLibraryInsertHint");
  if (hint) hint.textContent = `${secondaryEditInsertionIndexOverride === null ? "跟随播放头" : "已固定位置"} · 将插入到${location}`;
  $("#secondaryEditorLibraryBody")?.querySelectorAll("[data-secondary-material-insert]").forEach((button) => {
    button.title = `插入到${location}`;
    button.setAttribute("aria-label", `插入到${location}`);
  });
  const rangeSubmit = $("#secondaryEditorRangeSubmit");
  if (rangeSubmit) {
    rangeSubmit.textContent = !clips.length ? "插入时间线开头" : `插入到第 ${index} 段之后`;
    rangeSubmit.title = `插入到${location}`;
  }
}

function secondaryEditorPreviewMaterial(item) {
  if (!item || !secondaryEditSession) return;
  if (secondaryEditorInspectorHasChanges()) return void showToast("请先保存或取消当前片段设置，再预览素材");
  secondaryEditPreviewMaterialId = String(item.id);
  secondaryEditPreviewMaterialRange = { start: Number(item.start), end: Number(item.end), title: String(item.title || "素材") };
  switchSecondaryEditorView("source", Number(item.start));
  $("#secondaryEditorVideoBadge").textContent = `素材预览 · ${item.title || "未命名片段"}`;
  $("#secondaryEditorLibraryBody")?.querySelectorAll("[data-secondary-material]").forEach((node) => {
    const active = String(node.dataset.secondaryMaterial) === secondaryEditPreviewMaterialId;
    node.classList.toggle("previewing", active);
    node.querySelectorAll("[data-secondary-material-preview]").forEach((button) => button.setAttribute("aria-pressed", String(active)));
    const thumbLabel = node.querySelector(".secondary-library-thumb>em");
    const replayButton = node.querySelector("footer [data-secondary-material-preview]");
    if (thumbLabel) thumbLabel.textContent = active ? "正在预览" : "预览";
    if (replayButton) replayButton.textContent = active ? "重新播放" : "预览";
  });
  const video = $("#secondaryEditorVideo");
  const play = () => {
    video.currentTime = Number(item.start);
    video.playbackRate = 1;
    video.play().catch(() => {});
  };
  if (video?.readyState >= 1) play(); else video?.addEventListener("loadedmetadata", play, { once: true });
}

function clearSecondaryEditorMaterialPreview() {
  secondaryEditPreviewMaterialId = null;
  secondaryEditPreviewMaterialRange = null;
  $("#secondaryEditorLibraryBody")?.querySelectorAll("[data-secondary-material]").forEach((node) => {
    node.classList.remove("previewing");
    node.querySelectorAll("[data-secondary-material-preview]").forEach((button) => button.setAttribute("aria-pressed", "false"));
    const thumbLabel = node.querySelector(".secondary-library-thumb>em");
    const replayButton = node.querySelector("footer [data-secondary-material-preview]");
    if (thumbLabel) thumbLabel.textContent = "预览";
    if (replayButton) replayButton.textContent = "预览";
  });
}

function secondaryEditorInspectorHasChanges() {
  return Boolean(secondaryEditInspectorDraft?.dirty);
}

function secondaryEditorMarkPreviewStale(message = "需更新预览") {
  secondaryEditPreviewRequestToken += 1;
  secondaryEditPreviewPending = false;
  if (secondaryEditSession) secondaryEditSession.previewStatus = "stale";
  if (secondaryEditExactPreview) setSecondaryEditorSourceVideo();
  const state = $("#secondaryEditorPreviewState");
  if (state) {
    state.textContent = message;
    state.dataset.tone = "stale";
    state.classList.remove("hidden");
  }
  syncSecondaryEditorControls();
}

function secondaryEditorPreviewIsCurrent(session = secondaryEditSession) {
  return Boolean(
    session
    && session.previewStatus === "ready"
    && Number(session.previewRevision) === Number(session.revision),
  );
}

function syncSecondaryEditorAiScope() {
  const scope = $("#secondaryEditorAiScope");
  const summary = $("#secondaryEditorAiScopeSummary");
  if (!scope || !summary) return;
  const selected = secondaryEditSelectedClips.size;
  const total = secondaryEditSession?.clips?.length || 0;
  if (!selected && scope.value === "selection") scope.value = "timeline";
  scope.querySelector('option[value="selection"]').disabled = !selected;
  summary.textContent = scope.value === "timeline"
    ? `将检查整个成片，共 ${total} 个片段`
    : `只检查当前选择的 ${selected} 个片段`;
  summary.dataset.tone = scope.value === "timeline" ? "timeline" : "selection";
}

function secondaryEditorPlayheadContext(outputTime = secondaryEditPlayheadTime) {
  const model = secondaryEditorTimelineModel();
  const time = Math.max(0, Math.min(model.duration, Number(outputTime) || 0));
  let index = Math.max(0, Math.min(secondaryEditSequenceIndex, model.clips.length - 1));
  model.schedule.forEach((item, candidateIndex) => {
    if (time >= Number(item.outputStart || 0) - .001 && time <= Number(item.outputEnd || 0) + .001) index = candidateIndex;
  });
  const clip = model.clips[index];
  const schedule = model.schedule[index];
  if (!clip || !schedule) return null;
  const local = Math.max(0, time - Number(schedule.outputStart || 0));
  return {
    clip,
    index,
    outputTime: time,
    sourceTime: Math.min(Number(clip.sourceEnd), Number(clip.sourceStart) + local * Math.max(.01, Number(clip.playbackRate || 1))),
  };
}

function secondaryEditorRoundTiming(value) {
  return Math.round((Number(value) || 0) * 1000) / 1000;
}

function secondaryEditorTimelineModel() {
  const storedClips = secondaryEditSession?.clips || [];
  const draft = secondaryEditInspectorDraft;
  const canPreviewDraft = Boolean(
    draft?.dirty && draft.validRange && !draft.batch
    && storedClips.some((clip) => String(clip.id) === String(draft.clipId)),
  );
  const clips = storedClips.map((clip) => {
    if (!canPreviewDraft || String(clip.id) !== String(draft.clipId)) return clip;
    const transitionType = String(draft.transitionType || "cut");
    const savedTransition = clip.transitionIn || { type: "cut", duration: 0 };
    const transitionDuration = transitionType === "cut"
      ? 0
      : Number.isFinite(Number(draft.transitionDuration)) && Number(draft.transitionDuration) > 0
        ? Number(draft.transitionDuration)
        : savedTransition.type === transitionType && Number(savedTransition.duration) > 0
        ? Number(savedTransition.duration)
        : .35;
    return {
      ...clip,
      sourceStart: draft.sourceStart,
      sourceEnd: draft.sourceEnd,
      playbackRate: draft.playbackRate,
      duration: secondaryEditorRoundTiming((draft.sourceEnd - draft.sourceStart) / draft.playbackRate),
      transitionIn: { type: transitionType, duration: transitionDuration },
      audioBridge: { ...(clip.audioBridge || {}), type: draft.audioBridgeType },
      audioGain: draft.audioGain,
      muted: draft.muted,
      audioFadeIn: draft.audioFadeIn,
      audioFadeOut: draft.audioFadeOut,
    };
  });
  const effectiveDurations = clips.map((clip) => secondaryEditorRoundTiming(
    Math.max(0, Number(clip.sourceEnd || 0) - Number(clip.sourceStart || 0))
      / Math.max(.01, Number(clip.playbackRate || 1)),
  ));
  let outputEnd = 0;
  const schedule = clips.map((clip, index) => {
    const effectiveDuration = effectiveDurations[index];
    const transition = clip.transitionIn || { type: "cut", duration: 0 };
    const transitionOverlap = index > 0 && ["dissolve", "fade_black"].includes(transition.type)
      ? secondaryEditorRoundTiming(Math.min(
        Math.max(0, Number(transition.duration || 0)),
        effectiveDurations[index - 1] / 3,
        effectiveDuration / 3,
      ))
      : 0;
    const outputStart = secondaryEditorRoundTiming(Math.max(0, outputEnd - transitionOverlap));
    outputEnd = secondaryEditorRoundTiming(outputStart + effectiveDuration);
    return {
      clipId: String(clip.id), outputStart, outputEnd,
      effectiveDuration, transitionOverlap,
    };
  });
  return {
    clips,
    schedule,
    duration: secondaryEditorRoundTiming(Math.max(0, ...schedule.map((item) => item.outputEnd))),
    preview: canPreviewDraft && Boolean(draft.timingChanged),
    draftClipId: canPreviewDraft ? String(draft.clipId) : "",
  };
}

function secondaryEditorSchedule() {
  return secondaryEditorTimelineModel().schedule;
}

function secondaryEditorTimelineDuration() {
  return secondaryEditorTimelineModel().duration;
}

function secondaryEditorTimelineTickStep(duration, width) {
  const targetTicks = Math.max(2, Math.floor(width / 82));
  const raw = duration / targetTicks;
  return [0.25, 0.5, 1, 2, 5, 10, 15, 30, 60, 120, 300, 600]
    .find((value) => value >= raw) || 1200;
}

function secondaryEditorRulerLabel(time, tickStep) {
  if (tickStep < 1) return `${Number(time).toFixed(tickStep < .5 ? 2 : 1)}s`;
  return formatClock(time);
}

function secondaryEditorFrameRate() {
  return contentBoundaryFrameRate(currentJob);
}

function secondaryEditorClipMediaMarkup(clip) {
  const sprite = timelineAssetsJobId === currentJob?.id ? timelineAssets?.sprite : null;
  if (!sprite?.items?.length || !timelineAssets?.spriteUrl) return '<span class="secondary-clip-media-placeholder"></span>';
  const start = Number(clip.sourceStart || 0);
  const end = Math.max(start, Number(clip.sourceEnd || start));
  const frameCount = 4;
  const spriteWidth = Math.max(1, Number(sprite.spriteWidth || 1));
  const spriteHeight = Math.max(1, Number(sprite.spriteHeight || 1));
  const tileWidth = Math.max(1, Number(sprite.tileWidth || 1));
  const tileHeight = Math.max(1, Number(sprite.tileHeight || 1));
  const spriteUrl = escapeHtml(timelineAssets.spriteUrl);
  return Array.from({ length: frameCount }, (_unused, index) => {
    const target = start + (end - start) * (index + .5) / frameCount;
    const item = sprite.items.reduce((best, current) => (
      Math.abs(Number(current.time) - target) < Math.abs(Number(best.time) - target) ? current : best
    ));
    const viewX = Math.max(0, Number(item.column || 0) * tileWidth);
    const viewY = Math.max(0, Number(item.row || 0) * tileHeight);
    return `<i class="secondary-clip-frame"><svg viewBox="${viewX} ${viewY} ${tileWidth} ${tileHeight}" preserveAspectRatio="xMidYMid slice" focusable="false"><image href="${spriteUrl}" width="${spriteWidth}" height="${spriteHeight}"></image></svg></i>`;
  }).join("");
}

function drawSecondaryEditorWaveforms() {
  const data = waveformJobId === currentJob?.id ? waveformData : null;
  const values = data?.maximums?.length ? data.maximums : data?.rms || [];
  const minimums = data?.minimums?.length ? data.minimums : null;
  const duration = Math.max(.01, Number(data?.duration || currentJob?.videoInfo?.duration || 0));
  $("#secondaryEditorTimeline")?.querySelectorAll("canvas[data-secondary-waveform]").forEach((canvas) => {
    const width = Math.max(1, Math.round(canvas.clientWidth));
    const height = Math.max(1, Math.round(canvas.clientHeight));
    const ratio = Math.min(2, window.devicePixelRatio || 1);
    canvas.width = Math.round(width * ratio);
    canvas.height = Math.round(height * ratio);
    const context = canvas.getContext("2d");
    if (!context || !values.length || data?.hasAudio === false) return;
    context.scale(ratio, ratio);
    context.clearRect(0, 0, width, height);
    const gain = Math.max(0, Math.min(2, Number(canvas.dataset.audioGain || 1)));
    const muted = canvas.dataset.muted === "true";
    const theme = getComputedStyle(canvas);
    context.strokeStyle = theme.getPropertyValue(muted ? "--ct-on-media" : "--ct-waveform-on-media").trim() || "#cbe7aa";
    context.globalAlpha = muted ? .45 : Math.min(1, .65 + gain * .25);
    context.lineWidth = 1;
    context.beginPath();
    const start = Number(canvas.dataset.sourceStart || 0);
    const end = Math.max(start, Number(canvas.dataset.sourceEnd || start));
    const peak = Math.max(.01, Number(data.normalizationPeak || 0), ...values.slice(0, 2000).map((value) => Math.abs(Number(value) || 0)));
    for (let x = 0; x < width; x += 1) {
      const time = start + (end - start) * x / Math.max(1, width - 1);
      const index = Math.max(0, Math.min(values.length - 1, Math.round(time / duration * (values.length - 1))));
      const high = Math.min(1, Math.abs(Number(values[index]) || 0) / peak);
      const low = minimums ? Math.min(1, Math.abs(Number(minimums[index]) || 0) / peak) : high;
      context.moveTo(x + .5, height / 2 - high * height * .42);
      context.lineTo(x + .5, height / 2 + low * height * .42);
    }
    context.stroke();
  });
}

function secondaryEditorMediaStateLabel() {
  const hasFrames = timelineAssetsJobId === currentJob?.id && Boolean(timelineAssets?.sprite?.items?.length);
  const hasWaveform = waveformJobId === currentJob?.id && Boolean(waveformData?.rms?.length || waveformData?.maximums?.length);
  if (hasFrames && hasWaveform) return "画面缩略图 · 音频波形";
  if (hasFrames) return waveformData?.hasAudio === false ? "画面缩略图 · 无音轨" : "画面缩略图 · 波形载入中";
  if (hasWaveform) return "音频波形 · 画面载入中";
  return "正在载入媒体索引";
}

function secondaryEditorShowSnapGuide(outputTime, label = "已吸附") {
  const guide = $("#secondaryEditorSnapGuide");
  const duration = Math.max(.01, secondaryEditorTimelineDuration());
  if (!guide) return;
  guide.style.left = `${Math.max(0, Math.min(100, Number(outputTime || 0) / duration * 100))}%`;
  const textNode = guide.querySelector("span");
  if (textNode) textNode.textContent = label;
  guide.classList.remove("hidden");
}

function secondaryEditorHideSnapGuide() {
  $("#secondaryEditorSnapGuide")?.classList.add("hidden");
}

function secondaryEditorSnapSourceTime(value, { altKey = false, clipId = "" } = {}) {
  const fps = secondaryEditorFrameRate();
  const frameValue = Math.round(Math.max(0, Number(value) || 0) * fps) / fps;
  if (!secondaryEditSnapEnabled || altKey) return { time: frameValue, label: `帧 ${Math.round(frameValue * fps)}` };
  const candidates = [];
  (timelineAssets?.sceneCuts || []).forEach((time) => candidates.push({ time: Number(time), label: "镜头切点" }));
  (waveformData?.silences || []).forEach((range) => {
    candidates.push({ time: Number(range.start), label: "静音开始" }, { time: Number(range.end), label: "静音结束" });
  });
  (secondaryEditSession?.clips || []).forEach((clip) => {
    if (String(clip.id) === String(clipId)) return;
    candidates.push({ time: Number(clip.sourceStart), label: "片段入点" }, { time: Number(clip.sourceEnd), label: "片段出点" });
  });
  const canvas = $("#secondaryEditorTimelineCanvas");
  const threshold = Math.max(3 / fps, secondaryEditorTimelineDuration() / Math.max(1, canvas?.clientWidth || 1) * 10);
  const match = candidates.filter((item) => Number.isFinite(item.time) && Math.abs(item.time - frameValue) <= threshold)
    .sort((left, right) => Math.abs(left.time - frameValue) - Math.abs(right.time - frameValue))[0];
  return match || { time: frameValue, label: `帧 ${Math.round(frameValue * fps)}` };
}

function beginSecondaryEditorTrim(event, clipId, boundary) {
  const clip = secondaryEditClip(clipId);
  const node = event.currentTarget.closest("[data-secondary-clip]");
  if (!clip || !node || secondaryEditBusy) return;
  if (secondaryEditorInspectorHasChanges()) {
    showToast("请先保存或取消当前片段设置");
    return;
  }
  event.preventDefault();
  event.stopPropagation();
  const clips = secondaryEditSession?.clips || [];
  const clipIndex = clips.findIndex((item) => String(item.id) === String(clipId));
  const adjacentIndex = boundary === "start" ? clipIndex - 1 : clipIndex + 1;
  const adjacentClip = clips[adjacentIndex] || null;
  const model = secondaryEditorTimelineModel();
  const schedule = model.schedule || [];
  secondaryEditTrimDrag = {
    clipId: String(clipId), boundary, node, startX: event.clientX,
    sourceStart: Number(clip.sourceStart), sourceEnd: Number(clip.sourceEnd),
    previewStart: Number(clip.sourceStart), previewEnd: Number(clip.sourceEnd),
    playbackRate: Math.max(.01, Number(clip.playbackRate || 1)),
    outputStart: Number(schedule[clipIndex]?.outputStart || 0),
    outputEnd: Number(schedule[clipIndex]?.outputEnd || 0),
    timelineDuration: Math.max(.01, Number(model.duration || secondaryEditSession.duration || 0)),
    adjacentClip,
    adjacentNode: adjacentClip ? $("#secondaryEditorTimeline")?.querySelector(`[data-secondary-clip="${CSS.escape(String(adjacentClip.id))}"]`) : null,
    adjacentSourceStart: Number(adjacentClip?.sourceStart || 0),
    adjacentSourceEnd: Number(adjacentClip?.sourceEnd || 0),
    adjacentPreviewStart: Number(adjacentClip?.sourceStart || 0),
    adjacentPreviewEnd: Number(adjacentClip?.sourceEnd || 0),
    adjacentPlaybackRate: Math.max(.01, Number(adjacentClip?.playbackRate || 1)),
    adjacentOutputStart: Number(schedule[adjacentIndex]?.outputStart || 0),
    adjacentOutputEnd: Number(schedule[adjacentIndex]?.outputEnd || 0),
    rollMode: Boolean(event.shiftKey && adjacentClip),
  };
  node.classList.add("trimming");
  secondaryEditTrimDrag.adjacentNode?.classList.toggle("roll-trimming", secondaryEditTrimDrag.rollMode);
  document.addEventListener("pointermove", moveSecondaryEditorTrim);
  document.addEventListener("pointerup", finishSecondaryEditorTrim, { once: true });
}

function moveSecondaryEditorTrim(event) {
  const drag = secondaryEditTrimDrag;
  if (!drag || !secondaryEditSession) return;
  const rollMode = Boolean(event.shiftKey && drag.adjacentClip);
  drag.rollMode = rollMode;
  drag.adjacentNode?.classList.toggle("roll-trimming", rollMode);
  drag.previewStart = drag.sourceStart;
  drag.previewEnd = drag.sourceEnd;
  drag.adjacentPreviewStart = drag.adjacentSourceStart;
  drag.adjacentPreviewEnd = drag.adjacentSourceEnd;
  const canvas = $("#secondaryEditorTimelineCanvas");
  const sourceDuration = Math.max(0, Number(currentJob?.videoInfo?.duration || 0));
  const outputDelta = (event.clientX - drag.startX) / Math.max(1, canvas?.clientWidth || 1) * drag.timelineDuration;
  const originalBoundary = drag.boundary === "start" ? drag.sourceStart : drag.sourceEnd;
  const raw = originalBoundary + outputDelta * drag.playbackRate;
  const snapped = secondaryEditorSnapSourceTime(raw, { altKey: event.altKey, clipId: drag.clipId });
  let actualBoundary = snapped.time;
  let outputBoundary = 0;
  if (rollMode) {
    let delta = (snapped.time - originalBoundary) / drag.playbackRate;
    if (drag.boundary === "start") {
      const minimum = Math.max(
        -drag.sourceStart / drag.playbackRate,
        (drag.adjacentSourceStart + .25 - drag.adjacentSourceEnd) / drag.adjacentPlaybackRate,
      );
      const maximum = Math.min(
        (drag.sourceEnd - .25 - drag.sourceStart) / drag.playbackRate,
        ((sourceDuration || Infinity) - drag.adjacentSourceEnd) / drag.adjacentPlaybackRate,
      );
      delta = Math.max(minimum, Math.min(maximum, delta));
      drag.previewStart = drag.sourceStart + delta * drag.playbackRate;
      drag.adjacentPreviewEnd = drag.adjacentSourceEnd + delta * drag.adjacentPlaybackRate;
      actualBoundary = drag.previewStart;
      outputBoundary = drag.outputStart + delta;
    } else {
      const minimum = Math.max(
        (drag.sourceStart + .25 - drag.sourceEnd) / drag.playbackRate,
        -drag.adjacentSourceStart / drag.adjacentPlaybackRate,
      );
      const maximum = Math.min(
        ((sourceDuration || Infinity) - drag.sourceEnd) / drag.playbackRate,
        (drag.adjacentSourceEnd - .25 - drag.adjacentSourceStart) / drag.adjacentPlaybackRate,
      );
      delta = Math.max(minimum, Math.min(maximum, delta));
      drag.previewEnd = drag.sourceEnd + delta * drag.playbackRate;
      drag.adjacentPreviewStart = drag.adjacentSourceStart + delta * drag.adjacentPlaybackRate;
      actualBoundary = drag.previewEnd;
      outputBoundary = drag.outputEnd + delta;
    }
  } else if (drag.boundary === "start") {
    drag.previewStart = Math.max(0, Math.min(drag.previewEnd - .25, snapped.time));
    actualBoundary = drag.previewStart;
    outputBoundary = drag.outputEnd - (drag.previewEnd - drag.previewStart) / drag.playbackRate;
  } else {
    drag.previewEnd = Math.min(sourceDuration || Infinity, Math.max(drag.previewStart + .25, snapped.time));
    actualBoundary = drag.previewEnd;
    outputBoundary = drag.outputStart + (drag.previewEnd - drag.previewStart) / drag.playbackRate;
  }
  const effectiveDuration = (drag.previewEnd - drag.previewStart) / drag.playbackRate;
  const previewOutputStart = drag.boundary === "start" ? outputBoundary : drag.outputStart;
  drag.node.style.width = `${Math.max(.05, effectiveDuration / drag.timelineDuration * 100)}%`;
  drag.node.style.left = `${Math.max(0, previewOutputStart) / drag.timelineDuration * 100}%`;
  const sourceLabel = drag.node.querySelector("[data-secondary-clip-source]");
  if (sourceLabel) sourceLabel.textContent = `源片 ${formatPreciseTimecode(drag.previewStart)} → ${formatPreciseTimecode(drag.previewEnd)}`;
  if (drag.adjacentClip && drag.adjacentNode) {
    const adjacentDuration = (drag.adjacentPreviewEnd - drag.adjacentPreviewStart) / drag.adjacentPlaybackRate;
    const adjacentOutputStart = rollMode && drag.boundary === "end" ? outputBoundary : drag.adjacentOutputStart;
    drag.adjacentNode.style.left = `${Math.max(0, adjacentOutputStart) / drag.timelineDuration * 100}%`;
    drag.adjacentNode.style.width = `${Math.max(.05, adjacentDuration / drag.timelineDuration * 100)}%`;
    const adjacentLabel = drag.adjacentNode.querySelector("[data-secondary-clip-source]");
    if (adjacentLabel) adjacentLabel.textContent = `源片 ${formatPreciseTimecode(drag.adjacentPreviewStart)} → ${formatPreciseTimecode(drag.adjacentPreviewEnd)}`;
  }
  const trimLabel = drag.node.querySelector("[data-secondary-trim-readout]");
  if (trimLabel) trimLabel.textContent = `${drag.boundary === "start" ? "源片入点" : "源片出点"} ${formatPreciseTimecode(actualBoundary)} · ${rollMode ? "滚动边界，总时长不变" : "当前片段，后续自动顺延"}`;
  secondaryEditorShowSnapGuide(outputBoundary, rollMode ? "Shift 滚动裁剪" : snapped.label);
  const video = $("#secondaryEditorVideo");
  if (video && !secondaryEditExactPreview) {
    try { video.currentTime = actualBoundary; } catch {}
  }
}

async function finishSecondaryEditorTrim() {
  const drag = secondaryEditTrimDrag;
  secondaryEditTrimDrag = null;
  document.removeEventListener("pointermove", moveSecondaryEditorTrim);
  secondaryEditorHideSnapGuide();
  if (!drag) return;
  drag.node.classList.remove("trimming");
  drag.adjacentNode?.classList.remove("roll-trimming");
  const changed = Math.abs(drag.previewStart - drag.sourceStart) > .0005
    || Math.abs(drag.previewEnd - drag.sourceEnd) > .0005;
  if (!changed) return renderSecondaryEditorTimeline();
  if (drag.rollMode && drag.adjacentClip) {
    await secondaryEditorOperation({
      type: "roll_trim", clipId: drag.clipId, boundary: drag.boundary,
      sourceStart: drag.previewStart, sourceEnd: drag.previewEnd,
      adjacentClipId: drag.adjacentClip.id,
      adjacentSourceStart: drag.adjacentPreviewStart,
      adjacentSourceEnd: drag.adjacentPreviewEnd,
    });
  } else {
    await secondaryEditorOperation({ type: "trim_clip", clipId: drag.clipId, sourceStart: drag.previewStart, sourceEnd: drag.previewEnd });
  }
}

function nudgeSecondaryEditorTrim(clipId, boundary, direction) {
  const clip = secondaryEditClip(clipId);
  if (!clip || secondaryEditBusy) return;
  const frame = 1 / secondaryEditorFrameRate();
  let start = Number(clip.sourceStart);
  let end = Number(clip.sourceEnd);
  if (boundary === "start") start = Math.max(0, Math.min(end - .25, start + direction * frame));
  else end = Math.min(Number(currentJob?.videoInfo?.duration || Infinity), Math.max(start + .25, end + direction * frame));
  void secondaryEditorOperation({ type: "trim_clip", clipId: clip.id, sourceStart: start, sourceEnd: end });
}

function secondaryEditorSnapOutputTime(value, { altKey = false, cueId = "" } = {}) {
  const fps = secondaryEditorFrameRate();
  const duration = Math.max(0, secondaryEditorTimelineDuration());
  const frameValue = Math.round(Math.max(0, Math.min(duration, Number(value) || 0)) * fps) / fps;
  if (!secondaryEditSnapEnabled || altKey) return { time: frameValue, label: `帧 ${Math.round(frameValue * fps)}` };
  const candidates = [];
  secondaryEditorSchedule().forEach((item) => {
    candidates.push({ time: Number(item.outputStart), label: "片段边界" }, { time: Number(item.outputEnd), label: "片段边界" });
  });
  (secondaryEditSubtitleDraft?.cues || []).forEach((cue) => {
    if (String(cue.id) === String(cueId)) return;
    candidates.push({ time: Number(cue.start), label: "字幕边界" }, { time: Number(cue.end), label: "字幕边界" });
  });
  (secondaryEditSession?.textLayers || []).forEach((layer) => {
    if (String(layer.id) === String(cueId)) return;
    candidates.push({ time: Number(layer.start), label: "文本边界" }, { time: Number(layer.end), label: "文本边界" });
  });
  const canvas = $("#secondaryEditorTimelineCanvas");
  const threshold = Math.max(3 / fps, duration / Math.max(1, canvas?.clientWidth || 1) * 10);
  const match = candidates.filter((item) => Number.isFinite(item.time) && Math.abs(item.time - frameValue) <= threshold)
    .sort((left, right) => Math.abs(left.time - frameValue) - Math.abs(right.time - frameValue))[0];
  return match || { time: frameValue, label: `帧 ${Math.round(frameValue * fps)}` };
}

function secondaryEditorTextLayers() {
  return Array.isArray(secondaryEditSession?.textLayers) ? secondaryEditSession.textLayers : [];
}

function secondaryEditorTextLayer(layerId = secondaryEditSelectedTextLayerId) {
  return secondaryEditorTextLayers().find((item) => String(item.id) === String(layerId)) || null;
}

function secondaryEditorTextLayerStyle(layer) {
  return secondaryEditorNormalizedSubtitleStyle(layer?.style || { vertical: "middle" });
}

function renderSecondaryEditorTextCanvas() {
  const canvas = $("#secondaryEditorTextLayerCanvas");
  if (!canvas) return;
  const active = secondaryEditExactPreview ? [] : secondaryEditorTextLayers().filter((layer) =>
    secondaryEditPlayheadTime >= Number(layer.start) && secondaryEditPlayheadTime <= Number(layer.end)
  );
  const media = secondaryEditorSubtitleMediaRect();
  canvas.innerHTML = active.map((layer) => {
    const style = secondaryEditorTextLayerStyle(layer);
    const horizontalBase = ({ left: .05, center: .5, right: .95 })[style.horizontal] ?? .5;
    const verticalBase = ({ top: .05, middle: .5, bottom: .95 })[style.vertical] ?? .5;
    const left = media.left + media.width * Math.max(.05, Math.min(.95, horizontalBase + style.offsetXRatio));
    const top = media.top + media.height * Math.max(.05, Math.min(.95, verticalBase + style.offsetYRatio));
    const transformX = style.horizontal === "left" ? "0" : style.horizontal === "right" ? "-100%" : "-50%";
    const transformY = style.vertical === "top" ? "0" : style.vertical === "bottom" ? "-100%" : "-50%";
    const color = style.preset === "bold" ? "#ffe66d" : "#fff";
    const background = style.preset === "social" ? "rgba(0,0,0,.62)" : "rgba(4,8,10,.72)";
    return `<div class="secondary-editor-text-layer${String(layer.id) === String(secondaryEditSelectedTextLayerId) ? " selected" : ""}" data-secondary-text-layer="${escapeHtml(layer.id)}" tabindex="0" style="left:${left}px;top:${top}px;max-width:${media.width * .9}px;font-size:${Math.max(8, Math.min(media.width, media.height) * style.fontSizeRatio)}px;font-weight:${["bold", "social"].includes(style.preset) ? 750 : 650};transform:translate(${transformX},${transformY})"><button type="button" data-secondary-text-move aria-label="拖动文本位置">✥</button><span data-secondary-text-content contenteditable="plaintext-only" role="textbox" spellcheck="false" style="color:${color};background:${background}">${escapeHtml(layer.text || "")}</span><button type="button" data-secondary-text-resize aria-label="拖动调整文本大小"></button></div>`;
  }).join("");
  canvas.querySelectorAll("[data-secondary-text-layer]").forEach((node) => {
    const layer = secondaryEditorTextLayer(node.dataset.secondaryTextLayer);
    if (!layer) return;
    const select = () => {
      secondaryEditSelectedTextLayerId = String(layer.id);
      renderSecondaryEditorTextTrack();
      syncSecondaryEditorControls();
      node.classList.add("selected");
    };
    node.addEventListener("pointerdown", (event) => {
      if (event.target.closest("[data-secondary-text-content],[data-secondary-text-move],[data-secondary-text-resize]")) return;
      beginSecondaryEditorTextLayerTransform(event, layer.id, "move");
    });
    node.addEventListener("click", select);
    node.querySelector("[data-secondary-text-move]")?.addEventListener("pointerdown", (event) => beginSecondaryEditorTextLayerTransform(event, layer.id, "move"));
    node.querySelector("[data-secondary-text-resize]")?.addEventListener("pointerdown", (event) => beginSecondaryEditorTextLayerTransform(event, layer.id, "resize"));
    const editor = node.querySelector("[data-secondary-text-content]");
    editor?.addEventListener("focus", (event) => { select(); $("#secondaryEditorVideo")?.pause(); event.stopPropagation(); });
    editor?.addEventListener("input", () => {
      layer.text = String(editor.textContent || "").replace(/\r?\n/g, " ").slice(0, 500);
      secondaryEditorMarkPreviewStale("文本已变化 · 预览样片需要重新生成");
      renderSecondaryEditorTextTrack();
    });
    editor?.addEventListener("keydown", (event) => {
      if (event.key === "Enter") { event.preventDefault(); editor.blur(); }
      if (event.key === "Escape") { event.preventDefault(); editor.blur(); }
      event.stopPropagation();
    });
    editor?.addEventListener("blur", () => {
      if (!String(layer.text || "").trim()) void secondaryEditorOperation({ type: "delete_text_layer", layerId: layer.id });
      else void secondaryEditorOperation({ type: "update_text_layer", layerId: layer.id, text: layer.text });
    });
  });
}

function renderSecondaryEditorTextTrack() {
  $("#secondaryEditorTextTrack")?.classList.toggle("hidden", !secondaryEditorTextLayers().length);
  const summary = $("#secondaryEditorTextSummary");
  const blocks = $("#secondaryEditorTextBlocks");
  if (!summary || !blocks || !secondaryEditSession) return;
  const layers = secondaryEditorTextLayers();
  summary.classList.toggle("hidden", Boolean(layers.length));
  summary.textContent = "暂无文本 · 点击上方“＋ 文本”添加";
  const duration = Math.max(.01, secondaryEditorTimelineDuration());
  blocks.innerHTML = layers.map((layer) => {
    const start = Math.max(0, Number(layer.start || 0));
    const end = Math.min(duration, Number(layer.end || start));
    return `<article class="secondary-editor-text-block${String(layer.id) === String(secondaryEditSelectedTextLayerId) ? " selected" : ""}" data-secondary-text-block="${escapeHtml(layer.id)}" style="left:${start / duration * 100}%;width:${Math.max(.08, (end - start) / duration * 100)}%" title="独立文本 · 成片 ${escapeHtml(formatPreciseTimecode(start))} → ${escapeHtml(formatPreciseTimecode(end))}"><span class="secondary-text-time-handle start" data-secondary-text-time-handle="start"></span><span data-secondary-text-track-content contenteditable="plaintext-only" spellcheck="false">${escapeHtml(layer.text || "")}</span><span class="secondary-text-time-handle end" data-secondary-text-time-handle="end"></span></article>`;
  }).join("");
  blocks.querySelectorAll("[data-secondary-text-block]").forEach((node) => {
    const layer = secondaryEditorTextLayer(node.dataset.secondaryTextBlock);
    if (!layer) return;
    node.addEventListener("click", (event) => {
      if (event.target.closest("[contenteditable],[data-secondary-text-time-handle]")) return;
      secondaryEditSelectedTextLayerId = String(layer.id);
      secondaryEditorSeekOutputTime(Number(layer.start || 0), false, true);
      renderSecondaryEditorTextTrack();
      renderSecondaryEditorTextCanvas();
    });
    const editor = node.querySelector("[data-secondary-text-track-content]");
    editor?.addEventListener("click", (event) => event.stopPropagation());
    editor?.addEventListener("keydown", (event) => { if (event.key === "Enter") { event.preventDefault(); editor.blur(); } });
    editor?.addEventListener("input", () => { layer.text = String(editor.textContent || "").slice(0, 500); secondaryEditorMarkPreviewStale("文本已变化 · 预览样片需要重新生成"); });
    editor?.addEventListener("blur", () => {
      if (!String(layer.text || "").trim()) void secondaryEditorOperation({ type: "delete_text_layer", layerId: layer.id });
      else void secondaryEditorOperation({ type: "update_text_layer", layerId: layer.id, text: layer.text });
    });
    node.querySelectorAll("[data-secondary-text-time-handle]").forEach((handle) => {
      handle.addEventListener("pointerdown", (event) => beginSecondaryEditorTextTimeDrag(event, layer.id, handle.dataset.secondaryTextTimeHandle));
    });
  });
}

function beginSecondaryEditorTextTimeDrag(event, layerId, boundary) {
  const layer = secondaryEditorTextLayer(layerId);
  const canvas = $("#secondaryEditorTimelineCanvas");
  if (!layer || !canvas || secondaryEditBusy) return;
  event.preventDefault(); event.stopPropagation(); $("#secondaryEditorVideo")?.pause();
  secondaryEditSelectedTextLayerId = String(layer.id);
  secondaryEditTextTimeDrag = { layer, boundary, startX: event.clientX, originalStart: Number(layer.start), originalEnd: Number(layer.end), width: Math.max(1, canvas.clientWidth), duration: secondaryEditorTimelineDuration() };
  document.addEventListener("pointermove", moveSecondaryEditorTextTimeDrag);
  document.addEventListener("pointerup", finishSecondaryEditorTextTimeDrag, { once: true });
  document.addEventListener("pointercancel", finishSecondaryEditorTextTimeDrag, { once: true });
}

function moveSecondaryEditorTextTimeDrag(event) {
  const drag = secondaryEditTextTimeDrag;
  if (!drag) return;
  const delta = (event.clientX - drag.startX) / drag.width * drag.duration;
  if (drag.boundary === "start") drag.layer.start = Math.min(drag.originalEnd - .08, Math.max(0, secondaryEditorSnapOutputTime(drag.originalStart + delta, { altKey: event.altKey, cueId: drag.layer.id }).time));
  else drag.layer.end = Math.max(drag.originalStart + .08, Math.min(drag.duration, secondaryEditorSnapOutputTime(drag.originalEnd + delta, { altKey: event.altKey, cueId: drag.layer.id }).time));
  renderSecondaryEditorTextTrack(); renderSecondaryEditorTextCanvas(); secondaryEditorMarkPreviewStale("文本时长已变化 · 预览样片需要重新生成");
}

function finishSecondaryEditorTextTimeDrag() {
  const drag = secondaryEditTextTimeDrag;
  if (!drag) return;
  secondaryEditTextTimeDrag = null;
  document.removeEventListener("pointermove", moveSecondaryEditorTextTimeDrag);
  void secondaryEditorOperation({ type: "update_text_layer", layerId: drag.layer.id, start: drag.layer.start, end: drag.layer.end });
}

function beginSecondaryEditorTextLayerTransform(event, layerId, mode) {
  const layer = secondaryEditorTextLayer(layerId);
  if (!layer || secondaryEditBusy || secondaryEditExactPreview) return;
  event.preventDefault(); event.stopPropagation(); $("#secondaryEditorVideo")?.pause();
  secondaryEditSelectedTextLayerId = String(layer.id);
  const style = secondaryEditorTextLayerStyle(layer);
  layer.style = { ...style };
  secondaryEditTextLayerDrag = { layer, mode, startX: event.clientX, startY: event.clientY, startStyle: { ...style }, media: secondaryEditorSubtitleMediaRect() };
  renderSecondaryEditorTextTrack(); syncSecondaryEditorControls();
  document.addEventListener("pointermove", moveSecondaryEditorTextLayerTransform);
  document.addEventListener("pointerup", finishSecondaryEditorTextLayerTransform, { once: true });
  document.addEventListener("pointercancel", finishSecondaryEditorTextLayerTransform, { once: true });
}

function moveSecondaryEditorTextLayerTransform(event) {
  const drag = secondaryEditTextLayerDrag;
  if (!drag) return;
  const dx = event.clientX - drag.startX, dy = event.clientY - drag.startY;
  if (drag.mode === "resize") {
    const diagonal = Math.max(1, Math.hypot(drag.media.width, drag.media.height));
    drag.layer.style.fontSizeRatio = Math.max(.012, Math.min(.12, drag.startStyle.fontSizeRatio + (dx + dy) / diagonal * .12));
  } else {
    drag.layer.style.offsetXRatio = Math.max(-.45, Math.min(.45, drag.startStyle.offsetXRatio + dx / Math.max(1, drag.media.width)));
    drag.layer.style.offsetYRatio = Math.max(-.45, Math.min(.45, drag.startStyle.offsetYRatio + dy / Math.max(1, drag.media.height)));
  }
  renderSecondaryEditorTextCanvas(); secondaryEditorMarkPreviewStale("文本位置或大小已变化 · 预览样片需要重新生成");
}

function finishSecondaryEditorTextLayerTransform() {
  const drag = secondaryEditTextLayerDrag;
  if (!drag) return;
  secondaryEditTextLayerDrag = null;
  document.removeEventListener("pointermove", moveSecondaryEditorTextLayerTransform);
  void secondaryEditorOperation({ type: "update_text_layer", layerId: drag.layer.id, style: drag.layer.style });
}

function renderSecondaryEditorSubtitleTrack() {
  const summary = $("#secondaryEditorSubtitleSummary");
  const blocks = $("#secondaryEditorSubtitleBlocks");
  if (!summary || !blocks || !secondaryEditSession) return;
  const enabled = Boolean(secondaryEditSession.subtitleEnabled);
  $("#secondaryEditorSubtitleTrack")?.classList.toggle("hidden", !enabled && !(secondaryInspectorMode === "subtitle" && $("#secondaryEditor")?.classList.contains("inspector-open")));
  const draftId = String(secondaryEditSession.subtitleDraftId || "");
  const draftReady = draftId && String(secondaryEditSubtitleDraft?.id || "") === draftId;
  const timingPreview = secondaryEditorTimelineModel().preview;
  summary.classList.toggle("hidden", Boolean(draftReady) && !timingPreview);
  if (timingPreview && draftReady) summary.textContent = "排期预览中 · 保存后需重新对齐字幕";
  else if (!enabled) summary.textContent = "未启用";
  else if (!draftId) summary.textContent = "待校对";
  else if (!draftReady) summary.textContent = "正在载入字幕块";
  blocks.innerHTML = "";
  if (!draftReady) return;
  const duration = Math.max(.01, secondaryEditorTimelineDuration());
  const cues = (secondaryEditSubtitleDraft.cues || []).filter((cue) => Number(cue.outputIndex || 0) === 0 && Number(cue.end) > Number(cue.start));
  blocks.innerHTML = cues.map((cue) => {
    const start = Math.max(0, Number(cue.start || 0));
    const end = Math.min(duration, Number(cue.end || start));
    const textBox = cue.kind === "text_box";
    const cueLabel = `${textBox ? "文本框" : "字幕"} · 成片 ${formatPreciseTimecode(start)} → ${formatPreciseTimecode(end)}`;
    const interactionHint = timingPreview ? "单击定位；保存排期后可编辑" : "单击定位，双击或按 Enter 编辑";
    return `<article class="secondary-editor-subtitle-block${textBox ? " text-box" : ""}${secondaryEditSelectedCueId === String(cue.id) ? " selected" : ""}" data-secondary-cue="${escapeHtml(cue.id)}" data-secondary-cue-kind="${textBox ? "text_box" : "subtitle"}" role="group" tabindex="0" aria-label="${escapeHtml(`${cueLabel}；${interactionHint}`)}" style="left:${start / duration * 100}%;width:${Math.max(.08, (end - start) / duration * 100)}%" title="${escapeHtml(`${cueLabel} · ${interactionHint}`)}"><span class="secondary-subtitle-handle start" data-secondary-cue-handle="start" role="button" tabindex="0" aria-label="调整字幕成片入点"></span><span data-secondary-cue-text contenteditable="false" tabindex="-1" aria-label="字幕文字，双击编辑" spellcheck="false">${escapeHtml(cue.text || "")}</span><span class="secondary-subtitle-handle end" data-secondary-cue-handle="end" role="button" tabindex="0" aria-label="调整字幕成片出点"></span></article>`;
  }).join("");
  blocks.querySelectorAll("[data-secondary-cue]").forEach((node) => {
    const cue = cues.find((item) => String(item.id) === String(node.dataset.secondaryCue));
    if (!cue) return;
    const cueId = String(cue.id);
    const currentCue = () => secondaryEditSubtitleDraft?.cues?.find((item) => String(item.id) === cueId) || null;
    const editor = node.querySelector("[data-secondary-cue-text]");
    const selectCue = () => {
      const activeCue = currentCue();
      if (!activeCue) return;
      secondaryEditSelectedCueId = cueId;
      secondaryEditorSeekOutputTime(Number(activeCue.start || 0), false, true);
      const activeClip = secondaryEditorTimelineModel().clips[secondaryEditSequenceIndex];
      if (activeClip) {
        secondaryEditSelectedClips = new Set([String(activeClip.id)]);
        secondaryEditSelectionAnchorId = String(activeClip.id);
        $("#secondaryEditorTimeline")?.querySelectorAll("[data-secondary-clip]").forEach((clipNode) => {
          clipNode.classList.toggle("selected", String(clipNode.dataset.secondaryClip) === String(activeClip.id));
        });
        renderSecondaryEditorInspector();
      }
      blocks.querySelectorAll("[data-secondary-cue]").forEach((cueNode) => {
        cueNode.classList.toggle("selected", cueNode === node);
      });
      syncSecondaryEditorControls();
    };
    const beginEditing = () => {
      if (!editor || timingPreview) return;
      selectCue();
      editor.setAttribute("contenteditable", "true");
      editor.tabIndex = 0;
      node.classList.add("is-editing");
      editor.focus({ preventScroll: true });
      const selection = window.getSelection?.();
      if (selection) {
        const range = document.createRange();
        range.selectNodeContents(editor);
        range.collapse(false);
        selection.removeAllRanges();
        selection.addRange(range);
      }
    };
    node.addEventListener("click", (event) => {
      if (event.target.closest("[data-secondary-cue-handle]")) return;
      selectCue();
    });
    node.addEventListener("keydown", (event) => {
      if (event.target.closest("[data-secondary-cue-handle]") || editor?.isContentEditable) return;
      if (event.key === "Enter") {
        event.preventDefault(); event.stopPropagation();
        if (timingPreview) selectCue(); else beginEditing();
      } else if (event.key === " ") {
        event.preventDefault(); event.stopPropagation(); selectCue();
      }
    });
    editor?.addEventListener("dblclick", (event) => {
      event.preventDefault(); event.stopPropagation(); beginEditing();
    });
    editor?.addEventListener("keydown", (event) => {
      if (event.key === "Enter" || event.key === "Escape") {
        event.preventDefault(); event.stopPropagation(); editor.blur();
      }
    });
    const syncEditorText = () => {
      const activeCue = currentCue();
      if (!activeCue) return;
      const value = String(editor?.textContent || "").slice(0, 500);
      if (value === String(activeCue.text || "")) return;
      activeCue.text = value;
      secondaryEditorMarkPreviewStale("字幕已变化 · 预览样片需要重新生成");
    };
    editor?.addEventListener("input", () => {
      syncEditorText();
      scheduleSecondaryEditorSubtitleSave(650);
    });
    editor?.addEventListener("blur", () => {
      syncEditorText();
      editor.setAttribute("contenteditable", "false");
      editor.tabIndex = -1;
      node.classList.remove("is-editing");
      scheduleSecondaryEditorSubtitleSave(0);
    });
    node.querySelectorAll("[data-secondary-cue-handle]").forEach((handle) => {
      handle.addEventListener("pointerdown", (event) => beginSecondaryEditorCueDrag(event, cueId, handle.dataset.secondaryCueHandle));
      handle.addEventListener("keydown", (event) => {
        if (event.key !== "ArrowLeft" && event.key !== "ArrowRight") return;
        event.preventDefault();
        event.stopPropagation();
        nudgeSecondaryEditorCue(cueId, handle.dataset.secondaryCueHandle, event.key === "ArrowLeft" ? -1 : 1);
      });
    });
  });
}

function secondaryEditorCueAtPlayhead() {
  const cues = (secondaryEditSubtitleDraft?.cues || []).filter((cue) => Number(cue.outputIndex || 0) === 0);
  return cues.find((cue) => String(cue.id) === String(secondaryEditSelectedCueId))
    || cues.find((cue) => secondaryEditPlayheadTime >= Number(cue.start) && secondaryEditPlayheadTime <= Number(cue.end))
    || null;
}

function secondaryEditorSubtitleCueAtPreviewTime() {
  const active = (secondaryEditSubtitleDraft?.cues || []).filter((item) =>
    Number(item.outputIndex || 0) === 0
    && secondaryEditPlayheadTime >= Number(item.start)
    && secondaryEditPlayheadTime <= Number(item.end)
  );
  return active.find((item) => String(item.id) === String(secondaryEditSelectedCueId))
    || active.find((item) => String(item.text || "").trim())
    || null;
}

function secondaryEditorSubtitleMediaRect() {
  const player = $(".secondary-editor-player");
  const video = $("#secondaryEditorVideo");
  if (!player || !video) return { left: 0, top: 0, width: 1, height: 1 };
  const playerRect = player.getBoundingClientRect();
  const videoRect = video.getBoundingClientRect();
  const boxWidth = Math.max(1, videoRect.width);
  const boxHeight = Math.max(1, videoRect.height);
  const sourceWidth = Math.max(1, Number(video.videoWidth || currentJob?.videoInfo?.width || 16));
  const sourceHeight = Math.max(1, Number(video.videoHeight || currentJob?.videoInfo?.height || 9));
  const scale = Math.min(boxWidth / sourceWidth, boxHeight / sourceHeight);
  const width = sourceWidth * scale;
  const height = sourceHeight * scale;
  return {
    left: videoRect.left - playerRect.left + (boxWidth - width) / 2,
    top: videoRect.top - playerRect.top + (boxHeight - height) / 2,
    width,
    height,
  };
}

function secondaryEditorNormalizedSubtitleStyle(value = null) {
  const raw = value && typeof value === "object" ? value : {};
  const preset = ["clean", "bold", "social"].includes(String(raw.preset || "")) ? String(raw.preset) : "clean";
  return {
    preset,
    fontSizeRatio: Math.max(.012, Math.min(.08, Number(raw.fontSizeRatio || ({ clean: .04, bold: .046, social: .052 })[preset]))),
    horizontal: ["left", "center", "right"].includes(String(raw.horizontal || "")) ? String(raw.horizontal) : "center",
    vertical: ["top", "middle", "bottom"].includes(String(raw.vertical || "")) ? String(raw.vertical) : "bottom",
    offsetXRatio: Math.max(-.4, Math.min(.4, Number(raw.offsetXRatio || 0))),
    offsetYRatio: Math.max(-.4, Math.min(.4, Number(raw.offsetYRatio || 0))),
  };
}

function secondaryEditorSubtitleStyleForCue(cue) {
  return secondaryEditorNormalizedSubtitleStyle(
    secondaryEditSubtitleDraft?.cueStyleOverrides?.[String(cue?.id || "")]
    || secondaryEditSubtitleDraft?.globalStyle
  );
}

function secondaryEditorSubtitleClipContext() {
  const context = secondaryEditorPlayheadContext();
  const model = secondaryEditorTimelineModel();
  const schedule = context ? model.schedule[context.index] : null;
  return context && schedule ? { ...context, schedule } : null;
}

function secondaryEditorSubtitleCuesForCurrentClip(currentCue = secondaryEditorSubtitleCueAtPreviewTime()) {
  const context = secondaryEditorSubtitleClipContext();
  if (!context || !secondaryEditSubtitleDraft) return currentCue ? [currentCue] : [];
  const start = Number(context.schedule.outputStart || 0);
  const end = Number(context.schedule.outputEnd || start);
  const cues = (secondaryEditSubtitleDraft.cues || []).filter((cue) => {
    if (Number(cue.outputIndex || 0) !== 0 || Number(cue.end) <= Number(cue.start)) return false;
    const middle = (Number(cue.start) + Number(cue.end)) / 2;
    return middle >= start - .001 && middle <= end + .001;
  });
  if (currentCue && !cues.includes(currentCue)) cues.push(currentCue);
  return cues;
}

function secondaryEditorEditableSubtitleStyles(cue) {
  if (!secondaryEditSubtitleDraft || !cue) return [];
  secondaryEditSubtitleDraft.cueStyleOverrides ||= {};
  const base = secondaryEditorSubtitleStyleForCue(cue);
  return secondaryEditorSubtitleCuesForCurrentClip(cue).map((item) => {
    const cueId = String(item.id || "");
    secondaryEditSubtitleDraft.cueStyleOverrides[cueId] = { ...base };
    return secondaryEditSubtitleDraft.cueStyleOverrides[cueId];
  });
}

function syncSecondaryEditorSubtitleTransformControls(cue = secondaryEditorSubtitleCueAtPreviewTime()) {
  const context = secondaryEditorSubtitleClipContext();
  const clipLabel = $("#secondarySubtitleTransformClip");
  const countLabel = $("#secondarySubtitleTransformCount");
  const clipCues = cue ? secondaryEditorSubtitleCuesForCurrentClip(cue) : [];
  if (clipLabel) clipLabel.textContent = context?.clip?.title || (context ? `片段 ${context.index + 1}` : "请先定位到一个片段");
  if (countLabel) countLabel.textContent = context
    ? `第 ${context.index + 1} 段 · ${formatPreciseTimecode(context.schedule.outputStart)} → ${formatPreciseTimecode(context.schedule.outputEnd)} · ${clipCues.length} 条字幕`
    : "只会调整当前片段";
  const reset = $("#secondarySubtitleTransformReset");
  if (reset) reset.disabled = !secondaryEditSubtitleDraft || !cue || secondaryEditSubtitleSaving;
  const hint = $("#secondaryEditorSubtitleTransformHint");
  if (hint) hint.textContent = cue
    ? `当前为第 ${Number(context?.index || 0) + 1} 段 · 拖动字幕调整本片段位置，拖动右下角调整大小`
    : "播放到有字幕的位置后，可在视频上直接拖动；修改只应用于当前时间线片段。";
}

function renderSecondaryEditorSubtitlePreview() {
  const host = $("#secondaryEditorSubtitlePreview");
  const text = host?.querySelector("[data-secondary-subtitle-preview-text]");
  if (!host || !text) return;
  const cue = secondaryEditorSubtitleCueAtPreviewTime();
  text.textContent = cue?.text || "";
  host.classList.toggle("hidden", !cue || secondaryEditExactPreview);
  host.dataset.secondarySubtitleCue = String(cue?.id || "");
  syncSecondaryEditorSubtitleTransformControls(cue);
  if (!cue || secondaryEditExactPreview) return;
  const style = secondaryEditorSubtitleStyleForCue(cue);
  const media = secondaryEditorSubtitleMediaRect();
  const horizontalBase = ({ left: .05, center: .5, right: .95 })[style.horizontal] ?? .5;
  const verticalBase = ({ top: .05, middle: .5, bottom: .95 })[style.vertical] ?? .95;
  const leftRatio = Math.max(.05, Math.min(.95, horizontalBase + style.offsetXRatio));
  const topRatio = Math.max(.05, Math.min(.95, verticalBase + style.offsetYRatio));
  host.style.left = `${media.left + media.width * leftRatio}px`;
  host.style.top = `${media.top + media.height * topRatio}px`;
  host.style.maxWidth = `${media.width * .9}px`;
  host.style.fontSize = `${Math.max(8, Math.min(media.width, media.height) * style.fontSizeRatio)}px`;
  host.style.fontWeight = ["bold", "social"].includes(style.preset) ? "750" : "650";
  host.style.transform = `translate(${style.horizontal === "left" ? "0" : style.horizontal === "right" ? "-100%" : "-50%"},${style.vertical === "top" ? "0" : style.vertical === "bottom" ? "-100%" : "-50%"})`;
  text.style.background = style.preset === "social" ? "rgba(0,0,0,.62)" : "rgba(4,8,10,.72)";
  text.style.color = style.preset === "bold" ? "#ffe66d" : "#fff";
}

function beginSecondaryEditorSubtitleCanvasTransform(event, mode = "move") {
  const cue = secondaryEditorSubtitleCueAtPreviewTime();
  const host = $("#secondaryEditorSubtitlePreview");
  if (!cue || !host || !secondaryEditSubtitleDraft || secondaryEditSubtitleSaving || secondaryEditExactPreview) return;
  const styles = secondaryEditorEditableSubtitleStyles(cue);
  if (!styles.length) return;
  event.preventDefault();
  event.stopPropagation();
  $("#secondaryEditorVideo")?.pause();
  secondaryEditSelectedCueId = String(cue.id);
  secondaryEditSubtitleCanvasDrag = {
    mode,
    cueId: String(cue.id),
    styles: styles.map((style) => ({
      style,
      startOffsetX: Number(style.offsetXRatio || 0),
      startOffsetY: Number(style.offsetYRatio || 0),
      startFontSize: Number(style.fontSizeRatio || .04),
    })),
    startX: event.clientX,
    startY: event.clientY,
    media: secondaryEditorSubtitleMediaRect(),
  };
  host.classList.add("is-transforming");
  renderSecondaryEditorSubtitleTrack();
  document.addEventListener("pointermove", moveSecondaryEditorSubtitleCanvasTransform);
  document.addEventListener("pointerup", finishSecondaryEditorSubtitleCanvasTransform, { once: true });
  document.addEventListener("pointercancel", finishSecondaryEditorSubtitleCanvasTransform, { once: true });
}

function moveSecondaryEditorSubtitleCanvasTransform(event) {
  const drag = secondaryEditSubtitleCanvasDrag;
  if (!drag) return;
  event.preventDefault();
  const deltaX = event.clientX - drag.startX;
  const deltaY = event.clientY - drag.startY;
  if (drag.mode === "resize") {
    const diagonal = Math.max(1, Math.hypot(drag.media.width, drag.media.height));
    drag.styles.forEach((item) => {
      item.style.fontSizeRatio = Math.max(.012, Math.min(.08, item.startFontSize + (deltaX + deltaY) / diagonal * .12));
    });
  } else {
    drag.styles.forEach((item) => {
      item.style.offsetXRatio = Math.max(-.4, Math.min(.4, item.startOffsetX + deltaX / Math.max(1, drag.media.width)));
      item.style.offsetYRatio = Math.max(-.4, Math.min(.4, item.startOffsetY + deltaY / Math.max(1, drag.media.height)));
    });
  }
  renderSecondaryEditorSubtitlePreview();
  secondaryEditorMarkPreviewStale("字幕位置或大小已变化 · 预览样片需要重新生成");
}

function finishSecondaryEditorSubtitleCanvasTransform() {
  if (!secondaryEditSubtitleCanvasDrag) return;
  secondaryEditSubtitleCanvasDrag = null;
  $("#secondaryEditorSubtitlePreview")?.classList.remove("is-transforming");
  document.removeEventListener("pointermove", moveSecondaryEditorSubtitleCanvasTransform);
  document.removeEventListener("pointerup", finishSecondaryEditorSubtitleCanvasTransform);
  document.removeEventListener("pointercancel", finishSecondaryEditorSubtitleCanvasTransform);
  scheduleSecondaryEditorSubtitleSave(0);
}

function nudgeSecondaryEditorSubtitleCanvas(mode, directionX = 0, directionY = 0, coarse = false) {
  const cue = secondaryEditorSubtitleCueAtPreviewTime();
  const styles = secondaryEditorEditableSubtitleStyles(cue);
  if (!cue || !styles.length || secondaryEditSubtitleSaving) return;
  if (mode === "resize") {
    styles.forEach((style) => { style.fontSizeRatio = Math.max(.012, Math.min(.08, Number(style.fontSizeRatio || .04) + directionX * (coarse ? .004 : .002))); });
  } else {
    const step = coarse ? .025 : .01;
    styles.forEach((style) => {
      style.offsetXRatio = Math.max(-.4, Math.min(.4, Number(style.offsetXRatio || 0) + directionX * step));
      style.offsetYRatio = Math.max(-.4, Math.min(.4, Number(style.offsetYRatio || 0) + directionY * step));
    });
  }
  renderSecondaryEditorSubtitlePreview();
  scheduleSecondaryEditorSubtitleSave(250);
}

function resetSecondaryEditorSubtitleTransform() {
  const cue = secondaryEditorSubtitleCueAtPreviewTime();
  if (!secondaryEditSubtitleDraft || !cue || secondaryEditSubtitleSaving) return;
  secondaryEditorSubtitleCuesForCurrentClip(cue).forEach((item) => { delete secondaryEditSubtitleDraft.cueStyleOverrides?.[String(item.id)]; });
  renderSecondaryEditorSubtitlePreview();
  scheduleSecondaryEditorSubtitleSave(0);
}

async function addSecondaryEditorTextLayer() {
  if (!secondaryEditSession || secondaryEditBusy) return;
  const duration = Math.max(0, secondaryEditorTimelineDuration());
  if (duration < .08) return void showToast("时间线为空，暂时不能添加文本");
  let start = Math.max(0, Math.min(duration - .08, secondaryEditPlayheadTime));
  let end = Math.min(duration, start + 2.5);
  if (end - start < .5 && duration >= .5) {
    start = Math.max(0, duration - 2.5);
    end = duration;
  }
  const created = await secondaryEditorOperation({
    type: "add_text_layer", start, end, text: "输入文本",
    style: { preset: "clean", fontSizeRatio: .04, horizontal: "center", vertical: "middle", offsetXRatio: 0, offsetYRatio: 0 },
  });
  if (!created || !secondaryEditSelectedTextLayerId) return;
  secondaryEditorSeekOutputTime(start, false, true);
  window.requestAnimationFrame(() => {
    const editor = $(`[data-secondary-text-layer="${CSS.escape(String(secondaryEditSelectedTextLayerId))}"] [data-secondary-text-content]`);
    if (!editor) return;
    editor.focus();
    const range = document.createRange();
    range.selectNodeContents(editor);
    const selection = window.getSelection();
    selection?.removeAllRanges();
    selection?.addRange(range);
  });
}

function splitSecondaryEditorSubtitleCue() {
  const cue = secondaryEditorCueAtPlayhead();
  if (!cue) return void showToast("请选择播放头所在的字幕");
  const split = secondaryEditPlayheadTime;
  if (split - Number(cue.start) < .08 || Number(cue.end) - split < .08) return void showToast("播放头距离字幕边界太近");
  const text = String(cue.text || "");
  const pivot = Math.max(1, Math.floor(text.length / 2));
  const second = { ...cue, id: `cue_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 7)}`, start: split, text: text.slice(pivot).trim(), originalText: "" };
  cue.end = split;
  cue.text = text.slice(0, pivot).trim();
  secondaryEditSubtitleDraft.cues.push(second);
  secondaryEditSelectedCueId = second.id;
  secondaryEditorMarkPreviewStale("字幕已变化 · 预览样片需要重新生成");
  renderSecondaryEditorSubtitleTrack();
  scheduleSecondaryEditorSubtitleSave(0);
}

function mergeSecondaryEditorSubtitleCue() {
  const cue = secondaryEditorCueAtPlayhead();
  if (!cue) return void showToast("请先选择一条字幕");
  const cues = (secondaryEditSubtitleDraft?.cues || []).filter((item) => Number(item.outputIndex || 0) === Number(cue.outputIndex || 0)).sort((a, b) => Number(a.start) - Number(b.start));
  const next = cues[cues.indexOf(cue) + 1];
  if (!next) return void showToast("当前字幕后面没有可合并条目");
  cue.end = next.end;
  cue.text = `${String(cue.text || "").trim()} ${String(next.text || "").trim()}`.trim().slice(0, 500);
  secondaryEditSubtitleDraft.cues = secondaryEditSubtitleDraft.cues.filter((item) => item !== next);
  secondaryEditSelectedCueId = String(cue.id);
  secondaryEditorMarkPreviewStale("字幕已变化 · 预览样片需要重新生成");
  renderSecondaryEditorSubtitleTrack();
  scheduleSecondaryEditorSubtitleSave(0);
}

function deleteSecondaryEditorSubtitleCue() {
  const cue = secondaryEditorCueAtPlayhead();
  if (!cue) return void showToast("请先选择一条字幕");
  secondaryEditSubtitleDraft.cues = secondaryEditSubtitleDraft.cues.filter((item) => item !== cue);
  secondaryEditSelectedCueId = null;
  secondaryEditorMarkPreviewStale("字幕已变化 · 预览样片需要重新生成");
  renderSecondaryEditorSubtitleTrack();
  scheduleSecondaryEditorSubtitleSave(0);
}

async function loadSecondaryEditorSubtitleDraft() {
  const draftId = String(secondaryEditSession?.subtitleDraftId || "");
  if (!currentJob?.id || !draftId) {
    secondaryEditSubtitleDraft = null;
    secondaryEditSubtitleLoadingId = null;
    renderSecondaryEditorSubtitleTrack();
    return null;
  }
  if (String(secondaryEditSubtitleDraft?.id || "") === draftId) return secondaryEditSubtitleDraft;
  if (secondaryEditSubtitleLoadingId === draftId) return null;
  secondaryEditSubtitleLoadingId = draftId;
  renderSecondaryEditorSubtitleTrack();
  try {
    const payload = await api(`/api/jobs/${encodeURIComponent(currentJob.id)}/subtitle-drafts/${encodeURIComponent(draftId)}`);
    if (!secondaryEditorOpen() || String(secondaryEditSession?.subtitleDraftId || "") !== draftId) return;
    secondaryEditSubtitleDraft = payload.draft;
    renderSecondaryEditorSubtitleTrack();
    renderSecondaryEditorSubtitlePreview();
    return secondaryEditSubtitleDraft;
  } catch (error) {
    if (String(secondaryEditSession?.subtitleDraftId || "") === draftId) {
      const summary = $("#secondaryEditorSubtitleSummary");
      if (summary) summary.textContent = "字幕块载入失败";
      showToast(error.message);
    }
    return null;
  } finally {
    if (secondaryEditSubtitleLoadingId === draftId) secondaryEditSubtitleLoadingId = null;
  }
}

function secondaryEditorSubtitleBurnReadiness() {
  if (!secondaryEditSession?.subtitleEnabled) return { ready: true, reason: "disabled" };
  const server = secondaryEditSession.subtitleReadiness && typeof secondaryEditSession.subtitleReadiness === "object"
    ? secondaryEditSession.subtitleReadiness : null;
  if (server && server.ready === false && server.status !== "disabled") {
    return { ready: false, reason: String(server.status || "subtitle_review_required"), message: String(server.message || "字幕必须完成校对后才能烧录") };
  }
  const draftId = String(secondaryEditSession.subtitleDraftId || "");
  if (!draftId) return { ready: false, reason: "missing_draft", message: "请先生成并确认当前时间线的字幕草稿" };
  const draft = String(secondaryEditSubtitleDraft?.id || "") === draftId ? secondaryEditSubtitleDraft : null;
  if (!draft) return { ready: false, reason: "loading", message: "正在载入字幕草稿状态" };
  const status = String(draft.status || "draft");
  if (!["confirmed", "auto_reviewed"].includes(status)) {
    return { ready: false, reason: "subtitle_review_required", message: "字幕草稿尚未完成校对，请先确认文字与断句" };
  }
  if (draft.sourceSubtitleAcknowledged === false) {
    return { ready: false, reason: "source_subtitle_ack_required", message: "请先确认原视频字幕状态，避免重复叠加字幕" };
  }
  if (secondaryEditSubtitleNeedsReview) {
    return { ready: false, reason: "timeline_changed", message: "时间线已变化，需要重新对齐字幕" };
  }
  return { ready: true, reason: "ready" };
}

async function ensureSecondaryEditorSubtitleReadyForBurn() {
  if (!secondaryEditSession?.subtitleEnabled) return true;
  const draftId = String(secondaryEditSession.subtitleDraftId || "");
  if (draftId && String(secondaryEditSubtitleDraft?.id || "") !== draftId) await loadSecondaryEditorSubtitleDraft();
  const readiness = secondaryEditorSubtitleBurnReadiness();
  if (readiness.ready) return true;
  if (readiness.reason === "loading") {
    showToast(readiness.message || "正在载入字幕状态，请稍后重试");
    return false;
  }
  const draft = await reviewSecondaryEditorSubtitles();
  return Boolean(draft);
}

function scheduleSecondaryEditorSubtitleSave(delay = 500) {
  window.clearTimeout(secondaryEditSubtitleSaveTimer);
  secondaryEditSubtitleSaveTimer = window.setTimeout(saveSecondaryEditorSubtitleDraft, delay);
  const status = $("#secondaryEditorSubtitleStatus");
  if (status) status.textContent = "字幕修改待保存";
  secondaryEditorMarkPreviewStale("字幕已变化 · 预览样片需要重新生成");
}

async function saveSecondaryEditorSubtitleDraft() {
  window.clearTimeout(secondaryEditSubtitleSaveTimer);
  secondaryEditSubtitleSaveTimer = null;
  const draft = secondaryEditSubtitleDraft;
  if (!currentJob?.id || !draft || secondaryEditSubtitleSaving) {
    if (secondaryEditSubtitleSaving) scheduleSecondaryEditorSubtitleSave(300);
    return;
  }
  secondaryEditSubtitleSaving = true;
  const status = $("#secondaryEditorSubtitleStatus");
  if (status) status.textContent = "正在保存字幕修改";
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/subtitle-drafts/${encodeURIComponent(draft.id)}`, {
      method: "PUT",
      body: {
        revision: draft.revision,
        cues: draft.cues,
        globalStyle: draft.globalStyle || {},
        cueStyleOverrides: draft.cueStyleOverrides || {},
        confirmed: draft.status === "confirmed",
        sourceSubtitleAcknowledged: Boolean(draft.sourceSubtitleAcknowledged),
      },
    });
    if (String(secondaryEditSubtitleDraft?.id || "") === String(draft.id)) secondaryEditSubtitleDraft = payload.draft;
    if (status) status.textContent = "字幕修改已保存并保持确认";
  } catch (error) {
    if (status) status.textContent = "字幕保存失败，请重试";
    showToast(error.message);
  } finally {
    secondaryEditSubtitleSaving = false;
  }
}

function beginSecondaryEditorCueDrag(event, cueId, boundary) {
  if (secondaryEditorInspectorHasChanges()) {
    showToast("请先保存或取消当前片段设置");
    return;
  }
  const cue = secondaryEditSubtitleDraft?.cues?.find((item) => String(item.id) === String(cueId));
  if (!cue || secondaryEditSubtitleSaving) return;
  event.preventDefault();
  event.stopPropagation();
  secondaryEditCueDrag = {
    cue, cueId: String(cueId), boundary, startX: event.clientX,
    start: Number(cue.start), end: Number(cue.end),
  };
  document.addEventListener("pointermove", moveSecondaryEditorCueDrag);
  document.addEventListener("pointerup", finishSecondaryEditorCueDrag, { once: true });
}

function nudgeSecondaryEditorCue(cueId, boundary, direction) {
  if (secondaryEditorInspectorHasChanges()) return void showToast("请先保存或取消当前片段设置");
  const cue = secondaryEditSubtitleDraft?.cues?.find((item) => String(item.id) === String(cueId));
  if (!cue || secondaryEditSubtitleSaving) return;
  const frame = 1 / secondaryEditorFrameRate();
  const duration = Math.max(.01, secondaryEditorTimelineDuration());
  const ordered = (secondaryEditSubtitleDraft?.cues || []).filter((item) => Number(item.outputIndex || 0) === Number(cue.outputIndex || 0)).sort((left, right) => Number(left.start) - Number(right.start));
  const index = ordered.indexOf(cue);
  const previousEnd = index > 0 ? Number(ordered[index - 1].end) : 0;
  const nextStart = index >= 0 && index < ordered.length - 1 ? Number(ordered[index + 1].start) : duration;
  if (boundary === "start") cue.start = Math.max(previousEnd, Math.min(Number(cue.end) - .08, Number(cue.start) + direction * frame));
  else cue.end = Math.min(nextStart, Math.max(Number(cue.start) + .08, Number(cue.end) + direction * frame));
  renderSecondaryEditorSubtitleTrack();
  scheduleSecondaryEditorSubtitleSave(0);
}

function moveSecondaryEditorCueDrag(event) {
  const drag = secondaryEditCueDrag;
  if (!drag || !secondaryEditSession) return;
  const canvas = $("#secondaryEditorTimelineCanvas");
  const duration = Math.max(.01, secondaryEditorTimelineDuration());
  const delta = (event.clientX - drag.startX) / Math.max(1, canvas?.clientWidth || 1) * duration;
  const raw = (drag.boundary === "start" ? drag.start : drag.end) + delta;
  const snapped = secondaryEditorSnapOutputTime(raw, { altKey: event.altKey, cueId: drag.cueId });
  const ordered = (secondaryEditSubtitleDraft?.cues || []).filter((cue) => Number(cue.outputIndex || 0) === Number(drag.cue.outputIndex || 0)).sort((left, right) => Number(left.start) - Number(right.start));
  const index = ordered.indexOf(drag.cue);
  const previousEnd = index > 0 ? Number(ordered[index - 1].end) : 0;
  const nextStart = index >= 0 && index < ordered.length - 1 ? Number(ordered[index + 1].start) : duration;
  if (drag.boundary === "start") drag.cue.start = Math.max(previousEnd, Math.min(Number(drag.cue.end) - .08, snapped.time));
  else drag.cue.end = Math.min(nextStart, Math.max(Number(drag.cue.start) + .08, snapped.time));
  renderSecondaryEditorSubtitleTrack();
  secondaryEditorShowSnapGuide(drag.boundary === "start" ? drag.cue.start : drag.cue.end, snapped.label);
}

function finishSecondaryEditorCueDrag() {
  if (!secondaryEditCueDrag) return;
  secondaryEditCueDrag = null;
  document.removeEventListener("pointermove", moveSecondaryEditorCueDrag);
  secondaryEditorHideSnapGuide();
  scheduleSecondaryEditorSubtitleSave(0);
}

function updateSecondaryEditorPlayhead(outputTime, reveal = false) {
  if (!secondaryEditSession) return;
  const duration = Math.max(0, secondaryEditorTimelineDuration());
  const value = Math.max(0, Math.min(duration, Number(outputTime) || 0));
  secondaryEditPlayheadTime = value;
  const percent = duration > 0 ? value / duration * 100 : 0;
  const playhead = $("#secondaryEditorPlayhead");
  const label = $("#secondaryEditorPlayheadLabel");
  const ruler = $("#secondaryEditorRuler");
  if (playhead) playhead.style.left = `${percent}%`;
  if (label) label.textContent = formatPreciseTimecode(value);
  if (ruler) {
    ruler.setAttribute("aria-valuemax", String(duration));
    ruler.setAttribute("aria-valuenow", String(value));
    ruler.setAttribute("aria-valuetext", formatPreciseTimecode(value));
  }
  renderSecondaryEditorSubtitlePreview();
  renderSecondaryEditorTextCanvas();
  const schedule = secondaryEditorSchedule();
  $("#secondaryEditorTimeline")?.querySelectorAll("[data-secondary-clip]").forEach((node, index) => {
    const item = schedule[index] || {};
    const start = Number(item.outputStart || 0);
    const end = Number(item.outputEnd || start);
    const progress = end > start ? Math.max(0, Math.min(1, (value - start) / (end - start))) : 0;
    node.style.setProperty("--clip-progress", String(progress));
    node.classList.toggle("playing", value >= start && value <= end && secondaryEditSequenceIndex === index);
  });
  if (reveal) {
    const viewport = $("#secondaryEditorTimelineViewport");
    const canvas = $("#secondaryEditorTimelineCanvas");
    if (viewport && canvas && canvas.offsetWidth > viewport.clientWidth) {
      const x = value / Math.max(.01, duration) * canvas.offsetWidth;
      const margin = Math.min(120, viewport.clientWidth * .2);
      if (x < viewport.scrollLeft + margin) viewport.scrollLeft = Math.max(0, x - margin);
      else if (x > viewport.scrollLeft + viewport.clientWidth - margin) viewport.scrollLeft = x - viewport.clientWidth + margin;
    }
  }
  syncSecondaryEditorMediaControls();
  if (secondaryEditInsertionIndexOverride === null) syncSecondaryEditorInsertionHint();
}

function renderSecondaryEditorMarkers() {
  const host = $("#secondaryEditorMarkers");
  if (!host || !secondaryEditSession) return;
  const duration = Math.max(.01, secondaryEditorTimelineDuration());
  const clips = secondaryEditorTimelineModel().clips;
  const schedule = secondaryEditorSchedule();
  const rows = (secondaryEditSession.markers || []).map((marker) => {
    const index = clips.findIndex((clip) => String(clip.id) === String(marker.clipId));
    const clip = clips[index];
    const item = schedule[index];
    if (!clip || !item) return null;
    const local = (Number(marker.sourceTime) - Number(clip.sourceStart)) / Math.max(.01, Number(clip.playbackRate || 1));
    return { marker, outputTime: Math.max(Number(item.outputStart), Math.min(Number(item.outputEnd), Number(item.outputStart) + local)) };
  }).filter(Boolean);
  host.innerHTML = rows.map(({ marker, outputTime }) => `<article class="secondary-editor-marker${secondaryEditSelectedMarkerId === String(marker.id) ? " selected" : ""}" data-secondary-marker="${escapeHtml(marker.id)}" style="left:${outputTime / duration * 100}%" title="${escapeHtml(formatPreciseTimecode(outputTime))}"><i></i><span contenteditable="plaintext-only" spellcheck="false">${escapeHtml(marker.label || "标记")}</span><button type="button" data-marker-delete aria-label="删除标记">×</button></article>`).join("");
  host.querySelectorAll("[data-secondary-marker]").forEach((node) => {
    const row = rows.find(({ marker }) => String(marker.id) === String(node.dataset.secondaryMarker));
    if (!row) return;
    node.addEventListener("click", (event) => {
      if (event.target.closest("[contenteditable],[data-marker-delete]")) return;
      secondaryEditSelectedMarkerId = String(row.marker.id);
      secondaryEditorSeekOutputTime(row.outputTime, false);
      renderSecondaryEditorMarkers();
    });
    node.querySelector("[data-marker-delete]")?.addEventListener("click", (event) => {
      event.stopPropagation();
      secondaryEditSelectedMarkerId = null;
      void secondaryEditorOperation({ type: "delete_marker", markerId: row.marker.id });
    });
    const label = node.querySelector("[contenteditable]");
    label?.addEventListener("keydown", (event) => {
      if (event.key === "Enter") { event.preventDefault(); label.blur(); }
    });
    label?.addEventListener("blur", () => {
      const next = String(label.textContent || "标记").trim().slice(0, 80) || "标记";
      if (next !== String(row.marker.label || "标记")) void secondaryEditorOperation({ type: "update_marker", markerId: row.marker.id, label: next });
    });
  });
}

function secondaryEditorDropIndexAtClientX(clientX, timeline = $("#secondaryEditorTimeline")) {
  if (!timeline) return 0;
  const nodes = [...timeline.querySelectorAll("[data-secondary-clip]")];
  const x = Number(clientX);
  if (!Number.isFinite(x)) return nodes.length;
  const index = nodes.findIndex((node) => {
    const rect = node.getBoundingClientRect();
    return x < rect.left + rect.width / 2;
  });
  return index < 0 ? nodes.length : index;
}

function secondaryEditorReorderedClipIds(clips, selectedClipIds, rawTargetIndex) {
  const ids = (clips || []).map((clip) => String(clip.id));
  const selection = selectedClipIds instanceof Set ? selectedClipIds : new Set(selectedClipIds || []);
  const moving = ids.filter((id) => selection.has(id));
  if (!moving.length) return ids;
  const remaining = ids.filter((id) => !selection.has(id));
  const rawTarget = Math.max(0, Math.min(ids.length, Number(rawTargetIndex) || 0));
  const selectedBeforeTarget = ids.slice(0, rawTarget).filter((id) => selection.has(id)).length;
  const target = Math.max(0, Math.min(remaining.length, rawTarget - selectedBeforeTarget));
  remaining.splice(target, 0, ...moving);
  return remaining;
}

function updateSecondaryEditorDropTarget(event, timeline, schedule, duration) {
  secondaryEditDropIndex = secondaryEditorDropIndexAtClientX(event.clientX, timeline);
  const guide = $("#secondaryEditorDropGuide");
  const boundary = secondaryEditDropIndex >= schedule.length
    ? Number(schedule.at(-1)?.outputEnd || duration)
    : Number(schedule[secondaryEditDropIndex]?.outputStart || 0);
  if (guide) {
    guide.style.left = `${Math.max(0, Math.min(100, boundary / Math.max(.01, duration) * 100))}%`;
    guide.classList.remove("hidden");
  }
  return secondaryEditDropIndex;
}

function commitSecondaryEditorClipDrop(clips, targetIndex) {
  if (!secondaryEditDraggedClipId) return;
  const current = clips.map((clip) => String(clip.id));
  const reordered = secondaryEditorReorderedClipIds(clips, secondaryEditSelectedClips, targetIndex);
  if (reordered.every((id, index) => id === current[index])) return;
  void secondaryEditorOperation({ type: "reorder_clips", clipIds: reordered });
}

function renderSecondaryEditorTimeline() {
  const timeline = $("#secondaryEditorTimeline");
  const viewport = $("#secondaryEditorTimelineViewport");
  const canvas = $("#secondaryEditorTimelineCanvas");
  const ruler = $("#secondaryEditorRuler");
  if (!timeline || !viewport || !canvas || !ruler || !secondaryEditSession) return;
  const model = secondaryEditorTimelineModel();
  const clips = model.clips;
  const schedule = model.schedule;
  const duration = Math.max(.01, model.duration);
  const viewportStyle = window.getComputedStyle(viewport);
  const viewportInlinePadding = (Number.parseFloat(viewportStyle.paddingLeft) || 0) + (Number.parseFloat(viewportStyle.paddingRight) || 0);
  const viewportWidth = Math.max(360, (viewport.clientWidth || 720) - viewportInlinePadding);
  const canvasWidth = Math.max(
    viewportWidth,
    secondaryEditTimelinePixelsPerSecond ? duration * secondaryEditTimelinePixelsPerSecond : viewportWidth,
  );
  canvas.style.width = `${Math.round(canvasWidth)}px`;
  const tickStep = secondaryEditorTimelineTickStep(duration, canvasWidth);
  const tickCount = Math.max(1, Math.ceil(duration / tickStep));
  const tickTimes = Array.from({ length: tickCount + 1 }, (_unused, index) => Math.min(duration, index * tickStep))
    .filter((time, index, values) => index === 0 || time - values[index - 1] >= tickStep * .4);
  ruler.innerHTML = tickTimes.map((time) => {
    const left = time / duration * 100;
    return `<span class="secondary-editor-ruler-tick" style="left:${left}%"><i></i><b>${secondaryEditorRulerLabel(time, tickStep)}</b></span>`;
  }).join("");
  timeline.innerHTML = clips.length ? clips.map((clip, index) => {
    const item = schedule[index] || {};
    const start = Number(item.outputStart || 0);
    const end = Number(item.outputEnd ?? start + Number(clip.duration || 0));
    const left = start / duration * 100;
    const width = Math.max(.05, (end - start) / duration * 100);
    const transition = ({ dissolve: "叠化", fade_black: "淡黑", cut: "硬切" })[clip.transitionIn?.type || "cut"];
    const pending = model.preview && String(clip.id) === model.draftClipId;
    const clipLabel = escapeHtml(`${clip.title || "片段"}，成片 ${formatPreciseTimecode(start)} 到 ${formatPreciseTimecode(end)}${pending ? "，待保存预览" : ""}`);
    return `<article draggable="true" role="group" class="secondary-timeline-clip${secondaryEditSelectedClips.has(String(clip.id)) ? " selected" : ""}${pending ? " pending-settings" : ""}" data-secondary-clip="${escapeHtml(clip.id)}" style="left:${left}%;width:${width}%;--clip-order:${index}" aria-label="${clipLabel}"><button class="secondary-clip-select" data-secondary-clip-select type="button" aria-label="选择并定位${clipLabel}"></button>${index > 0 ? `<button class="secondary-transition-chip" data-secondary-transition type="button" aria-label="编辑${escapeHtml(clip.title || "片段")}的入场转场" title="点击编辑转场">${escapeHtml(transition)}${clip.transitionIn?.type !== "cut" ? ` · ${Number(clip.transitionIn?.duration || 0).toFixed(2)}s` : ""}</button>` : ""}<button class="secondary-clip-delete" data-secondary-clip-delete type="button" aria-label="从时间线删除${escapeHtml(clip.title || "片段")}" title="从时间线删除并自动闭合空隙">×</button><span class="secondary-clip-media" aria-hidden="true">${secondaryEditorClipMediaMarkup(clip)}<canvas data-secondary-waveform data-source-start="${Number(clip.sourceStart || 0)}" data-source-end="${Number(clip.sourceEnd || 0)}" data-audio-gain="${Number(clip.audioGain ?? 1)}" data-muted="${Boolean(clip.muted)}"></canvas></span><span class="secondary-clip-copy"><strong>${String(index + 1).padStart(2, "0")} · ${escapeHtml(clip.title || "片段")}${pending ? " · 待保存" : ""}</strong><small>${Number(end - start).toFixed(1)} 秒</small><em data-secondary-clip-source>源片 ${formatPreciseTimecode(clip.sourceStart)} → ${formatPreciseTimecode(clip.sourceEnd)} · ${Number(clip.playbackRate || 1)}× · ${transition}${clip.muted ? " · 静音" : Number(clip.audioGain ?? 1) !== 1 ? ` · ${Math.round(Number(clip.audioGain) * 100)}%` : ""}</em></span><span class="secondary-trim-readout" data-secondary-trim-readout>拖动边缘调整源片入点 / 出点</span><button class="secondary-trim-handle start" data-secondary-trim="start" type="button" aria-label="调整片段源片入点"></button><button class="secondary-trim-handle end" data-secondary-trim="end" type="button" aria-label="调整片段源片出点"></button></article>`;
  }).join("") : '<div class="secondary-editor-timeline-empty">时间线为空。从左侧素材库加入片段后再导出。</div>';
  timeline.querySelectorAll("[data-secondary-clip]").forEach((node) => {
    const clipId = String(node.dataset.secondaryClip);
    const clipIndex = Math.max(0, clips.findIndex((clip) => String(clip.id) === clipId));
    node.addEventListener("click", (event) => {
      if (event.target.closest("[data-secondary-trim],[data-secondary-clip-delete]")) return;
      if (secondaryEditorInspectorHasChanges() && !secondaryEditSelectedClips.has(clipId)) {
        showToast("请先保存或取消当前片段设置");
        return;
      }
      const index = Math.max(0, clips.findIndex((clip) => String(clip.id) === clipId));
      if (event.shiftKey && secondaryEditSelectionAnchorId) {
        const anchor = clips.findIndex((clip) => String(clip.id) === String(secondaryEditSelectionAnchorId));
        if (anchor >= 0) {
          secondaryEditSelectedClips = new Set(clips.slice(Math.min(anchor, index), Math.max(anchor, index) + 1).map((clip) => String(clip.id)));
          secondaryEditSequenceIndex = index;
          secondaryEditorSeekOutputTime(Number(schedule[index]?.outputStart || 0), false, true);
          renderSecondaryEditorTimeline();
          renderSecondaryEditorInspector();
          return;
        }
      }
      if (event.metaKey || event.ctrlKey) {
        if (secondaryEditSelectedClips.has(clipId)) secondaryEditSelectedClips.delete(clipId); else secondaryEditSelectedClips.add(clipId);
        secondaryEditSelectionAnchorId = clipId;
        renderSecondaryEditorTimeline();
        renderSecondaryEditorInspector();
        return;
      }
      secondaryEditSelectionAnchorId = clipId;
      const item = schedule[index] || {};
      const rect = node.getBoundingClientRect();
      const fraction = Math.max(0, Math.min(1, (event.clientX - rect.left) / Math.max(1, rect.width)));
      const outputTime = Number(item.outputStart || 0) + fraction * Math.max(0, Number(item.outputEnd || 0) - Number(item.outputStart || 0));
      secondaryEditorSeekOutputTime(outputTime, false);
    });
    node.querySelector("[data-secondary-clip-delete]")?.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      void secondaryEditorOperation({ type: "delete_clips", clipIds: [clipId] });
    });
    node.querySelectorAll("[data-secondary-trim]").forEach((handle) => {
      const adjacentDirection = handle.dataset.secondaryTrim === "start" ? "前一片段" : "后一片段";
      handle.title = "拖动调整当前片段；Shift+拖动进行滚动裁剪";
      handle.setAttribute("aria-label", `调整当前片段源片${handle.dataset.secondaryTrim === "start" ? "入点" : "出点"}；按住 Shift 联动${adjacentDirection}`);
      handle.addEventListener("pointerdown", (event) => beginSecondaryEditorTrim(event, clipId, handle.dataset.secondaryTrim));
      handle.addEventListener("keydown", (event) => {
        if (event.key !== "ArrowLeft" && event.key !== "ArrowRight") return;
        event.preventDefault();
        event.stopPropagation();
        nudgeSecondaryEditorTrim(clipId, handle.dataset.secondaryTrim, event.key === "ArrowLeft" ? -1 : 1);
      });
    });
    node.querySelector("[data-secondary-transition]")?.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      secondaryEditSelectedClips = new Set([clipId]);
      secondaryEditSelectionAnchorId = clipId;
      secondaryEditSequenceIndex = clipIndex;
      renderSecondaryEditorTimeline();
      renderSecondaryEditorInspector();
      $("#secondaryInspectorTransition")?.focus();
    });
    node.addEventListener("dragstart", (event) => {
      if (secondaryEditorInspectorHasChanges()) {
        event.preventDefault();
        showToast("请先保存或取消当前片段设置");
        return;
      }
      if (!secondaryEditSelectedClips.has(clipId)) secondaryEditSelectedClips = new Set([clipId]);
      secondaryEditDraggedClipId = clipId;
      timeline.querySelectorAll("[data-secondary-clip]").forEach((clipNode) => clipNode.classList.toggle("dragging", secondaryEditSelectedClips.has(String(clipNode.dataset.secondaryClip))));
      event.dataTransfer?.setData("text/plain", clipId);
      if (event.dataTransfer) event.dataTransfer.effectAllowed = "move";
    });
    node.addEventListener("dragend", () => {
      secondaryEditDraggedClipId = null;
      secondaryEditDropIndex = null;
      timeline.querySelectorAll(".dragging").forEach((clipNode) => clipNode.classList.remove("dragging"));
      $("#secondaryEditorDropGuide")?.classList.add("hidden");
    });
    node.addEventListener("dragover", (event) => {
      event.preventDefault();
      if (event.dataTransfer) event.dataTransfer.dropEffect = secondaryEditDraggedMaterial ? "copy" : "move";
      updateSecondaryEditorDropTarget(event, timeline, schedule, duration);
    });
    node.addEventListener("drop", (event) => {
      event.preventDefault();
      event.stopPropagation();
      const targetIndex = updateSecondaryEditorDropTarget(event, timeline, schedule, duration);
      if (secondaryEditDraggedMaterial) {
        const item = secondaryEditDraggedMaterial;
        secondaryEditDraggedMaterial = null;
        void secondaryEditorOperation({ type: "insert_clip", sourceStart: item.start, sourceEnd: item.end, title: item.title, sourceRef: item.sourceRef, targetIndex });
        return;
      }
      commitSecondaryEditorClipDrop(clips, targetIndex);
    });
  });
  timeline.addEventListener("dragover", (event) => {
    if (!secondaryEditDraggedMaterial && !secondaryEditDraggedClipId) return;
    event.preventDefault();
    if (event.dataTransfer) event.dataTransfer.dropEffect = secondaryEditDraggedMaterial ? "copy" : "move";
    updateSecondaryEditorDropTarget(event, timeline, schedule, duration);
  });
  timeline.addEventListener("drop", (event) => {
    if (!secondaryEditDraggedMaterial && !secondaryEditDraggedClipId) return;
    event.preventDefault();
    const targetIndex = updateSecondaryEditorDropTarget(event, timeline, schedule, duration);
    if (secondaryEditDraggedMaterial) {
      const item = secondaryEditDraggedMaterial;
      secondaryEditDraggedMaterial = null;
      void secondaryEditorOperation({ type: "insert_clip", sourceStart: item.start, sourceEnd: item.end, title: item.title, sourceRef: item.sourceRef, targetIndex });
      return;
    }
    commitSecondaryEditorClipDrop(clips, targetIndex);
  });
  $("#secondaryEditorTimelineMeta").textContent = `${clips.length} 个片段 · ${model.duration.toFixed(1)} 秒${model.preview ? " · 预览" : ""}`;
  const draftState = $("#secondaryEditorTimelineDraftState");
  draftState?.classList.toggle("hidden", !model.preview);
  if (draftState && model.preview) draftState.textContent = `待保存排期 · 原 ${Number(secondaryEditSession.duration || 0).toFixed(2)} 秒 → ${model.duration.toFixed(2)} 秒`;
  $(".secondary-editor-timeline-section")?.classList.toggle("has-timing-preview", model.preview);
  const mediaState = $("#secondaryEditorMediaState");
  if (mediaState) mediaState.textContent = secondaryEditorMediaStateLabel();
  $("#secondaryEditorZoomFit")?.classList.toggle("active", secondaryEditTimelinePixelsPerSecond === null);
  $("#secondaryEditorSnap")?.classList.toggle("active", secondaryEditSnapEnabled);
  $("#secondaryEditorSnap")?.setAttribute("aria-pressed", String(secondaryEditSnapEnabled));
  renderSecondaryEditorSubtitleTrack();
  renderSecondaryEditorTextTrack();
  renderSecondaryEditorMarkers();
  window.requestAnimationFrame(drawSecondaryEditorWaveforms);
  updateSecondaryEditorPlayhead(Math.min(secondaryEditPlayheadTime, duration));
  syncSecondaryEditorInsertionHint();
  syncSecondaryEditorControls();
}

function renderSecondaryEditorInspector() {
  const clip = secondaryEditClip();
  const batch = secondaryEditSelectedClips.size > 1;
  const empty = $("#secondaryInspectorEmpty");
  const form = $("#secondaryInspectorForm");
  empty?.classList.toggle("hidden", Boolean(clip));
  form?.classList.toggle("hidden", !clip);
  $("#secondaryInspectorTitle").textContent = batch ? `批量设置 · ${secondaryEditSelectedClips.size} 个片段` : clip?.title || "选择一个片段";
  if (!clip || !form) return;
  const draft = secondaryEditInspectorDraft?.dirty && String(secondaryEditInspectorDraft.clipId) === String(clip.id)
    ? secondaryEditInspectorDraft
    : null;
  $("#secondaryInspectorStart").value = draft?.rawStart ?? formatPreciseTimecode(clip.sourceStart);
  $("#secondaryInspectorEnd").value = draft?.rawEnd ?? formatPreciseTimecode(clip.sourceEnd);
  $("#secondaryInspectorStart").disabled = batch;
  $("#secondaryInspectorEnd").disabled = batch;
  $("#secondaryInspectorSpeed").value = String(draft?.playbackRate ?? Number(clip.playbackRate || 1));
  $("#secondaryInspectorTransition").value = draft?.transitionType ?? clip.transitionIn?.type ?? "cut";
  $("#secondaryInspectorTransitionDuration").value = String(draft?.transitionDuration ?? Number(clip.transitionIn?.duration || 0));
  $("#secondaryInspectorAudio").value = draft?.audioBridgeType ?? clip.audioBridge?.type ?? "none";
  const gain = Number(draft?.audioGain ?? clip.audioGain ?? 1);
  $("#secondaryInspectorGain").value = String(Math.round(gain * 100));
  $("#secondaryInspectorGainValue").textContent = `${Math.round(gain * 100)}%`;
  $("#secondaryInspectorMuted").checked = Boolean(draft?.muted ?? clip.muted);
  $("#secondaryInspectorFadeIn").value = String(draft?.audioFadeIn ?? Number(clip.audioFadeIn ?? .06));
  $("#secondaryInspectorFadeOut").value = String(draft?.audioFadeOut ?? Number(clip.audioFadeOut ?? .06));
  renderSecondaryEditorInspectorTiming(clip);
  syncSecondaryEditorInspectorDraftState();
}

function captureSecondaryEditorInspectorDraft() {
  const clip = secondaryEditClip();
  if (!clip) return;
  const rawStart = String($("#secondaryInspectorStart")?.value || "").trim();
  const rawEnd = String($("#secondaryInspectorEnd")?.value || "").trim();
  const sourceStart = parseTimecodeValue(rawStart);
  const sourceEnd = parseTimecodeValue(rawEnd);
  const playbackRate = Math.max(.01, Number($("#secondaryInspectorSpeed")?.value || clip.playbackRate || 1));
  const transitionType = String($("#secondaryInspectorTransition")?.value || clip.transitionIn?.type || "cut");
  const audioBridgeType = String($("#secondaryInspectorAudio")?.value || clip.audioBridge?.type || "none");
  const rawTransitionDuration = Math.max(0, Number($("#secondaryInspectorTransitionDuration")?.value || 0));
  const transitionDuration = transitionType === "cut" ? 0 : rawTransitionDuration > 0 ? rawTransitionDuration : .35;
  const audioGain = Math.max(0, Math.min(2, Number($("#secondaryInspectorGain")?.value || 100) / 100));
  const muted = Boolean($("#secondaryInspectorMuted")?.checked);
  const audioFadeIn = Math.max(0, Math.min(.35, Number($("#secondaryInspectorFadeIn")?.value || 0)));
  const audioFadeOut = Math.max(0, Math.min(.35, Number($("#secondaryInspectorFadeOut")?.value || 0)));
  const batch = secondaryEditSelectedClips.size > 1;
  const targets = (secondaryEditSession?.clips || []).filter((item) => secondaryEditSelectedClips.has(String(item.id)));
  const sourceDuration = Math.max(0, Number(currentJob?.videoInfo?.duration || 0));
  const validRange = Number.isFinite(sourceStart) && Number.isFinite(sourceEnd)
    && sourceStart >= 0 && sourceEnd - sourceStart >= .25
    && (!sourceDuration || sourceEnd <= sourceDuration + .01);
  const rangeChanged = !batch && validRange && (
    Math.abs(sourceStart - Number(clip.sourceStart || 0)) > .005
    || Math.abs(sourceEnd - Number(clip.sourceEnd || 0)) > .005
  );
  const speedChanged = targets.some((item) => Math.abs(playbackRate - Number(item.playbackRate || 1)) > .005);
  const transitionChanged = targets.some((item) => transitionType !== String(item.transitionIn?.type || "cut"));
  const audioChanged = targets.some((item) => audioBridgeType !== String(item.audioBridge?.type || "none"));
  const transitionDurationChanged = transitionType !== "cut" && targets.some((item) => Math.abs(transitionDuration - Number(item.transitionIn?.duration || 0)) > .005);
  const mixChanged = targets.some((item) => Math.abs(audioGain - Number(item.audioGain ?? 1)) > .005 || muted !== Boolean(item.muted)
    || Math.abs(audioFadeIn - Number(item.audioFadeIn ?? .06)) > .005 || Math.abs(audioFadeOut - Number(item.audioFadeOut ?? .06)) > .005);
  const dirty = (!batch && !validRange) || rangeChanged || speedChanged || transitionChanged || transitionDurationChanged || audioChanged || mixChanged;
  secondaryEditInspectorDraft = dirty ? {
    clipId: String(clip.id), rawStart, rawEnd, sourceStart, sourceEnd,
    playbackRate, transitionType, transitionDuration, audioBridgeType, audioGain, muted, audioFadeIn, audioFadeOut,
    validRange: batch || validRange, dirty, batch,
    rangeChanged, speedChanged, transitionChanged, transitionDurationChanged, audioChanged, mixChanged,
    timingChanged: !batch && validRange && (rangeChanged || speedChanged || transitionChanged || transitionDurationChanged),
  } : null;
  $("#secondaryInspectorGainValue").textContent = `${Math.round(audioGain * 100)}%`;
  if (secondaryEditInspectorDraft?.dirty && secondaryEditExactPreview) setSecondaryEditorSourceVideo();
  renderSecondaryEditorInspectorTiming(clip);
  renderSecondaryEditorTimeline();
  syncSecondaryEditorInspectorDraftState();
  const previewClip = secondaryEditorTimelineModel().clips.find((item) => String(item.id) === String(clip.id));
  const video = $("#secondaryEditorVideo");
  if (video && previewClip && secondaryEditView === "sequence" && !secondaryEditExactPreview) {
    video.playbackRate = Number(previewClip.playbackRate || 1);
    if (video.currentTime < Number(previewClip.sourceStart) || video.currentTime > Number(previewClip.sourceEnd)) {
      video.currentTime = Number(previewClip.sourceStart);
    }
  }
}

function resetSecondaryEditorInspectorDraft() {
  secondaryEditInspectorDraft = null;
  const clip = secondaryEditClip();
  if (clip) {
    const video = $("#secondaryEditorVideo");
    if (video && secondaryEditView === "sequence" && !secondaryEditExactPreview) video.playbackRate = Number(clip.playbackRate || 1);
  }
  renderSecondaryEditorInspector();
  renderSecondaryEditorTimeline();
  syncSecondaryEditorInspectorDraftState();
}

function syncSecondaryEditorInspectorDraftState() {
  const dirty = secondaryEditorInspectorHasChanges();
  const valid = !dirty || Boolean(secondaryEditInspectorDraft?.validRange);
  $("#secondaryInspectorForm")?.classList.toggle("has-pending-changes", dirty);
  if ($("#secondaryInspectorReset")) $("#secondaryInspectorReset").disabled = secondaryEditBusy || !dirty;
  if ($("#secondaryInspectorSave")) $("#secondaryInspectorSave").disabled = secondaryEditBusy || !dirty || !valid;
  const state = $("#secondaryEditorSaveState");
  if (state && !secondaryEditBusy && state.dataset.state !== "error") {
    state.textContent = dirty
      ? "未保存"
      : "已保存";
    state.dataset.state = dirty ? "draft" : "saved";
  }
  renderSecondaryEditorPreflight();
  syncSecondaryEditorControls();
}

function renderSecondaryEditorInspectorTiming(clip = secondaryEditClip()) {
  if (!clip) return;
  const startInput = $("#secondaryInspectorStart");
  const endInput = $("#secondaryInspectorEnd");
  const rawStart = parseTimecodeValue(startInput?.value);
  const rawEnd = parseTimecodeValue(endInput?.value);
  const sourceLimit = Math.max(0, Number(currentJob?.videoInfo?.duration || 0));
  const validRange = Number.isFinite(rawStart) && Number.isFinite(rawEnd) && rawStart >= 0
    && rawEnd - rawStart >= .25 && (!sourceLimit || rawEnd <= sourceLimit + .01);
  startInput?.setAttribute("aria-invalid", String(!Number.isFinite(rawStart) || rawStart < 0 || Number.isFinite(rawEnd) && rawEnd - rawStart < .25));
  endInput?.setAttribute("aria-invalid", String(!Number.isFinite(rawEnd) || sourceLimit && rawEnd > sourceLimit + .01 || Number.isFinite(rawStart) && rawEnd - rawStart < .25));
  const summary = $("#secondaryInspectorTimingSummary");
  const startMeta = $("#secondaryInspectorStartMeta");
  const endMeta = $("#secondaryInspectorEndMeta");
  if (!validRange) {
    if (startMeta) startMeta.textContent = Number.isFinite(rawStart) ? `${rawStart.toFixed(2)} 秒` : "请输入有效时间码";
    if (endMeta) endMeta.textContent = Number.isFinite(rawEnd) ? `${rawEnd.toFixed(2)} 秒` : "请输入有效时间码";
    if (summary) summary.innerHTML = `<small class="invalid">源片范围必须位于视频内，且至少保留 0.25 秒${sourceLimit ? `（源视频总长 ${formatPreciseTimecode(sourceLimit)}）` : ""}。</small>`;
    return;
  }
  const start = rawStart;
  const end = rawEnd;
  const rate = Math.max(.01, Number($("#secondaryInspectorSpeed")?.value || clip.playbackRate || 1));
  const fps = secondaryEditorFrameRate();
  const sourceDuration = Math.max(0, end - start);
  const playbackDuration = sourceDuration / rate;
  const index = Math.max(0, (secondaryEditSession?.clips || []).findIndex((item) => String(item.id) === String(clip.id)));
  const model = secondaryEditorTimelineModel();
  const schedule = model.schedule[index] || {};
  const outputStart = Number(schedule.outputStart || 0);
  const changed = Math.abs(start - Number(clip.sourceStart || 0)) > .005
    || Math.abs(end - Number(clip.sourceEnd || 0)) > .005
    || Math.abs(rate - Number(clip.playbackRate || 1)) > .005
    || String($("#secondaryInspectorTransition")?.value || "cut") !== String(clip.transitionIn?.type || "cut");
  const outputEnd = Number(schedule.outputEnd ?? outputStart + playbackDuration);
  if (startMeta) startMeta.textContent = `${start.toFixed(2)} 秒 · TC ${formatFrameTimecode(start, fps)}`;
  if (endMeta) endMeta.textContent = `${end.toFixed(2)} 秒 · TC ${formatFrameTimecode(end, fps)}`;
  if (!summary) return;
  const overlap = Number(schedule.transitionOverlap || 0);
  const savedDuration = Number(secondaryEditSession?.duration || 0);
  summary.innerHTML = `<div class="secondary-inspector-timing-line"><span>源片 <b>${sourceDuration.toFixed(2)} 秒</b></span><span>播放 <b>${playbackDuration.toFixed(2)} 秒${rate !== 1 ? ` / ${rate}×` : ""}</b></span><span>${changed ? "预计位置" : "成片位置"} <b>${formatPreciseTimecode(outputStart)} - ${formatPreciseTimecode(outputEnd)}</b></span><span>${changed ? "预计总长" : "成片总长"} <b>${changed ? `${savedDuration.toFixed(2)} - ` : ""}${model.duration.toFixed(2)} 秒</b></span></div>${overlap ? `<small>与前一片段有 ${overlap.toFixed(2)} 秒转场重叠，成片总长会相应缩短。</small>` : ""}`;
}

function renderSecondaryEditorProposal() {
  const host = $("#secondaryEditorProposal");
  const proposal = secondaryEditSession?.pendingProposal;
  if (!host) return;
  host.closest(".secondary-ai-editor")?.classList.toggle("has-proposal", Boolean(proposal));
  host.classList.toggle("hidden", !proposal);
  if (!proposal) { host.innerHTML = ""; return; }
  const preview = proposal.preview || {};
  const variants = Array.isArray(secondaryEditSession?.proposalVariants) ? secondaryEditSession.proposalVariants : [];
  const variantNav = variants.length > 1
    ? `<nav class="secondary-proposal-variants" aria-label="时间线结构方案">${variants.map((item, index) => `<button type="button" class="${String(item.id) === String(proposal.id) ? "active" : ""}" data-secondary-proposal-select="${escapeHtml(String(item.id || ""))}"><b>方案 ${index + 1}</b><span>${escapeHtml(item.title || item.direction || "结构方案")}</span></button>`).join("")}</nav>`
    : "";
  const clipsById = new Map((secondaryEditSession?.clips || []).map((clip, index) => [String(clip.id), { ...clip, index }]));
  const operationLabels = (proposal.operations || []).map((operation) => {
    const ids = operation.type === "update_clip" || operation.type === "trim_clip" || operation.type === "split_clip"
      ? [String(operation.clipId || "")]
      : (operation.clipIds || []).map(String);
    const names = ids.map((id) => clipsById.get(id)).filter(Boolean).map((clip) => `${String(clip.index + 1).padStart(2, "0")} · ${clip.title || "片段"}`);
    const target = names.length ? ` · ${names.slice(0, 2).join("、")}${names.length > 2 ? ` 等 ${names.length} 段` : ""}` : "";
    if (operation.type === "delete_clips") return `移除 ${ids.length || 1} 个片段${target}`;
    if (operation.type === "reorder_clips") return `重新排列整个成片，共 ${ids.length} 个片段`;
    if (operation.type === "split_clip") return `拆分片段${target}`;
    if (operation.type === "trim_clip") return `调整片段入点或出点${target}`;
    if (operation.type === "add_marker") return `添加时间线标记${target}`;
    if (["update_clip", "update_clips"].includes(operation.type)) {
      const settings = [];
      if (operation.playbackRate) settings.push(`速度 ${Number(operation.playbackRate)}×`);
      if (operation.transitionType) settings.push(`转场 ${({ cut: "硬切", dissolve: "短叠化", fade_black: "淡黑" })[operation.transitionType] || operation.transitionType}`);
      if (operation.muted === true) settings.push("静音");
      if (operation.muted === false) settings.push("恢复声音");
      return `${settings.length ? settings.join("、") : "更新片段设置"}${target}`;
    }
    return `更新时间线${target}`;
  });
  const preflightIssues = preview.preflight?.issues || [];
  const issueMarkup = preflightIssues.length
    ? `<div class="secondary-proposal-warning"><strong>应用前提示</strong><span>${preflightIssues.map((item) => escapeHtml(item.message || "请检查此项修改")).join("；")}</span></div>`
    : "";
  host.innerHTML = `${variantNav}<div class="secondary-proposal-heading"><b>待确认提案</b><strong>${escapeHtml(proposal.title || "快捷编辑提案")}</strong></div><p>${escapeHtml(proposal.summary || "待确认修改")}</p><dl class="secondary-proposal-impact"><div><dt>修改</dt><dd>${Number(preview.operationCount || proposal.operations?.length || 0)} 项</dd></div><div><dt>时长</dt><dd>${Number(preview.durationBefore || 0).toFixed(1)} → ${Number(preview.durationAfter || 0).toFixed(1)} 秒</dd></div><div><dt>片段</dt><dd>${Number(preview.clipCountBefore || 0)} → ${Number(preview.clipCountAfter || 0)}</dd></div></dl>${operationLabels.length ? `<section class="secondary-proposal-operations"><strong>具体会修改</strong><ol>${operationLabels.map((label) => `<li>${escapeHtml(label)}</li>`).join("")}</ol></section>` : ""}${issueMarkup}<small>当前时间线尚未改变。${variants.length > 1 ? "先比较结构方案，再应用其中一份。" : ""}应用后请生成审核样片，核对实际画面。</small><footer><button type="button" data-secondary-proposal-cancel>放弃提案</button><button class="primary" type="button" data-secondary-proposal-apply>应用到时间线</button></footer>`;
  host.querySelectorAll("[data-secondary-proposal-select]").forEach((button) => button.addEventListener("click", () => selectSecondaryEditorProposal(button.dataset.secondaryProposalSelect)));
  host.querySelector("[data-secondary-proposal-cancel]")?.addEventListener("click", cancelSecondaryEditorProposal);
  host.querySelector("[data-secondary-proposal-apply]")?.addEventListener("click", applySecondaryEditorProposal);
}

async function selectSecondaryEditorProposal(proposalId) {
  if (!proposalId || !currentJob?.id || !secondaryEditSession?.id || secondaryEditBusy) return;
  const context = captureSecondaryEditorAction();
  setSecondaryEditorBusy(true, "正在切换结构方案");
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/edit-sessions/${encodeURIComponent(secondaryEditSession.id)}/proposals/${encodeURIComponent(proposalId)}/select`, { method: "POST", body: {} });
    if (!context.current()) return;
    secondaryEditSession = payload.session;
    syncSecondaryEditorSessionToJob();
    renderSecondaryEditorProposal();
  } catch (error) { if (context.current()) showToast(error.message); }
  finally { if (context.current(false)) setSecondaryEditorBusy(false, "方案待确认"); }
}

function renderSecondaryEditor() {
  if (!secondaryEditSession) return;
  const outputHint = $("#secondaryEditorOutputSettingsHint");
  if (outputHint) outputHint.textContent = secondaryEditSession.reframe?.aspect
    ? `成片 ${secondaryEditSession.reframe.aspect} · ${secondaryEditSession.reframe.fit === "crop" ? "居中裁切" : "完整画面、虚化补边"}`
    : "使用当前精剪的画幅和清晰度设置。";
  const base = (currentJob?.outputVersions || []).find((version) => version.id === secondaryEditSession.baseVersionId);
  const identity = base ? `V${base.number || 1}` : "当前方案";
  $("#secondaryEditorTitle").textContent = `正在编辑 ${identity} 草稿 · 已采用 ${secondaryEditSession.clips?.length || 0} 段，共 ${secondaryEditorTimelineDuration().toFixed(1)} 秒`;
  $("#secondaryEditorClose").textContent = base ? `← 返回 ${identity}` : "← 返回当前任务";
  $("#secondaryEditorClose").setAttribute("aria-label", base ? `返回 ${identity} 预览` : "返回当前任务");
  $("#secondaryEditorDelete").textContent = "从当前结果移除";
  const state = $("#secondaryEditorSaveState");
  if (state && !secondaryEditBusy && state.dataset.state !== "error" && state.dataset.state !== "draft") {
    state.textContent = secondaryEditSession.status === "rendering" ? "正在生成版本" : "草稿已加载";
    state.dataset.operation = secondaryEditSession.status === "rendering" ? "render" : "idle";
  }
  renderSecondaryEditorLibrary();
  renderSecondaryEditorTimeline();
  renderSecondaryEditorInspector();
  renderSecondaryEditorProposal();
  const subtitleEnabled = Boolean(secondaryEditSession.subtitleEnabled);
  $("#secondaryEditorSubtitleEnabled").checked = subtitleEnabled;
  const subtitleReadiness = secondaryEditorSubtitleBurnReadiness();
  $("#secondaryEditorSubtitleStatus").textContent = !subtitleEnabled
    ? "字幕必须确认后才能烧录"
    : subtitleReadiness.ready ? "字幕草稿已确认并绑定当前时间线"
      : subtitleReadiness.message || "字幕草稿尚未完成校对";
  const summary = $("#secondaryEditorSubtitleSummary");
  if (summary) {
    summary.textContent = !subtitleEnabled ? "未启用" : subtitleReadiness.ready ? "已确认字幕" : "待校对";
    summary.classList.toggle("active", subtitleEnabled && subtitleReadiness.ready);
  }
  void loadSecondaryEditorSubtitleDraft();
  renderSecondaryEditorPreflight();
  const previewState = $("#secondaryEditorPreviewState");
  if (previewState && !secondaryEditPreviewPending && !secondaryEditExactPreview) {
    if (secondaryEditSession.previewStatus === "stale") {
      previewState.textContent = "需更新预览";
      previewState.dataset.tone = "stale";
      previewState.classList.remove("hidden");
    } else if (secondaryEditSession.previewStatus === "failed") {
      previewState.textContent = secondaryEditSession.previewError || "预览样片生成失败";
      previewState.dataset.tone = "error";
      previewState.classList.remove("hidden");
    }
  }
  syncSecondaryInspectorMode();
  syncSecondaryEditorControls();
}

function renderSecondaryEditorPreflight() {
  const node = $("#secondaryEditorPreflight");
  if (!node || !secondaryEditSession) return;
  const model = secondaryEditorTimelineModel();
  const preflight = secondaryEditSession.preflight || {};
  const dirty = secondaryEditorInspectorHasChanges();
  const blocking = (preflight.issues || []).filter(item => item.severity === "error");
  const warnings = (preflight.issues || []).filter(item => item.severity === "warning");
  const title = dirty ? "修改预览 · 保存后重新检查" : blocking.length ? "生成前需修正"
    : warnings.length ? "时间线检查有提醒" : preflight.ready === true ? "时间线检查通过" : "时间线尚未检查";
  const details = dirty ? [] : [...blocking, ...warnings].map(item => escapeHtml(item.message));
  node.dataset.tone = dirty ? "pending" : blocking.length ? "error" : warnings.length ? "warning" : preflight.ready === true ? "ready" : "pending";
  node.innerHTML = `<strong>${title}</strong><small>${model.clips.length} 个片段 · 成片 ${formatPreciseTimecode(model.duration)}${details.length ? ` · ${details.join("；")}` : ""}</small>`;
}

function syncSecondaryInspectorMode() {
  document.querySelectorAll("[data-secondary-inspector-tab]").forEach((button) => {
    const active = button.dataset.secondaryInspectorTab === secondaryInspectorMode;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
  });
  document.querySelectorAll("[data-secondary-inspector-panel]").forEach((panel) => {
    panel.classList.toggle("inspector-panel-hidden", panel.dataset.secondaryInspectorPanel !== secondaryInspectorMode);
  });
}

function setSecondaryInspectorMode(mode = "clip") {
  secondaryInspectorMode = ["clip", "subtitle", "ai", "export"].includes(mode) ? mode : "clip";
  syncSecondaryInspectorMode();
  const editor = $("#secondaryEditor");
  if (editor) {
    editor.classList.add("inspector-open");
    editor.classList.remove("library-open");
    syncSecondaryEditorPanelAria();
    persistSecondaryEditorLayout();
    scheduleSecondaryEditorTimelineLayout();
  }
}

function syncSecondaryEditorControls() {
  const selected = secondaryEditSelectedClips.size;
  const pendingSettings = secondaryEditorInspectorHasChanges();
  if ($("#secondaryEditorUndo")) $("#secondaryEditorUndo").disabled = secondaryEditBusy || pendingSettings || !secondaryEditSession?.canUndo;
  if ($("#secondaryEditorRedo")) $("#secondaryEditorRedo").disabled = secondaryEditBusy || pendingSettings || !secondaryEditSession?.canRedo;
  if ($("#secondaryEditorDelete")) $("#secondaryEditorDelete").disabled = secondaryEditBusy || pendingSettings || !selected;
  const context = secondaryEditorPlayheadContext();
  const insideClip = Boolean(context && context.sourceTime - Number(context.clip.sourceStart) >= .25 && Number(context.clip.sourceEnd) - context.sourceTime >= .25);
  if ($("#secondaryEditorSplit")) $("#secondaryEditorSplit").disabled = secondaryEditBusy || pendingSettings || selected !== 1 || !insideClip;
  if ($("#secondaryEditorTrimLeft")) $("#secondaryEditorTrimLeft").disabled = secondaryEditBusy || pendingSettings || selected !== 1 || !insideClip;
  if ($("#secondaryEditorTrimRight")) $("#secondaryEditorTrimRight").disabled = secondaryEditBusy || pendingSettings || selected !== 1 || !insideClip;
  if ($("#secondaryEditorDuplicate")) $("#secondaryEditorDuplicate").disabled = secondaryEditBusy || pendingSettings || !selected;
  if ($("#secondaryEditorMarker")) $("#secondaryEditorMarker").disabled = secondaryEditBusy || pendingSettings || !context;
  if ($("#secondaryEditorExport")) $("#secondaryEditorExport").disabled = secondaryEditBusy || pendingSettings || !secondaryEditSession?.clips?.length || secondaryEditSession?.preflight?.ready === false;
  if ($("#secondaryEditorPreview")) $("#secondaryEditorPreview").disabled = secondaryEditBusy || secondaryEditPreviewPending || pendingSettings || !secondaryEditSession?.clips?.length;
  if ($("#secondaryEditorSubtitleReview")) $("#secondaryEditorSubtitleReview").disabled = secondaryEditBusy || pendingSettings;
  if ($("#secondaryEditorSubtitleEnabled")) $("#secondaryEditorSubtitleEnabled").disabled = secondaryEditBusy || pendingSettings;
  if ($("#secondaryInspectorReset")) $("#secondaryInspectorReset").disabled = secondaryEditBusy || !pendingSettings;
  if ($("#secondaryInspectorSave")) $("#secondaryInspectorSave").disabled = secondaryEditBusy || !pendingSettings || !secondaryEditInspectorDraft?.validRange;
  if ($("#secondaryInspectorTransitionDuration")) $("#secondaryInspectorTransitionDuration").disabled = secondaryEditBusy || $("#secondaryInspectorTransition")?.value === "cut";
  if ($("#secondaryEditorInsertTarget")) $("#secondaryEditorInsertTarget").disabled = secondaryEditBusy;
  if ($("#secondaryEditorInsertFollow")) $("#secondaryEditorInsertFollow").disabled = secondaryEditBusy || secondaryEditInsertionIndexOverride === null;
  const previewCurrent = secondaryEditorPreviewIsCurrent();
  const previewButton = $("#secondaryEditorPreview");
  const exportButton = $("#secondaryEditorExport");
  const freshness = $("#secondaryEditorPreviewFreshness");
  if (previewButton) {
    previewButton.textContent = previewCurrent ? "重新生成审核样片" : secondaryEditPreviewPending ? "正在生成审核样片" : "生成审核样片";
    previewButton.dataset.state = previewCurrent ? "current" : "required";
  }
  if (exportButton) {
    exportButton.textContent = "生成成片";
    exportButton.disabled ||= !previewCurrent;
    exportButton.dataset.state = previewCurrent ? "ready" : "blocked";
    exportButton.title = previewCurrent
      ? "生成当前剪辑的成片版本"
      : "请先更新审核样片";
  }
  if (freshness) {
    const proposalPending = Boolean(secondaryEditSession?.pendingProposal);
    freshness.textContent = proposalPending
      ? "提案待确认"
      : secondaryEditPreviewPending
        ? "正在生成预览"
        : previewCurrent
          ? "预览为最新"
          : secondaryEditSession?.previewStatus === "failed"
            ? "预览生成失败"
            : "预览待更新";
    freshness.dataset.tone = proposalPending
      ? "proposal"
      : secondaryEditPreviewPending
        ? "loading"
        : previewCurrent
          ? "ready"
          : secondaryEditSession?.previewStatus === "failed" ? "error" : "stale";
  }
  syncSecondaryEditorAiScope();
  const textDelete = $("#secondaryTextDelete");
  if (textDelete) {
    textDelete.classList.toggle("hidden", !secondaryEditSelectedTextLayerId);
    textDelete.disabled = secondaryEditBusy || !secondaryEditorTextLayer();
  }
}

function syncSecondaryEditorSessionToJob(session = secondaryEditSession) {
  if (!currentJob || !session?.id) return;
  const sessions = Array.isArray(currentJob.editSessions) ? [...currentJob.editSessions] : [];
  const index = sessions.findIndex((item) => String(item?.id || "") === String(session.id));
  if (index >= 0) sessions[index] = session;
  else sessions.push(session);
  currentJob.editSessions = sessions;
  currentJob.activeEditSessionId = String(session.id);
  currentJob.activeEditSession = session;
}

function activateSecondaryEditorSession(session, job = currentJob, { inspectorMode = "clip", instruction = "" } = {}) {
    secondaryEditActivationToken += 1;
    secondaryEditSession = session;
    const saveState = $("#secondaryEditorSaveState");
    if (saveState) { saveState.dataset.state = "saved"; saveState.dataset.operation = "idle"; }
    currentJob = job || currentJob;
    syncSecondaryEditorSessionToJob(session);
    secondaryEditSelectedClips = new Set(secondaryEditSession.clips?.[0]?.id ? [String(secondaryEditSession.clips[0].id)] : []);
    secondaryEditSequenceIndex = 0;
    secondaryEditView = "sequence";
    secondaryEditSourceTime = 0;
    secondaryEditInsertionIndexOverride = null;
    secondaryEditPreviewMaterialId = null;
    secondaryEditPreviewMaterialRange = null;
    secondaryEditorMaterialFilter = null;
    secondaryEditorMaterialSearch = "";
    if ($("#secondaryEditorLibrarySearch")) $("#secondaryEditorLibrarySearch").value = "";
    secondaryEditExactPreview = false;
    secondaryInspectorMode = ["clip", "subtitle", "ai", "export"].includes(inspectorMode) ? inspectorMode : "clip";
    secondaryEditTimelinePixelsPerSecond = null;
    secondaryEditPlayheadTime = 0;
    secondaryEditSnapEnabled = true;
    secondaryEditTrimDrag = null;
    secondaryEditCueDrag = null;
    secondaryEditSubtitleCanvasDrag = null;
    secondaryEditSubtitleDraft = null;
    secondaryEditSubtitleLoadingId = null;
    secondaryEditSubtitleNeedsReview = false;
    secondaryEditInspectorDraft = null;
    secondaryEditSelectionAnchorId = secondaryEditSession.clips?.[0]?.id ? String(secondaryEditSession.clips[0].id) : null;
    secondaryEditDropIndex = null;
    secondaryEditDraggedMaterial = null;
    secondaryEditSelectedMarkerId = null;
    secondaryEditSelectedCueId = null;
    secondaryEditSelectedTextLayerId = null;
    secondaryEditTextLayerDrag = null;
    secondaryEditTextTimeDrag = null;
    secondaryEditPreviewPending = false;
    secondaryEditPreviewRequestToken += 1;
    const aiScope = $("#secondaryEditorAiScope");
    if (aiScope) aiScope.value = instruction ? "timeline" : "selection";
    restoreSecondaryEditorLayout();
    setSecondaryEditorSourceVideo();
    syncSecondaryEditorViewButtons();
    renderSecondaryEditor();
    secondaryEditorSeekOutputTime(0, false);
    const aiInput = $("#secondaryEditorAiInput");
    if (aiInput && instruction && !secondaryEditSession.pendingProposal) aiInput.value = String(instruction).slice(0, 500);
    void loadTimelineAssets(currentJob).finally(() => { if (secondaryEditorOpen()) renderSecondaryEditorTimeline(); });
    void loadWaveform(currentJob).finally(() => { if (secondaryEditorOpen()) renderSecondaryEditorTimeline(); });
}

function secondaryEditorStoredLayout() {
  try {
    const value = JSON.parse(localStorage.getItem(secondaryEditorLayoutStorageKey) || "null");
    return value && typeof value === "object" ? value : null;
  } catch { return null; }
}

function persistSecondaryEditorLayout() {
  const editor = $("#secondaryEditor");
  if (!editor) return;
  try {
    localStorage.setItem(secondaryEditorLayoutStorageKey, JSON.stringify({
      upperRatio: secondaryEditorUpperRatio,
      inspectorWidth: secondaryEditorInspectorWidth,
      libraryOpen: editor.classList.contains("library-open"),
      inspectorOpen: editor.classList.contains("inspector-open"),
    }));
  } catch { /* Layout persistence is optional. */ }
}

function secondaryEditorInspectorWidthBounds() {
  const layoutWidth = $(".secondary-editor-layout")?.getBoundingClientRect()?.width || window.innerWidth || 1024;
  const min = 360;
  const max = Math.max(min, Math.min(560, layoutWidth - 620));
  return { min, max };
}

function secondaryEditorDefaultInspectorWidth() {
  return Math.max(380, Math.min(460, Math.round((window.innerWidth || 1440) * .24)));
}

function scheduleSecondaryEditorTimelineLayout() {
  if (!secondaryEditorOpen()) return;
  if (secondaryEditorTimelineLayoutFrame !== null) window.cancelAnimationFrame(secondaryEditorTimelineLayoutFrame);
  secondaryEditorTimelineLayoutFrame = window.requestAnimationFrame(() => {
    secondaryEditorTimelineLayoutFrame = null;
    renderSecondaryEditorTimeline();
  });
}

function setSecondaryEditorInspectorWidth(value, { persist = false } = {}) {
  const { min, max } = secondaryEditorInspectorWidthBounds();
  secondaryEditorInspectorWidth = Math.max(min, Math.min(max, Number(value) || secondaryEditorDefaultInspectorWidth()));
  const editor = $("#secondaryEditor");
  editor?.style.setProperty("--secondary-inspector-width", `${secondaryEditorInspectorWidth}px`);
  const handle = $("#secondaryEditorInspectorResize");
  handle?.setAttribute("aria-valuemin", String(Math.round(min)));
  handle?.setAttribute("aria-valuemax", String(Math.round(max)));
  handle?.setAttribute("aria-valuenow", String(Math.round(secondaryEditorInspectorWidth)));
  if (persist) persistSecondaryEditorLayout();
  if (secondaryEditTimelinePixelsPerSecond === null) scheduleSecondaryEditorTimelineLayout();
}

function setSecondaryEditorUpperRatio(value, { persist = false } = {}) {
  secondaryEditorUpperRatio = Math.max(36, Math.min(58, Number(value) || 58));
  const editor = $("#secondaryEditor");
  editor?.style.setProperty("--secondary-editor-upper-ratio", `${secondaryEditorUpperRatio}%`);
  const handle = $("#secondaryEditorWorkspaceResize");
  handle?.setAttribute("aria-valuenow", String(Math.round(secondaryEditorUpperRatio)));
  if (persist) persistSecondaryEditorLayout();
  if (secondaryEditTimelinePixelsPerSecond === null) scheduleSecondaryEditorTimelineLayout();
}

function syncSecondaryEditorPanelAria() {
  const editor = $("#secondaryEditor");
  if (!editor) return;
  const libraryOpen = editor.classList.contains("library-open");
  const inspectorOpen = editor.classList.contains("inspector-open");
  const compact = window.innerWidth <= 780;
  $("#secondaryEditorLibraryToggle")?.setAttribute("aria-expanded", String(libraryOpen));
  $("#secondaryEditorInspectorToggle")?.setAttribute("aria-expanded", String(inspectorOpen));
  const library = $("#secondaryEditorLibrary");
  const inspector = $("#secondaryEditorInspector");
  if (compact) {
    if (library) library.inert = !libraryOpen;
    if (inspector) inspector.inert = !inspectorOpen;
  } else {
    if (library) library.inert = false;
    if (inspector) inspector.inert = false;
  }
  $("#secondaryEditorPanelScrim")?.classList.toggle("hidden", !compact || (!libraryOpen && !inspectorOpen));
  renderSecondaryEditorSubtitleTrack();
}

function restoreSecondaryEditorLayout() {
  const editor = $("#secondaryEditor");
  if (!editor) return;
  const stored = secondaryEditorStoredLayout();
  setSecondaryEditorUpperRatio(stored?.upperRatio ?? 58);
  setSecondaryEditorInspectorWidth(stored?.inspectorWidth ?? secondaryEditorDefaultInspectorWidth());
  editor.classList.remove("library-open", "inspector-open");
  if (stored?.inspectorOpen) editor.classList.add("inspector-open");
  else if (stored?.libraryOpen) editor.classList.add("library-open");
  syncSecondaryEditorPanelAria();
}

const secondaryEditorShellIsolation = new Map();

function setSecondaryEditorShellIsolation(open) {
  document.body.classList.toggle("secondary-editor-open", open);
  const editor = $("#secondaryEditor");
  const background = [$("#appSidebar"), $("#ctV4Topbar"), $("#workspace"), $("#libraryView"), $("#settingsPanel")].filter(Boolean);
  if (open) {
    window.ClipTalkPrepareWorkspaceOverlay?.("secondaryEditor");
    secondaryEditorShellIsolation.clear();
    background.forEach((node) => {
      secondaryEditorShellIsolation.set(node, { inert: node.inert, ariaHidden: node.getAttribute("aria-hidden") });
      node.inert = true;
      node.setAttribute("aria-hidden", "true");
    });
    if (editor) {
      editor.inert = false;
      editor.setAttribute("aria-hidden", "false");
      editor.dataset.overlayState = "open";
    }
  } else {
    secondaryEditorShellIsolation.forEach((state, node) => {
      node.inert = state.inert;
      if (state.ariaHidden === null) node.removeAttribute("aria-hidden");
      else node.setAttribute("aria-hidden", state.ariaHidden);
    });
    secondaryEditorShellIsolation.clear();
    if (editor) {
      editor.inert = true;
      editor.setAttribute("aria-hidden", "true");
      editor.dataset.overlayState = "closed";
    }
  }
}

async function openSecondaryEditor(versionId, outputFilename, sourceDraft = null) {
  if (!currentJob?.id || (!versionId && !sourceDraft?.sourceSearchId) || secondaryEditBusy) return;
  const jobId = String(currentJob.id);
  const activation = ++secondaryEditActivationToken;
  const action = captureJobAction();
  const current = () => jobActionStillCurrent(action) && activation === secondaryEditActivationToken && secondaryEditorOpen();
  $("#secondaryEditor")?.classList.remove("hidden");
  setSecondaryEditorShellIsolation(true);
  restoreSecondaryEditorLayout();
  setSecondaryEditorBusy(true, sourceDraft ? "正在建立内容精剪草稿" : "正在建立版本草稿");
  try {
    const body = sourceDraft || { baseVersionId: versionId, outputFilename };
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(jobId)}/edit-sessions`, { method: "POST", body });
    if (!current()) return;
    setSecondaryEditorBusy(false);
    activateSecondaryEditorSession(payload.session, payload.job || currentJob);
  } catch (error) {
    if (!current()) return;
    closeSecondaryEditor();
    showToast(error.message);
  } finally {
    if (current()) setSecondaryEditorBusy(
      false,
      secondaryEditSession ? "已保存" : "",
    );
  }
}

window.ClipTalkOpenAgentTimeline = async ({ sessionId = "", instruction = "", variantCount = 1, reviewPendingProposal = false } = {}) => {
  if (!currentJob?.id || secondaryEditBusy) return false;
  const jobId = String(currentJob.id);
  let job = currentJob;
  let session = (job.editSessions || []).find((item) => String(item?.id || "") === String(sessionId));
  if (!session) {
    const payload = await api(`/api/jobs/${encodeURIComponent(jobId)}`);
    if (!currentJob || String(currentJob.id) !== jobId) return false;
    job = payload.job || currentJob;
    session = (job.editSessions || []).find((item) => String(item?.id || "") === String(sessionId));
  }
  if (!session) throw new Error("Agent 创建的精剪时间线不存在，请重新运行当前计划");
  closeCandidateDrawer();
  $("#secondaryEditor")?.classList.remove("hidden");
  setSecondaryEditorShellIsolation(true);
  restoreSecondaryEditorLayout();
  setSecondaryEditorBusy(true, "正在打开 Agent 精剪时间线");
  try {
    const request = session.agentTimelineRequest || {};
    const proposalInstruction = String(instruction || request.instruction || "").trim();
    activateSecondaryEditorSession(session, job, { inspectorMode: "ai", instruction: proposalInstruction });
    setSecondaryInspectorMode("ai");
    const count = Math.max(1, Math.min(4, Number(variantCount || request.variantCount || 1)));
    const hasPendingProposal = Boolean(session.pendingProposal);
    if (hasPendingProposal && !reviewPendingProposal) {
      const apply = $("#secondaryEditorProposal [data-secondary-proposal-apply]");
      if (apply) {
        apply.disabled = true;
        apply.textContent = "核对后返回 Agent";
        apply.title = "下一步确认时间线时再应用草案";
      }
    }
    showToast(reviewPendingProposal
      ? (hasPendingProposal
        ? "请再次核对草案；确认无误后点击“应用到草稿”，再返回 Agent 继续。"
        : String(session.previewStatus || "") === "ready"
        ? "审核样片已经就绪。"
        : "已打开 Agent 时间线结果。")
      : hasPendingProposal
        ? "Agent 已生成草案。请先核对但暂不应用，返回 Agent 点击“草案已核对，继续”。"
        : count > 1
        ? (Array.isArray(session.proposalVariants) && session.proposalVariants.length > 1
          ? `已生成 ${session.proposalVariants.length} 个结构方向；请切换比较后选择一份。`
          : `计划要求 ${count} 个结构方向；当前仅生成一份有效草案，可修改指令后重新生成。`)
        : "计划指令已填入“AI 修改”，点击“查看修改预案”生成待审核草案。", "success");
    if (!reviewPendingProposal && !hasPendingProposal) $("#secondaryEditorAiInput")?.focus();
    return true;
  } catch (error) {
    closeSecondaryEditor();
    throw error;
  } finally {
    setSecondaryEditorBusy(false, secondaryEditSession?.pendingProposal ? "提案待确认" : secondaryEditSession ? "精剪已载入" : "");
  }
};

window.ClipTalkOpenAgentReview = async ({ sessionId = "" } = {}) => {
  const opened = await window.ClipTalkOpenAgentTimeline({ sessionId, reviewPendingProposal: true });
  if (!opened || !secondaryEditSession) return false;
  const previewUrl = String(secondaryEditSession.previewUrl || "");
  if (!previewUrl || String(secondaryEditSession.previewStatus || "") !== "ready") {
    throw new Error("审核样片尚未就绪，请稍后刷新计划状态");
  }
  const video = $("#secondaryEditorVideo");
  if (!video) throw new Error("审核播放器不可用");
  secondaryEditView = "sequence";
  secondaryEditExactPreview = true;
  const fingerprint = encodeURIComponent(String(secondaryEditSession.previewFingerprint || ""));
  video.dataset.sourceUrl = previewUrl;
  video.src = `${previewUrl}${previewUrl.includes("?") ? "&" : "?"}fingerprint=${fingerprint}`;
  video.load();
  syncSecondaryEditorViewButtons();
  renderSecondaryEditor();
  $("#secondaryEditorVideoBadge").textContent = "审核样片";
  $("#secondaryEditorVideoBadge").classList.remove("hidden");
  setSecondaryEditorBusy(false, `审核样片 · 草稿 r${secondaryEditSession.revision}`);
  video.play().catch(() => {});
  showToast("已打开审核样片", "success");
  return true;
};

window.ClipTalkOpenAgentSubtitleReview = async ({ sessionId = "" } = {}) => {
  if (!currentJob?.id) return false;
  const targetSessionId = String(sessionId || currentJob.activeEditSessionId || "");
  const session = (currentJob.editSessions || []).find(
    (item) => String(item?.id || "") === targetSessionId,
  );
  if (!session?.clips?.length) throw new Error("当前计划没有可用于生成字幕的时间线");
  const actionToken = captureJobAction();
  const segments = session.clips.map((clip) => ({
    id: clip.id,
    start: Number(clip.sourceStart),
    end: Number(clip.sourceEnd),
    playbackRate: Number(clip.playbackRate || 1),
    silenceCuts: Array.isArray(clip.silenceCuts) ? clip.silenceCuts : [],
    transitionIn: clip.transitionIn || { type: "cut", duration: 0 },
  }));
  const draft = await reviewSubtitlesBeforeRender(
    [{ segments }],
    session.subtitleStyle || "clean",
  );
  if (!draft || !jobActionStillCurrent(actionToken)) return false;
  const payload = await apiJson(
    `/api/jobs/${encodeURIComponent(actionToken.jobId)}/edit-sessions/${encodeURIComponent(session.id)}`,
    {
      method: "PATCH",
      body: {
        revision: Number(session.revision || 0),
        operation: {
          type: "set_subtitle",
          enabled: true,
          subtitleDraftId: draft.id,
          subtitleStyle: session.subtitleStyle || "clean",
        },
      },
    },
  );
  if (!jobActionStillCurrent(actionToken)) return false;
  syncSecondaryEditorSessionToJob(payload.session);
  window.ClipTalkRefreshCurrentJob?.();
  showToast("字幕文字与断句已保存；将继续生成带字幕审核样片。", "success");
  return true;
};

window.ClipTalkOpenAgentPreview = async ({
  id = "", filename = "", previewUrl = "", videoUrl = "", title = "审核样片",
  width = 0, height = 0, duration = 0, aspect = "", sessionId = "", revision = 0,
  sourceEditSessionId = "", outputKind = "", segments = [], clipCount = null,
  qualityStatus = "", preflight = null, reframe = null, socialReframe = false,
  sourceOutputFilename = "", subtitleMode = "", subtitleStrategy = "",
  subtitleBurned = false, subtitleUrl = "", coverUrl = "",
  coverIntroAvailable = false, coverIncluded = false, introIncluded = false,
  displayName = "", displayTitle = "", reason = "", editingExplanation = null,
  timelineEvents = [], timelineHierarchyVersion = null,
} = {}) => {
  if (!currentJob?.id) return false;
  const safePreviewUrl = String(previewUrl || videoUrl || "").trim();
  const previewKey = String(filename || id || sessionId || sourceEditSessionId || safePreviewUrl).trim()
    .replace(/[^a-zA-Z0-9._-]+/g, "-")
    .replace(/^-+|-+$/g, "")
    .slice(0, 96);
  const safeFilename = String(filename || (previewKey ? `${previewKey}.mp4` : "")).trim();
  if (!safePreviewUrl && !safeFilename) throw new Error("审核预览地址不存在，请重新运行当前计划");
  const locatedPreview = locateJobOutput(safeFilename);
  if (!locatedPreview || locatedPreview.output?.previewOnly || locatedPreview.version?.previewOnly) {
    const previous = locatedPreview?.output || {};
    const item = {
      ...previous,
      filename: safeFilename,
      title: String(title || previous.title || "审核样片"),
      displayName: String(displayName || previous.displayName || "Agent 审核预览"),
      displayTitle: String(displayTitle || title || previous.displayTitle || previous.title || "审核样片"),
      previewUrl: safePreviewUrl || previous.previewUrl || previous.videoUrl || "",
      videoUrl: safePreviewUrl || previous.videoUrl || previous.previewUrl || "",
      width: Number(width || previous.width || 0),
      height: Number(height || previous.height || 0),
      duration: Number(duration || previous.duration || 0),
      previewOnly: true,
      outputKind: String(outputKind || previous.outputKind || "agent_review_preview"),
      sourceEditSessionId: String(sourceEditSessionId || sessionId || previous.sourceEditSessionId || ""),
      editSessionId: String(sourceEditSessionId || sessionId || previous.editSessionId || ""),
      revision: Number(revision || previous.revision || 0),
      segments: Array.isArray(segments) && segments.length ? segments : (Array.isArray(previous.segments) ? previous.segments : []),
      clipCount: Number.isFinite(Number(clipCount)) ? Number(clipCount) : previous.clipCount,
      qualityStatus: String(qualityStatus || previous.qualityStatus || ""),
      sourceOutputFilename: String(sourceOutputFilename || previous.sourceOutputFilename || ""),
      subtitleMode: String(subtitleMode || previous.subtitleMode || ""),
      subtitleStrategy: String(subtitleStrategy || previous.subtitleStrategy || ""),
      subtitleBurned: Boolean(subtitleBurned || previous.subtitleBurned),
      subtitleUrl: String(subtitleUrl || previous.subtitleUrl || ""),
      coverUrl: String(coverUrl || previous.coverUrl || ""),
      coverIntroAvailable: Boolean(coverIntroAvailable || previous.coverIntroAvailable),
      coverIncluded: Boolean(coverIncluded || previous.coverIncluded),
      introIncluded: Boolean(introIncluded || previous.introIncluded),
      reason: String(reason || previous.reason || ""),
      socialReframe: Boolean(socialReframe || previous.socialReframe || aspect || reframe?.aspect || previous.reframe?.aspect),
      reframe: reframe && typeof reframe === "object"
        ? { ...reframe }
        : (aspect ? { ...(previous.reframe || {}), aspect: String(aspect) } : previous.reframe),
      editingExplanation: editingExplanation && typeof editingExplanation === "object"
        ? editingExplanation : previous.editingExplanation,
      timelineEvents: Array.isArray(timelineEvents) && timelineEvents.length ? timelineEvents : (previous.timelineEvents || []),
      timelineHierarchyVersion: Number.isFinite(Number(timelineHierarchyVersion))
        ? Number(timelineHierarchyVersion) : previous.timelineHierarchyVersion,
      ...(preflight && typeof preflight === "object" ? { preflight } : {}),
    };
    currentJob.agentPreviewOutputs = [
      ...(currentJob.agentPreviewOutputs || []).filter((output) => String(output?.filename || "") !== safeFilename),
      item,
    ];
  }
  renderOutputs(currentJob);
  selectOutput(safeFilename, true);
  renderOutputPreviewSelector(currentJob);
  window.ClipTalkWorkspaceController?.syncMaterialsSummary?.();
  showToast(`已打开${aspect ? ` ${aspect}` : ""}审核样片`, "success");
  return true;
};

function closeSecondaryEditor() {
  secondaryEditActivationToken += 1;
  secondaryEditBusy = false;
  const video = $("#secondaryEditorVideo");
  video?.pause();
  if (video) { video.removeAttribute("src"); video.load(); }
  $("#secondaryEditor")?.classList.add("hidden");
  setSecondaryEditorShellIsolation(false);
  secondaryEditSession = null;
  secondaryEditSelectedClips = new Set();
  secondaryEditExactPreview = false;
  secondaryEditSourceTime = 0;
  secondaryEditInsertionIndexOverride = null;
  secondaryEditPreviewMaterialId = null;
  secondaryEditPreviewMaterialRange = null;
  secondaryEditTimelinePixelsPerSecond = null;
  secondaryEditPlayheadTime = 0;
  secondaryEditTrimDrag = null;
  secondaryEditCueDrag = null;
  if (secondaryEditSubtitleCanvasDrag) finishSecondaryEditorSubtitleCanvasTransform();
  secondaryEditSubtitleCanvasDrag = null;
  secondaryEditSubtitleDraft = null;
  secondaryEditSubtitleLoadingId = null;
  secondaryEditSubtitleSaving = false;
  secondaryEditSubtitleNeedsReview = false;
  secondaryEditInspectorDraft = null;
  secondaryEditSelectionAnchorId = null;
  secondaryEditDropIndex = null;
  secondaryEditDraggedMaterial = null;
  secondaryEditSelectedMarkerId = null;
  secondaryEditSelectedCueId = null;
  secondaryEditSelectedTextLayerId = null;
  secondaryEditTextLayerDrag = null;
  secondaryEditTextTimeDrag = null;
  secondaryEditPreviewPending = false;
  secondaryEditPreviewRequestToken += 1;
  secondaryEditorResizeDrag = null;
  secondaryEditorInspectorResizeDrag = null;
  if (secondaryEditorTimelineLayoutFrame !== null) window.cancelAnimationFrame(secondaryEditorTimelineLayoutFrame);
  secondaryEditorTimelineLayoutFrame = null;
  $("#secondaryEditor")?.classList.remove("library-open", "inspector-open");
  window.clearTimeout(secondaryEditSubtitleSaveTimer);
  secondaryEditSubtitleSaveTimer = null;
  document.removeEventListener("pointermove", moveSecondaryEditorTrim);
  document.removeEventListener("pointermove", moveSecondaryEditorCueDrag);
  document.removeEventListener("pointermove", moveSecondaryEditorSubtitleCanvasTransform);
  document.removeEventListener("pointermove", moveSecondaryEditorTextLayerTransform);
  document.removeEventListener("pointermove", moveSecondaryEditorTextTimeDrag);
  document.removeEventListener("pointermove", moveSecondaryEditorWorkspaceResize);
  document.removeEventListener("pointerup", finishSecondaryEditorWorkspaceResize);
  document.removeEventListener("pointercancel", finishSecondaryEditorWorkspaceResize);
  document.removeEventListener("pointermove", moveSecondaryEditorInspectorResize);
  document.removeEventListener("pointerup", finishSecondaryEditorInspectorResize);
  document.removeEventListener("pointercancel", finishSecondaryEditorInspectorResize);
}

window.ClipTalkActivateModalFocus = activateModalFocus;
window.ClipTalkCloseCandidateDrawer = closeCandidateDrawer;
window.ClipTalkPrepareWorkspaceOverlay = (kind = "") => {
  const secondaryOpen = secondaryEditorOpen();
  if (secondaryOpen && !["secondaryEditor", "subtitle", "cover"].includes(kind)) return false;
  if (kind !== "candidate") closeCandidateDrawer({ restoreFocus: false });
  if (kind !== "timeline") closeTimelinePrecisionDrawer();
  if (kind !== "agentPlan") window.ClipTalkCloseAgentPlanDrawer?.({ restoreFocus: false });
  if (kind !== "cover") closeCoverImagePreview();
  if (kind !== "subtitle" && !$("#subtitleReview")?.classList.contains("hidden")) closeSubtitleReview(null);
  return true;
};

async function requestCloseSecondaryEditor() {
  if (secondaryEditorInspectorHasChanges()) {
    const confirmed = await requestActionConfirmation({
      title: "放弃未保存的片段设置",
      summary: "当前时间线正在展示待保存排期。返回任务后，这些片段设置不会保留。",
      details: ["源片边界、速度、转场和声音设置将恢复到上次保存状态"],
      confirmLabel: "放弃并返回",
    });
    if (!confirmed) return;
  }
  const filename = secondaryEditSession?.baseOutputFilename;
  closeSecondaryEditor();
  if (filename && locateJobOutput(filename)) selectOutput(filename, false);
}

function secondaryEditorMediaControlModel() {
  const video = $("#secondaryEditorVideo");
  if (secondaryEditView === "source") {
    const preview = secondaryEditPreviewMaterialRange;
    if (preview) {
      const start = Number(preview.start || 0);
      const end = Math.max(start, Number(preview.end || start));
      return { coordinate: "material", current: Math.max(0, Number(video?.currentTime || start) - start), duration: end - start, sourceOffset: start };
    }
    return {
      coordinate: "source",
      current: Number(video?.currentTime || secondaryEditSourceTime || 0),
      duration: Number(video?.duration || currentJob?.videoInfo?.duration || 0),
      sourceOffset: 0,
    };
  }
  return {
    coordinate: "sequence",
    current: secondaryEditExactPreview ? Number(video?.currentTime || 0) : Number(secondaryEditPlayheadTime || 0),
    duration: secondaryEditExactPreview ? Number(video?.duration || secondaryEditorTimelineDuration()) : secondaryEditorTimelineDuration(),
    sourceOffset: 0,
  };
}

function syncSecondaryEditorMediaControls() {
  const video = $("#secondaryEditorVideo");
  const seek = $("#secondaryEditorMediaSeek");
  const clock = $("#secondaryEditorMediaClock");
  const play = $("#secondaryEditorMediaPlay");
  const mute = $("#secondaryEditorMediaMute");
  const fullscreen = $("#secondaryEditorMediaFullscreen");
  if (!video || !seek || !clock || !play || !mute || !fullscreen) return;
  const model = secondaryEditorMediaControlModel();
  const duration = Math.max(0, Number(model.duration || 0));
  const current = Math.max(0, Math.min(duration, Number(model.current || 0)));
  seek.max = String(duration);
  if (seek.dataset.scrubbing !== "true") seek.value = String(current);
  seek.dataset.coordinate = model.coordinate;
  seek.style.setProperty("--media-progress", `${duration > 0 ? current / duration * 100 : 0}%`);
  clock.textContent = `${formatPreciseTimecode(current)} / ${formatPreciseTimecode(duration)}`;
  play.textContent = video.paused ? "▶" : "❚❚";
  play.setAttribute("aria-label", video.paused ? "播放" : "暂停");
  mute.textContent = video.muted ? "取消静音" : "静音";
  mute.setAttribute("aria-pressed", String(video.muted));
  fullscreen.textContent = document.fullscreenElement === $(".secondary-editor-player") ? "退出全屏" : "全屏";
}

function setSecondaryEditorSourceVideo() {
  const video = $("#secondaryEditorVideo");
  if (!video || !currentJob) return;
  const sourceUrl = currentJob.previewUrl || currentJob.sourceUrl;
  if (!video.getAttribute("src") || video.dataset.sourceUrl !== sourceUrl || secondaryEditExactPreview) {
    video.dataset.sourceUrl = sourceUrl;
    video.src = sourceUrl;
    video.load();
  }
  secondaryEditExactPreview = false;
  if (secondaryEditView === "source") video.playbackRate = 1;
  const badge = $("#secondaryEditorVideoBadge");
  if (badge) {
    badge.textContent = secondaryEditView === "sequence" ? "成片顺序预览" : "源视频 · 完整时间线";
    badge.classList.toggle("hidden", secondaryEditView === "sequence");
  }
  syncSecondaryEditorMediaControls();
}

function syncSecondaryEditorViewButtons() {
  $("#secondaryEditor")?.querySelectorAll("[data-secondary-view]").forEach((button) => {
    const active = button.dataset.secondaryView === secondaryEditView;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
  });
}

function switchSecondaryEditorView(view, sourceTime = null) {
  if (!secondaryEditSession || !["sequence", "source"].includes(view)) return;
  const video = $("#secondaryEditorVideo");
  if (view === "source") {
    const context = secondaryEditorPlayheadContext();
    const target = sourceTime !== null && Number.isFinite(Number(sourceTime)) ? Number(sourceTime) : Number(context?.sourceTime ?? video?.currentTime ?? 0);
    secondaryEditView = "source";
    secondaryEditSourceTime = Math.max(0, target);
    syncSecondaryEditorViewButtons();
    setSecondaryEditorSourceVideo();
    const seek = () => {
      video.playbackRate = 1;
      video.currentTime = Math.min(Number(video.duration || currentJob?.videoInfo?.duration || secondaryEditSourceTime), secondaryEditSourceTime);
      $("#secondaryEditorClock").textContent = `源片 ${formatPreciseTimecode(video.currentTime)} / ${formatPreciseTimecode(video.duration || currentJob?.videoInfo?.duration || 0)}`;
      syncSecondaryEditorMediaControls();
    };
    if (video?.readyState >= 1) seek(); else video?.addEventListener("loadedmetadata", seek, { once: true });
    return;
  }
  clearSecondaryEditorMaterialPreview();
  const sourcePosition = secondaryEditView === "source" ? Number(video?.currentTime || secondaryEditSourceTime) : null;
  secondaryEditView = "sequence";
  syncSecondaryEditorViewButtons();
  if (Number.isFinite(sourcePosition)) {
    const model = secondaryEditorTimelineModel();
    const index = model.clips.findIndex((clip) => sourcePosition >= Number(clip.sourceStart) && sourcePosition <= Number(clip.sourceEnd));
    if (index >= 0) {
      const clip = model.clips[index];
      const outputTime = Number(model.schedule[index]?.outputStart || 0)
        + (sourcePosition - Number(clip.sourceStart)) / Math.max(.01, Number(clip.playbackRate || 1));
      secondaryEditorSeekOutputTime(outputTime, false);
      return;
    }
  }
  secondaryEditorSeekClip(secondaryEditClip() || secondaryEditSession.clips?.[0], false);
}

function secondaryEditorSeekOutputTime(outputTime, autoplay = false, preserveSelection = false) {
  if (!secondaryEditSession) return;
  const video = $("#secondaryEditorVideo");
  const activation = secondaryEditActivationToken;
  const model = secondaryEditorTimelineModel();
  const clips = model.clips;
  const schedule = model.schedule;
  const duration = Math.max(0, model.duration);
  const time = Math.max(0, Math.min(duration, Number(outputTime) || 0));
  let index = 0;
  schedule.forEach((item, candidateIndex) => {
    const start = Number(item.outputStart || 0);
    const end = Number(item.outputEnd || start);
    if (time >= start - .001 && (time < end - .001 || candidateIndex === schedule.length - 1)) index = candidateIndex;
  });
  const clip = clips[index];
  if (!video || !clip) {
    updateSecondaryEditorPlayhead(time);
    return;
  }
  const selectionChanged = !preserveSelection && (secondaryEditSequenceIndex !== index || !secondaryEditSelectedClips.has(String(clip.id)) || secondaryEditSelectedClips.size !== 1);
  secondaryEditSequenceIndex = index;
  if (!preserveSelection) {
    secondaryEditSelectedClips = new Set([String(clip.id)]);
    secondaryEditSelectionAnchorId = String(clip.id);
  }
  secondaryEditView = "sequence";
  syncSecondaryEditorViewButtons();
  updateSecondaryEditorPlayhead(time);
  if (selectionChanged) {
    renderSecondaryEditorTimeline();
    renderSecondaryEditorInspector();
  }
  const seek = () => {
    if (activation !== secondaryEditActivationToken || !secondaryEditorOpen()) return;
    if (secondaryEditExactPreview) {
      video.playbackRate = 1;
      video.currentTime = time;
    } else {
      const item = schedule[index] || {};
      const localOutputTime = Math.max(0, time - Number(item.outputStart || 0));
      const sourceTime = Number(clip.sourceStart || 0) + localOutputTime * Math.max(.01, Number(clip.playbackRate || 1));
      video.currentTime = Math.min(Number(clip.sourceEnd || sourceTime), sourceTime);
      video.playbackRate = Number(clip.playbackRate || 1);
    }
    if (autoplay) video.play().catch(() => {});
  };
  if (!secondaryEditExactPreview) setSecondaryEditorSourceVideo();
  if (video.readyState >= 1) seek(); else video.addEventListener("loadedmetadata", seek, { once: true });
  $("#secondaryEditorClock").textContent = `成片 ${formatPreciseTimecode(time)} / ${formatPreciseTimecode(duration)}`;
}

function secondaryEditorSeekClip(clip, autoplay = true) {
  if (!clip) return;
  const index = Math.max(0, (secondaryEditSession?.clips || []).findIndex((item) => String(item.id) === String(clip.id)));
  secondaryEditorSeekOutputTime(Number(secondaryEditorSchedule()[index]?.outputStart || 0), autoplay);
}

function adjustSecondaryEditorTimelineZoom(direction) {
  if (!secondaryEditSession) return;
  const viewport = $("#secondaryEditorTimelineViewport");
  const duration = Math.max(.01, secondaryEditorTimelineDuration());
  const fitPixelsPerSecond = Math.max(1, Number(viewport?.clientWidth || 720) / duration);
  const previousCanvas = $("#secondaryEditorTimelineCanvas");
  const previousWidth = Math.max(1, Number(previousCanvas?.offsetWidth || viewport?.clientWidth || 720));
  const playheadX = secondaryEditPlayheadTime / duration * previousWidth;
  const viewportOffset = Math.max(0, playheadX - Number(viewport?.scrollLeft || 0));
  if (direction === "fit") secondaryEditTimelinePixelsPerSecond = null;
  else {
    const current = secondaryEditTimelinePixelsPerSecond || fitPixelsPerSecond;
    const next = direction === "in" ? current * 1.6 : current / 1.6;
    secondaryEditTimelinePixelsPerSecond = next <= fitPixelsPerSecond * 1.02 ? null : Math.min(160, Math.max(fitPixelsPerSecond, next));
  }
  renderSecondaryEditorTimeline();
  window.requestAnimationFrame(() => {
    const canvas = $("#secondaryEditorTimelineCanvas");
    if (!viewport || !canvas) return;
    const nextX = secondaryEditPlayheadTime / duration * canvas.offsetWidth;
    viewport.scrollLeft = Math.max(0, nextX - Math.min(viewport.clientWidth, viewportOffset));
  });
}

function secondaryEditorStepFrame(direction) {
  if (!secondaryEditSession) return;
  const frame = 1 / secondaryEditorFrameRate();
  secondaryEditorSeekOutputTime(secondaryEditPlayheadTime + Number(direction || 0) * frame, false);
}

function secondaryEditorOutputTimeFromPointer(event) {
  const ruler = $("#secondaryEditorRuler");
  if (!ruler || !secondaryEditSession) return 0;
  const rect = ruler.getBoundingClientRect();
  const fraction = Math.max(0, Math.min(1, (event.clientX - rect.left) / Math.max(1, rect.width)));
  return fraction * Math.max(0, secondaryEditorTimelineDuration());
}

async function secondaryEditorOperation(operation, { allowInspectorDraft = false } = {}) {
  if (!currentJob?.id || !secondaryEditSession || secondaryEditBusy) return null;
  if (secondaryEditorInspectorHasChanges() && !allowInspectorDraft) {
    showToast("请先保存或取消当前片段设置");
    return null;
  }
  const activation = secondaryEditActivationToken;
  const jobId = String(currentJob.id);
  const sessionId = String(secondaryEditSession.id);
  const current = () => secondaryEditorOpen() && activation === secondaryEditActivationToken && String(currentJob?.id) === jobId && String(secondaryEditSession?.id) === sessionId;
  let failed = false;
  const previousClips = secondaryEditSession.clips || [];
  const previousTextLayerIds = new Set(secondaryEditorTextLayers().map((layer) => String(layer.id)));
  const selectedIndex = Math.max(0, previousClips.findIndex((clip) => secondaryEditSelectedClips.has(String(clip.id))));
  setSecondaryEditorBusy(true, "保存中", "save");
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/edit-sessions/${encodeURIComponent(secondaryEditSession.id)}`, { method: "PATCH", body: { revision: secondaryEditSession.revision, operation } });
    if (!current()) return null;
    secondaryEditSession = payload.session;
    syncSecondaryEditorSessionToJob();
    secondaryEditInspectorDraft = null;
    const subtitleInvalidatingOperations = new Set(["trim_clip", "roll_trim", "trim_to_playhead", "split_clip", "delete_clips", "duplicate_clips", "reorder_clips", "insert_clip", "update_clip", "update_clips"]);
    const timelineChangingOperations = new Set([...subtitleInvalidatingOperations, "add_marker", "update_marker", "delete_marker", "add_text_layer", "update_text_layer", "delete_text_layer"]);
    if (operation.type === "set_subtitle") secondaryEditSubtitleNeedsReview = false;
    else if (subtitleInvalidatingOperations.has(operation.type) && secondaryEditSession.subtitleDraftId) secondaryEditSubtitleNeedsReview = true;
    if (timelineChangingOperations.has(operation.type) || operation.type === "set_subtitle") secondaryEditorMarkPreviewStale();
    secondaryEditSelectedClips = new Set([...secondaryEditSelectedClips].filter((id) => secondaryEditSession.clips.some((clip) => String(clip.id) === id)));
    secondaryEditSequenceIndex = Math.max(0, Math.min(secondaryEditSequenceIndex, Math.max(0, secondaryEditSession.clips.length - 1)));
    secondaryEditPlayheadTime = Math.min(secondaryEditPlayheadTime, secondaryEditorTimelineDuration());
    let insertedClip = null;
    if (operation.type === "add_text_layer") {
      const added = secondaryEditorTextLayers().find((layer) => !previousTextLayerIds.has(String(layer.id)));
      secondaryEditSelectedTextLayerId = added ? String(added.id) : null;
    } else if (operation.type === "delete_text_layer" && String(operation.layerId) === String(secondaryEditSelectedTextLayerId)) {
      secondaryEditSelectedTextLayerId = null;
    } else if (secondaryEditSelectedTextLayerId && !secondaryEditorTextLayer()) {
      secondaryEditSelectedTextLayerId = null;
    }
    if (operation.type === "insert_clip") {
      const previousIds = new Set(previousClips.map((clip) => String(clip.id)));
      const insertedIndex = secondaryEditSession.clips.findIndex((clip) => !previousIds.has(String(clip.id)));
      if (insertedIndex >= 0) {
        secondaryEditInsertionIndexOverride = null;
        clearSecondaryEditorMaterialPreview();
        insertedClip = secondaryEditSession.clips[insertedIndex];
        secondaryEditSequenceIndex = insertedIndex;
        secondaryEditSelectedClips = new Set([String(insertedClip.id)]);
        secondaryEditPlayheadTime = Number(secondaryEditorSchedule()[insertedIndex]?.outputStart || 0);
      }
    }
    if (operation.type === "delete_clips" && secondaryEditSession.clips.length && !secondaryEditSelectedClips.size) {
      secondaryEditSequenceIndex = Math.min(selectedIndex, secondaryEditSession.clips.length - 1);
      secondaryEditSelectedClips = new Set([String(secondaryEditSession.clips[secondaryEditSequenceIndex].id)]);
      secondaryEditPlayheadTime = Number(secondaryEditorSchedule()[secondaryEditSequenceIndex]?.outputStart || 0);
    }
    renderSecondaryEditor();
    if (insertedClip) secondaryEditorSeekOutputTime(secondaryEditPlayheadTime, false);
    return payload.session;
  } catch (error) { failed = true; if (current()) showToast(error.message); return null; }
  finally {
    if (current()) {
      setSecondaryEditorBusy(false, failed ? "保存失败" : "已保存", "save");
      if (failed) $("#secondaryEditorSaveState").dataset.state = "error";
    }
  }
}

async function secondaryEditorHistory(direction) {
  if (!currentJob?.id || !secondaryEditSession || secondaryEditBusy) return;
  if (secondaryEditorInspectorHasChanges()) return void showToast("请先保存或取消当前片段设置");
  const activation = secondaryEditActivationToken;
  const jobId = String(currentJob.id);
  const sessionId = String(secondaryEditSession.id);
  const current = () => secondaryEditorOpen() && activation === secondaryEditActivationToken && String(currentJob?.id) === jobId && String(secondaryEditSession?.id) === sessionId;
  let failed = false;
  setSecondaryEditorBusy(true, direction === "undo" ? "正在撤销" : "正在重做", "save");
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/edit-sessions/${encodeURIComponent(secondaryEditSession.id)}/${direction}`, { method: "POST", body: { revision: secondaryEditSession.revision } });
    if (!current()) return null;
    secondaryEditSession = payload.session;
    syncSecondaryEditorSessionToJob();
    secondaryEditorMarkPreviewStale();
    if (secondaryEditSession.subtitleDraftId) secondaryEditSubtitleNeedsReview = true;
    secondaryEditSelectedClips = new Set([...secondaryEditSelectedClips].filter((id) => secondaryEditSession.clips.some((clip) => String(clip.id) === id)));
    renderSecondaryEditor();
  } catch (error) { failed = true; if (current()) showToast(error.message); }
  finally {
    if (current()) {
      setSecondaryEditorBusy(false, failed ? "保存失败" : "已保存", "save");
      if (failed) $("#secondaryEditorSaveState").dataset.state = "error";
    }
  }
}

function captureSecondaryEditorAction() {
  const jobId = String(currentJob?.id || "");
  const sessionId = String(secondaryEditSession?.id || "");
  const revision = Number(secondaryEditSession?.revision || 0);
  const activationToken = secondaryEditActivationToken;
  return Object.freeze({ jobId, sessionId, revision, activationToken,
    current: (checkRevision = true) => secondaryEditorOpen()
      && activationToken === secondaryEditActivationToken
      && jobId === String(currentJob?.id || "") && sessionId === String(secondaryEditSession?.id || "")
      && (!checkRevision || revision === Number(secondaryEditSession?.revision || 0)),
    path: `/api/jobs/${encodeURIComponent(jobId)}/edit-sessions/${encodeURIComponent(sessionId)}`,
  });
}

async function reviewSecondaryEditorSubtitles() {
  if (!secondaryEditSession?.clips?.length) return null;
  if (secondaryEditorInspectorHasChanges()) {
    showToast("请先保存或取消当前片段设置，再重新对齐字幕");
    return null;
  }
  const context = captureSecondaryEditorAction();
  const style = secondaryEditSession.subtitleStyle || "clean";
  const draft = await reviewSubtitlesBeforeRender([{ segments: secondaryEditSession.clips.map((clip) => ({ ...clip, id: clip.id, start: Number(clip.sourceStart), end: Number(clip.sourceEnd), playbackRate: Number(clip.playbackRate || 1), transitionIn: clip.transitionIn || { type: "cut", duration: 0 } })) }], style);
  if (!draft || !context.current()) return null;
  const saved = await secondaryEditorOperation({ type: "set_subtitle", enabled: true, subtitleDraftId: draft.id, subtitleStyle: style });
  return saved && context.current(false) ? draft : null;
}

async function requestSecondaryEditorPreview() {
  if (!currentJob?.id || !secondaryEditSession || secondaryEditBusy) return;
  if (secondaryEditorInspectorHasChanges()) return void showToast("请先保存或取消当前片段设置");
  let context = captureSecondaryEditorAction();
  if (secondaryEditSession.subtitleEnabled) {
    const ready = await ensureSecondaryEditorSubtitleReadyForBurn();
    if (!ready || !context.current(false)) return;
    context = captureSecondaryEditorAction();
  }
  if (secondaryEditPreviewPending) return;
  secondaryEditPreviewPending = true;
  const token = ++secondaryEditPreviewRequestToken;
  const requestedRevision = Number(secondaryEditSession.revision || 0);
  syncSecondaryEditorControls();
  const state = $("#secondaryEditorPreviewState");
  try {
    if (state) { state.textContent = "生成中"; state.dataset.tone = "loading"; state.classList.remove("hidden"); }
    const sessionId = String(secondaryEditSession.id);
    const payload = await apiJson(`${context.path}/preview`, { method: "POST", body: { revision: requestedRevision } });
    if (!context.current() || token !== secondaryEditPreviewRequestToken) return;
    const requestedFingerprint = String(payload.session?.previewFingerprint || "");
    if (Number(secondaryEditSession?.revision || -1) === requestedRevision) secondaryEditSession = payload.session;
    if (state) { state.textContent = payload.reused ? "正在打开已有预览样片" : "生成中"; state.dataset.tone = "loading"; state.classList.remove("hidden"); }
    for (let attempt = 0; attempt < 120; attempt += 1) {
      if (!secondaryEditorOpen() || token !== secondaryEditPreviewRequestToken) return;
      await new Promise((resolve) => window.setTimeout(resolve, 1000));
      if (!context.current() || token !== secondaryEditPreviewRequestToken) return;
      const response = await api(`/api/jobs/${encodeURIComponent(context.jobId)}`);
      if (!context.current() || token !== secondaryEditPreviewRequestToken) return;
      const session = response.job?.editSessions?.find((item) => String(item.id) === sessionId);
      if (!session) throw new Error("编辑草稿已不存在");
      if (Number(secondaryEditSession?.revision || -1) !== requestedRevision || String(session.previewFingerprint || "") !== requestedFingerprint) {
        if (state) { state.textContent = "预览生成期间时间线已变化 · 已保留当前编辑"; state.dataset.tone = "stale"; }
        return;
      }
      secondaryEditSession = session;
      if (session.previewStatus === "ready" && String(session.previewFingerprint || "") === requestedFingerprint) {
        const video = $("#secondaryEditorVideo");
        secondaryEditExactPreview = true;
        secondaryEditView = "sequence";
        video.src = `${session.previewUrl}&fingerprint=${encodeURIComponent(requestedFingerprint)}`;
        video.load();
        video.play().catch(() => {});
        $("#secondaryEditorVideoBadge").textContent = "预览样片";
        $("#secondaryEditorVideoBadge").classList.remove("hidden");
        if (state) { state.textContent = "预览已就绪"; state.dataset.tone = "ready"; }
        renderSecondaryEditor();
        return;
      }
      if (session.previewStatus === "failed") throw new Error(session.previewError || "预览样片生成失败");
    }
    throw new Error("预览样片生成时间较长，请稍后重试")
  } catch (error) {
    if (!context.current() || token !== secondaryEditPreviewRequestToken) return;
    if (state) { state.textContent = error.message; state.dataset.tone = "error"; state.classList.remove("hidden"); }
    showToast(error.message);
  } finally {
    if (context.current(false) && token === secondaryEditPreviewRequestToken) {
      secondaryEditPreviewPending = false;
      syncSecondaryEditorControls();
    }
  }
}

async function generateSecondaryEditorVersion() {
  if (!currentJob?.id || !secondaryEditSession?.clips?.length || secondaryEditBusy) return;
  if (secondaryEditorInspectorHasChanges()) return void showToast("请先保存或取消当前片段设置");
  let context = captureSecondaryEditorAction();
  let subtitleDraftId = secondaryEditSession.subtitleEnabled ? secondaryEditSession.subtitleDraftId || null : null;
  if ($("#secondaryEditorSubtitleEnabled")?.checked) {
    const ready = await ensureSecondaryEditorSubtitleReadyForBurn();
    if (!ready || !context.current(false)) return;
    context = captureSecondaryEditorAction();
    subtitleDraftId = secondaryEditSession.subtitleDraftId || secondaryEditSubtitleDraft?.id || null;
  }
  const versionLabel = String($("#secondaryEditorVersionLabel")?.value || "精剪版").trim() || "精剪版";
  const previewCurrent = secondaryEditSession.previewStatus === "ready" && Number(secondaryEditSession.previewRevision) === Number(secondaryEditSession.revision);
  if (!previewCurrent) {
    const generate = await requestActionConfirmation({ title: "先生成审核样片", summary: "审核样片需要更新", details: [], confirmLabel: "生成审核样片" });
    if (generate && context.current()) await requestSecondaryEditorPreview();
    return;
  }
  const confirmed = await requestActionConfirmation({ title: "生成成片", summary: `${versionLabel} · ${secondaryEditorTimelineDuration().toFixed(1)} 秒 · ${secondaryEditSession.subtitleEnabled && subtitleDraftId ? "带字幕" : "无字幕"}`, details: [], confirmLabel: "开始生成" });
  if (!confirmed) return;
  if (!context.current()) return void showToast("任务或精剪时间线已变化，请重新审核后生成");
  setSecondaryEditorBusy(true, "正在生成成片", "render");
  try {
    const payload = await apiJson(`${context.path}/render`, { method: "POST", body: { revision: context.revision, subtitleMode: subtitleDraftId ? "burn" : "none", subtitleStyle: secondaryEditSession.subtitleStyle || "clean", subtitleDraftId, versionLabel } });
    if (!context.current()) return;
    setSecondaryEditorBusy(false);
    currentJob = payload.job || currentJob;
    closeSecondaryEditor();
    renderJob(currentJob);
    clearTimeout(pollTimer);
    pollJob();
  } catch (error) { if (context.current()) showToast(error.message); }
  finally { if (context.current(false)) setSecondaryEditorBusy(false); }
}

async function createSecondaryEditorProposal(text, selectedClipIds = [...secondaryEditSelectedClips]) {
  if (!currentJob?.id || !secondaryEditSession || secondaryEditBusy) return;
  const context = captureSecondaryEditorAction();
  setSecondaryEditorBusy(true, "正在解析编辑指令");
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/edit-sessions/${encodeURIComponent(secondaryEditSession.id)}/proposals`, { method: "POST", body: { revision: secondaryEditSession.revision, text, selectedClipIds } });
    if (!context.current()) return;
    secondaryEditSession = payload.session;
    syncSecondaryEditorSessionToJob();
    secondaryEditorMarkPreviewStale();
    renderSecondaryEditorProposal();
  } catch (error) { if (context.current()) showToast(error.message); }
  finally { if (context.current(false)) setSecondaryEditorBusy(false, "提案待确认"); }
}

async function applySecondaryEditorProposal() {
  const proposal = secondaryEditSession?.pendingProposal;
  if (!proposal || secondaryEditBusy) return;
  const context = captureSecondaryEditorAction();
  let failed = false;
  setSecondaryEditorBusy(true, "正在应用提案", "save");
  try {
    const payload = await apiJson(`/api/jobs/${encodeURIComponent(currentJob.id)}/edit-sessions/${encodeURIComponent(secondaryEditSession.id)}/proposals/${encodeURIComponent(proposal.id)}/apply`, { method: "POST", body: {} });
    if (!context.current()) return;
    secondaryEditSession = payload.session;
    syncSecondaryEditorSessionToJob();
    if (secondaryEditSession.subtitleDraftId) secondaryEditSubtitleNeedsReview = true;
    secondaryEditSelectedClips = new Set([...secondaryEditSelectedClips].filter((id) => secondaryEditSession.clips.some((clip) => String(clip.id) === id)));
    renderSecondaryEditor();
  } catch (error) { failed = true; if (context.current()) showToast(error.message); }
  finally {
    if (context.current(false)) {
      setSecondaryEditorBusy(false, failed ? "保存失败" : "已保存", "save");
      if (failed && $("#secondaryEditorSaveState")) $("#secondaryEditorSaveState").dataset.state = "error";
    }
  }
}

async function cancelSecondaryEditorProposal() {
  const proposal = secondaryEditSession?.pendingProposal;
  if (!proposal || secondaryEditBusy) return;
  const context = captureSecondaryEditorAction();
  try {
    const payload = await api(`/api/jobs/${encodeURIComponent(currentJob.id)}/edit-sessions/${encodeURIComponent(secondaryEditSession.id)}/proposals/${encodeURIComponent(proposal.id)}`, { method: "DELETE" });
    if (!context.current()) return;
    secondaryEditSession = payload.session;
    syncSecondaryEditorSessionToJob();
    renderSecondaryEditorProposal();
  } catch (error) { if (context.current()) showToast(error.message); }
}

$("#secondaryEditorClose")?.addEventListener("click", requestCloseSecondaryEditor);
$("#secondaryEditorUndo")?.addEventListener("click", () => secondaryEditorHistory("undo"));
$("#secondaryEditorRedo")?.addEventListener("click", () => secondaryEditorHistory("redo"));
$("#secondaryEditorPreview")?.addEventListener("click", requestSecondaryEditorPreview);
$("#secondaryEditorExport")?.addEventListener("click", generateSecondaryEditorVersion);
$("#secondaryEditorZoomOut")?.addEventListener("click", () => adjustSecondaryEditorTimelineZoom("out"));
$("#secondaryEditorZoomFit")?.addEventListener("click", () => adjustSecondaryEditorTimelineZoom("fit"));
$("#secondaryEditorZoomIn")?.addEventListener("click", () => adjustSecondaryEditorTimelineZoom("in"));
$("#secondaryEditorPreviousFrame")?.addEventListener("click", () => secondaryEditorStepFrame(-1));
$("#secondaryEditorNextFrame")?.addEventListener("click", () => secondaryEditorStepFrame(1));
$("#secondaryEditorSnap")?.addEventListener("click", () => {
  secondaryEditSnapEnabled = !secondaryEditSnapEnabled;
  renderSecondaryEditorTimeline();
});
$("#secondaryEditorDelete")?.addEventListener("click", () => secondaryEditorOperation({ type: "delete_clips", clipIds: [...secondaryEditSelectedClips] }));
function trimSecondaryEditorAtPlayhead(side) {
  const context = secondaryEditorPlayheadContext();
  if (!context || secondaryEditSelectedClips.size !== 1) return;
  void secondaryEditorOperation({ type: "trim_to_playhead", clipId: context.clip.id, sourceTime: context.sourceTime, side });
}
$("#secondaryEditorSplit")?.addEventListener("click", () => {
  const context = secondaryEditorPlayheadContext();
  if (!context) return;
  secondaryEditorOperation({ type: "split_clip", clipId: context.clip.id, sourceTime: context.sourceTime });
});
$("#secondaryEditorTrimLeft")?.addEventListener("click", () => trimSecondaryEditorAtPlayhead("left"));
$("#secondaryEditorTrimRight")?.addEventListener("click", () => trimSecondaryEditorAtPlayhead("right"));
$("#secondaryEditorDuplicate")?.addEventListener("click", () => secondaryEditorOperation({ type: "duplicate_clips", clipIds: [...secondaryEditSelectedClips] }));
$("#secondaryEditorMarker")?.addEventListener("click", () => {
  const context = secondaryEditorPlayheadContext();
  if (context) void secondaryEditorOperation({ type: "add_marker", clipId: context.clip.id, sourceTime: context.sourceTime, label: `标记 ${formatPreciseTimecode(context.outputTime)}` });
});
$("#secondaryEditorShortcuts")?.addEventListener("click", (event) => {
  const panel = $("#secondaryEditorShortcutPanel");
  const open = panel?.classList.toggle("hidden") === false;
  event.currentTarget.setAttribute("aria-expanded", String(open));
});
$("#secondaryInspectorForm")?.addEventListener("submit", async (event) => {
  event.preventDefault();
  const clip = secondaryEditClip();
  if (!clip) return;
  const sourceStart = parseTimecodeValue($("#secondaryInspectorStart").value);
  const sourceEnd = parseTimecodeValue($("#secondaryInspectorEnd").value);
  const batch = secondaryEditSelectedClips.size > 1;
  const targetClips = (secondaryEditSession?.clips || []).filter((item) => secondaryEditSelectedClips.has(String(item.id)));
  if (!batch && (!Number.isFinite(sourceStart) || !Number.isFinite(sourceEnd))) return void showToast("源片时间格式无效，请输入 01:23.45 或直接输入秒数");
  const sourceLimit = Math.max(0, Number(currentJob?.videoInfo?.duration || 0));
  if (!batch && (sourceStart < 0 || sourceEnd - sourceStart < .25 || sourceLimit && sourceEnd > sourceLimit + .01)) {
    return void showToast("源片范围必须位于视频内，并至少保留 0.25 秒");
  }
  const settings = {
    playbackRate: Number($("#secondaryInspectorSpeed").value),
    transitionType: $("#secondaryInspectorTransition").value,
    transitionDuration: $("#secondaryInspectorTransition").value === "cut" ? 0 : Number($("#secondaryInspectorTransitionDuration").value || .35) || .35,
    audioBridgeType: $("#secondaryInspectorAudio").value,
    audioGain: Number($("#secondaryInspectorGain").value || 100) / 100,
    muted: Boolean($("#secondaryInspectorMuted").checked),
    audioFadeIn: Number($("#secondaryInspectorFadeIn").value || 0),
    audioFadeOut: Number($("#secondaryInspectorFadeOut").value || 0),
  };
  const rangeChanged = !batch && (Math.abs(sourceStart - Number(clip.sourceStart || 0)) > .005
    || Math.abs(sourceEnd - Number(clip.sourceEnd || 0)) > .005);
  const settingsChanged = targetClips.some((item) => Math.abs(settings.playbackRate - Number(item.playbackRate || 1)) > .005
    || settings.transitionType !== String(item.transitionIn?.type || "cut")
    || settings.transitionType !== "cut" && Math.abs(settings.transitionDuration - Number(item.transitionIn?.duration || 0)) > .005
    || settings.audioBridgeType !== String(item.audioBridge?.type || "none")
    || Math.abs(settings.audioGain - Number(item.audioGain ?? 1)) > .005
    || settings.muted !== Boolean(item.muted)
    || Math.abs(settings.audioFadeIn - Number(item.audioFadeIn ?? .06)) > .005
    || Math.abs(settings.audioFadeOut - Number(item.audioFadeOut ?? .06)) > .005);
  if (rangeChanged) {
    const trimmed = await secondaryEditorOperation(
      { type: "trim_clip", clipId: clip.id, sourceStart, sourceEnd },
      { allowInspectorDraft: true },
    );
    if (!trimmed) return;
  }
  if (settingsChanged) {
    const updated = await secondaryEditorOperation(
      batch
        ? { type: "update_clips", clipIds: [...secondaryEditSelectedClips], ...settings }
        : { type: "update_clip", clipId: clip.id, ...settings },
      { allowInspectorDraft: true },
    );
    if (!updated) return;
  }
  if (!rangeChanged && !settingsChanged) resetSecondaryEditorInspectorDraft();
});
[$("#secondaryInspectorStart"), $("#secondaryInspectorEnd"), $("#secondaryInspectorSpeed"), $("#secondaryInspectorTransition"), $("#secondaryInspectorTransitionDuration"), $("#secondaryInspectorAudio"), $("#secondaryInspectorGain"), $("#secondaryInspectorMuted"), $("#secondaryInspectorFadeIn"), $("#secondaryInspectorFadeOut")].forEach((input) => {
  input?.addEventListener("input", captureSecondaryEditorInspectorDraft);
  input?.addEventListener("change", captureSecondaryEditorInspectorDraft);
});
$("#secondaryInspectorReset")?.addEventListener("click", resetSecondaryEditorInspectorDraft);
$("#secondaryEditorRangeForm")?.addEventListener("submit", (event) => {
  event.preventDefault();
  const sourceStart = parseTimecodeValue($("#secondaryEditorRangeStart").value);
  const sourceEnd = parseTimecodeValue($("#secondaryEditorRangeEnd").value);
  if (!Number.isFinite(sourceStart) || !Number.isFinite(sourceEnd)) return void showToast("源片时间格式无效，请输入 01:23.45 或直接输入秒数");
  secondaryEditorOperation({ type: "insert_clip", sourceStart, sourceEnd, title: $("#secondaryEditorRangeTitle").value || "补充片段", targetIndex: secondaryEditorInsertionIndex() });
});
$("#secondaryEditorInsertTarget")?.addEventListener("change", (event) => {
  const clips = secondaryEditSession?.clips || [];
  const value = Number(event.currentTarget.value);
  secondaryEditInsertionIndexOverride = Number.isInteger(value) ? Math.max(0, Math.min(clips.length, value)) : null;
  syncSecondaryEditorInsertionHint();
});
$("#secondaryEditorInsertFollow")?.addEventListener("click", () => {
  secondaryEditInsertionIndexOverride = null;
  syncSecondaryEditorInsertionHint();
});
$("#secondaryEditor")?.querySelectorAll("[data-secondary-library]").forEach((button) => button.addEventListener("click", () => {
  $("#secondaryEditor")?.querySelectorAll("[data-secondary-library]").forEach((item) => { item.classList.toggle("active", item === button); item.setAttribute("aria-selected", String(item === button)); });
  $("#secondaryEditorLibraryBody")?.classList.toggle("hidden", button.dataset.secondaryLibrary !== "mode");
  $("#secondaryEditorRangeForm")?.classList.toggle("hidden", button.dataset.secondaryLibrary !== "source");
  $("#secondaryEditorLibrary")?.classList.toggle("source-range-active", button.dataset.secondaryLibrary === "source");
  const context = $("#secondaryEditorLibraryContext");
  if (context) context.textContent = button.dataset.secondaryLibrary === "source"
    ? "输入源视频的真实入点与出点，片段只会作为虚拟范围加入时间线。"
    : secondaryEditorWorkflowContext(secondaryEditSession?.workflowKind || workflowKindForJob(currentJob));
}));
$("#secondaryEditorLibraryFilters")?.querySelectorAll("[data-secondary-material-filter]").forEach((button) => button.addEventListener("click", () => {
  secondaryEditorMaterialFilter = button.dataset.secondaryMaterialFilter;
  renderSecondaryEditorLibrary();
}));
$("#secondaryEditorLibrarySearch")?.addEventListener("input", (event) => {
  secondaryEditorMaterialSearch = String(event.currentTarget.value || "");
  renderSecondaryEditorLibrary();
});
$("#secondaryEditor")?.querySelectorAll("[data-secondary-view]").forEach((button) => button.addEventListener("click", () => {
  if (button.dataset.secondaryView === "source") clearSecondaryEditorMaterialPreview();
  switchSecondaryEditorView(button.dataset.secondaryView);
}));
$("#secondaryEditorSubtitleEnabled")?.addEventListener("change", async (event) => {
  const checkbox = event.currentTarget;
  if (!checkbox.checked) await secondaryEditorOperation({ type: "set_subtitle", enabled: false, subtitleDraftId: null, subtitleStyle: secondaryEditSession?.subtitleStyle || "clean" });
  else if (!secondaryEditSession?.subtitleDraftId) {
    const draft = await reviewSecondaryEditorSubtitles();
    if (!draft) checkbox.checked = false;
  } else {
    const ready = await ensureSecondaryEditorSubtitleReadyForBurn();
    if (!ready) {
      checkbox.checked = Boolean(secondaryEditSession?.subtitleEnabled);
      return;
    }
    await secondaryEditorOperation({ type: "set_subtitle", enabled: true, subtitleDraftId: secondaryEditSession.subtitleDraftId, subtitleStyle: secondaryEditSession.subtitleStyle || "clean" });
  }
});
$("#secondaryEditorSubtitleReview")?.addEventListener("click", reviewSecondaryEditorSubtitles);
$("#secondarySubtitleAdd")?.addEventListener("click", addSecondaryEditorTextLayer);
$("#secondaryTextDelete")?.addEventListener("click", () => {
  const layer = secondaryEditorTextLayer();
  if (layer) void secondaryEditorOperation({ type: "delete_text_layer", layerId: layer.id });
});
$("#secondarySubtitleSplit")?.addEventListener("click", splitSecondaryEditorSubtitleCue);
$("#secondarySubtitleMerge")?.addEventListener("click", mergeSecondaryEditorSubtitleCue);
$("#secondarySubtitleDelete")?.addEventListener("click", deleteSecondaryEditorSubtitleCue);
$("#secondarySubtitleTransformReset")?.addEventListener("click", resetSecondaryEditorSubtitleTransform);
$("#secondaryEditorSubtitlePreview")?.addEventListener("pointerdown", (event) => {
  if (event.target.closest("[data-secondary-subtitle-resize],[data-secondary-subtitle-move],[data-secondary-subtitle-preview-text]")) return;
  beginSecondaryEditorSubtitleCanvasTransform(event, "move");
});
$("#secondaryEditorSubtitlePreview")?.querySelector("[data-secondary-subtitle-move]")?.addEventListener("pointerdown", (event) => {
  beginSecondaryEditorSubtitleCanvasTransform(event, "move");
});
$("#secondaryEditorSubtitlePreview")?.querySelector("[data-secondary-subtitle-resize]")?.addEventListener("pointerdown", (event) => {
  beginSecondaryEditorSubtitleCanvasTransform(event, "resize");
});
$("#secondaryEditorSubtitlePreview")?.querySelector("[data-secondary-subtitle-preview-text]")?.addEventListener("focus", (event) => {
  $("#secondaryEditorVideo")?.pause();
  $("#secondaryEditorSubtitlePreview")?.classList.add("is-editing");
  const cueId = String($("#secondaryEditorSubtitlePreview")?.dataset.secondarySubtitleCue || "");
  if (cueId) {
    secondaryEditSelectedCueId = cueId;
    renderSecondaryEditorSubtitleTrack();
  }
  event.stopPropagation();
});
$("#secondaryEditorSubtitlePreview")?.querySelector("[data-secondary-subtitle-preview-text]")?.addEventListener("input", (event) => {
  const cueId = String($("#secondaryEditorSubtitlePreview")?.dataset.secondarySubtitleCue || "");
  const cue = secondaryEditSubtitleDraft?.cues?.find((item) => String(item.id) === cueId);
  if (!cue) return;
  const value = String(event.currentTarget.textContent || "").replace(/\r?\n/g, " ").slice(0, 500);
  if (event.currentTarget.textContent !== value) event.currentTarget.textContent = value;
  cue.text = value;
  scheduleSecondaryEditorSubtitleSave(650);
});
$("#secondaryEditorSubtitlePreview")?.querySelector("[data-secondary-subtitle-preview-text]")?.addEventListener("keydown", (event) => {
  if (event.key === "Enter") { event.preventDefault(); event.currentTarget.blur(); }
  else if (event.key === "Escape") { event.preventDefault(); event.currentTarget.blur(); }
  event.stopPropagation();
});
$("#secondaryEditorSubtitlePreview")?.querySelector("[data-secondary-subtitle-preview-text]")?.addEventListener("blur", () => {
  $("#secondaryEditorSubtitlePreview")?.classList.remove("is-editing");
  renderSecondaryEditorSubtitleTrack();
  scheduleSecondaryEditorSubtitleSave(0);
});
$("#secondaryEditorSubtitlePreview")?.addEventListener("keydown", (event) => {
  if (event.target.closest("[data-secondary-subtitle-preview-text]")) return;
  if (!["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown"].includes(event.key)) return;
  event.preventDefault();
  event.stopPropagation();
  const resize = event.target.closest("[data-secondary-subtitle-resize]");
  if (resize) nudgeSecondaryEditorSubtitleCanvas("resize", ["ArrowRight", "ArrowUp"].includes(event.key) ? 1 : -1, 0, event.shiftKey);
  else nudgeSecondaryEditorSubtitleCanvas("move", event.key === "ArrowLeft" ? -1 : event.key === "ArrowRight" ? 1 : 0, event.key === "ArrowUp" ? -1 : event.key === "ArrowDown" ? 1 : 0, event.shiftKey);
});
$("#secondaryEditorAiForm")?.addEventListener("submit", (event) => {
  event.preventDefault();
  const input = $("#secondaryEditorAiInput");
  const text = String(input?.value || "").trim();
  if (!text) return;
  const scope = $("#secondaryEditorAiScope")?.value || "selection";
  const selectedClipIds = scope === "timeline"
    ? (secondaryEditSession?.clips || []).map((clip) => String(clip.id))
    : [...secondaryEditSelectedClips];
  if (!selectedClipIds.length) return void showToast("请先选择片段，或将修改范围改为整个成片");
  createSecondaryEditorProposal(text, selectedClipIds);
});
$("#secondaryEditorAiScope")?.addEventListener("change", syncSecondaryEditorAiScope);
$("#secondaryEditor")?.querySelectorAll("[data-secondary-command]").forEach((button) => button.addEventListener("click", () => {
  const input = $("#secondaryEditorAiInput");
  if (input) { input.value = button.dataset.secondaryCommand || ""; input.focus(); }
  const scope = $("#secondaryEditorAiScope");
  if (scope && button.dataset.secondaryCommandScope) scope.value = button.dataset.secondaryCommandScope;
  syncSecondaryEditorAiScope();
}));
const secondaryEditorRuler = $("#secondaryEditorRuler");
$("#secondaryEditorTimelineViewport")?.addEventListener("wheel", (event) => {
  if (!(event.ctrlKey || event.metaKey)) return;
  event.preventDefault();
  adjustSecondaryEditorTimelineZoom(event.deltaY < 0 ? "in" : "out");
}, { passive: false });
secondaryEditorRuler?.addEventListener("pointerdown", (event) => {
  if (!secondaryEditSession || secondaryEditBusy) return;
  event.preventDefault();
  secondaryEditorRuler.dataset.scrubbing = "true";
  secondaryEditorRuler.setPointerCapture?.(event.pointerId);
  secondaryEditorSeekOutputTime(secondaryEditorOutputTimeFromPointer(event), false);
});
secondaryEditorRuler?.addEventListener("pointermove", (event) => {
  if (secondaryEditorRuler.dataset.scrubbing !== "true") return;
  secondaryEditorSeekOutputTime(secondaryEditorOutputTimeFromPointer(event), false);
});
["pointerup", "pointercancel"].forEach((eventName) => secondaryEditorRuler?.addEventListener(eventName, (event) => {
  delete secondaryEditorRuler.dataset.scrubbing;
  secondaryEditorRuler.releasePointerCapture?.(event.pointerId);
}));
secondaryEditorRuler?.addEventListener("keydown", (event) => {
  if (!secondaryEditSession || secondaryEditBusy) return;
  const step = event.shiftKey ? 1 : 1 / secondaryEditorFrameRate();
  let next = null;
  if (event.key === "ArrowLeft") next = secondaryEditPlayheadTime - step;
  else if (event.key === "ArrowRight") next = secondaryEditPlayheadTime + step;
  else if (event.key === "Home") next = 0;
  else if (event.key === "End") next = secondaryEditorTimelineDuration();
  if (next === null) return;
  event.preventDefault();
  event.stopPropagation();
  secondaryEditorSeekOutputTime(next, false);
});
$("#secondaryEditorMediaPlay")?.addEventListener("click", () => {
  const video = $("#secondaryEditorVideo");
  if (!video || !secondaryEditSession) return;
  if (!video.paused) return void video.pause();
  const model = secondaryEditorMediaControlModel();
  if (Number(model.duration || 0) > 0 && Number(model.current || 0) >= Number(model.duration) - .03) {
    if (model.coordinate === "sequence" && !secondaryEditExactPreview) secondaryEditorSeekOutputTime(0, false);
    else video.currentTime = Number(model.sourceOffset || 0);
  }
  video.play().catch(() => {});
});
$("#secondaryEditorMediaSeek")?.addEventListener("input", (event) => {
  if (!secondaryEditSession) return;
  const video = $("#secondaryEditorVideo");
  const model = secondaryEditorMediaControlModel();
  const value = Math.max(0, Math.min(Number(model.duration || 0), Number(event.currentTarget.value || 0)));
  if (model.coordinate === "sequence" && !secondaryEditExactPreview) secondaryEditorSeekOutputTime(value, false);
  else if (video) {
    video.currentTime = Number(model.sourceOffset || 0) + value;
    if (secondaryEditView === "source") secondaryEditSourceTime = video.currentTime;
    if (secondaryEditExactPreview) updateSecondaryEditorPlayhead(value);
  }
  syncSecondaryEditorMediaControls();
});
$("#secondaryEditorMediaSeek")?.addEventListener("pointerdown", (event) => { event.currentTarget.dataset.scrubbing = "true"; });
["pointerup", "pointercancel", "change"].forEach((eventName) => $("#secondaryEditorMediaSeek")?.addEventListener(eventName, (event) => {
  delete event.currentTarget.dataset.scrubbing;
  syncSecondaryEditorMediaControls();
}));
$("#secondaryEditorMediaMute")?.addEventListener("click", () => {
  const video = $("#secondaryEditorVideo");
  if (!video) return;
  video.muted = !video.muted;
  syncSecondaryEditorMediaControls();
});
$("#secondaryEditorMediaFullscreen")?.addEventListener("click", async () => {
  const player = $(".secondary-editor-player");
  try {
    if (document.fullscreenElement) await document.exitFullscreen();
    else await player?.requestFullscreen?.();
  } catch { showToast("浏览器未允许进入全屏"); }
  syncSecondaryEditorMediaControls();
});
$("#secondaryEditorVideo")?.addEventListener("dblclick", () => $("#secondaryEditorMediaFullscreen")?.click());
["play", "pause", "loadedmetadata", "durationchange", "volumechange", "ended"].forEach((eventName) => $("#secondaryEditorVideo")?.addEventListener(eventName, syncSecondaryEditorMediaControls));
$("#secondaryEditorVideo")?.addEventListener("loadedmetadata", () => { renderSecondaryEditorSubtitlePreview(); renderSecondaryEditorTextCanvas(); });
document.addEventListener("fullscreenchange", () => { syncSecondaryEditorMediaControls(); renderSecondaryEditorSubtitlePreview(); renderSecondaryEditorTextCanvas(); });
$("#secondaryEditorVideo")?.addEventListener("timeupdate", (event) => {
  const video = event.currentTarget;
  if (!secondaryEditSession) return;
  if (secondaryEditExactPreview) {
    updateSecondaryEditorPlayhead(video.currentTime, true);
    $("#secondaryEditorClock").textContent = `成片 ${formatPreciseTimecode(video.currentTime)} / ${formatPreciseTimecode(video.duration || secondaryEditorTimelineDuration())}`;
    return;
  }
  if (secondaryEditView === "source") {
    secondaryEditSourceTime = Number(video.currentTime || 0);
    if (secondaryEditPreviewMaterialRange && video.currentTime >= Number(secondaryEditPreviewMaterialRange.end) - .03) {
      video.pause();
      video.currentTime = Number(secondaryEditPreviewMaterialRange.end);
      $("#secondaryEditorVideoBadge").textContent = `素材预览完成 · ${secondaryEditPreviewMaterialRange.title}`;
    }
    const selectedId = secondaryEditClip()?.id;
    const selected = secondaryEditorTimelineModel().clips.find((clip) => String(clip.id) === String(selectedId));
    if (selected && video.currentTime >= Number(selected.sourceStart || 0) && video.currentTime <= Number(selected.sourceEnd || 0)) {
      const index = Math.max(0, (secondaryEditSession.clips || []).findIndex((clip) => String(clip.id) === String(selected.id)));
      const item = secondaryEditorSchedule()[index];
      const local = (video.currentTime - Number(selected.sourceStart || 0)) / Math.max(.01, Number(selected.playbackRate || 1));
      updateSecondaryEditorPlayhead(Number(item?.outputStart || 0) + local);
    }
    $("#secondaryEditorClock").textContent = `源片 ${formatPreciseTimecode(video.currentTime)} / ${formatPreciseTimecode(video.duration || currentJob?.videoInfo?.duration || 0)}`;
    syncSecondaryEditorMediaControls();
    return;
  }
  const model = secondaryEditorTimelineModel();
  const clips = model.clips;
  let clip = clips[secondaryEditSequenceIndex] || clips[0];
  if (!clip) return;
  if (video.currentTime < Number(clip.sourceStart) - .05 || video.currentTime > Number(clip.sourceEnd) + .15) video.currentTime = Number(clip.sourceStart);
  if (video.currentTime >= Number(clip.sourceEnd) - .03) {
    if (secondaryEditorInspectorHasChanges()) {
      video.pause();
      const previewEnd = Number(model.schedule[secondaryEditSequenceIndex]?.outputEnd || model.duration);
      updateSecondaryEditorPlayhead(previewEnd, true);
      $("#secondaryEditorClock").textContent = `成片 ${formatPreciseTimecode(previewEnd)} / ${formatPreciseTimecode(model.duration)}`;
      return;
    }
    if (secondaryEditSequenceIndex < clips.length - 1) {
      secondaryEditSequenceIndex += 1;
      const next = clips[secondaryEditSequenceIndex];
      video.currentTime = Number(next.sourceStart);
      video.playbackRate = Number(next.playbackRate || 1);
      secondaryEditSelectedClips = new Set([String(next.id)]);
      clip = next;
      renderSecondaryEditorTimeline();
      renderSecondaryEditorInspector();
    } else {
      video.pause();
      updateSecondaryEditorPlayhead(model.duration, true);
    }
  }
  const schedule = secondaryEditorSchedule()[secondaryEditSequenceIndex];
  const local = Math.max(0, video.currentTime - Number(clip.sourceStart)) / Math.max(.01, Number(clip.playbackRate || 1));
  const outputTime = Math.min(model.duration, Number(schedule?.outputStart || 0) + local);
  updateSecondaryEditorPlayhead(outputTime, true);
  $("#secondaryEditorClock").textContent = `成片 ${formatPreciseTimecode(outputTime)} / ${formatPreciseTimecode(model.duration)}`;
});

function moveSecondaryEditorWorkspaceResize(event) {
  if (!secondaryEditorResizeDrag) return;
  event.preventDefault();
  const ratio = (event.clientY - secondaryEditorResizeDrag.top) / Math.max(1, secondaryEditorResizeDrag.height) * 100;
  setSecondaryEditorUpperRatio(ratio);
}

function finishSecondaryEditorWorkspaceResize() {
  if (!secondaryEditorResizeDrag) return;
  secondaryEditorResizeDrag = null;
  $("#secondaryEditorWorkspaceResize")?.classList.remove("dragging");
  document.removeEventListener("pointermove", moveSecondaryEditorWorkspaceResize);
  document.removeEventListener("pointerup", finishSecondaryEditorWorkspaceResize);
  document.removeEventListener("pointercancel", finishSecondaryEditorWorkspaceResize);
  persistSecondaryEditorLayout();
}

$("#secondaryEditorWorkspaceResize")?.addEventListener("pointerdown", (event) => {
  if (event.button !== 0) return;
  const layout = $(".secondary-editor-layout");
  const rect = layout?.getBoundingClientRect();
  if (!rect?.height) return;
  event.preventDefault();
  secondaryEditorResizeDrag = { top: rect.top, height: rect.height };
  event.currentTarget.classList.add("dragging");
  document.addEventListener("pointermove", moveSecondaryEditorWorkspaceResize);
  document.addEventListener("pointerup", finishSecondaryEditorWorkspaceResize, { once: true });
  document.addEventListener("pointercancel", finishSecondaryEditorWorkspaceResize, { once: true });
});
$("#secondaryEditorWorkspaceResize")?.addEventListener("dblclick", () => setSecondaryEditorUpperRatio(58, { persist: true }));
$("#secondaryEditorWorkspaceResize")?.addEventListener("keydown", (event) => {
  if (!["ArrowUp", "ArrowDown", "Home", "End"].includes(event.key)) return;
  event.preventDefault();
  const next = event.key === "Home" ? 36 : event.key === "End" ? 58 : secondaryEditorUpperRatio + (event.key === "ArrowUp" ? -2 : 2);
  setSecondaryEditorUpperRatio(next, { persist: true });
});

function moveSecondaryEditorInspectorResize(event) {
  if (!secondaryEditorInspectorResizeDrag) return;
  event.preventDefault();
  const width = secondaryEditorInspectorResizeDrag.width - (event.clientX - secondaryEditorInspectorResizeDrag.clientX);
  setSecondaryEditorInspectorWidth(width);
}

function finishSecondaryEditorInspectorResize() {
  if (!secondaryEditorInspectorResizeDrag) return;
  secondaryEditorInspectorResizeDrag = null;
  $("#secondaryEditorInspectorResize")?.classList.remove("dragging");
  document.removeEventListener("pointermove", moveSecondaryEditorInspectorResize);
  document.removeEventListener("pointerup", finishSecondaryEditorInspectorResize);
  document.removeEventListener("pointercancel", finishSecondaryEditorInspectorResize);
  persistSecondaryEditorLayout();
}

$("#secondaryEditorInspectorResize")?.addEventListener("pointerdown", (event) => {
  if (event.button !== 0 || window.innerWidth < 1024) return;
  event.preventDefault();
  secondaryEditorInspectorResizeDrag = { clientX: event.clientX, width: secondaryEditorInspectorWidth };
  event.currentTarget.classList.add("dragging");
  document.addEventListener("pointermove", moveSecondaryEditorInspectorResize);
  document.addEventListener("pointerup", finishSecondaryEditorInspectorResize, { once: true });
  document.addEventListener("pointercancel", finishSecondaryEditorInspectorResize, { once: true });
});
$("#secondaryEditorInspectorResize")?.addEventListener("dblclick", () => setSecondaryEditorInspectorWidth(secondaryEditorDefaultInspectorWidth(), { persist: true }));
$("#secondaryEditorInspectorResize")?.addEventListener("keydown", (event) => {
  if (!["ArrowLeft", "ArrowRight", "Home", "End"].includes(event.key)) return;
  event.preventDefault();
  const { min, max } = secondaryEditorInspectorWidthBounds();
  const next = event.key === "Home" ? min : event.key === "End" ? max : secondaryEditorInspectorWidth + (event.key === "ArrowLeft" ? 16 : -16);
  setSecondaryEditorInspectorWidth(next, { persist: true });
});
window.addEventListener("resize", () => {
  if (secondaryEditorOpen()) restoreSecondaryEditorLayout();
  if (secondaryEditorOpen() && secondaryEditTimelinePixelsPerSecond === null) renderSecondaryEditorTimeline();
  if (secondaryEditorOpen()) { renderSecondaryEditorSubtitlePreview(); renderSecondaryEditorTextCanvas(); }
});
document.addEventListener("keydown", (event) => {
  if (!secondaryEditorOpen()) return;
  const editable = /INPUT|TEXTAREA|SELECT/.test(event.target?.tagName || "") || Boolean(event.target?.isContentEditable);
  if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "z") { event.preventDefault(); secondaryEditorHistory(event.shiftKey ? "redo" : "undo"); }
  else if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "y") { event.preventDefault(); secondaryEditorHistory("redo"); }
  else if (!editable && (event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "b") { event.preventDefault(); $("#secondaryEditorSplit")?.click(); }
  else if (!editable && event.code === "Space") { event.preventDefault(); const video = $("#secondaryEditorVideo"); if (video?.paused) video.play().catch(() => {}); else video?.pause(); }
  else if (!editable && event.key.toLowerCase() === "q") { event.preventDefault(); trimSecondaryEditorAtPlayhead("left"); }
  else if (!editable && event.key.toLowerCase() === "w") { event.preventDefault(); trimSecondaryEditorAtPlayhead("right"); }
  else if (!editable && event.key === ",") { event.preventDefault(); secondaryEditorStepFrame(-1); }
  else if (!editable && event.key === ".") { event.preventDefault(); secondaryEditorStepFrame(1); }
  else if (!editable && event.key === "ArrowLeft") { event.preventDefault(); secondaryEditorStepFrame(event.shiftKey ? -secondaryEditorFrameRate() : -1); }
  else if (!editable && event.key === "ArrowRight") { event.preventDefault(); secondaryEditorStepFrame(event.shiftKey ? secondaryEditorFrameRate() : 1); }
  else if (!editable && (event.key === "+" || event.key === "=")) { event.preventDefault(); adjustSecondaryEditorTimelineZoom("in"); }
  else if (!editable && event.key === "-") { event.preventDefault(); adjustSecondaryEditorTimelineZoom("out"); }
  else if (!editable && event.key === "/") { event.preventDefault(); adjustSecondaryEditorTimelineZoom("fit"); }
  else if (!editable && (event.key === "Delete" || event.key === "Backspace") && secondaryEditorTextLayer()) { event.preventDefault(); secondaryEditorOperation({ type: "delete_text_layer", layerId: secondaryEditSelectedTextLayerId }); }
  else if (!editable && (event.key === "Delete" || event.key === "Backspace") && secondaryEditSelectedClips.size) { event.preventDefault(); secondaryEditorOperation({ type: "delete_clips", clipIds: [...secondaryEditSelectedClips] }); }
  else if (event.key === "Escape" && !subtitleReviewDraft) void requestCloseSecondaryEditor();
});

function toggleSecondaryEditorPanel(panel) {
  const editor = $("#secondaryEditor");
  if (!editor) return;
  const className = panel === "library" ? "library-open" : "inspector-open";
  const other = panel === "library" ? "inspector-open" : "library-open";
  const open = !editor.classList.contains(className);
  editor.classList.toggle(className, open);
  if (open) editor.classList.remove(other);
  syncSecondaryEditorPanelAria();
  persistSecondaryEditorLayout();
  scheduleSecondaryEditorTimelineLayout();
}
$("#secondaryEditorLibraryToggle")?.addEventListener("click", () => toggleSecondaryEditorPanel("library"));
$("#secondaryEditorInspectorToggle")?.addEventListener("click", () => toggleSecondaryEditorPanel("inspector"));
$("#secondaryInspectorTabs")?.querySelectorAll("[data-secondary-inspector-tab]").forEach((button) => {
  button.addEventListener("click", () => setSecondaryInspectorMode(button.dataset.secondaryInspectorTab));
});

const reviewMoreActions = $("#reviewMoreActions");
document.addEventListener("pointerdown", (event) => {
  if (reviewMoreActions?.open && !reviewMoreActions.contains(event.target)) reviewMoreActions.open = false;
});
document.addEventListener("keydown", (event) => {
  if (event.key !== "Escape" || !reviewMoreActions?.open) return;
  reviewMoreActions.open = false;
  reviewMoreActions.querySelector("summary")?.focus();
});
reviewMoreActions?.querySelectorAll("a, button").forEach((control) => {
  control.addEventListener("click", () => { reviewMoreActions.open = false; });
});

loadHealth();
loadSetupReadiness({ autoOpen: true });
setInterval(loadHealth, 15000);
clearInterval(elapsedTicker);
elapsedTicker = setInterval(() => {
  if (currentJob) {
    updateJobElapsedClock(currentJob);
    const inlineElapsed = document.querySelector("[data-inline-elapsed]");
    if (inlineElapsed) {
      inlineElapsed.textContent = processingElapsedLabel(currentJob);
    }
    const etaText = progressEtaText(currentJob, !stageProgressIsDeterminate(currentJob));
    const consoleEta = $("#jobEta");
    const inlineEta = document.querySelector("[data-inline-eta]");
    const cancellationPending = document.querySelector("#inlineAnalysisProgress")?.dataset.cancelRequested === "true";
    if (consoleEta && !cancellationPending) consoleEta.textContent = etaText;
    if (inlineEta && !cancellationPending) inlineEta.textContent = etaText;
  }
}, 1000);
document.addEventListener("visibilitychange", () => {
  if (document.hidden) {
    clearTimeout(pollTimer);
    pollTimer = null;
  } else if (currentJob && jobNeedsPolling(currentJob)) {
    pollJob();
  } else {
    loadHealth();
  }
});
setupDirectorWorkspace();
homeNavigationRequested = false;
// Reset transient DOM state without discarding the last opened task.  The
// history loader below uses that id to restore the same task after refresh.
try {
  resetWorkspace(true, false);
} catch (error) {
  // A legacy optional panel must not abort dashboard initialization.  The
  // home grid is still usable even if one transient workspace node is absent.
  console.error("工作区初始化失败，继续加载主页", error);
  studio?.classList.add("home-mode");
  $("#homeView")?.classList.remove("hidden");
}
homeNavigationRequested = false;
// Load the dashboard independently from task restoration. A restore request
// must never be allowed to prevent the recent-task cards from rendering. Keep
// a delayed retry as a safety net for browsers restoring a cached DOM shell.
window.setTimeout(loadHomeTasks, 0);
window.setTimeout(() => {
  const grid = $("#homeTaskGrid");
  if (grid?.querySelector(".home-loading-card")) loadHomeTasks();
}, 1200);
restoreCurrentJob();
syncThinkingOrbs(document);
syncBorderBeams(document);
updateComposerBeam();


document.querySelectorAll("[data-secondary-open-panel]").forEach(button => {
  button.addEventListener("click", () => {
    setSecondaryInspectorMode(button.dataset.secondaryOpenPanel);
    renderSecondaryEditorSubtitleTrack();
    button.closest("details")?.removeAttribute("open");
  });
});
document.querySelector("[data-secondary-add-material]")?.addEventListener("click", () => {
  const editor = $("#secondaryEditor");
  editor?.classList.remove("inspector-open");
  editor?.classList.add("library-open");
  syncSecondaryEditorPanelAria();
  persistSecondaryEditorLayout();
});
document.querySelectorAll("[data-secondary-close-panel]").forEach(button => {
  button.addEventListener("click", () => {
    $("#secondaryEditor")?.classList.remove("library-open", "inspector-open");
    syncSecondaryEditorPanelAria();
    persistSecondaryEditorLayout();
  });
});
$("#secondaryEditorPanelScrim")?.addEventListener("click", () => {
  $("#secondaryEditor")?.classList.remove("library-open", "inspector-open");
  syncSecondaryEditorPanelAria();
  persistSecondaryEditorLayout();
});

$("#secondaryEditor .secondary-editor-action-overflow")?.addEventListener("click", event => {
  if (event.target.closest("button")) event.currentTarget.removeAttribute("open");
});
