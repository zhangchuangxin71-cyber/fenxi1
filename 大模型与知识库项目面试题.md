# 大模型与知识库项目面试题

> 配套实现分析：[大模型与知识库项目实现详解](./大模型与知识库项目实现详解.md)  
> 使用建议：先尝试脱稿回答每个问题，再对照“回答要点”；涉及流程的问题尽量亲手画图。

---

## 一、项目整体架构

### 1. 请用 3 分钟介绍这个项目

回答要点：

- 这是一个文档知识库和大模型内容生产平台。
- 摄取服务负责下载、解析、建树和入库。
- MinerU 负责 PDF/OCR/版面解析。
- PostgreSQL 保存文档、目录节点、页面和用户绑定。
- 检索服务负责查询分类、文档路由、树导航和页级证据获取。
- Knowledge Chat、Report Agent、WeChat Agent 共享检索服务。
- 普通问答基本无状态，公众号长流程使用 LangGraph Checkpoint 和 Artifact 持久化。

### 2. 一份 PDF 从上传到最终回答，完整经过哪些步骤？

```text
OSS 上传
→ ingestion API
→ 计算内容哈希和 doc_id
→ 异步任务队列
→ MinerU/本地解析
→ 构建 PageIndex 树
→ 写入 documents/nodes/pages/bindings
→ 查询分类
→ 文档路由
→ 节点导航
→ 获取原始页面
→ LLM 基于证据回答
→ SSE 返回文本和引用
```

### 3. 为什么要拆成多个微服务，而不是放在一个项目里？

回答要点：

- 文档解析耗时长，不能阻塞在线问答。
- 检索可以被问答、报告、公众号多个应用复用。
- 各服务的扩容方式不同。
- 故障隔离：MinerU 失败不应直接拖垮聊天服务。
- Agent 应用可以独立迭代。
- 代价是调用链、鉴权、监控和一致性更复杂。

### 4. 哪些服务是有状态的，哪些是无状态的？

- Knowledge Chat：基本无状态，历史由客户端传入。
- Report Agent：大纲和报告主要由客户端传回。
- Retrieval Service：请求级状态，长期数据在 PostgreSQL。
- Ingestion：任务状态持久化，但队列和部分调度是进程内的。
- WeChat Agent：有状态，使用 PostgreSQL Checkpoint 和 Artifact。

---

## 二、文档摄取与更新

### 5. 为什么文档 ID 根据内容哈希生成？

回答要点：

- 相同内容得到稳定 ID。
- 支持内容去重。
- 避免重复 OCR、解析和摘要。
- 内容变更自动产生新版本 ID。
- 文件名不能作为身份，因为文件名可重复、可修改。

可能的追问：这种设计有什么缺点？

- 无法原地覆盖更新。
- 内容稍微变化就产生新 ID。
- 需要业务层维护逻辑文档与版本的关系。
- 大文件提交阶段必须完整下载和计算哈希。

### 6. 更新文档是否需要重启服务？

不需要。内容变化后重新调用摄取 API，系统生成新的 `doc_id`。推荐：

```text
上传新版本
→ 等待解析完成
→ 业务端切换 doc_id
→ 删除旧版本绑定
```

### 7. 相同文件重复上传会怎样？

- 同内容生成相同 `doc_id`。
- 正式文档在同一用户和知识库下通常返回重复冲突。
- 临时文档会复用已有结果，不重新解析。
- 不同用户可以共享内容实体，但拥有各自的 binding。
- 幂等键主要防止调用方重试造成重复任务。

### 8. 幂等键和内容去重有什么区别？

- 幂等键判断“这是不是同一次业务提交”。
- 内容哈希判断“这是不是同一份文件内容”。
- 幂等键相同但文件不同，理论上应该识别为调用语义冲突。
- 内容相同但幂等键不同，仍然是重复内容。

### 9. 为什么解析任务要异步执行？

- OCR、Office 转换、MinerU、摘要生成都可能耗时很长。
- HTTP 请求不适合一直阻塞。
- 可以独立控制并发、超时、重试和优先级。
- 客户端通过 `task_id` 查询进度或接收回调。

### 10. 正式文档和临时文档有什么区别？

- 临时文档进入高优先队列。
- 临时文档关联 session，但当前读取授权主要由 user/kb/doc_ids 决定。
- 临时文档可在会话结束时批量删除。
- 相同临时文档重复上传会复用。
- 正式文档用于长期知识库。

### 11. MinerU 失败怎么办？

- 重试、退避、超时和并发限制。
- 根据配置退回本地解析器。
- 记录 MinerU raw 修复状态。
- 后续可异步补充 `raw_mineru`。
- 补 raw 不重建 page/node，也不改变 doc_id。

---

## 三、数据模型

### 12. 为什么要把 `documents` 和 `document_bindings` 分开？

```text
documents：共享内容
document_bindings：用户/知识库归属
```

好处：

- 相同内容只解析和存储一次。
- 多个用户可以绑定同一内容。
- 删除一个用户的文档不会影响其他用户。
- 内容去重和权限归属解耦。

### 13. `doc_nodes` 和 `doc_pages` 为什么都要存在？

- `doc_nodes` 是文档地图，用于章节导航。
- `doc_pages` 是原始证据，用于回答和引用。
- 节点摘要适合低成本路由。
- 最终回答回到页面，减少摘要失真。

### 14. 删除文档时会删除什么？

- 先删除当前 `user_id + kb_id` 的 binding。
- 如果还有其他 binding，共享文档继续保留。
- 没有任何 binding 后，才删除 documents、nodes 和 pages。
- 正在摄取的正式文档删除可能返回冲突。

### 15. 为什么部分字段放 JSONB，部分放关系表？

- 页和节点需要频繁查询、过滤和关联，适合关系表。
- MinerU 原始结构变化较多，适合 JSONB。
- 大段页面正文不应在 JSONB 和关系表中重复保存。
- JSONB 灵活，但索引、约束和更新成本需要控制。

---

## 四、PageIndex 与检索算法

### 16. 这个项目和传统向量 RAG 有什么不同？

传统向量 RAG：

```text
固定长度切块
→ embedding
→ 向量 Top-K
→ LLM
```

本项目：

```text
文档目录树
→ 查询分类
→ 文档路由
→ 节点导航
→ 读取原始页面
→ LLM
```

当前主链路没有独立的向量数据库。它更适合结构清晰的长文档、报告、制度和论文。

### 17. 为什么不把所有页面直接发给大模型？

- 上下文长度有限。
- 成本和延迟过高。
- 无关内容会降低模型注意力。
- 多文档时容易被前面的文档占满预算。
- 因此先定位文档和章节，再读取具体页面。

### 18. 为什么最终证据来自页面，而不是节点摘要？

- 摘要可能遗漏条件、数字和限定词。
- 摘要是模型二次生成内容，不是原始事实。
- 页面可以提供稳定页码引用。
- 节点负责导航，页面负责取证。

### 19. 检索为什么要分成四种类型？

| 类型 | 作用 |
|---|---|
| `scope_direct` | 文档范围和元数据 |
| `routed_direct` | 页码、目录、指定章节 |
| `routed_focused` | 精确事实 |
| `routed_broad` | 全文总结和跨文档比较 |

原因：不同问题需要不同检索策略。用户明确问第 5 页时，没有必要做语义检索；全文总结也不能只返回最相关的三页。

### 20. Focused Search 如何工作？

```text
查看文档描述
→ 查看顶层节点
→ 选择可能相关章节
→ 展开子节点
→ 获取页码范围
→ 阅读具体页面
→ 判断 accept/possible/reject
```

如果树结构不可用，则退回页面扫描。

### 21. Broad Retrieval 如何保证全文覆盖？

- 排除封面、目录等低信息页面。
- 每个主要章节选代表页面。
- 没有目录时均匀采样。
- 用查询关键词补充排序。
- 多文档之间轮询取页。
- 防止第一篇文档吃完 Token 预算。

### 22. BM25 在这个项目中用在哪里？

旧版 BM25 主要对以下内容做文档级预筛，而不是页面级全文检索：

```text
文档名称 + 文档描述 + 文件名
```

中文使用单字和相邻双字的轻量分词。

### 23. `top_k` 为什么不是绝对硬限制？

- 用户指定第 1～10 页时，直接结果可能超过 `top_k`。
- scope/direct 返回的是原子业务结果。
- 合并阶段更重要的是 Token 预算和文档覆盖。
- focused accept 证据优先，possible 和 broad 再补充。

### 24. 多文档名字非常相似怎么办？

- 先用关键词、文件名和描述预筛。
- 用 IDF/锚点词进一步缩小。
- LLM 将文档分为 accept、possible、reject。
- 如果仍无法可靠区分，应要求用户澄清，不能随便选一篇。

---

## 五、知识问答

### 25. Knowledge Chat 为什么需要两次模型调用？

第一次负责查询规划：

- 是否需要检索。
- 指代解析。
- 拆分子问题。
- 生成检索查询。

第二次负责：

- 基于证据生成最终回答。
- 插入引用。
- 流式输出。

把“决定查什么”和“最终怎么回答”分开，更容易控制质量。

### 26. 如何理解“这篇”“刚上传的文件”？

服务先调用文档元数据接口获得：

- 新上传文档 ID。
- 真实文件名。
- 当前唯一文档。
- 完整聊天历史。

然后让路由模型结合这些信息消解指代。

### 27. 如果检索没有结果，系统应该怎么回答？

需要区分：

- 根本不需要检索。
- 检索执行成功但无结果。
- 检索服务异常。
- 有知识库证据。

不能在无结果时声称“根据知识库”。前端也应展示对应 basis 状态。

### 28. 引用是怎么生成的？

- 检索 chunk 编号为 `[1]`、`[2]`。
- Prompt 要求模型引用编号。
- 流结束后解析正文实际出现的编号。
- 只返回真正使用过的 references。
- 引用必须映射回文档和页码。

### 29. 如何防御知识库 Prompt Injection？

- 文件名和正文都作为不可信数据。
- 文档中的指令不能覆盖系统提示。
- 证据只能用于回答事实，不能决定权限和工具范围。
- 工具参数必须由受信逻辑校验。
- 不能让文档内容扩大可访问的 doc_ids。

---

## 六、报告 Agent

### 30. 报告 Agent 支持哪些意图？

- 通用问答。
- 知识库问答。
- 生成大纲。
- 修改大纲。
- 撰写报告。
- 编辑报告。

### 31. 为什么查询改写要带 profile？

不同任务的检索需求不同：

- `precise`：少而精。
- `overview`：增加章节覆盖。
- `report`：更大 Token 预算和多文档覆盖。
- `compare`：分别检索多个对象，再做比较。

### 32. Report Agent 为什么不像 WeChat Agent 那样持久化状态？

Report Agent 主要由客户端传回历史消息、当前大纲、已确认大纲和当前报告。

优点是服务容易水平扩容；缺点是客户端复杂、请求体变大，且状态完整性依赖客户端。

---

## 七、公众号 Agent 与 LangGraph

### 33. 为什么公众号 Agent 要使用 LangGraph Checkpoint？

因为流程可能持续很久，并多次等待用户：

```text
调研方向确认
→ 任务书审核
→ 大纲审核
→ 文章审核
```

Checkpoint 让图可以暂停、在进程重启后恢复，并从 interrupt 继续执行。

### 34. Checkpoint 和 Artifact 有什么区别？

- Checkpoint：保存程序执行状态，解决“从哪个节点恢复”。
- Artifact：保存业务产物，解决“用户有哪些文章版本”。

Checkpoint 面向运行时；Artifact 面向产品和业务。

### 35. 什么是 HITL？

HITL 是 Human in the Loop，即人工参与决策。

系统在关键阶段暂停，用户可以：

- approve
- revise
- regenerate
- cancel
- 选择冲突处理方式

然后通过 `interrupt_id` 恢复工作流。

### 36. 为什么需要 `previous_response_id + interrupt_id`？

- 防止旧页面提交过期审核。
- 确保决策针对当前等待节点。
- 防止重复点击。
- 防止上一个 revision 的操作覆盖新 revision。

### 37. revision 和 parent artifact 有什么作用？

- 每次重要修改创建新 revision。
- `parent_artifact_id` 形成版本链。
- 可以追踪来源和回滚。
- 新版本只继承已批准阶段的产物。
- 防止未审核内容污染后续流程。

### 38. 用户断开 SSE，为什么不立即取消任务？

- 手机锁屏和网络抖动很常见。
- 长时间调研或写作不应该因此丢失。
- 后台继续运行并保存状态。
- 用户重连后可以查询或恢复。
- 真正取消应调用明确的 cancel API。

### 39. 如何避免同一会话同时运行两个文章任务？

- PostgreSQL advisory lock。
- 同 session 串行 admission。
- 条件更新检查 response 所有权和状态。
- 新请求可以将旧任务标记为 superseded。
- 运行时和业务表共同检查当前 revision。

---

## 八、可靠性与性能

### 40. 如果 LLM 返回的 JSON 不符合格式怎么办？

- 使用严格 JSON Schema/Pydantic。
- 解析失败后做修复或有限重试。
- 使用规则 fallback。
- 记录原始错误和 trace。
- 不能无限重试。
- 对高风险决策宁可让用户澄清。

### 41. 检索服务超时怎么办？

- 整体 deadline。
- 为结果合并预留时间。
- 节点级超时。
- 已经找到的证据尽量作为部分结果返回。
- 没有任何证据时才返回 503/504。
- LLM 调用配合 circuit breaker 和限流。

### 42. 进程内队列有什么问题？

- 多实例之间队列不共享。
- 进程退出时待处理内存任务可能丢失。
- 扩容后公平性不好控制。
- 需要数据库恢复扫描或外部队列。
- 大规模生产环境可考虑 Redis Streams、RabbitMQ、Kafka 或 Celery。

### 43. 如何评估这套 RAG 系统？

至少评估：

- 文档路由准确率。
- 页级 Recall@K。
- 证据覆盖率。
- 答案忠实度。
- 引用正确率。
- 无答案识别率。
- P50/P95/P99 延迟。
- 每次请求模型调用次数。
- 输入/输出 Token 成本。
- 超时和 fallback 比例。

---

## 九、安全问题

### 44. 当前项目最明显的安全风险是什么？

当前 Retrieval Repository 虽然接收 `user_id/kb_id/session_id`，但部分 SQL 主要依赖调用方传入的 `doc_ids`，没有在数据库查询中完整验证 binding。

风险：

```text
如果攻击者获得其他用户的 doc_id
→ 直接调用检索服务
→ 可能越权读取页面
```

修复措施：

- 检索服务只开放内网。
- 上游逐个校验 doc_id。
- SQL 增加 `EXISTS document_bindings`。
- 临时文档同时验证 session 和临时标记。
- 增加跨用户越权自动化测试。

### 45. 为什么不能只依赖 API Gateway 鉴权？

Gateway 只能确认“你是谁”，不能自动确认“你是否拥有这个 doc_id”。正确做法是：

```text
身份认证
+ 资源级授权
+ 数据库级绑定校验
```

---

## 十、设计改进题

### 46. 如果让你加入向量检索，你会放在哪里？

一个合理方案：

```text
文档路由
→ PageIndex 缩小章节范围
→ 对候选页面做 embedding 检索
→ reranker
→ 页级证据合并
```

不建议立即用向量检索完全替换结构检索。可以让目录树负责结构、向量检索负责语义召回、Reranker 负责最终排序、原始页面负责引用。

### 47. 如何实现真正的文档版本管理？

增加逻辑文档和文档版本两层：

```text
logical_documents
  logical_document_id
  user_id
  kb_id
  display_name
  current_version_id

document_versions
  version_id
  logical_document_id
  doc_id
  version_number
  status
  created_at
```

更新流程：

```text
新版本入库
→ 解析成功
→ 事务切换 current_version_id
→ 旧版本保留或延迟清理
```

### 48. 如何实现零停机重建索引？

- 保留旧索引版本继续提供检索。
- 新版本写入独立 version。
- 完成后执行质量检查。
- 原子切换 current version。
- 延迟删除旧节点和页面。
- 失败时继续使用旧版本。

### 49. 如果文档达到百万级，当前方案会遇到什么问题？

- 元数据和文档路由上下文过大。
- PostgreSQL 页面查询压力增加。
- 多次 LLM 路由成本过高。
- 进程内队列不适合大规模。
- 需要分层召回、专用搜索索引、缓存和分库。
- 可能需要 BM25/Elasticsearch、向量索引与 PageIndex 混合方案。

---

## 十一、面试官可能要求现场画的图

建议能够脱稿画出以下四张图。

### 1. 文档上传时序图

```text
Client → Ingestion → OSS → MinerU → PostgreSQL
```

### 2. 数据模型

```text
documents
├── document_bindings
├── doc_nodes
└── doc_pages
```

### 3. 检索图

```text
分类 → 文档路由 → direct/focused/broad → merge
```

### 4. 公众号 Agent 状态图

```text
调研 → 任务书审核 → 大纲审核 → 文章审核 → 图片 → HTML
```

---

## 十二、最容易暴露“只看过 README”的追问题

- BM25 检索的是页面还是文档元数据？
- 最终引用来自节点还是页面？
- 同文件不同名字，`doc_id` 是否相同？
- 删除一个 binding 会不会删除共享文档？
- Raw MinerU Repair 会不会重建节点？
- `top_k` 是否是所有路径的硬限制？
- Knowledge Chat 为什么先查文件名？
- SSE 开始后发生异常，HTTP 状态码还能不能改？
- Checkpoint 和 Artifact 为什么同时存在？
- 用户断开 SSE 是否会取消远端文章任务？
- 当前检索 SQL 是否真正按照 user/kb 隔离？

如果这些问题都能结合源码回答，基本可以证明真正理解了项目，而不只是会罗列“FastAPI + LangGraph + PostgreSQL + RAG”。

---

## 十三、模拟面试评分建议

可以给每个问题按 0～3 分自评：

| 分数 | 表现 |
|---:|---|
| 0 | 不知道或回答明显错误 |
| 1 | 能说概念，但无法联系本项目 |
| 2 | 能结合本项目流程和数据结构回答 |
| 3 | 能说明设计取舍、故障场景和改进方案 |

重点问题为第 2、5、12、16、19、20、25、34、44、46、47 题。若这些问题可以稳定达到 2～3 分，已经具备较好的项目讲解能力。
