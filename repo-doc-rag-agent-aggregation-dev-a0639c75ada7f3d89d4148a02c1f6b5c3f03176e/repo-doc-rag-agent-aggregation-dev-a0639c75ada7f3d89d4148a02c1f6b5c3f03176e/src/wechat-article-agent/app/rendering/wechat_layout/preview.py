from __future__ import annotations


def preview_iframe_document(final_html: str) -> str:
    """Return the product artifact unchanged; viewport framing belongs to the caller."""

    return final_html
