# 仅供一次性 wechat-article-db-init 容器使用，不会注入 runtime 或 adapter。
# 账号需要具备 CREATE ROLE 和 CREATE DATABASE 权限，并连接到已存在的维护库。
DATABASE_ADMIN_URL=postgresql://<admin-user>:<admin-password>@<database-host>:5432/postgres
