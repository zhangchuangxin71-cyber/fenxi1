# 全部项目功能、技术栈与学习路线分析

> 分析目录：`/path/to/workspace/全部项目`  
> 记录日期：2026-08-25  
> 分析方式：静态阅读各项目的 README、依赖清单、API 路由、核心算法、Docker Compose、测试目录及重复代码差异；未连接外部 PostgreSQL、OSS、Ark、Milvus，也未启动完整生产服务。

## 1. 总体结论

当前目录表面上有 6 个顶层文件夹，但其中一个是包含多个服务的聚合仓库，另有一个项目在聚合仓库中存在更新版本。去除重复版本后，可以按 10 个主要业务项目理解。

它们形成两条相对独立的技术主线：

```text
多媒体生产线

长视频导入与切片
    ↓
图片/视频向量化与语义检索
    ↓
文案、配音、素材、字幕合成为新视频


文档智能生产线

OSS 文档
    ↓
MinerU / 原生解析器
    ↓
PageIndex 结构化入库 PostgreSQL
    ↓
智能检索服务
    ├── 知识库问答
    ├── 报告生成与编辑
    └── 微信公众号文章生成
```

顶层目录概览：

| 顶层目录 | 项目定位 | 建议理解方式 |
|---|---|---|
| `video-clip-main-...` | 长视频导入、分镜、切片服务 | 多媒体素材加工上游 |
| `multimodal-retrieval-master-...` | 图片/视频/文本向量化与检索 | 多媒体素材搜索中台 |
| `media-editor-master-...` | 文案、音频、素材、字幕自动成片 | 多媒体内容生产下游 |
| `pageindex-rag-master-...` | 本地多文档解析、索引与 CLI 问答 | 早期一体化 RAG 项目 |
| `rag-report-agent-master-...` | 私有知识库问答与报告生成 | 报告 Agent 的较早独立版本 |
| `repo-doc-rag-agent-aggregation-dev-...` | 六服务、七容器的 RAG 生产平台 | 当前最完整的文档智能系统 |

---

## 2. 项目之间的包含、重复与上下游关系

### 2.1 明确的代码包含关系

聚合仓库中的：

```text
repo-doc-rag-agent-aggregation.../src/rag-report-agent
```

是外部独立目录：

```text
rag-report-agent-master...
```

的更新版本。

代码差异比对显示，聚合版基本保留了独立版的主体，同时新增或完善了：

- 调用独立 `rag-retrieval-service` 的远程检索客户端。
- `remote` 与 legacy PostgreSQL 检索后端切换。
- 远程检索超时、异常映射和安全处理。
- Page-only chunk 兼容。
- 动态 `max_return_tokens`。
- 与远程检索相关的单元测试、错误测试和环境变量文档。

因此：

- 学习当前报告 Agent，应优先阅读聚合仓库内的版本。
- 外部 `rag-report-agent-master...` 可以作为较小、较容易理解的旧快照。
- 不建议同时修改两个目录，否则容易产生版本分叉。

### 2.2 功能上的演进和包含关系

`pageindex-rag-master...` 是一个早期的一体化 CLI 应用，它在同一个进程中完成文档解析、索引、检索和问答。

聚合平台将这些职责拆成了多个独立服务：

| 早期 `pageindex-rag` 职责 | 聚合平台中的对应服务 |
|---|---|
| PDF、Office、Markdown 文档解析 | `MinerU` + `repo-doc-ingestion` 的原生解析器 |
| PageIndex 树、页面、元数据入库 | `repo-doc-ingestion` |
| 文档选择、章节/页面导航、证据召回 | `rag-retrieval-service` |
| 多文档知识问答 | `rag-knowledge-chat` |
| 报告生成 | `rag-report-agent` |
| 公众号文章生产 | `wechat-article-agent` |

这是一种“功能上的演进与超集关系”，不是简单地把 `pageindex-rag` 原目录复制进聚合仓库。新平台重新设计了服务边界、API 契约、并发控制和部署方式。

### 2.3 多媒体项目之间的关系

三个多媒体项目没有直接的目录包含关系，也没有形成一套开箱即用的自动调用链，但业务上能够组成完整流程：

```text
video-clip
负责把长视频加工成可复用镜头素材
    ↓
multimodal-retrieval
负责让图片、视频和镜头可以被自然语言搜索
    ↓
media-editor
负责把文案、配音、检索到的素材和字幕合成为成片
```

容易混淆的几点：

- `media-editor` 内部也有 `clip.py`，但它只是成片过程中的素材裁切，不等于完整的 `video-clip` 服务。
- `media-editor` 会生成视觉检索关键词，但自身没有完整实现 CLIP/Milvus 素材库。
- `multimodal-retrieval` 负责向量化和搜索，不负责最终视频合成。
- `video-clip` 的 semantic detector 使用 Ark 对视频做语义分镜，不等于 Chinese-CLIP 向量检索。

---

## 3. 多媒体项目详细分析

## 3.1 Video Clip API

目录：[`video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/README.md)

### 项目目标

把远程长视频导入系统，提供预览、手动分段、自动分镜、并行切片、缩略图以及 OSS 发布能力。它更接近“视频素材预处理服务”，不是完整的时间线视频编辑器。

### 主要功能

#### 视频导入

- 支持 OSS Key。
- 支持 OSS URL。
- 支持普通 HTTP/HTTPS URL。
- HTTP 导入包含公网 URL 和 IP 检查，用于降低 SSRF 风险。
- 对 URL 导入和 OSS 操作提供重试机制。

#### 视频读取与预览

- 使用 FFprobe 获取时长、帧数、帧率和媒体信息。
- 支持视频流式预览和 HTTP Range 请求。
- 生成视频胶片条，帮助前端快速观察整个时间轴。
- 从音频 PCM 数据生成归一化波形峰值。

#### 分镜

- 手动分镜：调用方提交明确的起止时间。
- 传统自动分镜：使用 PySceneDetect 和 OpenCV 检测画面变化。
- 语义自动分镜：压缩代理视频后调用豆包 Ark，由多模态模型判断语义镜头边界。
- 对过短片段进行合并和后处理。
- 对时间点进行帧边界校正，减少切片重复帧或丢帧。

#### 切片与发布

- 使用 FFmpeg/FFprobe 裁切视频。
- 支持单任务内部多个 FFmpeg worker 并行处理。
- 对裁切结果进行时长和帧数校验。
- 可直接把 FFmpeg 输出通过 multipart 流式上传 OSS，减少完整视频落盘。
- 生成片段封面和缩略图。
- 提供 CDN URL、OSS 签名 URL 和异常回退。

#### 运维能力

- 健康检查覆盖 FFmpeg、FFprobe、OSS、调度器和内存账本。
- Docker 镜像中记录 Git SHA 和构建时间，便于确认部署代码版本。
- 定期清理临时文件、过期派生产物和孤儿对象。
- 对磁盘空间压力提供主动回收。

### 架构特点

```text
FastAPI Route
    ↓
Service 层
    ↓
同进程 InlineScheduler / ThreadPool
    ↓
FFmpeg、PySceneDetect、Ark、OSS
```

任务状态保存在进程内的 `MemoryStore` 中，因此：

- 只能安全运行一个 API 实例。
- 不适合直接水平扩容多个副本。
- 进程重启后旧 `video_id` 和 `job_id` 失效。
- 它没有 Redis、Celery 或持久化任务数据库。

### 主要技术

- Python 3、FastAPI、Uvicorn。
- Pydantic v2 数据模型。
- FFmpeg、FFprobe。
- PySceneDetect、OpenCV。
- 豆包 Ark 多模态 API。
- 阿里云 OSS SDK `oss2`。
- `ThreadPoolExecutor` 并发。
- Docker、Docker Compose。
- React 18、TypeScript、Vite 参考前端。

### 推荐阅读顺序

1. [`backend/main.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/main.py)
2. [`backend/models/schemas.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/models/schemas.py)
3. [`backend/api/v1/videos.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/api/v1/videos.py)
4. [`backend/api/v1/jobs.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/api/v1/jobs.py)
5. [`backend/services/job_service.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/services/job_service.py)
6. [`backend/core/scene_detector.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/core/scene_detector.py)
7. [`backend/core/semantic_detector.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/core/semantic_detector.py)
8. [`backend/core/cutter.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/core/cutter.py)
9. [`backend/storage/oss_client.py`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/backend/storage/oss_client.py)
10. [`frontend/reference-ui`](video-clip-main-130ec4612c08d7fe222c0bcfbf545a93c66d2457/frontend/reference-ui/README.md)

---

## 3.2 Chinese CLIP / Multimodal Retrieval

目录：[`multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/README.md)

### 项目目标

为中文图片、视频和文本提供统一向量化与检索能力。向量服务负责编码与入库，图片和视频模块负责查询已有索引。

### 模块划分

| 模块 | 职责 |
|---|---|
| `embed_core` | CLIP、BGE、视频分段、向量处理和 Milvus 公共库 |
| `embedding_service` | FastAPI 向量化 HTTP API、异步媒体任务、OSS 下载和 Milvus 写入 |
| `picture` | 图片 CLIP/BGE 双路检索与简单搜索界面 |
| `video_retrieval` | 视频 CLIP 检索以及 BGE+SQLite FTS 混合检索 |
| `models` | 本地模型配置和下载后的权重目录 |
| `runtime` | 任务数据库、日志和临时文件 |

### 向量化能力

#### Chinese-CLIP

- 文本和图片编码到同一语义空间。
- 支持“用文字查图片/视频画面”。
- 视频会抽取多帧并生成片段或整片向量。
- 使用 L2 归一化，使余弦相似度检索更稳定。

#### BGE

- 使用 `BAAI/bge-large-zh-v1.5` 编码中文描述。
- 更适合基于标题、标签、字幕、说明文字做文本语义检索。
- 查询时添加中文 retrieval instruction。

### 视频代表性计算

项目不是简单地均匀抽一帧，而是综合考虑：

- 帧之间的视觉变化，即 motion score。
- 一个片段内部的视觉多样性。
- 帧在片段时间轴上的覆盖程度。
- embedding norm。
- 一个片段是否与其他片段过于相似，即 genericness。
- 已选代表片段之间的相似度惩罚。

多样化选帧会先对归一化帧向量执行 KMeans，再选择距离每个聚类中心最近的帧。

### 混合检索

视频 Hybrid 模式由两部分组成：

```text
BGE 查询向量 → Milvus 稠密检索 ┐
                                 ├→ Reciprocal Rank Fusion → 最终结果
查询关键词 → SQLite FTS 稀疏检索 ┘
```

这样可以同时利用：

- 稠密向量的语义泛化能力。
- 关键词检索对专有名词、地点、产品名的精确匹配能力。

### 主要技术

- PyTorch CPU 版。
- Hugging Face Transformers。
- `ChineseCLIPModel`、`ChineseCLIPProcessor`。
- BGE Large Chinese。
- NumPy、OpenCV、Pillow。
- Scikit-learn KMeans。
- Milvus / PyMilvus。
- SQLite FTS、BM25、RRF。
- FastAPI、Pydantic、Uvicorn。
- 阿里云 OSS。
- Docker。

### 推荐阅读顺序

1. [`embed_core/clip/embedding.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/embed_core/clip/embedding.py)
2. [`embed_core/bge/embedder.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/embed_core/bge/embedder.py)
3. [`embed_core/clip/frame_selection.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/embed_core/clip/frame_selection.py)
4. [`embed_core/clip/video_representation.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/embed_core/clip/video_representation.py)
5. [`embed_core/milvus_store.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/embed_core/milvus_store.py)
6. [`embedding_service/api.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/embedding_service/api.py)
7. [`picture/dual_search_service.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/picture/dual_search_service.py)
8. [`video_retrieval/hybrid/hybrid_retrieval.py`](multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9/video_retrieval/hybrid/hybrid_retrieval.py)

---

## 3.3 Media Editor / 文案自动成片

目录：[`media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/README.md)

### 项目目标

把口播文案、外部 TTS 音频和图片/视频素材合成为可以发布的视频，同时处理复杂的字幕排版、音视频时长和画幅适配。

### 完整工作流

```text
原始文案
    ↓
LLM 或规则分成画面单元
    ↓
为每个画面单元生成视觉检索关键词
    ↓
外部 TTS 生成每句 WAV URL
    ↓
下载、调速、拼接音频，构造 SRT 时间轴
    ↓
调用方为各句绑定图片或视频素材
    ↓
素材裁切、循环、缩放、填充、转场
    ↓
人声和 BGM 混音
    ↓
生成 ASS/SRT 字幕并检查安全区
    ↓
硬字幕或软字幕合成 MP4
    ↓
MP4、WAV、ASS 上传 OSS
```

### 文案分镜

- 支持 LLM、规则以及自动降级模式。
- LLM 使用 OpenAI 兼容接口调用豆包 Ark。
- Prompt 要求不改写原文，只插入视觉单元标记。
- 为具象画面生成 Chinese-CLIP 风格检索词。
- 将纯情绪、感叹、抽象语句合并到相邻可视化段落。
- 对 LLM 输出进行文本保真度、长度和边界验证。
- LLM 失败时回退到标点、语义和 jieba 规则。

### 音频与时间轴

- 接收外部 TTS WAV URL，不包含真正的 TTS 模型或厂商 SDK。
- 使用 FFprobe/FFmpeg 检测音频时长。
- 支持语速调整、句间停顿和音频拼接。
- 支持人声音量和 BGM 音量控制。
- 支持音频淡入淡出和 crossfade。

### 素材处理

- 支持图片转视频。
- 支持视频按口播时长裁切或补齐。
- 支持不同分辨率和纵横比的 scale/crop/pad。
- 支持无声片段导出、素材拼接和视频转场。
- 对画布尺寸进行严格断言，防止输出分辨率异常。

### 字幕系统

字幕部分是本项目最复杂、最值得单独研究的领域：

- SRT 和 ASS 双格式。
- ASS 样式、字体扫描和 FontName 映射。
- 中文标点、引号、括号保护。
- jieba 分词和词性辅助断行。
- 字幕每秒字符数 CPS 控制。
- 单行/双行像素宽度约束。
- 安全区、字号、描边、边距和位置计算。
- 渐进式字幕显示。
- 可选 Whisper ASR 对齐。
- 预览和正式烧录尽量使用同一套排版逻辑。

### API 阶段

- `POST /api/v1/split/run`：提交分句任务。
- `POST /api/v1/audio/process/run`：处理外部 TTS 音频。
- `GET /api/v1/subtitle/fonts`：查询可用字体。
- `POST /api/v1/visual/preview/run`：生成字幕/素材预览。
- `POST /api/v1/compose/run`：执行最终成片。
- 阶段任务查询和取消接口。
- 维护、工作区清理和协调状态接口。

### 主要技术

- Python `src/` package 布局、Setuptools。
- FastAPI、Pydantic、Uvicorn。
- FFmpeg、FFprobe、libass。
- ASS、SRT。
- jieba 中文分词和词性标注。
- FontTools 和字体元数据读取。
- OpenAI 兼容 Ark API。
- 可选 OpenAI Whisper。
- `ThreadPoolExecutor`。
- 阿里云 OSS。
- Docker、Docker Compose。

### 推荐阅读顺序

1. [`src/videoaudiotext/api/app.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/api/app.py)
2. [`src/videoaudiotext/api/schemas.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/api/schemas.py)
3. [`src/videoaudiotext/text/llm_split.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/text/llm_split.py)
4. [`src/videoaudiotext/retrieval/queries.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/retrieval/queries.py)
5. [`src/videoaudiotext/audio/timeline.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/audio/timeline.py)
6. [`src/videoaudiotext/media/clip.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/media/clip.py)
7. [`src/videoaudiotext/subtitle/display.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/subtitle/display.py)
8. [`src/videoaudiotext/subtitle/export.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/subtitle/export.py)
9. [`src/videoaudiotext/core/compose.py`](media-editor-master-a89f9578b6263d471456be1a1b28ab74324044a6/src/videoaudiotext/core/compose.py)

---

## 4. 文档 RAG 项目详细分析

## 4.1 PageIndex RAG / repo-agent-qa

目录：[`pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b`](pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b/README.md)

### 项目目标

在本地索引多个文档，使用 PostgreSQL 持久化 PageIndex，然后通过 CLI 进行限定文档或跨文档问答。

### 文档数据结构

它不是简单地把文档切成固定长度向量 chunk，而是保存：

- `documents`：文档元信息和描述。
- `doc_nodes`：章节树节点、父子关系、标题、摘要和页码范围。
- `doc_pages`：页面编号与页面原文。

这种结构能够支持“查看目录”“查某章节”“读取指定页”“跨文档比较”等操作。

### 检索和问答流程

```text
用户问题
    ↓
结合历史改写问题
    ↓
BM25 预筛候选文档
    ↓
LLM 文档路由
    ↓
通过文档树定位章节和页码
    ↓
读取页面原文形成证据
    ↓
LLM 生成带引用答案
    ↓
证据校验、引用规范化、记忆更新
```

项目同时保留两类实现：

- 固定节点式 LangGraph pipeline。
- OpenAI Agents SDK + `function_tool` 的工具调用式 Agent。

Agent 可使用的核心工具包括：

- 列出文档。
- 获取文档元数据。
- 获取文档结构。
- 获取指定页面内容。

### 主要技术

- Python 3.11。
- PostgreSQL、Psycopg。
- PageIndex 树结构。
- LangGraph。
- OpenAI Agents SDK。
- LiteLLM、OpenAI SDK、豆包 Ark。
- BM25、jieba。
- PDFMiner、PDFPlumber、PyMuPDF、PyPDF2。
- python-docx、python-pptx、openpyxl。
- CLI、Typer/Rich 相关工具。
- Docker。

### 推荐阅读顺序

1. [`app/document_assistant_agent.py`](pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b/app/document_assistant_agent.py)
2. [`app/pageindex/postgres_store.py`](pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b/app/pageindex/postgres_store.py)
3. [`app/core/document_tools.py`](pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b/app/core/document_tools.py)
4. [`app/qa/pipeline_nodes.py`](pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b/app/qa/pipeline_nodes.py)
5. [`app/qa/langgraph_adapter.py`](pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b/app/qa/langgraph_adapter.py)
6. [`app/qa/agent_mode.py`](pageindex-rag-master-fddbdbb4cd990f0340cf3b08767fe3d189cf828b/app/qa/agent_mode.py)

### 学习价值与局限

学习价值：

- 服务数量少，容易看到从问题到答案的全流程。
- 能比较固定工作流和工具调用 Agent 的差异。
- 能学习非向量式、树导航式 RAG。

局限：

- 主要是 CLI，不是完整生产 API 平台。
- 解析、检索、问答职责耦合在一个项目中。
- 生产架构已经在聚合平台中进一步拆分和重构。

---

## 4.2 独立版 RAG Report Agent

目录：[`rag-report-agent-master-d60e2231b31cb4052e4656017e0a62cba8269174`](rag-report-agent-master-d60e2231b31cb4052e4656017e0a62cba8269174/README.md)

### 项目目标

基于已有 PageIndex/PostgreSQL 数据完成知识库问答、报告大纲、报告写作和报告编辑，并以 SSE 形式把不同类型的增量输出发送给前端。

### Agent 能力

- 意图识别。
- 一个请求中的多意图拆分。
- 问题改写和复合问题拆分。
- 文档路由。
- 知识库证据召回。
- 普通知识问答。
- 大纲生成和修改。
- 用户确认大纲后生成报告。
- 已有报告局部编辑。
- 图片媒资检索和 Markdown 图片插入。
- 引用和 chunk ID 返回。

### LangGraph 节点

主要节点可以从 [`app/agent/nodes`](rag-report-agent-master-d60e2231b31cb4052e4656017e0a62cba8269174/app/agent/nodes) 阅读：

- `intent.py`：意图识别。
- `query_rewrite.py`：问题改写。
- `doc_router.py`：文档路由。
- `retrieve.py`：检索。
- `qa_generate.py`：问答生成。
- `outline_generate.py`：大纲生成。
- `outline_modify.py`：大纲修改。
- `report_write.py`：报告正文。
- `report_edit.py`：报告编辑。
- `report_images.py`：报告配图。
- `task_dispatch.py`：多意图任务分发。

### SSE 输出

服务通过不同事件区分：

- 流开始和流程阶段。
- 可读思考过程。
- 普通对话文本。
- 大纲增量。
- 报告正文增量。
- 引用和错误。

这适合前端把普通聊天、大纲编辑区和报告正文区分别渲染。

### 版本判断

独立版适合快速阅读主体结构，但聚合仓库内版本更新，生产学习应以聚合版为准。

---

## 5. RAG 聚合平台详细分析

目录：[`repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e`](repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/README.md)

以下简称 `rag-platform`。

### 5.1 平台架构

```text
                         ┌──────────────────────┐
OSS 文档 ──→ MinerU ──→ │ repo-doc-ingestion   │
                         └──────────┬───────────┘
                                    ↓
                           外部 PostgreSQL
                                    ↑
                         ┌──────────┴───────────┐
                         │ rag-retrieval-service│
                         └──────┬──────┬────────┘
                                │      │
                 ┌──────────────┘      └─────────────────┐
                 ↓                                       ↓
       rag-knowledge-chat                       rag-report-agent
                 │
                 └────────────→ wechat-article-agent
                                  ├── public adapter
                                  └── private LangGraph runtime
```

PostgreSQL 不由顶层 Compose 创建，而是外部依赖。平台通过 Docker 网络连接已有数据库。

### 5.2 MinerU

目录：[`src/MinerU`](repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/src/MinerU/README.md)

功能：

- PDF、图片、DOC/DOCX、PPT/PPTX、XLS/XLSX、CSV、Markdown 和 TXT 解析。
- 文档版面和阅读顺序识别。
- 页眉、页脚和页码处理。
- OCR，多语言文字识别。
- 表格转 HTML。
- 公式转 LaTeX。
- 图片、标题、段落和列表结构抽取。
- 输出 Markdown、JSON 和中间结构。
- 支持 CPU、GPU、MPS，不同 VLM/pipeline 后端。
- 提供 CLI、FastAPI、Gradio 和路由服务。

主要技术：PyTorch、Transformers、OpenCV、OCR、VLM、PDFium、FastAPI、Gradio、ModelScope、Hugging Face。

这是一个大型上游开源项目。学习本业务平台时应先关注其 API 和输出格式，不必一开始深入模型推理内部。

### 5.3 Repo Doc Ingestion

目录：[`src/repo-doc-ingestion`](repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/src/repo-doc-ingestion/README.md)

功能：

- API 接收 OSS `oss_key`。
- 下载文件到任务临时目录。
- 普通文档或临时文档异步入库。
- 使用 MinerU 或原生解析器。
- 将 MinerU 结果转换成统一 PageIndex 结构。
- 写入文档、绑定关系、章节节点、页面和任务表。
- 查询任务和文档状态。
- 删除文档。
- 任务完成后清理下载和临时文件。
- MinerU 失败时按配置回退到原生解析器。
- 对历史文档缺失的 MinerU raw 结果执行异步修复。

支持格式包括 PDF、DOC、DOCX、Markdown、TXT、HTML、XLSX、PPTX。

主要表：

- `documents`
- `document_bindings`
- `doc_nodes`
- `doc_pages`
- `ingestion_tasks`
- `ingestion_task_events`

主要技术：FastAPI、后台 worker、OSS、PostgreSQL、Psycopg、PyMuPDF、PyPDF2、python-docx、openpyxl、python-pptx、LibreOffice、MinerU HTTP API、豆包 Ark Vision。

重点阅读：

1. `app/api/app.py`
2. `app/ingestion/agent_adapter.py`
3. `app/parser/mineru_adapter.py`
4. `app/parser/multiformat_parser.py`
5. `app/storage/postgres_store.py`

### 5.4 RAG Retrieval Service

目录：[`src/rag-retrieval-service`](repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/src/rag-retrieval-service/README.md)

### 定位

独立、无状态、page-first 的 Agentic Retrieval 服务。它只返回证据，不直接回答用户问题。

这里也不是传统的 Embedding 向量检索系统。主要依靠：

- 调用方提供的 `doc_ids` 和 `temp_doc_ids` 限定文档范围。
- 文档名和 description 关键词预筛。
- LLM query 分类和文档路由。
- PageIndex node tree 导航。
- PostgreSQL 页面原文读取。
- token 预算和代码规则合并。

### 四类检索组

| 分类 | 用途 |
|---|---|
| `routed_focused` | 精确问题；路由到目标文档，扫描节点树，返回命中页面 |
| `routed_broad` | 总结/对比等宽问题；在预算内覆盖较多正文 |
| `routed_direct` | 指定页、章节、目录或文档摘要 |
| `scope_direct` | 直接回答当前文档范围、数量、元信息等问题 |

### 工程能力

- Fast/robust 两种 query 分类策略。
- 复合问题分组。
- 文档关键词硬预筛和二级预筛。
- LPT 分组，让不同长度的 LLM 批次更均衡。
- 全局 LLM 并发池和公平请求队列。
- 用户级 RPM 限流。
- timeout、重试和轻量熔断。
- LLM 失败时规则降级。
- 部分批次失败时保留已有结果。
- `top_k` 软目标和 `max_return_tokens` 硬预算。
- 进程内 trace，不依赖 LangSmith。
- MinerU raw 读取和缺失结果修复触发。

主要技术：FastAPI、LangGraph、OpenAI 兼容 Ark、Psycopg、PostgreSQL、Tiktoken、Pydantic Settings、HTTPX、异步并发、熔断和限流。

重点阅读：

1. `app/api/app.py`
2. `app/graph/builder.py`
3. `app/graph/state.py`
4. `app/classification/`
5. `app/tools/node_tree.py`
6. `app/tools/direct.py`
7. `app/db/repositories.py`
8. `app/llm/gateway.py`
9. `app/observability/trace.py`

### 5.5 RAG Knowledge Chat

目录：[`src/rag-knowledge-chat`](repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/src/rag-knowledge-chat/README.md)

### 定位

对外提供 OpenAI Chat Completions 兼容接口的无状态知识库问答服务。

### 请求流程

```text
messages + rag scope
    ↓
第一次 Ark 调用：严格 JSON Schema 路由
    ├── 不需要检索 → 通识回答
    ├── 指代不明确 → 请求澄清
    └── 需要检索 → 生成已拆分、已消解指代的 query 数组
                           ↓
                 rag-retrieval-service
                           ↓
第二次 Ark 调用：基于编号证据流式生成答案
                           ↓
OpenAI-compatible SSE + rag 扩展信息
```

核心特点：

- 不保存服务端聊天历史。
- 调用者必须在每次请求的 `messages` 中携带历史。
- `session_id` 只用于临时文档归属，不表示会话记忆。
- 固定由代码注入 `user_id/kb_id/doc_ids/temp_doc_ids`，模型不能扩大检索范围。
- 回答正文使用标准 `choices[0].delta.content`。
- 引用、chunks、检索状态和 debug 放在自定义 `rag` 字段中。
- 支持 reasoning 增量。
- 可选入站 API Key。

主要技术：FastAPI、HTTPX、OpenAI SDK、Pydantic Settings、SSE-Starlette、严格 JSON Schema、Docker、uv、Ruff、pytest。

### 5.6 聚合版 RAG Report Agent

目录：[`src/rag-report-agent`](repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/src/rag-report-agent/README.md)

它在独立版报告 Agent 基础上，把生产检索来源切换为 `rag-retrieval-service`：

- Agent 仍负责意图识别、query rewrite、文档路由和最终生成。
- 检索服务负责返回 page 原文和元信息 chunk。
- 内置 PostgreSQL retriever 只作为 legacy 兼容。
- 支持临时文档 `session_id` 透传。
- 支持动态 token 预算。
- 保留问答、大纲、报告、编辑、引用、配图和 SSE 能力。

重点阅读：

1. `app/agent/graph.py`
2. `app/agent/state.py`
3. `app/agent/nodes/intent.py`
4. `app/agent/nodes/task_dispatch.py`
5. `app/agent/nodes/retrieve.py`
6. `app/rag/remote_retriever.py`
7. `app/agent/nodes/report_write.py`
8. `app/api/sse.py`

### 5.7 WeChat Article Agent

目录：[`src/wechat-article-agent`](repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/repo-doc-rag-agent-aggregation-dev-a0639c75ada7f3d89d4148a02c1f6b5c3f03176e/src/wechat-article-agent/README.md)

### 项目目标

生成微信公众号文章，不只返回一段文本，而是管理完整、可暂停、可确认、可修改、可恢复的内容生产工作流。

### 工作流

```text
用户目标和已有材料
    ↓
文档研读 + 可选 Ark 联网素材搜索
    ↓
素材归一化、来源展示和事实冲突检测
    ↓
必要时 HITL 人工确认冲突
    ↓
生成任务书 → HITL
    ↓
生成大纲 → HITL
    ↓
生成 Markdown 正文 → HITL
    ↓
Seedream AI 配图
    ↓
HTML 排版和安全清洗
    ↓
保存版本化 article artifact
```

### 高级能力

- LangGraph checkpoint 持久化到 PostgreSQL。
- Human-in-the-loop 审批和恢复。
- 文档澄清、素材冲突确认。
- 任务书、大纲、正文三阶段确认。
- 完成后的局部修改。
- 每次修改产生 artifact revision。
- 正式取消接口。
- 失败恢复和 TTL 清理。
- Responses-like SSE 协议。
- 节点、工具、reasoning、产物和审批卡片事件。
- Markdown 转 HTML。
- HTML/CSS 安全清洗。
- 桌面和移动端渲染验收。

### 双容器架构

```text
调用方
  ↓ 8140
公开 FastAPI Adapter
  ↓ 容器内网络
私有 LangGraph Agent Server 8141
  ↓
PostgreSQL Checkpoint + Artifact
```

公开 adapter 负责产品协议、SSE、取消和结果适配；私有 runtime 负责实际 LangGraph run。私有端口不映射到宿主机。

### 主要技术

- Python 3.12。
- LangGraph 1.2。
- LangGraph SDK、LangGraph CLI。
- `langgraph-checkpoint-postgres`。
- FastAPI、SSE-Starlette。
- PostgreSQL、Psycopg Pool。
- Ark Responses、Seedream、远程 RAG Retrieval。
- Jinja2、Markdown-It-Py。
- BeautifulSoup、Bleach、TinyCSS2。
- Playwright 渲染测试。
- mypy strict、Ruff、pytest。

这是整个目录中 Agent 状态管理、HITL、持久化和前端协议最复杂的项目，建议最后学习。

---

## 6. 技术栈横向总结

### Web API

- FastAPI：所有主要后端服务的 API 框架。
- Uvicorn：ASGI 服务运行器。
- Pydantic v2：请求、响应、配置和状态模型。
- HTTPX：异步内部服务和外部 API 请求。
- SSE-Starlette：流式 Agent 输出。
- OpenAPI/Swagger：接口契约和联调。

### AI 和 Agent

- 豆包 Ark：主要 LLM 和多模态服务。
- OpenAI SDK/兼容接口：统一调用 Ark 等兼容服务。
- LangGraph：状态图、节点编排、checkpoint、HITL。
- OpenAI Agents SDK：早期 PageIndex 工具调用 Agent。
- LiteLLM：部分旧项目的模型适配。
- Seedream：公众号文章 AI 配图。

### 检索

项目中存在两种完全不同的检索体系：

| 检索体系 | 项目 | 方法 |
|---|---|---|
| 多媒体向量检索 | `multimodal-retrieval` | Chinese-CLIP/BGE + Milvus + SQLite FTS + RRF |
| 文档 Agentic Retrieval | PageIndex/RAG 平台 | PostgreSQL 页面原文 + 文档树 + BM25/关键词 + LLM 路由 |

不要把聚合平台的 RAG 检索误认为 Milvus/Embedding 检索；当前主链路是 page-first、树导航和 LLM 路由。

### 数据与存储

- PostgreSQL：PageIndex 文档、节点、页面、任务、checkpoint 和 artifact。
- Milvus：图片和视频的稠密向量。
- SQLite FTS：视频文字描述的稀疏检索。
- 阿里云 OSS：源视频、视频切片、文档、音频、成片和字幕。
- 本地文件系统/Docker Volume：临时工作区、模型缓存、日志和短期 runtime 数据。

### 多媒体

- FFmpeg/FFprobe：裁切、转码、拼接、混音、转场、字幕烧录和媒体探测。
- PySceneDetect/OpenCV：传统视频分镜。
- ASS/SRT/libass：字幕生产和渲染。
- Whisper：可选字幕 ASR 对齐。
- Chinese-CLIP：图文跨模态向量。
- BGE：中文文本稠密向量。
- KMeans：代表帧选择。

### 文档解析

- MinerU：复杂版面、OCR、表格、公式、图片和阅读顺序。
- PyMuPDF、PyPDF2、PDFMiner/PDFPlumber。
- python-docx、python-pptx、openpyxl。
- LibreOffice：Office 格式转换和渲染支持。

### 工程与运维

- Docker、Docker Compose。
- `uv` 和 pip 两类 Python 依赖管理方式。
- pytest、pytest-asyncio、Respx。
- Ruff、Black、mypy。
- 健康检查、就绪检查、日志轮转。
- 限流、并发池、超时、重试、熔断和规则降级。

---

## 7. 推荐学习路线

## 7.1 第一阶段：共同基础

先掌握：

1. Python 模块、包、虚拟环境和类型注解。
2. `async def`、协程、线程池与阻塞函数的区别。
3. FastAPI 路由、依赖注入、异常处理和生命周期。
4. Pydantic 模型和 Settings。
5. HTTP、REST、JSON、OpenAPI 和 SSE。
6. PostgreSQL 基本表设计、事务、索引和连接池。
7. Dockerfile、Compose、volume、network 和环境变量。
8. pytest、mock 和 HTTP 客户端测试。

建议先写一个小项目：FastAPI 接收任务、后台处理、查询状态、把结果存到 PostgreSQL。

## 7.2 多媒体方向路线

### 阶段 A：Video Clip

目标：理解一次视频任务如何从 HTTP API 进入 FFmpeg。

练习：

- 用 FFprobe 获取视频元数据。
- 用 FFmpeg 按时间裁切一个片段。
- 用 FastAPI 返回支持 Range 的视频。
- 用 OpenCV 比较相邻帧差异。
- 把任务状态从内存字典升级到 SQLite/PostgreSQL。

### 阶段 B：Multimodal Retrieval

目标：理解向量的生成、归一化、存储和召回。

练习：

- 编码一组图片和文字，计算余弦相似度。
- 对视频均匀抽帧并做 KMeans 代表帧选择。
- 建立一个小型 Milvus collection。
- 比较 CLIP 搜索和 BGE 描述搜索结果。
- 实现稠密结果与 BM25 结果的 RRF 融合。

### 阶段 C：Media Editor

目标：理解媒体时间轴和生产级字幕系统。

练习：

- 将多段 WAV 拼接成主音轨。
- 根据音频时长生成 SRT。
- 把图片转换成指定时长的视频。
- 将不同画幅的视频适配到 1080×1920。
- 生成 ASS 并烧录中文字幕。
- 混合人声和 BGM。

### 综合练习

```text
上传一个长视频
→ 自动分镜和切片
→ 切片向量化写入 Milvus
→ 输入一段口播文案
→ 为每句检索相似镜头
→ 外部 TTS 生成音频
→ 自动合成为带字幕短视频
```

## 7.3 RAG/Agent 方向路线

### 阶段 A：PageIndex RAG

目标：先理解一体化 RAG 的完整数据流。

练习：

- 把 Markdown 解析成章节树。
- 将章节和页面写入 PostgreSQL。
- 实现 BM25 文档预筛。
- 实现 `list_documents`、`get_structure`、`get_page` 三个工具。
- 让 LLM 基于工具返回内容回答并给出引用。

### 阶段 B：Ingestion

目标：理解文档解析和入库为何需要独立服务。

练习：

- OSS 下载到临时目录。
- 建立异步入库任务表。
- 接入一个文档解析 API。
- 将不同格式统一成 document/node/page。
- 保证失败时清理临时文件并记录任务事件。

### 阶段 C：Retrieval Service

目标：理解复杂问题如何路由到不同检索策略。

练习：

- 将问题分成 focused、broad、direct、scope。
- 根据文档名和 description 做关键词预筛。
- 使用章节树定位可能相关页面。
- 设置 token 硬预算。
- 为 LLM 失败增加规则 fallback。
- 记录不含敏感正文的 trace。

### 阶段 D：Knowledge Chat

目标：理解无状态、OpenAI-compatible 的 RAG API。

练习：

- 从完整 messages 消解“这份文档”“另外两篇”等指代。
- 用严格 JSON Schema 生成路由结果。
- 通过 SSE 输出答案。
- 将引用放入扩展字段。
- 区分“不需要检索”“检索无结果”“检索异常”。

### 阶段 E：Report Agent

目标：学习多意图和长内容工作流。

练习：

- 意图识别后并行或顺序分发多个任务。
- 先生成大纲，再等待用户确认。
- 根据大纲逐章节生成报告。
- 对已有报告执行局部修改。
- 把大纲、正文和普通对话拆成不同 SSE 事件。

### 阶段 F：WeChat Article Agent

目标：学习状态持久化和 Human-in-the-loop。

练习：

- LangGraph checkpoint 存入 PostgreSQL。
- 在图中 interrupt，等待用户确认后继续。
- 保存版本化 artifact。
- 实现取消、失败恢复和 TTL 清理。
- 把 Markdown 安全转换为 HTML。
- 用 Playwright 验证桌面和移动端渲染。

---

## 8. 推荐的源码阅读方法

不要从第一行开始顺序阅读整个仓库。每个服务都按以下顺序阅读：

1. README：确定服务边界和外部依赖。
2. `.env.example`：理解真实运行所需资源。
3. `requirements.txt` 或 `pyproject.toml`：识别技术栈。
4. API schemas：理解输入输出契约。
5. API routes：找出请求入口。
6. service/graph：理解业务编排。
7. core/parser/retrieval：深入核心算法。
8. storage/client：理解数据库和外部服务。
9. tests：反向理解边界条件和错误处理。
10. Dockerfile/Compose：理解部署和服务连接方式。

建议为每个项目画四张小图：

- 请求时序图。
- 数据流图。
- 状态变化图。
- 外部依赖图。

---

## 9. 学习时需要特别注意的问题

### 外部依赖较多

完整运行需要不同组合的：

- 豆包 Ark API Key。
- 阿里云 OSS Bucket 和凭证。
- PostgreSQL。
- Milvus。
- MinerU 模型。
- FFmpeg/FFprobe。
- LibreOffice 和中文字体。

因此不应一开始直接启动整个聚合平台。

### 注意凭证安全

- 从 `.env.example` 复制到本地 `.env`。
- 不要提交真实 API Key、OSS Secret 和数据库密码。
- 日志中不要打印签名 URL、Authorization header、完整 prompt 或敏感文档正文。

### 注意服务安全边界

- 多个服务当前没有内置 API Key 鉴权。
- 检索服务按调用方传入的 doc ID 读取内容，可信上游必须先做权限验证。
- 这些端口应该放在内网、网关、防火墙或 VPN 后面，不能直接暴露公网。

### 注意状态语义

- `video-clip` 的任务状态在内存中，重启即丢失。
- `rag-knowledge-chat` 是无状态的，聊天历史来自请求 `messages`。
- `rag-report-agent` 的历史和报告状态主要由请求携带。
- `wechat-article-agent` 才使用 PostgreSQL checkpoint 保存图状态。
- RAG 中的 `session_id` 常用于临时文档归属，不一定代表服务端聊天记忆。

### 注意技术体系不要混淆

- Multimodal Retrieval 是 Embedding + Milvus 的向量检索。
- RAG Platform 是 PageIndex + PostgreSQL + 树导航 + LLM 路由。
- Media Editor 接收外部 TTS 音频，不提供 TTS 模型。
- MinerU 负责解析，不负责问答。
- Retrieval Service 负责证据，不直接生成最终答案。

### 注意项目复杂度

建议复杂度从低到高大致排列为：

```text
video-clip API 基础流程
    ↓
multimodal embedding 基础
    ↓
pageindex-rag 一体化问答
    ↓
repo-doc-ingestion
    ↓
rag-knowledge-chat
    ↓
rag-retrieval-service
    ↓
rag-report-agent
    ↓
media-editor 字幕与时间轴
    ↓
wechat-article-agent 状态化 HITL
```

这个顺序不是按代码质量排名，而是按理解所需的领域知识和外部依赖数量排列。

---

## 10. 最终学习建议

如果目标是快速具备项目维护能力，推荐先选一条主线，不要同时学习全部项目。

### 偏后端与音视频

```text
FastAPI
→ FFmpeg/FFprobe
→ video-clip
→ CLIP/BGE/Milvus
→ multimodal-retrieval
→ media-editor
```

### 偏大模型与知识库

```text
FastAPI + PostgreSQL
→ PageIndex 数据结构
→ pageindex-rag
→ repo-doc-ingestion
→ rag-retrieval-service
→ rag-knowledge-chat
→ rag-report-agent
→ wechat-article-agent
```

### 偏部署运维

```text
Dockerfile
→ Docker Compose
→ volume/network/healthcheck
→ 单服务部署
→ 外部 PostgreSQL/OSS/Ark 联调
→ rag-platform 七容器统一部署
→ 日志、限流、超时、重试、熔断和容量测试
```

最值得沉淀的三个综合能力是：

1. 把 AI 模型调用包进可靠的业务 API，而不是只写一次性脚本。
2. 理解数据从上传、处理、持久化、检索到生成结果的完整生命周期。
3. 为外部模型和服务的不稳定性设计超时、重试、限流、降级、恢复和可观测性。

