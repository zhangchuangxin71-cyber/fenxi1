# 项目经历优化版

> 优化原则：每条采用“负责范围 → 核心设计 → 难点/结果”的结构；技术名词与当前源码保持一致。量化结果来自原始材料，投递前应确认测试口径、硬件环境和原始记录可以复述。

## 一、可直接放入简历的版本

### ClipTalk｜对话式 AI 视频剪辑 Agent（独立完成）

开源地址：https://github.com/example-org/ClipTalk

- 从 0 到 1 完成 Web 前端、FastAPI 服务与 FFmpeg/Python 媒体处理链路，建立素材、分析任务、候选片段、事件组和成片产物的生命周期管理；设计可编辑事件时间线，支持片段新增、边界调整、删除、重排、跨组移动及撤销/重做，并通过持久化检查点支持异常中断后的任务恢复。
- 设计高光、人物、主题和声纹等自然语言剪辑 Agent，将视觉、语音、情绪与声音证据组织为事件级 Evidence Graph，按任务和证据完备度路由 VLM/LLM；采用“需求确认—AI 剪辑提案—用户审核—应用时间线”的 Human-in-the-Loop 流程，打通视频导入、内容分析、候选定位、边界细化、草稿审核、时间线应用和成片导出的完整链路。

### 企业知识问答与多模态语义检索平台

- 基于 MinerU、PageIndex、LangGraph 与 PostgreSQL 构建多格式文档 RAG 平台，支持 PDF、Word、PPT、HTML 等文档异步解析、内容去重、层级目录索引和页级证据存储；设计“查询分类—多文档路由—节点树导航—原始页面取证—Token 预算合并”检索链路，支持跨文档比较及原文、文件和页码溯源。在 119 条人工标注评测集上，通过检索策略、Top-K、Prompt 和基座模型迭代，将问答准确率由 42.0% 提升至 84.9%，并将固定测试环境下 50 页 PDF 的索引耗时压缩至 40 秒以内。
- 基于 Chinese-CLIP 与 BGE 构建图文和视频统一向量服务，采用视频分段抽帧、KMeans 多样化选帧、注意力池化和代表片段筛选，并通过 BGE/Milvus 稠密召回与 SQLite FTS/BM25 稀疏召回的 RRF 融合提升专有名词和语义查询效果；完成向量存储由 FAISS 向 Milvus 的迁移，视频检索 Top-1 由 14% 提升至 38%、Top-10 达 73%，模型预热后的文搜视频查询延迟控制在 200 ms 以内。

### 文案驱动的 AI 视频生成与智能分镜工具链（独立完成）

- 重构文案驱动的短视频生产链路，统一 LLM 视觉分句、素材检索词生成、外部 TTS 音频接入、主音轨/SRT 时间轴、素材绑定、ASS 字幕和 FFmpeg 成片；设计 `[V:检索词]/[NV]` 原文内联标记 Prompt，并加入文本保真校验、动态合并和规则降级，系统解决字幕错位、漏字、破词、短句闪屏、语义割裂和多主体误合并等 6 类质量问题，分句任务并发能力提升 4 倍。
- 从 0 到 1 搭建在线视频智能切片服务，支持手动打点、ContentDetector 画面检测、AdaptiveDetector 抗闪检测和 VLM 语义分镜；基于 FastAPI、FFmpeg/FFprobe 与阿里云 OSS 打通安全导入、Range 预览、胶片条/波形、帧级精确切割、并行处理、发布下载和过期资源回收，并以半开帧区间、音轨补齐及帧数/时长双校验减少相邻切片重复帧、缺帧和尾段截断问题。

---

## 二、为什么这样修改

### 1. ClipTalk

- 将“Agent 驱动的视频剪辑工作台”收紧为“对话式 AI 视频剪辑 Agent”，与开源仓库定位一致。
- 公开代码可以支撑事件组片段新增、边界调整、删除、重排、移动和 undo/redo。
- 公开代码和 README 可以支撑高光、人物、主题、声纹剪辑、Evidence Graph、剪辑提案和多阶段用户确认。
- 暂未使用“多轨时间线引擎、命令模式、版本回退”表述，因为公开仓库中缺少足够明确的实现证据。若确有未公开实现，应准备对应类图、数据结构和演示后再加回。

### 2. 文档 RAG

- 当前生产主线使用 MinerU，而不是 Docling。
- 检索最终返回的是页级原始证据，因此使用“页级证据/页码溯源”比泛化的“chunk 级”更准确。
- 用具体的检索链路替代“搭建系统”，更容易体现你真正理解 PageIndex 的差异化价值。
- “多文档交叉验证”改为源码更能支撑的“多文档路由、跨文档比较”。如果评测中确实实现事实交叉验证，可在面试中另行说明验证规则。

### 3. 多模态检索

- 补充 KMeans 多样化选帧、代表片段和 RRF 两路召回，使 Top-1 提升有技术原因可讲。
- `200 ms` 明确为模型预热后的查询延迟，不包含视频下载、抽帧和建索引。
- FAISS 迁移 Milvus 是原始材料中的经历；当前代码只直接证明 Milvus 主线，面试前应准备迁移前后的数据规模、索引参数和压测对比。

### 4. 文案自动成片

- 项目不直接提供 TTS 模型，改为“外部 TTS 音频接入”。
- 强调 SRT 主时间轴、ASS 排版和 FFmpeg 成片，更能体现媒体工程难点。
- 将“撤销重做”留给 ClipTalk；独立切片服务当前更有说服力的难点是 SSRF 安全导入、Range 预览、帧边界、音轨补齐、并发和资源回收。

---

## 三、量化数据的面试口径

### 问答准确率 42.0% → 84.9%

需要能回答：

- 119 条评测集如何构造，训练集和评测集是否混用。
- “准确率”由人工、LLM-as-Judge 还是 Exact Match 计算。
- 问题类型如何分布：单文档、多文档、页码、总结、无答案等。
- 每次迭代分别提升了多少，是否做过消融实验。

### 50 页 PDF 索引耗时 ≤ 40 秒

需要说明：

- CPU/GPU、内存、网络和模型部署环境。
- 是否包含 OSS 下载、MinerU、OCR、摘要和数据库写入。
- PDF 是否为扫描件，页内表格和图片比例。
- 指标是平均值、P95 还是最好值。

### 视频检索 Top-1 14% → 38%，Top-10 73%

需要说明：

- 查询数、视频数、候选片段数和标注方法。
- 命中单位是整条视频、segment 还是帧。
- 14% 对应哪个 baseline，38% 增加了哪些组件。
- CLIP、BGE、FTS、RRF、代表帧分别贡献多少。

### 查询延迟 ≤ 200 ms

需要说明：

- 是否排除模型冷启动和网络传输。
- 使用 CPU 还是 GPU，Milvus 数据规模多大。
- 是平均、P95 还是 P99。
- 是否包含 BGE/CLIP query encoding、Milvus 查询和 RRF 合并。

### 分句并发能力提升 4 倍

需要说明：

- 优化前后的 QPS、并发数和 P95 延迟。
- 是否通过 LLM Semaphore、chunk 并行、线程池或缓存实现。
- 是否增加限流、重试和降级以保证成功率。
- 吞吐提升后模型成本和错误率是否变化。

---

## 四、压缩版（简历空间不足时使用）

### ClipTalk｜对话式 AI 视频剪辑 Agent（独立完成）

- 从 0 到 1 打通 Web、FastAPI 与 FFmpeg 媒体链路，设计任务状态机、事件时间线及片段增删、边界调整、重排、移动和撤销/重做能力；开源地址：https://github.com/example-org/ClipTalk。
- 构建高光、人物、主题和声纹剪辑 Agent，以 Evidence Graph 聚合视觉/语音证据，并通过“需求确认—AI 提案—用户审核—应用时间线”实现可控的端到端视频剪辑。

### 企业知识问答与多模态语义检索平台

- 基于 MinerU、PageIndex、LangGraph 和 PostgreSQL 构建多格式文档 RAG，采用多文档路由、树导航和页级取证；在 119 条评测集上将准确率由 42.0% 提升至 84.9%，50 页 PDF 索引耗时压缩至 40 秒以内。
- 基于 Chinese-CLIP、BGE、Milvus 和 SQLite FTS 构建视频混合检索，以多样化选帧、注意力池化和 RRF 融合将 Top-1 由 14% 提升至 38%、Top-10 提升至 73%，预热查询延迟控制在 200 ms 内。

### 文案驱动的 AI 视频生成与智能分镜工具链（独立完成）

- 统一 LLM 视觉分句、外部 TTS 音频、SRT/ASS 字幕和 FFmpeg 成片时间轴；通过内联标记 Prompt、文本保真校验和规则降级解决 6 类分句/字幕质量问题，并发能力提升 4 倍。
- 基于 FastAPI、PySceneDetect/Ark、FFmpeg 和 OSS 搭建智能切片服务，支持手动、画面、抗闪和语义分镜，以及安全导入、Range 预览、帧级精确切割、并行发布和资源回收。
