# Chinese-CLIP / BGE 多模态检索项目面试题

> 对应项目：`multimodal-retrieval-master-dc02c04b5e2ccb439b42f23299618451a901b0a9`  
> 配套总览：[项目功能技术栈与学习路线](./项目功能技术栈与学习路线.md)  
> 定位：图片、视频和中文文本的向量化、Milvus 检索及 BGE + SQLite FTS 混合检索。

---

## 一、项目整体理解

### 1. 请用 3 分钟介绍这个项目

回答要点：

- 使用 Chinese-CLIP 将中文查询、图片和视频画面编码到同一向量空间。
- 使用 BGE 对标题、标签、字幕和说明文字进行中文语义编码。
- `embedding_service` 提供同步查询编码和异步图片/视频向量任务。
- Milvus 保存稠密向量及展示元数据。
- SQLite 保存任务状态和视频文本元数据，并通过 FTS 提供稀疏检索。
- 视频检索支持画面级 CLIP，也支持 BGE 稠密检索与 SQLite BM25 的 RRF 融合。

### 2. 项目中的主要模块分别负责什么？

| 模块 | 职责 |
|---|---|
| `embed_core` | CLIP/BGE 编码、视频分段、选帧、池化和 Milvus 公共能力 |
| `embedding_service` | FastAPI、异步任务、模型注册、OSS 下载和写向量 |
| `picture` | 图片 CLIP/BGE 双路查询和展示服务 |
| `video_retrieval` | 视频 CLIP 检索与文本 Hybrid Retrieval |
| `models` | 本地模型权重 |
| `runtime` | SQLite 任务库、日志、索引和临时文件 |

### 3. 一条视频如何变成可检索向量？

```text
视频 OSS/本地输入
→ 分成时间片段
→ 每段按 sample_fps 抽取候选帧
→ 高分辨率帧缩放
→ Chinese-CLIP 编码候选帧
→ KMeans 选择多样代表帧
→ 段内 softmax attention pooling
→ 计算片段重要性
→ 段间再次池化为整片向量
→ 可选写入 Milvus
```

### 4. 这个项目与文档知识库 RAG 有什么区别？

- 本项目主线是真正的 embedding + 向量数据库检索。
- 文档 RAG 主线是目录树路由和页级取证。
- 本项目检索对象是图片、视频画面或视频文字描述，不直接生成问答答案。
- 两者可以组合，但不能把 CLIP 检索等同于知识库 RAG。

---

## 二、Chinese-CLIP 与 BGE

### 5. Chinese-CLIP 解决什么问题？

- 文本和图片使用不同编码分支，但映射到同一个语义空间。
- 查询“夜晚城市街道”可以匹配没有人工标签、但画面语义相近的图片或视频帧。
- 适合跨模态的“文字搜画面”。

### 6. BGE 在这里解决什么问题？

- 编码标题、标签、字幕、caption 和 description。
- 更适合文本对文本的中文语义匹配。
- 查询端会添加 BGE retrieval instruction，文档端不添加。
- 对产品名等精确词仍需与稀疏检索组合。

### 7. 为什么不能用 BGE 直接编码图片像素？

BGE 是文本 embedding 模型。项目中的“图片 BGE”实际上编码图片的文字描述，而不是像 CLIP 一样直接编码图像像素。

### 8. CLIP 和 BGE 能直接使用同一个 Milvus collection 吗？

通常不能：

- 模型维度可能不同。
- 向量空间和相似度语义不同。
- 模型版本也可能不同。
- 因此项目按 scheme、kind 和 profile 生成独立 collection。

### 9. 为什么向量需要 L2 归一化？

```text
v_normalized = v / max(||v||, 1e-12)
```

- 归一化后内积等价于余弦相似度。
- 避免向量长度主导排序。
- 查询和入库使用一致归一化，分数更稳定。
- `clamp_min` 防止零向量导致除零。

### 10. 为什么编码时使用 `torch.no_grad()` 和 `model.eval()`？

- 检索只做推理，不需要计算梯度。
- 降低显存/内存消耗。
- 禁用 Dropout 等训练行为，保证结果稳定。
- CPU 环境也能明显减少计算负担。

### 11. 为什么要批量编码？

- 模型一次处理多条可以提高矩阵计算利用率。
- 降低 Python 和框架调用开销。
- batch 太大又会导致 GPU/CPU 内存不足，因此需要配置。

### 12. GPU 不可用时如何处理？

- 配置请求 CUDA，但 `torch.cuda.is_available()` 为 false 时自动回退 CPU。
- GPU 可使用 FP16；CPU 通常使用 FP32。
- 需要在健康信息中暴露实际 device，避免误以为正在使用 GPU。

### 13. 为什么要记录模型 manifest？

Manifest 应记录：

- backend、model name 和 revision。
- vector dimension。
- max length、pooling 和 normalize。
- query instruction。
- 索引构建参数。

这样查询端能发现模型与索引不兼容，避免用新模型查询旧向量。

---

## 三、视频抽帧与代表性计算

### 14. 为什么视频不能简单只取首帧？

- 首帧可能是黑场、封面或转场。
- 长视频包含多个语义场景。
- 单帧无法代表完整时间范围。
- 需要分段、抽帧、选代表帧和分层池化。

### 15. `sample_fps` 和 `max_frames` 分别控制什么？

- `sample_fps` 控制时间采样密度。
- `max_frames` 为每段候选帧设置硬上限。
- 两者共同控制信息覆盖、模型成本和临时图片数量。

### 16. 为什么抽帧后还要做 KMeans？

- 均匀抽帧仍可能得到大量相似连续画面。
- 对归一化 embedding 聚类，可以按视觉语义而不是只按时间分组。
- 每个聚类选择距离中心最近的帧，得到更具代表性的多样画面。

### 17. KMeans 选帧有什么局限？

- K 需要预先指定。
- 计算量随帧数和维度增加。
- 聚类中心代表常见画面，可能忽略极短但关键事件。
- 对时间连续性没有直接约束。
- 可以结合运动峰值、关键帧和时间覆盖改进。

### 18. 段内向量如何聚合？

项目使用 softmax attention pooling：根据帧向量特征或 norm 生成权重，再对归一化帧向量加权求和，最后归一化。

它比简单平均更能突出信息量高的代表帧，但权重规则仍需要用检索指标验证。

### 19. 如何计算 motion score？

- 一种方法是相邻 CLIP embedding 的 `1 - cosine similarity`。
- 项目还对灰度帧计算平均绝对像素差。
- 最终运动分数结合帧差和时间覆盖。
- 单纯像素差容易受闪光和镜头抖动影响。

### 20. visual diversity 如何计算？

- 归一化段内所有帧向量。
- 计算两两余弦相似度矩阵。
- 取上三角的平均 `1 - similarity`。
- 值越大表示该片段内部视觉变化越丰富。

### 21. temporal coverage 的作用是什么？

- 计算已选帧时间戳跨度占片段总时长的比例。
- 防止所有代表帧都集中在片段开头。
- 它反映时间覆盖，不直接等于真实运动程度。

### 22. 片段 importance score 由哪些因素组成？

```text
importance
= embedding_norm_weight × embedding_norm
+ motion_weight × motion
+ diversity_weight × visual_diversity
```

权重是启发式参数，应通过下游检索效果调优。

### 23. genericness 是什么？

- 计算某片段与同视频其他片段的平均相似度。
- 与所有片段都很相似的片段 genericness 高。
- 选择代表片段时对它进行惩罚，避免总是选重复背景。

### 24. 如何保证选出的多个代表片段彼此不同？

采用逐个贪心选择：

```text
最终分数
= importance
- genericness_weight × genericness
- diversity_penalty_weight × 与已选片段最大相似度
```

选出一个后，再重新评估剩余候选。

### 25. frame、segment、representative、whole-video 四个层级有什么区别？

- frame：定位最具体画面，数据量最大。
- segment：对应一个时间区间，适合跳转播放。
- representative：精选少量重要片段，降低索引规模。
- whole-video：一个视频一个向量，适合粗粒度召回，但定位能力弱。

---

## 四、Milvus 向量存储

### 26. Milvus collection 如何命名？

名称由前缀、媒体/检索 kind 和 profile 组成，并将 `-`、`/` 等字符规范化。例如不同来源 profile 使用独立 collection，避免模型或数据集混合。

### 27. Milvus 中除了 vector 还保存什么？

- string primary key。
- media/video/image ID。
- 路径、OSS key、时间范围。
- 模型名称、模态、scheme。
- caption、标签和展示元数据。
- dynamic fields 允许不同 kind 扩展字段。

### 28. 写入向量前为什么必须检查维度？

- collection 建立时 dimension 已固定。
- 写错维度会导致错误或索引不可用。
- 查询向量也必须与 collection 维度完全一致。
- 模型升级改变维度时应该新建 collection，而不是覆盖旧索引。

### 29. 为什么使用 upsert？

- 相同主键重新计算时可以覆盖旧向量。
- 方便任务重试和幂等写入。
- 但必须确保主键包含稳定的媒体身份和向量层级，否则不同对象可能误覆盖。

### 30. `COSINE` 和向量归一化是否重复？

- Milvus COSINE 本身按余弦语义计算。
- 提前归一化能让不同本地计算、池化和 Milvus 行为保持一致。
- 如果改用 inner product，归一化更是保证等价余弦的重要条件。

### 31. Strong consistency 有什么取舍？

- 写入后立即查询更容易看到新向量。
- 一致性语义简单。
- 代价可能是更高延迟和吞吐损失。
- 批量离线索引可以评估是否使用较弱一致性。

### 32. 模型升级时如何重建索引？

推荐：

```text
新模型 + 新 manifest
→ 新 collection/profile
→ 批量回填
→ 离线评测
→ 查询流量切换
→ 延迟删除旧 collection
```

不能让新查询模型直接查询旧模型向量。

---

## 五、异步向量服务

### 33. 为什么文本查询同步，而图片和视频异步？

- 一条文本编码通常延迟可控。
- 图片可能需要下载和解码。
- 视频涉及下载、分段、抽帧、多批模型推理和临时文件清理。
- 长任务应返回 job ID 后轮询。

### 34. 异步任务状态保存在哪里？

- SQLite `tasks` 表。
- 典型状态为 queued、running、succeeded、failed。
- 保存 job kind、progress、result、错误和时间。
- 使用 thread-local connection 适配线程池访问。

### 35. 服务重启时如何处理 running 任务？

- SQLite 中任务记录仍存在。
- 启动清理将旧 running 任务标为中断/失败，而不是继续假装运行。
- 临时目录按策略清理。
- 当前线程任务本身无法跨进程恢复，完整恢复需要可靠外部队列。

### 36. 为什么 TaskManager 需要 TTL 和最大任务数？

- 结果 JSON 和错误长期积累会让 SQLite 膨胀。
- 最大任务数提供容量保护。
- TTL 只清理完成或失败的旧任务，不能误删 running。
- 结果过大还需要限制序列化体积。

### 37. 为什么服务启动时预加载并 warmup 模型？

- 避免第一次用户请求承担模型加载成本。
- 提前发现权重缺失和模型不兼容。
- 触发框架初始化和首次推理开销。
- readiness 应在关键模型准备后再通过。

### 38. `return_vector` 和 `write_vector` 为什么分开？

- 有些调用者只需要将向量写入索引，不需要通过 HTTP 返回大数组。
- 有些调试或离线流程只需要返回向量，不写库。
- 两个开关分离可以减少响应体、网络和重复存储。

### 39. 线程池执行模型任务有什么风险？

- 多线程共享模型是否线程安全需要验证。
- CPU 推理线程和底层 BLAS 线程可能嵌套过度并发。
- GPU 任务并发过大容易 OOM。
- SQLite 写竞争和临时磁盘压力也需要限制。

---

## 六、混合检索

### 40. Hybrid Retrieval 的完整流程是什么？

```text
查询文本
├─BGE query instruction → BGE 向量 → Milvus dense Top-N
└─关键词规范化 → SQLite FTS BM25 Top-N
                             ↓
                    Reciprocal Rank Fusion
                             ↓
                         最终 Top-K
```

### 41. 为什么需要稠密和稀疏两路？

- 稠密检索擅长同义表达和语义泛化。
- 稀疏检索擅长人名、产品名、地点和编号等精确词。
- 两路组合降低单一检索器的盲点。

### 42. SQLite FTS 中为什么给字段不同 BM25 权重？

项目对 tags、description、caption 设置不同重要程度。标签通常更精炼，应比冗长 caption 具有更强排序影响。具体权重需要业务评测，而不是固定真理。

### 43. RRF 如何计算？

```text
score(d) = Σ 1 / (k + rank_i(d))
```

- 只依赖名次，不依赖不同检索器不可比的原始分数。
- 同时被两路命中的结果会累积分数。
- `k` 越大，头部名次差异影响越平滑。

### 44. 为什么不直接把 BM25 分数和余弦分数相加？

- 两种分数范围和分布不同。
- BM25 还可能使用“越小越优”的数据库内部表达。
- 不做校准直接相加会让一路支配结果。
- RRF 用 rank 避免复杂分数校准。

### 45. 多个 profile 的结果如何合并？

- 每个 profile 使用自己的 metadata DB 和 Milvus collection。
- 若 manifest 签名一致，可共享同一个 query embedder 和查询向量。
- 相同 video ID 的分数累加，并合并来源 profile。
- 模型签名不同则不能共享查询向量。

### 46. 为什么查询结果要返回时间段？

- 视频检索最终需要跳转到可播放位置。
- whole-video 只能返回 `0～duration`。
- frame/segment 索引应返回真实 start/end，才能让用户直接预览命中镜头。

---

## 七、评测、运维与设计改进

### 47. 如何评估图片/视频检索质量？

- Recall@K、Precision@K、mAP、MRR、NDCG。
- 文本搜画面的人工相关性。
- 关键镜头定位误差。
- 不同 query 类型分桶：场景、人物、动作、品牌、抽象描述。
- CLIP、BGE、FTS 和 Hybrid 的消融对比。

### 48. 如何评估代表帧选择？

- 代表帧对人工关键画面的覆盖率。
- 已选帧之间的平均相似度。
- 时间覆盖比例。
- 相同索引规模下的检索 Recall。
- KMeans、均匀抽帧、运动峰值方法的对照实验。

### 49. 如何降低视频入库成本？

- 先做低成本分镜或变化检测。
- 限制每段 sample FPS 和 max frames。
- 批量模型推理。
- 使用 GPU/FP16。
- 缓存媒体内容哈希和模型版本。
- 只为变化内容重建向量。
- 离线批量写 Milvus。

### 50. 百万级视频时需要哪些改造？

- 分区或多 collection 策略。
- 合理选择 Milvus ANN 索引及参数。
- 批量 upsert 与异步构建索引。
- 元数据过滤和租户隔离。
- 外部可靠任务队列。
- 模型服务独立扩容与动态 batch。
- 热门查询缓存和分层召回。

### 51. 这个项目有哪些安全问题需要关注？

- OSS/URL 下载的 SSRF 和大小限制。
- Milvus 不能直接暴露公网。
- API 需要鉴权、租户过滤和 collection 访问控制。
- 日志不能记录 OSS 签名和密钥。
- 模型文件必须来自可信来源。
- 返回原始向量可能泄露数据特征，应按业务控制。

### 52. 如何与另外两个媒体项目形成完整链路？

```text
video-clip：长视频分镜并产出镜头素材
→ multimodal-retrieval：为镜头建立 CLIP/BGE 索引并支持文本搜索
→ media-editor：根据文案检索词选择素材并自动成片
```

当前三个仓库没有形成开箱即用的一键调用链，需要统一媒体 ID、OSS 元数据和检索 API 契约。

---

## 八、容易暴露“只看过 README”的追问

- 图片 BGE 编码的是像素还是 description？
- CLIP 为什么要 L2 normalize？
- 为什么 KMeans 在归一化向量上执行？
- motion、diversity 和 temporal coverage 有什么区别？
- genericness 为什么要惩罚？
- segment 向量和 whole-video 向量如何得到？
- Milvus collection 为什么必须按模型/profile 隔离？
- 服务重启后 SQLite 中的 running job 如何处理？
- `return_vector` 与 `write_vector` 为什么分开？
- RRF 为什么比直接相加原始分数更稳健？
- 多 profile 何时可以共享 query embedding？

---

## 九、现场画图题

### 1. 视频向量化

```text
Video → Segment → Sample Frames → CLIP → KMeans
                                  ↓
                         Segment Pooling
                                  ↓
                     Importance / Diversity
                                  ↓
                          Video Pooling → Milvus
```

### 2. Hybrid Retrieval

```text
Query → BGE → Milvus Dense ┐
                            ├→ RRF → Top-K
Query → FTS → SQLite BM25 ┘
```

---

## 十、自评重点

重点题：第 3、5、7、9、13、16、18、22、23、25、28、32、35、40、43、45、47、50 题。

| 分数 | 表现 |
|---:|---|
| 0 | 只知道 CLIP 可以文字搜图片 |
| 1 | 能说出 CLIP、BGE、Milvus |
| 2 | 能解释视频选帧、分层池化、RRF 和任务状态 |
| 3 | 能设计模型版本迁移、百万级扩容和离线评测 |
